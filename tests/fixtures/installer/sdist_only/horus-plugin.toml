manifest_version = 1
name = "horus-example-sdist"
version = "0.1.0"
description = "Example sdist-only plugin (should be refused without --allow-sdist)."
author = "horus-os tests"
license = "Apache-2.0"
horus_os_compat = ">=0.5,<0.6"
capabilities = ["filesystem.read"]

[contributions]
tools = []
adapters = []
