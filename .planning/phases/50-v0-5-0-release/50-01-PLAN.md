---
phase: 50-v0-5-0-release
plan: 1
type: execute
wave: 1
depends_on: [49]
files_modified:
  - pyproject.toml                            # MODIFIED (version "0.4.0" -> "0.5.0", ONE LINE)
  - src/horus_os/__init__.py                  # MODIFIED (__version__ "0.4.0" -> "0.5.0", ONE LINE)
  - CHANGELOG.md                              # MODIFIED (promote [0.5.0] - YYYY-MM-DD to [0.5.0] - 2026-05-26; insert fresh [Unreleased] stub above)
  - .planning/phases/50-v0-5-0-release/50-01-SUMMARY.md  # NEW (last task)
autonomous: true
requirements:
  - REL-10  # Tag v0.5.0 with CHANGELOG and GitHub Release; docs/MIGRATION-v0.4-to-v0.5.md documents v5->v6 schema + two new direct deps
tags:
  - release
  - v0.5
  - rel-10
  - stop-before-tag
  - changelog-promotion

must_haves:
  truths:
    - "After the executor completes this plan, `grep '0.5.0' pyproject.toml src/horus_os/__init__.py CHANGELOG.md` shows the bump landed in all three places"
    - "`.venv/bin/python -c 'from horus_os import __version__; print(__version__)'` returns `0.5.0` after the version-bump commit lands"
    - "CHANGELOG.md carries `[0.5.0] - 2026-05-26` as the topmost dated section AND a fresh empty `[Unreleased]` stub above it; the existing v0.5 Added / Changed / Migration content is preserved byte-for-byte modulo the heading rename"
    - "The plan STOPS before `git tag -a v0.5.0` AND before `gh release create v0.5.0`; the SUMMARY documents the exact commands the maintainer runs after explicit approval"
    - "`.venv/bin/python scripts/release_gate.py --skip-build --skip-tests` reports OK for the 6 active checks (pricing-freshness, ci-two-variant-smoke, docs-drift, plugin-install-smoke-ci, reference-plugin-manifest-valid, v0-4-fixture-roundtrip); the wheel-bundle and pytest checks are SKIPped intentionally per the env-var override semantics and are the maintainer's tag-time responsibility"
  artifacts:
    - path: "pyproject.toml"
      provides: "Version 0.5.0 in [project] table"
      contains: "version = \"0.5.0\""
    - path: "src/horus_os/__init__.py"
      provides: "__version__ = '0.5.0' for runtime introspection"
      contains: "__version__ = \"0.5.0\""
    - path: "CHANGELOG.md"
      provides: "Complete [0.5.0] - 2026-05-26 section preserved verbatim from the v0.5.0 draft authored in Phases 47-49; fresh [Unreleased] stub on top for the next cycle"
      contains: "## [0.5.0] - 2026-05-26"
    - path: ".planning/phases/50-v0-5-0-release/50-01-SUMMARY.md"
      provides: "Phase 50 summary with STOP-BEFORE-TAG block (maintainer-only tag + gh release + STATE.md update commands)"
      contains: "## STOP-BEFORE-TAG"
  key_links:
    - from: "CHANGELOG.md [0.5.0]"
      to: "docs/MIGRATION-v0.4-to-v0.5.md"
      via: "explicit `See docs/MIGRATION-v0.4-to-v0.5.md for upgrade notes from v0.4.` reference (already present in the v0.5 draft from Phase 47)"
      pattern: "MIGRATION-v0.4-to-v0.5.md"
    - from: ".planning/phases/50-v0-5-0-release/50-01-SUMMARY.md"
      to: "docs/RELEASE.md"
      via: "STOP-BEFORE-TAG block reproduces docs/RELEASE.md `## Release procedure` steps 7-9 verbatim so the maintainer runs them after explicit approval"
      pattern: "git tag -a v0.5.0|gh release create v0.5.0"
---

<objective>
Ship the v0.5.0 release bookend. Bump version to 0.5.0 in `pyproject.toml` and `src/horus_os/__init__.py`. Promote the existing `[0.5.0] - YYYY-MM-DD` CHANGELOG draft section (authored across Phases 47-49) to `[0.5.0] - 2026-05-26` and insert a fresh empty `[Unreleased]` stub on top for the next cycle. Author Phase 50 SUMMARY with a STOP-BEFORE-TAG block reproducing the exact `git tag -a v0.5.0` + `git push origin v0.5.0` + `gh release create v0.5.0` + STATE.md update commands the maintainer runs after explicit approval. The executor does NOT run those commands.

This phase mirrors the v0.4 Phase 39 release pattern: the executor prepares everything (version bump, CHANGELOG promotion, SUMMARY) but the tag + GitHub Release + STATE.md milestone roll-forward are user-confirmation gates per docs/RELEASE.md `## Release procedure`.
</objective>

<context>
@.planning/phases/39-three-os-gate-release-migration-doc/39-01-PLAN.md  # v0.4 Phase 39 structural reference
@.planning/phases/39-three-os-gate-release-migration-doc/39-01-SUMMARY.md  # SUMMARY shape with STOP-BEFORE-TAG block
@.planning/ROADMAP.md  # Phase 50 success criteria
@.planning/REQUIREMENTS.md  # REL-10 contract
@docs/RELEASE.md  # 8-check release-gate procedure + tag/release commands
@CHANGELOG.md  # current [0.5.0] - YYYY-MM-DD draft to promote
</context>

<tasks>

<task id="1" type="auto">
<name>Bump version to 0.5.0 in pyproject.toml and src/horus_os/__init__.py</name>
<files>
  <file>pyproject.toml</file>
  <file>src/horus_os/__init__.py</file>
</files>

<actions>
1. Edit `pyproject.toml` line 7: `version = "0.4.0"` → `version = "0.5.0"`. ONE LINE.
2. Edit `src/horus_os/__init__.py` line 34: `__version__ = "0.4.0"` → `__version__ = "0.5.0"`. ONE LINE.
3. No other changes in either file.
</actions>

<verification>
- `grep '^version = ' pyproject.toml` returns exactly `version = "0.5.0"` (one line).
- `grep '^__version__' src/horus_os/__init__.py` returns exactly `__version__ = "0.5.0"` (one line).
- `.venv/bin/python -c "import horus_os; print(horus_os.__version__)"` prints `0.5.0`.
- `git diff --stat HEAD` shows two files changed, two lines touched (one each).
- `ruff check pyproject.toml src/horus_os/__init__.py` returns no errors.
</verification>

<done_when>
- Both literals updated from `0.4.0` to `0.5.0`.
- Runtime import returns the new version.
- Ruff clean on both files.
</done_when>

<commit>chore(release): bump version to 0.5.0</commit>
</task>

<task id="2" type="auto">
<name>Promote CHANGELOG [0.5.0] - YYYY-MM-DD to [0.5.0] - 2026-05-26 and insert fresh [Unreleased] stub</name>
<files>
  <file>CHANGELOG.md</file>
</files>

<actions>
1. Locate the existing `## [0.5.0] - YYYY-MM-DD` heading at line 7 of CHANGELOG.md (the v0.5 draft authored in Phases 47-49).
2. Rename ONLY the heading: `## [0.5.0] - YYYY-MM-DD` → `## [0.5.0] - 2026-05-26`. The body content (Added / Changed / Migration sections totaling lines 7-131) MUST stay byte-identical.
3. Insert a fresh empty `[Unreleased]` stub immediately above the `[0.5.0] - 2026-05-26` heading. The stub is exactly:
   ```
   ## [Unreleased]
   
   ```
   (heading + one blank line; no body content yet)
4. Prior `[0.4.0] - 2026-05-26`, `[0.3.0] - 2026-05-24`, `[0.2.0] - 2026-05-23`, `[0.1.0] - 2026-05-23` sections stay byte-identical.
</actions>

<verification>
- `grep -n '^## \[' CHANGELOG.md` shows in order: `[Unreleased]`, `[0.5.0] - 2026-05-26`, `[0.4.0] - 2026-05-26`, `[0.3.0] - 2026-05-24`, `[0.1.0] - 2026-05-23`, `[0.2.0] - 2026-05-23`.
- `grep -c '^## \[Unreleased\]$' CHANGELOG.md` returns exactly `1`.
- `grep -c '^## \[0.5.0\] - 2026-05-26$' CHANGELOG.md` returns exactly `1`.
- `grep -c '^## \[0.5.0\] - YYYY-MM-DD$' CHANGELOG.md` returns exactly `0` (the YYYY-MM-DD placeholder is gone).
- `grep -c 'See \`docs/MIGRATION-v0.4-to-v0.5.md\`' CHANGELOG.md` still returns `1` (the migration-doc reference inside the v0.5 body was preserved).
- The `[0.4.0] - 2026-05-26` section through `[0.2.0] - 2026-05-23` section is byte-identical to the pre-promotion state (verify via `git diff CHANGELOG.md` showing only the v0.5 heading rename + the new `[Unreleased]` stub insertion above it).
</verification>

<done_when>
- v0.5 section is dated 2026-05-26.
- A fresh empty `[Unreleased]` stub exists above it.
- All v0.5 body content is preserved.
- All prior version sections are untouched.
</done_when>

<commit>docs(release): promote Unreleased to 0.5.0 changelog</commit>
</task>

<task id="3" type="auto">
<name>Run release-gate active checks + verify version propagation</name>
<files></files>

<actions>
This is a verification-only task; no source modifications. Run the release-gate's 6 active checks (skipping the slow wheel build + pytest which the maintainer runs at tag time):

1. `HORUS_OS_RELEASE_GATE_SKIP_BUILD=1 HORUS_OS_RELEASE_GATE_SKIP_TESTS=1 .venv/bin/python scripts/release_gate.py 2>&1 | tee /tmp/phase-50-release-gate.txt`
2. Confirm 6 OK lines (pricing-freshness, ci-two-variant-smoke, docs-drift, plugin-install-smoke-ci, reference-plugin-manifest-valid, v0-4-fixture-roundtrip) and 2 SKIP lines (wheel-pricing-bundle, pytest).
3. `.venv/bin/python -m pytest -q 2>&1 | tail -3` — full suite must pass (~1011).
4. `.venv/bin/python -c "from horus_os import __version__; print(__version__)"` — prints `0.5.0`.
5. `grep -m1 'version = ' pyproject.toml` — shows `version = "0.5.0"`.
6. `grep -m1 '^## ' CHANGELOG.md` — shows `## [Unreleased]`.

This task does NOT commit anything; the gate output and verification results land in the Phase 50 SUMMARY's `## Release gate output` section as evidence.
</actions>

<verification>
- Release-gate command exits 0 (the 2 SKIPs do not count as failures per release_gate.py main()'s skip-handling).
- All 6 active checks report OK.
- Full pytest suite returns 0 with ~1011 passed (no regressions from the version bump or CHANGELOG edit).
- Runtime version introspection returns `0.5.0`.
</verification>

<done_when>
- Release-gate output captured for the SUMMARY.
- Pytest passes.
- Version introspection confirms the bump propagates through the package init.
</done_when>

<commit>(no commit — verification only; results land in the SUMMARY)</commit>
</task>

<task id="4" type="auto">
<name>Author Phase 50 SUMMARY with STOP-BEFORE-TAG block</name>
<files>
  <file>.planning/phases/50-v0-5-0-release/50-01-SUMMARY.md</file>
</files>

<actions>
1. Create `.planning/phases/50-v0-5-0-release/50-01-SUMMARY.md` mirroring the v0.4 Phase 39 SUMMARY shape:
   - Frontmatter with phase, plan, subsystem=release, tags, dependency graph (requires: Phases 40-49; provides: version bump + CHANGELOG promotion + STOP-BEFORE-TAG block), requirements-completed: [REL-10], tech-stack patterns (release-gate-before-tag, STOP-BEFORE-TAG protocol, CHANGELOG promotion pattern), key-files, decisions, metrics.
   - Title: `# Phase 50 Plan 01: v0.5.0 release Summary`.
   - One-liner: substantive single sentence describing what shipped (v0.5.0 release bookend with version bump + CHANGELOG promotion + STOP-BEFORE-TAG handoff).
   - `## Commits` table listing the 3 commits (Task 1 chore(release), Task 2 docs(release), Task 4 state).
   - `## Requirements satisfied` section explaining REL-10 status.
   - `## ROADMAP Success Criteria` checklist mapping to Phase 50's 4 success criteria.
   - `## Anti-scope held` confirming no source/test files outside the version literals + CHANGELOG + SUMMARY were touched.
   - `## STOP-BEFORE-TAG` block with the exact maintainer-only commands (release_gate full invocation, `git tag -a v0.5.0`, `git push origin v0.5.0`, `awk` to extract CHANGELOG, `gh release create v0.5.0`, STATE.md update).
   - `## Release gate output` block reproducing the captured `/tmp/phase-50-release-gate.txt` output verbatim.
   - `## Self-Check` checklist (will be filled in post-self-check by the executor's self-check step).

2. After authoring SUMMARY, run STATE.md + ROADMAP.md + REQUIREMENTS.md updates per state_updates flow:
   - `gsd-sdk query state.advance-plan` (or manual edit if SDK absent)
   - Mark Phase 50 complete in ROADMAP progress table (50. v0.5.0 release | v0.5 | 1/1 | Complete (pending maintainer tag) | 2026-05-26)
   - Mark REL-10 complete in REQUIREMENTS.md traceability table
   - Update STATE.md `status` to `milestone_complete` and `last_activity` to `2026-05-26 — Phase 50 prepared; STOP-BEFORE-TAG handed to maintainer`.

3. Commit message: `state(50): mark Phase 50 complete + v0.5 milestone shipped (STOP-BEFORE-TAG)`.
4. The final commit stages `.planning/phases/50-v0-5-0-release/50-01-SUMMARY.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` together so the maintainer reads a single coherent snapshot.
</actions>

<verification>
- SUMMARY file exists at `.planning/phases/50-v0-5-0-release/50-01-SUMMARY.md`.
- STOP-BEFORE-TAG section contains the literal commands `git tag -a v0.5.0` and `gh release create v0.5.0`.
- The release-gate output block in SUMMARY matches `/tmp/phase-50-release-gate.txt` byte-for-byte.
- STATE.md `status` field is `milestone_complete` (or equivalent; the literal STATE field schema is preserved).
- ROADMAP.md Phase 50 row reads `1/1 | Complete` for plans-complete.
- REQUIREMENTS.md REL-10 row reads `Complete`.
- `git tag -l v0.5.0` returns empty (the STOP-BEFORE-TAG hold is intact).
</verification>

<done_when>
- SUMMARY authored with STOP-BEFORE-TAG block.
- STATE.md / ROADMAP.md / REQUIREMENTS.md reflect Phase 50 complete + v0.5 shipped pending tag.
- The maintainer can read the SUMMARY and run the STOP-BEFORE-TAG block standalone without additional context.
</done_when>

<commit>state(50): mark Phase 50 complete + v0.5 milestone shipped (STOP-BEFORE-TAG)</commit>
</task>

</tasks>

<success_criteria>
1. `grep '0.5.0' pyproject.toml src/horus_os/__init__.py CHANGELOG.md` shows the version landed in all three files.
2. `.venv/bin/python scripts/release_gate.py --skip-build --skip-tests` reports 6 OK + 2 SKIP (no FAIL).
3. `.venv/bin/python -m pytest -q` passes (~1011 tests).
4. CHANGELOG.md has `[0.5.0] - 2026-05-26` dated section AND a fresh empty `[Unreleased]` stub above it.
5. `.planning/phases/50-v0-5-0-release/50-01-SUMMARY.md` exists with a `## STOP-BEFORE-TAG` section containing the maintainer-only commands.
6. `git tag -l v0.5.0` returns empty (executor did NOT tag).
7. STATE.md and ROADMAP.md and REQUIREMENTS.md reflect Phase 50 complete + REL-10 done + v0.5 milestone shipped pending maintainer tag.
</success_criteria>

<anti_scope>
DO NOT:
- Run `git tag -a v0.5.0` or any `git tag` command for this release.
- Run `git push origin v0.5.0` or push any tag to origin.
- Run `gh release create v0.5.0`.
- Modify any source file outside `pyproject.toml`, `src/horus_os/__init__.py`, `CHANGELOG.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and the new SUMMARY.
- Touch any test file (no new tests; the existing 1011 suite is the gate).
- Touch any doc file under `docs/` (the docs trio is the v0.5 substrate from Phases 47-49; Phase 50 only edits CHANGELOG).
- Refresh `pricing.json` (the release_gate `pricing-freshness` check is reported by Task 3; if it ever fails, that is a maintainer-tag-time deviation per docs/RELEASE.md, not Phase 50's job to fix).
- Touch the v0.5 body content in CHANGELOG.md beyond the heading-rename + the new `[Unreleased]` stub. The Added / Changed / Migration sections are authored material from Phases 47-49 and must round-trip byte-for-byte.
</anti_scope>
</content>
</invoke>