---
phase: 47-documentation-refresh-docs-trio
plan: 01
type: execute
wave: 1
depends_on: [46]
files_modified:
  - docs/PLUGINS.md
  - docs/PLUGIN-SECURITY.md
  - docs/MIGRATION-v0.4-to-v0.5.md
  - docs/manifest-v1.schema.json
  - scripts/build_manifest_schema.py
  - README.md
  - CHANGELOG.md
  - src/horus_os/plugins/installer.py
  - tests/docs/__init__.py
  - tests/docs/test_plugins_md_anatomy.py
  - tests/docs/test_plugin_security_threat_sentence.py
  - tests/docs/test_migration_v04_v05_schema_commands.py
  - tests/docs/test_manifest_schema_json_in_sync.py
autonomous: true
requirements:
  - REFERENCE-02
  - REL-12

must_haves:
  truths:
    - "`docs/PLUGINS.md` exists and walks the plugin author through, in order: 'What is a plugin?' (one paragraph), 'Anatomy of horus-plugin.toml' (annotated example block byte-identical to `tests/fixtures/manifests/manifest_v1_full.toml`), 'Capability catalog' (a table with one row per `Capability` enum member from `src/horus_os/plugins/capability_catalog.py` containing the dotted-key + the verbatim `DESCRIPTIONS[cap]` text), 'Lifecycle hooks' (the `start(ctx)` / `stop()` contract; `asyncio.wait_for(timeout=2.0)` bound; warning that a hung `start` becomes a load-time error), 'Testing your plugin' (using the three-tier fixtures from Phase 46: `make_synthetic_plugin` tier-1, `fake_plugin_entry_points` tier-2, `clean_venv` tier-3 gated by `--run-installer-e2e`), 'Walkthrough of the reference plugin' (enumerates the four Phase 48 scenarios as a forward-reference contract, NOT shipped code), 'Public API surface' (`horus_os.plugins.api` re-exports are the only supported import surface), and 'Distributing your plugin' (entry points, `pip install`, `~/.horus-os/plugins/` filesystem path)."
    - "`docs/PLUGINS.md` reaches the user under one section title titled 'Capability catalog' that includes a markdown table whose first row covers `filesystem.read` and whose `description` column matches `DESCRIPTIONS[Capability.FILESYSTEM_READ]` byte-for-byte; the same parity holds for `filesystem.write`, `net.outbound`, `secrets.read`. Drift is caught by `tests/docs/test_plugins_md_anatomy.py` (REFERENCE-02, Pitfall 12)."
    - "The embedded `horus-plugin.toml` example block in `docs/PLUGINS.md` is byte-identical (or section-for-section identical, modulo a docs-only header comment) to `tests/fixtures/manifests/manifest_v1_full.toml`. The test enforces this by reading both files and comparing content."
    - "`docs/PLUGIN-SECURITY.md` exists and contains the LITERAL sentence `plugins execute in the horus-os Python process` (lowercase 'p', no period inflection — the exact phrase from REL-12 acceptance criterion). The sentence lives inside a `## Threat model` section. `tests/docs/test_plugin_security_threat_sentence.py` greps for this substring and fails if absent (REL-12, Pitfall 1)."
    - "`docs/PLUGIN-SECURITY.md` ships these sections in order: '## Threat model' (with the literal sentence; lists the in-process loading reality, file/env/secret access), '## Trust contract' (default-deny grants per (plugin_name, plugin_version, capability) tuple; manifest-hash bound; revocable at any time; audit log persists every grant transition), '## What v0.5 does NOT defend against' (no OS-level sandbox; no PyPI supply-chain protection; social-engineering of manifest text — explicitly the PROJECT.md out-of-scope list), '## Recommended user practices' (install only from trusted authors, audit capabilities at install time, `horus-os plugins info <name>` for after-the-fact review, disable plugins via dashboard or CLI)."
    - "`docs/PLUGIN-SECURITY.md` is ≤400 lines (one-sitting read per ROADMAP §47 success criterion 3). The file is linked from the installer's grant prompt: `src/horus_os/plugins/installer.py::render_grant_prompt` includes a literal string fragment `docs/PLUGIN-SECURITY.md` (e.g. 'Read docs/PLUGIN-SECURITY.md before granting capabilities.') so the user reading the terminal prompt sees the doc path."
    - "`docs/MIGRATION-v0.4-to-v0.5.md` exists, mirrors the shape of `docs/MIGRATION-v0.3-to-v0.4.md` (TL;DR → What is new → Schema migration → New dependencies → Roll back → Breaking change scan → Verification command), and contains: a 'TL;DR' paragraph stating that v0.5 is purely additive (no breaking changes to v0.4 surfaces); a 'What is new' section naming the plugin system; a 'Schema migration v5 to v6' section listing the three new tables (`plugins`, `plugin_capabilities`, `plugin_status` per the Phase 46 truth) + two NULLABLE columns (`tool_invocations.plugin_name`, `llm_calls.plugin_name`) + one index, all additive; a 'New base dependencies' section explaining `pydantic>=2.7,<3` (manifest validation) and `packaging>=24.0` (PEP 440 `horus_os_compat` parsing via SpecifierSet); a 'How to roll back' section documenting `horus-os --disable-all-plugins` (the existing CLI flag wired in `src/horus_os/__main__.py:93` and `src/horus_os/cli/serve_cmd.py:23`) and the explicit no-downgrade-path statement; a 'Breaking change scan' section that says verbatim 'There are no breaking changes to v0.4 features'; a 'Verification' section with the command `sqlite3 ~/.horus-os/data.db \"PRAGMA user_version\"` and the expected output `6`."
    - "`docs/manifest-v1.schema.json` exists and is byte-identical to `json.dumps(MANIFEST_V1_SCHEMA.model_json_schema(), indent=2, sort_keys=True)` on the v0.5 runtime constant. The file is the Phase 49 release-gate target. `tests/docs/test_manifest_schema_json_in_sync.py` enforces parity. The Phase 46 Pitfall 12 test `tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py::test_docs_drift_against_committed_schema_file` is currently `pytest.skip`'d because `DOCS_SCHEMA_PATH.is_file()` returns False; once `docs/manifest-v1.schema.json` lands, the skip auto-clears (the test conditionally skips only when the file is absent) and the gate goes green WITHOUT any test-file edit."
    - "`scripts/build_manifest_schema.py` exists, is executable as a stdlib-only `python scripts/build_manifest_schema.py` invocation, reads `MANIFEST_V1_SCHEMA.model_json_schema()` and writes the result to `docs/manifest-v1.schema.json` with `indent=2, sort_keys=True` plus a trailing newline. Running the script twice in a row produces no diff (idempotent). The script's module docstring documents the regeneration command for plugin authors editing the schema."
    - "`README.md` 'What's included' or a new 'What is new in v0.5' section names the plugin system in one line: 'Plugins — third-party tools and adapters loadable from a manifest with default-deny capability grants.' The 'Documents' section gains two new bullet entries linking `docs/PLUGINS.md` and `docs/PLUGIN-SECURITY.md`. The version badge on line 6 stays at v0.3.0 (no version bump in this phase; Phase 50 promotes it)."
    - "`CHANGELOG.md` `[Unreleased]` section is replaced by a draft `[0.5.0] - YYYY-MM-DD` heading (date is intentionally placeholder `YYYY-MM-DD` until Phase 50 stamps the actual release date) with three Keep-a-Changelog 1.1.0-conformant subsections: **Added** (one bullet per shipped v0.5 capability: plugin manifest contract w/ MANIFEST_V1_SCHEMA, two-phase installer with wheel-only default + --allow-sdist, default-deny capability grants pinned to (plugin_name, plugin_version, manifest_hash), bounded `asyncio.wait_for(2.0)` lifecycle, `/plugins` dashboard tab, per-plugin observability via `plugin_name` columns on `llm_calls` + `tool_invocations`, `horus-os plugins` CLI with 9 subcommands {install, uninstall, list, info, enable, disable, update, grant, revoke}, `--disable-all-plugins` escape hatch, three-tier test fixtures, 12-pitfall regression suite, reference plugin forward-reference for Phase 48, three new docs files); **Changed** (`[project.dependencies]` now includes `pydantic>=2.7,<3` and `packaging>=24.0` for manifest validation and PEP 440 compat parsing); **Migration** (v5→v6 additive schema migration; v0.4 databases continue to read byte-identical)."
    - "`tests/docs/test_plugins_md_anatomy.py` exists and asserts: (a) `docs/PLUGINS.md` exists and is non-empty; (b) the content of `tests/fixtures/manifests/manifest_v1_full.toml` appears verbatim inside a fenced code block in `docs/PLUGINS.md` (use `Path.read_text` on both files; the fixture content must be a substring of the doc content); (c) for each `Capability` enum member in `capability_catalog.py`, the dotted-key string AND the `DESCRIPTIONS[cap]` text both appear in `docs/PLUGINS.md`. Test failure messages name the missing element so the docs author can fix without grepping."
    - "`tests/docs/test_plugin_security_threat_sentence.py` exists and asserts: (a) the file `docs/PLUGIN-SECURITY.md` exists; (b) it contains the literal substring `plugins execute in the horus-os Python process` (lowercase, exact spelling per REL-12); (c) it contains a markdown section heading matching the regex `^##\\s+Threat model\\s*$` (MULTILINE); (d) the file is ≤400 lines (`len(text.splitlines()) <= 400`); (e) `src/horus_os/plugins/installer.py` contains the substring `docs/PLUGIN-SECURITY.md` (so the grant prompt references the doc)."
    - "`tests/docs/test_migration_v04_v05_schema_commands.py` exists and asserts: (a) `docs/MIGRATION-v0.4-to-v0.5.md` exists; (b) it contains the substring `PRAGMA user_version`; (c) it contains the substring `pydantic>=2.7,<3` and `packaging>=24.0`; (d) it contains the substring `--disable-all-plugins`; (e) it contains the phrase `no breaking changes` (case-insensitive substring search); (f) `src/horus_os/storage.py:SCHEMA_VERSION == 6` so the verification command's expected output matches reality."
    - "`tests/docs/test_manifest_schema_json_in_sync.py` exists and asserts: (a) `docs/manifest-v1.schema.json` exists; (b) `json.loads(DOCS_SCHEMA_PATH.read_text())` equals `MANIFEST_V1_SCHEMA.model_json_schema()`; (c) the canonical re-serialization `json.dumps(schema, indent=2, sort_keys=True) + '\\n'` round-trips byte-stable against the committed file (the file must end with a single trailing newline). Same byte-stability discipline as Pitfall 12's serialization test."
    - "Running `pytest tests/docs/ tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py -v` passes with zero skips except the pre-existing `test_manifest_v1_schema_emits_object_shape`/serialization tests that already pass. The previously-skipped `test_docs_drift_against_committed_schema_file` now activates (file exists → skip branch not taken → assertion runs → passes). No edits to `test_pitfall_12_docs_drift.py` itself; the auto-activate behavior is structural."
    - "`grep -c '^plugins execute in the horus-os Python process' docs/PLUGIN-SECURITY.md` returns ≥1 (the literal acceptance sentence anchors a paragraph). Comment-stripping hygiene per planner gate: the file's `## Threat model` heading prose includes the sentence as PROSE, not a code fence header that grep -c would silently count from a comment block."
    - "`docs/PLUGINS.md` and `docs/PLUGIN-SECURITY.md` and `docs/MIGRATION-v0.4-to-v0.5.md` and `docs/manifest-v1.schema.json` are all newly-created files; `README.md` and `CHANGELOG.md` are EXTENDED (existing content preserved byte-identical for sections not touched). `src/horus_os/plugins/installer.py` is touched ONLY to add the `docs/PLUGIN-SECURITY.md` literal string to the grant prompt; the existing grant-decision tree (lines 426-494) is preserved byte-identical."

  artifacts:
    - path: docs/PLUGINS.md
      provides: "Plugin-author guide. Sections in order: What is a plugin? / Anatomy of horus-plugin.toml / Capability catalog / Lifecycle hooks / Testing your plugin / Walkthrough of the reference plugin / Public API surface / Distributing your plugin."
      min_lines: 250
      contains: "Anatomy of `horus-plugin.toml`"
    - path: docs/PLUGIN-SECURITY.md
      provides: "Threat model + trust contract + scope-of-defence + user practices. Linked from installer grant prompt."
      min_lines: 80
      contains: "plugins execute in the horus-os Python process"
    - path: docs/MIGRATION-v0.4-to-v0.5.md
      provides: "v0.4 → v0.5 upgrade guide. Mirrors v0.3→v0.4 shape: TL;DR + What is new + Schema migration v5→v6 + New base dependencies + How to roll back + Breaking change scan + Verification command."
      min_lines: 150
      contains: "PRAGMA user_version"
    - path: docs/manifest-v1.schema.json
      provides: "JSON Schema export of MANIFEST_V1_SCHEMA. The Phase 49 release-gate target. Byte-identical to `json.dumps(MANIFEST_V1_SCHEMA.model_json_schema(), indent=2, sort_keys=True) + '\\n'`."
      contains: "\"manifest_version\""
    - path: scripts/build_manifest_schema.py
      provides: "Idempotent regeneration script. Stdlib-only after importing horus_os.plugins.manifest.MANIFEST_V1_SCHEMA. Writes docs/manifest-v1.schema.json."
      contains: "MANIFEST_V1_SCHEMA.model_json_schema"
    - path: README.md
      provides: "EXTENDED — gains a 'What is new in v0.5' section (one paragraph + plugin one-liner) and two new 'Documents' bullets for the plugins guide + security doc. Existing v0.2/v0.3 sections, the Quickstart, the Project status, and the License section preserved byte-identical."
      contains: "default-deny capability grants"
    - path: CHANGELOG.md
      provides: "EXTENDED — `[Unreleased]` replaced by draft `[0.5.0] - YYYY-MM-DD` section with Added / Changed / Migration subsections per Keep-a-Changelog 1.1.0. Existing [0.4.0] and earlier sections preserved byte-identical."
      contains: "[0.5.0]"
    - path: src/horus_os/plugins/installer.py
      provides: "EXTENDED — `render_grant_prompt` (function at lines 410-423) gains a literal string fragment referencing `docs/PLUGIN-SECURITY.md` in the printed prompt. Existing grant-decision logic in `prompt_for_grants` (lines 426-494) preserved byte-identical."
      contains: "docs/PLUGIN-SECURITY.md"
    - path: tests/docs/__init__.py
      provides: "Empty package marker so the docs test directory is collected by pytest."
    - path: tests/docs/test_plugins_md_anatomy.py
      provides: "Asserts docs/PLUGINS.md embeds manifest_v1_full.toml verbatim and every Capability/DESCRIPTIONS entry."
      contains: "manifest_v1_full.toml"
    - path: tests/docs/test_plugin_security_threat_sentence.py
      provides: "Asserts the REL-12 literal sentence + Threat model section heading + ≤400 lines + installer linkage."
      contains: "plugins execute in the horus-os Python process"
    - path: tests/docs/test_migration_v04_v05_schema_commands.py
      provides: "Asserts the migration doc contains PRAGMA user_version, both new base deps, --disable-all-plugins, no-breaking-changes language, and SCHEMA_VERSION == 6."
      contains: "PRAGMA user_version"
    - path: tests/docs/test_manifest_schema_json_in_sync.py
      provides: "Asserts docs/manifest-v1.schema.json is byte-identical to the canonical serialization of MANIFEST_V1_SCHEMA.model_json_schema()."
      contains: "MANIFEST_V1_SCHEMA.model_json_schema"

  key_links:
    - from: "docs/PLUGINS.md (Anatomy of horus-plugin.toml section)"
      to: "tests/fixtures/manifests/manifest_v1_full.toml"
      via: "verbatim TOML code block in the doc"
      pattern: "manifest_version = 1"
    - from: "docs/PLUGINS.md (Capability catalog table)"
      to: "src/horus_os/plugins/capability_catalog.py::DESCRIPTIONS"
      via: "row per Capability enum member, each carrying DESCRIPTIONS[cap] verbatim"
      pattern: "filesystem\\.(read|write)|net\\.outbound|secrets\\.read"
    - from: "docs/PLUGIN-SECURITY.md (## Threat model section)"
      to: "src/horus_os/plugins/installer.py::render_grant_prompt"
      via: "the prompt prints a 'See docs/PLUGIN-SECURITY.md' line so the user reading the prompt clicks through to the doc"
      pattern: "docs/PLUGIN-SECURITY\\.md"
    - from: "docs/manifest-v1.schema.json"
      to: "src/horus_os/plugins/manifest.py::MANIFEST_V1_SCHEMA"
      via: "Phase 49 release-gate diff (scripts/release_gate.py); Phase 46 Pitfall 12 test test_docs_drift_against_committed_schema_file auto-activates"
      pattern: "MANIFEST_V1_SCHEMA\\.model_json_schema"
    - from: "scripts/build_manifest_schema.py"
      to: "docs/manifest-v1.schema.json"
      via: "writes the canonical JSON serialization with indent=2, sort_keys=True, trailing newline"
      pattern: "json\\.dumps.*indent=2.*sort_keys=True"
---

<objective>
Phase 47 ships the v0.5 documentation trio plus the docs-drift schema artifact. Plugin authors get `docs/PLUGINS.md` as a single-source authoring guide. Existing v0.4 users get `docs/MIGRATION-v0.4-to-v0.5.md` to understand the additive schema migration and the two new base deps. Security-conscious users (and the installer's own grant prompt) get `docs/PLUGIN-SECURITY.md` with the literal REL-12 sentence pinning the threat model. The release-gate target `docs/manifest-v1.schema.json` ships the JSON-Schema mirror of `MANIFEST_V1_SCHEMA` so Phase 49's gate can diff. Five small docs tests pin the contracts. `README.md` and `CHANGELOG.md` get v0.5 surface call-outs (final version date stamps land in Phase 50).

Purpose: Without this phase, the v0.5 milestone ships code without an author-facing contract — plugin authors would have to read source to learn the manifest shape. REL-12 explicitly requires the threat sentence in writing. REFERENCE-02 explicitly requires the docs-drift trip-wire. Pitfall 12 is unaddressed without the four files in this plan.

Output: 4 new docs files, 1 new build script, 1 docs test package, 5 new tests, 2 docstring extensions (README.md, CHANGELOG.md), 1 one-line code extension (installer.py grant prompt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/46-test-surface-three-tier-fixtures-pitfall-suite/46-01-SUMMARY.md
@docs/MIGRATION-v0.3-to-v0.4.md
@docs/OBSERVABILITY.md
@docs/OTEL.md
@README.md
@CHANGELOG.md
@src/horus_os/plugins/manifest.py
@src/horus_os/plugins/capability_catalog.py
@src/horus_os/plugins/installer.py
@src/horus_os/storage.py
@tests/fixtures/manifests/manifest_v1_full.toml
@tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py

<interfaces>
<!-- Pinned anchors the executor needs. No code exploration required. -->

From src/horus_os/plugins/manifest.py:
- `MANIFEST_V1_SCHEMA(BaseModel)` is the pydantic v2 schema (line 92). It declares the required top-level fields `manifest_version`, `name`, `version`, `description`, `author`, `license`, `horus_os_compat`; optional fields `contributions` (a `ManifestContributions` sub-model with `tools: list[ContributionEntry]` and `adapters: list[ContributionEntry]`), `capabilities: list[str]`, `homepage`, `issue_tracker`.
- The class name is literally `MANIFEST_V1_SCHEMA` (UPPER_CASE; NOT `ManifestV1Schema`). Import with `from horus_os.plugins.manifest import MANIFEST_V1_SCHEMA`.
- `MANIFEST_V1_SCHEMA.model_json_schema()` returns a dict with `type='object'`, `properties=...`, `required=[...]` shape (pydantic v2 BaseModel default).

From src/horus_os/plugins/capability_catalog.py:
- `class Capability(StrEnum)` declares members `FILESYSTEM_READ`, `FILESYSTEM_WRITE`, `NET_OUTBOUND`, `SECRETS_READ` with values `"filesystem.read"`, `"filesystem.write"`, `"net.outbound"`, `"secrets.read"`.
- `DESCRIPTIONS: Mapping[Capability, str]` maps each enum member to a plain-English one-line description (e.g. `Capability.FILESYSTEM_READ: "Read any file in your home directory, including notes, secrets, and SSH keys."`).
- Module-level `assert set(DESCRIPTIONS.keys()) == set(Capability)` already gates parity at import time.

From src/horus_os/plugins/installer.py:
- `render_grant_prompt(spec, stdout)` lives at lines ~410-423. Iterates through `enumerate(spec.capabilities)` and writes one line per capability with its description. The Phase 47 extension is a single literal string write: a "See docs/PLUGIN-SECURITY.md for the security model." line emitted before the grant decision prompt at line 421-423. The existing decision-tree function `prompt_for_grants` (lines 426-494) is NOT modified.

From src/horus_os/storage.py:
- `SCHEMA_VERSION = 6` (line 28). The verification command `sqlite3 ~/.horus-os/data.db "PRAGMA user_version"` returns this integer.

From src/horus_os/cli/serve_cmd.py and src/horus_os/__main__.py:
- `--disable-all-plugins` is wired in `__main__.py:93` as `dest="disable_all_plugins"` and consumed in `serve_cmd.py:23` as `getattr(args, "disable_all_plugins", False)`. The migration doc's "How to roll back" section references this flag verbatim.

From tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py (Phase 46):
- The test `test_docs_drift_against_committed_schema_file` (line 91) currently checks `if not DOCS_SCHEMA_PATH.is_file(): pytest.skip(...)` (lines 102-106). When Phase 47 lands `docs/manifest-v1.schema.json`, the file-existence check passes, the skip is NOT taken, the diff assertion runs. No edit to this test file is needed (and none is allowed — Phase 46 is frozen).

From CHANGELOG.md:
- The `[Unreleased]` section (lines 7-11) is empty Added/Changed under a top heading. Phase 47 replaces this `[Unreleased]` heading with a `[0.5.0] - YYYY-MM-DD` heading and fills the Added/Changed sections plus a new Migration section. Phase 50 promotes `YYYY-MM-DD` to the actual release date.

From README.md:
- Line 6 carries the v0.3.0 release badge. NOT changed in this phase. Phase 50 will bump.
- Line 64 currently lists v0.2 features; lines 80-95 list v0.3 features. Phase 47 adds a new "What is new in v0.5" section after the v0.3 section.
- Line 97 ("## Documents") lists doc files; Phase 47 adds two bullets for `docs/PLUGINS.md` and `docs/PLUGIN-SECURITY.md`.

From tests/fixtures/manifests/manifest_v1_full.toml:
- 30 lines; declares manifest_version=1, name="horus-os-test-full", version="1.2.3", description, author, license, horus_os_compat=">=0.5,<0.7,!=0.5.1", homepage, issue_tracker, capabilities=[4 names], plus two `[[contributions.tools]]` entries and two `[[contributions.adapters]]` entries. This is the source-of-truth fixture that `docs/PLUGINS.md` embeds verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the docs trio + manifest schema artifact + build script</name>
  <files>docs/PLUGINS.md, docs/PLUGIN-SECURITY.md, docs/MIGRATION-v0.4-to-v0.5.md, docs/manifest-v1.schema.json, scripts/build_manifest_schema.py, src/horus_os/plugins/installer.py</files>
  <action>
    Create the four documentation artifacts and the regeneration script. Order matters: write the build script and run it once to produce the JSON schema first (so PLUGINS.md and PLUGIN-SECURITY.md can reference it confidently); then author the three markdown docs; finally make the one-line installer extension wiring `docs/PLUGIN-SECURITY.md` into the grant prompt.

    Step A — `scripts/build_manifest_schema.py`. Stdlib-only after the local import. Pattern: `from horus_os.plugins.manifest import MANIFEST_V1_SCHEMA; import json, pathlib; out = pathlib.Path('docs/manifest-v1.schema.json'); out.parent.mkdir(parents=True, exist_ok=True); out.write_text(json.dumps(MANIFEST_V1_SCHEMA.model_json_schema(), indent=2, sort_keys=True) + '\n', encoding='utf-8'); print(f'wrote {out}')`. Module docstring documents the regeneration command (`python scripts/build_manifest_schema.py`) and that the output is the Phase 49 release-gate target. Make the script executable shebang-style (`#!/usr/bin/env python3`). Then run it once locally to produce `docs/manifest-v1.schema.json`. Both files commit together.

    Step B — `docs/PLUGINS.md`. Mirror the prose voice of `docs/OBSERVABILITY.md` (short declarative paragraphs; no marketing). Sections IN ORDER:
    - `# Plugins` (h1) + one-sentence purpose line.
    - `## What is a plugin?` — one paragraph. A plugin is a Python package that ships a `horus-plugin.toml` manifest, declares capabilities it needs, and contributes tools and/or adapters to a running horus-os instance. Discovery is via `importlib.metadata` entry points OR a local `~/.horus-os/plugins/` directory.
    - `## Anatomy of horus-plugin.toml` — annotated TOML code fence containing the EXACT content of `tests/fixtures/manifests/manifest_v1_full.toml` (read it at authoring time, paste verbatim, do not paraphrase). Add a 3-5 line prose paragraph after the fence explaining each top-level field. Per Pitfall 12 source-of-truth discipline, the fixture is the canonical example; the doc embeds it; the test `test_plugins_md_anatomy.py` enforces parity.
    - `## Capability catalog` — markdown table with columns `Capability` | `Description`. One row per `Capability` enum member from `capability_catalog.py`. The `Description` cell carries the verbatim `DESCRIPTIONS[Capability.XXX]` text. Author this by reading `capability_catalog.py` and copying the four strings.
    - `## Lifecycle hooks` — three subsections covering `start(ctx)` contract (called once at FastAPI lifespan startup; receives a `PluginContext` with `filesystem`/`secrets`/`net` shims gated by capability grants), `stop()` contract (called at lifespan shutdown), and the `asyncio.wait_for(timeout=2.0)` bound (Phase 43's ISOLATE-02 enforcement; hung `start` becomes a load-time `PluginLoadError`, not a hang). One paragraph each.
    - `## Testing your plugin` — name the three Phase 46 fixture tiers: tier 1 `make_synthetic_plugin(name, capabilities)` for in-process unit tests; tier 2 `fake_plugin_entry_points` for discovery-walk tests; tier 3 `clean_venv` for end-to-end install tests gated by the `--run-installer-e2e` pytest flag. One paragraph each.
    - `## Walkthrough of the reference plugin` — forward reference: "Phase 48 ships `examples/horus-os-example-plugin/` as a reference implementation. Until then, the four scenarios it will demonstrate are: (a) a tool requiring `filesystem.read` and using `ctx.filesystem.read(path)`; (b) a tool requiring `secrets.read` and using `ctx.secrets.read(key)`; (c) a lifecycle adapter with `start(ctx)` + `stop()` that respects the 2.0s `asyncio.wait_for` bound; (d) a single package registering BOTH tools and an adapter via its manifest." Explicitly mark this as a contract, not a tutorial, until Phase 48 lands.
    - `## Public API surface` — `horus_os.plugins.api` is the ONLY supported import surface for plugin authors. Any `from horus_os.<other_submodule>` import will be banned by the Phase 48 ruff custom rule (TEST-21). List the names exported by `horus_os.plugins.api.__all__` as documented in Phase 46's `test_pitfall_08_public_api_leak.py` (don't introspect; just enumerate `{'PluginContext', 'Capability', 'PluginSpec', 'Adapter', 'AdapterContext', 'LifecycleAdapter', 'CapabilityGuard', 'PermissionDenied', 'require_capability', 'Tool'}` as a markdown table).
    - `## Distributing your plugin` — three paragraphs. (1) Entry points: declare `[project.entry-points."horus_os.plugins"]` in your `pyproject.toml`; the value is the dotted path to your manifest-discovery hook (see Phase 42 substrate). (2) `pip install`: a user installs your plugin via `horus-os plugins install <package-spec>`, which two-phase-installs (download-only → manifest validation → capability prompt → final install) per Pitfall 4. (3) Local-directory: drop a `horus-plugin.toml` + Python module under `~/.horus-os/plugins/<plugin_name>/` and run `horus-os plugins reload`. (Even though `reload` isn't in the 9 CLI subcommands, document it as a Phase 50+ surface or use `disable`/`enable` instead. CHOICE: use the existing `enable`/`disable` shape — say "restart horus-os to pick up filesystem-installed plugins" since reload doesn't ship in v0.5.)
    - Link to `docs/PLUGIN-SECURITY.md` from the top of the file (one-line "Read this before granting capabilities" callout).

    Step C — `docs/PLUGIN-SECURITY.md`. ≤400 lines. Sections IN ORDER:
    - `# Plugin Security` (h1) + a one-paragraph framing of why this doc exists (REL-12 + Pitfall 1).
    - `## Threat model`. First paragraph MUST contain the LITERAL sentence: `plugins execute in the horus-os Python process` — write it as a complete prose sentence inside the section body, e.g. "In horus-os v0.5, plugins execute in the horus-os Python process. They share the file descriptors, environment variables, and access to `~/.horus-os/secrets.json` that horus-os itself holds." This sentence is the REL-12 acceptance criterion; the test greps for it verbatim. The section continues by enumerating the in-process consequences (file/env/secret access by default unless capability-gated; capability grants reduce but do not eliminate this attack surface).
    - `## Trust contract`. Default-deny posture: every capability is denied until explicitly granted. Grants are pinned to the `(plugin_name, plugin_version, manifest_hash)` triple — version upgrades and manifest changes re-prompt. The grant table (`plugin_capability_grants`) is the only path to allow; helper shims (`ctx.filesystem`, `ctx.secrets`, `ctx.net`) call `check_capability` before every operation and raise `PermissionDenied` otherwise. Every grant transition (issue/revoke) appends to the audit log. The audit log persists across plugin uninstall (forensics survive cleanup).
    - `## What v0.5 does NOT defend against`. Bulleted list explicitly enumerating PROJECT.md out-of-scope items: (1) OS-level sandboxing — plugins run in the same process, so a malicious plugin granted `filesystem.read` can read your secrets file. (2) PyPI supply-chain attacks — see Wiz LiteLLM TeamPCP writeup, Checkmarx command-jacking research; install only from authors you trust. (3) Social engineering of manifest text — a plugin can describe itself as "innocent log viewer" in its `description` field while requesting `filesystem.read`; the capability prompt surfaces what the plugin asks for, not what it claims. (4) Network egress filtering — `net.outbound` grants are binary (allow/deny), not per-host.
    - `## Recommended user practices`. Bullets: (a) install only plugins authored by people or organizations you trust; (b) review the capability list at install time; the prompt is plain-English by design; (c) audit installed plugins after the fact via `horus-os plugins info <name>`; (d) disable plugins quickly via the `/plugins` dashboard tab or `horus-os plugins disable <name>`; (e) revoke single capabilities via `horus-os plugins revoke <name> <capability>` without uninstalling.
    Keep the file under 400 lines (the test enforces this).

    Step D — `docs/MIGRATION-v0.4-to-v0.5.md`. Mirror the shape of `docs/MIGRATION-v0.3-to-v0.4.md` (which the executor MUST read before writing this one). Sections IN ORDER:
    - `# Migration from v0.4 to v0.5` (h1).
    - `## TL;DR` — one paragraph. v0.5 is purely additive. Every v0.4 surface (observability, the OTel adapter, the `horus-os usage` CLI, the `/observability` dashboard, the four shipped adapters, the JSON API, the CLI) continues to work byte-identical. No removals. No deprecations. What lights up: a third-party plugin system with default-deny capability grants, a `/plugins` dashboard tab, a `horus-os plugins` CLI subcommand family, per-plugin observability attribution, and three new docs files.
    - `## What is new` — same prose voice as v0.3→v0.4. Subsections for "Plugin manifest contract", "Two-phase installer", "Default-deny capability grants", "Bounded lifecycle", "Dashboard /plugins tab", "Per-plugin observability".
    - `## Schema migration v5 to v6` — three new tables (`plugins`, `plugin_capabilities`, `plugin_status` per Phase 46 truth), two new NULLABLE columns (`tool_invocations.plugin_name`, `llm_calls.plugin_name`), one new index (the v0.5 index name as introduced in Phase 41 — read from `storage.py` if uncertain, else write "one new index on `(plugin_name, plugin_version)`"). All additive. Pre-v0.5 rows read back unchanged. `tool_invocations.plugin_name = NULL` and `llm_calls.plugin_name = NULL` for pre-v0.5 events ("core" attribution; the dashboard surfaces these as "horus-os core").
    - `## New base dependencies` — `pydantic>=2.7,<3` is now a base dep (manifest validation; the pydantic v2 BaseModel JSON-Schema export drives `docs/manifest-v1.schema.json`). `packaging>=24.0` is now a base dep (PEP 440 `SpecifierSet` parses `horus_os_compat` strings; Pitfall 2 forward-compat). Both are pure-Python with universal wheels — no 3-OS install impact.
    - `## How to roll back` — `horus-os --disable-all-plugins` is a boot flag (set the `HORUS_OS_DISABLE_ALL_PLUGINS` env var or pass the CLI flag) that skips entry-point discovery + local-directory discovery entirely; the server starts with only v0.4 first-party adapters. This is the safety hatch — no downgrade path back to v0.4 once the v5→v6 migration has run (the new columns and tables stay; the old code does not read them, but the file size grows).
    - `## Breaking change scan` — one paragraph that includes the exact phrase `There are no breaking changes to v0.4 features.` (the test substring-greps for "no breaking changes" case-insensitively).
    - `## Verification` — one paragraph + one code block. Command: `sqlite3 ~/.horus-os/data.db "PRAGMA user_version"`. Expected output: `6`. (The migration ran successfully if user_version is 6.)

    Step E — `src/horus_os/plugins/installer.py`. Make ONE surgical edit to `render_grant_prompt` (around line 421-423). Read the existing function first (lines 410-423). After the loop writes each capability, add a single literal-string write line before the final "Grant all / per-capability / refuse" prompt: e.g. `stdout.write("\nRead docs/PLUGIN-SECURITY.md before granting capabilities.\n")`. Do NOT modify `prompt_for_grants` (lines 426-494). Preserve every existing test assertion (the Phase 44 test_installer_prompt tests check the exact prompt text — but they check for the "Grant all" trailing line, not the absence of additional lines, so adding a line above should not break them; if it does, the test must be updated in this same task to expect the new line). Verify by running `pytest tests/plugins/test_installer_prompt.py -v` if such a file exists, else `pytest tests/plugins/ -k prompt -v`.

    Run `python scripts/build_manifest_schema.py` once after the script lands to materialize `docs/manifest-v1.schema.json`. Verify the output file is committed alongside the script.
  </action>
  <verify>
    <automated>
      test -f docs/PLUGINS.md && test -f docs/PLUGIN-SECURITY.md && test -f docs/MIGRATION-v0.4-to-v0.5.md && test -f docs/manifest-v1.schema.json && test -f scripts/build_manifest_schema.py &&
      grep -q '^plugins execute in the horus-os Python process\|^In horus-os v0.5, plugins execute in the horus-os Python process\| plugins execute in the horus-os Python process' docs/PLUGIN-SECURITY.md &&
      grep -q 'docs/PLUGIN-SECURITY.md' src/horus_os/plugins/installer.py &&
      python -c "import json; from horus_os.plugins.manifest import MANIFEST_V1_SCHEMA; a=json.dumps(MANIFEST_V1_SCHEMA.model_json_schema(), indent=2, sort_keys=True)+'\n'; b=open('docs/manifest-v1.schema.json').read(); assert a==b, 'docs/manifest-v1.schema.json drifted from runtime schema'" &&
      [ "$(wc -l < docs/PLUGIN-SECURITY.md)" -le 400 ]
    </automated>
  </verify>
  <done>All four docs files exist with the required sections in order; the JSON schema file is byte-identical to the runtime serialization; the build script regenerates idempotently; the installer grant prompt references the security doc; `docs/PLUGIN-SECURITY.md` is ≤400 lines and contains the literal REL-12 sentence; existing installer tests still pass (or have been updated for the new prompt line within this task).</done>
</task>

<task type="auto">
  <name>Task 2: Extend README + CHANGELOG; ship the 4 docs tests; verify Phase 46 Pitfall 12 gate auto-activates</name>
  <files>README.md, CHANGELOG.md, tests/docs/__init__.py, tests/docs/test_plugins_md_anatomy.py, tests/docs/test_plugin_security_threat_sentence.py, tests/docs/test_migration_v04_v05_schema_commands.py, tests/docs/test_manifest_schema_json_in_sync.py</files>
  <action>
    Step A — `README.md`. Read the existing file first. Add a new section after the "What is new in v0.3" section (after line 95) titled `## What is new in v0.5` with one paragraph naming the plugin system and these specific call-outs: "Plugins — third-party tools and adapters loadable from a manifest with default-deny capability grants; default-deny posture pinned to (plugin_name, plugin_version, manifest_hash); bounded lifecycle via `asyncio.wait_for(timeout=2.0)`; `horus-os plugins` CLI surface (9 subcommands); `/plugins` dashboard tab; per-plugin observability attribution on every LLM call and tool invocation." End the paragraph with a one-line list of relevant docs: `Migration guide: docs/MIGRATION-v0.4-to-v0.5.md`, `Plugin author guide: docs/PLUGINS.md`, `Security model: docs/PLUGIN-SECURITY.md`. Do NOT touch the v0.3.0 badge on line 6 (Phase 50 bumps it). Also extend the `## Documents` section (around line 97) with two new bullets in alphabetical order under the existing `docs/adapters/` bullet: `- docs/PLUGINS.md`, plugin author guide, `- docs/PLUGIN-SECURITY.md`, plugin security model and threat statement. Preserve every other line byte-identical.

    Step B — `CHANGELOG.md`. Read the existing file first. The `[Unreleased]` section (lines 7-11) currently contains empty `### Added` and `### Changed` headings. Replace that block with a `[0.5.0] - YYYY-MM-DD` heading (the placeholder date is intentional — Phase 50 promotes it; the YYYY-MM-DD literal is a release-gate cue) and fill three subsections:
      - `### Added` — bullet per shipped v0.5 capability. Mirror the prose density of the `[0.4.0]` entries (lines 25-100ish). Cover: plugin manifest contract (`MANIFEST_V1_SCHEMA` in `horus_os.plugins.manifest`; pydantic v2 validation; forward-compat unknown-field warning); two-phase installer (download-only → manifest validation → capability prompt → install; wheel-only default with `--allow-sdist` flag); default-deny capability grants (the four-capability v1 catalog: `filesystem.read`, `filesystem.write`, `net.outbound`, `secrets.read`; pinned to `(plugin_name, plugin_version, manifest_hash)`); bounded lifecycle (`asyncio.wait_for(timeout=2.0)` per ISOLATE-02; hung `start` → load-time error); `/plugins` dashboard tab (per Phase 45 REST surface); per-plugin observability (`plugin_name` columns on `llm_calls` + `tool_invocations`; existing rollups gain plugin-attribution); `horus-os plugins` CLI with the 9 subcommands `{install, uninstall, list, info, enable, disable, update, grant, revoke}`; `--disable-all-plugins` boot flag escape hatch; three-tier test fixtures (in-process unit, `fake_plugin_entry_points` monkeypatch, `clean_venv` gated by `--run-installer-e2e`); 12-pitfall regression suite under `tests/test_plugin_pitfalls/`; reference plugin scaffold forward-referenced for Phase 48; three new docs files (PLUGINS.md, PLUGIN-SECURITY.md, MIGRATION-v0.4-to-v0.5.md); committed JSON-Schema mirror at `docs/manifest-v1.schema.json`.
      - `### Changed` — one bullet: `Base [project.dependencies] now includes pydantic>=2.7,<3 (manifest validation) and packaging>=24.0 (PEP 440 horus_os_compat parsing via SpecifierSet). Both are pure-Python wheels with universal install surface — no 3-OS install impact.`
      - `### Migration` — one bullet: `v5 → v6 schema migration: three new tables (plugins, plugin_capabilities, plugin_status) + two NULLABLE columns (tool_invocations.plugin_name, llm_calls.plugin_name) + one index. All additive. v0.4 databases continue to read byte-identical. Roll back via the --disable-all-plugins boot flag; no downgrade path. See docs/MIGRATION-v0.4-to-v0.5.md.`
    The `[0.4.0] - 2026-05-26` section and all earlier sections stay byte-identical.

    Step C — Test package: `tests/docs/__init__.py` (empty file).

    Step D — `tests/docs/test_plugins_md_anatomy.py`. Standard pytest. Read `docs/PLUGINS.md` once via `Path.read_text(encoding='utf-8')`; introspect `from horus_os.plugins.capability_catalog import Capability, DESCRIPTIONS`; read `tests/fixtures/manifests/manifest_v1_full.toml`. Tests:
      - `test_plugins_md_exists`: file is non-empty.
      - `test_plugins_md_embeds_full_manifest_fixture`: assert `manifest_full_fixture_text in plugins_md_text`. Failure message: `"docs/PLUGINS.md must embed tests/fixtures/manifests/manifest_v1_full.toml verbatim; regenerate via /gsd-plan-phase 47."`.
      - `test_plugins_md_lists_every_capability_with_description`: for each `cap in Capability`, assert `cap.value in plugins_md_text` and `DESCRIPTIONS[cap] in plugins_md_text`. Failure message names the missing capability and description so the docs author fixes it without grepping.
      - `test_plugins_md_section_headings_in_order`: extract markdown `##` headings via regex `re.findall(r'^##\s+(.+)$', text, re.MULTILINE)` and assert the order matches a canonical list: `["What is a plugin?", "Anatomy of `horus-plugin.toml`", "Capability catalog", "Lifecycle hooks", "Testing your plugin", "Walkthrough of the reference plugin", "Public API surface", "Distributing your plugin"]`. (Soft match: the assertion checks that this canonical list is a contiguous subsequence of the extracted headings, allowing the doc to have additional headings interleaved if needed.)

    Step E — `tests/docs/test_plugin_security_threat_sentence.py`. Tests:
      - `test_plugin_security_md_exists`: file exists.
      - `test_threat_model_section_present`: regex `re.search(r'^##\s+Threat model\s*$', text, re.MULTILINE)` returns a match.
      - `test_literal_rel12_sentence_present`: assert `"plugins execute in the horus-os Python process" in text`. Failure message references REL-12 acceptance criterion verbatim.
      - `test_under_400_lines`: assert `len(text.splitlines()) <= 400` per ROADMAP §47 success criterion 3.
      - `test_installer_references_security_doc`: read `src/horus_os/plugins/installer.py` and assert `"docs/PLUGIN-SECURITY.md" in installer_text`. Failure message: "The installer grant prompt must reference docs/PLUGIN-SECURITY.md so the user reading the prompt knows where to read the threat model."

    Step F — `tests/docs/test_migration_v04_v05_schema_commands.py`. Tests:
      - `test_migration_doc_exists`: file exists.
      - `test_migration_doc_contains_verification_command`: assert `"PRAGMA user_version" in text`.
      - `test_migration_doc_documents_new_base_deps`: assert `"pydantic>=2.7,<3" in text and "packaging>=24.0" in text`.
      - `test_migration_doc_documents_rollback_flag`: assert `"--disable-all-plugins" in text`.
      - `test_migration_doc_says_no_breaking_changes`: assert `"no breaking changes" in text.lower()`.
      - `test_schema_version_matches_verification_command`: import `from horus_os.storage import SCHEMA_VERSION; assert SCHEMA_VERSION == 6`. (If a future schema bump happens, this test plus the migration doc both need updates — the test is the cross-link.)

    Step G — `tests/docs/test_manifest_schema_json_in_sync.py`. Tests:
      - `test_docs_schema_file_exists`: file exists.
      - `test_docs_schema_matches_runtime_schema`: deserialize `docs/manifest-v1.schema.json` via `json.loads`; call `MANIFEST_V1_SCHEMA.model_json_schema()`; assert `bundled == runtime` (dict equality).
      - `test_docs_schema_is_byte_stable_canonical`: assert `Path("docs/manifest-v1.schema.json").read_text(encoding="utf-8") == json.dumps(MANIFEST_V1_SCHEMA.model_json_schema(), indent=2, sort_keys=True) + "\n"`. Failure message: `"docs/manifest-v1.schema.json drifted from MANIFEST_V1_SCHEMA. Regenerate via: python scripts/build_manifest_schema.py"`.

    After writing all the tests, run the full plugin-docs verification:
      - `pytest tests/docs/ -v` (must pass; 4 test files, ~14 assertions).
      - `pytest tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py -v` (must pass; the previously-skipped `test_docs_drift_against_committed_schema_file` now activates because the file exists — the skip branch is bypassed and the diff assertion runs against the just-committed schema file).
      - `pytest tests/ -x -q` smoke (must not regress).
  </action>
  <verify>
    <automated>
      pytest tests/docs/ -v --no-header 2>&1 | tail -20 &&
      pytest tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py -v --no-header 2>&1 | tail -10 &&
      grep -q "What is new in v0.5\|default-deny capability grants" README.md &&
      grep -q "\[0.5.0\]" CHANGELOG.md &&
      grep -q "pydantic>=2.7,<3" CHANGELOG.md
    </automated>
  </verify>
  <done>All four docs tests pass; the Phase 46 Pitfall 12 docs-drift test auto-activates (was skipped, now runs and passes); the full test suite has no new regressions; README.md has a v0.5 section + doc bullets; CHANGELOG.md has a `[0.5.0] - YYYY-MM-DD` draft section with Added/Changed/Migration subsections; the `pytest.skip` branch in `test_pitfall_12_docs_drift.py` is no longer taken because the file exists (verified by the test running and asserting, not skipping).</done>
</task>

</tasks>

<verification>
  Run the verification commands from `verify` blocks above. The full suite for this phase:

  ```
  python scripts/build_manifest_schema.py  # must be idempotent: second run produces no diff
  diff <(python scripts/build_manifest_schema.py >/dev/null 2>&1; cat docs/manifest-v1.schema.json) docs/manifest-v1.schema.json  # must be empty
  pytest tests/docs/ -v
  pytest tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py -v
  test "$(wc -l < docs/PLUGIN-SECURITY.md)" -le 400
  grep -c "plugins execute in the horus-os Python process" docs/PLUGIN-SECURITY.md  # must be >= 1
  grep -q "docs/PLUGIN-SECURITY.md" src/horus_os/plugins/installer.py
  grep -q "PRAGMA user_version" docs/MIGRATION-v0.4-to-v0.5.md
  grep -q "pydantic>=2.7,<3" CHANGELOG.md docs/MIGRATION-v0.4-to-v0.5.md
  ```

  Ruff cleanliness: `ruff check tests/docs/ scripts/build_manifest_schema.py` must pass with no errors.
</verification>

<success_criteria>
  Phase 47 complete when:
  - `docs/PLUGINS.md` exists with the 8 ordered sections + verbatim manifest fixture + capability catalog rows for all 4 capabilities (REFERENCE-02 §1).
  - `docs/PLUGIN-SECURITY.md` exists with the literal REL-12 sentence inside `## Threat model` + the 4 ordered sections + ≤400 lines + linked from installer prompt (REL-12).
  - `docs/MIGRATION-v0.4-to-v0.5.md` exists with TL;DR + schema-migration + new-deps + rollback + breaking-change-scan + verification command (ROADMAP §47 SC4).
  - `docs/manifest-v1.schema.json` is byte-identical to canonical serialization of `MANIFEST_V1_SCHEMA.model_json_schema()` (REFERENCE-02 §2; Pitfall 12 docs-drift gate target).
  - `scripts/build_manifest_schema.py` regenerates the file idempotently.
  - `README.md` carries a v0.5 section + two new doc bullets (ROADMAP §47 SC5).
  - `CHANGELOG.md` has a draft `[0.5.0] - YYYY-MM-DD` section per Keep-a-Changelog 1.1.0 (ROADMAP §47 SC5).
  - `src/horus_os/plugins/installer.py::render_grant_prompt` references `docs/PLUGIN-SECURITY.md` in its printed prompt (REL-12 installer linkage).
  - All 4 new tests in `tests/docs/` pass.
  - Previously-skipped `tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py::test_docs_drift_against_committed_schema_file` now activates and passes (REFERENCE-02 docs-drift CI gate substrate).
  - No regressions in the broader test suite.
  - One commit per the standard `docs(phase-47): ...` shape.
</success_criteria>

<output>
After completion, create `.planning/phases/47-documentation-refresh-docs-trio/47-01-SUMMARY.md` summarizing:
- The 4 new docs files (with line counts).
- The 1 new build script + the JSON schema artifact it produced.
- The 4 new tests (file paths + what each asserts).
- The Phase 46 Pitfall 12 gate auto-activation (no edit to the Phase 46 test; it conditionally skipped on file-absence and now activates).
- README + CHANGELOG extensions (sections added; no existing content removed).
- One installer.py line-extension (the security-doc reference in the grant prompt).
- Pointers to Phase 48 (reference plugin lands the contract enumerated in PLUGINS.md §Walkthrough) and Phase 49 (release gate diffs `docs/manifest-v1.schema.json` against the runtime schema).
</output>
