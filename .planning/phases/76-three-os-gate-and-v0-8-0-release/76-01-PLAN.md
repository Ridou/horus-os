---
phase: 76-three-os-gate-and-v0-8-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/horus_os/__init__.py
  - tests/test_pyproject_extras.py
autonomous: true
requirements:
  - REL-18
  - REL-19
must_haves:
  truths:
    - "A bare pip install of horus-os pulls zero v0.8 optional dependencies (no openai, fastembed, onnxruntime, sqlite-vec, mcp, readability-lxml, pypdf, Pillow)."
    - "Each v0.8 capability has its own optional extra declared in pyproject.toml with the exact pin from STACK.md."
    - "The [research] convenience meta-extra installs the full v0.8 infrastructure layer in one command."
    - "The [all] extra does NOT pull the heavy local-memory deps (onnxruntime, fastembed, sqlite-vec) so a fresh .[all] install stays cross-OS clean."
    - "__version__ reads 0.8.0 and matches pyproject [project].version."
  artifacts:
    - path: "pyproject.toml"
      provides: "Seven new optional-dependency tables plus version bump and [all]-exclusion comments"
      contains: "local-memory ="
    - path: "src/horus_os/__init__.py"
      provides: "__version__ string"
      contains: "0.8.0"
    - path: "tests/test_pyproject_extras.py"
      provides: "Regression test asserting extra membership and [all] exclusions"
      exports: []
  key_links:
    - from: "pyproject.toml [project.optional-dependencies].research"
      to: "local-llm, local-memory, mcp, web, pdf, vision extras"
      via: "self-referential horus-os[...] meta-extra"
      pattern: "horus-os\\[local-llm,local-memory,mcp,web,pdf,vision\\]"
    - from: "tests/test_pyproject_extras.py"
      to: "pyproject.toml"
      via: "tomllib.load on the project root pyproject"
      pattern: "tomllib\\.load"
---

<objective>
Declare the seven v0.8 optional extras in pyproject.toml with the exact pins verified in STACK.md, bump the package version to 0.8.0, and pin a regression test that locks the extra membership and the [all]-exclusion contract. This is the foundation plan: the CI install-smoke jobs (76-02) and the migration/CHANGELOG (76-03) both reference extras that must exist first.

Purpose: REL-18 (all new deps declared with cross-OS wheels behind extras) and REL-19 (bare install stays clean; every integration optional) both start here. The onnxruntime Intel-macOS pin lives in the [local-memory] table; the [all]-exclusion mirrors the REL-17 precedent set for [supabase] and [vercel].
Output: Updated pyproject.toml, bumped __version__, and tests/test_pyproject_extras.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md

<interfaces>
<!-- Current pyproject [project.optional-dependencies] shape (extracted; executor reuses this exact TOML idiom). -->
From pyproject.toml:
- dependencies = ["pydantic>=2.7,<3", "packaging>=24.0", "starlette>=0.46.0"]
- Existing extras: anthropic, gemini, dashboard, discord, supabase, slack, calendar, otel, all, dev
- The `all` extra is a flat list that omits heavy/standalone extras already (per the REL-17 precedent that excluded [supabase] and [vercel]); [discord] is also excluded from [all].
- [tool.setuptools.package-data] and [project.entry-points."horus_os.adapters"] are NOT touched by this plan.

From src/horus_os/__init__.py:
- __version__ = "0.5.0"  (a plain module-level string; bump in place)
- __version__ is re-exported in __all__.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the seven v0.8 optional extras and exclude heavy ones from [all]</name>
  <read_first>
    - pyproject.toml (the file being modified; mirror the exact list-of-strings idiom already used for the otel and calendar extras)
    - .planning/research/STACK.md "Complete Dependency Matrix" section (lines ~315-369: the source of truth for every pin)
  </read_first>
  <action>
    In the `[project.optional-dependencies]` table of pyproject.toml, add seven new extras using the exact pins from STACK.md: `local-llm = ["openai>=2.40.0"]`; `local-memory = ["fastembed>=0.8.0", "onnxruntime>=1.17.0,<1.19.0", "sqlite-vec>=0.1.9"]` with an inline comment on the onnxruntime line stating the pin guarantees universal2 wheels for Intel macOS until Microsoft restores Intel coverage (no em-dash); `mcp = ["mcp>=1.27.2"]`; `web = ["readability-lxml>=0.8.4.1", "httpx>=0.27.0"]`; `pdf = ["pypdf>=6.12.2"]`; `vision = ["Pillow>=10.0"]`; and the convenience meta-extra `research = ["horus-os[local-llm,local-memory,mcp,web,pdf,vision]"]`.
    Do NOT add any of these dependencies to the base `[project.dependencies]` list (REL-19: bare install stays clean). Do NOT add the heavy local-memory deps (onnxruntime, fastembed, sqlite-vec) to the `[all]` extra; the local-memory triplet needs the Intel-macOS pin and is exercised by its own dedicated install-smoke variant in plan 76-02, so it stays out of `[all]` exactly as [supabase] and [vercel] were excluded for REL-17. Add `openai>=2.40.0`, `mcp>=1.27.2`, `readability-lxml>=0.8.4.1`, `pypdf>=6.12.2`, and `Pillow>=10.0` to the `[all]` list so `.[all]` exercises the light cross-OS-wheel extras in the existing install-smoke job; httpx is already pulled transitively. Add a comment block above `[all]` documenting that local-memory is deliberately excluded (Intel-macOS onnxruntime pin, own smoke variant) mirroring the REL-17 [supabase]/[vercel] exclusion.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -c "import tomllib,pathlib; d=tomllib.loads(pathlib.Path('pyproject.toml').read_text()); ex=d['project']['optional-dependencies']; assert {'local-llm','local-memory','mcp','web','pdf','vision','research'} <= set(ex), set(ex); assert 'onnxruntime>=1.17.0,<1.19.0' in ex['local-memory']; assert not any('onnxruntime' in s for s in ex['all']), 'onnxruntime leaked into [all]'; print('OK extras')"</automated>
  </verify>
  <done>tomllib confirms all seven extras present, the onnxruntime pin string is exact, and no onnxruntime/fastembed/sqlite-vec string appears in the [all] list.</done>
</task>

<task type="auto">
  <name>Task 2: Bump __version__ to 0.8.0 and add the pyproject-extras regression test</name>
  <read_first>
    - src/horus_os/__init__.py (the file being modified; __version__ is a plain string at module top)
    - pyproject.toml (read the version field and the optional-dependencies table the test will assert against)
    - tests/test_storage.py (source-of-truth pattern for how the suite reads the repo root and uses plain assert-based tests; mirror its import/fixture idiom)
  </read_first>
  <action>
    Set `__version__ = "0.8.0"` in src/horus_os/__init__.py and set `version = "0.8.0"` in the `[project]` table of pyproject.toml. Create tests/test_pyproject_extras.py with at least one regression test per code change: (a) `test_version_matches` parses pyproject with `tomllib` and asserts `project.version == horus_os.__version__ == "0.8.0"`; (b) `test_v0_8_extras_declared` asserts the seven extras exist with the exact STACK.md pin strings, including the `onnxruntime>=1.17.0,<1.19.0` Intel-macOS pin and the self-referential `horus-os[local-llm,local-memory,mcp,web,pdf,vision]` research meta-extra entry; (c) `test_base_dependencies_stay_minimal` asserts none of openai, fastembed, onnxruntime, sqlite-vec, mcp, readability-lxml, pypdf, or Pillow appears in `project.dependencies` (REL-19 bare-install guarantee); (d) `test_local_memory_excluded_from_all` asserts no onnxruntime/fastembed/sqlite-vec string is in the `[all]` extra (REL-17-style exclusion). Resolve the pyproject path via pathlib from the test file location, not a hardcoded absolute path.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -m pytest tests/test_pyproject_extras.py -q && ruff check pyproject.toml src/horus_os/__init__.py tests/test_pyproject_extras.py</automated>
  </verify>
  <done>The new test file passes all four tests; ruff is clean on the touched files; __version__ and pyproject version both read 0.8.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package index -> install env | pip resolves and installs the declared extra deps from PyPI on the user's machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | new PyPI deps (openai, fastembed, onnxruntime, sqlite-vec, mcp, readability-lxml, pypdf, Pillow) added to extras | mitigate | All pins are exact lower bounds verified against PyPI in STACK.md (HIGH confidence, release dates recorded); pip-audit runs on every PR via the v0.6 audit.yml workflow and will flag any advisory against these new transitive deps. No version is `[ASSUMED]`/`[SUS]`/`[SLOP]`; STACK.md "Sources" enumerates each verified release. |
| T-76-02 | Elevation of Privilege | heavy native dep (onnxruntime) silently pulled by a bare install | mitigate | onnxruntime/fastembed/sqlite-vec are confined to the [local-memory] extra and excluded from [all] and from base dependencies; test_base_dependencies_stay_minimal + test_local_memory_excluded_from_all are the regression backstops. |
| T-76-SC | Tampering | pip installs of v0.8 extras | mitigate | Package Legitimacy Audit covers all eight new deps; see audit note below. No install task in this phase runs an unaudited package. |
</threat_model>

<verification>
- `python -c` tomllib probe confirms the seven extras and the [all] exclusion.
- `pytest tests/test_pyproject_extras.py` green.
- `ruff check` clean on all touched files.
- No em-dash (U+2014) in any added pyproject comment or test docstring.
</verification>

<success_criteria>
- pyproject.toml declares local-llm, local-memory, mcp, web, pdf, vision, and research extras with exact STACK.md pins.
- onnxruntime is pinned `>=1.17.0,<1.19.0` only inside [local-memory].
- A bare `pip install horus-os` (base dependencies) pulls none of the eight v0.8 deps.
- `[all]` excludes the local-memory triplet, mirroring the REL-17 [supabase]/[vercel] exclusion.
- __version__ and pyproject version both read 0.8.0.
- tests/test_pyproject_extras.py passes and is committed.
</success_criteria>

<output>
Create `.planning/phases/76-three-os-gate-and-v0-8-0-release/76-01-SUMMARY.md` when done.
</output>
