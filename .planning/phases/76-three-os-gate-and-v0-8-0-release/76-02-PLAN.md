---
phase: 76-three-os-gate-and-v0-8-0-release
plan: 02
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - .github/workflows/ci.yml
  - scripts/install_smoke.py
  - tests/test_install_smoke_v0_8_extras.py
autonomous: true
requirements:
  - TEST-38
  - REL-18
must_haves:
  truths:
    - "CI declares an install-smoke variant that installs the [local-memory] extra and proves the onnxruntime Intel-macOS pin resolves a wheel on macOS, Ubuntu, and Windows for Python 3.11 and 3.12."
    - "CI declares an install-smoke variant that installs the light v0.8 extras (local-llm, mcp, web, pdf, vision) and imports each new module without a cloud key set."
    - "install_smoke.py asserts every v0.8 capability module imports and that no v0.8 tool registers when its extra/config is absent."
    - "The release-gate has a check that greps ci.yml for the new install-smoke job names so they cannot silently disappear before a tag."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "install-smoke-local-memory and install-smoke-v0-8-extras jobs on the 3-OS x 2-Python matrix"
      contains: "install-smoke-local-memory"
    - path: "scripts/install_smoke.py"
      provides: "v0.8 import + tool-absence assertions appended to the existing smoke driver"
      contains: "V0_8"
    - path: "tests/test_install_smoke_v0_8_extras.py"
      provides: "Local dry-run regression asserting the smoke snippet logic and ci.yml job presence"
      exports: []
  key_links:
    - from: ".github/workflows/ci.yml install-smoke-local-memory job"
      to: "pyproject [local-memory] extra"
      via: "pip install -e .[local-memory]"
      pattern: "\\.\\[.*local-memory.*\\]"
    - from: "scripts/release_gate.py check"
      to: ".github/workflows/ci.yml"
      via: "grep for literal job names install-smoke-local-memory and install-smoke-v0-8-extras"
      pattern: "install-smoke-local-memory"
---

<objective>
Add the three-OS install-smoke coverage for the v0.8 extras: a dedicated [local-memory] variant that forces the onnxruntime Intel-macOS pin to resolve, a light-extras variant that imports every new v0.8 module with zero cloud key, the install_smoke.py assertions those jobs run, and a release-gate grep check that locks the new job names. TEST-38 (install-smoke green with new extras + onnxruntime pin) and the CI half of REL-18 (cross-OS wheels verified) land here.

Purpose: the actual three-OS gate runs in GitHub Actions and cannot run locally. This plan delivers the CI config, the smoke driver logic, and a local dry-run that exercises the snippet so the executor can prove the logic before the real gate runs. The SUMMARY must flag that the real macOS/Ubuntu/Windows x Python 3.11/3.12 gate runs only in GitHub Actions.
Output: extended ci.yml, extended scripts/install_smoke.py, a new dry-run test, and a noted release-gate check addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@76-01-SUMMARY.md

<interfaces>
<!-- Existing CI job idiom the new jobs must mirror byte-for-byte (extracted from ci.yml). -->
From .github/workflows/ci.yml:
- Existing jobs: lint-and-test, install-smoke, install-smoke-no-otel, install-smoke-with-otel, install-smoke-plugin.
- Every install-smoke job uses: `needs: lint-and-test`; `strategy.fail-fast: false`; `matrix.os: [ubuntu-latest, macos-latest, windows-latest]`; `matrix.python-version: ["3.11", "3.12"]`.
- Checkout is pinned by SHA with persist-credentials: false: `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`.
- Setup-python is pinned by SHA: `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0` with `cache: pip` and `cache-dependency-path: pyproject.toml`.
- Cross-OS steps split linux/macos (shell: bash) vs windows (shell: pwsh) using `if: runner.os != 'Windows'` / `if: runner.os == 'Windows'` (see install-smoke-plugin for the exact pattern).
- CIHARD-04 (v0.6): every `uses:` MUST be a 40-char SHA pin; reuse the two SHAs above verbatim.

From scripts/install_smoke.py:
- `main()` runs numbered checks (1..15) inside a try/finally that owns a tmp_dir; returns 0 on success, 1 on failure.
- Helper `run_python(snippet, extra_args=...)` runs a snippet in a child process; `expect(label, result, code=, in_stdout=[...])` asserts exit code + stdout substrings.
- `SCHEMA_VERSION_EXPECTED` is already bumped to 12 by Phase 74 (skills schema sweep); this plan does NOT touch it.
- v0.8 module paths (created in Phases 69-75): horus_os._providers._openai_compat, horus_os.memory.vector, horus_os.mcp, horus_os.tools.web_search, horus_os.tools.vision, horus_os.tools.shell, horus_os.research, horus_os.skills.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two v0.8 install-smoke CI jobs (light-extras + local-memory pin)</name>
  <read_first>
    - .github/workflows/ci.yml (the file being modified; copy the install-smoke-with-otel and install-smoke-plugin job blocks as the structural template)
    - .planning/research/STACK.md (onnxruntime Intel-macOS pin rationale, lines ~60-82)
  </read_first>
  <action>
    Append two new jobs to ci.yml after install-smoke-plugin, each on the standard `needs: lint-and-test` / `fail-fast: false` / `os: [ubuntu-latest, macos-latest, windows-latest]` / `python-version: ["3.11", "3.12"]` matrix, with checkout and setup-python pinned to the two existing SHAs (CIHARD-04) and persist-credentials: false.
    Job `install-smoke-v0-8-extras` (name field `install-smoke-v0-8-extras / ${{ matrix.os }} / Python ${{ matrix.python-version }}`): `pip install -e ".[dev,local-llm,mcp,web,pdf,vision]"`, then a step that runs `python -c` importing horus_os._providers._openai_compat, horus_os.mcp, horus_os.tools.web_search, horus_os.tools.vision (and pypdf, readability-lxml as the [web]/[pdf] proof) and prints an OK sentinel; then a step running `python scripts/install_smoke.py` so the v0.8 tool-absence assertions execute with the extras present but no provider/search/MCP config and no cloud key (REL-19 zero-config-start coverage rides along here).
    Job `install-smoke-local-memory` (name field `install-smoke-local-memory / ${{ matrix.os }} / Python ${{ matrix.python-version }}`): `pip install -e ".[dev,local-memory]"` (this is the job whose only purpose is to prove the onnxruntime `<1.19.0` pin resolves a real wheel on every OS x Python combo, TEST-38), then a step that runs `python -c "import onnxruntime, fastembed, sqlite_vec; print('LOCAL_MEMORY_WHEELS_OK', onnxruntime.__version__)"` and a step asserting the resolved onnxruntime version is `< 1.19.0` (parse with packaging.version) so a future resolver drift fails the gate. Add a top-of-job comment naming TEST-38 and stating the Intel-macOS universal2-wheel rationale; do NOT use em-dashes in the comment.
    Do NOT rename or remove any existing job (byte-identity contract from REL-17/TEST-25).
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -c "import re,pathlib; y=pathlib.Path('.github/workflows/ci.yml').read_text(); assert 'install-smoke-local-memory' in y and 'install-smoke-v0-8-extras' in y; assert '.[dev,local-memory]' in y or '\".[dev,local-memory]\"' in y; shas=re.findall(r'uses:\\s*\\S+@([0-9a-f]{40})', y); assert all(len(s)==40 for s in shas), 'non-SHA pin'; assert 'install-smoke-plugin' in y and 'install-smoke-with-otel' in y, 'existing job removed'; print('OK ci jobs')"</automated>
  </verify>
  <done>ci.yml contains both new jobs on the full matrix, every `uses:` is SHA-pinned, the [local-memory] install step is present, and no existing install-smoke job was removed or renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend install_smoke.py with v0.8 import + tool-absence assertions and a local dry-run test</name>
  <read_first>
    - scripts/install_smoke.py (the file being modified; reuse the `run_python` / `expect` helpers and the numbered-check structure; append after check 15)
    - src/horus_os/tools/registry.py (source-of-truth for how tools register and how to query the registry so the absence assertion targets the real API, e.g. the ToolRegistry build/list path)
  </read_first>
  <action>
    In scripts/install_smoke.py add a v0.8 surface block (checks 16+) guarded so it runs only when the v0.8 modules are importable (the light-extras and local-memory jobs install them; the existing `.[all]` install-smoke job does not, so wrap the v0.8 imports in a capability probe and skip cleanly when a module is absent, printing a `V0_8_SURFACE_SKIPPED` sentinel). Add a snippet `V0_8_IMPORT_SNIPPET` that imports the v0.8 capability modules present under the installed extras and prints `V0_8_IMPORT_OK`. Add a snippet `V0_8_TOOL_ABSENCE_SNIPPET` that builds a default ToolRegistry with zero v0.8 config and asserts: the web_search tool is absent (WEB-01 absent-until-configured), the shell tool is absent (SHELL-01 default-deny double gate), and no MCP-namespaced `mcp:` tool is registered (MCP-03 opt-in) so install-smoke doubles as the REL-19 "no feature activates silently on install" proof; print `V0_8_TOOL_ABSENCE_OK`. Wire both via `expect(...)`. Create tests/test_install_smoke_v0_8_extras.py as the local dry-run (at least one regression test per code change): assert the two snippet constants exist in install_smoke.py and contain the absence assertions; assert ci.yml declares both new job names and the `.[dev,local-memory]` install line; and, when the local-memory extra is importable in the executor venv, run the V0_8_TOOL_ABSENCE_SNIPPET in-process and assert the OK sentinel, else `pytest.skip` with a message that the real assertion runs in the GitHub Actions install-smoke-local-memory job.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -m pytest tests/test_install_smoke_v0_8_extras.py -q && ruff check scripts/install_smoke.py tests/test_install_smoke_v0_8_extras.py && python -c "import ast,pathlib; ast.parse(pathlib.Path('scripts/install_smoke.py').read_text()); print('install_smoke parses')"</automated>
  </verify>
  <done>install_smoke.py still parses and contains V0_8_IMPORT_SNIPPET + V0_8_TOOL_ABSENCE_SNIPPET with the web/shell/mcp absence assertions; the dry-run test passes (skipping the in-process leg when local-memory is not installed); ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add the install-smoke-v0-8 release-gate grep check</name>
  <read_first>
    - scripts/release_gate.py (the file being modified; mirror check_plugin_install_smoke_ci_present which greps ci.yml for a literal job name and returns a CheckResult, and append to the --check argparse choices enum exactly as the v0.5/v0.6 checks did)
    - .github/workflows/ci.yml (the grep target; confirm the literal job-name strings)
  </read_first>
  <action>
    Add `check_v0_8_install_smoke_ci_present(ci_yml_path)` to scripts/release_gate.py that greps ci.yml for the literal strings `install-smoke-local-memory` AND `install-smoke-v0-8-extras` and returns a passing CheckResult only when both are present, mirroring the single-grep shape of `check_plugin_install_smoke_ci_present`. Append the new check value `v0-8-install-smoke-ci` to the `--check` argparse `choices` tuple in `main()` (append only, never rename existing values, per the byte-identity enum contract) and add the matching dispatch block alongside the other ci-presence checks. Update the module docstring count from FOURTEEN to FIFTEEN checks. Do NOT use em-dashes in the new docstring or comment text.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python scripts/release_gate.py --check v0-8-install-smoke-ci && ruff check scripts/release_gate.py</automated>
  </verify>
  <done>`release_gate.py --check v0-8-install-smoke-ci` exits 0 against the ci.yml from Task 1; the new value is in the choices enum; no existing check value was renamed; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI resolver -> CI runner | the install-smoke jobs resolve and install the v0.8 extra wheels on three OSes |
| installed package -> ToolRegistry | the absence assertions verify untrusted-by-default tools (shell, web, mcp) do not auto-register |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Denial of Service | onnxruntime resolver drift pulling an Intel-macOS-less 1.19+ wheel | mitigate | install-smoke-local-memory asserts the resolved onnxruntime version is < 1.19.0 via packaging.version on every OS x Python combo; the gate fails before a broken wheel ships. |
| T-76-04 | Elevation of Privilege | a v0.8 tool (shell, web, mcp) auto-registering on a bare install | mitigate | V0_8_TOOL_ABSENCE_SNIPPET asserts shell/web/mcp tools are absent with zero config; this is the install-smoke-level guard backing SHELL-01/WEB-01/MCP-03 default-deny postures established in their owning phases. |
| T-76-05 | Tampering | a maintainer silently deleting an install-smoke job before a tag | mitigate | check_v0_8_install_smoke_ci_present greps ci.yml for both literal job names; the release gate fails if either disappears. |
| T-76-SC | Tampering | pip installs in CI of v0.8 extras | mitigate | Covered by the Package Legitimacy Audit (see plan 76-01 audit note); all eight deps are verified in STACK.md Sources. |
</threat_model>

<verification>
- `python -c` ci.yml probe confirms both job names, the [local-memory] install line, and all-SHA pins.
- `pytest tests/test_install_smoke_v0_8_extras.py` green (in-process leg skips cleanly without local-memory installed).
- `release_gate.py --check v0-8-install-smoke-ci` exits 0.
- `ruff check` clean on all touched files.
- SUMMARY explicitly flags that the real three-OS x two-Python gate runs only in GitHub Actions; the local deliverable is the config plus the dry run.
- No em-dash (U+2014) anywhere in added YAML comments, docstrings, or test prose.
</verification>

<success_criteria>
- ci.yml declares install-smoke-v0-8-extras and install-smoke-local-memory on the 3-OS x 2-Python matrix with SHA-pinned actions.
- The local-memory job proves the onnxruntime `<1.19.0` pin resolves a wheel and asserts the resolved version is below 1.19.0 (TEST-38).
- install_smoke.py asserts every installed v0.8 module imports and that shell/web/mcp tools are absent with zero config.
- The release gate greps ci.yml for both new job names.
- All existing install-smoke jobs remain byte-identical (no rename/removal).
</success_criteria>

<output>
Create `.planning/phases/76-three-os-gate-and-v0-8-0-release/76-02-SUMMARY.md` when done; include the explicit note that the live three-OS gate runs in GitHub Actions only.
</output>
