---
phase: 76-three-os-gate-and-v0-8-0-release
plan: 03
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - CHANGELOG.md
  - docs/MIGRATION-v0.7-to-v0.8.md
  - README.md
  - tests/test_zero_config_start.py
autonomous: true
requirements:
  - REL-19
must_haves:
  truths:
    - "CHANGELOG has a complete [0.8.0] section describing the v0.8 capabilities and every new optional extra."
    - "docs/MIGRATION-v0.7-to-v0.8.md documents the new extras, the additive SCHEMA_VERSION 11->12 skills migration, and v0.7 upgrade instructions."
    - "README documents the new optional extras and reaffirms that horus-os runs locally with only an LLM key."
    - "A regression test proves horus-os starts with zero cloud API keys and zero optional extras configured, and no feature activates silently on install."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[0.8.0] release section promoted from [Unreleased]"
      contains: "## [0.8.0]"
    - path: "docs/MIGRATION-v0.7-to-v0.8.md"
      provides: "v0.7 to v0.8 upgrade guide with extras table and v11->v12 note"
      contains: "11 to 12"
    - path: "README.md"
      provides: "Optional-extras documentation for the v0.8 capabilities"
      contains: "local-memory"
    - path: "tests/test_zero_config_start.py"
      provides: "Zero-cloud-key, zero-extra start regression"
      exports: []
  key_links:
    - from: "tests/test_zero_config_start.py"
      to: "horus_os startup path (CLI doctor / app build) with no env keys"
      via: "monkeypatched env with ANTHROPIC_API_KEY and GOOGLE_API_KEY unset"
      pattern: "monkeypatch\\.delenv"
    - from: "docs/MIGRATION-v0.7-to-v0.8.md"
      to: "pyproject [project.optional-dependencies]"
      via: "extras table listing local-llm/local-memory/mcp/web/pdf/vision/research"
      pattern: "local-llm"
---

<objective>
Ship the user-facing release prose and the zero-config-start proof for v0.8: the CHANGELOG [0.8.0] section, the v0.7-to-v0.8 migration guide (documenting the seven new extras and the additive skills SCHEMA_VERSION 11->12 migration), the README extras documentation, and a regression test that proves horus-os starts with zero cloud keys and zero extras configured. REL-19 (tagged with CHANGELOG and migration notes; every integration optional; starts with zero cloud config) lands here.

Purpose: REL-19 has a documentation half and a behavioral half. The docs (CHANGELOG, migration guide, README) are the tagged-release artifacts; the test/test_zero_config_start.py is the executable proof that no feature activates silently on install.
Output: CHANGELOG.md, docs/MIGRATION-v0.7-to-v0.8.md, README.md, tests/test_zero_config_start.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@76-01-SUMMARY.md

<interfaces>
<!-- Release-prose idioms to mirror (extracted from the repo). -->
From CHANGELOG.md:
- Keep-a-Changelog + SemVer format. An empty `## [Unreleased]` stub sits above the most recent dated section (currently `## [0.5.0] - 2026-05-27`). The v0.6/v0.7 sections are added by their own release phases; v0.8 promotes [Unreleased] to `## [0.8.0] - <date>` and leaves a fresh empty [Unreleased] stub above it.
- Each section uses `### Added`, links to its migration doc with `See docs/MIGRATION-...md`, and uses prose with no em-dashes.

From docs/MIGRATION-v0.4-to-v0.5.md:
- Structure: `# Migration from vX to vY`, `## TL;DR`, `## What is new`, subsections per capability, an upgrade-steps section. Mirror this skeleton.

From README.md:
- The install line is `pip install 'horus-os[all]'` (line ~69). The extras are documented in prose near the feature list. v0.8 adds the optional-extras paragraph.

From src/horus_os: the startup surfaces with no keys are `horus-os doctor` (reports endpoint/config status) and the FastAPI app build (create_app). The provider layer must not raise on import when no cloud key is set; cloud providers are only constructed lazily when a run needs them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the [0.8.0] CHANGELOG section and the v0.7-to-v0.8 migration guide</name>
  <read_first>
    - CHANGELOG.md (the file being modified; copy the [0.5.0] section shape, the [Unreleased] stub idiom, and the "See docs/MIGRATION..." link convention)
    - docs/MIGRATION-v0.4-to-v0.5.md (source-of-truth structure for the new migration doc: TL;DR, What is new, upgrade steps)
    - .planning/research/STACK.md (the extras table to reproduce accurately in the migration doc)
  </read_first>
  <action>
    Promote the empty `## [Unreleased]` in CHANGELOG.md to `## [0.8.0] - <release date>` and insert a fresh empty `## [Unreleased]` stub above it. Write the [0.8.0] `### Added` body covering the eight v0.8 capabilities (local LLM provider, on-device ONNX vector memory, MCP client with trust gate, web search + SSRF-guarded fetch, vision/PDF analysis, Deep Research flagship, skills system, gated shell) and listing every new optional extra: [local-llm], [local-memory] (with the onnxruntime Intel-macOS pin note), [mcp], [web], [pdf], [vision], and the [research] convenience meta-extra. Add a `### Changed` note that SCHEMA_VERSION moved 11 to 12 for the additive skills table. End with `See docs/MIGRATION-v0.7-to-v0.8.md for upgrade notes from v0.7.` No em-dashes.
    Create docs/MIGRATION-v0.7-to-v0.8.md following the v0.4-to-v0.5 skeleton: a TL;DR stating v0.8 is additive and every new capability is opt-in behind an extra or a config flag (bare install unchanged); a "What is new" section per capability; a table of the seven new extras with their pip install commands and the exact pins; an explicit note that the skills table triggers the additive, idempotent SCHEMA_VERSION 11 to 12 migration that runs automatically on first start after upgrade and that v0.7 (v11) databases load cleanly under v12 (cross-reference MIG-07 / the Phase 74 sweep); and upgrade steps (`pip install --upgrade horus-os`, optionally add extras, run `horus-os memory download-model` only if enabling vector memory). No em-dashes.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -c "import pathlib; c=pathlib.Path('CHANGELOG.md').read_text(); m=pathlib.Path('docs/MIGRATION-v0.7-to-v0.8.md').read_text(); assert '## [0.8.0]' in c and '## [Unreleased]' in c; assert all(e in c for e in ['local-llm','local-memory','mcp','research']); assert '11 to 12' in m and 'local-memory' in m; em = chr(0x2014); assert em not in c and em not in m, 'em-dash found'; print('OK changelog+migration')"</automated>
  </verify>
  <done>CHANGELOG has a [0.8.0] section plus a fresh [Unreleased] stub listing all seven extras and the v11->v12 note; the migration doc exists with the extras table and the 11 to 12 migration note; neither file contains an em-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Document the v0.8 extras in README and add the zero-config-start regression test</name>
  <read_first>
    - README.md (the file being modified; the install line and feature-list prose near line ~69 and ~129)
    - tests/test_storage.py (source-of-truth pattern for repo-root resolution and plain-assert test style; mirror its idiom)
    - tests/test_install_smoke.py (pattern for invoking horus-os surfaces in-process / via subprocess with a clean env)
  </read_first>
  <action>
    In README.md add a short "Optional extras" paragraph (or extend the existing extras prose) documenting that the v0.8 capabilities install behind `pip install 'horus-os[local-llm]'`, `[local-memory]`, `[mcp]`, `[web]`, `[pdf]`, `[vision]`, and the `[research]` convenience meta-extra, and reaffirm the REL-19 promise that horus-os runs locally with only an LLM key and that no extra activates a feature on a bare install. Mention the onnxruntime Intel-macOS pin caveat for [local-memory]. No em-dashes.
    Create tests/test_zero_config_start.py with the REL-19 behavioral proof (at least one regression test per the documented behavior): (a) `test_starts_with_no_cloud_keys` uses monkeypatch to `delenv` ANTHROPIC_API_KEY and GOOGLE_API_KEY (raising=False) and asserts the core startup surface succeeds with no exception (build the FastAPI app via create_app and/or run `horus-os doctor` in a subprocess with a scrubbed env and assert a zero exit / a status line, not a crash); (b) `test_no_v0_8_extra_imports_on_bare_path` asserts that importing the top-level horus_os package and building the default ToolRegistry does NOT import openai, fastembed, onnxruntime, sqlite_vec, mcp, readability, pypdf, or PIL (assert each is absent from sys.modules after the import), proving no feature activates silently; (c) `test_default_registry_has_no_dangerous_tools` asserts the default-config ToolRegistry exposes no shell, web_search, or mcp-prefixed tool (REL-19 "every integration optional"). Resolve paths via pathlib; scrub env via monkeypatch, never the real environment.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -m pytest tests/test_zero_config_start.py -q && ruff check README.md tests/test_zero_config_start.py 2>/dev/null; ruff check tests/test_zero_config_start.py && python -c "import pathlib; r=pathlib.Path('README.md').read_text(); em = chr(0x2014); assert 'local-memory' in r and 'research' in r and em not in r; print('OK readme')"</automated>
  </verify>
  <done>README documents all seven extras and the only-an-LLM-key promise with no em-dash; tests/test_zero_config_start.py passes all three tests proving zero-key startup and no silent feature activation; ruff clean on the test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env -> startup | horus-os reads cloud-provider keys from the environment; absence must be a clean degraded state, not a crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-06 | Denial of Service | a v0.8 capability module imported eagerly on the bare startup path, crashing when its extra is absent | mitigate | test_no_v0_8_extra_imports_on_bare_path asserts none of the eight v0.8 deps appears in sys.modules after a bare import; lazy-import contract from Phases 69-75 is the upstream mitigation, this test is the release backstop. |
| T-76-07 | Information disclosure | docs accidentally instructing users to expose a default-on dangerous tool | accept | The migration doc and README only document opt-in extras and reaffirm default-deny; no enablement instruction for shell/web/mcp is added here. Low risk, prose-only. |
</threat_model>

<verification>
- `python -c` probes confirm CHANGELOG [0.8.0] + extras, the migration doc 11-to-12 note, and the README extras prose.
- `pytest tests/test_zero_config_start.py` green: zero-key start succeeds and no v0.8 dep loads on the bare path.
- `ruff check` clean on the new test file.
- Em-dash guard (U+2014 absent) asserted in the verify commands for CHANGELOG, migration doc, and README.
</verification>

<success_criteria>
- CHANGELOG.md has a complete [0.8.0] section and a fresh [Unreleased] stub.
- docs/MIGRATION-v0.7-to-v0.8.md documents the seven new extras and the additive SCHEMA_VERSION 11->12 skills migration with v0.7 upgrade steps.
- README documents the v0.8 extras and the only-an-LLM-key promise.
- tests/test_zero_config_start.py proves horus-os starts with zero cloud keys and that no v0.8 extra or dangerous tool activates on a bare install.
- No em-dash in any added prose.
</success_criteria>

<output>
Create `.planning/phases/76-three-os-gate-and-v0-8-0-release/76-03-SUMMARY.md` when done.
</output>
