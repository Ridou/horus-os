---
phase: 02
plan: "00"
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - pyproject.toml
  - src/horus_os/__init__.py
  - src/horus_os/types.py
  - src/horus_os/agent.py
  - src/horus_os/_providers/__init__.py
  - src/horus_os/_providers/_anthropic.py
  - src/horus_os/_providers/_gemini.py
  - tests/test_agent.py
  - tests/test_provider_anthropic.py
  - tests/test_provider_gemini.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - CORE-05
tags:
  - agent-runtime
  - anthropic
  - gemini
  - tool-use

must_haves:
  truths:
    - "`run_agent(prompt, provider='anthropic'|'gemini', tools=None, model=None)` returns an `AgentResult` with `text`, `tool_uses`, `provider`, `model`, `usage`"
    - "`run_agent_async(...)` is an async version with the same signature and return type"
    - "Default model for Anthropic is `claude-sonnet-4-6`, default model for Gemini is `gemini-2.5-flash`. Both overridable per call."
    - "Tools are declared with a single shared `Tool` type (name, description, parameters, optional handler). The provider modules translate this shape into each SDK's native format."
    - "When `tools` is supplied, every tool_use the model returns is captured in `AgentResult.tool_uses` with name, input, and tool_use_id. Handlers are NOT auto-invoked in Phase 02 (the full tool-execution loop lands in Phase 04)."
    - "API keys come from environment variables: `ANTHROPIC_API_KEY` for Anthropic, `GEMINI_API_KEY` (with `GOOGLE_API_KEY` as a fallback alias) for Gemini. No keys are read from any other source."
    - "Provider SDKs are imported lazily inside the provider modules so a user with only one provider installed does not get import errors at package-load time."
    - "Test suite mocks both SDK clients. No live API calls in CI."
  artifacts:
    - path: "src/horus_os/types.py"
      provides: "`Tool` dataclass, `ToolUse` dataclass, `AgentResult` dataclass"
      contains: "@dataclass"
      min_lines: 25
    - path: "src/horus_os/agent.py"
      provides: "`run_agent`, `run_agent_async` entry points that dispatch to the right provider module"
      contains: "def run_agent"
      min_lines: 30
    - path: "src/horus_os/_providers/_anthropic.py"
      provides: "Sync + async Anthropic call returning a normalized `AgentResult`"
      contains: "def call_anthropic"
      min_lines: 40
    - path: "src/horus_os/_providers/_gemini.py"
      provides: "Sync + async Gemini call returning a normalized `AgentResult`"
      contains: "def call_gemini"
      min_lines: 40
    - path: "tests/test_agent.py"
      provides: "Dispatch + parameter-validation tests for `run_agent` and `run_agent_async`"
      contains: "def test_"
      min_lines: 30
    - path: "tests/test_provider_anthropic.py"
      provides: "Mocked Anthropic SDK tests covering text response and tool_use response"
      contains: "def test_"
      min_lines: 30
    - path: "tests/test_provider_gemini.py"
      provides: "Mocked Gemini SDK tests covering text response and tool_use response"
      contains: "def test_"
      min_lines: 30

tasks:
  - id: "1"
    title: "Define shared types in `src/horus_os/types.py`"
    files:
      - src/horus_os/types.py
      - src/horus_os/__init__.py
    notes: "Dataclasses: `Tool(name, description, parameters: dict, handler: Callable | None = None)`, `ToolUse(id: str, name: str, input: dict)`, `AgentResult(text: str, tool_uses: list[ToolUse], provider: str, model: str, usage: dict)`. Re-export from package __init__."

  - id: "2"
    title: "Anthropic provider module `src/horus_os/_providers/_anthropic.py`"
    files:
      - src/horus_os/_providers/__init__.py
      - src/horus_os/_providers/_anthropic.py
    notes: "Lazy import `anthropic`. Functions `call_anthropic(prompt, tools, model, max_tokens) -> AgentResult` (sync) and `call_anthropic_async(...)` (async). Default model `claude-sonnet-4-6`. Translate `Tool` to Anthropic tool schema. Parse `response.content` into normalized text + tool_uses."

  - id: "3"
    title: "Gemini provider module `src/horus_os/_providers/_gemini.py`"
    files:
      - src/horus_os/_providers/_gemini.py
    notes: "Lazy import `google.genai`. Functions `call_gemini(prompt, tools, model) -> AgentResult` (sync) and `call_gemini_async(...)` (async). Default model `gemini-2.5-flash`. Read API key from `GEMINI_API_KEY` then fall back to `GOOGLE_API_KEY`. Translate `Tool` to Gemini's `FunctionDeclaration` shape. Parse response.candidates[0].content.parts into normalized text + tool_uses."

  - id: "4"
    title: "Top-level dispatcher `src/horus_os/agent.py`"
    files:
      - src/horus_os/agent.py
      - src/horus_os/__init__.py
    notes: "`run_agent(prompt, provider, tools, model, **kwargs) -> AgentResult` and `run_agent_async(...)`. Validates provider in {'anthropic', 'gemini'}, raises `ValueError` otherwise. Re-export `run_agent`, `run_agent_async`, `Tool`, `ToolUse`, `AgentResult` from `horus_os.__init__`."

  - id: "5"
    title: "pyproject.toml: add anthropic and google-genai as optional dependencies"
    files:
      - pyproject.toml
    notes: "Add `[project.optional-dependencies]` groups: `anthropic = ['anthropic>=0.40']`, `gemini = ['google-genai>=0.3']`, plus `all` that union both. Dev extras still include pytest and ruff; the agent providers stay optional so a minimal install is small."

  - id: "6"
    title: "Tests for top-level dispatcher (mocked)"
    files:
      - tests/test_agent.py
    notes: "Mock both `call_anthropic` and `call_gemini` at the dispatcher boundary. Tests: dispatch routes to the correct provider, invalid provider raises ValueError, default model is set per provider, async path also dispatches, kwargs pass through."

  - id: "7"
    title: "Tests for Anthropic provider (mocked SDK)"
    files:
      - tests/test_provider_anthropic.py
    notes: "Use `pytest.MonkeyPatch.setattr` to replace `anthropic.Anthropic` (and `AsyncAnthropic`) with fakes. Assertions: text-only response returns AgentResult with empty tool_uses; tool_use response is parsed into `ToolUse(id, name, input)`; usage dict is populated; raises clear error when `ANTHROPIC_API_KEY` is unset (i.e., when the SDK itself raises)."

  - id: "8"
    title: "Tests for Gemini provider (mocked SDK)"
    files:
      - tests/test_provider_gemini.py
    notes: "Same shape as Anthropic tests. Mock `google.genai.Client`. Cover text-only and function-call responses. Verify `GEMINI_API_KEY` is read first, `GOOGLE_API_KEY` is the fallback."

  - id: "9"
    title: "Local verification: lint, format, test"
    files: []
    notes: "Run ruff check, ruff format --check, pytest -v. All must pass before commit. CLI smoke still prints `horus-os 0.0.1`."

  - id: "10"
    title: "Commit + write SUMMARY.md"
    files:
      - .planning/phases/02-agent-runtime-core/02-00-SUMMARY.md
    notes: "Final SUMMARY.md captures what shipped, key decisions, and known limitations to address in later phases."

threat_model:
  - threat: "User runs a tests path that accidentally hits the real API"
    mitigation: "Provider modules check for the API key at the entry point and the SDK clients are replaced with fakes via monkeypatch in every test. CI never sets `ANTHROPIC_API_KEY` or `GEMINI_API_KEY`."
  - threat: "Importing horus_os fails on a fresh install because optional SDKs are missing"
    mitigation: "All SDK imports are lazy and live inside the provider call site, not at module load. `horus_os` and `horus_os.agent` import cleanly with zero optional deps installed."

success_criteria:
  - "ruff check exits 0"
  - "ruff format --check exits 0"
  - "pytest -v exits 0 with at least 9 new tests (3 dispatcher + 3 anthropic + 3 gemini)"
  - "`from horus_os import run_agent, run_agent_async, Tool, ToolUse, AgentResult` succeeds"
  - "`horus-os --version` still prints `horus-os 0.0.1`"
  - "02-00-SUMMARY.md exists and is committed"
