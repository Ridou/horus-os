---
phase: 36-observability-dashboard-tab
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/horus_os/server/static/index.html
  - src/horus_os/server/api.py
  - tests/test_dashboard_observability.py
  - tests/test_dashboard_agents_extension.py
  - tests/test_dashboard_pricing_banner.py
  - tests/test_server_pricing_status.py
autonomous: true

requirements:
  - DASH-4-01
  - DASH-4-02
  - DASH-4-03
  - DASH-4-05

tags: [dashboard, observability, frontend, vanilla-js, pitfall-5, pitfall-10, pitfall-11, v0.4, dash-4]

# Dependency graph
requires:
  - phase: "34-01"  # PricingTable.is_stale + updated_at_age_days (banner substrate)
  - phase: "35-01"  # /api/observability/{cost,latency,tools,llm-calls} + /api/agents rollup fields
provides:
  - "src/horus_os/server/static/index.html: new Observability tab (6th tab) with cost/latency/tools panels + window selector + small-sample + NULL rendering + pricing staleness banner"
  - "src/horus_os/server/static/index.html: /agents tab extended with total_cost_usd, latency_p50_ms, latency_p95_ms, uncosted_runs columns + uncosted-runs explanatory tile"
  - "src/horus_os/server/api.py: GET /api/observability/pricing-status returning {updated_at, updated_at_age_days, is_stale} for the banner"
  - "src/horus_os/server/api.py: app.state.pricing_table exposes the PricingTable instance constructed in create_app so the new route can read it"

# Tech stack
tech-stack:
  added: []
  patterns:
    - "Vanilla JS only (no new deps, no framework, no charting library); CSS-bar chart via div widths sized by (cost / max_cost) * 100% for the cost-by-agent panel"
    - "Tab JS mirrors the Phase 27 Adapters tab pattern: setInterval(5000) cleared on tab switch via the existing setTab() machinery"
    - "Small-sample render contract: when sample_count < 10, percentile cells render '—' with title='need at least 10 runs for percentile' (Pitfall 10 line 264)"
    - "Pre-v0.4 NULL render contract: when total_cost_usd === null on an /agents row, cost + latency cells render '—' with title='no cost data captured before v0.4' (Pitfall 11)"
    - "Uncosted-runs explanatory tile rendered when SUM(uncosted_runs) across all agents is > 0, copy: '{N} runs from before v0.4 (no cost data)'"
    - "Pricing-staleness banner thresholds (Pitfall 5): age < 30 days = no banner; 30-89 days = yellow; >= 90 days = red. Copy: 'Pricing data is {N} days old. Override via HORUS_OS_PRICING_PATH.' Override path string MUST appear verbatim so the cure is one cp-and-edit operation away."
    - "Pricing-status endpoint returns {updated_at: 'YYYY-MM-DD', updated_at_age_days: int, is_stale: bool} sourced from PricingTable.updated_at + .updated_at_age_days(datetime.utcnow()) + .is_stale(datetime.utcnow(), 30)"
    - "Window selector drives all three panels: <select id='obs-window'> with options 24h / 7d (default selected) / 30d; change handler re-runs loadObservability()"
    - "Cost-by-agent panel renders CSS bar chart: each row is <div class='cost-bar'> with inline width = round(cost_usd / max_cost * 100, 1) + '%'; bar label shows agent name + $cost rounded to 6dp"
    - "Latency p50/p95 panel renders a table with columns: agent | p50_ms | p95_ms | sample_count; small-sample contract applies per-row"
    - "Tool reliability panel renders a list: each tool shows tool_name | success_rate% | call_count | last_error_type (when present); success_rate of null renders '—' with title='insufficient data (no success or error rows in window)'"
    - "/agents tab fetches the existing /api/agents (no window param, hardcoded 7d on backend per Phase 35); rolls up uncosted_runs across rows and renders the explanatory tile above the table"
    - "Stdlib only on backend; vanilla JS only on frontend; reuse existing escapeHtml() helper and pill/table CSS classes"

# Key files
key-files:
  created:
    - tests/test_dashboard_observability.py
    - tests/test_dashboard_agents_extension.py
    - tests/test_dashboard_pricing_banner.py
    - tests/test_server_pricing_status.py
    - .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md
  modified:
    - src/horus_os/server/static/index.html
    - src/horus_os/server/api.py
---

<objective>
Ship the Observability dashboard tab plus the /agents tab cost/latency extension, the pricing-staleness banner, and the small-sample + pre-v0.4 NULL render contracts. The backend half landed in Phases 32-35; this plan is the user-visible surface that turns the captured + queried data into something an operator actually reads.

Purpose: Close v0.4's DASH-4 requirement family (01/02/03/05). DASH-4-04 was the /api/agents backend extension and landed in Phase 35; this plan ships the frontend render for it. The plan also ships the Pitfall 10 small-sample render rule, the Pitfall 11 pre-v0.4 NULL contract surfaced as an explanatory tile (not as silent zeros), and the Pitfall 5 pricing-staleness banner with explicit override-path copy.

Output: A new 6th tab on the existing single-page dashboard (Chat / Traces / Agents / Writes / Adapters / Observability) plus four new columns on the /agents tab, plus a top-of-tab banner that yellows at 30 days of pricing-staleness and reds at 90. All existing v0.3 dashboard surfaces (chat SSE, trace explorer, agents list, writes audit, Adapters tab + polling) keep working byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md
@.planning/phases/34-pricing-table-and-cost-annotation/34-01-SUMMARY.md
@src/horus_os/server/static/index.html
@src/horus_os/server/api.py
@src/horus_os/observability/queries.py
@src/horus_os/observability/pricing.py
@tests/test_dashboard_adapters.py
@tests/test_server_observability_routes.py

<interfaces>
Key contracts the executor consumes (extracted from Phase 34 + 35 source so no codebase scavenger hunt is needed).

From src/horus_os/observability/pricing.py (Phase 34):
```python
class PricingTable:
    version: str                                # e.g. "1"
    updated_at: date                            # e.g. date(2026, 5, 26)
    release_version: str                        # e.g. "0.4.0"
    def is_stale(self, now: datetime, threshold_days: int = 30) -> bool: ...
    def updated_at_age_days(self, now: datetime) -> int: ...
```

From src/horus_os/server/api.py (Phase 34 wiring, line 134):
```python
_pricing_table = PricingTable(Config.load(_resolved_data_dir).pricing_path)
_bus.subscribe(CostAnnotator(_pricing_table).on_event)
```
This local `_pricing_table` is NOT currently exposed on `app.state`. Task 1 adds `app.state.pricing_table = _pricing_table` so the new pricing-status route can read it without re-constructing.

From src/horus_os/observability/queries.py (Phase 35) — return shapes the JS consumes:
```python
# /api/observability/cost   -> {"agents": [...]}
# Each row:
#   {agent: str, total_cost_usd: float, total_input_tokens: int,
#    total_output_tokens: int, total_cache_creation_input_tokens: int,
#    total_cache_read_input_tokens: int, run_count: int, uncosted_runs: int}
# Ordering: total_cost_usd DESC, tie-break by agent ASC.

# /api/observability/latency  -> {"p50_ms": int|null, "p95_ms": int|null, "sample_count": int}
# Empty window: {p50_ms: null, p95_ms: null, sample_count: 0}

# /api/observability/tools  -> {"tools": [...]}
# Each row:
#   {tool_name: str, call_count: int, success_count: int, error_count: int,
#    retry_then_success_count: int, expected_no_result_count: int,
#    success_rate: float|null, last_error_type: str|null, last_error_at: str|null}
# Ordering: call_count DESC, tie-break by tool_name ASC.

# /api/observability/llm-calls?since=7d -> {"calls": [...up to 100, DESC by created_at...]}
# NOT used by the dashboard panels in this phase. Reserved for future drilldown.

# /api/agents (already extended in Phase 35) -> {"agents": [...]}
# Each row adds: total_runs (int), total_cost_usd (float|null),
#                latency_p50_ms (int|null), latency_p95_ms (int|null),
#                uncosted_runs (int)
# v0.3 keys preserved: name, system_prompt, default_model, allowed_tools,
#                      memory_scope, created_at, updated_at, last_activity_at
```

Existing index.html surface the new code must integrate with (lines from src/horus_os/server/static/index.html):
- Tab nav: lines 67-73 (<nav> with 5 <button data-tab="..."> children)
- Tab sections: lines 75-114 (<section class="tab" id="..."> for each)
- JS setTab() machinery: lines 133-149 — adds active class, clears adaptersPoll, calls loader by name
- Existing JS helpers reused: $, $$, fetchJson, escapeHtml
- Existing CSS classes reused: .row, .grow, .pill, .pill.success, .pill.error, .pill.muted, .empty, table styling

New CSS classes required (added inside the existing <style> block in <head>):
- .obs-banner.yellow { background, border, color } and .obs-banner.red { ... } for the staleness banner
- .cost-bar { display: flex; align-items: center; gap: 8px; margin: 4px 0; } and .cost-bar-fill { background: var(--accent); height: 18px; border-radius: 3px; } for the CSS bar chart
- .uncosted-tile { padding: 8px 12px; border: 1px dashed var(--border); border-radius: 6px; color: var(--text-muted); margin-bottom: 12px; }
- .obs-panel { margin-bottom: 24px; } for spacing between the three panels

New polling variable required in JS:
- `let observabilityPoll = null;` declared alongside `let adaptersPoll = null;` at the top of the inline <script>
- setTab() machinery extended: when leaving observability tab, clearInterval(observabilityPoll); when entering, call loadObservability() and set a 5s polling interval
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/observability/pricing-status route + expose PricingTable on app.state</name>
  <files>src/horus_os/server/api.py, tests/test_server_pricing_status.py</files>

  <read_first>
    - src/horus_os/server/api.py (lines 60-145 — create_app body where _pricing_table is constructed but not exposed on app.state; verify exact line numbers, the wiring lives between the CORS middleware setup and the static dir mount)
    - src/horus_os/observability/pricing.py (PricingTable.updated_at, PricingTable.is_stale, PricingTable.updated_at_age_days return shapes)
    - tests/test_server_observability_routes.py (TestClient + tmp_path + create_app fixture pattern to mirror)
  </read_first>

  <behavior>
    - Test 1 (RED): GET /api/observability/pricing-status returns 200 with body {updated_at: str (ISO YYYY-MM-DD), updated_at_age_days: int, is_stale: bool}. The values come from the live PricingTable instance constructed in create_app. Assert all three keys are present, updated_at matches the bundled pricing.json updated_at field (currently "2026-05-26"), updated_at_age_days is a non-negative integer, is_stale is a Python bool.
    - Test 2 (RED): The route is registered under the existing /api/observability/* prefix family so the dashboard fetches it from the same base. Assert response.headers["content-type"].startswith("application/json").
    - Test 3 (RED): app.state.pricing_table is the same PricingTable instance the CostAnnotator subscriber holds. Construct a TestClient(create_app(data_dir=tmp_path)), assert app.state.pricing_table is not None, assert isinstance(app.state.pricing_table, PricingTable), assert app.state.pricing_table.updated_at == date(2026, 5, 26) (matches the bundled file). This guards against future refactors silently constructing a second instance.
    - Test 4 (RED): With HORUS_OS_PRICING_PATH env override pointing at a fixture pricing.json whose updated_at is "2025-01-01", the route reflects the override (large updated_at_age_days, is_stale=true). Use monkeypatch.setenv to set the env var before constructing the app. This guards Pitfall 5 + PRICE-04 user-override precedence reaching all the way to the staleness banner data source.
  </behavior>

  <action>
    Add `app.state.pricing_table = _pricing_table` immediately after the existing line 134 (`_pricing_table = PricingTable(...)`). Do NOT touch the existing subscribe order (CostAnnotator MUST stay registered before SQLitePersister per Phase 34 contract).

    Add a new route `@app.get("/api/observability/pricing-status")` inside create_app, placed adjacent to the four existing observability routes (after `/api/observability/llm-calls` ends around line 380). Implementation: import `from datetime import datetime` at module top (already imported per existing code, confirm before adding duplicate). Read `pt = app.state.pricing_table`. Compute `now = datetime.utcnow()`. Return `{"updated_at": pt.updated_at.isoformat(), "updated_at_age_days": pt.updated_at_age_days(now), "is_stale": pt.is_stale(now, 30)}`.

    NO new dependencies. NO touches to pricing.py, queries.py, persist.py, bus.py, cost.py, agent.py, tools/loop.py, storage.py, or pyproject.toml. This task changes ONLY src/horus_os/server/api.py (two-line insertion + new ~10-line route handler).

    Create tests/test_server_pricing_status.py with the four tests above. Test 4 (env override) constructs a fixture pricing.json on disk: minimal valid shape with `version="1"`, `updated_at="2025-01-01"`, `release_version="0.4.0"`, and a `models` dict containing exactly one model entry so PricingTable.__init__ does not reject the file. Use the existing fixture-construction patterns from tests/observability/test_pricing_override.py as a reference.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_pricing_status.py -xvs && grep -c "app.state.pricing_table" src/horus_os/server/api.py | grep -v '^0$' && grep -c '@app.get("/api/observability/pricing-status")' src/horus_os/server/api.py | grep -v '^0$' && .venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check src/ tests/ && .venv/bin/ruff format --check src/ tests/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'app.state.pricing_table' src/horus_os/server/api.py` returns >= 1 (PricingTable instance exposed on app.state for the new route to read)
    - `grep -c '@app.get("/api/observability/pricing-status")' src/horus_os/server/api.py` returns exactly 1 (new route registered)
    - `pytest tests/test_server_pricing_status.py -xvs` exits 0 with all 4 tests passing
    - `pytest tests/ -q` exits 0 with previous test count + 4 new tests (no regressions to the 585 existing tests)
    - `git diff --stat src/horus_os/observability/pricing.py src/horus_os/observability/queries.py src/horus_os/observability/persist.py src/horus_os/observability/bus.py src/horus_os/observability/cost.py src/horus_os/agent.py src/horus_os/tools/loop.py src/horus_os/storage.py pyproject.toml` returns ZERO lines (anti-scope held)
    - `ruff check src/ tests/` exits 0
    - `ruff format --check src/ tests/` exits 0
  </acceptance_criteria>

  <done>The pricing-status route is live and reads from the same PricingTable instance the CostAnnotator subscriber uses. Env override flows end-to-end so the Task 4 staleness banner can trust the source. Anti-scope file list is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Observability tab skeleton + window selector + 3 panels + Pitfall 10 small-sample render</name>
  <files>src/horus_os/server/static/index.html, tests/test_dashboard_observability.py</files>

  <read_first>
    - src/horus_os/server/static/index.html (full file; lines 67-73 nav, lines 75-114 tab sections, lines 117-149 JS setTab() machinery, lines 352-396 loadAdapters() polling pattern to mirror, lines 7-60 existing CSS to extend)
    - tests/test_dashboard_adapters.py (the Phase 27 test pattern that asserts HTML markers and JS function names; mirror its structure)
    - src/horus_os/observability/queries.py (cost_by_agent, latency_p50_p95, tool_reliability return shapes — already in the &lt;interfaces&gt; block above)
  </read_first>

  <behavior>
    - Test 1 (RED): GET / returns 200 and the body contains `data-tab="observability"` (nav button marker) AND `id="observability"` (section marker). Mirrors test_dashboard_html_contains_adapters_tab_marker pattern.
    - Test 2 (RED): The body contains `id="obs-window"` (window selector), `value="24h"`, `value="7d"`, `value="30d"`, and `selected` (the selected attribute appears at least once near a `value="7d"` token). Assert the count of `<option value="7d"` substrings is >= 1 and the selected default is 7d via `grep -c 'value="7d" selected' body or similar`.
    - Test 3 (RED): The body contains all three panel container ids: `id="obs-cost-panel"`, `id="obs-latency-panel"`, `id="obs-tools-panel"`. Assert each appears at least once.
    - Test 4 (RED): The body contains the JS function names `loadObservability`, `renderCostPanel`, `renderLatencyPanel`, `renderToolsPanel`, AND the polling variable `observabilityPoll`, AND the API path strings `/api/observability/cost`, `/api/observability/latency`, `/api/observability/tools`.
    - Test 5 (RED): The setTab machinery is extended: assert the body contains `if (name === "observability")` (the new tab dispatch in setTab) and `clearInterval(observabilityPoll)` (poll cleanup on tab switch). Also assert `5000` appears (mirroring the 5-second poll cadence per Phase 27).
    - Test 6 (RED) — Pitfall 10 small-sample render contract: the body contains the literal string `need at least 10 runs for percentile` (this is the hover title text the render contract requires). Also assert the body contains the comparison logic substring `sample_count < 10` (the JS render-time guard). The "—" em-dash character ('—') MUST appear in the JS code path that renders below-threshold cells; assert the body contains the substring `'—'` or `"—"` near the small-sample render code (use a Python str search on the JS-quoted form `—`).
    - Test 7 (RED) — v0.3 regression: all five existing tabs still register. Assert `data-tab="chat"`, `data-tab="traces"`, `data-tab="agents"`, `data-tab="writes"`, `data-tab="adapters"` all present. Assert `loadAdapters`, `loadAgents`, `loadTraces`, `loadWrites` all present. Assert `adaptersPoll` and `clearInterval(adaptersPoll)` still present (Phase 27 contract preserved).
  </behavior>

  <action>
    Add a 6th `<button data-tab="observability">Observability</button>` to the `<nav>` element, placed AFTER the existing Adapters button (around line 72). Order matters: Chat, Traces, Agents, Writes, Adapters, Observability.

    Add a 6th tab `<section class="tab" id="observability">` immediately after the Adapters section closes (around line 114). The section contains, in order:
    1. A header `<div class="row">` with `<h2 style="margin: 0; font-size: 14px;">Observability</h2>`, `<span class="grow"></span>`, the window selector `<select id="obs-window"><option value="24h">24h</option><option value="7d" selected>7d</option><option value="30d">30d</option></select>`, and a refresh button `<button class="ghost" id="refresh-observability">Refresh</button>`.
    2. A `<div id="obs-banner-container"></div>` placeholder where Task 4's pricing-staleness banner will render (this task leaves it empty; banner content lands in Task 4).
    3. Three panel containers in order: `<div class="obs-panel" id="obs-cost-panel"><h3>Cost by agent</h3><div id="obs-cost-body" class="empty">loading</div></div>`, then `<div class="obs-panel" id="obs-latency-panel"><h3>Latency p50 / p95</h3><div id="obs-latency-body" class="empty">loading</div></div>`, then `<div class="obs-panel" id="obs-tools-panel"><h3>Tool reliability</h3><div id="obs-tools-body" class="empty">loading</div></div>`.

    Extend the `<style>` block in `<head>` with the new CSS classes documented in the &lt;interfaces&gt; section: `.obs-banner.yellow`, `.obs-banner.red`, `.cost-bar`, `.cost-bar-fill`, `.uncosted-tile`, `.obs-panel`. Use existing CSS vars (--accent, --border, --text-muted, etc.) for color consistency.

    Extend the inline `<script>` block at the top with a new polling variable: `let observabilityPoll = null;` declared adjacent to `let adaptersPoll = null;` (around line 121).

    Extend `setTab(name)` to handle observability: add `if (observabilityPoll) { clearInterval(observabilityPoll); observabilityPoll = null; }` to the existing poll-cleanup block (around line 137-140), and add `if (name === "observability") { loadObservability(); observabilityPoll = setInterval(loadObservability, 5000); }` to the dispatch block (around line 144-147).

    Add the new JS functions, modeled directly on the loadAdapters() pattern:
    - `async function loadObservability()` — reads `const window = $("#obs-window").value`, then in parallel via Promise.all fetches `/api/observability/cost?since=${window}`, `/api/observability/latency?since=${window}`, `/api/observability/tools?since=${window}`. Each result is passed to the matching render function. Catches per-fetch errors and renders the per-panel error inside that panel's body div (one failing panel does not blank the others).
    - `function renderCostPanel(data)` — receives `{agents: [...]}` from /api/observability/cost. Empty list → `<div class="empty">no cost data in window</div>`. Otherwise computes `maxCost = Math.max(...data.agents.map(a => a.total_cost_usd || 0))` then renders one CSS bar per agent: `<div class="cost-bar"><div class="cost-bar-fill" style="width: ${pct}%"></div><span>${agent} $${cost.toFixed(6)}</span></div>` where `pct = maxCost > 0 ? (cost / maxCost * 100).toFixed(1) : 0`. Agents with `total_cost_usd === null` render with width 0 and `—` instead of the dollar amount (Pitfall 11 honesty: don't lie about cost).
    - `function renderLatencyPanel(data)` — receives `{p50_ms, p95_ms, sample_count}`. If `sample_count === 0`, render `<div class="empty">no latency data in window</div>`. Otherwise render a single-row table with columns: `sample_count | p50 | p95`. Apply the small-sample render contract: if `sample_count < 10`, the p50 and p95 cells render `<td title="need at least 10 runs for percentile">—</td>` instead of the numeric ms value. Otherwise render `${p50_ms}ms` and `${p95_ms}ms`.
    - `function renderToolsPanel(data)` — receives `{tools: [...]}`. Empty list → `<div class="empty">no tool calls in window</div>`. Otherwise a table with columns: `tool_name | call_count | success_rate | last_error_type`. For each row: if `success_rate === null`, render `<td title="insufficient data (no success or error rows in window)">—</td>`; otherwise render `${(success_rate * 100).toFixed(1)}%`. last_error_type renders empty when null. Pitfall 7 contract: never render `last_error_message` or any text-content field; the backend strips it. Pitfall 9 contract: retry_then_success_count is available in the data but this panel does NOT inflate the error column with it.

    Wire `$("#refresh-observability").addEventListener("click", loadObservability);` next to the existing refresh wiring. Wire `$("#obs-window").addEventListener("change", loadObservability);` so the selector drives all three panels.

    NO new JS dependencies. NO charting library. NO framework. Reuse existing `fetchJson`, `escapeHtml`, `$`, `$$` helpers. The em-dash character used for small-sample / NULL cells is the Unicode "—" (the HTML/JS-quoted form); this is NOT the same as a typed em-dash in committed prose, which the project rules ban. The Unicode character in JS string literals is the render-time representation, not project prose.

    Test file tests/test_dashboard_observability.py mirrors tests/test_dashboard_adapters.py exactly: TestClient + create_app(data_dir=tmp_path), GET "/", assertions on response.text substrings. All seven tests above use plain Python substring searches; no BeautifulSoup, no JS execution.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard_observability.py -xvs && grep -c 'data-tab="observability"' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'id="observability"' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'id="obs-window"' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'observabilityPoll' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'need at least 10 runs for percentile' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'sample_count < 10' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c '/api/observability/cost' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c '/api/observability/latency' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c '/api/observability/tools' src/horus_os/server/static/index.html | grep -v '^0$' && .venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check src/ tests/</automated>
  </verify>

  <acceptance_criteria>
    - All 7 tests in tests/test_dashboard_observability.py pass
    - `grep -c 'data-tab="observability"' src/horus_os/server/static/index.html` returns 1 (single nav button)
    - `grep -c 'id="observability"' src/horus_os/server/static/index.html` returns 1 (single tab section)
    - `grep -c 'observabilityPoll' src/horus_os/server/static/index.html` returns >= 3 (declaration + clearInterval + setInterval)
    - `grep -c 'need at least 10 runs for percentile' src/horus_os/server/static/index.html` returns >= 1 (Pitfall 10 hover copy)
    - `grep -c 'sample_count < 10' src/horus_os/server/static/index.html` returns >= 1 (Pitfall 10 render guard)
    - Existing Phase 27 Adapters tab markers still present: `grep -c 'data-tab="adapters"'`, `'loadAdapters'`, `'adaptersPoll'` all return >= 1 (regression guard)
    - Existing v0.2 tab markers still present: `data-tab="chat"`, `data-tab="traces"`, `data-tab="agents"`, `data-tab="writes"` all grep >= 1
    - `pytest tests/ -q` exits 0 (no regressions across 589+ tests)
    - `ruff check src/ tests/` exits 0
    - NO new dependencies in pyproject.toml (anti-scope; verified by git diff)
  </acceptance_criteria>

  <done>The Observability tab is present, renders three panel containers, the window selector drives all three panels, the small-sample render contract is encoded in the JS, and all v0.3 surfaces continue working unchanged. Banner container exists but is empty (Task 4 fills it).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /agents tab extension + Pitfall 11 NULL render + uncosted-runs explanatory tile</name>
  <files>src/horus_os/server/static/index.html, tests/test_dashboard_agents_extension.py</files>

  <read_first>
    - src/horus_os/server/static/index.html (lines 91-98 existing agents section, lines 308-333 existing loadAgents() function — this task extends the rendered table to add 4 new columns and prepends the uncosted-runs tile)
    - .planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md (lines 195-213: the exact shape /api/agents now returns with the 5 new fields — already in the &lt;interfaces&gt; block above)
    - tests/test_dashboard_adapters.py (HTML-marker test pattern to mirror)
  </read_first>

  <behavior>
    - Test 1 (RED): GET / contains the four new column header strings in the agents table area: `total_cost_usd` (or its display name), `latency_p50_ms` (or display name), `latency_p95_ms` (or display name), `uncosted_runs` (or display name). Acceptance: assert the body contains the literal strings `>cost</th>` AND `>p50 ms</th>` AND `>p95 ms</th>` AND `>uncosted</th>` (column header rendering — pick short display names but pin them in the test). These four headers must appear within ~300 characters of the `agents-body` id or inside the loadAgents() JS template.
    - Test 2 (RED) — Pitfall 11 hover copy: the body contains the literal hover string `no cost data captured before v0.4`. This is the title attribute the render uses on pre-v0.4 NULL cells.
    - Test 3 (RED) — Uncosted tile: the body contains the literal string `runs from before v0.4` (the explanatory tile copy when uncosted_runs > 0 across all agents). The tile container id `id="agents-uncosted-tile"` must be present so the JS can show/hide it. Assert `grep -c 'id="agents-uncosted-tile"' returns 1`.
    - Test 4 (RED) — NULL render logic: the body contains the substring `total_cost_usd === null` (the JS conditional that triggers the NULL render path). This guards against a future refactor silently dropping the NULL-handling branch and rendering "null" or "$0.00" instead.
    - Test 5 (RED) — uncosted-runs sum: the body contains the substring `reduce` and `uncosted_runs` close together (the JS that sums uncosted_runs across all rows to decide whether to show the tile). Acceptance: assert `grep -c 'uncosted_runs' src/horus_os/server/static/index.html` returns >= 2 (at least one reference per agent row plus one in the tile logic).
    - Test 6 (RED) — Backward-compat: the existing column markers stay present. Assert `>name</th>`, `>default_model</th>`, `>allowed_tools</th>`, `>last_activity_at</th>`, `>system_prompt</th>` all still grep >= 1 (the v0.3 agents table columns are not dropped, only extended).
  </behavior>

  <action>
    Modify the existing loadAgents() function (around lines 308-333) and the agents-body section (around lines 91-98) of src/horus_os/server/static/index.html.

    1. Inside the agents section markup (around line 96-97), insert a tile container BEFORE the existing `<div id="agents-body">`: `<div id="agents-uncosted-tile" class="uncosted-tile" style="display: none;"></div>`. This tile is hidden by default; loadAgents() shows it when sum(uncosted_runs) > 0.

    2. Inside loadAgents() (around line 318-329), extend the row template. The existing 5 columns stay: name, default_model, allowed_tools, last_activity_at, system_prompt. ADD four new columns rendered AFTER name and BEFORE default_model (so cost/latency are visually adjacent to the agent name, not buried at the end): cost, p50 ms, p95 ms, uncosted. Final column order: name | cost | p50 ms | p95 ms | uncosted | default_model | allowed_tools | last_activity_at | system_prompt.

    3. For each row, apply the NULL render contract: when `a.total_cost_usd === null`, render the cost cell as `<td title="no cost data captured before v0.4">—</td>` AND the two latency cells as `<td title="no cost data captured before v0.4">—</td>` (Pitfall 11 — both cost and latency are missing for pre-v0.4 traces, both get the same hover copy). When `a.total_cost_usd !== null` AND `a.latency_p50_ms === null` (mixed case where cost was captured but no llm_calls rows landed yet), the cost cell renders `$${cost.toFixed(6)}` and the latency cells render `—` with title="need at least 1 llm_calls row" (no Pitfall 10 small-sample logic on /agents — that lives on the Observability tab; here NULL just means "no data yet").

    4. The uncosted column renders `a.uncosted_runs` as an integer (always present, default 0, per Phase 35 backend contract — never null).

    5. After the table is rendered, compute `const totalUncosted = data.agents.reduce((sum, a) => sum + (a.uncosted_runs || 0), 0);`. If totalUncosted > 0, set the tile innerHTML to `${totalUncosted} runs from before v0.4 (no cost data)` and `display: block`. Otherwise leave display: none.

    Update the table header row to include the four new column headers between `>name</th>` and `>default_model</th>`: `<th>cost</th><th>p50 ms</th><th>p95 ms</th><th>uncosted</th>`.

    Pitfall 11 contract: the NULL render path MUST use the `—` Unicode character (em-dash), NOT a literal "-" or "0" or "null". The hover title MUST contain the exact substring `no cost data captured before v0.4` (the test pins this; future copy edits go through the test).

    NO changes to /api/agents on the backend (Phase 35 already shipped the 5 new fields). NO new CSS beyond the .uncosted-tile class added in Task 2. NO new dependencies.

    Test file tests/test_dashboard_agents_extension.py uses the same TestClient + create_app(tmp_path) + response.text pattern as tests/test_dashboard_adapters.py.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard_agents_extension.py -xvs && grep -c 'id="agents-uncosted-tile"' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'no cost data captured before v0.4' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'runs from before v0.4' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'total_cost_usd === null' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'uncosted_runs' src/horus_os/server/static/index.html | grep -v '^0$' && .venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check src/ tests/</automated>
  </verify>

  <acceptance_criteria>
    - All 6 tests in tests/test_dashboard_agents_extension.py pass
    - `grep -c 'id="agents-uncosted-tile"' src/horus_os/server/static/index.html` returns exactly 1
    - `grep -c 'no cost data captured before v0.4' src/horus_os/server/static/index.html` returns >= 1 (Pitfall 11 hover copy)
    - `grep -c 'runs from before v0.4' src/horus_os/server/static/index.html` returns >= 1 (uncosted tile copy)
    - `grep -c 'total_cost_usd === null' src/horus_os/server/static/index.html` returns >= 1 (NULL render branch present)
    - `grep -c 'uncosted_runs' src/horus_os/server/static/index.html` returns >= 2 (row render + tile sum)
    - The 5 v0.3 agents-table headers still present: name, default_model, allowed_tools, last_activity_at, system_prompt (regression guard)
    - `pytest tests/ -q` exits 0 (no regressions; previous tests still green)
    - `ruff check src/ tests/` exits 0
    - NO backend changes to /api/agents (anti-scope; verified by `git diff src/horus_os/server/api.py` showing only Task 1's additions, no /api/agents touches)
  </acceptance_criteria>

  <done>The /agents tab now shows cost + p50 + p95 + uncosted_runs columns. Pre-v0.4 rows render "—" with the documented hover. The explanatory tile appears above the table when any agent has uncosted runs. v0.3 column layout is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Pricing-staleness banner (Pitfall 5) with 3 color thresholds + override-path copy</name>
  <files>src/horus_os/server/static/index.html, tests/test_dashboard_pricing_banner.py</files>

  <read_first>
    - src/horus_os/server/static/index.html (the Task 2 obs-banner-container div added inside the observability section; the .obs-banner.yellow / .obs-banner.red CSS classes added in Task 2)
    - src/horus_os/server/api.py (the Task 1 /api/observability/pricing-status route — confirm response shape {updated_at, updated_at_age_days, is_stale})
    - .planning/research/PITFALLS.md (Pitfall 5 lines 125-129: banner copy MUST include the user override path so the cure is one cp-and-edit operation away)
  </read_first>

  <behavior>
    - Test 1 (RED) — Render logic present in HTML: the body contains the JS function name `renderStalenessBanner`. The body contains the string `HORUS_OS_PRICING_PATH` (override-path copy, Pitfall 5 line 125). The body contains the path string `/api/observability/pricing-status` (the endpoint the banner JS fetches). The body contains threshold constants or comparisons: assert substrings `< 30` AND `< 90` appear somewhere in the JS (the two threshold boundaries: < 30 no banner, 30-89 yellow, >= 90 red).
    - Test 2 (RED) — CSS classes present: assert `grep -c '\.obs-banner\.yellow' src/horus_os/server/static/index.html` returns >= 1, `grep -c '\.obs-banner\.red' src/horus_os/server/static/index.html` returns >= 1 (CSS classes from Task 2 actually defined in the &lt;style&gt; block; Task 2 added the classes but did not necessarily populate the rules; this test re-asserts they're styled).
    - Test 3 (RED) — Banner copy: the body contains the exact substring `Pricing data is` (the start of the banner sentence). Pitfall 5 line 125 mandates the copy structure "Pricing data is N days old. Override via HORUS_OS_PRICING_PATH." — the test pins the prefix and the override-path token; the N is interpolated at render time.
    - Test 4 (RED) — Backend route returns the data the banner needs (re-asserts Task 1 contract for the banner-specific consumer): the dashboard JS uses the response. Add an integration assertion: with a custom HORUS_OS_PRICING_PATH env override pointing at a fixture pricing.json with updated_at="2025-01-01" (>=480 days old at 2026-05-26), GET /api/observability/pricing-status returns is_stale: true and updated_at_age_days >= 90. This is the "fixture-driven banner state" test that proves the banner will render red when the data warrants.
    - Test 5 (RED) — Bundled-pricing freshness: with no env override, GET /api/observability/pricing-status returns updated_at_age_days for the bundled pricing.json (currently dated 2026-05-26). The bundled file is fresh as of today, so is_stale should be false. This test future-proofs the banner: if a future contributor forgets to bump pricing.json updated_at across release boundaries, the banner correctly fires (and this test stays false-positive-free because it freezes the expected behavior to is_stale matching whatever the bundled file declares; use freezegun or monkeypatch the datetime if the test needs date stability across CI runs).
  </behavior>

  <action>
    Add a JS function `async function renderStalenessBanner()` to src/horus_os/server/static/index.html, called inside loadObservability() BEFORE the three panel loaders fire (so the banner is the topmost rendered element). Implementation:

    ```
    async function renderStalenessBanner() {
      const container = $("#obs-banner-container");
      try {
        const data = await fetchJson("/api/observability/pricing-status");
        const age = data.updated_at_age_days;
        if (age < 30) {
          container.innerHTML = "";  // no banner when fresh
          return;
        }
        const colorClass = age < 90 ? "yellow" : "red";
        const msg = "Pricing data is " + age + " days old. Override via HORUS_OS_PRICING_PATH.";
        container.innerHTML = '<div class="obs-banner ' + colorClass + '">' + escapeHtml(msg) + '</div>';
      } catch (e) {
        container.innerHTML = "";  // banner is best-effort; fail silent
      }
    }
    ```
    (The above is a behavioral description, not a code excerpt to paste verbatim — the executor adapts it to the surrounding JS style. The point is: three branches (no banner / yellow / red), exact threshold values (30 and 90), exact override-path token (HORUS_OS_PRICING_PATH).)

    Update `loadObservability()` (added in Task 2) to call `renderStalenessBanner()` before the three panel fetches: `renderStalenessBanner(); const window = $("#obs-window").value; ...`. The banner does not need to be in the Promise.all; it can fire-and-forget so a slow pricing-status route never blocks the panels.

    Populate the .obs-banner.yellow and .obs-banner.red CSS rules in the &lt;style&gt; block. Use existing color variables: yellow = `{ background: rgba(210, 153, 34, 0.15); border: 1px solid rgb(210, 153, 34); color: rgb(210, 153, 34); padding: 8px 12px; border-radius: 6px; margin-bottom: 12px; }` (pick reasonable colors that don't conflict with the existing dark theme). Red uses the existing `var(--error)` for border/color: `{ background: rgba(248, 81, 73, 0.15); border: 1px solid var(--error); color: var(--error); padding: 8px 12px; border-radius: 6px; margin-bottom: 12px; }`.

    Pitfall 5 contract anchors: (a) threshold 30 days exactly (matches PricingTable.is_stale default), (b) threshold 90 days exactly (research locked this for the red escalation), (c) the literal substring `HORUS_OS_PRICING_PATH` appears in the banner copy verbatim so the cure path is one cp-and-edit operation away per Pitfall 5 line 129, (d) banner failure is silent (the dashboard panels still render even if pricing-status is unreachable; the banner is decoration, not blocking).

    Test file tests/test_dashboard_pricing_banner.py: 3 HTML-marker tests + 2 integration tests against /api/observability/pricing-status. The integration tests use the same env-override fixture pattern from tests/test_server_pricing_status.py (Task 1).

    For Test 5 date-stability: use `freezegun` if it's already a test dependency (check pyproject.toml first via grep); otherwise monkeypatch `horus_os.observability.pricing.datetime` or pass a known `now` through the route's accessor. The simplest approach: do NOT pin `is_stale: false` against the bundled file (which goes stale over time); instead pin `is_stale === pt.is_stale(datetime.utcnow(), 30)` by computing the expected value at test time from the same source the route uses. The test asserts internal consistency, not a hardcoded boolean.

    NO new dependencies (no freezegun unless already present). NO touches to pricing.py, queries.py, persist.py, bus.py, cost.py.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard_pricing_banner.py -xvs && grep -c 'renderStalenessBanner' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'HORUS_OS_PRICING_PATH' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c '/api/observability/pricing-status' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c 'Pricing data is' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c '< 30' src/horus_os/server/static/index.html | grep -v '^0$' && grep -c '< 90' src/horus_os/server/static/index.html | grep -v '^0$' && grep -cE '\.obs-banner\.yellow|\.obs-banner\.red' src/horus_os/server/static/index.html | grep -v '^0$' && .venv/bin/python -m pytest tests/ -q && .venv/bin/ruff check src/ tests/ && .venv/bin/ruff format --check src/ tests/</automated>
  </verify>

  <acceptance_criteria>
    - All 5 tests in tests/test_dashboard_pricing_banner.py pass
    - `grep -c 'renderStalenessBanner' src/horus_os/server/static/index.html` returns >= 2 (function definition + call from loadObservability)
    - `grep -c 'HORUS_OS_PRICING_PATH' src/horus_os/server/static/index.html` returns >= 1 (override path copy, Pitfall 5 line 129)
    - `grep -c '/api/observability/pricing-status' src/horus_os/server/static/index.html` returns >= 1 (endpoint reference)
    - `grep -c 'Pricing data is' src/horus_os/server/static/index.html` returns >= 1 (banner copy prefix)
    - `grep -c '< 30' src/horus_os/server/static/index.html` returns >= 1 AND `grep -c '< 90' src/horus_os/server/static/index.html` returns >= 1 (the two threshold boundaries)
    - CSS rules `.obs-banner.yellow` and `.obs-banner.red` both defined with at least background/border/color rules (grep confirms both class selectors appear in the &lt;style&gt; block)
    - Integration test (env override -> stale fixture) confirms is_stale: true and updated_at_age_days >= 90 flow end-to-end from PricingTable through the route into the consumer
    - `pytest tests/ -q` exits 0 (no regressions; total ~600 tests passing)
    - `ruff check src/ tests/` and `ruff format --check src/ tests/` both exit 0
    - NO new pyproject.toml dependencies (anti-scope)
  </acceptance_criteria>

  <done>The pricing-staleness banner renders on the Observability tab with the three documented color states. Yellow at 30 days, red at 90, override-path copy verbatim. Banner failure is silent so the panels render even if pricing-status is unreachable.</done>
</task>

<task type="auto">
  <name>Task 5: SUMMARY.md</name>
  <files>.planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md</files>

  <read_first>
    - .planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md (the SUMMARY template structure to mirror: frontmatter, What shipped, Commits table, Requirements satisfied, ROADMAP Success Criteria, Pitfalls guarded, Threat register, Deviations, Test counts, Out of scope, Handoff, Self-Check)
    - All four PLAN.md tasks above and their verify commands (collect the exact test counts and grep numbers for the Self-Check section)
  </read_first>

  <action>
    Create .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md mirroring the Phase 35 SUMMARY structure. Include:

    1. YAML frontmatter with `phase`, `plan: "01"`, `subsystem: dashboard`, `tags: [dashboard, observability, frontend, vanilla-js, pitfall-5, pitfall-10, pitfall-11, v0.4, dash-4]`, `requires` (34-01, 35-01), `provides` (the Observability tab + agents extension + pricing-status route + app.state.pricing_table), `requirements-completed` (DASH-4-01, DASH-4-02, DASH-4-03, DASH-4-05), `tech-stack.added: []`, `tech-stack.patterns` (vanilla JS, 5s poll cadence, small-sample render, NULL render, banner thresholds, CSS bar chart), `key-files.created` (4 new test files + this SUMMARY), `key-files.modified` (index.html + api.py), `duration`, `completed`, `total-tests`, `commits`.

    2. Sections matching Phase 35 SUMMARY: What shipped, Commits table (TDD RED/GREEN pairs per task), Requirements satisfied (DASH-4-01/02/03/05 each tied to the task and test that satisfies it), ROADMAP Success Criteria checklist (5 SCs ticked with task references), Pitfalls guarded table (Pitfall 5 / 10 / 11 each tied to a task and pin test), Anti-scope held (`git diff --stat` against the locked file list returns ZERO), Threat register outcomes (placeholder if security_enforcement is disabled — note that this plan has no new trust boundaries because all routes inherit Phase 32-35's bound-localhost contract), Deviations from plan, Authentication gates, Test counts (before / after with table per new file), Out of scope (deliberate), Forward dependencies for Phase 37 (CLI shares the same query module so dashboard and CLI cannot disagree), Self-Check (verified all plan commits exist, all key files exist, pytest exits 0, ruff clean, all grep gates pass).

    The SUMMARY MUST contain a top-level "Self-Check: PASSED" line at the end after every Self-Check assertion has been verified by running the actual commands.
  </action>

  <verify>
    <automated>test -f .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md && head -1 .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md | grep -q '^---$' && grep -c 'DASH-4-01\|DASH-4-02\|DASH-4-03\|DASH-4-05' .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md | awk '$1 >= 4 {exit 0} {exit 1}' && grep -q 'Self-Check: PASSED' .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - SUMMARY file exists at the canonical path
    - YAML frontmatter opens with `---` on line 1
    - All 4 requirement IDs (DASH-4-01, DASH-4-02, DASH-4-03, DASH-4-05) appear in the SUMMARY body
    - All 3 pitfalls (5, 10, 11) appear in the Pitfalls guarded table with explicit owner task + pin test
    - All 5 ROADMAP SCs ticked
    - Self-Check section present with "Self-Check: PASSED" terminator
    - Anti-scope check explicit: `git diff` against the forbidden file list (bus.py, persist.py, cost.py, pricing.py, queries.py, agent.py, tools/loop.py, storage.py, pyproject.toml) returns zero
  </acceptance_criteria>

  <done>SUMMARY shipped; Phase 36 is complete and ready for the orchestrator to merge back and mark closed in ROADMAP + STATE.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "User clicks the Observability tab and sees three panels: cost-by-agent bar chart, latency p50/p95 table, tool reliability list"
    - "User changes the window selector (24h / 7d / 30d) and all three panels reload with data for the new window"
    - "When latency sample_count < 10, the p50 and p95 cells render — with hover text 'need at least 10 runs for percentile', never as 0ms or a percentile number (Pitfall 10)"
    - "User visits /agents tab and sees four new columns (cost, p50 ms, p95 ms, uncosted) sourced from the Phase 35 /api/agents extension"
    - "Pre-v0.4 trace rows on the /agents tab render — for cost and latency cells with hover text 'no cost data captured before v0.4'; an explanatory tile above the table shows 'N runs from before v0.4 (no cost data)' so the missing dollars are explained, not hidden (Pitfall 11)"
    - "When pricing.json.updated_at is older than 30 days, a yellow banner appears at the top of the Observability tab; when older than 90 days, the banner is red; copy includes 'Override via HORUS_OS_PRICING_PATH' so the user knows the fix path (Pitfall 5)"
    - "All v0.3 dashboard surfaces (Chat SSE, Traces explorer, Agents list with original columns, Writes audit, Adapters tab with toggle + 5s polling) continue working unchanged after the new tab + extension land"
  artifacts:
    - path: "src/horus_os/server/static/index.html"
      provides: "6th tab nav button + observability tab section + window selector + 3 panel containers + JS polling + small-sample / NULL render contracts + pricing staleness banner + /agents tab extension"
      contains: "data-tab=\"observability\", id=\"observability\", id=\"obs-window\", loadObservability, observabilityPoll, renderStalenessBanner, sample_count < 10, total_cost_usd === null, HORUS_OS_PRICING_PATH, agents-uncosted-tile"
    - path: "src/horus_os/server/api.py"
      provides: "GET /api/observability/pricing-status route + app.state.pricing_table exposure"
      contains: "@app.get(\"/api/observability/pricing-status\"), app.state.pricing_table"
    - path: "tests/test_server_pricing_status.py"
      provides: "4 backend tests for the pricing-status route + env override + same-instance guard"
    - path: "tests/test_dashboard_observability.py"
      provides: "7 frontend HTML-marker tests for the new tab structure + JS function names + small-sample render contract + v0.3 regression guard"
    - path: "tests/test_dashboard_agents_extension.py"
      provides: "6 frontend tests for the /agents tab extension + Pitfall 11 NULL render + uncosted-runs tile + v0.3 column backward-compat"
    - path: "tests/test_dashboard_pricing_banner.py"
      provides: "5 tests for the staleness banner JS + 3-color threshold logic + override-path copy + integration with /api/observability/pricing-status"
    - path: ".planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md"
      provides: "Phase 36 plan 01 summary with requirements satisfied, ROADMAP SCs ticked, Pitfalls guarded table, anti-scope verified"
  key_links:
    - from: "src/horus_os/server/static/index.html (loadObservability JS)"
      to: "/api/observability/cost?since={window}"
      via: "fetchJson called from loadObservability(); response.agents passed to renderCostPanel"
      pattern: "loadObservability.*\\/api\\/observability\\/cost"
    - from: "src/horus_os/server/static/index.html (loadObservability JS)"
      to: "/api/observability/latency?since={window}"
      via: "fetchJson; response {p50_ms, p95_ms, sample_count} passed to renderLatencyPanel; sample_count < 10 triggers — render"
      pattern: "renderLatencyPanel.*sample_count < 10"
    - from: "src/horus_os/server/static/index.html (loadObservability JS)"
      to: "/api/observability/tools?since={window}"
      via: "fetchJson; response.tools passed to renderToolsPanel"
      pattern: "renderToolsPanel"
    - from: "src/horus_os/server/static/index.html (renderStalenessBanner JS)"
      to: "/api/observability/pricing-status"
      via: "fetchJson; response.updated_at_age_days drives 3-color threshold (none / yellow / red)"
      pattern: "renderStalenessBanner.*pricing-status"
    - from: "src/horus_os/server/api.py (pricing-status route handler)"
      to: "app.state.pricing_table (PricingTable instance from Phase 34)"
      via: "PricingTable.updated_at + .updated_at_age_days(now) + .is_stale(now, 30)"
      pattern: "app\\.state\\.pricing_table"
    - from: "src/horus_os/server/static/index.html (loadAgents JS extension)"
      to: "/api/agents response (Phase 35 extension with total_cost_usd, latency_p50_ms, latency_p95_ms, uncosted_runs)"
      via: "Per-row render with NULL branch on total_cost_usd === null; tile aggregates uncosted_runs across rows"
      pattern: "total_cost_usd === null"
    - from: "Window selector #obs-window change event"
      to: "loadObservability() reloads all 3 panels"
      via: "change handler calls loadObservability() which reads $('#obs-window').value"
      pattern: "obs-window.*addEventListener.*loadObservability|change.*loadObservability"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/observability/* | Untrusted client query string `since=...` reaches parse_window() then a parameterized SQL `?` placeholder. Phase 35 already mitigated this; Phase 36 only adds a consumer that re-uses the same path. |
| browser -> /api/observability/pricing-status | Read-only route, no parameters, reads from app.state.pricing_table (constructed at boot from PricingTable). No new attack surface. |
| pricing.json (user override via HORUS_OS_PRICING_PATH) -> PricingTable -> /api/observability/pricing-status | User-controlled file content (PRICE-04). Phase 34 already accepted this risk (T-34-01) since the operator owns the override file. Phase 36 only displays `updated_at_age_days` from it; no new disclosure beyond what Phase 34 already accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | renderToolsPanel reading `last_error_type` from /api/observability/tools | mitigate | The dashboard JS renders ONLY the fields the backend route returns; Phase 35 already pinned that the queries module never reads the text-content error column. This task adds NO new column reads. Pin test: tests/test_dashboard_observability.py asserts the JS does not reference `last_error_message` or `error_text` (`grep -c 'last_error_message\|error_text' src/horus_os/server/static/index.html` returns 0). |
| T-36-02 | Information Disclosure | renderCostPanel rendering agent names + dollar amounts | accept | Agent names and aggregate dollar amounts are non-PII metadata the user already sees on the existing /api/agents and /api/observability/cost responses. Localhost-bound; no auth surface in v0.4 (intentional per FEATURES research). |
| T-36-03 | Tampering | obs-window selector value sent as `?since=...` query string | mitigate | Phase 35 mitigation re-used: parse_window() rejects malformed input with HTTPException(400); all SQL uses `?` placeholders. The dashboard JS does not sanitize the selector value because the backend is the trust boundary; the selector is a fixed `<select>` with three hardcoded options, so injection requires a hand-crafted request, which then hits the backend's parse_window guard. |
| T-36-04 | Denial of Service | 5-second poll cadence amplifying load on a slow SQLite | mitigate | The poll only fires while the Observability tab is the active tab (setTab() clears observabilityPoll on tab switch, mirroring Phase 27 contract). Idle-tab cost = zero. Active-tab cost = 4 queries per 5 seconds, each O(log N) on indexed `created_at` columns from Phase 32. Pin: dashboard tests assert clearInterval is called on tab switch. |
| T-36-05 | Information Disclosure | banner displaying `updated_at_age_days` integer derived from pricing.json | accept | An integer day count over a file shipped publicly in the wheel reveals nothing the wheel itself does not. No PII. |
| T-36-06 | Spoofing | banner copy claiming "Override via HORUS_OS_PRICING_PATH" without verifying the env var works | mitigate | Task 1 Test 4 (env override -> stale fixture -> banner data flows through) proves end-to-end that the env override path the banner advertises is real. If the override path silently broke, the test would fail before the banner ever shipped. |
| T-36-07 | Information Disclosure | uncosted-runs tile leaking aggregate run-count history | accept | Sum of `uncosted_runs` is just an integer count of pre-v0.4 traces; no per-trace content reaches the tile. The user already sees the per-trace records on the Traces tab; the tile is an aggregate of what's already exposed. |
</threat_model>

<verification>
## Overall phase checks (run after all 5 tasks complete)

1. **Full test suite:** `.venv/bin/python -m pytest tests/ -q` exits 0 with previous test count (585 from Phase 35) + ~22 new tests (4 + 7 + 6 + 5 = 22 from Tasks 1-4) for ~607 total.

2. **Lint + format:** `.venv/bin/ruff check src/ tests/` exits 0; `.venv/bin/ruff format --check src/ tests/` exits 0.

3. **No-wallclock lint:** `.venv/bin/python scripts/lint_no_wallclock.py` exits 0 (only Python files use perf_counter; HTML/JS exempt from the lint).

4. **Anti-scope diff:** `git diff --stat main..HEAD -- src/horus_os/observability/bus.py src/horus_os/observability/persist.py src/horus_os/observability/cost.py src/horus_os/observability/pricing.py src/horus_os/observability/pricing.json src/horus_os/observability/queries.py src/horus_os/agent.py src/horus_os/tools/loop.py src/horus_os/storage.py pyproject.toml` returns NO output (zero touches to locked files).

5. **No new dependencies:** `git diff main..HEAD -- pyproject.toml` returns empty.

6. **No charting library / no JS framework:** `grep -rE 'chart\.js|d3\.js|react|vue|svelte' src/horus_os/server/static/index.html` returns ZERO matches.

7. **v0.3 dashboard regression:** all five existing tabs (chat, traces, agents, writes, adapters) and their JS functions (loadTraces, loadAgents, loadWrites, loadAdapters, toggleAdapter) still appear in the HTML and the Phase 27 test (tests/test_dashboard_adapters.py) still passes.

8. **6th tab presence:** `grep -c 'data-tab="observability"' src/horus_os/server/static/index.html` returns 1; `grep -c 'id="observability"' src/horus_os/server/static/index.html` returns 1.

9. **Pitfall 10 contract:** `grep -c 'need at least 10 runs for percentile' src/horus_os/server/static/index.html` >= 1 AND `grep -c 'sample_count < 10' src/horus_os/server/static/index.html` >= 1.

10. **Pitfall 11 contract:** `grep -c 'no cost data captured before v0.4' src/horus_os/server/static/index.html` >= 1 AND `grep -c 'total_cost_usd === null' src/horus_os/server/static/index.html` >= 1 AND `grep -c 'runs from before v0.4' src/horus_os/server/static/index.html` >= 1.

11. **Pitfall 5 contract:** `grep -c 'HORUS_OS_PRICING_PATH' src/horus_os/server/static/index.html` >= 1 AND `grep -c '< 30' src/horus_os/server/static/index.html` >= 1 AND `grep -c '< 90' src/horus_os/server/static/index.html` >= 1 AND `grep -c '/api/observability/pricing-status' src/horus_os/server/api.py` >= 1.

12. **SUMMARY exists and is well-formed:** the SUMMARY file at the canonical path exists, opens with YAML frontmatter, contains all 4 requirement IDs, all 3 pitfalls (5/10/11), all 5 ROADMAP SCs ticked, and terminates with `Self-Check: PASSED`.
</verification>

<success_criteria>
## Measurable completion (mirrors the 5 ROADMAP SCs)

- [ ] **SC1 (DASH-4-01, DASH-4-02):** `/observability` tab renders 3 panels (cost-by-agent bar chart, latency p50/p95 table, tool reliability list) with a window selector driving all three. **Pin:** tests/test_dashboard_observability.py Tests 1-5 pass. Grep gates: `data-tab="observability"`, `id="observability"`, `id="obs-window"`, `id="obs-cost-panel"`, `id="obs-latency-panel"`, `id="obs-tools-panel"`, `loadObservability`, the three /api/observability/* path strings.
- [ ] **SC2 (DASH-4-03, Pitfall 10):** Percentile cells with `sample_count < 10` render `—` with hover text `need at least 10 runs for percentile`; never as a number, never as `0ms`. **Pin:** tests/test_dashboard_observability.py Test 6 passes. Grep gates: literal hover string, `sample_count < 10` JS guard.
- [ ] **SC3 (DASH-4-05, Pitfall 11):** Pre-v0.4 trace rows in the /agents table render `—` for cost and latency cells with hover text `no cost data captured before v0.4`; a separate tile shows `N runs from before v0.4 (no cost data)`. **Pin:** tests/test_dashboard_agents_extension.py Tests 1-6 pass. Grep gates: `total_cost_usd === null`, hover string, tile copy, `id="agents-uncosted-tile"`.
- [ ] **SC4 (Pitfall 5):** Pricing-staleness banner renders when `pricing.json.updated_at > 30 days old`; yellow at 30-89 days, red at 90+; copy includes `Override via HORUS_OS_PRICING_PATH`. **Pin:** tests/test_dashboard_pricing_banner.py Tests 1-5 pass. Grep gates: `HORUS_OS_PRICING_PATH`, threshold values 30 and 90, `Pricing data is` banner-copy prefix.
- [ ] **SC5 (DASH-4-04 frontend half + v0.3 backward-compat):** Existing `/agents` tab shows the new `total_cost_usd`, `latency_p50_ms`, `latency_p95_ms`, `uncosted_runs` columns sourced from Phase 35; v0.3 surfaces (Traces, SSE chat, Agents original columns, Writes, Adapters) keep working unchanged. **Pin:** tests/test_dashboard_agents_extension.py Test 6 (v0.3 columns regression guard) + tests/test_dashboard_observability.py Test 7 (v0.3 tabs regression guard) + tests/test_dashboard_adapters.py (existing Phase 27 tests still passing).

When all 5 are checked AND the verification block runs clean (pytest 0 fail, ruff 0 fail, anti-scope diff 0 lines, SUMMARY present + PASSED), this plan is done.
</success_criteria>

<output>
After all 5 tasks complete and the verification block passes, the SUMMARY at `.planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md` is the deliverable artifact. The orchestrator (gsd-execute-phase) reads it after merge-back to update STATE.md and tick the Phase 36 row in ROADMAP.md.
</output>
