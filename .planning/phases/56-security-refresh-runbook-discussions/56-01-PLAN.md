---
phase: 56
plan: "01"
subsystem: security-runbook-discussions
type: execute
wave: 0
depends_on: []
files_modified:
  - SECURITY.md
  - docs/RELEASE.md
  - docs/MAINTAINER-RUNBOOK.md
  - .planning/rollback/flip-gate-revert.md
  - tests/test_security_runbook_substrate.py
autonomous: true
requirements: [SECDISC-01, SECDISC-02, SECDISC-03, SECDISC-04, RUNBOOK-01, RUNBOOK-02, DISCGH-01]
tags: [docs, security, runbook, rollback]
key_files:
  created:
    - docs/MAINTAINER-RUNBOOK.md
    - .planning/rollback/flip-gate-revert.md
    - tests/test_security_runbook_substrate.py
  modified:
    - SECURITY.md
    - docs/RELEASE.md
must_haves:
  truths:
    - "SECDISC-01: SECURITY.md `(not active yet)` section STAGED with PHASE-59-FLIP comment marker; NOT deleted"
    - "SECDISC-02: severity-tier SLOs (ack 7d; fix critical 14d / high 30d / medium 90d / low none); coordinated disclosure 90d; over-capacity language explicit"
    - "SECDISC-03: supported-versions table covers v0.5.x and v0.6.x; retirement policy stated; test-advisory ritual documented"
    - "SECDISC-04 + DISCGH-01: docs/RELEASE.md appended with one-time repo-settings checklist (private vuln reporting, Dependabot alerts, secret scanning + push protection, Discussions); each item has a gh api verification command"
    - "RUNBOOK-01: docs/MAINTAINER-RUNBOOK.md covers release procedure (extended with release.yml + signed tag + SBOM attach) AND post-flip operational playbook"
    - "RUNBOOK-02: .planning/rollback/flip-gate-revert.md exists with one-commit revert template"
    - "DISCGH-01: MAINTAINER-RUNBOOK repo-settings section names the Discussions categories (General, Q&A, Show and Tell, Ideas)"
    - "tests/test_security_runbook_substrate.py lints all the above; ruff clean; no em-dashes; existing `(not active yet)` text PRESERVED"
---

<objective>
Single-wave execution: land SECURITY.md refresh + MAINTAINER-RUNBOOK + rollback template + RELEASE.md repo-settings checklist + test file in one plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/56-security-refresh-runbook-discussions/56-CONTEXT.md
@SECURITY.md
@docs/RELEASE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SECURITY.md refresh</name>
  <action>
    Edit SECURITY.md:
    - Update Supported-versions table: 0.5.x yes, 0.6.x yes, < 0.5 no (retirement policy stated)
    - Add severity-tier SLOs section: ack 7d; fix targets critical 14d, high 30d, medium 90d, low no commitment; coordinated disclosure 90d default; over-capacity language ("if we go silent, file a public issue tagged `security-update-followup`")
    - Add test-advisory ritual: "we publish at least one rehearsal GHSA before any real CVE"
    - Add PHASE-59-FLIP staged comment marker BEFORE the "Contributor-pipeline security (not active yet)" section; DO NOT DELETE the section
  </action>
  <acceptance_criteria>
    - SECURITY.md contains literals: "PHASE-59-FLIP", "Severity", "14 days", "30 days", "90 days", "v0.5.x", "v0.6.x", "rehearsal GHSA", "security-update-followup"
    - Existing "(not active yet)" section PRESERVED
    - No em-dashes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: docs/MAINTAINER-RUNBOOK.md</name>
  <action>
    Create docs/MAINTAINER-RUNBOOK.md as the single doc covering:
    - Release procedure (mirror docs/RELEASE.md step 6.5 reference; extend with the release.yml-runs + signed-tag + SBOM-attach order)
    - Post-flip operational playbook: freeze triggers, throttle triggers (when to set `accepted-for-review` cap), burnout triggers, "is this PR worth my time?" decision matrix
    - Repo-settings checklist (cross-referenced from docs/RELEASE.md)
    - Discussions enabling step with category list (General, Q&A, Show and Tell, Ideas)
  </action>
  <acceptance_criteria>
    - File exists; contains all required sections
    - Contains literals: "gitsign.connectorID", "freeze", "throttle", "burnout", "General", "Q&A", "Show and Tell", "Ideas"
    - No em-dashes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: .planning/rollback/flip-gate-revert.md</name>
  <action>
    Create .planning/rollback/flip-gate-revert.md with a one-commit revert template that restores the pre-flip prose (NOTICE block in PR template; "Status: not currently accepting" in CONTRIBUTING.md; "(not active yet)" in SECURITY.md; etc.). Format as a literal patch the maintainer can `git apply` if Phase 59's atomic flip needs to be backed out for any reason.
  </action>
  <acceptance_criteria>
    - File exists; documents the revert plan
    - Contains an example git command for the revert
    - No em-dashes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: docs/RELEASE.md repo-settings checklist append</name>
  <action>
    Append a new section `## One-time repo settings checklist (v0.6 contribution-gate setup)` to docs/RELEASE.md after the existing `## Post-release` section. Each item has a `gh api` command:
    - Enable private vulnerability reporting: `gh api -X PATCH /repos/Ridou/horus-os --field security_and_analysis.private_vulnerability_reporting.status=enabled`
    - Enable Dependabot alerts: documented (not a single gh api call; user follows the Settings -> Code security path)
    - Enable Dependabot security updates: same
    - Enable secret scanning + push protection: `gh api -X PATCH /repos/Ridou/horus-os --field security_and_analysis.secret_scanning.status=enabled --field security_and_analysis.secret_scanning_push_protection.status=enabled`
    - Enable Discussions: `gh api -X PATCH /repos/Ridou/horus-os --field has_discussions=true` then create the four categories (General, Q&A, Show and Tell, Ideas) via the Settings UI
  </action>
  <acceptance_criteria>
    - docs/RELEASE.md contains new H2 "One-time repo settings checklist"
    - Contains the literal `gh api` at least 3 times
    - Contains "private_vulnerability_reporting"
    - No em-dashes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: tests/test_security_runbook_substrate.py</name>
  <action>
    Create a single test file that lints:
    - SECURITY.md PHASE-59-FLIP marker; severity SLOs; supported-versions; "(not active yet)" preserved
    - docs/MAINTAINER-RUNBOOK.md exists with required sections
    - .planning/rollback/flip-gate-revert.md exists
    - docs/RELEASE.md has the new repo-settings checklist section
  </action>
  <acceptance_criteria>
    - Test file exists; all tests pass; ruff clean; no em-dashes
  </acceptance_criteria>
</task>

</tasks>
