---
phase: 33-capture-at-the-runner-sse-branch
plan: "01"
type: execute
wave: 1
depends_on: ["32-01"]
files_modified:
  - src/horus_os/observability/__init__.py
  - src/horus_os/observability/bus.py
  - src/horus_os/agent.py
  - src/horus_os/tools/loop.py
  - src/horus_os/server/api.py
  - src/horus_os/storage.py
  - scripts/lint_no_wallclock.py
  - tests/observability/test_bus.py
  - tests/observability/test_runner_capture.py
  - tests/observability/test_tool_loop_capture.py
  - tests/observability/test_sse_capture.py
  - tests/observability/test_run_end_rollup_integration.py
  - tests/observability/test_pitfall_3_negative_latency.py
  - tests/observability/test_pitfall_9_tool_status_columns.py
  - tests/perf/test_capture_overhead.py
  - tests/test_lint_no_wallclock.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - METRIC-01
  - METRIC-02
  - METRIC-03
  - METRIC-04
  - METRIC-05
  - TEST-12

must_haves:
  truths:
    - "A 3-iteration agent run with stubbed usage={input:100,output:50} per turn produces three llm_calls rows AND traces.total_input_tokens == 300 (Pitfall 1 structurally fixed)"
    - "A streaming run through /api/chat/stream produces an llm_calls row whose input_tokens and output_tokens are read from stream.get_final_message().usage (Anthropic) or response.usage_metadata (Gemini); never silently $0 (Pitfall 2)"
    - "Every llm_calls and tool_invocations row carries latency_ms measured via time.perf_counter(); SQLitePersister asserts latency_ms >= 0; lint guard fails CI if time.time() appears in observability/, agent.py, tools/loop.py, or server/api.py (Pitfall 3)"
    - "tool_invocations rows persist status (success or error), retry_count (NULL when SDK does not expose), and last_error_text as exception class name only (no user-supplied content) (Pitfall 9 substrate)"
    - "Capture-overhead benchmark in CI compares a fixture 5-iteration / 3-tool-call loop against tests/perf/v0_3_baseline.json on the 3-OS matrix and asserts within 50ms; the benchmark skips (not fails) when no matching (os, python) baseline entry exists (METRIC-05, Pitfall 8)"
  artifacts:
    - path: "src/horus_os/observability/__init__.py"
      provides: "get_observation_bus() module-level singleton accessor for runtime + test wiring"
      contains: "def get_observation_bus"
    - path: "src/horus_os/observability/bus.py"
      provides: "LLMCallEvent.docstring states the latency_ms contract (Pitfall 4); no new code, prose anchor"
      contains: "Pitfall 4"
    - path: "src/horus_os/agent.py"
      provides: "run_agent_loop accepts trace_id, publishes LLMCallEvent per Conversation.send and RunEndEvent on loop exit"
      exports: ["run_agent_loop"]
    - path: "src/horus_os/tools/loop.py"
      provides: "_execute_one publishes ObsToolCallEvent (aliased from observability.bus) after the existing perf_counter timing block, with status/error_type/last_error_text populated"
      exports: ["_execute_one", "execute_tool_uses"]
    - path: "src/horus_os/server/api.py"
      provides: "_event_stream wraps the SSE branch with perf_counter, extracts terminal usage from stream object, publishes LLMCallEvent then RunEndEvent; never persists 0 for a non-empty stream"
      contains: "get_final_message"
    - path: "src/horus_os/storage.py"
      provides: "record_trace accepts an optional trace_id parameter so the runner can pre-generate and pass it through to LLMCallEvents (so RUN_END rollup UPDATE matches the row)"
      exports: ["Database.record_trace"]
    - path: "scripts/lint_no_wallclock.py"
      provides: "WATCHED_PATHS extended to include src/horus_os/server/api.py so Pitfall 3 gate covers all three capture sites"
      contains: "server/api.py"
    - path: "tests/observability/test_run_end_rollup_integration.py"
      provides: "3-iteration loop with stubbed Conversation, asserts traces.total_input_tokens == 300 and three llm_calls rows (Pitfall 1 regression test)"
      contains: "test_three_iteration_rollup"
    - path: "tests/observability/test_sse_capture.py"
      provides: "SSE branch test with stubbed Anthropic stream returning known usage; asserts llm_calls row has non-zero tokens (Pitfall 2 regression test)"
      contains: "test_sse_anthropic_usage_persisted"
    - path: "tests/observability/test_pitfall_3_negative_latency.py"
      provides: "Asserts the persister AssertionError on negative latency_ms is still the gate after Phase 33's wiring"
      contains: "test_negative_latency_rejected"
    - path: "tests/observability/test_pitfall_9_tool_status_columns.py"
      provides: "Tool failure path populates status='error', error_type='ValueError', last_error_text='ValueError' (class name only, no user content)"
      contains: "test_tool_failure_records_class_name_only"
    - path: "tests/perf/test_capture_overhead.py"
      provides: "5-iteration / 3-tool-call benchmark reads v0_3_baseline.json, computes median wall-clock with Phase 33 wiring active, asserts within 50ms; skips when no matching (os, python) entry"
      contains: "test_capture_overhead_within_50ms"
    - path: ".github/workflows/ci.yml"
      provides: "new CI step running pytest tests/perf/test_capture_overhead.py on the 3-OS matrix (METRIC-05)"
      contains: "capture-overhead benchmark"
  key_links:
    - from: "src/horus_os/agent.py:run_agent_loop"
      to: "src/horus_os/observability/__init__.py:get_observation_bus"
      via: "module-level singleton; runner calls bus.publish on each iteration"
      pattern: "get_observation_bus\\(\\)\\.publish"
    - from: "src/horus_os/agent.py:run_agent_loop"
      to: "src/horus_os/storage.py:Database.record_trace"
      via: "trace_id pre-generated in the run_agent_loop caller (api.py) and passed through to BOTH bus.publish(LLMCallEvent) AND record_trace(trace_id=...) so the RUN_END rollup UPDATE matches the existing traces row"
      pattern: "trace_id="
    - from: "src/horus_os/tools/loop.py:_execute_one"
      to: "src/horus_os/observability/bus.py:ToolCallEvent (aliased ObsToolCallEvent)"
      via: "bus.publish AFTER the existing perf_counter block; trace_id flows in via the ToolRegistry or a new keyword on execute_tool_uses"
      pattern: "ObsToolCallEvent\\("
    - from: "src/horus_os/server/api.py:_event_stream"
      to: "src/horus_os/observability/bus.py:LLMCallEvent + RunEndEvent"
      via: "after the async-for loop drains, read stream's terminal usage (get_final_message for Anthropic, response.usage_metadata for Gemini); publish LLMCallEvent then RunEndEvent BEFORE db.record_trace"
      pattern: "LLMCallEvent\\(.*streaming"
    - from: ".github/workflows/ci.yml:capture-overhead benchmark"
      to: "tests/perf/test_capture_overhead.py + tests/perf/v0_3_baseline.json"
      via: "pytest invocation on the 3-OS matrix reads baseline JSON, runs fixture loop with Phase 33 wiring, compares medians"
      pattern: "test_capture_overhead"
---

<objective>
Wire the Phase 32 ObservationBus into the live runner so real agent runs publish LLMCallEvent and RunEndEvent (from `agent.run_agent_loop`), ToolCallEvent (from `tools.loop._execute_one`), and a streaming-path LLMCallEvent + RunEndEvent (from `server.api._event_stream`). Cost stays NULL until Phase 34. Fix the two confirmed v0.3 correctness bugs along the way: per-iteration token undercount (Pitfall 1) and silent $0 on streamed runs (Pitfall 2). Extend the Phase 32 lint guard (Pitfall 3) to cover `server/api.py` since Phase 33 puts the third capture site there. Add a 3-OS CI capture-overhead benchmark (METRIC-05, TEST-12) that compares against the pinned `tests/perf/v0_3_baseline.json` and SKIPS when no matching entry exists so unseeded combos do not block CI.

Purpose: After this phase, the SQLite `llm_calls` and `tool_invocations` tables fill with real per-call rows on every chat run (sync, async, and streaming), and `traces.total_input_tokens / total_output_tokens / total_duration_ms` reflect summed totals across all iterations. `total_cost_usd` stays NULL until Phase 34's CostAnnotator subscribes BEFORE the persister.

Output: Five modified source files (`agent.py`, `tools/loop.py`, `server/api.py`, `storage.py`, `lint_no_wallclock.py`), one modified observability bootstrap (`observability/__init__.py` gains the singleton accessor), one modified bus docstring (Pitfall 4 contract), seven new tests, one CI workflow step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/32-schema-migration-persistence-skeleton-v0-3-baseline/32-01-SUMMARY.md

<interfaces>
Extracted from Phase 32 surface and the v0.3 runner. Executor should use these directly without re-exploring the codebase.

From src/horus_os/observability/bus.py (Phase 32, frozen for this phase, only consumed):

  @dataclass(kw_only=True)
  class LLMCallEvent(ObservationEvent):
      kind: Literal["LLM_CALL", "TOOL_CALL", "RUN_END"] = "LLM_CALL"
      trace_id: str
      iteration_idx: int
      provider: str
      model: str
      call_id: str = field(default_factory=_new_id)
      input_tokens: int = 0
      output_tokens: int = 0
      cache_creation_input_tokens: int = 0
      cache_read_input_tokens: int = 0
      cost_usd: float | None = None
      pricing_missing: bool = False
      latency_ms: int = 0
      status: str = "success"
      error_message: str | None = None
      error_type: str | None = None

  @dataclass(kw_only=True)
  class ToolCallEvent(ObservationEvent):
      kind: Literal["LLM_CALL", "TOOL_CALL", "RUN_END"] = "TOOL_CALL"
      trace_id: str
      tool_name: str
      invocation_id: str = field(default_factory=_new_id)
      parent_trace_id: str | None = None
      latency_ms: int = 0
      status: str = "success"
      error_message: str | None = None
      error_type: str | None = None
      retry_count: int | None = None
      output_size: int | None = None

  @dataclass(kw_only=True)
  class RunEndEvent(ObservationEvent):
      kind: Literal["LLM_CALL", "TOOL_CALL", "RUN_END"] = "RUN_END"
      trace_id: str
      latency_ms: int = 0

  class ObservationBus:
      def subscribe(self, handler) -> Callable[[], None]: ...
      def publish(self, event: ObservationEvent) -> None: ...

From src/horus_os/observability/persist.py (Phase 32, frozen for this phase, only consumed):

  class SQLitePersister:
      def __init__(self, db: Database) -> None: ...
      def on_event(self, event: ObservationEvent) -> None:
          # LLM_CALL -> INSERT into llm_calls
          # TOOL_CALL -> INSERT into tool_invocations
          # RUN_END -> UPDATE traces with SUM(...) rollups
          # Hard assertion: event.latency_ms >= 0

From src/horus_os/types.py (v0.3, NOT to be replaced, naming collision target):

  @dataclass
  class ToolCallEvent:
      # Streaming-surface notification, distinct from observability.bus.ToolCallEvent.
      # The runner and api.py still import THIS class for SSE tool_call surfacing.
      name: str
      input: dict[str, Any]

From src/horus_os/agent.py (v0.3, this is the primary modification target):

  def run_agent_loop(
      prompt: str,
      *,
      registry: ToolRegistry,
      provider: str = "anthropic",
      model: str | None = None,
      max_iterations: int = 10,
      budget: IterationBudget | None = None,
      system_prompt: str | None = None,
      on_tool_result: Callable[[ToolResult], None] | None = None,
  ) -> AgentResult:
      # Phase 33 adds: trace_id: str | None = None keyword.
      # When None, generates one internally so legacy callers still work.

From src/horus_os/tools/loop.py (v0.3, secondary modification target):

  def execute_tool_uses(
      registry: ToolRegistry,
      result: AgentResult,
      *,
      on_log: Callable[[ToolResult], None] | None = None,
  ) -> list[ToolResult]:
      # Phase 33 adds: trace_id: str | None = None keyword (threaded into _execute_one).

  def _execute_one(registry: ToolRegistry, use: ToolUse) -> ToolResult:
      # Phase 33 signature: _execute_one(registry, use, *, trace_id: str | None = None)
      # After the existing latency_ms line, publish ObsToolCallEvent(
      #     trace_id=trace_id, tool_name=use.name, latency_ms=outcome.latency_ms,
      #     status="success" if outcome.error is None else "error",
      #     error_type=outcome.error.split(":", 1)[0] if outcome.error else None,
      #     error_message=outcome.error.split(":", 1)[0] if outcome.error else None,
      #     retry_count=None,
      # ) when trace_id is not None.

From src/horus_os/server/api.py (v0.3, THIRD capture site):

  async def _event_stream() -> AsyncGenerator[bytes, None]:
      text_parts: list[str] = []
      start = time.perf_counter()
      try:
          async for chunk in run_agent_stream(prompt, provider=provider, model=model, system=system_prompt):
              if isinstance(chunk, ToolCallEvent):  # this is the types.ToolCallEvent
                  yield _sse({"type": "tool_call", ...})
                  continue
              text_parts.append(chunk)
              yield _sse({"type": "token", "text": chunk})
      except Exception as exc:
          ...
      latency_ms = int((time.perf_counter() - start) * 1000)
      trace_id = db.record_trace(prompt, AgentResult(text="".join(text_parts), ...), latency_ms=latency_ms, agent_profile_name=agent_name)
      yield _sse({"type": "done", "trace_id": trace_id, "latency_ms": latency_ms})

  # Phase 33 changes: stream into a variable so terminal usage can be read after drain.
  # For Anthropic: stream_anthropic_async needs to surface the final_msg.usage; today the
  # generator wraps `async with client.messages.stream(...) as stream` and yields text + ToolCallEvent
  # without exposing the stream object. Phase 33 must add a way to read the terminal usage from
  # the generator (a final usage event yielded, or a sentinel ObservationEvent yielded, or a
  # local accumulator on a class wrapper). Read _providers/_anthropic.py:stream_anthropic_async
  # in the executor before touching this. NEVER persist 0 for a non-empty stream.

From src/horus_os/_providers/_anthropic.py:_parse_anthropic_response (the usage shape):

  def _parse_anthropic_response(response, provider, model) -> AgentResult:
      usage_obj = getattr(response, "usage", None)
      usage: dict[str, Any] = {}
      if usage_obj is not None:
          for key in ("input_tokens", "output_tokens", "cache_creation_input_tokens", "cache_read_input_tokens"):
              value = getattr(usage_obj, key, None)
              if value is not None:
                  usage[key] = value
      return AgentResult(..., usage=usage)

  # Phase 33 reads result.usage from each Conversation.send return inside run_agent_loop:
  #   input_tokens                 = result.usage.get("input_tokens", 0)
  #   output_tokens                = result.usage.get("output_tokens", 0)
  #   cache_creation_input_tokens  = result.usage.get("cache_creation_input_tokens", 0)
  #   cache_read_input_tokens      = result.usage.get("cache_read_input_tokens", 0)

From src/horus_os/_providers/_gemini.py:_parse_gemini_response (the usage shape):

  usage_obj = getattr(response, "usage_metadata", None)
  # keys: "prompt_token_count" (=> input_tokens), "candidates_token_count" (=> output_tokens),
  #       "total_token_count" (informational)
  # Gemini has no cache_* fields.

  # Phase 33 normalizes Gemini usage into the LLMCallEvent shape:
  #   input_tokens   = result.usage.get("prompt_token_count", 0)
  #   output_tokens  = result.usage.get("candidates_token_count", 0)
  #   cache_*        = 0

From src/horus_os/storage.py:Database.record_trace (Phase 33 modifies signature):

  def record_trace(
      self,
      prompt: str,
      result: AgentResult,
      *,
      parent_trace_id: str | None = None,
      agent_profile_name: str | None = None,
      latency_ms: int | None = None,
      status: str = "success",
      error_message: str | None = None,
  ) -> str:
      # Phase 33: add keyword arg `trace_id: str | None = None`.
      # When None, generate uuid4 (current behavior). When supplied, use it verbatim.
      # All existing callers (server/api.py:chat sync path, tests) keep working unchanged
      # because the keyword is optional and defaults preserve v0.3 behavior.

From src/horus_os/_providers/_anthropic.py:stream_anthropic_async (the SSE provider):

  async def stream_anthropic_async(prompt, *, model, max_tokens=DEFAULT_MAX_TOKENS, system=None):
      async with client.messages.stream(**request) as stream:
          async for text in stream.text_stream:
              yield text
          final_msg = await stream.get_final_message()
          for block in getattr(final_msg, "content", []) or []:
              if getattr(block, "type", None) == "tool_use":
                  yield ToolCallEvent(name=..., input=...)

  # Phase 33 design choice (executor must implement one of these):
  #   Option A (preferred): yield one terminal sentinel object _StreamUsage(usage=final_msg.usage)
  #     so the SSE handler in api.py can isinstance-check and extract usage without changing
  #     stream_anthropic_async's public iteration shape (text + ToolCallEvent only for
  #     existing consumers; new consumers receive the trailing sentinel and decide what to do).
  #   Option B: refactor stream_anthropic_async to a class with a `final_usage` property
  #     populated after iteration completes. More invasive.
  # Pick Option A unless a blocker emerges; document the choice in the task action.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body -> server/api.py | User-supplied `prompt` flows into agent run; never persisted into observability event fields beyond `prompt` going into existing `traces.prompt` (unchanged from v0.3) |
| Tool handler raise -> tool_invocations.error_message | Exception `str(exc)` may contain user-supplied paths/URLs/credentials per Pitfall 7/9; Phase 33 persists exception CLASS NAME only via `error_type`, never `str(exc)`, in any indexable column |
| Provider SDK response -> LLMCallEvent | Provider returns `usage` dict (token counts only, integers); no PII surface |
| Phase 33 lint guard scope | Adds `src/horus_os/server/api.py` to WATCHED_PATHS; `time.time()` would be silently accepted today in the SSE branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information disclosure | tool_invocations.error_message | mitigate | Tasks 3 + 7: populate `error_type` with `type(exc).__name__` only; `error_message` field set to the same class name string, NEVER `str(exc)` (which leaks user paths). Test `tests/observability/test_pitfall_9_tool_status_columns.py` asserts `error_message` does not contain a user-supplied input from the tool's invocation arguments. |
| T-33-02 | Tampering | latency_ms negative or wall-clock-rewound values | mitigate | Phase 32's `SQLitePersister._insert_*` already asserts `latency_ms >= 0`. Phase 33 keeps that assertion live; Task 7 adds a regression test pointing the runner at a sleep-then-rewind scenario to prove the gate fires. Lint guard extended to api.py (Task 6) prevents future SSE code from using `time.time()`. |
| T-33-03 | Denial of service | Slow subscriber blocks runner | accept | Phase 32 already wraps each subscriber call in `try/except BaseException`. Phase 33 only adds publishers; no new subscriber surface. Phase 38's OTel adapter handles its own batching. |
| T-33-04 | Information disclosure | SSE stream text accumulation | accept | Already accepted in v0.3: `text_parts` is built locally and persisted into `traces.response_text` (existing column). Phase 33 does not change this; observability `LLMCallEvent` carries token COUNTS only, not content. |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ObservationBus singleton accessor and wire SQLitePersister at create_app startup</name>
  <files>src/horus_os/observability/__init__.py, src/horus_os/server/api.py, tests/observability/test_bus.py</files>
  <read_first>
    - src/horus_os/observability/__init__.py (full; likely just re-exports today; this file becomes the singleton holder)
    - src/horus_os/observability/bus.py lines 133-176 (ObservationBus.subscribe/publish contract)
    - src/horus_os/observability/persist.py (full; SQLitePersister constructor + on_event)
    - src/horus_os/server/api.py lines 1-110 (create_app definition + app.state wiring of adapter_registry, tool_registry, same pattern Phase 33 follows)
    - .planning/research/ARCHITECTURE.md §1.1 "All events flow through: ObservationBus" + §4.1 "create_app wires: bus = ObservationBus(); bus.subscribe(SQLitePersister(db).on_event)"
    - .planning/phases/32-schema-migration-persistence-skeleton-v0-3-baseline/32-01-SUMMARY.md "What is NOT yet wired" section (Phase 32 ships infrastructure only; Phase 33's Task 1 is the wiring)
  </read_first>
  <behavior>
    - get_observation_bus() returns the same ObservationBus instance across calls within one process (module-level singleton)
    - reset_observation_bus_for_tests() (test-only helper) creates a fresh bus; subsequent get_observation_bus() returns the new one
    - create_app() instantiates the bus via get_observation_bus(), constructs SQLitePersister(db), subscribes it to the bus, and stores the bus on app.state.observation_bus for future code paths (Phase 34 CostAnnotator, Phase 38 OtelAdapter)
    - app.state.observation_bus is the same object get_observation_bus() returns
    - With no events published, persister never writes a row (no-op safety)
  </behavior>
  <action>
Modify `src/horus_os/observability/__init__.py`: add a module-level `_BUS: ObservationBus | None = None` and two functions `def get_observation_bus() -> ObservationBus` (lazy-creates and returns; idempotent) and `def reset_observation_bus_for_tests() -> ObservationBus` (sets `_BUS = None` then returns a fresh one from get_observation_bus). Re-export `ObservationBus`, `LLMCallEvent`, `ToolCallEvent`, `RunEndEvent`, `SQLitePersister`, `get_observation_bus`, `reset_observation_bus_for_tests` via `__all__`.

In `src/horus_os/server/api.py:create_app`, after the existing `db = Database(cfg.db_path)` wiring (the place where app.state.adapter_registry and app.state.tool_registry are set, near line 107), import `from horus_os.observability import get_observation_bus, SQLitePersister`, call `bus = get_observation_bus()`, `bus.subscribe(SQLitePersister(db).on_event)`, and `app.state.observation_bus = bus`. Use a single Database instance shared with the rest of create_app (do not open a second connection). This step is the wire Phase 32 deliberately left for Phase 33 (per 32-01-SUMMARY.md "What is NOT yet wired").

Add tests to `tests/observability/test_bus.py` (append; do not rewrite existing tests): `test_get_observation_bus_is_singleton` (two calls return the same object), `test_reset_observation_bus_for_tests_yields_fresh_instance` (after reset, the object differs from a prior call), `test_create_app_subscribes_sqlitepersister` (uses fastapi.testclient.TestClient, asserts `app.state.observation_bus` exists, that publishing a synthetic LLMCallEvent on it after init lands a row in `llm_calls` via the wired persister, first seed a `traces` row via `db.record_trace` so the trace_id FK is satisfied).
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/observability/test_bus.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `from horus_os.observability import get_observation_bus, SQLitePersister, reset_observation_bus_for_tests` succeeds
    - Two `get_observation_bus()` calls return `is` the same object
    - After `reset_observation_bus_for_tests()`, the subsequent `get_observation_bus()` returns a different object
    - `app.state.observation_bus` is set after `create_app()` returns
    - A synthetic LLMCallEvent published to `app.state.observation_bus` after a seeded trace lands one row in `llm_calls` (proves the persister is wired)
    - All Phase 32 tests in `tests/observability/test_bus.py` and `tests/observability/test_persister.py` continue to pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire LLMCallEvent capture into run_agent_loop with trace_id threading (Pitfall 1 substrate)</name>
  <files>src/horus_os/agent.py, src/horus_os/storage.py, tests/observability/test_runner_capture.py</files>
  <read_first>
    - src/horus_os/agent.py (full; the `run_agent_loop` body at lines 116-158 is the modification target)
    - src/horus_os/types.py lines 34-45 (the v0.3 `types.ToolCallEvent` which MUST NOT be replaced; it is imported by agent.py line 21)
    - src/horus_os/observability/bus.py lines 53-83 (LLMCallEvent dataclass; note the naming collision warning in lines 96-101)
    - src/horus_os/_providers/_anthropic.py lines 40-65 (the `usage` keys returned: input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens)
    - src/horus_os/_providers/_gemini.py lines 40-74 (the `usage` keys returned: prompt_token_count, candidates_token_count)
    - src/horus_os/storage.py lines 230-273 (`record_trace`; the signature change adds `trace_id: str | None = None` so the caller can pre-generate and pass it through)
    - .planning/research/PITFALLS.md lines 22-42 (Pitfall 1 full description; the per-iteration overwrite bug)
    - .planning/research/ARCHITECTURE.md lines 187-200 (Pattern 2 "Capture at the narrowest stable boundary")
  </read_first>
  <behavior>
    - `run_agent_loop` accepts a new keyword `trace_id: str | None = None`; when None, generates `uuid4().hex` internally so legacy callers still work
    - Each `Conversation.send` is wrapped in `time.perf_counter()` (NEVER `time.time()`)
    - After each send returns, an LLMCallEvent is published via `get_observation_bus().publish(...)` with: trace_id (the pre-generated one), iteration_idx (0, 1, 2, ...), provider, model (from result.provider / result.model), input_tokens / output_tokens / cache_creation_input_tokens / cache_read_input_tokens parsed from result.usage with provider-specific key normalization (Anthropic uses input_tokens, Gemini uses prompt_token_count), latency_ms (int milliseconds), status="success" / error_type=None on success
    - On `Conversation.send` exception: publishes LLMCallEvent with status="error", error_type=type(exc).__name__, input_tokens=0, output_tokens=0, latency_ms set, then re-raises (the exception still propagates; capture is observability not error handling)
    - The naming collision: `from horus_os.observability.bus import LLMCallEvent, ToolCallEvent as ObsToolCallEvent`, the existing `from horus_os.types import ToolCallEvent` import is PRESERVED unchanged (still used by run_agent_stream's yield surface)
    - `Database.record_trace` accepts `trace_id: str | None = None`; when supplied, uses it verbatim; when None, generates uuid4 (current behavior)
  </behavior>
  <action>
Modify `src/horus_os/storage.py:record_trace`: add keyword `trace_id: str | None = None` as the first new keyword in the signature. Replace `trace_id = uuid.uuid4().hex` (line 242) with `trace_id = trace_id if trace_id is not None else uuid.uuid4().hex`. No other change to record_trace. All 30+ existing callers continue to work; api.py's chat sync handler will pass `trace_id=...` after Task 4.

Modify `src/horus_os/agent.py`:
1. Add `import time` and `import uuid` at the top with the existing stdlib imports.
2. Add `from horus_os.observability import get_observation_bus` and `from horus_os.observability.bus import LLMCallEvent, ToolCallEvent as ObsToolCallEvent, RunEndEvent` to the imports. Add a one-line comment on the ObsToolCallEvent alias: `# ObsToolCallEvent aliases the observability ToolCallEvent to avoid collision with horus_os.types.ToolCallEvent which run_agent_stream yields.`
3. Keep the existing `from horus_os.types import AgentResult, Tool, ToolCallEvent, ToolResult` line verbatim (do not remove `ToolCallEvent` from this import; it is the streaming-surface event).
4. In `run_agent_loop` (line 116), add keyword `trace_id: str | None = None` to the signature.
5. Inside the function body, after `_check_provider(provider)` and the budget setup, add `_trace_id = trace_id if trace_id is not None else uuid.uuid4().hex` and `bus = get_observation_bus()` and `iteration_idx = 0`.
6. Wrap the first `conversation.send(prompt=prompt, tools=tools)` in: `_t0 = time.perf_counter()`, try `result = conversation.send(...)`, except BaseException as exc: publish error LLMCallEvent (iteration_idx=0, status="error", error_type=type(exc).__name__, latency_ms=int((time.perf_counter()-_t0)*1000), input_tokens=0, output_tokens=0, provider=provider, model=(model or "")) and re-raise. On success, compute `_latency_ms = int((time.perf_counter()-_t0)*1000)` and publish a success LLMCallEvent with usage extracted via a small helper `_extract_usage(provider, result.usage) -> tuple[int,int,int,int]` that returns (input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens). For provider=="anthropic": read `usage.get("input_tokens", 0)`, `usage.get("output_tokens", 0)`, `usage.get("cache_creation_input_tokens", 0)`, `usage.get("cache_read_input_tokens", 0)`. For provider=="gemini": read `usage.get("prompt_token_count", 0)`, `usage.get("candidates_token_count", 0)`, 0, 0.
7. Inside the `while result.tool_uses` loop, increment iteration_idx BEFORE the next send. Repeat the same perf_counter + publish pattern around `result = conversation.send(tool_results=outcomes, tools=tools)`.
8. After the loop exits, do NOT publish RunEndEvent. The RunEndEvent is owned by the CALLER (see Task 4) so it fires AFTER `db.record_trace` writes the row the rollup UPDATE matches. The runner only publishes per-iteration LLMCallEvents (and per-tool ToolCallEvents via Task 3). Add a one-line comment at the bottom of the function: `# RunEndEvent is published by the caller (server/api.py:chat) AFTER db.record_trace, so the rollup UPDATE matches an existing traces row. See Phase 33 Task 4.`

Create `tests/observability/test_runner_capture.py` with tests:
- `test_run_agent_loop_publishes_llm_call_per_iteration`: stub Conversation that returns one tool_use for first two sends and a text-only third send (mirrors `scripts/capture_v0_3_baseline.py:_StubConversation`). Pre-generate a trace_id. Subscribe a list-collecting handler. Run `run_agent_loop(..., trace_id=trace_id)`. Assert three LLMCallEvents collected, iteration_idx values [0, 1, 2], all carry trace_id, provider, model. Assert ZERO RunEndEvents collected (the runner does not publish them; Task 4 ships the integration test that covers the caller-published RunEndEvent).
- `test_run_agent_loop_extracts_anthropic_usage`: stub returns AgentResult with usage={"input_tokens": 100, "output_tokens": 50, "cache_read_input_tokens": 25, "cache_creation_input_tokens": 10}. Assert published LLMCallEvent has input_tokens=100, output_tokens=50, cache_read_input_tokens=25, cache_creation_input_tokens=10.
- `test_run_agent_loop_extracts_gemini_usage`: provider="gemini", stub returns usage={"prompt_token_count": 200, "candidates_token_count": 80}. Assert LLMCallEvent.input_tokens=200, output_tokens=80, cache_read=0, cache_creation=0.
- `test_run_agent_loop_generates_trace_id_when_omitted`: call without trace_id kwarg; assert the published LLMCallEvent has a 32-char hex trace_id.
- `test_run_agent_loop_publishes_error_event_and_reraises`: stub whose first send raises ValueError("boom"). Assert one LLMCallEvent published with status="error", error_type="ValueError", input_tokens=0; assert the ValueError propagates.

Use `reset_observation_bus_for_tests()` at test setup to isolate from other tests.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/observability/test_runner_capture.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <done>
    - All five tests in `test_runner_capture.py` pass
    - `python scripts/lint_no_wallclock.py` exits 0 (agent.py uses perf_counter, not time())
    - Existing `tests/test_agent_loop.py` and `tests/test_agent.py` continue to pass with the new optional `trace_id` keyword
    - Existing `tests/test_storage.py` tests for `record_trace` continue to pass (optional keyword is additive)
    - `from horus_os.types import ToolCallEvent` import in agent.py is still present (run_agent_stream needs it)
    - The naming collision is resolved via the `as ObsToolCallEvent` alias with a code comment
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire ToolCallEvent capture into tools/loop.py:_execute_one (METRIC-02 substrate, Pitfall 9)</name>
  <files>src/horus_os/tools/loop.py, src/horus_os/agent.py, tests/observability/test_tool_loop_capture.py</files>
  <read_first>
    - src/horus_os/tools/loop.py (full; the `_execute_one` body at lines 76-85 is the modification target; existing perf_counter timing at line 78 stays unchanged)
    - src/horus_os/agent.py modified by Task 2 (the `execute_tool_uses` call inside `run_agent_loop` at line 156, Task 3 threads trace_id through this call)
    - src/horus_os/observability/bus.py lines 85-114 (ToolCallEvent dataclass shape)
    - .planning/research/PITFALLS.md lines 221-249 (Pitfall 9: status enum, retry_count NULL allowed, error_type as class name only, NEVER user-supplied content)
  </read_first>
  <behavior>
    - `_execute_one` accepts a new keyword `trace_id: str | None = None`. When None, no event is published (back-compat with direct callers including unit tests).
    - When trace_id is provided, after the existing `outcome.latency_ms = int(...)` line, an ObsToolCallEvent (aliased) is published with: trace_id, tool_name=use.name, latency_ms (from outcome), status="success" if outcome.error is None else "error", error_type=outcome.error.split(":", 1)[0] if outcome.error else None (Python exception strings are formatted as "ClassName: message" by the existing line 83), error_message=outcome.error.split(":", 1)[0] if outcome.error else None (CLASS NAME ONLY, never the message body per Pitfall 9; this is intentional duplication into the column so a Phase 35 query can read it from `error_message` alone without re-splitting), retry_count=None (SDK does not expose retry counts; documented in Pitfall 4 and Pitfall 9), output_size=len(str(outcome.output).encode("utf-8")) if outcome.output is not None else 0.
    - `execute_tool_uses` accepts a new keyword `trace_id: str | None = None` and forwards it into every `_execute_one` call (sequential, single-delegate, and parallel-delegate paths).
    - `run_agent_loop` (modified in Task 2) now calls `execute_tool_uses(registry, result, on_log=on_tool_result, trace_id=_trace_id)` so tools get the same trace_id as the LLM calls.
  </behavior>
  <action>
Modify `src/horus_os/tools/loop.py`:
1. Add imports at the top: `from horus_os.observability import get_observation_bus` and `from horus_os.observability.bus import ToolCallEvent as ObsToolCallEvent`. Add a one-line comment on the alias.
2. Add `trace_id: str | None = None` keyword to `execute_tool_uses` signature.
3. Add `trace_id: str | None = None` keyword to `_execute_one` signature.
4. After the existing `outcome.latency_ms = int(...)` line in `_execute_one`, add a conditional publish block:
   ```
   # gated on trace_id so direct callers (unit tests, scripts) without an observability context
   # do not publish phantom events
   ```
   When trace_id is not None: compute `_status = "success" if outcome.error is None else "error"`; compute `_error_class = outcome.error.split(":", 1)[0] if outcome.error else None` (the existing line 83 formats `outcome.error` as `f"{type(exc).__name__}: {exc}"`, so splitting on `:` yields just the class name); compute `_output_size = len(str(outcome.output).encode("utf-8")) if outcome.output is not None else 0`; publish via `get_observation_bus().publish(ObsToolCallEvent(trace_id=trace_id, tool_name=use.name, latency_ms=outcome.latency_ms or 0, status=_status, error_type=_error_class, error_message=_error_class, retry_count=None, output_size=_output_size))`. CRITICAL: `error_message` gets the class name, NOT `outcome.error` (which contains the full message and may have user-supplied paths per Pitfall 9).
5. In each `_execute_one(registry, use)` call inside `execute_tool_uses` (sequential at line 53, single-delegate at line 60, and the ThreadPoolExecutor.submit at line 66), pass `trace_id=trace_id` through.

Modify `src/horus_os/agent.py` (the one line touched by Task 2): change `outcomes = execute_tool_uses(registry, result, on_log=on_tool_result)` to `outcomes = execute_tool_uses(registry, result, on_log=on_tool_result, trace_id=_trace_id)`.

Create `tests/observability/test_tool_loop_capture.py` with:
- `test_successful_tool_publishes_event_with_status_success`: registry with a noop tool returning "ok"; call `_execute_one(registry, ToolUse(id="x", name="noop", input={}), trace_id="abc123")`. Subscribe a collector. Assert one ToolCallEvent collected with trace_id="abc123", tool_name="noop", status="success", error_type=None, error_message=None, retry_count=None, output_size=2 (len of "ok" bytes).
- `test_failing_tool_publishes_event_with_class_name_only`: registry with a tool that raises `ValueError("/Users/santino/secret/path leaked")`. Assert ToolCallEvent.status="error", error_type="ValueError", error_message="ValueError" (class name only, assert the literal "/Users/santino/secret/path" does NOT appear anywhere in the event fields).
- `test_omitted_trace_id_publishes_nothing`: call `_execute_one(registry, use)` without trace_id; assert the collector received zero events.
- `test_execute_tool_uses_threads_trace_id_into_all_paths`: build an AgentResult with three tool_uses (one noop, two delegate_to_agent, needs a delegate handler that returns ok). Call `execute_tool_uses(registry, result, trace_id="zzz")`. Assert three ToolCallEvents all carry trace_id="zzz".

Use `reset_observation_bus_for_tests()` at test setup.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/observability/test_tool_loop_capture.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <done>
    - All four tests pass
    - `python scripts/lint_no_wallclock.py` exits 0
    - Existing `tests/test_tool_loop.py` and `tests/test_delegation.py` continue to pass with the new optional `trace_id` keyword
    - Failing-tool tests prove the error_message column contains the class name only (never user-supplied content per Pitfall 9 and the T-33-01 mitigation)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration test for the 3-iteration rollup (Pitfall 1 structural fix) and wire api.py chat sync path to pass trace_id</name>
  <files>src/horus_os/server/api.py, tests/observability/test_run_end_rollup_integration.py</files>
  <read_first>
    - src/horus_os/server/api.py lines 270-320 (the chat sync handler `chat()` at the POST /api/chat route; Phase 33 modifies it to pre-generate trace_id and pass into both run_agent_loop and record_trace)
    - src/horus_os/agent.py modified by Tasks 2 and 3
    - src/horus_os/observability/persist.py lines 156-203 (the `_rollup_trace` UPDATE; this is what RunEndEvent triggers; the rollup MATCHES on `WHERE trace_id = ?` so the trace_id in the LLMCallEvents, the RunEndEvent, AND the existing record_trace call MUST be the same)
    - .planning/research/PITFALLS.md lines 22-42 (Pitfall 1; the regression test specified at line 32 is the canonical assertion this task ships)
  </read_first>
  <behavior>
    - RunEndEvent ownership: the CALLER publishes RunEndEvent, NOT `run_agent_loop`. Rationale: the persister's `_rollup_trace` runs `UPDATE traces SET ... WHERE trace_id = ?`, so the traces row must already exist when RunEndEvent fires. `db.record_trace` is the writer, and it runs in the caller (api.py) AFTER `run_agent_loop` returns. So the publish must happen AFTER `db.record_trace` in the caller. Task 2 already excludes the publish from `run_agent_loop`.
    - The POST /api/chat handler: (a) pre-generates `_trace_id = uuid.uuid4().hex`, (b) passes `trace_id=_trace_id` into `run_agent_loop`, (c) on return, calls `db.record_trace(..., trace_id=_trace_id)`, (d) AFTER record_trace, publishes `RunEndEvent(trace_id=_trace_id, latency_ms=<full_handler_wall_time>)`.
    - The runner-measured latency is unaffected; the api.py handler already times its own wall-clock via `time.perf_counter()` at line 289, and that's what goes into RunEndEvent.latency_ms.
    - This task ships: (1) the api.py wiring (sync chat + error path), (2) the Pitfall 1 canonical regression test that proves a 3-iteration run yields total_input_tokens=300 instead of the v0.3 "last iteration overwrites" 100.
  </behavior>
  <action>
Task 2 already excludes the RunEndEvent publish from `run_agent_loop` (step 8 of Task 2's action). This task only adds the caller-side wiring; do NOT re-touch `run_agent_loop`.

**Modify `src/horus_os/server/api.py` chat sync handler (around line 285-319)**:
1. Add imports at top of api.py: `from horus_os.observability import get_observation_bus` and `from horus_os.observability.bus import RunEndEvent`. Add `import uuid` if not present.
2. In the chat() handler, BEFORE `start = time.perf_counter()`, generate `_trace_id = uuid.uuid4().hex`.
3. Pass `trace_id=_trace_id` into `run_agent_loop(...)`.
4. In the success path: replace `trace_id = db.record_trace(prompt, result, latency_ms=latency_ms)` with `db.record_trace(prompt, result, latency_ms=latency_ms, trace_id=_trace_id)` then `trace_id = _trace_id`. AFTER record_trace, publish: `get_observation_bus().publish(RunEndEvent(trace_id=_trace_id, latency_ms=latency_ms))`. This fires the rollup which updates the just-inserted traces row.
5. In the exception path (line 299-310): same pattern, call record_trace with trace_id=_trace_id, then publish RunEndEvent with the same trace_id and latency_ms. Status remains "error" on the traces row (record_trace already accepts status keyword).

Create `tests/observability/test_run_end_rollup_integration.py`:
- `test_three_iteration_rollup`: This is the Pitfall 1 canonical regression test from PITFALLS.md line 32. Setup: real Database with init(), reset_observation_bus_for_tests(), subscribe SQLitePersister(db) to the bus. Stub Conversation that returns AgentResult with usage={"input_tokens": 100, "output_tokens": 50} on each send; first two sends include one tool_use, third send returns text-only (mirrors `scripts/capture_v0_3_baseline.py:_StubConversation` exactly). Pre-generate trace_id. Call: (a) `result = run_agent_loop("prompt", registry=registry, provider="anthropic", trace_id=trace_id)`; (b) `db.record_trace("prompt", result, latency_ms=42, trace_id=trace_id)`; (c) `get_observation_bus().publish(RunEndEvent(trace_id=trace_id, latency_ms=42))`. Then:
  - `SELECT COUNT(*) FROM llm_calls WHERE trace_id = ?` returns 3 (NOT 1; this is the Pitfall 1 structural fix proof)
  - `SELECT total_input_tokens, total_output_tokens, total_duration_ms FROM traces WHERE trace_id = ?` returns (300, 150, 42)
  - `SELECT total_cost_usd FROM traces WHERE trace_id = ?` returns None (Phase 34's job; NULL is honest per Pitfall 5)
- `test_run_end_rollup_with_tool_invocations`: same stub but assert `SELECT COUNT(*) FROM tool_invocations WHERE trace_id = ?` returns 2 (the two iterations that emitted tool_uses).
- `test_api_chat_endpoint_e2e_rollup`: TestClient against the real create_app, monkey-patch `_new_conversation` in `horus_os.agent` to the stub, POST /api/chat with `{"prompt": "test"}`, assert response has trace_id; SELECT from the test DB on that trace_id; assert llm_calls has 3 rows and traces has total_input_tokens=300.

Use `reset_observation_bus_for_tests()` at test setup.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/observability/test_run_end_rollup_integration.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <done>
    - All three tests pass
    - `traces.total_input_tokens == 300` for the 3-iteration loop with stubbed 100 input tokens per iteration (Pitfall 1 fixed)
    - `traces.total_cost_usd IS NULL` (Phase 34 will populate; do not write 0)
    - Existing `tests/test_server_api.py` tests for POST /api/chat continue to pass (the additional trace_id wiring is internal; the response shape is unchanged)
    - `python scripts/lint_no_wallclock.py` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: SSE branch capture in server/api.py:_event_stream with terminal-usage extraction (Pitfall 2)</name>
  <files>src/horus_os/_providers/_anthropic.py, src/horus_os/_providers/_gemini.py, src/horus_os/server/api.py, tests/observability/test_sse_capture.py</files>
  <read_first>
    - src/horus_os/server/api.py lines 321-411 (the full /api/chat/stream handler and `_event_stream` body)
    - src/horus_os/_providers/_anthropic.py lines 119-152 (stream_anthropic_async; the `async with client.messages.stream(...) as stream` block; `stream.get_final_message()` already called at line 146 for tool_use extraction)
    - src/horus_os/_providers/_gemini.py lines 137-175 (stream_gemini_async; chunks come through `client.aio.models.generate_content_stream`; `usage_metadata` lives on the response after iteration)
    - src/horus_os/agent.py lines 74-113 (run_agent_stream which fans out to provider-specific streams)
    - src/horus_os/types.py lines 34-45 (the types.ToolCallEvent yielded mid-stream, Phase 33's terminal usage event is a SEPARATE class to avoid collision)
    - .planning/research/PITFALLS.md lines 45-66 (Pitfall 2: SSE silent $0; explicit guidance to read stream.get_final_message().usage for Anthropic and response.usage_metadata for Gemini; NEVER persist 0 for a non-empty stream; fall back to char-length estimate with estimated=1)
  </read_first>
  <behavior>
    - The SSE handler measures wall-clock from before run_agent_stream starts to after it drains via time.perf_counter() (already does this)
    - After the stream drains, the handler reads the terminal usage and publishes an LLMCallEvent + RunEndEvent BEFORE the existing db.record_trace + the final SSE done payload
    - For Anthropic: stream_anthropic_async is modified to yield a final sentinel object (`_StreamUsage`) carrying the final_msg.usage dict. The SSE handler isinstance-checks each chunk; non-string non-ToolCallEvent non-_StreamUsage chunks would never happen (defensive: ignore).
    - For Gemini: stream_gemini_async is modified to yield a final sentinel `_StreamUsage(usage={"prompt_token_count": N, "candidates_token_count": M})` after the last text chunk, reading from the last chunk's `usage_metadata` if present (Gemini surfaces it on the final chunk per the SDK).
    - If terminal usage is unavailable (stream errored mid-flight before the sentinel was reached): the SSE handler computes a fallback estimate: `_estimated_output_tokens = max(1, len("".join(text_parts)) // 4)` (rough chars-per-token heuristic from Pitfall 2 guidance) and publishes an LLMCallEvent with `input_tokens=0, output_tokens=_estimated_output_tokens, status="success"`. NEVER `output_tokens=0` for a non-empty stream, that is the Pitfall 2 silent-$0 anti-pattern.
    - The `_StreamUsage` sentinel is defined inside `_providers/_anthropic.py` (private; underscore-prefixed) and re-exported from `_providers/_gemini.py` with the same shape; the SSE handler imports both or uses isinstance against a unified shape. Cleanest: define `_StreamUsage` once in `src/horus_os/_providers/__init__.py` (or a shared `_types.py`) and import from both provider modules.
  </behavior>
  <action>
Create `src/horus_os/_providers/_stream_types.py` (NEW module, internal): define a single private dataclass `@dataclass class _StreamUsage:` with one field `usage: dict[str, Any]`. Add `__all__ = ["_StreamUsage"]`. This is the terminal sentinel both providers yield.

Modify `src/horus_os/_providers/_anthropic.py:stream_anthropic_async`:
- Import `_StreamUsage` from `._stream_types`.
- After the existing `final_msg = await stream.get_final_message()` line (line 146), AFTER the for-loop that yields ToolCallEvents (line 147-152), add: `usage_dict: dict[str, Any] = {}` then `usage_obj = getattr(final_msg, "usage", None); if usage_obj is not None: for key in ("input_tokens","output_tokens","cache_creation_input_tokens","cache_read_input_tokens"): value = getattr(usage_obj, key, None); if value is not None: usage_dict[key] = value` then `yield _StreamUsage(usage=usage_dict)`.

Modify `src/horus_os/_providers/_gemini.py:stream_gemini_async`:
- Import `_StreamUsage`.
- Accumulate the last seen `chunk.usage_metadata` (Gemini sets it on the final chunk per the SDK). Inside the async-for: track `_last_usage = getattr(chunk, "usage_metadata", None) or _last_usage` (initialize to None before the loop). After the loop, after the `for event in tool_events: yield event` line, if `_last_usage is not None`: extract `prompt_token_count` and `candidates_token_count` into a dict, then `yield _StreamUsage(usage=usage_dict)`. If `_last_usage` is None, still yield `_StreamUsage(usage={})` so the SSE handler can hit its estimation fallback.

Modify `src/horus_os/server/api.py:_event_stream`:
1. Import `from horus_os._providers._stream_types import _StreamUsage` (acceptable cross-import; this is a server-side consumer of a private provider type). Import `from horus_os.observability import get_observation_bus` and `from horus_os.observability.bus import LLMCallEvent, RunEndEvent` if not already imported by Task 4. Import `import uuid`.
2. Before `start = time.perf_counter()` inside `_event_stream`, generate `_trace_id = uuid.uuid4().hex` and initialize `_terminal_usage: dict[str, Any] = {}`.
3. In the async-for loop (line 355-371), add a branch: `if isinstance(chunk, _StreamUsage): _terminal_usage = chunk.usage; continue` BEFORE the existing `isinstance(chunk, ToolCallEvent)` branch. The sentinel is consumed by the SSE handler; it never reaches the wire.
4. In the success path (after `latency_ms = int(...)`): compute the LLMCallEvent's token fields. If `_terminal_usage` has tokens: use them with provider-specific normalization (Anthropic keys are direct; Gemini: prompt_token_count -> input_tokens, candidates_token_count -> output_tokens). If `_terminal_usage` is empty AND `text_parts` non-empty: fallback `_est = max(1, len("".join(text_parts)) // 4); input_tokens=0; output_tokens=_est` and add a one-line code comment: `# Pitfall 2: never persist 0 output_tokens for a non-empty stream; estimate via 4-char-per-token heuristic per PITFALLS.md line 55.` Publish LLMCallEvent with trace_id=_trace_id, iteration_idx=0, provider=provider, model=model, input_tokens=..., output_tokens=..., cache_creation_input_tokens=..., cache_read_input_tokens=..., latency_ms=latency_ms, status="success".
5. Call existing `trace_id = db.record_trace(prompt, AgentResult(...), latency_ms=latency_ms, agent_profile_name=agent_name, trace_id=_trace_id)` then `trace_id = _trace_id`. Publish `get_observation_bus().publish(RunEndEvent(trace_id=_trace_id, latency_ms=latency_ms))` AFTER record_trace (sequencing per Task 4).
6. In the exception path: publish an error LLMCallEvent with status="error", error_type=type(exc).__name__, plus the fallback-estimated output_tokens (Pitfall 2 still applies on mid-flight errors with partial text), then record_trace with trace_id=_trace_id, then RunEndEvent.

Create `tests/observability/test_sse_capture.py`:
- `test_sse_anthropic_usage_persisted`: Monkey-patch `horus_os._providers._anthropic.stream_anthropic_async` to an async generator that yields three text chunks then a `_StreamUsage(usage={"input_tokens": 250, "output_tokens": 120, "cache_read_input_tokens": 30, "cache_creation_input_tokens": 10})`. POST /api/chat/stream via TestClient. Read the SSE response. Then `SELECT input_tokens, output_tokens, cache_read_input_tokens, cache_creation_input_tokens, latency_ms FROM llm_calls WHERE trace_id = ?` returns (250, 120, 30, 10, >=0). Assert `SELECT total_input_tokens FROM traces WHERE trace_id = ?` == 250. Assert `total_cost_usd IS NULL`.
- `test_sse_gemini_usage_normalized`: monkey-patch stream_gemini_async to yield text then `_StreamUsage(usage={"prompt_token_count": 400, "candidates_token_count": 150})`. POST with provider="gemini". Assert `input_tokens=400, output_tokens=150, cache_*=0`.
- `test_sse_empty_terminal_usage_falls_back_to_estimate`: monkey-patch stream_anthropic_async to yield three text chunks ("hello", " ", "world") then `_StreamUsage(usage={})`. POST. Assert `llm_calls.output_tokens >= 1` (estimated, never 0). Assert it equals `max(1, len("hello world") // 4)` == 2.
- `test_sse_never_persists_zero_for_nonempty_stream`: same stub as previous; assert `SELECT COUNT(*) FROM llm_calls WHERE input_tokens=0 AND output_tokens=0` == 0 over the test set. This is the canonical Pitfall 2 warning-sign check from PITFALLS.md line 61.
- `test_sse_error_path_publishes_error_event_with_estimated_tokens`: stub that yields "partial" text then raises ConnectionError. Assert the persisted llm_calls row has status="error", error_type="ConnectionError", output_tokens >= 1.

Use `reset_observation_bus_for_tests()` at test setup.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/observability/test_sse_capture.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <done>
    - All five tests pass
    - Pitfall 2 fixed: no llm_calls row from /api/chat/stream lands at `input_tokens=0 AND output_tokens=0` when the stream produced text
    - Existing `tests/test_server_stream.py` and `tests/test_streaming_partial_failure.py` continue to pass (the `_StreamUsage` sentinel is private and existing tests don't subscribe to it)
    - `python scripts/lint_no_wallclock.py` exits 0, but ONLY because Task 6 extends WATCHED_PATHS to include server/api.py FIRST; if Task 6 runs before this task, this is enforced
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Extend lint_no_wallclock to cover server/api.py and document the Pitfall 4 latency_ms contract</name>
  <files>scripts/lint_no_wallclock.py, src/horus_os/observability/bus.py, tests/test_lint_no_wallclock.py</files>
  <read_first>
    - scripts/lint_no_wallclock.py (full; 107 lines; WATCHED_PATHS list near the top defines the scope; current scope is observability/ + agent.py + tools/loop.py)
    - tests/test_lint_no_wallclock.py (full; the pytest wrapper that calls the script)
    - src/horus_os/observability/bus.py lines 54-83 (the existing LLMCallEvent docstring already has the Pitfall 4 contract for latency_ms; Task 6 strengthens it with a TTFT-not-captured note)
    - .planning/research/PITFALLS.md lines 69-89 (Pitfall 3 enforcement scope: explicit name of `agent.py`, `tools/loop.py`, and observability/) and lines 93-112 (Pitfall 4: latency contract docstring + TTFT mention)
    - The Phase 33 success criterion 3 from ROADMAP: "a ruff/grep CI rule fails the build if `time.time()` appears inside `horus_os/observability/`, `agent.py`, or `tools/loop.py`". Phase 33 ALSO needs to cover `server/api.py` because Task 5 puts perf_counter code in the SSE branch there.
  </read_first>
  <behavior>
    - WATCHED_PATHS in scripts/lint_no_wallclock.py includes `src/horus_os/server/api.py`
    - The Pitfall 3 negative test (injecting `time.time()` into api.py) fails the gate with the offending file:line
    - LLMCallEvent docstring includes a NOTE that latency_ms does NOT include TTFT (Pitfall 4) and a one-line forward-reference: "ttft_ms is a future column under consideration"
    - The PITFALLS.md docstring exemption (triple-quoted regions) keeps working: the lint guard does not self-trip on the contract docstring that NAMES `time.time()` as forbidden
  </behavior>
  <action>
Modify `scripts/lint_no_wallclock.py`: locate the WATCHED_PATHS list (near the top, before the main scan loop). Add `"src/horus_os/server/api.py"` as a new entry. Update the docstring at the top to reflect the new scope (mention server/api.py alongside observability/, agent.py, tools/loop.py).

Modify `src/horus_os/observability/bus.py` LLMCallEvent docstring (lines 54-66): add a NOTE paragraph at the end of the existing docstring (triple-quoted region, the lint guard will exempt it):

```
NOTE on Pitfall 4 (PITFALLS.md): latency_ms is wall-clock end to end including SDK
retries, backoff, queueing, and stream drain. It does NOT include time-to-first-token
(TTFT) which is the actual UX number for streaming chat. A `ttft_ms` column is a
future v0.5 consideration and is intentionally NOT a Phase 33 deliverable.
```

Modify `tests/test_lint_no_wallclock.py` (or add a sibling test if the existing file is structured as a single function): add `test_lint_no_wallclock_includes_server_api`. The test asserts `"src/horus_os/server/api.py"` appears in the `WATCHED_PATHS` constant imported from scripts.lint_no_wallclock (or, if importing the script is awkward, the test runs the script with `--show-paths` flag, add this flag to the script if it does not exist, printing the WATCHED_PATHS list one per line).

Add `test_lint_guard_fires_on_api_py_violation`: write a fake test that creates a temporary Python file with `time.time()` in it, monkey-patches `WATCHED_PATHS` to point at the temp file, runs the scan, asserts it exits with status 1. (This proves the scanner mechanics still work after the path-list change.)
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && python scripts/lint_no_wallclock.py && pytest tests/test_lint_no_wallclock.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `python scripts/lint_no_wallclock.py` exits 0 with all four watched paths clean
    - `src/horus_os/server/api.py` appears in WATCHED_PATHS
    - `test_lint_no_wallclock_includes_server_api` passes
    - Existing `tests/test_lint_no_wallclock.py` tests continue to pass
    - LLMCallEvent docstring carries the Pitfall 4 latency_ms contract paragraph
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Negative-latency regression + Pitfall 9 tool-status / class-name-only assertions</name>
  <files>tests/observability/test_pitfall_3_negative_latency.py, tests/observability/test_pitfall_9_tool_status_columns.py</files>
  <read_first>
    - src/horus_os/observability/persist.py lines 92-95 and 128-131 (the two `assert event.latency_ms >= 0` guards that Phase 32 already shipped; Task 7 ships regression tests that prove the guards still fire after Phase 33's wiring)
    - tests/observability/test_persister.py (the existing Phase 32 test_negative_latency_rejects test; Task 7 adds an integration-level companion that exercises the FULL runner path)
    - .planning/research/PITFALLS.md lines 69-89 (Pitfall 3) and lines 221-249 (Pitfall 9)
    - src/horus_os/tools/loop.py modified by Task 3 (error_message column populated with CLASS NAME ONLY)
  </read_first>
  <behavior>
    - A negative latency_ms ANYWHERE in the publish chain results in an AssertionError raised by the persister (which is then swallowed by ObservationBus.publish per the bus contract); the row is NOT inserted; a follow-up SELECT returns zero rows
    - When a tool raises `ValueError("/home/santino/secret/path leaked")`, the persisted tool_invocations row has `error_type='ValueError'`, `error_message='ValueError'` (class name only), and the literal string `/home/santino/secret/path` does NOT appear in any column
    - retry_count column is NULL for SDK-driven tools (Anthropic SDK does not surface retry counts per Pitfall 9 design)
    - status column takes values 'success' or 'error' only (no 'retry_then_success' yet, that's a future Pitfall 9 enhancement, not Phase 33)
  </behavior>
  <action>
Create `tests/observability/test_pitfall_3_negative_latency.py`:
- `test_persister_swallows_negative_latency_publish`: Make a Database, subscribe SQLitePersister, publish an LLMCallEvent with latency_ms=-5. The persister's assertion fires; ObservationBus swallows. `SELECT COUNT(*) FROM llm_calls WHERE trace_id=?` returns 0. Verify the negative row was rejected (Pitfall 3 substrate still active after Phase 33 wiring).
- `test_persister_swallows_negative_latency_tool_call`: same shape for a ToolCallEvent with latency_ms=-1. Zero rows in tool_invocations.

Create `tests/observability/test_pitfall_9_tool_status_columns.py`:
- `test_tool_failure_records_class_name_only`: Real Database + persister wired to bus. Build a registry with a tool whose handler raises `ValueError("/Users/santino/secret/path/credentials.json leaked")`. Pre-generate trace_id. Seed a traces row via db.record_trace with that trace_id. Run `execute_tool_uses(registry, AgentResult(tool_uses=[ToolUse(id="x", name="leakytool", input={})]), trace_id=trace_id)`. SELECT `status, error_type, error_message` from tool_invocations WHERE trace_id=?: assert `(status, error_type, error_message) == ("error", "ValueError", "ValueError")`. Assert `"/Users/santino/secret/path"` does NOT appear in any column of that row (concatenate all columns via SELECT * and check the literal absent).
- `test_tool_success_records_status_success_with_null_error`: noop tool returning "ok". Assert row has `status="success"`, `error_type IS NULL`, `error_message IS NULL`, `retry_count IS NULL` (Phase 33 leaves retry_count NULL per Pitfall 9 + research clarification on Anthropic SDK).
- `test_retry_count_is_null_for_sdk_driven_tools`: same setup; assert `SELECT retry_count FROM tool_invocations WHERE tool_name='noop'` returns None. Document in a code comment that retry_count is best-effort and Phase 33's research validation confirmed Anthropic SDK does not surface per-call retry counts without monkey-patching transport.

Use `reset_observation_bus_for_tests()` at test setup.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/observability/test_pitfall_3_negative_latency.py tests/observability/test_pitfall_9_tool_status_columns.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All five tests pass
    - The Pitfall 3 persister assertion still gates writes after Phase 33 wiring
    - Tool failure rows persist class name only (T-33-01 mitigation verified)
    - retry_count stays NULL with a code comment explaining the SDK limitation per Pitfall 9
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Capture-overhead benchmark test (METRIC-05, TEST-12)</name>
  <files>tests/perf/test_capture_overhead.py</files>
  <read_first>
    - tests/perf/v0_3_baseline.json (full; the JSON shape is `{"samples": [{"os": "darwin", "python": "3.12", "horus_os_version": "0.3.0", "median_3_iteration_loop_ms": 0.005, "n_samples": 20, "captured_at": "..."}]}`)
    - scripts/capture_v0_3_baseline.py (full; mirror its measurement style, `_StubConversation`, monkey-patch `agent_module._new_conversation`, N=20 medians via `time.perf_counter` + `statistics.median`)
    - src/horus_os/agent.py modified by Tasks 2 + 3 (the wired runner is what the Phase 33 benchmark times)
    - .planning/research/PITFALLS.md lines 196-219 (Pitfall 8: SQLite write amplification; capture-overhead benchmark is the guard)
    - The Phase 33 success criterion 5 from ROADMAP: "Capture-overhead benchmark in CI runs the fixture 5-iteration / 3-tool-call loop on the 3-OS matrix and asserts total wall-clock is within 50ms of the Phase 32 baseline"
  </read_first>
  <behavior>
    - The benchmark loads `tests/perf/v0_3_baseline.json` and finds the entry matching the current `(sys.platform-normalized, "python_major.python_minor")`
    - If no matching entry exists, the test SKIPS (not fails) with a clear xfail/skip message so CI does not block on unseeded combos (per planning-context guidance "SKIP-when-no-matching-entry guard")
    - The benchmark runs a fixture 5-iteration / 3-tool-call loop N=20 times against the FULL Phase 33 wiring (real ObservationBus, real SQLitePersister writing to a tmp SQLite DB, real LLMCallEvent + ToolCallEvent + RunEndEvent publishes), measures wall-clock median via perf_counter
    - The assertion: `actual_median_ms <= baseline_median_ms + 50` where baseline_median_ms is read from the matching JSON entry
    - The fixture uses a stub Conversation that returns 5 iterations with one tool_use each on iterations 0-3 and text-only on iteration 4 (yielding 5 LLMCallEvents + 4 ToolCallEvents + 1 RunEndEvent = 10 publishes total; close to the spec's "5-iteration / 3-tool-call" with a margin)

    Note on the spec's exact "5-iteration / 3-tool-call" wording: ROADMAP success criterion 5 names "5-iteration / 3-tool-call loop". To match exactly, use 5 iterations where iterations 0, 1, 2 emit one tool_use each (= 3 tool_calls) and iterations 3, 4 are text-only (no tool_use). Total: 5 LLM publishes + 3 TOOL publishes + 1 RUN_END = 9 publishes. This matches the spec.
  </behavior>
  <action>
Create `tests/perf/test_capture_overhead.py`:

Imports: `import json, statistics, sys, time` from stdlib; `from pathlib import Path`; `import pytest`; `from horus_os import Database; from horus_os.agent import run_agent_loop; from horus_os.observability import get_observation_bus, reset_observation_bus_for_tests, SQLitePersister; from horus_os.observability.bus import RunEndEvent; from horus_os.tools.registry import ToolRegistry; from horus_os.types import AgentResult, Tool, ToolUse; from horus_os import agent as agent_module; import uuid`.

Constants: `BASELINE_PATH = Path(__file__).parent / "v0_3_baseline.json"`, `N_SAMPLES = 20`, `LOOP_ITERATIONS = 5`, `TOOL_CALLS_PER_RUN = 3`, `OVERHEAD_TOLERANCE_MS = 50`.

Helpers:
- `_platform_key()`, copy verbatim from `scripts/capture_v0_3_baseline.py:_platform_key` (linux/darwin/win32 normalization).
- `_python_key()`, copy verbatim.
- `_load_matching_baseline_ms() -> float | None`, read BASELINE_PATH JSON, scan samples for matching (os, python), return the median_3_iteration_loop_ms or None.
- `_StubConversation`, mirror the one in capture_v0_3_baseline.py but with `LOOP_ITERATIONS=5` and `TOOL_CALLS_PER_RUN=3`: first 3 sends return AgentResult with one tool_use + usage={"input_tokens": 100, "output_tokens": 50}; sends 4 and 5 return text-only with same usage.
- `_stub_new_conversation(provider, model, *, system_prompt=None)`, return _StubConversation.
- `_noop_handler() -> str`, return "noop-result".
- `_build_registry() -> ToolRegistry`, register the noop tool (mirror script).

Test `test_capture_overhead_within_50ms(tmp_path)`:
1. `baseline_ms = _load_matching_baseline_ms()`; if None: `pytest.skip(f"No v0_3 baseline entry for ({_platform_key()}, py{_python_key()}). Run scripts/capture_v0_3_baseline.py on this host first.")`.
2. Setup: `reset_observation_bus_for_tests()`, create `db = Database(tmp_path / "horus.sqlite"); db.init()`, subscribe SQLitePersister(db) to the bus.
3. Save `original = agent_module._new_conversation` and monkey-patch with `_stub_new_conversation`. Wrap in try/finally to restore.
4. Build a registry once; reuse across samples.
5. For each of N_SAMPLES iterations: pre-generate trace_id (uuid4 hex); pre-seed traces row via `db.record_trace("benchmark", AgentResult(text="", provider="anthropic", model="stub"), latency_ms=0, trace_id=trace_id)`; record `t0 = time.perf_counter()`; call `run_agent_loop("prompt", registry=registry, provider="anthropic", max_iterations=LOOP_ITERATIONS, trace_id=trace_id)`; publish `get_observation_bus().publish(RunEndEvent(trace_id=trace_id, latency_ms=0))`; record `samples.append(time.perf_counter() - t0)`.
6. `actual_median_ms = round(statistics.median(samples) * 1000, 3)`.
7. `assert actual_median_ms <= baseline_ms + OVERHEAD_TOLERANCE_MS, f"Capture overhead {actual_median_ms}ms exceeds baseline {baseline_ms}ms + {OVERHEAD_TOLERANCE_MS}ms tolerance"`.
8. Pytest output prints both numbers for the CI log: `print(f"capture_overhead: baseline={baseline_ms}ms actual={actual_median_ms}ms delta={actual_median_ms - baseline_ms:.3f}ms")`.

Test `test_baseline_json_shape_is_valid()`: load BASELINE_PATH, assert top-level is a dict with "samples" key, each sample has the six required fields (os, python, horus_os_version, median_3_iteration_loop_ms, n_samples, captured_at). This is a guard against schema drift between Phase 32's capture script and Phase 33's reader.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/perf/test_capture_overhead.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `test_baseline_json_shape_is_valid` passes
    - On darwin py3.12 (the only currently-seeded baseline entry), `test_capture_overhead_within_50ms` either PASSES (overhead within 50ms of 0.005ms baseline) OR raises a clear assertion message showing the actual vs baseline numbers
    - On any other (os, python) combo, the test SKIPS with a message naming the missing baseline entry (no CI block)
    - The benchmark exercises the FULL Phase 33 wiring (real bus, real persister, real SQLite writes) so it catches any regression that adds > 50ms of overhead
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: Wire the capture-overhead benchmark into CI on the 3-OS matrix (TEST-12)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full; the `lint-and-test` job already runs on the 3-OS × 2-python matrix; Task 9 adds a new step there OR a parallel job)
    - tests/perf/test_capture_overhead.py created by Task 8
    - .planning/research/PITFALLS.md lines 196-219 (Pitfall 8: capture-overhead in CI is the guard against SQLite write amplification regression)
  </read_first>
  <behavior>
    - A new CI step "capture-overhead benchmark (METRIC-05 / TEST-12)" runs `pytest tests/perf/test_capture_overhead.py -v` on the 3-OS × 2-python matrix as part of the lint-and-test job
    - On hosts where the baseline JSON has no matching entry, the test skips (already implemented in Task 8) and CI stays green
    - On hosts WITH a matching entry, the test asserts within-50ms and CI fails on regression
  </behavior>
  <action>
Modify `.github/workflows/ci.yml`: in the `lint-and-test` job, AFTER the existing `Run pytest` step (line 44-45) and BEFORE the `CLI smoke check` step (line 47-48), add a new step:

```
      - name: capture-overhead benchmark (METRIC-05 / TEST-12)
        run: pytest tests/perf/test_capture_overhead.py -v
```

Use the EXACT step name `capture-overhead benchmark (METRIC-05 / TEST-12)` so the CI logs are searchable by requirement ID. No new matrix dimension; reuse the existing 3-OS × 2-python matrix.

Document in the workflow file with a comment above the step: `# METRIC-05 + TEST-12: asserts Phase 33 capture overhead stays within 50ms of the v0.3 baseline (tests/perf/v0_3_baseline.json). Skips when no matching (os, python) baseline entry exists so CI does not block on unseeded combos.`

No other workflow changes. The existing time.time() lint gate step (line 41-42) already covers the extended WATCHED_PATHS from Task 6 (the script scans WATCHED_PATHS regardless of which CI step runs it).
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && python -c "import yaml; cfg = yaml.safe_load(open('.github/workflows/ci.yml')); steps = cfg['jobs']['lint-and-test']['steps']; names = [s.get('name','') for s in steps]; assert any('capture-overhead' in n for n in names), f'capture-overhead step missing; got: {names}'; print('OK: capture-overhead step present:', [n for n in names if 'capture-overhead' in n][0])"</automated>
  </verify>
  <done>
    - The CI workflow has a step named `capture-overhead benchmark (METRIC-05 / TEST-12)` inside `lint-and-test`
    - The step runs `pytest tests/perf/test_capture_overhead.py -v`
    - The 3-OS × 2-python matrix is unchanged; no new matrix dimension introduced
    - YAML parses cleanly
  </done>
</task>

</tasks>

<verification>
After all nine tasks complete, the following observable checks must pass (run from `/Users/santino/Projects/horus-os`):

1. **Pitfall 1 structural fix (MUST-HAVE 1):**
   `pytest tests/observability/test_run_end_rollup_integration.py::test_three_iteration_rollup -x` exits 0.
   The 3-iteration stubbed run lands `traces.total_input_tokens == 300` (NOT 100) and three rows in `llm_calls`.

2. **Pitfall 2 SSE fix (MUST-HAVE 2):**
   `pytest tests/observability/test_sse_capture.py -x` exits 0.
   No row in `llm_calls` from `/api/chat/stream` has `input_tokens=0 AND output_tokens=0` after a non-empty stream.

3. **Pitfall 3 lint guard (MUST-HAVE 3):**
   `python scripts/lint_no_wallclock.py` exits 0.
   WATCHED_PATHS includes `src/horus_os/server/api.py`.
   `pytest tests/observability/test_pitfall_3_negative_latency.py -x` exits 0.

4. **Pitfall 9 substrate (MUST-HAVE 4):**
   `pytest tests/observability/test_pitfall_9_tool_status_columns.py -x` exits 0.
   `tool_invocations.error_message` contains exception CLASS NAME only on failure; user-supplied paths do not leak into the column.

5. **METRIC-05 / TEST-12 (MUST-HAVE 5):**
   `pytest tests/perf/test_capture_overhead.py -v` either passes (within 50ms of baseline) or skips with a clear "no matching baseline entry" message. CI step `capture-overhead benchmark (METRIC-05 / TEST-12)` runs on the 3-OS × 2-python matrix.

6. **Full suite green:**
   `pytest -x -q` exits 0 with all 459 prior tests + the ~20 Phase 33 new tests passing.

7. **Lint + format clean:**
   `ruff check .` exits 0; `ruff format --check .` exits 0; `python scripts/lint_no_wallclock.py` exits 0.

8. **Anti-scope respected (`git status --short` after all commits):**
   - `src/horus_os/observability/bus.py` modified ONLY for the LLMCallEvent docstring (Pitfall 4 contract). No bus class behavior changes.
   - `src/horus_os/observability/persist.py` UNCHANGED (Phase 32's; Phase 33 only consumes it).
   - `src/horus_os/storage.py` modified ONLY to add the `trace_id` keyword to `record_trace`; schema untouched.
   - `pyproject.toml` UNCHANGED (no new deps).
   - No `pricing.json`, no OTel, no `CostAnnotator` (Phase 34's job).
   - No `observability/queries.py`, no new `/api/observability/*` routes (Phase 35's job).

9. **Requirements traceability:**
   All six requirement IDs (METRIC-01..05, TEST-12) are addressed by at least one task in this plan, and each is named in the `requirements` frontmatter field.
</verification>

<success_criteria>
Phase 33 is complete when ALL of the following hold:

- [ ] A real POST /api/chat run with a 3-iteration agent loop writes three rows to `llm_calls` AND `traces.total_input_tokens` equals the sum across iterations (Pitfall 1 fixed, ROADMAP success criterion 1).
- [ ] A real POST /api/chat/stream run extracts the terminal `usage` from the Anthropic `MessageStream.get_final_message()` (or Gemini `response.usage_metadata`) and persists non-zero `input_tokens` and `output_tokens` (Pitfall 2 fixed, ROADMAP success criterion 2). When terminal usage is unavailable, the fallback estimate is used; ZERO is never persisted for a non-empty stream.
- [ ] Every `llm_calls` and `tool_invocations` row uses `time.perf_counter()` for `latency_ms`; the lint guard `scripts/lint_no_wallclock.py` watches all four scope paths (observability/, agent.py, tools/loop.py, server/api.py); SQLitePersister still asserts `latency_ms >= 0` and refuses negative inserts (ROADMAP success criterion 3).
- [ ] Tool invocation rows persist `status` ∈ {success, error}; `retry_count` is NULL with a code comment explaining the Anthropic SDK limitation; `error_message` carries the exception CLASS NAME only (never `str(exc)` which would leak user-supplied content per Pitfall 9 + T-33-01) (ROADMAP success criterion 4).
- [ ] CI step `capture-overhead benchmark (METRIC-05 / TEST-12)` runs on the 3-OS × 2-python matrix and asserts within 50ms of the v0.3 baseline; skips when no matching entry exists (ROADMAP success criterion 5).
- [ ] Full test suite passes (`pytest -x -q` exits 0); `ruff check .` exits 0; `ruff format --check .` exits 0; `python scripts/lint_no_wallclock.py` exits 0.
- [ ] `total_cost_usd` stays NULL on every `traces` row (Phase 34's job to populate; Phase 33 must not write zero per Pitfall 5).
- [ ] No new dependencies added (`pyproject.toml` untouched).
- [ ] No touches to `observability/bus.py` (Phase 32) beyond the LLMCallEvent docstring, no touches to `observability/persist.py`, no touches to `storage.py` schema, no `pricing.json`, no OTel, no `observability/queries.py`.
- [ ] All six requirement IDs covered: METRIC-01 (Task 2), METRIC-02 (Tasks 3 + 7), METRIC-03 (Task 5), METRIC-04 (Task 4), METRIC-05 (Tasks 8 + 9), TEST-12 (Tasks 8 + 9).
</success_criteria>

<output>
After completion, create `.planning/phases/33-capture-at-the-runner-sse-branch/33-01-SUMMARY.md` following the Phase 32 SUMMARY structure (frontmatter with phase, plan, subsystem, tags, requires, provides, requirements-completed, tech-stack, key-files, metrics; body with "What shipped", "Requirements satisfied", "ROADMAP Success Criteria", "Pitfalls guarded", "Deviations from plan", "Test counts", "Out of scope", "Self-Check"). The SUMMARY must explicitly enumerate which capture site addresses which Pitfall and which requirement ID; this is the audit trail Phase 34's CostAnnotator + Phase 35's queries will read.
</output>
