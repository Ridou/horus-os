---
phase: "35-query-module-and-read-apis"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/horus_os/observability/queries.py             # NEW
  - src/horus_os/observability/__init__.py            # MODIFY: re-export window parser + query fns
  - src/horus_os/server/api.py                        # MODIFY: 4 new GET routes + /api/agents extension
  - tests/observability/test_queries_window.py        # NEW
  - tests/observability/test_queries_agent_totals.py  # NEW
  - tests/observability/test_queries_cost_by_agent.py # NEW
  - tests/observability/test_queries_latency_percentiles.py # NEW
  - tests/observability/test_queries_tool_reliability.py    # NEW
  - tests/test_server_observability_routes.py         # NEW
  - tests/test_server_agents.py                       # MODIFY: assert new rollup fields
  - .planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md  # NEW (final task)
autonomous: true
requirements:
  - DASH-4-04
tags: [queries, read-api, ntile, percentiles, observability, v0.4, dash-4-04]
subsystem: observability

must_haves:
  truths:
    - "observability/queries.py exposes agent_totals(window), cost_by_agent(window), latency_p50_p95(window), tool_reliability(window) as pure functions returning JSON-serializable dicts; ALL aggregation runs in SQL via NTILE; no Python percentile math (Pitfall 10 anti-pattern guard)"
    - "Four new GET routes /api/observability/{cost,latency,tools,llm-calls} accept ?since=24h|7d|30d|Nh|Nd and return the query-module output as JSON; default window is 7d; invalid window returns 400"
    - "Existing /api/agents route gains total_runs, total_cost_usd, latency_p50, latency_p95 fields per agent, sourced from v0.4 rollup columns on traces; pre-v0.4 rows contribute NULL and are excluded from cost sums via COALESCE (DASH-4-04 backend half, Pitfall 11 NULL handling)"
    - "Percentile queries return p50=None and p95=None (NOT 0) for windows with no data; queries return raw sample_count alongside p50/p95 so callers (Phase 36 dashboard, Phase 37 CLI) can apply the n-threshold render rule (Pitfall 10)"
    - "Reliability query honors the status enum so retry_then_success rows do NOT count toward error_count; query never reads error_message content, only error_type (Pitfall 9)"
  artifacts:
    - path: "src/horus_os/observability/queries.py"
      provides: "parse_window + agent_totals + cost_by_agent + latency_p50_p95 + tool_reliability pure functions over Database"
      exports: ["parse_window", "agent_totals", "cost_by_agent", "latency_p50_p95", "tool_reliability"]
      min_lines: 150
    - path: "src/horus_os/server/api.py"
      provides: "Four new /api/observability/* GET routes + /api/agents extension with rollup columns"
      contains: "/api/observability/cost"
    - path: "tests/observability/test_queries_latency_percentiles.py"
      provides: "NTILE pinpoint test + n<10 NULL test + empty-window NULL test (Pitfall 10)"
      contains: "p50_p95"
    - path: "tests/observability/test_queries_tool_reliability.py"
      provides: "retry_then_success NOT counted as error (Pitfall 9) + error_message never read"
      contains: "retry_then_success"
    - path: "tests/test_server_observability_routes.py"
      provides: "4 new routes return JSON for ?since=7d; default-window contract; 400 on invalid window"
      contains: "/api/observability/cost"
  key_links:
    - from: "src/horus_os/observability/queries.py"
      to: "src/horus_os/storage.py:Database"
      via: "Database._connect() borrowed per query"
      pattern: "with db._connect"
    - from: "src/horus_os/server/api.py:/api/observability/cost"
      to: "queries.cost_by_agent"
      via: "thin route wrapper, no SQL in route handler"
      pattern: "from horus_os.observability.queries import"
    - from: "src/horus_os/server/api.py:/api/agents"
      to: "queries.agent_totals"
      via: "merge agent_totals rows into existing _profile_to_dict output"
      pattern: "agent_totals"
    - from: "queries.latency_p50_p95"
      to: "llm_calls.latency_ms via NTILE(100) OVER (ORDER BY latency_ms)"
      via: "SQLite-side window function, NEVER Python statistics.quantiles"
      pattern: "NTILE\\(100\\)"
---

<objective>
Build `observability/queries.py` once so the Phase 36 dashboard and Phase 37 CLI cannot drift. All percentiles via SQLite-side `NTILE(100) OVER (ORDER BY latency_ms)`, never aggregate-of-aggregates and never Python `statistics.quantiles`. Ship the four new `/api/observability/{cost,latency,tools,llm-calls}` GET routes plus the `/api/agents` extension that adds rollup columns to the existing v0.3 surface (DASH-4-04 backend half).

Purpose: Phase 36 (dashboard) and Phase 37 (CLI) consume the same query module so the two surfaces cannot disagree. Pitfalls 9 (retry-aware reliability), 10 (small-sample percentile bias), and 11 (pre-v0.4 NULL handling) are baked into the query layer at SQL level so downstream callers cannot bypass them.

Output: `src/horus_os/observability/queries.py` (NEW, ~150-250 lines pure functions), `src/horus_os/server/api.py` MODIFIED with 4 new routes + `/api/agents` extension, eight new test files asserting the contracts on a seeded database. Zero touches to `bus.py`, `persist.py`, `cost.py`, `pricing.py`, `pricing.json`, `agent.py`, `tools/loop.py`, `storage.py`, `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/32-schema-migration-persistence-skeleton-v0-3-baseline/32-01-SUMMARY.md
@.planning/phases/33-capture-at-the-runner-sse-branch/33-01-SUMMARY.md
@.planning/phases/34-pricing-table-and-cost-annotation/34-01-SUMMARY.md
@CLAUDE.md
@src/horus_os/observability/__init__.py
@src/horus_os/observability/persist.py
@src/horus_os/storage.py
@src/horus_os/server/api.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from frozen schema + existing modules. -->
<!-- DO NOT modify any of these; this plan only READS from them. -->

Tables this plan reads (from src/horus_os/storage.py, schema FROZEN by Phase 32):

traces (rollup columns added in v4->v5 migration; all NULLABLE):
  trace_id            TEXT UNIQUE
  created_at          TEXT (ISO 8601 UTC)
  provider            TEXT
  model               TEXT
  latency_ms          INTEGER (pre-v0.4 wall-clock; do NOT use for percentiles, use llm_calls.latency_ms)
  status              TEXT
  parent_trace_id     TEXT NULL
  agent_profile_name  TEXT NULL (joins to agent_profiles.name)
  total_input_tokens  INTEGER NULL  (NULL on pre-v0.4 rows)
  total_output_tokens INTEGER NULL  (NULL on pre-v0.4 rows)
  total_cost_usd      REAL    NULL  (NULL on pre-v0.4 rows; NULL also when ANY contributing llm_calls.cost_usd is NULL per Pitfall 5)
  total_duration_ms   INTEGER NULL  (NULL on pre-v0.4 rows)

llm_calls (one row per Conversation.send, written by Phase 33+34):
  call_id                     TEXT UNIQUE
  trace_id                    TEXT
  iteration_idx               INTEGER
  created_at                  TEXT (ISO 8601 UTC)
  provider                    TEXT
  model                       TEXT
  input_tokens                INTEGER (default 0)
  output_tokens               INTEGER (default 0)
  cache_creation_input_tokens INTEGER (default 0)
  cache_read_input_tokens     INTEGER (default 0)
  cost_usd                    REAL NULL (NULL when model unknown to PricingTable, per Pitfall 5)
  pricing_missing             INTEGER (0 or 1)
  latency_ms                  INTEGER NOT NULL (monotonic via perf_counter; canonical source for LLM-call percentiles)
  status                      TEXT (default 'success')
  error_message               TEXT NULL (Pitfall 9: NEVER read this in queries.py; tool-reliability query reads error_type only)
  error_type                  TEXT NULL (exception class name)

tool_invocations (one row per _execute_one):
  invocation_id   TEXT UNIQUE
  trace_id        TEXT
  parent_trace_id TEXT NULL
  created_at      TEXT (ISO 8601 UTC)
  tool_name       TEXT
  latency_ms      INTEGER NOT NULL (monotonic)
  status          TEXT (default 'success'; enum: 'success', 'error', 'retry_then_success', 'expected_no_result')
  error_message   TEXT NULL (Pitfall 9: do NOT SELECT this in queries.py)
  error_type      TEXT NULL (exception class name; safe to SELECT)
  retry_count     INTEGER NULL
  output_size     INTEGER NULL

agent_profiles:
  name          TEXT UNIQUE
  created_at    TEXT
  updated_at    TEXT
  (full row consumed by existing _profile_to_dict in server/api.py:658-670)

Existing helpers this plan extends, NOT replaces:
  src/horus_os/server/api.py:183  @app.get("/api/agents")  -> extends payload with rollup cols (DASH-4-04)
  src/horus_os/server/api.py:642  _trace_to_dict(record: TraceRecord) -> dict
  src/horus_os/server/api.py:658  _profile_to_dict(profile, *, last_activity_at) -> dict (rollup fields merge here)
  src/horus_os/server/api.py:673  _last_activity_for(db, name) -> str | None

Database constructor + connection: storage.py:154-172.
  db = Database(cfg.db_path)
  with db._connect() as conn:  # WAL + synchronous=NORMAL, row_factory=sqlite3.Row

Existing observability re-exports (src/horus_os/observability/__init__.py:56-67):
  CostAnnotator, LLMCallEvent, ModelPricing, ObservationBus, ObservationEvent,
  PricingTable, RunEndEvent, SQLitePersister, ToolCallEvent,
  get_observation_bus, reset_observation_bus_for_tests

After this plan, add to __all__:
  agent_totals, cost_by_agent, latency_p50_p95, parse_window, tool_reliability

Lint guard (scripts/lint_no_wallclock.py):
  WATCHED_DIRS includes src/horus_os/observability/ -- queries.py inherits the time.time() ban.
  WATCHED_FILES includes src/horus_os/server/api.py -- the new routes must use perf_counter() if they
  do any timing (they should not need to; queries.py is the only place that matters).

Test pattern (from tests/test_server_agents.py):
  from fastapi.testclient import TestClient
  from horus_os import Database, create_app
  def _client(tmp_path: Path) -> TestClient:
      return TestClient(create_app(data_dir=tmp_path))

Pricing override hygiene for tests:
  Phase 34 wires PricingTable in create_app via Config.load(...).pricing_path. The
  SQLitePersister Phase 32 wrote handles cost_usd NULL via all-or-nothing rollup;
  test fixtures can seed llm_calls rows directly with sqlite3 INSERT (bypassing the
  bus) to set up cost / latency / status state for query assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queries.py skeleton + parse_window + agent_totals</name>
  <files>
    src/horus_os/observability/queries.py
    src/horus_os/observability/__init__.py
    tests/observability/test_queries_window.py
    tests/observability/test_queries_agent_totals.py
  </files>
  <read_first>
    - src/horus_os/observability/__init__.py (to extend __all__)
    - src/horus_os/storage.py:154-300 (Database._connect, list_profiles, schema columns)
    - src/horus_os/observability/persist.py:156-203 (the rollup SQL shape to mirror for SUM(COALESCE(...)) semantics)
    - .planning/research/ARCHITECTURE.md:215-244 (NTILE pattern reference + Anti-Pattern 2 against Python percentiles)
    - .planning/research/PITFALLS.md:279-302 (Pitfall 11 pre-v0.4 NULL handling)
  </read_first>
  <behavior>
    - parse_window("24h")  -> datetime threshold = now_utc - timedelta(hours=24); returns ISO 8601 string compatible with created_at TEXT comparison
    - parse_window("7d")   -> threshold 7 days back; default for routes
    - parse_window("30d")  -> threshold 30 days back
    - parse_window("48h")  -> generic Nh form; threshold 48h back
    - parse_window("14d")  -> generic Nd form; threshold 14d back
    - parse_window("bad")  -> raises ValueError with message naming the bad input
    - parse_window("0h"), parse_window("-1d"), parse_window("") -> all raise ValueError
    - agent_totals(db, window) returns list of dicts: [{agent: str, total_runs: int, total_input_tokens: int|None, total_output_tokens: int|None, total_cost_usd: float|None, latency_p50_ms: int|None, latency_p95_ms: int|None, sample_count: int}]
    - agent_totals counts ALL runs in window (including pre-v0.4 rows) for total_runs
    - agent_totals uses SUM(COALESCE(total_input_tokens, 0)) and SUM(COALESCE(total_output_tokens, 0)) when rows exist; returns None ONLY when all contributing rows are NULL on that column
    - agent_totals uses CASE WHEN COUNT(*) > COUNT(total_cost_usd) THEN NULL ELSE SUM(total_cost_usd) END (all-or-nothing per Pitfall 5 honesty contract) OR returns SUM of non-NULL rows with a separate uncosted_runs counter -- pick the variant that matches Phase 36's expected render contract: prefer "COALESCE(SUM, 0) for sum + COUNT(*) FILTER (WHERE total_cost_usd IS NULL) AS uncosted_runs" per Pitfall 11 line 291 (cost sums exclude NULL rows, uncosted_runs surfaced separately so the dashboard never hides missing dollars)
    - agent_totals latency_p50_ms / latency_p95_ms / sample_count computed via NTILE(100) OVER (PARTITION BY agent_profile_name ORDER BY latency_ms) on llm_calls JOINed via trace_id, NOT on traces.latency_ms (Phase 33 captures per-call monotonic latency on llm_calls.latency_ms; traces.latency_ms is the older v0.3 wall-clock)
    - agent_totals returns latency_p50_ms = None when sample_count < 1 for that agent; returns sample_count alongside so caller applies n-threshold rule (Pitfall 10)
    - agent_totals never sees rows older than the window; WHERE created_at >= parse_window(window) applied to BOTH traces (for run counting) and llm_calls (for percentiles)
    - All four query functions accept Database (not raw path) for consistency with persist.py pattern and for testability
  </behavior>
  <action>
    Create `src/horus_os/observability/queries.py` with module-top docstring (PER PITFALL 10 LINE 265: explicit comment "All percentile aggregations operate on raw latency_ms; NEVER aggregate-of-aggregates").

    Define `parse_window(window: str) -> str` that returns an ISO 8601 UTC threshold string suitable for direct comparison against the `created_at TEXT` columns. Accept exact forms `24h`, `7d`, `30d` AND any positive integer N followed by `h` or `d`. Reject negative, zero, and malformed forms with `ValueError(f"invalid window: {window!r}; expected forms like '24h', '7d', '30d'")`. Use `datetime.now(timezone.utc)` then format with `.isoformat()` to match the persister's `_now_iso()` shape; verify by reading one row from `created_at` in the existing test fixtures.

    Define `agent_totals(db: Database, window: str) -> list[dict[str, Any]]`. Implementation: one SQL query that LEFT JOINs `agent_profiles` -> `traces` (on `agent_profile_name = name`) -> `llm_calls` (on `trace_id = trace_id`) filtered by `created_at >= ?` on BOTH `traces` and `llm_calls`. Use a CTE for the NTILE windowing per agent. Return per-agent: `total_runs` from `COUNT(DISTINCT traces.trace_id)`, `total_input_tokens` and `total_output_tokens` from `SUM(traces.total_input_tokens)` / `SUM(traces.total_output_tokens)` (SQL SUM auto-skips NULLs, returns NULL when all NULL (keep that; the result is None-friendly for JSON), `total_cost_usd` from `COALESCE(SUM(traces.total_cost_usd), 0)` summed across non-NULL rows, plus a separate `uncosted_runs` from `COUNT(*) FILTER (WHERE traces.total_cost_usd IS NULL)` (Pitfall 11 surface contract). For percentiles, use the NTILE pattern documented in ARCHITECTURE.md lines 220-235 against `llm_calls.latency_ms` partitioned by `traces.agent_profile_name`. Return `latency_p50_ms`, `latency_p95_ms` as INTEGER ms or None when sample_count = 0.

    Add `parse_window` and `agent_totals` to the module-top `__all__` in `queries.py`. Re-export both from `observability/__init__.py` by extending the existing `__all__` list (alphabetized insertion).

    Use `pathlib.Path` for any path handling (none expected here). Type-hint every public function. Ruff line length 100. No em-dashes anywhere in docstrings or comments. Never import or call `time.time()` (lint guard on observability/).
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && .venv/bin/python -m pytest tests/observability/test_queries_window.py tests/observability/test_queries_agent_totals.py -xvs && .venv/bin/ruff check src/horus_os/observability/queries.py src/horus_os/observability/__init__.py && .venv/bin/python scripts/lint_no_wallclock.py && grep -c "NTILE(100)" src/horus_os/observability/queries.py | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - File `src/horus_os/observability/queries.py` exists with module docstring containing the literal "never aggregate-of-aggregates" or "no aggregate of aggregates" (Pitfall 10 anchor comment).
    - `parse_window` raises `ValueError` on `""`, `"0h"`, `"-1d"`, `"bad"`, `"7"`, `"d7"`, `"7days"`.
    - `parse_window("7d")` returns a string strictly less than `parse_window("24h")` (7 days back is older than 24 hours back).
    - `agent_totals(db, "7d")` against an empty database returns `[]` (empty list, never raises).
    - `agent_totals(db, "7d")` with one seeded agent and N seeded traces returns a list with one dict whose `total_runs == N`.
    - `agent_totals` returns `latency_p50_ms is None and latency_p95_ms is None` when there are zero `llm_calls` rows in the window for that agent, AND `sample_count == 0` in the same row.
    - `agent_totals` returns `total_cost_usd` as a `float` (SUM of non-NULL rows) and a separate `uncosted_runs` integer counting NULL rows (Pitfall 11 contract).
    - `grep -c "NTILE(100)" src/horus_os/observability/queries.py` returns >= 1.
    - `grep -c "statistics\.\(quantiles\|median\|mean\)" src/horus_os/observability/queries.py` returns 0 (no Python percentile math).
    - `grep -v '^#' src/horus_os/observability/queries.py | grep -c "time\.time()"` returns 0.
    - `from horus_os.observability import parse_window, agent_totals` succeeds (re-exports landed in __init__.py).
    - pytest tests pass; ruff clean; lint_no_wallclock clean.
  </acceptance_criteria>
  <done>parse_window + agent_totals shipped with SQL-side NTILE percentiles, no Python percentile math, COALESCE-aware NULL handling for pre-v0.4 rows, and a separate uncosted_runs counter so the dashboard never hides missing dollars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cost_by_agent query</name>
  <files>
    src/horus_os/observability/queries.py
    src/horus_os/observability/__init__.py
    tests/observability/test_queries_cost_by_agent.py
  </files>
  <read_first>
    - src/horus_os/observability/queries.py (just-created from Task 1)
    - src/horus_os/storage.py:35-54 (traces.total_cost_usd + total_input_tokens + total_output_tokens columns)
    - src/horus_os/observability/persist.py:156-203 (the all-or-nothing rollup contract that produces total_cost_usd NULL when ANY contributing llm_call has cost_usd NULL)
    - .planning/research/PITFALLS.md:279-302 (Pitfall 11)
  </read_first>
  <behavior>
    - cost_by_agent(db, window) returns list of dicts: [{agent: str, total_cost_usd: float, total_input_tokens: int, total_output_tokens: int, total_cache_read_input_tokens: int, total_cache_creation_input_tokens: int, run_count: int, uncosted_runs: int}]
    - SUM(traces.total_cost_usd) uses SQL's NULL-skipping behavior; result is wrapped in COALESCE(..., 0.0) so cost is always a float (0.0 means "no costed runs in window for this agent")
    - uncosted_runs counts rows where total_cost_usd IS NULL via COUNT(*) FILTER (WHERE traces.total_cost_usd IS NULL) -- includes both pre-v0.4 rows (no rollup at all) and Pitfall 5 rows (unknown model)
    - Token totals join llm_calls so cache tokens are summed (the traces rollup only has total_input_tokens + total_output_tokens, NOT cache columns)
    - Returned ordering: by total_cost_usd DESC (most expensive agent first); ties broken by agent name ASC for deterministic test output
    - Agents with zero runs in the window are EXCLUDED from the result (not returned with all-zeros rows)
  </behavior>
  <action>
    Append `cost_by_agent(db: Database, window: str) -> list[dict[str, Any]]` to `queries.py`. Single SQL query LEFT JOINs `traces` to `llm_calls` on `trace_id` (so cache token columns from llm_calls can be summed alongside the traces rollup). GROUP BY `agent_profile_name`. Apply `WHERE traces.created_at >= ?` (use `parse_window`). Return rows where `run_count >= 1`. Surface `uncosted_runs` so Phase 36 can render "N runs from before v0.4 with no cost data" without hiding the missing dollars (Pitfall 11 line 291 verbatim).

    Round `total_cost_usd` to 6 decimal places at the Python boundary (matches Phase 34 cost test contract `cost_usd == 0.006150`). Use `round(value, 6)` after the SQL fetch, NOT in SQL (SQLite `ROUND` rounds half-to-even on some platforms vs half-up on others; Python `round` is consistent across the 3-OS matrix).

    Add `cost_by_agent` to `queries.py.__all__` and re-export from `observability/__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && .venv/bin/python -m pytest tests/observability/test_queries_cost_by_agent.py -xvs && .venv/bin/ruff check src/horus_os/observability/queries.py && .venv/bin/python scripts/lint_no_wallclock.py</automated>
  </verify>
  <acceptance_criteria>
    - `cost_by_agent(db, "7d")` on empty DB returns `[]`.
    - Seed 1 agent + 3 traces (2 with total_cost_usd = 0.001 each, 1 with total_cost_usd = NULL). Assert one dict returned with `total_cost_usd == 0.002`, `run_count == 3`, `uncosted_runs == 1`.
    - Seed 2 agents with different costs; assert returned list is ordered by cost DESC.
    - Agent with all traces older than the window is absent from the result (not present with all-zeros).
    - Cache token sums come from llm_calls (seed an llm_calls row with `cache_read_input_tokens=500`, assert that value flows through to the returned dict).
    - `grep -c "ROUND\\|round(" src/horus_os/observability/queries.py` returns >= 1 for the Python `round(` call; SQL `ROUND(` should NOT appear.
    - pytest passes; ruff clean; lint clean.
  </acceptance_criteria>
  <done>cost_by_agent pure function shipped; cost sums via SQL NULL-skipping + Python rounding for cross-OS determinism; uncosted_runs surfaced so the dashboard explains missing dollars rather than hiding them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: latency_p50_p95 with NTILE + Pitfall 10 small-sample + empty-window NULL contract</name>
  <files>
    src/horus_os/observability/queries.py
    src/horus_os/observability/__init__.py
    tests/observability/test_queries_latency_percentiles.py
  </files>
  <read_first>
    - src/horus_os/observability/queries.py (with parse_window + agent_totals + cost_by_agent from Tasks 1-2)
    - .planning/research/PITFALLS.md:252-275 (Pitfall 10 in full -- the n-threshold rule, the empty-window rule, the "never aggregate-of-aggregates" rule)
    - .planning/research/ARCHITECTURE.md:215-244 (the canonical NTILE SQL the architecture committed to)
  </read_first>
  <behavior>
    - latency_p50_p95(db, window) returns dict: {p50_ms: int|None, p95_ms: int|None, sample_count: int}
    - Empty window (zero llm_calls rows in range) -> {p50_ms: None, p95_ms: None, sample_count: 0} -- NEVER {p50_ms: 0, p95_ms: 0, sample_count: 0}
    - Single sample (sample_count = 1) -> p50_ms == p95_ms == that sample's latency_ms (NTILE collapses); sample_count returned so caller's render layer can decide to hide (Phase 36 hides anything with n<10)
    - N samples all at 100ms -> p50_ms == p95_ms == 100 (boundary test from Pitfall 10 line 267)
    - Window range parsed via parse_window; query filters on llm_calls.created_at >= threshold
    - Uses NTILE(100) OVER (ORDER BY latency_ms) -- NO PARTITION BY (this function is global across all agents/models; per-agent breakdown lives in agent_totals)
    - sample_count is COUNT(*) over the windowed llm_calls subset
    - DOES NOT call statistics.quantiles, statistics.median, sorted(...)[len//2], or any Python-side percentile math. Lint check via grep.
  </behavior>
  <action>
    Append `latency_p50_p95(db: Database, window: str) -> dict[str, Any]` to `queries.py`. SQL pattern (adapt the ARCHITECTURE.md template; document the exact SQL in the function docstring):

    The CTE selects `latency_ms` + `NTILE(100) OVER (ORDER BY latency_ms) AS pct` from `llm_calls` WHERE `created_at >= ?`. The outer SELECT returns `MAX(CASE WHEN pct <= 50 THEN latency_ms END) AS p50`, `MAX(CASE WHEN pct <= 95 THEN latency_ms END) AS p95`, and `COUNT(*) AS sample_count`. SQLite returns NULL for `MAX(... CASE WHEN ... END)` when no rows match; the function returns that NULL through to JSON as Python `None`. NEVER coerce None to 0.

    Add `latency_p50_p95` to `__all__`; re-export from `observability/__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && .venv/bin/python -m pytest tests/observability/test_queries_latency_percentiles.py -xvs && .venv/bin/ruff check src/horus_os/observability/queries.py && .venv/bin/python scripts/lint_no_wallclock.py && python3 -c "import subprocess,sys; out=subprocess.check_output(['grep','-c','-E','statistics\\.(quantiles|median|mean)','src/horus_os/observability/queries.py'],cwd='/Users/santino/Projects/horus-os').decode().strip(); sys.exit(0 if out=='0' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Empty DB: `latency_p50_p95(db, "7d") == {"p50_ms": None, "p95_ms": None, "sample_count": 0}` (NULL not 0; Pitfall 10 contract).
    - Seed 10 llm_calls rows all with `latency_ms = 100`: `latency_p50_p95(db, "7d") == {"p50_ms": 100, "p95_ms": 100, "sample_count": 10}` (Pitfall 10 line 267 boundary test).
    - Seed 100 llm_calls rows with `latency_ms = 1..100`: assert returned `p50_ms` is in `[50, 51]` and `p95_ms` is in `[95, 96]` (NTILE bucket boundary inclusive range; SQLite's NTILE is deterministic but the boundary row assignment can be either side depending on ORDER BY tie behavior).
    - Single sample (sample_count = 1, latency_ms = 42): `p50_ms == 42 and p95_ms == 42 and sample_count == 1` (NTILE collapses on n=1; the function returns it, render layer hides per the n>=10 rule).
    - Out-of-window rows (created_at older than the window threshold) do not contribute to either percentile or sample_count.
    - `grep -E "statistics\\.(quantiles|median|mean)" src/horus_os/observability/queries.py` returns NO MATCHES (exit code 1; confirmed in the automated verify gate above).
    - `grep -c "NTILE(100)" src/horus_os/observability/queries.py` returns >= 2 (one from Task 1's agent_totals, one from this task's latency_p50_p95).
    - pytest passes; ruff clean; lint clean.
  </acceptance_criteria>
  <done>latency_p50_p95 shipped with SQLite-side NTILE percentiles, NULL-on-empty contract baked in at SQL level, sample_count surfaced for caller-side n-threshold rule, and a grep gate proving no Python percentile math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: tool_reliability with retry-aware aggregation (Pitfall 9) and Pitfall 7 error_message guard</name>
  <files>
    src/horus_os/observability/queries.py
    src/horus_os/observability/__init__.py
    tests/observability/test_queries_tool_reliability.py
  </files>
  <read_first>
    - src/horus_os/observability/queries.py (with the three preceding functions)
    - src/horus_os/storage.py:112-131 (tool_invocations columns including status, retry_count, error_type, error_message)
    - .planning/research/PITFALLS.md:221-248 (Pitfall 9 in full: status enum, error_type vs error_message, retry-aware aggregation, never-read-message-content rule)
    - src/horus_os/observability/persist.py:128-154 (writes tool_invocations rows with status enum; the values that can land in status)
  </read_first>
  <behavior>
    - tool_reliability(db, window) returns list of dicts: [{tool_name: str, call_count: int, success_count: int, error_count: int, retry_then_success_count: int, expected_no_result_count: int, success_rate: float|None, last_error_type: str|None, last_error_at: str|None}]
    - status enum: 'success', 'error', 'retry_then_success', 'expected_no_result'
    - error_count counts ONLY status='error' rows. retry_then_success rows DO NOT contribute to error_count (Pitfall 9 hard rule line 237).
    - success_count counts status IN ('success', 'retry_then_success') (a retried-then-succeeded call IS a success for reliability purposes; the retry signal lives separately in retry_then_success_count for the "flakiness" panel)
    - expected_no_result_count is informational; treated as neither success nor error (Pitfall 9 line 236)
    - success_rate = success_count / (success_count + error_count) when denominator > 0, ELSE None. expected_no_result rows are EXCLUDED from the denominator.
    - last_error_type comes from MAX(created_at)'s error_type for rows where status='error'; last_error_at is that timestamp. Both None when no errors in window.
    - SQL NEVER references error_message column. This is enforced by a grep test in the file's test module (Pitfall 7 + Pitfall 9 line 238 line 245).
    - Returned ordering: by call_count DESC (most-used tool first); ties by tool_name ASC.
    - Tools with zero calls in window are absent.
  </behavior>
  <action>
    Append `tool_reliability(db: Database, window: str) -> list[dict[str, Any]]` to `queries.py`. SQL aggregation GROUP BY `tool_name` over `tool_invocations` WHERE `created_at >= ?`:
    - `call_count = COUNT(*)`
    - `success_count = SUM(CASE WHEN status IN ('success', 'retry_then_success') THEN 1 ELSE 0 END)`
    - `error_count = SUM(CASE WHEN status = 'error' THEN 1 ELSE 0 END)`
    - `retry_then_success_count = SUM(CASE WHEN status = 'retry_then_success' THEN 1 ELSE 0 END)`
    - `expected_no_result_count = SUM(CASE WHEN status = 'expected_no_result' THEN 1 ELSE 0 END)`
    - last_error info via correlated subquery: `(SELECT error_type FROM tool_invocations ti2 WHERE ti2.tool_name = ti.tool_name AND ti2.status = 'error' AND ti2.created_at >= ? ORDER BY created_at DESC LIMIT 1)` (and same shape for `last_error_at`).

    Compute `success_rate` in Python after the fetch: `round(success / (success + error), 4) if (success + error) > 0 else None`.

    CRITICAL: the SQL string literal must NOT contain the substring `error_message`. Add a module-level test in `tests/observability/test_queries_tool_reliability.py` that opens `src/horus_os/observability/queries.py` and asserts `"error_message"` does NOT appear anywhere in the file (Pitfall 7 + Pitfall 9 hard rule).

    Add `tool_reliability` to `__all__`; re-export from `observability/__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && .venv/bin/python -m pytest tests/observability/test_queries_tool_reliability.py -xvs && .venv/bin/ruff check src/horus_os/observability/queries.py && .venv/bin/python scripts/lint_no_wallclock.py && grep -v '^#' src/horus_os/observability/queries.py | grep -c "error_message" | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - Empty DB: `tool_reliability(db, "7d") == []`.
    - Seed `read_file` tool with 4 success + 1 retry_then_success + 1 error: `success_count == 5`, `error_count == 1`, `retry_then_success_count == 1`, `success_rate == round(5/6, 4) == 0.8333`.
    - Pitfall 9 regression test: a tool with 10 success + 5 retry_then_success + 0 errors has `error_count == 0` (NOT 5; this is the exact bug Pitfall 9 prevents).
    - expected_no_result rows do NOT inflate error_count and are NOT in the success_rate denominator (a tool with 0 success, 0 error, 3 expected_no_result returns success_rate = None, NOT 0.0 or 1.0).
    - `last_error_type` returns the exception class name from the most recent error row; `last_error_at` is that row's `created_at`.
    - When no errors exist in window: `last_error_type is None and last_error_at is None`.
    - The test file `tests/observability/test_queries_tool_reliability.py` contains an assertion: `assert "error_message" not in Path("src/horus_os/observability/queries.py").read_text()`. Test passes (proves Pitfall 7 + Pitfall 9 column hygiene).
    - `grep -v '^#' src/horus_os/observability/queries.py | grep -c "error_message"` returns 0 (uncommented occurrences forbidden; the file-content assertion above ALSO catches commented occurrences).
    - pytest passes; ruff clean; lint clean.
  </acceptance_criteria>
  <done>tool_reliability shipped with retry-aware aggregation (retry_then_success NOT counted as error), success_rate None on empty denominator, error_message column never SELECTed or referenced anywhere in queries.py, and a regression test pinning the column-hygiene contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Four new /api/observability/* GET routes wired in server/api.py + integration tests</name>
  <files>
    src/horus_os/server/api.py
    tests/test_server_observability_routes.py
  </files>
  <read_first>
    - src/horus_os/server/api.py:1-200 (existing route patterns, _config() helper, 503 contract, Database construction)
    - src/horus_os/server/api.py:280-355 (POST /api/chat to see the cfg/db pattern)
    - src/horus_os/observability/queries.py (the four functions just shipped)
    - tests/test_server_agents.py:1-30 (TestClient pattern, _client(tmp_path) helper)
  </read_first>
  <behavior>
    - GET /api/observability/cost?since=7d -> 200 JSON {agents: [...cost_by_agent rows...]}
    - GET /api/observability/latency?since=7d -> 200 JSON {p50_ms, p95_ms, sample_count} from latency_p50_p95
    - GET /api/observability/tools?since=7d -> 200 JSON {tools: [...tool_reliability rows...]}
    - GET /api/observability/llm-calls?since=7d -> 200 JSON {calls: [...]} returning raw llm_calls rows joined with their trace_id, ordered by created_at DESC, limited to last 100 in window (the "raw rows" route is for Phase 36's drill-down view; small-sample handling deferred to caller per phase split)
    - Default window when ?since= omitted is "7d" (matches ROADMAP SC #2)
    - Invalid window (anything parse_window rejects) -> 400 with detail "invalid window: '...'; expected forms like '24h', '7d', '30d'"
    - DB-not-initialized -> 503 with detail "database not initialized; run `horus-os init`" (matches existing pattern at server/api.py:158)
    - Routes are THIN: import queries functions, call them, wrap the result in the JSON envelope, return. NO SQL in route handlers.
    - llm-calls route is the only route NOT backed by a queries.py function (it returns raw rows for drilldown). It opens its own conn via Database._connect() and SELECTs llm_calls columns directly (NO error_message column; that field is Pitfall 7-restricted even in the drilldown route).
  </behavior>
  <action>
    In `src/horus_os/server/api.py`, after the existing `/api/agents` route block (around line 195) and BEFORE `_persist_write` helper at line 632, add four new route handlers using FastAPI's `@app.get(...)` decorator. Each handler:
    1. Calls `_config()` to get the active Config
    2. Checks `cfg.db_path.exists()`; if not, `raise HTTPException(503, detail="database not initialized; run `horus-os init`")`
    3. Wraps the queries call in `try: ... except ValueError as exc: raise HTTPException(400, detail=str(exc))` to convert parse_window failures into 400s
    4. Returns the result wrapped in a dict envelope: `{"agents": [...]}`, `{"p50_ms": ..., "p95_ms": ..., "sample_count": ...}`, `{"tools": [...]}`, `{"calls": [...]}`

    Add the import at the top of `server/api.py` alongside the existing `from horus_os.observability import (...)` block:
    `from horus_os.observability.queries import cost_by_agent, latency_p50_p95, parse_window, tool_reliability`

    For the `/api/observability/llm-calls` route, SELECT explicit columns (NOT `SELECT *`): `call_id, trace_id, iteration_idx, created_at, provider, model, input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens, cost_usd, pricing_missing, latency_ms, status, error_type` -- DELIBERATELY OMIT `error_message` per Pitfall 7 hygiene. Order by `created_at DESC`, LIMIT 100.

    No `time.time()` anywhere in the new handlers (lint guard on server/api.py). No new fenced code blocks in this action; implementation goes in the file, not in this plan.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && .venv/bin/python -m pytest tests/test_server_observability_routes.py -xvs && .venv/bin/ruff check src/horus_os/server/api.py && .venv/bin/python scripts/lint_no_wallclock.py && grep -c "/api/observability/cost\|/api/observability/latency\|/api/observability/tools\|/api/observability/llm-calls" src/horus_os/server/api.py | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/observability/cost` (no query string) returns 200 with `{"agents": []}` on an empty database (after `horus-os init`-style fixture setup via `Database(tmp_path/...).init()`).
    - `GET /api/observability/cost?since=invalid` returns 400 with detail containing the literal substring "invalid window".
    - `GET /api/observability/cost?since=24h`, `?since=7d`, `?since=30d`, `?since=48h`, `?since=14d` all return 200.
    - `GET /api/observability/latency?since=7d` on empty DB returns `{"p50_ms": null, "p95_ms": null, "sample_count": 0}` (JSON null, not 0).
    - `GET /api/observability/tools?since=7d` on empty DB returns `{"tools": []}`.
    - `GET /api/observability/llm-calls?since=7d` returns `{"calls": [...]}`; seeded llm_calls rows appear ordered by created_at DESC, capped at 100, with explicit allowed columns and NO `error_message` field in the JSON payload (assert via `"error_message" not in returned_call_dict.keys()`).
    - Calling any of the four routes when `db_path` doesn't exist returns 503 with the existing "database not initialized" detail.
    - `grep -c "/api/observability/" src/horus_os/server/api.py` returns >= 4.
    - `grep -v '^#' src/horus_os/server/api.py | grep -c "time\.time()"` returns 0 (preserves existing lint contract).
    - pytest passes; ruff clean; lint clean.
  </acceptance_criteria>
  <done>Four new /api/observability/* routes shipped as thin wrappers over queries.py; default ?since=7d; 400 on invalid window; 503 when DB missing; llm-calls drilldown route omits error_message column per Pitfall 7; full integration test coverage on a TestClient-mounted app.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: /api/agents extension with rollup columns (DASH-4-04 backend half) + Pitfall 11 NULL handling test</name>
  <files>
    src/horus_os/server/api.py
    tests/test_server_agents.py
  </files>
  <read_first>
    - src/horus_os/server/api.py:183-196 (the existing agents_list route (this is the EXACT 14-line block being extended)
    - src/horus_os/server/api.py:658-681 (_profile_to_dict helper + _last_activity_for; the extension merges rollup fields here OR at the route layer)
    - src/horus_os/observability/queries.py (agent_totals from Task 1; THIS is the data source)
    - tests/test_server_agents.py (existing test patterns to extend, NOT replace)
    - .planning/research/PITFALLS.md:279-302 (Pitfall 11 NULL handling for pre-v0.4 rows)
  </read_first>
  <behavior>
    - GET /api/agents returns the existing payload PLUS new per-agent fields: total_runs, total_cost_usd, latency_p50_ms, latency_p95_ms, uncosted_runs
    - Source: agent_totals(db, "7d"). The default window for the /api/agents extension is also 7d (matches ROADMAP SC #2 and the dashboard default; the existing /api/agents route does NOT support ?since= for backward compat, so 7d is hardcoded here; if a future phase needs per-window, that's a Phase 36+ concern)
    - Agents with ZERO runs in 7d still appear in the response (the /api/agents route lists ALL profiles per its existing contract); their rollup fields are: total_runs=0, total_cost_usd=null, latency_p50_ms=null, latency_p95_ms=null, uncosted_runs=0
    - Agents WITH only pre-v0.4 runs (total_cost_usd IS NULL on all their traces in window): total_runs=N, total_cost_usd=null (NEVER 0), latency_p50_ms=null (no llm_calls rows exist for pre-v0.4 traces), uncosted_runs=N (Pitfall 11 contract: pre-v0.4 NULL surfaces as null + uncosted_runs counter)
    - Existing /api/agents response keys (name, system_prompt, default_model, allowed_tools, memory_scope, created_at, updated_at, last_activity_at) MUST be preserved byte-identical for backward compat
  </behavior>
  <action>
    Modify the `agents_list` route in `src/horus_os/server/api.py` (currently at lines 183-196). Steps:

    1. Call `agent_totals(db, "7d")` once, build a lookup dict `{row["agent"]: row for row in agent_totals_result}`.
    2. In the existing list comprehension that emits `_profile_to_dict(p, last_activity_at=...)` for each profile, MERGE the rollup fields from the lookup. For agents not present in the lookup (zero runs in window), use defaults: `{"total_runs": 0, "total_cost_usd": None, "latency_p50_ms": None, "latency_p95_ms": None, "uncosted_runs": 0}`.
    3. The merged dict is the new return shape; preserve every existing key.

    EITHER extend `_profile_to_dict` to take an optional `rollup: dict | None = None` kwarg, OR do the merge inline in the route handler. The inline merge keeps `_profile_to_dict` byte-identical (zero risk to other callers if any exist); prefer that path.

    Add the queries import at the top of `server/api.py` if it was not already added in Task 5:
    `from horus_os.observability.queries import agent_totals, cost_by_agent, latency_p50_p95, parse_window, tool_reliability`

    Update `tests/test_server_agents.py`: extend (do NOT replace) the existing `test_agents_list_returns_default_after_init` assertion to verify the new fields exist with their default-zero/null values on a fresh database. Add a new test `test_agents_list_pre_v0_4_rows_render_null_cost` that INSERTs a synthetic `traces` row with `agent_profile_name="default"`, `total_cost_usd=NULL`, `total_input_tokens=NULL`, `total_output_tokens=NULL`, `total_duration_ms=NULL`, `created_at` within window, and asserts that the API response shows that agent with `total_runs=1`, `total_cost_usd is None`, `uncosted_runs=1` (Pitfall 11 contract).

    Do NOT touch any other route in this task.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && .venv/bin/python -m pytest tests/test_server_agents.py -xvs && .venv/bin/ruff check src/horus_os/server/api.py && .venv/bin/python scripts/lint_no_wallclock.py</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/agents` on a fresh DB returns the existing payload with one new per-agent field group: `total_runs == 0`, `total_cost_usd is None`, `latency_p50_ms is None`, `latency_p95_ms is None`, `uncosted_runs == 0`.
    - All pre-existing per-agent keys (`name`, `system_prompt`, `default_model`, `allowed_tools`, `memory_scope`, `created_at`, `updated_at`, `last_activity_at`) are present and unchanged (no key renames, no removed fields).
    - Pitfall 11 regression test: insert a synthetic pre-v0.4 trace (total_cost_usd NULL); `GET /api/agents` returns that agent with `total_runs == 1`, `total_cost_usd is None` (JSON null, not 0), `uncosted_runs == 1`.
    - Insert a v0.4 trace with total_cost_usd = 0.005 alongside a pre-v0.4 trace (NULL): the agent returns `total_runs == 2`, `total_cost_usd == 0.005` (sum of non-NULL via SQL NULL-skipping + COALESCE wrapper, NOT including the NULL row), `uncosted_runs == 1`.
    - 503 contract preserved when DB not initialized.
    - All EXISTING tests in `test_server_agents.py` still pass (only assertions are added; none are removed or weakened).
    - pytest passes; ruff clean; lint clean.
  </acceptance_criteria>
  <done>/api/agents extended with total_runs + total_cost_usd + latency_p50_ms + latency_p95_ms + uncosted_runs sourced from agent_totals; existing payload keys preserved byte-identical for backward compat; Pitfall 11 NULL-handling test pinned with a synthetic pre-v0.4 row; DASH-4-04 backend half complete (Phase 36 will handle the frontend render).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Phase 35 SUMMARY.md</name>
  <files>
    .planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/34-pricing-table-and-cost-annotation/34-01-SUMMARY.md (template shape and frontmatter conventions)
    - .planning/STATE.md (for the prior-phase reference line)
    - The 6 prior tasks' commit messages (via git log) and the final test count
  </read_first>
  <behavior>
    - Frontmatter mirrors the Phase 34 SUMMARY shape: phase, plan, subsystem, tags, requires (33-01 for ObservationBus singleton + 34-01 for cost_usd populated), provides (5 query functions + 4 routes + extended /api/agents), requirements-completed (DASH-4-04), tech-stack (added: nothing, patterns: NTILE-on-llm_calls + SQL-side aggregation + COALESCE NULL-skipping + uncosted_runs separate counter + Pitfall 9 retry-aware status enum + Pitfall 7 error_message column hygiene), key-files (created vs modified), metrics (duration, completed date, total tests passed).
    - Body sections: Outcomes (what shipped), Pitfalls mapped (9, 10, 11 each with the specific test that pins the contract), Anti-scope held (zero touches to bus.py/persist.py/cost.py/pricing.py/agent.py/tools/loop.py/storage.py/pyproject.toml), Test count delta, Commits list, Handoff to Phase 36 + 37 (the four route URLs + queries function signatures they will consume).
  </behavior>
  <action>
    Create `.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md` mirroring the Phase 34 SUMMARY shape (read 34-01-SUMMARY.md first; replicate the frontmatter block and the section ordering). Populate from actual commits + test counts captured in this phase. Include a "Handoff" section naming the EXACT public surface Phase 36 (dashboard) and Phase 37 (CLI) will consume: the four query function names + signatures, the four route URL templates with their JSON envelope shapes, and the /api/agents extended field names.

    No em-dashes. No horizontal rules (`---` is reserved for the frontmatter fence). No blockquotes. Apache 2.0 license terms apply (no per-file header for .md).
  </action>
  <verify>
    <automated>test -f /Users/santino/Projects/horus-os/.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md && grep -q "DASH-4-04" /Users/santino/Projects/horus-os/.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md && grep -q "Pitfall 9" /Users/santino/Projects/horus-os/.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md && grep -q "Pitfall 10" /Users/santino/Projects/horus-os/.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md && grep -q "Pitfall 11" /Users/santino/Projects/horus-os/.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md && ! grep -- "—" /Users/santino/Projects/horus-os/.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md exists at the canonical path.
    - Frontmatter includes `requirements-completed: [DASH-4-04]`.
    - Body references Pitfalls 9, 10, 11 each with the specific test file that pins the contract.
    - "Handoff" section enumerates the four queries function signatures + four route URLs Phase 36 and Phase 37 will consume.
    - No em-dashes anywhere (project hard rule from CLAUDE.md line 17).
    - Test count delta documented (e.g., "tests/observability/test_queries_*.py adds N tests; tests/test_server_observability_routes.py adds M tests; tests/test_server_agents.py adds K assertions").
  </acceptance_criteria>
  <done>Phase 35 SUMMARY committed; Phase 36 + 37 have a single self-contained handoff document naming exactly what they can import and call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> /api/observability/* | The `since` query parameter is untrusted user input. parse_window MUST validate; invalid forms return 400, never a 500 or a SQL-level error. |
| HTTP client -> /api/observability/llm-calls | The drilldown route returns row-level data. error_message column is deliberately omitted from the SELECT so Pitfall 7 (PII leak via error contents) cannot reach the wire. |
| queries.py -> SQLite | All filters use parameterized `?` placeholders (sqlite3 driver-level parameterization). NEVER string-format the window into the SQL. |
| tool_invocations.error_message <-> any caller | This column is INDEXABLE-RESTRICTED per Pitfall 9 line 235 + Pitfall 7. Only the persister writes it. NO query function reads it. NO route returns it. Grep-enforced in Task 4 + Task 5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | parse_window window param | mitigate | Parameterized SQL placeholders (sqlite3 `?`), ValueError -> 400 conversion in route handler. No string-formatted SQL anywhere. |
| T-35-02 | Information Disclosure | /api/observability/llm-calls response payload | mitigate | Explicit SELECT column list excludes `error_message`. Assertion in Task 5 acceptance criteria verifies the field is absent from JSON output. Pitfall 7. |
| T-35-03 | Information Disclosure | tool_reliability response payload | mitigate | error_message column NEVER referenced in queries.py (file-content assertion in Task 4 test). Only error_type (exception class name, no user input) is surfaced via last_error_type. Pitfalls 7 + 9. |
| T-35-04 | Denial of Service | /api/observability/llm-calls unbounded scan | mitigate | LIMIT 100 baked into SQL; ?since filter required; idx_llm_calls_created_at index from Phase 32 makes the scan O(log N). |
| T-35-05 | Repudiation | Pre-v0.4 trace cost rendering | mitigate | uncosted_runs separate counter surfaces NULL-rolled rows so the dashboard cannot silently hide them; pre-v0.4 rows render as null + counter, never as $0 (Pitfall 11). |
| T-35-06 | Information Disclosure | Window parameter probing | accept | An attacker probing `?since=24h` vs `?since=30d` learns the rough age distribution of agent runs. Low value; localhost-bound dashboard; no auth surface exists in v0.4 (out-of-scope per RESEARCH.md FEATURES anti-features). |
| T-35-07 | Tampering | SQLite injection via tool_name / agent name string passthrough | mitigate | All response fields come from sqlite3.Row column access; no user input round-trips back into SQL. Tool/agent names that reach queries.py originate from rows the persister already wrote with parameterized inserts (Phase 32 contract). |
</threat_model>

<verification>
## End-of-Phase Verification

After all 7 tasks complete, run the full v0.4 test surface and confirm:

```
cd /Users/santino/Projects/horus-os && \
  .venv/bin/python -m pytest tests/ -x --tb=short && \
  .venv/bin/ruff check src/ tests/ && \
  .venv/bin/python scripts/lint_no_wallclock.py
```

All three commands must exit 0. Test count should be >= 520 (Phase 34 baseline) + N for the new query + route + extension tests (expect roughly 520 + 25-35 new).

## Anti-Scope Held

After completion, run:

```
git diff --stat 33de... HEAD -- src/horus_os/observability/bus.py src/horus_os/observability/persist.py src/horus_os/observability/cost.py src/horus_os/observability/pricing.py src/horus_os/observability/pricing.json src/horus_os/agent.py src/horus_os/tools/loop.py src/horus_os/storage.py pyproject.toml
```

Output MUST be empty (zero lines changed in any of those files). If ANY of those files appears in the diff, the plan failed and must be reverted.

## Pitfalls Mapped to Tasks

| Pitfall | Owner Task | Pin Test |
|---------|-----------|----------|
| Pitfall 9 (retry-aware reliability; never read error_message) | Task 4 | `tests/observability/test_queries_tool_reliability.py`: retry_then_success NOT counted as error; `"error_message" not in queries.py file content` |
| Pitfall 10 (small-sample percentile + NULL not 0 on empty) | Tasks 1, 3 | `tests/observability/test_queries_latency_percentiles.py`: empty window returns p50=None p95=None; n=10 all-at-100ms returns p50=p95=100; module docstring contains "never aggregate-of-aggregates"; grep gate confirms no Python percentile math |
| Pitfall 11 (pre-v0.4 NULL handling) | Tasks 2, 6 | `tests/observability/test_queries_cost_by_agent.py`: uncosted_runs counter for NULL rows; `tests/test_server_agents.py::test_agents_list_pre_v0_4_rows_render_null_cost` |
| Pitfall 7 (error_message PII never leaves persister) | Tasks 4, 5 | `tests/observability/test_queries_tool_reliability.py` file-content assertion; `tests/test_server_observability_routes.py`: llm-calls JSON omits error_message |

## DASH-4-04 Acceptance

`/api/agents` payload includes `total_runs`, `total_cost_usd`, `latency_p50_ms`, `latency_p95_ms`, `uncosted_runs` per agent; pre-v0.4 rows surface as `total_cost_usd: null` + `uncosted_runs: N` (NEVER 0). This is the BACKEND half of DASH-4-04; the frontend render half lands in Phase 36.
</verification>

<success_criteria>
1. `observability/queries.py` exposes `agent_totals(window)`, `cost_by_agent(window)`, `latency_p50_p95(window)`, `tool_reliability(window)` as pure functions returning JSON-serializable dicts; all aggregation in SQL via `NTILE(100) OVER (...)`; grep confirms zero `statistics.quantiles|median|mean` references in queries.py (Pitfall 10 anti-pattern guard).
2. Four new GET routes `/api/observability/{cost,latency,tools,llm-calls}` accept `?since=24h|7d|30d` (plus generic `Nh`/`Nd`) and return JSON; default window `7d` when omitted; invalid window returns 400 with a clear error message.
3. Existing `/api/agents` route gains `total_runs`, `total_cost_usd`, `latency_p50_ms`, `latency_p95_ms`, `uncosted_runs` per agent; pre-v0.4 rows surface as `null` + counter (DASH-4-04 backend; Pitfall 11).
4. Percentile queries return `None` (not 0) for empty windows; `sample_count` returned alongside `p50_ms`/`p95_ms` so callers can apply the n>=10 render rule (Pitfall 10).
5. `tool_reliability` honors the status enum: `retry_then_success` rows do NOT count toward `error_count`; query never reads `error_message` content (Pitfall 9); grep confirms `error_message` does not appear in queries.py.
6. Anti-scope held: zero edits to `bus.py`, `persist.py`, `cost.py`, `pricing.py`, `pricing.json`, `agent.py`, `tools/loop.py`, `storage.py`, `pyproject.toml`.
7. Lint guard green: `time.time()` does not appear in observability/ or server/api.py.
8. Full test suite passes (>= 520 + N new tests); ruff clean; lint_no_wallclock clean.
9. Phase 35 SUMMARY.md committed at `.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md` with handoff to Phase 36 + 37.
</success_criteria>

<output>
After completion, create `.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md` (Task 7 produces it as part of the plan). The SUMMARY is the handoff document Phase 36 (dashboard) and Phase 37 (CLI) read to import the query functions and consume the routes.
</output>
