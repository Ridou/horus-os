---
phase: 54
plan: "01"
subsystem: dependabot-zizmor
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_dependabot_yml_structure.py
  - tests/test_zizmor_workflow_structure.py
autonomous: true
requirements: [DEPBOT-01, DEPBOT-02, DEPBOT-03]
tags: [ci, security, dependabot, zizmor, wave-0, red-by-design]
key_files:
  created:
    - tests/test_dependabot_yml_structure.py
    - tests/test_zizmor_workflow_structure.py
must_haves:
  truths:
    - "tests/test_dependabot_yml_structure.py exists; production tests fail RED until Plan 02 creates .github/dependabot.yml v2 with pip + github-actions ecosystems, 4 pip groups (ai-sdks, otel, web-stack, dev-tools), version-updates only on each group, NO applies-to: security-updates anywhere"
    - "tests/test_zizmor_workflow_structure.py exists; production tests fail RED until Plan 02 creates .github/workflows/zizmor.yml with pull_request trigger filtered on .github/workflows/** paths, permissions: read-all, per-job security-events: write only, zizmorcore/zizmor-action SHA-pinned"
    - "Non-vacuity tests pass NOW via tmp_path synthetic fixtures (mirrors Phase 51/52/53 Plan 01 pattern)"
    - "No em-dashes; ruff clean"
---

<objective>
Wave 0 RED-by-design test scaffolding for Phase 54. Two new test files lint the Dependabot v2 config + the zizmor workflow before Plan 02 lands them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/54-dependabot-tuning-zizmor/54-CONTEXT.md
@.planning/phases/54-dependabot-tuning-zizmor/54-RESEARCH.md
@.planning/phases/51-ci-hardening-substrate/51-01-SUMMARY.md
@.planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_dependabot_yml_structure.py + tests/test_zizmor_workflow_structure.py</name>
  <files>tests/test_dependabot_yml_structure.py, tests/test_zizmor_workflow_structure.py</files>
  <read_first>
    - tests/test_audit_yml_structure.py (Phase 53 Plan 01 analog; same scanner shape + REPO_ROOT)
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py (provides _USES_PATTERN + _ALLOWED_REF)
    - .planning/phases/54-dependabot-tuning-zizmor/54-RESEARCH.md
  </read_first>
  <action>
    Create two test files at tests/ top-level following the Phase 53 convention: REPO_ROOT = Path(__file__).resolve().parents[1], encoding='utf-8' on read_text, no em-dashes, no PII.

    (A) tests/test_dependabot_yml_structure.py:
    DEPENDABOT_PATH = REPO_ROOT / ".github" / "dependabot.yml"

    Production tests (RED until Plan 02):
    - test_dependabot_yml_exists
    - test_version_2: assert text starts with `version: 2` or contains the literal
    - test_pip_ecosystem_present: text contains `package-ecosystem: pip`
    - test_github_actions_ecosystem_present: text contains `package-ecosystem: github-actions`
    - test_four_pip_groups: text contains `ai-sdks:`, `otel:`, `web-stack:`, `dev-tools:` (each as a YAML key); use re.MULTILINE for `^\s+<name>:`
    - test_each_group_version_updates_only: text contains `applies-to: version-updates` AT LEAST 4 times
    - test_no_security_updates_grouping: text does NOT contain `applies-to: security-updates` anywhere (the DEPBOT-02 hard rule)
    - test_cooldown_3_days_default: text contains `default-days: 3` (within a cooldown: block)
    - test_cooldown_14_days_majors: text contains `semver-major-days: 14`
    - test_anthropic_in_ai_sdks: text contains `anthropic` (in the ai-sdks group; substring check OK)
    - test_google_genai_in_ai_sdks: text contains `google-genai`
    - test_opentelemetry_in_otel: text contains `opentelemetry-*` (the wildcard)
    - test_pip_audit_in_dev_tools: text contains `pip-audit` (in dev-tools group)
    - test_schedule_weekly: text contains `interval: weekly` at least once

    Non-vacuity tests (tmp_path synthetic fixtures):
    - test_scanner_catches_security_grouping: write tmp YAML with `applies-to: security-updates`; assert the DEPBOT-02 scanner flags
    - test_scanner_catches_missing_version_2: write tmp YAML without `version: 2`; assert scanner flags
    - test_scanner_catches_single_ecosystem: write tmp YAML with only pip (no github-actions); assert scanner flags

    (B) tests/test_zizmor_workflow_structure.py:
    ZIZMOR_PATH = REPO_ROOT / ".github" / "workflows" / "zizmor.yml"
    Reuse _USES_PATTERN + _ALLOWED_REF.

    Production tests:
    - test_zizmor_yml_exists
    - test_zizmor_on_pull_request: text matches `on:` followed by `pull_request:`
    - test_zizmor_paths_filter: text contains `.github/workflows/**`
    - test_top_level_permissions_read_all
    - test_per_job_security_events_write: text contains `security-events: write`
    - test_zizmor_action_present: text contains `zizmorcore/zizmor-action@`
    - test_zizmor_action_sha_pinned: extract uses line for zizmor-action; assert ref matches _ALLOWED_REF
    - test_no_pull_request_target: text does NOT contain `pull_request_target`
    - test_persist_credentials_false
    - test_every_uses_sha_pinned

    Non-vacuity scanners (tmp_path):
    - test_scanner_catches_missing_paths_filter
    - test_scanner_catches_mutable_tag
    - test_scanner_catches_pull_request_target
  </action>
  <acceptance_criteria>
    - Both files exist at tests/ top-level
    - `pytest tests/test_dependabot_yml_structure.py tests/test_zizmor_workflow_structure.py --collect-only -q` reports >= 30 tests
    - All non-vacuity tests pass NOW
    - Production tests RED-by-design
    - ruff check + format pass on both files
    - No em-dashes
  </acceptance_criteria>
  <done>Two test files committed; RED-by-design.</done>
</task>

</tasks>
