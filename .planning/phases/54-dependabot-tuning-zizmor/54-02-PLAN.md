---
phase: 54
plan: "02"
subsystem: dependabot-zizmor
type: execute
wave: 1
depends_on: ["54-01"]
files_modified:
  - .github/dependabot.yml
  - .github/workflows/zizmor.yml
autonomous: true
requirements: [DEPBOT-01, DEPBOT-02, DEPBOT-03]
tags: [ci, security, dependabot, zizmor, wave-1, green-flip]
tech_stack:
  added:
    - "zizmorcore/zizmor-action@<sha> (CI-only)"
  patterns:
    - "Dependabot v2 with grouped version-updates and un-grouped security-updates"
    - "zizmor pull_request workflow with security-events: write per-job; top-level permissions: read-all"
key_files:
  created:
    - .github/dependabot.yml
    - .github/workflows/zizmor.yml
must_haves:
  truths:
    - "D-01: .github/dependabot.yml v2; pip + github-actions ecosystems; pip has 4 groups (ai-sdks, otel, web-stack, dev-tools) with applies-to: version-updates only; cooldown default 3 days + semver-major 14 days"
    - "D-02: NO `applies-to: security-updates` matcher anywhere in the file (DEPBOT-02 hard rule)"
    - "D-03: .github/workflows/zizmor.yml triggers on pull_request paths filtered to .github/workflows/**; top-level permissions: read-all; per-job security-events: write; zizmorcore/zizmor-action SHA-pinned to 40-char hex; actions/checkout persist-credentials: false"
    - "No em-dashes; conventional-commit prefixes; ruff clean"
---

<objective>
Wave 1 GREEN flip: create .github/dependabot.yml + .github/workflows/zizmor.yml. All Plan 01 RED tests flip GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/54-dependabot-tuning-zizmor/54-CONTEXT.md
@.planning/phases/54-dependabot-tuning-zizmor/54-RESEARCH.md
@.planning/phases/54-dependabot-tuning-zizmor/54-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/dependabot.yml + .github/workflows/zizmor.yml</name>
  <files>.github/dependabot.yml, .github/workflows/zizmor.yml</files>
  <action>
    Create both files per the RESEARCH.md shape. SHA pin for zizmorcore/zizmor-action resolved at execute time via `gh api`. actions/checkout uses the existing repo-wide SHA pin (11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2).

    NO em-dashes in any comment or doc-string. NO `pull_request_target`. NO `id-token: write` (Dependabot doesn't need OIDC; zizmor needs security-events: write only).

    Validate by running tests/test_dependabot_yml_structure.py + tests/test_zizmor_workflow_structure.py; all production tests must flip GREEN.
  </action>
  <acceptance_criteria>
    - Both files exist
    - All Plan 01 production tests pass
    - No em-dashes
    - ruff/format clean (these are .yml files; no Python lint)
  </acceptance_criteria>
  <done>GREEN flip complete.</done>
</task>

</tasks>
