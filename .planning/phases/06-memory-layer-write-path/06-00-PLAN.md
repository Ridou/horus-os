---
phase: 06
plan: "00"
type: execute
wave: 1
depends_on: ["03-00", "05-00"]
files_modified:
  - src/horus_os/storage.py
  - src/horus_os/types.py
  - src/horus_os/memory/notes.py
  - src/horus_os/memory/tools.py
  - src/horus_os/memory/__init__.py
  - src/horus_os/__init__.py
  - tests/test_storage.py
  - tests/test_notes_store_write.py
  - tests/test_memory_tools_write.py
autonomous: true
requirements:
  - MEM-02
  - MEM-03
tags:
  - memory
  - write
  - audit-trail
  - schema-v2

must_haves:
  truths:
    - "Schema version bumps to 2 with an additive `note_writes` table. Existing v1 databases upgrade idempotently on `Database.init()`. No data migration; the table is empty until the first write."
    - "`Database.record_note_write(operation, rel_path, bytes_before, bytes_after, content, trace_id=None) -> str` writes one row and returns a UUID4 hex write_id"
    - "`Database.list_note_writes(*, limit=50, offset=0) -> list[NoteWrite]` returns writes newest-first"
    - "`NoteWrite(write_id, created_at, operation, rel_path, bytes_before, bytes_after, content, trace_id)` mirrors the table"
    - "`NotesStore(notes_dir, *, on_write=None)` accepts an optional callback that receives a NoteWrite after every successful write"
    - "`NotesStore.create_note(rel_path, content) -> NoteWrite` creates a new markdown file. Raises FileExistsError if the file exists. Raises PermissionError on path escape. Auto-creates parent directories inside notes_dir."
    - "`NotesStore.append_note(rel_path, content) -> NoteWrite` appends to an existing file. Raises FileNotFoundError if the file does not exist. Raises PermissionError on path escape. Adds a single newline between existing content and the new content only when the existing file does not already end with a newline."
    - "Tool factories `create_note_tool(store)` and `append_note_tool(store)` return Tools whose handlers return the NoteWrite as a JSON-serializable dict"
    - "`on_write` callback exceptions are swallowed (logger failures do not poison writes)"
  artifacts:
    - path: "src/horus_os/storage.py"
      provides: "Extended schema, record_note_write, list_note_writes"
      contains: "note_writes"
      min_lines: 180
    - path: "src/horus_os/memory/notes.py"
      provides: "create_note and append_note methods on NotesStore"
      contains: "def create_note"
      min_lines: 100

tasks:
  - id: "1"
    title: "Add NoteWrite dataclass to types.py"
    files:
      - src/horus_os/types.py

  - id: "2"
    title: "Schema v2 + record_note_write + list_note_writes in storage.py"
    files:
      - src/horus_os/storage.py
    notes: "Bump SCHEMA_VERSION to 2. Append the note_writes CREATE TABLE to SCHEMA_SQL. Update init() to UPDATE schema_version when the existing row is below SCHEMA_VERSION."

  - id: "3"
    title: "create_note and append_note on NotesStore + on_write hook"
    files:
      - src/horus_os/memory/notes.py
    notes: "Path safety identical to read path. Newline normalization on append: insert '\\n' before the new content only when the existing file does not end with a newline."

  - id: "4"
    title: "Tool factories create_note_tool, append_note_tool"
    files:
      - src/horus_os/memory/tools.py
      - src/horus_os/memory/__init__.py

  - id: "5"
    title: "Re-export NoteWrite + new tool factories"
    files:
      - src/horus_os/__init__.py

  - id: "6"
    title: "Tests for storage v2 + write paths + tools"
    files:
      - tests/test_storage.py
      - tests/test_notes_store_write.py
      - tests/test_memory_tools_write.py
    notes: "Extend test_storage.py with two cases: schema v2 table exists, list_note_writes ordering. New file test_notes_store_write.py: create happy, create on existing raises, create with parent dir auto-create, append happy, append on missing raises, append newline normalization (file already ends with \\n vs does not), path escape blocked, on_write callback fires, on_write exception swallowed. New file test_memory_tools_write.py: tool name + schema + handler dict shape for both tools."

  - id: "7"
    title: "Verify lint + format + tests + CLI smoke"

  - id: "8"
    title: "Commit + SUMMARY + advance + push"

threat_model:
  - threat: "Agent writes to a file outside the notes folder"
    mitigation: "Same path-escape guard as the read path: resolve, verify the resolved path is a child of notes_dir, raise PermissionError otherwise."
  - threat: "Agent overwrites existing user content without consent"
    mitigation: "create_note raises FileExistsError on collision. append_note never deletes. There is no edit/replace tool in v0.1; explicit overwrite lands in a later phase with confirmation UX."

success_criteria:
  - "ruff check exits 0"
  - "ruff format --check exits 0"
  - "pytest -v exits 0 with at least 16 new tests"
  - "`from horus_os import NoteWrite, create_note_tool, append_note_tool` succeeds"
  - "Schema v1 databases open cleanly under v2 code (idempotent migration)"
  - "06-00-SUMMARY.md committed, ROADMAP and STATE advanced, branch pushed"
