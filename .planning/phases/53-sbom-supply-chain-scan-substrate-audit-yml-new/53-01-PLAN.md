---
phase: 53
plan: "01"
subsystem: sbom-supply-chain-scan-substrate
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_audit_yml_structure.py
  - tests/test_release_yml_sbom_steps.py
  - tests/test_pip_audit_ignore_discipline.py
  - tests/test_verify_release_sbom.py
  - tests/test_pyproject_pip_audit_dev.py
autonomous: true
requirements: [SBOM-01, SBOM-02, SBOM-03, SUPPLY-01, SUPPLY-02, SUPPLY-03, SUPPLY-04]
tags: [ci, security, sbom, supply-chain, regression-tests, wave-0, red-by-design]
tech_stack:
  added: []
  patterns:
    - "stdlib re regex scanning over .github/workflows/*.yml (no PyYAML)"
    - "Path(__file__).resolve().parents[1] for top-level tests"
    - "importlib.util.spec_from_file_location to load scripts/verify_release.py in-process (mirrors tests/test_release_gate.py:31-46)"
    - "RED-by-design production assertions (Wave 0 signal; Plan 02 turns GREEN)"
    - "Non-vacuity synthetic fixtures (tmp_path); scanners proven to fire BEFORE Wave 1 production code lands"
key_files:
  created:
    - tests/test_audit_yml_structure.py
    - tests/test_release_yml_sbom_steps.py
    - tests/test_pip_audit_ignore_discipline.py
    - tests/test_verify_release_sbom.py
    - tests/test_pyproject_pip_audit_dev.py
  modified: []
decisions:
  - "Tests live at tests/ top-level (mirrors Phase 52 Plan 01 D-02); REPO_ROOT = Path(__file__).resolve().parents[1]"
  - "Wave 0 tests are RED-by-design: production assertions fail until Plan 02 lands the workflow + script + pyproject edits. Non-vacuity tests pass NOW via tmp_path synthetic fixtures (mirrors Phase 51 Plan 01 + Phase 52 Plan 01)"
  - "verify_release.py SBOM check test (tests/test_verify_release_sbom.py) loads the script in-process via importlib.util.spec_from_file_location (mirrors PATTERNS Plan 01 Pattern A from Phase 52)"
  - "pip-audit ignore-list discipline test (tests/test_pip_audit_ignore_discipline.py) scans `.github/pip-audit-ignore.txt` and parses every CVE/GHSA/PYSEC-shaped line; rejects entries lacking a preceding `# YYYY-MM-DD: <reason>` comment"
  - "Pyproject test reads pyproject.toml as text (not via tomllib) for simplicity and grep-style assertion; mirrors prior test_pyproject style if any; otherwise uses tomllib (stdlib) and asserts 'pip-audit' substring in the dev extras list"
metrics:
  estimated_context: "~35%"
  task_count: 2
must_haves:
  truths:
    - "tests/test_audit_yml_structure.py exists at tests/ top-level and fails RED on SUPPLY-01 + SUPPLY-02 + SUPPLY-04 production assertions until Wave 1 creates .github/workflows/audit.yml with on: pull_request, top-level permissions: read-all, per-job contents: read, NO id-token: write anywhere, pip-audit dual-mode (-s osv AND -s pypi), strategy.matrix.extras over [dev] AND [dev,otel], dependency-review-action with the license allowlist"
    - "tests/test_release_yml_sbom_steps.py exists and fails RED on SBOM-01 + SBOM-02 production assertions until Wave 1 appends the two SBOM-generation steps + two attest-sbom invocations to .github/workflows/release.yml AND modifies the sigstore inputs glob to include ./dist/*.cdx.json"
    - "tests/test_pip_audit_ignore_discipline.py exists and fails RED on SUPPLY-03 until Wave 1 creates .github/pip-audit-ignore.txt + .github/pip-audit-tracking/README.md"
    - "tests/test_verify_release_sbom.py exists and fails RED on SBOM-03 until Wave 1 flips scripts/verify_release.py check_sbom_signature from CheckResult(ok=None, diagnostic='SKIPPED - Phase 53 ...') to an active check that verifies both SBOM bundles and runs the dependency-tree diff"
    - "tests/test_pyproject_pip_audit_dev.py exists and fails RED on SUPPLY-01 (pyproject row) until Wave 1 adds 'pip-audit>=2.10,<3' to pyproject.toml [project.optional-dependencies].dev"
    - "All non-vacuity synthetic-fixture tests pass NOW (Wave 0): each scanner is proven to fire on a known violation written to tmp_path, mirroring the Phase 52 Plan 01 §Non-Vacuity Coverage pattern"
    - "PITFALL (id-token in audit.yml) regression coverage: tests/test_audit_yml_structure.py asserts the workflow text does NOT contain the literal `id-token: write` anywhere (audit.yml runs on PRs from forks; granting OIDC there would let fork PRs mint sigstore identities)"
    - "Every test file uses encoding='utf-8' on read_text(), str(path) on all subprocess args, sys.executable not bare python (cross-OS-green discipline)"
    - "No em-dashes in any docstring, diagnostic message, or fixture text (CLAUDE.md HR3)"
  artifacts:
    - path: "tests/test_audit_yml_structure.py"
      provides: "Structural workflow lint for SUPPLY-01, SUPPLY-02, SUPPLY-04 against .github/workflows/audit.yml"
      contains: "audit.yml, on: pull_request, permissions: read-all, contents: read, pypa/gh-action-pip-audit, -s osv, -s pypi, strategy, matrix, extras, [dev], [dev,otel], dependency-review-action, allow-licenses, Apache-2.0, MIT, BSD-2-Clause, BSD-3-Clause, ISC, PSF-2.0, comment-summary-in-pr"
    - path: "tests/test_release_yml_sbom_steps.py"
      provides: "Structural workflow lint for SBOM-01, SBOM-02 against .github/workflows/release.yml additions"
      contains: "release.yml, cyclonedx-py environment, .venv-sbom-clean, .venv-sbom-extras, dist/horus_os-clean.cdx.json, dist/horus_os-dev-otel.cdx.json, --schema-version 1.6, --output-format JSON, attest-sbom, sbom-path, ./dist/*.cdx.json"
    - path: "tests/test_pip_audit_ignore_discipline.py"
      provides: "Lint for SUPPLY-03 ignore-list + tracking directory discipline"
      contains: ".github/pip-audit-ignore.txt, .github/pip-audit-tracking, YYYY-MM-DD, CVE, GHSA, PYSEC"
    - path: "tests/test_verify_release_sbom.py"
      provides: "Unit tests for SBOM-03 (verify_release.py check_sbom_signature active behavior)"
      contains: "check_sbom_signature, importlib.util.spec_from_file_location, _verify_release_under_test, EXPECTED_IDENTITY_TEMPLATE, EXPECTED_ISSUER, .cdx.json"
    - path: "tests/test_pyproject_pip_audit_dev.py"
      provides: "Lint for SUPPLY-01 pyproject row"
      contains: "pyproject.toml, [project.optional-dependencies], dev, pip-audit>=2.10,<3"
---

<objective>
Wave 0 RED-by-design test scaffolding for Phase 53: ship the five NEW test files
that lint the audit.yml + release.yml SBOM steps + pip-audit-ignore discipline
+ verify_release.py SBOM check + pyproject.toml [dev] extras change.

Purpose: Mirror Phase 51 + Phase 52 Plan 01 Wave 0 pattern. Tests use non-vacuity
synthetic fixtures so scanners are proven to fire BEFORE Wave 1 production code
lands. Production assertions are RED-by-design (the files they scan do not exist
yet, except release.yml which exists but lacks the SBOM additions); Plan 02
flips them GREEN by creating `.github/workflows/audit.yml`, extending
`.github/workflows/release.yml`, creating `.github/pip-audit-ignore.txt` +
`.github/pip-audit-tracking/README.md`, editing `scripts/verify_release.py`
check_sbom_signature, and adding `pip-audit>=2.10,<3` to pyproject.toml [dev].

Output: 5 new test files committed; running the quick test command exits non-zero
with predictable RED assertions on production tests and GREEN on every non-vacuity
synthetic-fixture test. No production source code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-CONTEXT.md
@.planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-RESEARCH.md
@.planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-VALIDATION.md
@.planning/phases/52-signing-substrate-release-yml-new/52-01-SUMMARY.md
@.planning/phases/52-signing-substrate-release-yml-new/52-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_audit_yml_structure.py + tests/test_release_yml_sbom_steps.py + tests/test_pip_audit_ignore_discipline.py + tests/test_pyproject_pip_audit_dev.py (workflow + ignore-list + pyproject RED scaffolds)</name>
  <files>tests/test_audit_yml_structure.py, tests/test_release_yml_sbom_steps.py, tests/test_pip_audit_ignore_discipline.py, tests/test_pyproject_pip_audit_dev.py</files>
  <read_first>
    - tests/test_release_yml_structure.py (Phase 52 analog; same regex-over-workflow-text pattern + REPO_ROOT shape)
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py (provides _USES_PATTERN and _ALLOWED_REF regex constants)
    - .github/workflows/release.yml (existing file; Plan 02 will extend it)
    - .github/workflows/ci.yml (the existing two-variant install-smoke pattern audit.yml mirrors)
    - pyproject.toml (current dev extras: fastapi, httpx, pytest, pytest-asyncio, ruff; Plan 02 appends pip-audit)
    - .planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-RESEARCH.md (audit.yml shape, release.yml diff shape, pip-audit-ignore discipline)
    - .planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-VALIDATION.md (rows for SBOM-01, SBOM-02, SUPPLY-01..04)
  </read_first>
  <action>
    Create four sibling test files at tests/ top-level. Each file MUST set `REPO_ROOT = Path(__file__).resolve().parents[1]`. Each file MUST follow the cross-OS subprocess discipline (encoding='utf-8' on read_text, sys.executable not bare python, str(path) on subprocess args). NO em-dashes in any docstring, diagnostic, or string literal.

    (A) tests/test_audit_yml_structure.py contains production assertions for SUPPLY-01, SUPPLY-02, SUPPLY-04 against .github/workflows/audit.yml:
      Define `AUDIT_YML_PATH = REPO_ROOT / ".github" / "workflows" / "audit.yml"`. Reuse the _USES_PATTERN and _ALLOWED_REF constants from tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py (copy into this file at module scope; do not import across test dirs).

      Production tests (RED until Plan 02 creates the file):
      1. test_audit_yml_exists: AUDIT_YML_PATH.is_file() is True; failure diagnostic: "Phase 53 Plan 02 (Wave 1) must create .github/workflows/audit.yml per SUPPLY-01"
      2. test_on_pull_request_trigger: regex assert text contains `re.compile(r"on:\s*\n\s*pull_request\s*:?", re.MULTILINE)` AND assert text does NOT contain `pull_request_target` (forbidden per CIHARD-01 + Phase 51 precedent)
      3. test_top_level_permissions_read_all: `re.compile(r"^permissions:\s*read-all\s*$", re.MULTILINE)` must match
      4. test_no_id_token_write_anywhere: assert literal `id-token: write` is NOT anywhere in the file (PITFALL: granting OIDC in audit.yml would let fork PRs mint sigstore identities)
      5. test_pip_audit_action_present: assert substring `pypa/gh-action-pip-audit` is in text
      6. test_pip_audit_action_sha_pinned: extract every uses: line that contains `pypa/gh-action-pip-audit`; assert each ref half matches _ALLOWED_REF (40-char hex)
      7. test_pip_audit_dual_mode: assert text contains both literal `-s osv` AND literal `-s pypi` (dual-mode requirement; if the gh-action wrapper uses `extra-args:` value, those substrings still appear in the workflow file)
      8. test_two_variant_matrix: assert text contains `strategy:` AND `matrix:` AND the literal `[dev]` AND the literal `[dev,otel]` (in the same job block; not a strict-distance check, just substring presence)
      9. test_dependency_review_action_present: assert substring `actions/dependency-review-action` is in text
      10. test_dependency_review_action_sha_pinned: extract uses: line for actions/dependency-review-action; assert ref matches _ALLOWED_REF
      11. test_dependency_review_license_allowlist: assert ALL of these literals are in text: `Apache-2.0`, `MIT`, `BSD-2-Clause`, `BSD-3-Clause`, `ISC`, `PSF-2.0`. Also assert literal `allow-licenses` is present.
      12. test_dependency_review_comment_on_failure: assert literal `comment-summary-in-pr: on-failure` is in text (PR-comment-on-rejection per SUPPLY-02)
      13. test_persist_credentials_false: assert literal `persist-credentials: false` is in text (CIHARD-03)
      14. test_every_uses_in_audit_yml_sha_pinned: iterate every `uses:` line via _USES_PATTERN; each ref half must match _ALLOWED_REF. Skip pure-comment lines.

      Non-vacuity tests (pass NOW; prove the scanners fire on synthetic violations written to tmp_path):
      - test_scanner_catches_synthetic_missing_permissions: write minimal YAML without `permissions: read-all`; assert the scanner flags
      - test_scanner_catches_synthetic_id_token_write: write YAML containing `id-token: write`; assert the scanner flags
      - test_scanner_catches_synthetic_mutable_tag: write YAML with `uses: pypa/gh-action-pip-audit@v1`; assert the SHA-pin scanner flags
      - test_scanner_catches_synthetic_missing_dual_mode: write YAML with only `-s osv` and no `-s pypi`; assert dual-mode scanner flags

      Helper functions take a `workflow_path` argument so the same helper can scan the real audit.yml and a synthetic tmp_path fixture.

    (B) tests/test_release_yml_sbom_steps.py contains production assertions for SBOM-01 + SBOM-02 against the Phase 53 additions to .github/workflows/release.yml:
      `RELEASE_YML_PATH = REPO_ROOT / ".github" / "workflows" / "release.yml"`. This file already exists (Phase 52); Plan 02 extends it.

      Production tests (RED until Plan 02 appends the SBOM steps):
      1. test_release_yml_exists: file must exist (was created Phase 52; this is a sanity check)
      2. test_fresh_venv_sbom_generation: assert text contains `python -m venv .venv-sbom-clean` AND `python -m venv .venv-sbom-extras` (NOT `pip freeze`). Diagnostic must cite SBOM-01 + the FRESH-venv rule.
      3. test_cyclonedx_environment_called: assert text contains substring `cyclonedx-py environment`
      4. test_schema_version_1_6_locked: assert text contains literal `--schema-version 1.6`
      5. test_output_format_json: assert text contains literal `--output-format JSON`
      6. test_two_sbom_variants: assert text contains both `horus_os-clean.cdx.json` AND `horus_os-dev-otel.cdx.json`
      7. test_cyclonedx_bom_version_pinned: assert text contains literal `'cyclonedx-bom>=7.3,<8'` (single-quoted to lock the version range)
      8. test_sigstore_inputs_include_cdx_json: extract the sigstore step's `inputs:` line; assert it ends with `./dist/*.cdx.json` (the smallest possible Phase 53 diff per Phase 52 D-10)
      9. test_two_attest_sbom_invocations: assert text contains EXACTLY TWO `uses: actions/attest-sbom@` occurrences
      10. test_attest_sbom_sha_pinned: extract every uses: line containing `actions/attest-sbom`; each ref must match the 40-char hex pattern (reuse _USES_PATTERN + _ALLOWED_REF; copy at module scope)
      11. test_attest_sbom_sbom_path_paired: assert text contains literal `sbom-path: 'dist/horus_os-clean.cdx.json'` AND literal `sbom-path: 'dist/horus_os-dev-otel.cdx.json'`
      12. test_pip_install_wheel_for_extras: assert text contains the substring `dist/*.whl[dev,otel]` (the extras SBOM venv installs the wheel with [dev,otel] extras per SBOM-02)

      Non-vacuity tests (pass NOW; tmp_path synthetic fixtures):
      - test_scanner_catches_pip_freeze_substitution: write synthetic release.yml containing `pip freeze` instead of `cyclonedx-py environment`; assert the FRESH-venv scanner flags
      - test_scanner_catches_single_attest_sbom: write synthetic with only ONE attest-sbom call; assert the two-invocation scanner flags
      - test_scanner_catches_unpinned_schema_version: write synthetic without `--schema-version 1.6`; assert the schema-version-locked scanner flags

    (C) tests/test_pip_audit_ignore_discipline.py contains production assertions for SUPPLY-03:
      `IGNORE_PATH = REPO_ROOT / ".github" / "pip-audit-ignore.txt"`. `TRACKING_DIR = REPO_ROOT / ".github" / "pip-audit-tracking"`. `TRACKING_README = TRACKING_DIR / "README.md"`.

      Helper `_parse_ignore_file(text: str) -> list[tuple[int, str, str | None]]`: returns (line_number, entry, preceding_comment) tuples for every non-blank non-comment line that looks like a CVE/GHSA/PYSEC ID (regex `r"^(CVE-\d+-\d+|GHSA-[a-zA-Z0-9-]+|PYSEC-\d+-\d+)$"`). The preceding_comment is the immediately-preceding `#`-line stripped of its `#`, or None if there is no comment on the line directly before.

      Production tests:
      1. test_ignore_file_exists: IGNORE_PATH.is_file() True
      2. test_tracking_dir_exists: TRACKING_DIR.is_dir() True
      3. test_tracking_readme_exists: TRACKING_README.is_file() True
      4. test_every_entry_has_dated_comment: for each parsed entry, assert preceding_comment is not None AND matches `re.compile(r"^\s*\d{4}-\d{2}-\d{2}\s*:\s*.+")` (the `YYYY-MM-DD: <reason>` discipline). Diagnostic names the violating entry + line number.
      5. test_tracking_readme_documents_convention: TRACKING_README text contains the literals `YYYY-MM-DD`, `CVE`, `pip-audit-ignore.txt`, and `transitive`
      6. test_at_launch_ignore_file_may_be_empty_but_format_documented: read IGNORE_PATH text; assert the file is non-empty (it MAY contain only a header comment documenting the format) AND assert the file's first non-blank line is a `#`-comment (the format documentation header)

      Non-vacuity tests:
      - test_parser_catches_undated_entry: write tmp_path content with `CVE-2025-12345` immediately preceded by `# no date here`; assert the discipline scanner flags
      - test_parser_catches_orphan_entry: write content with `CVE-2025-99999` with no preceding comment at all; assert the scanner flags
      - test_parser_accepts_well_formed_entry: write content with `# 2026-03-15: awaiting urllib3 1.27 release tracking issue #1234\nGHSA-abcd-1234-efgh\n`; assert no violations

    (D) tests/test_pyproject_pip_audit_dev.py contains production assertion for SUPPLY-01 pyproject row:
      `PYPROJECT_PATH = REPO_ROOT / "pyproject.toml"`. Use `tomllib` (stdlib in 3.11+) to parse.

      Production tests:
      1. test_pyproject_exists: PYPROJECT_PATH.is_file() True
      2. test_dev_extras_includes_pip_audit: parse pyproject with tomllib; navigate to `project.optional-dependencies.dev`; assert it is a list; assert at least one entry starts with `pip-audit` (substring match)
      3. test_dev_extras_pip_audit_version_pinned: same parse; find the pip-audit entry; assert it equals exactly `pip-audit>=2.10,<3` (the locked pin per SUPPLY-01)
      4. test_base_deps_unchanged: parse pyproject; navigate to `project.dependencies`; assert it does NOT contain any entry starting with `pip-audit` (the load-bearing constraint #1: zero base-dep change in v0.6 except this single [dev] extras change)
      5. test_no_other_optional_extras_have_pip_audit: parse pyproject; navigate to project.optional-dependencies; for every key OTHER than `dev`, assert the list does NOT contain `pip-audit` (only [dev] gets it; the [all] meta-extra is a convenience aggregate and pip-audit does NOT belong there)

      Non-vacuity: synthetic pyproject content via tmp_path:
      - test_scanner_catches_missing_pip_audit: write tmp pyproject with `dev = ["pytest"]`; assert scanner flags
      - test_scanner_catches_wrong_pin: write tmp pyproject with `dev = ["pip-audit"]` (unpinned); assert scanner flags
  </action>
  <verify>
    <automated>pytest tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_pyproject_pip_audit_dev.py -v 2>&amp;1 | grep -E "passed|failed"</automated>
    Expected Wave-0 state: all non-vacuity tests pass; all production tests fail RED with diagnostics naming the missing file + SBOM-NN / SUPPLY-NN requirement. Then run `ruff check tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_pyproject_pip_audit_dev.py` and `ruff format --check ...` (both must exit 0).
  </verify>
  <acceptance_criteria>
    - File tests/test_audit_yml_structure.py exists at top-level tests/
    - File tests/test_release_yml_sbom_steps.py exists at top-level tests/
    - File tests/test_pip_audit_ignore_discipline.py exists at top-level tests/
    - File tests/test_pyproject_pip_audit_dev.py exists at top-level tests/
    - Each file contains `REPO_ROOT = Path(__file__).resolve().parents[1]`
    - tests/test_audit_yml_structure.py contains every literal: `pypa/gh-action-pip-audit`, `actions/dependency-review-action`, `Apache-2.0`, `MIT`, `BSD-2-Clause`, `BSD-3-Clause`, `ISC`, `PSF-2.0`, `pull_request`, `permissions: read-all`, `[dev]`, `[dev,otel]`, `-s osv`, `-s pypi`, `id-token: write`, `comment-summary-in-pr: on-failure`, `persist-credentials: false`
    - tests/test_audit_yml_structure.py defines AT MINIMUM 14 production tests + 4 non-vacuity tests
    - tests/test_release_yml_sbom_steps.py contains every literal: `cyclonedx-py environment`, `--schema-version 1.6`, `--output-format JSON`, `horus_os-clean.cdx.json`, `horus_os-dev-otel.cdx.json`, `cyclonedx-bom>=7.3,<8`, `./dist/*.cdx.json`, `actions/attest-sbom`, `.venv-sbom-clean`, `.venv-sbom-extras`, `dist/*.whl[dev,otel]`
    - tests/test_release_yml_sbom_steps.py defines AT MINIMUM 12 production tests + 3 non-vacuity tests
    - tests/test_pip_audit_ignore_discipline.py contains the regex `r"^\s*\d{4}-\d{2}-\d{2}\s*:\s*.+"` (the dated-comment shape) and the literals `.github/pip-audit-ignore.txt`, `.github/pip-audit-tracking`
    - tests/test_pyproject_pip_audit_dev.py imports `tomllib` and reads pyproject.toml; asserts `pip-audit>=2.10,<3` in project.optional-dependencies.dev
    - `pytest tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_pyproject_pip_audit_dev.py --collect-only -q` reports >= 38 tests total (14+4 + 12+3 + 6+3 + 5+2 minimum)
    - All non-vacuity synthetic-fixture tests PASS NOW (run isolated: `pytest -v -k 'scanner or parser'` against the four files; expect all selected tests pass)
    - Production tests are RED-by-design; each failure diagnostic names the missing file + cites the SBOM-NN or SUPPLY-NN requirement
    - `ruff check tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_pyproject_pip_audit_dev.py` exits 0
    - `ruff format --check tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_pyproject_pip_audit_dev.py` exits 0
    - `grep -Pl '[\x{2013}\x{2014}]' tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_pyproject_pip_audit_dev.py` returns no matches
  </acceptance_criteria>
  <done>Four test files committed at tests/ top-level; production tests are RED-by-design; non-vacuity scanner tests are GREEN; ruff is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/test_verify_release_sbom.py (verify_release.py check_sbom_signature active-mode RED scaffold)</name>
  <files>tests/test_verify_release_sbom.py</files>
  <read_first>
    - tests/test_release_verification.py (Phase 52 analog; same importlib.util.spec_from_file_location pattern)
    - scripts/verify_release.py lines 283-296 (current check_sbom_signature stub returning ok=None, "SKIPPED - Phase 53 lands SBOM generation + signing")
    - .planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-RESEARCH.md (§verify_release.py SBOM check flip, describes the active-check shape Plan 02 lands)
    - .planning/phases/52-signing-substrate-release-yml-new/52-02-SUMMARY.md (Phase 52 D-08 stub contract that Phase 53 flips)
  </read_first>
  <action>
    Create tests/test_verify_release_sbom.py at tests/ top-level. Follow the same Pattern A in-process module loader from tests/test_release_verification.py (importlib.util.spec_from_file_location, register in sys.modules as `_verify_release_under_test`).

    Constants:
    - `REPO_ROOT = Path(__file__).resolve().parents[1]`
    - `EXPECTED_ISSUER = "https://token.actions.githubusercontent.com"` (must match the Phase 52 constant)
    - `CANONICAL_FIXTURE_DIR = REPO_ROOT / "tests" / "fixtures" / "sigstore" / "canonical"` (where the v0.6.0-rc1 rehearsal fixtures live; binaries are out of scope per Phase 52 Plan 01 decision)

    Production tests (RED until Plan 02 flips check_sbom_signature to active):
    1. test_check_sbom_signature_no_longer_returns_skipped: load the module via Pattern A; call `mod.check_sbom_signature(version="0.6.0-rc1")`; assert `result.ok is not None` (the stub flip means it must now return True or False, never None). Diagnostic must say "Phase 53 SBOM-03 must flip the D-08 SKIP stub".
    2. test_check_sbom_signature_signature_supports_bundle_artifact_args: introspect the function signature via `inspect.signature(mod.check_sbom_signature)`; assert the parameter list includes at least `version` and `cert_oidc_issuer` and at least one of (`clean_bundle_path`, `extras_bundle_path`, `clean_sbom_path`, `extras_sbom_path`) or a single `sbom_paths` arg, and assert the new signature is NOT the Phase 52 stub signature `(version)`. Diagnostic must say "Phase 53 must extend check_sbom_signature signature to accept SBOM bundles".
    3. test_check_sbom_signature_two_bundles_verified: invoke `mod.check_sbom_signature` with clean + extras bundle paths pointing into CANONICAL_FIXTURE_DIR; skip with pytest.skip("Canonical SBOM bundles land at v0.6.0-rc1 rehearsal time per VALIDATION.md Manual-Only Verifications") if either bundle file does not exist; otherwise assert `result.ok is True` AND `result.diagnostic` contains both literal `clean` AND `dev-otel` (or equivalent substrings naming both bundles)
    4. test_check_sbom_signature_diff_against_wheel_fails_on_stale_sbom: monkey-patch the dependency-tree diff function (whatever Plan 02 names it; the test discovers via getattr) to return a synthetic "stale" diff (extra components); assert `result.ok is False` AND `result.diagnostic` contains the word `stale` or `mismatch` or `differ` (case-insensitive). If the diff function name cannot be discovered, pytest.skip with a hint citing SBOM-03 + Plan 02 + the expected function name pattern (`_diff_sbom_against_wheel` or `check_sbom_dependency_tree`).
    5. test_main_sbom_check_runs_active: invoke `mod.main(["--version", "0.6.0-rc1", "--cert-oidc-issuer", EXPECTED_ISSUER, "--check", "sbom"])`; with capture_output verify the output does NOT contain `SKIP` (it must run actively now, even if the result is failure due to missing live fixtures). Skip the test if Plan 02 has not yet renamed/extended the SBOM check CLI choice.

    Non-vacuity tests (pass NOW; do not require Plan 02 changes):
    - test_phase_52_stub_currently_returns_skipped: explicitly call the CURRENT stub via Pattern A; assert `result.ok is None` AND `"SKIPPED" in result.diagnostic`. This test is INVERTED on Plan 02 land (the Wave 1 plan documents that this test flips its assertion when the stub is flipped; this Wave 0 test passes today as proof that the test infrastructure works).
    - test_module_loads_cleanly_today: call the module loader helper; assert it returns a non-None module. Passes today (the script exists from Phase 52).

    Note: tests 1-5 above will be RED today (because check_sbom_signature is still the Phase 52 stub returning ok=None). On Plan 02 land, tests 1-5 flip GREEN and the non-vacuity `test_phase_52_stub_currently_returns_skipped` flips RED, at which point the executor of Plan 02 MUST update that one non-vacuity test to assert the new active-mode shape, or delete it. The acceptance criteria for Plan 02 includes this inverse-flip update.
  </action>
  <verify>
    <automated>pytest tests/test_verify_release_sbom.py -v 2>&amp;1 | grep -E "passed|failed|skipped"</automated>
    Expected Wave-0 state: tests 1-5 fail RED; the two non-vacuity tests pass. Run `ruff check tests/test_verify_release_sbom.py` and `ruff format --check tests/test_verify_release_sbom.py` (both must exit 0).
  </verify>
  <acceptance_criteria>
    - File tests/test_verify_release_sbom.py exists at top-level tests/
    - File contains `REPO_ROOT = Path(__file__).resolve().parents[1]`
    - File contains the literals: `EXPECTED_ISSUER`, `https://token.actions.githubusercontent.com`, `check_sbom_signature`, `importlib.util.spec_from_file_location`, `_verify_release_under_test`, `0.6.0-rc1`
    - File defines AT MINIMUM 5 production tests + 2 non-vacuity tests
    - `pytest tests/test_verify_release_sbom.py --collect-only -q` reports >= 7 tests
    - `pytest tests/test_verify_release_sbom.py -v -k 'phase_52_stub or module_loads'` reports both tests PASSING (non-vacuity coverage proves the test infrastructure works)
    - `ruff check tests/test_verify_release_sbom.py` exits 0
    - `ruff format --check tests/test_verify_release_sbom.py` exits 0
    - `grep -Pl '[\x{2013}\x{2014}]' tests/test_verify_release_sbom.py` returns no matches
  </acceptance_criteria>
  <done>tests/test_verify_release_sbom.py committed; production tests RED-by-design; non-vacuity tests GREEN; ruff clean.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full Wave 0 verification:

```bash
pytest tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_verify_release_sbom.py tests/test_pyproject_pip_audit_dev.py -v
ruff check tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_verify_release_sbom.py tests/test_pyproject_pip_audit_dev.py
ruff format --check tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_verify_release_sbom.py tests/test_pyproject_pip_audit_dev.py
pytest --collect-only -q tests/test_audit_yml_structure.py tests/test_release_yml_sbom_steps.py tests/test_pip_audit_ignore_discipline.py tests/test_verify_release_sbom.py tests/test_pyproject_pip_audit_dev.py
```

Expected end state:
- Total tests collected: >= 45
- All non-vacuity / synthetic-fixture / today-stub tests PASS
- All production tests FAIL or SKIP (audit.yml does not exist; release.yml lacks SBOM steps; .github/pip-audit-ignore.txt does not exist; verify_release.py check_sbom_signature is still the stub; pyproject does not yet have pip-audit)
- Ruff clean on all five files
- No em-dashes in any new file
- Full suite delta: no NEW failures outside these five files
</verification>

<success_criteria>
- Five Wave 0 test files exist and are committed
- Production assertions are RED-by-design and cite the SBOM-NN / SUPPLY-NN requirement in failure diagnostics
- Non-vacuity / synthetic-fixture tests pass NOW
- Tests use `Path(__file__).resolve().parents[1]` and cross-OS subprocess discipline
- No em-dashes anywhere
- Ruff lint + format pass
- The Wave-0 test infrastructure is provably ready for Plan 02 to flip GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/53-sbom-supply-chain-scan-substrate-audit-yml-new/53-01-SUMMARY.md`
using the standard SUMMARY template. Required sections:
- Tasks Completed table
- Production Assertions: RED vs GREEN State table
- Non-Vacuity Coverage table
- Test Collection Counts
- Self-Check section
- Requirements Coverage table (SBOM-01..03 + SUPPLY-01..04)
- Hand-off Notes for Plan 02
</output>
