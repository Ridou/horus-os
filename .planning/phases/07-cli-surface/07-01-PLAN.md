---
phase: 07
plan: "01"
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - src/horus_os/_providers/_anthropic.py
  - src/horus_os/_providers/_gemini.py
  - src/horus_os/agent.py
  - src/horus_os/cli/__init__.py
  - src/horus_os/cli/run_cmd.py
  - src/horus_os/__main__.py
  - src/horus_os/__init__.py
  - tests/test_agent_loop.py
  - tests/test_conversation_anthropic.py
  - tests/test_conversation_gemini.py
  - tests/test_cli_run.py
autonomous: true
requirements:
  - CORE-02
  - AGENT-01
tags:
  - multi-turn-loop
  - run-subcommand
  - conversation-state

must_haves:
  truths:
    - "Each provider module exposes a `Conversation` class that holds message history and translates between the normalized Tool/ToolResult shape and the SDK's native shape"
    - "`Conversation.send(prompt=..., tool_results=..., tools=..., max_tokens=...)` accepts either an opening prompt OR tool results from the previous turn (exclusive). Raises ValueError when both or neither are given."
    - "`run_agent_loop(prompt, *, registry, provider='anthropic', model=None, max_iterations=10, on_tool_result=None) -> AgentResult` orchestrates the multi-turn loop: send prompt, execute tool_uses, send tool_results, repeat until no tool_uses or max_iterations"
    - "Tool execution exceptions are captured into ToolResult.error by `execute_tool_uses` and sent back to the model so it can recover or apologize"
    - "max_iterations defaults to 10 and is enforced; the final AgentResult is returned even if the loop runs to the iteration cap"
    - "`horus-os run <prompt> [--provider X] [--model Y] [--no-record] [--max-iterations N] [--data-dir PATH]` runs the loop with default tools registered (read_file, list_notes, search_notes, read_note, create_note, append_note) and records a trace"
    - "Default registry binds `read_file_tool(base_dir=config.notes_dir)` so file reads are sandboxed to the notes directory by default"
    - "Notes tool factories receive a NotesStore that has `on_write` wired to `Database.record_note_write` so every memory write lands in the audit trail"
    - "When no API key is set for the selected provider, the run subcommand prints a clear error pointing at the env var and exits 2 (rather than crashing in the SDK)"
    - "All provider calls remain lazily imported so optional dependencies stay optional"
  artifacts:
    - path: "src/horus_os/_providers/_anthropic.py"
      provides: "Conversation class with sync send"
      contains: "class Conversation"
      min_lines: 50
    - path: "src/horus_os/_providers/_gemini.py"
      provides: "Conversation class with sync send"
      contains: "class Conversation"
      min_lines: 50
    - path: "src/horus_os/agent.py"
      provides: "run_agent_loop function"
      contains: "def run_agent_loop"
      min_lines: 30
    - path: "src/horus_os/cli/run_cmd.py"
      provides: "horus-os run subcommand"
      contains: "def run_run"
      min_lines: 40

tasks:
  - id: "1"
    title: "Add Conversation class to _anthropic.py"
    files:
      - src/horus_os/_providers/_anthropic.py
    notes: "Persist message history in self._messages and the previous response's content in self._last_assistant_content. On send-with-tool-results, append the assistant turn then the user turn containing tool_result blocks. content for the tool_result is `str(output)` on success, or the error string with is_error=True on failure."

  - id: "2"
    title: "Add Conversation class to _gemini.py"
    files:
      - src/horus_os/_providers/_gemini.py
    notes: "Store contents list as native genai_types.Content objects. On send-with-tool-results, append the previous model Content + a new user Content containing FunctionResponse parts. response field is `{'content': output_or_error_string}`."

  - id: "3"
    title: "run_agent_loop in agent.py"
    files:
      - src/horus_os/agent.py
    notes: "Imports the registry execute helper from horus_os.tools.loop. Dispatches by provider to the right Conversation class. on_tool_result forwards to execute_tool_uses' on_log."

  - id: "4"
    title: "horus-os run subcommand"
    files:
      - src/horus_os/cli/run_cmd.py
      - src/horus_os/cli/__init__.py
      - src/horus_os/__main__.py
    notes: "Build a ToolRegistry with default tools. Wire NotesStore.on_write to Database.record_note_write. Record a final trace via Database.record_trace. Print the final text response plus a short summary line (provider, model, tool calls, latency)."

  - id: "5"
    title: "Re-export run_agent_loop from package root"
    files:
      - src/horus_os/__init__.py

  - id: "6"
    title: "Tests"
    files:
      - tests/test_conversation_anthropic.py
      - tests/test_conversation_gemini.py
      - tests/test_agent_loop.py
      - tests/test_cli_run.py
    notes: "Conversation tests use the sys.modules injection pattern from earlier. Loop tests use a fake Conversation injected via monkeypatch at the agent.py boundary. CLI test mocks run_agent_loop to assert the subcommand wires correctly."

  - id: "7"
    title: "Verify lint + format + tests + CLI smoke"

  - id: "8"
    title: "Commit + SUMMARY + ROADMAP/STATE advance (phase 07 closes) + push"

success_criteria:
  - "ruff check exits 0"
  - "ruff format --check exits 0"
  - "pytest -v exits 0 with at least 14 new tests"
  - "`from horus_os import run_agent_loop` succeeds"
  - "`horus-os run --help` shows the new subcommand"
  - "Phase 07 marked complete in ROADMAP/STATE, Phase 08 ready to plan"
