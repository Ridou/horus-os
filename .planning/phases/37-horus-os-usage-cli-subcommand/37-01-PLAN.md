---
phase: "37"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/horus_os/__main__.py
  - src/horus_os/cli/__init__.py
  - src/horus_os/cli/usage_cmd.py
  - src/horus_os/observability/queries.py
  - src/horus_os/observability/__init__.py
  - src/horus_os/server/api.py
  - docs/CLI.md
  - tests/test_cli_usage.py
  - tests/fixtures/usage_output_schema.json
  - tests/observability/test_queries_cost_by_model.py
  - tests/test_server_observability_routes.py
  - .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md
autonomous: true
requirements:
  - USAGE-01
  - USAGE-02
  - USAGE-03
  - USAGE-04
subsystem: cli
tags: [cli, usage, observability, v0.4, stdlib-only]

must_haves:
  truths:
    - "USAGE-01: `horus-os usage --since 7d` returns a usage report; --since accepts 24h, 7d, 30d, and any positive Nh/Nd form; invalid input exits non-zero with `invalid window:` on stderr"
    - "USAGE-02: `--format json|csv|table` (default table) controls output shape; the JSON schema is documented in docs/CLI.md and pinned by tests/test_cli_usage.py against tests/fixtures/usage_output_schema.json so schema drift fails the test"
    - "USAGE-03: `--by model|tool|agent` (default agent) slices the report; `--by agent` calls cost_by_agent; `--by tool` calls tool_reliability; `--by model` calls a NEW additive cost_by_model in queries.py plus a matching /api/observability/cost-by-model route; CLI --format json output equals the route's JSON body byte-for-byte over the same window where data overlaps"
    - "USAGE-04: costs render rounded to 6 decimal places via Python round(value, 6), durations as integer ms, with the same numeric values across all three formats; the canonical PRICE-02 cost 0.006150 (cache-aware, 1000 input + 200 output + 500 cache_read on claude-sonnet-4-6) renders exactly 0.006150 in JSON, NEVER 0.006149999999; jq pipes on JSON output never trip on float-precision noise"
  artifacts:
    - path: "src/horus_os/cli/usage_cmd.py"
      provides: "run_usage(args, *, stdout, stderr) -> int handler mirroring traces_cmd.py shape; three formatters (json/csv/table); dispatch by --by to cost_by_agent / tool_reliability / cost_by_model"
      min_lines: 80
    - path: "src/horus_os/__main__.py"
      provides: "argparse subparser `usage` registered with --since, --format, --by, --data-dir options"
      contains: 'sub.add_parser("usage"'
    - path: "src/horus_os/cli/__init__.py"
      provides: "run_usage exported from package and listed in __all__"
      contains: "run_usage"
    - path: "src/horus_os/observability/queries.py"
      provides: "ADDITIVE new public function cost_by_model(db, window) -> list[dict]; existing functions (parse_window, agent_totals, cost_by_agent, latency_p50_p95, tool_reliability) UNMODIFIED"
      contains: "def cost_by_model"
    - path: "src/horus_os/observability/__init__.py"
      provides: "cost_by_model added to imports and __all__"
      contains: "cost_by_model"
    - path: "src/horus_os/server/api.py"
      provides: "ADDITIVE new GET /api/observability/cost-by-model route returning {\"models\": [...]} with the SAME shape cost_by_model returns; existing 5 observability routes UNMODIFIED"
      contains: '/api/observability/cost-by-model'
    - path: "docs/CLI.md"
      provides: "horus-os usage subcommand reference plus JSON output schema with example output plus precision contract"
      contains: "horus-os usage"
    - path: "tests/fixtures/usage_output_schema.json"
      provides: "Pinned JSON output fixture; schema drift fails tests/test_cli_usage.py"
      contains: "0.006150"
    - path: "tests/test_cli_usage.py"
      provides: "End-to-end tests covering --since parsing, three formats, --by routing, byte-for-byte route parity (USAGE-03), float-precision rounding (USAGE-04), schema pin (USAGE-02)"
      min_lines: 120
    - path: "tests/observability/test_queries_cost_by_model.py"
      provides: "Unit tests for cost_by_model: empty window, window exclusion, multi-model rollup, cache token sums, uncosted_runs counter, ordering, package re-export"
      min_lines: 60
  key_links:
    - from: "src/horus_os/cli/usage_cmd.py"
      to: "src/horus_os/observability/queries.py"
      via: "import plus call cost_by_agent / cost_by_model / tool_reliability"
      pattern: "from horus_os.observability.queries import"
    - from: "src/horus_os/cli/usage_cmd.py"
      to: "src/horus_os/storage.py"
      via: "Config.load(args.data_dir) plus Database(cfg.db_path); db.exists check returning rc=1 to stderr (mirrors traces_cmd.py)"
      pattern: "Database\\(.*db_path\\)"
    - from: "src/horus_os/__main__.py"
      to: "src/horus_os/cli/usage_cmd.py"
      via: "import run_usage plus sub.add_parser('usage') plus set_defaults(func=run_usage)"
      pattern: "set_defaults\\(func=run_usage\\)"
    - from: "tests/test_cli_usage.py"
      to: "src/horus_os/server/api.py"
      via: "TestClient.get('/api/observability/cost-by-model?since=7d') byte-for-byte compared against run_usage(--format json --by model) over the same seeded DB"
      pattern: "/api/observability/cost-by-model"
---

<objective>
Ship `horus-os usage --since 7d --format json|csv|table --by model|tool|agent` as an argparse subparser. Reuses `observability/queries.py` from Phase 35 so the CLI and dashboard cannot disagree. Adds ONE additive function to queries.py (`cost_by_model`) and ONE additive route (`/api/observability/cost-by-model`) so `--by model` has a queryable counterpart the CLI can pin against byte-for-byte (USAGE-03). Stdlib `json` and `csv`; no new dependencies. Float-precision UX trap closed by Python `round(value, 6)` BEFORE serialization (USAGE-04, Pitfall float-precision).

Purpose: USAGE category (USAGE-01..04). v0.4 milestone progress 6/8 phases (75%).
Output: A `horus-os usage` subcommand that pipes cleanly to `jq` and `column -t -s,`, a documented JSON schema in `docs/CLI.md`, and a route-parity guarantee so anyone scripting against the CLI gets the same numbers as someone scripting against the HTTP API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md
@.planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md
@CLAUDE.md
@src/horus_os/__main__.py
@src/horus_os/cli/__init__.py
@src/horus_os/cli/traces_cmd.py
@src/horus_os/observability/queries.py
@src/horus_os/observability/__init__.py
@src/horus_os/server/api.py

<interfaces>
Extracted contracts. Use these directly. No codebase exploration needed.

From src/horus_os/cli/traces_cmd.py (CLOSEST analog, mirror this shape):

```
def run_traces(args: argparse.Namespace, *, stdout: TextIO, stderr: TextIO) -> int:
    config = Config.load(getattr(args, "data_dir", None))
    if not config.db_path.exists():
        stderr.write(f"No database at {config.db_path}. Run `horus-os init` first.\n")
        return 1
    db = Database(config.db_path)
    ...
```

Pattern: getattr for optional args; explicit Config.load; db_path.exists() guard returning rc=1 to stderr; json branch via `if getattr(args, "json", False)`; table branch via `_format_table(...)` returning a "\n".join(lines).

From src/horus_os/observability/queries.py:

```
def parse_window(window: str) -> str            # ValueError on bad input
def agent_totals(db, window) -> list[dict]      # NOT used by Phase 37 (covers per-agent rollup with percentiles; the CLI's --by agent uses cost_by_agent so dollar shape matches the /cost route)
def cost_by_agent(db, window) -> list[dict]     # USAGE-03 --by agent
def latency_p50_p95(db, window) -> dict         # NOT used by --by; latency is a separate slice not in USAGE-03's three options
def tool_reliability(db, window) -> list[dict]  # USAGE-03 --by tool
```

Return-shape conventions (already pinned by Phase 35 tests):

- cost_by_agent rows carry: agent, total_cost_usd (float, 6dp via Python round, None converts to 0.0 here per existing Phase 35 contract), total_input_tokens (int), total_output_tokens (int), total_cache_read_input_tokens (int), total_cache_creation_input_tokens (int), run_count (int), uncosted_runs (int). Ordered: total_cost_usd DESC, agent ASC.
- tool_reliability rows carry: tool_name, call_count, success_count, error_count, retry_then_success_count, expected_no_result_count, success_rate (float 4dp or None), last_error_type (exception class name only, never error text content), last_error_at. Ordered: call_count DESC, tool_name ASC.

From src/horus_os/server/api.py (existing observability routes; mirror this shape exactly):

```
@app.get("/api/observability/cost")
def observability_cost(since: str = "7d") -> dict[str, Any]:
    cfg = _config()
    if not cfg.db_path.exists():
        raise HTTPException(503, detail="database not initialized; run `horus-os init`")
    try:
        rows = cost_by_agent(Database(cfg.db_path), since)
    except ValueError as exc:
        raise HTTPException(400, detail=str(exc)) from exc
    return {"agents": rows}
```

Envelope: `{"agents": [...]}`, `{"tools": [...]}`. For `cost_by_model`, USE `{"models": [...]}` (matches the noun the route name uses).

From src/horus_os/__main__.py (subparser registration pattern; mirror the `traces` block at line 46):

```
traces_p = sub.add_parser("traces", help="List recent agent traces")
traces_p.add_argument("--data-dir", type=Path, default=None, help="...")
traces_p.add_argument("--limit", type=int, default=20, help="...")
traces_p.add_argument("--json", action="store_true", help="...")
traces_p.set_defaults(func=run_traces)
```

From src/horus_os/cli/__init__.py:

```
from horus_os.cli.traces_cmd import run_traces
__all__ = ["run_agents", "run_init", "run_run", "run_serve", "run_traces"]
```

Pattern: import run_<name>; add to __all__ alphabetically.

From src/horus_os/observability/__init__.py (lines 26-32 plus 63-80):

```
from horus_os.observability.queries import (
    agent_totals,
    cost_by_agent,
    latency_p50_p95,
    parse_window,
    tool_reliability,
)
__all__ = [..., "cost_by_agent", ..., "latency_p50_p95", "parse_window", ..., "tool_reliability"]
```

Pattern: import inside the multi-line block; add to __all__ alphabetically. The new symbol `cost_by_model` slots between `cost_by_agent` and `latency_p50_p95` (alphabetical).

From src/horus_os/observability/queries.py (cost_by_agent SQL pattern. MIRROR THIS for cost_by_model but partition by lc.model instead of agent):

- WITH windowed_traces CTE filters traces by created_at plus agent_profile_name IS NOT NULL
- WITH windowed_calls CTE joins llm_calls to windowed_traces by trace_id
- per-model rollup SUMs llm_calls.cost_usd (cost is per-call on llm_calls, not on traces)
- ORDER BY COALESCE(total_cost_usd, 0) DESC, model ASC for deterministic ties
- Cache tokens come directly from llm_calls SUM (NOT from traces rollup; traces only carries non-cache totals)
- Python `round(value, 6)` at the boundary (NOT SQLite ROUND); SQLite's banker rounding is platform-variable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: usage_cmd.py skeleton plus argparse wiring plus re-export</name>
  <files>
    src/horus_os/cli/usage_cmd.py,
    src/horus_os/cli/__init__.py,
    src/horus_os/__main__.py,
    tests/test_cli_usage.py
  </files>
  <read_first>
    src/horus_os/cli/traces_cmd.py (entire file, mirror its run_traces signature, Config.load plus db_path.exists guard, stderr handling, return-code conventions);
    src/horus_os/__main__.py lines 46-64 (traces subparser block, the exact shape to mirror) plus lines 11-12 (cli imports);
    src/horus_os/cli/__init__.py (entire, 19 lines; copy the import plus __all__ pattern);
    src/horus_os/observability/queries.py lines 56-86 (parse_window, reuse it for --since validation; ValueError to rc=1 with `invalid window:` on stderr, mirroring the route boundary's 400 conversion at line 334-335 of server/api.py).
  </read_first>
  <action>
    Create src/horus_os/cli/usage_cmd.py with:
    Module docstring (one line summarizing the subcommand, then a paragraph noting it reuses observability/queries.py so CLI and dashboard cannot disagree per USAGE-03, plus a paragraph noting the rounding contract per USAGE-04).
    Imports: `from __future__ import annotations`; `import argparse`; `from typing import TextIO`; `from horus_os.config import Config`; `from horus_os.storage import Database`; `from horus_os.observability.queries import cost_by_agent, parse_window, tool_reliability` (cost_by_model added in Task 4).
    Public function `run_usage(args: argparse.Namespace, *, stdout: TextIO, stderr: TextIO) -> int` mirroring traces_cmd.run_traces shape: 1. `config = Config.load(getattr(args, "data_dir", None))`. 2. `if not config.db_path.exists(): stderr.write(f"No database at {config.db_path}. Run \`horus-os init\` first.\n"); return 1`. 3. Parse window via `parse_window(getattr(args, "since", "7d"))`; on ValueError write `f"{exc}\n"` to stderr (the ValueError message from parse_window already starts with `invalid window:`) and return rc=1. Note: parse_window returns the ISO threshold string but we do NOT pass that to the query, the query functions accept the original `since` arg and call parse_window themselves. We validate up front so a bad arg fails fast at the CLI rather than after hitting the DB. 4. Build `db = Database(config.db_path)`. 5. Dispatch by `--by`: `agent` calls cost_by_agent(db, since); `tool` calls tool_reliability(db, since); `model` calls a placeholder that raises NotImplementedError("--by model lands in Task 4 of plan 37-01"). The placeholder is REPLACED in Task 4. Tests in this Task 1 cover agent plus tool dispatch only; Task 4 adds the model assertion. 6. Dispatch by `--format` to placeholder formatters `_format_json`, `_format_csv`, `_format_table` each defined as `def _format_X(rows: list[dict]) -> str: raise NotImplementedError("formatter lands in Task 2/3 of plan 37-01")`. Stub return value: the dispatch line `stdout.write(_format_X(rows) + "\n")` reaches the NotImplementedError. Tests in this Task 1 confirm the dispatch hits the right stub by asserting `pytest.raises(NotImplementedError)` with `match=` strings. 7. Return rc=0 on success path (only reachable once formatters exist in later tasks; for Task 1, all happy-path tests hit the NotImplementedError stubs by design).
    Modify src/horus_os/cli/__init__.py: insert `from horus_os.cli.usage_cmd import run_usage` (alphabetical after traces_cmd since `u > t`); add `"run_usage"` to `__all__` keeping alphabetical order (between run_traces and end since `usage > traces`).
    Modify src/horus_os/__main__.py: update the cli import line (currently `from horus_os.cli import run_agents, run_init, run_run, run_serve, run_traces`) to add `run_usage` alphabetically. Add a new subparser block AFTER the existing `agents_p` block (which ends around line 207) and BEFORE the `return parser` line with: `usage_p = sub.add_parser("usage", help="Print usage report (cost, latency, tool reliability)")`; `usage_p.add_argument("--data-dir", type=Path, default=None, help="Override the platform default data directory.")`; `usage_p.add_argument("--since", default="7d", help="Window (24h, 7d, 30d, or any Nh/Nd). Default 7d.")`; `usage_p.add_argument("--format", choices=["json", "csv", "table"], default="table", help="Output shape. Default table.")`; `usage_p.add_argument("--by", choices=["agent", "tool", "model"], default="agent", help="Slice the report. Default agent.")`; `usage_p.set_defaults(func=run_usage)`.
    Create tests/test_cli_usage.py with six tests: `test_usage_missing_db_writes_to_stderr_and_returns_1` (build args via build_parser().parse_args(["usage", "--data-dir", str(tmp_path)]); call run_usage with StringIO buffers; assert rc=1, stderr contains "No database at", stdout empty); `test_usage_invalid_since_writes_invalid_window_to_stderr` (init a real db via Config.load(tmp_path) plus Database(...).init() mirroring tests/test_cli_traces.py pattern, then call run_usage with --since "garbage"; assert rc=1 and stderr contains "invalid window:"); `test_usage_dispatch_agent_hits_format_stub` (init db, call run_usage with --by agent --format json against an empty db; assert pytest.raises(NotImplementedError, match="formatter lands in Task 2/3")); `test_usage_dispatch_tool_hits_format_stub` (same but --by tool --format csv); `test_usage_dispatch_model_hits_query_stub` (--by model --format table; assert pytest.raises(NotImplementedError, match="--by model lands in Task 4")); `test_usage_subparser_registered_with_choices` (import build_parser from horus_os.__main__; parse ["usage", "--since", "24h", "--format", "csv", "--by", "tool"] and assert args.since == "24h", args.format == "csv", args.by == "tool", args.func is run_usage); `test_run_usage_re_exported_from_cli_package` (`from horus_os.cli import run_usage` succeeds AND `run_usage` is in `horus_os.cli.__all__`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cli_usage.py -q && grep -c 'def run_usage' src/horus_os/cli/usage_cmd.py | grep -q '^1$' && grep -q 'sub.add_parser("usage"' src/horus_os/__main__.py && grep -q 'choices=\["agent", "tool", "model"\]' src/horus_os/__main__.py && grep -q 'choices=\["json", "csv", "table"\]' src/horus_os/__main__.py && .venv/bin/python -m horus_os usage --help > /dev/null && .venv/bin/ruff check src/horus_os/cli/usage_cmd.py src/horus_os/cli/__init__.py src/horus_os/__main__.py tests/test_cli_usage.py && .venv/bin/ruff format --check src/ tests/</automated>
  </verify>
  <done>
    The skeleton dispatches correctly to placeholder stubs that raise NotImplementedError with the documented messages. Tests prove --since validation happens before the DB hit and that the three --format choices and three --by choices route to the right placeholders. No formatter code exists yet (Task 2/3); no cost_by_model code exists yet (Task 4).
  </done>
</task>

<task type="auto">
  <name>Task 2: JSON formatter plus float-precision rounding plus docs/CLI.md schema plus pinning fixture</name>
  <files>
    src/horus_os/cli/usage_cmd.py,
    docs/CLI.md,
    tests/test_cli_usage.py,
    tests/fixtures/usage_output_schema.json
  </files>
  <read_first>
    .planning/research/PITFALLS.md line 403 (the float-precision UX trap row, the canonical Pitfall text for USAGE-04);
    .planning/research/PITFALLS.md line 400 (the table-truncate row, context for Task 3's table formatter);
    .planning/phases/34-pricing-table-and-cost-annotation/34-01-SUMMARY.md (lines 40-50, the hand-computed canonical cost `0.006150` derivation: `claude-sonnet-4-6` with 1000 input + 200 output + 500 cache_read yields `(1000*3 + 200*15 + 500*0.30) / 1_000_000 = 0.006150`. This is the load-bearing fixture cost for the schema pin);
    src/horus_os/observability/queries.py lines 196-294 (cost_by_agent, the exact return shape the formatter receives; especially the 6-decimal Python rounding at line 282).
  </read_first>
  <action>
    Replace the `_format_json(rows: list[dict]) -> str` stub in src/horus_os/cli/usage_cmd.py with the real implementation:
    Walk each row dict and produce a new dict with float fields rounded to 6 decimals via `round(value, 6)` (Python, NOT json.dumps's default behavior, NOT format strings, the Python round before serialization is the contract per USAGE-04).
    Specifically round these keys when present and non-None: `total_cost_usd`, `success_rate`, and the new `cost_usd` key on cost_by_model rows (added in Task 4).
    Cast these keys to int when present and non-None: any field whose name ends with `_ms` or `_tokens` or `_count` or `_runs` (the queries module already returns ints for these, but defensive int() guards against any float drift through SQLite's NULL handling).
    None values pass through as None (becomes JSON null). The query layer's Pitfall 11 honesty contract (None for uncosted, never 0) MUST survive serialization.
    Wrap in the envelope shape `{"by": "<agent|tool|model>", "since": "<original since arg>", "rows": [...]}`. The wrapper is what makes `jq '.rows[]'` work uniformly across the three --by modes.
    Return `json.dumps(payload, indent=2, sort_keys=True)` (sort_keys for deterministic output that diffs cleanly against the fixture).
    The envelope's `by` and `since` are passed into _format_json as additional args; update the call site in run_usage to pass them.
    Update the run_usage dispatch in src/horus_os/cli/usage_cmd.py so the `--format json` branch calls `_format_json(rows, by=args.by, since=args.since)`. Once Task 4 lands, this dispatch also handles `--by model` rows transparently because the formatter is shape-agnostic (it just rounds and wraps).
    Create docs/CLI.md as a NEW file (this is the canonical CLI reference; future CLI subcommands can extend it):
    Top-level heading "horus-os CLI reference".
    One-paragraph intro: "Every subcommand exposes `run_<name>(args, *, stdout, stderr) -> int`. Tests pass StringIO buffers and assert on captured text. All numeric output respects the precision contract documented under `horus-os usage` below."
    Section "## horus-os usage" with subsections for each flag, then a "### JSON output schema" subsection documenting the envelope shape and per-row fields for each --by mode, then a "### Precision contract" subsection: "Costs round to 6 decimal places via Python's `round(value, 6)` BEFORE JSON serialization. Durations cast to integer ms. This is uniform across `--format json|csv|table`. The 6-decimal rounding eliminates float-precision noise that breaks `jq` and `column` pipes (for example `0.04200000000000001` becomes `0.042`)."
    Example output block under "### Example": a triple-backtick json block showing the canonical 1-row response with `total_cost_usd: 0.006150` so the doc's example matches the test fixture exactly.
    Create tests/fixtures/usage_output_schema.json with the pinned canonical JSON output. The fixture is the body that run_usage emits for `--by agent --format json --since 7d` against a DB seeded with exactly ONE trace plus ONE llm_call carrying: agent_profile_name: "default"; model: "claude-sonnet-4-6"; input_tokens: 1000, output_tokens: 200, cache_read_input_tokens: 500; cost_usd: 0.006150 (hand-computed per PRICE-02). Shape: `{"by": "agent", "rows": [{"agent": "default", "run_count": 1, "total_cache_creation_input_tokens": 0, "total_cache_read_input_tokens": 500, "total_cost_usd": 0.006150, "total_input_tokens": 1000, "total_output_tokens": 200, "uncosted_runs": 0}], "since": "7d"}`. Note `sort_keys=True` puts keys in alphabetical order at every level. The fixture is the source of truth; the test diffs run_usage output against this exact bytes.
    Add to tests/test_cli_usage.py these new tests: `test_json_format_canonical_cost_renders_exactly_six_decimals` (seed the canonical row via a helper `_seed_canonical_run(db)` factored into a fixture or top-of-file helper, capture run_usage stdout, parse it, assert `payload["rows"][0]["total_cost_usd"] == 0.006150` AND the raw stdout substring `0.00615` is present AND `0.006149` is ABSENT AND `0.006150000000` is ABSENT, the USAGE-04 anti-float-noise canary); `test_json_format_matches_pinned_fixture_schema` (capture run_usage stdout for the same seeded DB, load the fixture, assert `json.loads(stdout) == json.loads(fixture)`, schema drift fails here); `test_json_format_empty_db_returns_empty_rows_envelope` (empty DB, --by agent --format json; assert payload == {"by": "agent", "rows": [], "since": "7d"}); `test_json_format_preserves_none_for_uncosted_run` (seed a row with total_cost_usd=NULL in traces mirroring Phase 35's test fixture pattern at tests/observability/test_queries_agent_totals.py; assert the JSON output has `total_cost_usd: null` Python None, NEVER `0` or `0.0`, Pitfall 11 surviving serialization); `test_json_envelope_carries_by_and_since` (call with --since 24h --by tool; assert payload["by"] == "tool" and payload["since"] == "24h"); `test_docs_cli_md_documents_horus_os_usage_subcommand` (file-content assertion: `(Path(repo_root) / "docs" / "CLI.md").read_text()` contains all of `## horus-os usage`, `### JSON output schema`, `### Precision contract`, `0.006150`, `--since`, `--format`, `--by`).
    Re-run the existing Task 1 tests; they should still pass since dispatch behavior unchanged for csv/table/model (still NotImplementedError stubs).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cli_usage.py -q && grep -c 'round(.*, 6)' src/horus_os/cli/usage_cmd.py | awk '$1 >= 1' | grep -q . && grep -q 'sort_keys=True' src/horus_os/cli/usage_cmd.py && grep -F '0.006150' tests/fixtures/usage_output_schema.json | grep -q . && ! grep -F '0.00614' tests/fixtures/usage_output_schema.json && python -c "import json; json.loads(open('tests/fixtures/usage_output_schema.json').read())" && test -f docs/CLI.md && grep -F 'horus-os usage' docs/CLI.md | grep -q . && grep -cE '## horus-os usage|### JSON output schema|### Precision contract' docs/CLI.md | grep -q '^3$' && .venv/bin/ruff check src/ tests/ && .venv/bin/ruff format --check src/ tests/</automated>
  </verify>
  <done>
    JSON formatter rounds all cost floats to 6 decimals via Python round() before json.dumps, preserves None for uncosted runs, and emits a stable envelope `{"by", "rows", "since"}` with sort_keys. The canonical PRICE-02 cost `0.006150` renders exactly as `0.006150` and the float-precision anti-canary (`0.006149` substring absence) holds. docs/CLI.md documents the schema and precision contract; tests/fixtures/usage_output_schema.json pins the exact bytes so any future change to keys, order, or precision fails loudly.
  </done>
</task>

<task type="auto">
  <name>Task 3: CSV plus table formatters plus tests (USAGE-04 precision parity across all three formats)</name>
  <files>
    src/horus_os/cli/usage_cmd.py,
    tests/test_cli_usage.py
  </files>
  <read_first>
    src/horus_os/cli/traces_cmd.py lines 39-47 (_format_table, the column-aligned printer to mirror, including the ellipsis truncation pattern from PITFALLS.md line 400);
    .planning/research/PITFALLS.md line 400 (the table-truncate row, long agent names need ellipsis, NOT mid-word break; reuse the existing `horus-os traces` truncation pattern);
    Python stdlib csv module: csv.DictWriter signature requires a stream plus fieldnames; write_header() plus writerows() is the canonical idiom;
    The just-shipped _format_json from Task 2 (read it to confirm field order convention so CSV column order matches JSON key order).
  </read_first>
  <action>
    Replace the `_format_csv(rows: list[dict]) -> str` stub in src/horus_os/cli/usage_cmd.py with the real implementation:
    Use `io.StringIO` as the buffer plus `csv.DictWriter(buf, fieldnames=fieldnames)`. fieldnames are computed from the first row's keys sorted alphabetically (matches JSON sort_keys=True ordering). If rows is empty, return an empty string (consumer-friendly: `wc -l` returns 0; the absence of a header is the empty-state signal).
    Apply the SAME rounding rules used in _format_json: round cost floats to 6 decimals via `round(value, 6)`, cast `_ms`/`_tokens`/`_count`/`_runs` ints via `int()`, pass None through (DictWriter writes None as empty string by default, which is the correct CSV empty-cell convention, distinguishable from 0). USAGE-04 precision-parity: the SAME numeric values in JSON appear in CSV.
    Call `writer.writeheader()` then `writer.writerows(processed_rows)`. Return `buf.getvalue()` WITHOUT a trailing newline (csv.DictWriter writes its own line terminators; the dispatch in run_usage already appends a "\n" via `stdout.write(_format_X(rows) + "\n")`).
    Replace the `_format_table(rows: list[dict]) -> str` stub with a column-aligned printer mirroring traces_cmd._format_table: If rows is empty, return `"(no usage data in window)"`. Compute column names from the first row's keys (alphabetical, same as CSV). Compute column widths as `max(len(str(value)) for value in column) + 2` capped at 30 chars (long agent names truncate with ellipsis `name[:27] + "..."` per PITFALLS.md line 400). Apply the SAME rounding rules (round costs to 6dp, cast durations/tokens/counts to int, render None as `-` single hyphen so the Pitfall 11 NULL contract has a visual analog in table mode). Render header line, separator line of `-` matching header length, then each row. Join with `"\n"` and return (no trailing newline; dispatch appends one).
    Add to tests/test_cli_usage.py these new tests: `test_csv_format_round_trip_via_dictreader` (seed canonical run, capture stdout for --format csv --by agent, parse with `csv.DictReader(io.StringIO(stdout))`, assert first row has `total_cost_usd` == `"0.00615"` OR `"0.006150"` via `float(...)` and `pytest.approx(0.006150, abs=1e-9)`. Verify column order alphabetical matches JSON); `test_csv_format_canonical_cost_no_float_precision_noise` (capture stdout for --format csv on the canonical seed; assert raw stdout substring `0.00614` is ABSENT); `test_csv_format_empty_db_returns_empty_string` (empty DB --format csv; assert stdout is just a single "\n" empty body plus dispatch's appended newline); `test_csv_format_uncosted_run_writes_empty_cell_not_zero` (seed an uncosted run total_cost_usd NULL in traces; assert the CSV total_cost_usd column for that row is empty string, NOT "0" or "0.0"); `test_table_format_canonical_renders_six_decimal_cost` (seed canonical, capture stdout for --format table --by agent, assert `0.006150` substring present, `0.006149` substring absent, `0ms` substring absent); `test_table_format_empty_db_renders_empty_state_message` (empty DB --format table; assert stdout contains `(no usage data in window)`); `test_table_format_long_agent_name_truncates_with_ellipsis` (seed a trace with agent_profile_name `"a" * 40`; capture --format table stdout; assert the line contains `...` AND the column count stays bounded by the 30-char cap); `test_table_format_uncosted_run_renders_hyphen_not_zero` (seed uncosted run; assert the cost cell renders `-` substring on that row, NOT `0` or `0.000000`); `test_all_three_formats_emit_same_numeric_cost_for_canonical_row` (seed canonical; for each --format {json, csv, table}, extract the numeric cost value via json.loads / DictReader / regex parse; assert all three == pytest.approx(0.006150, abs=1e-9), this is the USAGE-04 cross-format parity assertion).
    Re-run all prior tests (Tasks 1-2); they should still pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cli_usage.py -q && grep -c 'csv.DictWriter' src/horus_os/cli/usage_cmd.py | grep -q '^1$' && grep -c 'round(.*, 6)' src/horus_os/cli/usage_cmd.py | awk '$1 >= 2' | grep -q . && grep -F '"..."' src/horus_os/cli/usage_cmd.py | grep -q . && .venv/bin/ruff check src/ tests/ && .venv/bin/ruff format --check src/ tests/</automated>
  </verify>
  <done>
    All three formatters (json, csv, table) emit the canonical 0.006150 cost identically (USAGE-04 cross-format parity test pins this). CSV uses DictWriter with alphabetical fieldnames matching JSON sort_keys order; None values render as empty cells (NOT "0"). Table mode truncates long names with ellipsis per PITFALLS.md and renders None as `-` (NOT "0"). Empty windows render documented empty states uniquely per format. No --by model path tested yet (Task 4).
  </done>
</task>

<task type="auto">
  <name>Task 4: --by model implementation, additive cost_by_model query plus matching route plus CLI wiring plus byte-for-byte parity test (USAGE-03)</name>
  <files>
    src/horus_os/observability/queries.py,
    src/horus_os/observability/__init__.py,
    src/horus_os/server/api.py,
    src/horus_os/cli/usage_cmd.py,
    tests/observability/test_queries_cost_by_model.py,
    tests/test_server_observability_routes.py,
    tests/test_cli_usage.py
  </files>
  <read_first>
    src/horus_os/observability/queries.py lines 196-294 (cost_by_agent in full, the EXACT SQL pattern to mirror for cost_by_model; the CTE shape, the LEFT JOIN, the COALESCE ordering, the Python rounding at the boundary);
    src/horus_os/observability/queries.py lines 1-55 (module docstring plus __all__, the new export slot for cost_by_model);
    src/horus_os/observability/__init__.py lines 26-32 plus 63-80 (the two places cost_by_model gets added: import block plus __all__);
    src/horus_os/server/api.py lines 327-336 (observability_cost, the route shape to mirror; envelope key MUST be "models" for cost_by_model route);
    src/horus_os/server/api.py lines 40-47 (imports, add cost_by_model to the existing observability imports);
    tests/observability/test_queries_cost_by_agent.py if it exists (mirror its 9-test structure for the new test file);
    tests/test_server_observability_routes.py (mirror the route test shape for the 5 existing routes, 4 routes times {empty / valid window / 400 invalid / 503 missing DB});
    The cost_by_agent return shape verified in Task 1 interfaces section above (rows already pinned by Phase 35 tests).
  </read_first>
  <action>
    Add a new public function `cost_by_model(db: Database, window: str) -> list[dict[str, Any]]` to src/horus_os/observability/queries.py. Place it ALPHABETICALLY between `cost_by_agent` (ends around line 294) and `latency_p50_p95` (starts around line 297). Function design:
    Docstring: per-model cost breakdown over the window; one dict per model with at least one llm_call in the window. Mirror cost_by_agent's docstring style and the Pitfall 11 / Pitfall float-precision conventions.
    Return-shape: list of dicts. Each dict carries: `model` (str, the llm_calls.model value); `provider` (str, the llm_calls.provider value anthropic or gemini); `total_cost_usd` (float, 6dp via Python round, SUM(llm_calls.cost_usd) excluding NULL rows; 0.0 when SUM is NULL matching cost_by_agent's existing contract at line 282 where None converts to 0.0 inside this specific function family, the Phase 35 convention; uncosted_runs surfaces the NULL count separately); `total_input_tokens` (int, SUM(llm_calls.input_tokens)); `total_output_tokens` (int, SUM(llm_calls.output_tokens)); `total_cache_read_input_tokens` (int, SUM(llm_calls.cache_read_input_tokens)); `total_cache_creation_input_tokens` (int, SUM(llm_calls.cache_creation_input_tokens)); `call_count` (int, COUNT(*) on llm_calls); `uncosted_calls` (int, SUM(CASE WHEN cost_usd IS NULL THEN 1 ELSE 0 END), Pitfall 5 plus any future NULL-cost row surfaces as a separate counter so the CLI/dashboard explains missing dollars).
    SQL: ONE CTE `windowed_calls` selecting model, provider, cost_usd, input_tokens, output_tokens, cache_read_input_tokens, cache_creation_input_tokens FROM llm_calls WHERE created_at >= ?; then GROUP BY model, provider with the SUMs and CASE-WHEN above. All filters via parameterized `?` placeholder; never string-format the window into SQL (Phase 35 T-35-01 mitigation pattern).
    ORDER BY COALESCE(total_cost_usd, 0) DESC, model ASC (mirrors cost_by_agent ordering at line 272).
    Python-side: `round(float(total_cost), 6) if total_cost is not None else 0.0` for total_cost_usd; `int(value or 0)` for token sums; `int(row[...])` for counts. Same Pitfall float-precision (USAGE-04) and Pitfall 11 honesty (uncosted_calls counter) contracts as cost_by_agent.
    Add `cost_by_model` to the module `__all__` list at line 47-53 ALPHABETICALLY (between `cost_by_agent` and `latency_p50_p95`).
    Modify src/horus_os/observability/__init__.py: line 26-32 import block: add `cost_by_model,` ALPHABETICALLY between `cost_by_agent,` and `latency_p50_p95,`. Line 63-80 `__all__`: add `"cost_by_model",` ALPHABETICALLY between `"cost_by_agent",` and `"get_observation_bus",` (alphabetical with `cost_by_model > cost_by_agent`).
    Modify src/horus_os/server/api.py: line 40-47 imports: add `cost_by_model,` to the `from horus_os.observability.queries import (...)` block ALPHABETICALLY. Add a new route AFTER `observability_cost` (line 327-336) and BEFORE `observability_latency`: `@app.get("/api/observability/cost-by-model")` decorating `def observability_cost_by_model(since: str = "7d") -> dict[str, Any]:` whose body mirrors observability_cost exactly but calls `cost_by_model(Database(cfg.db_path), since)` and returns `{"models": rows}`. Envelope key MUST be `models` so the noun matches the route name and the CLI envelope `{"by": "model", "rows": [...]}` translates to it cleanly (USAGE-03 byte-for-byte parity test below ties them together).
    Modify src/horus_os/cli/usage_cmd.py: replace the `--by model` placeholder (raising NotImplementedError) with: import `cost_by_model` from horus_os.observability.queries (add to the existing import line); dispatch `--by model` to `cost_by_model(db, since)`. No formatter changes needed; the JSON/CSV/table formatters from Tasks 2-3 are shape-agnostic and handle the new row shape transparently (rounding rules already apply by key-name suffix).
    Create tests/observability/test_queries_cost_by_model.py with 7 tests mirroring tests/observability/test_queries_cost_by_agent.py structure: `test_cost_by_model_empty_db_returns_empty_list`; `test_cost_by_model_window_excludes_old_rows` (seed one row at -8 days, one at -3 days, --since 7d returns only the recent); `test_cost_by_model_multi_model_rollup` (seed three rows: 2 claude-sonnet-4-6 plus 1 gemini-2.5-pro; assert two model rows returned, costs SUMmed correctly per model); `test_cost_by_model_cache_tokens_sum_from_llm_calls` (seed cache tokens; assert sums); `test_cost_by_model_uncosted_calls_surfaces_null_cost_rows` (seed one row with cost_usd=NULL; assert uncosted_calls == 1, total_cost_usd == 0.0, matches cost_by_agent contract); `test_cost_by_model_ordering_cost_desc_then_name_asc` (seed three models with explicit costs; assert order: highest cost first, ties broken alphabetically); `test_cost_by_model_re_exported_from_observability_package` (`from horus_os.observability import cost_by_model` succeeds; symbol in `__all__`).
    Append to tests/test_server_observability_routes.py 4 tests for the new route (mirror the 4-test pattern for the existing /cost route): `test_route_cost_by_model_empty_db_returns_empty_models`; `test_route_cost_by_model_valid_window_returns_seeded_rows`; `test_route_cost_by_model_invalid_window_returns_400`; `test_route_cost_by_model_missing_db_returns_503`.
    Append to tests/test_cli_usage.py these new tests: `test_usage_by_model_json_canonical_renders_six_decimals` (seed canonical row, call --by model --format json, assert payload["by"] == "model", payload["rows"][0]["model"] == "claude-sonnet-4-6", payload["rows"][0]["total_cost_usd"] == 0.006150, anti-float-noise canary holds); `test_usage_by_model_csv_matches_json_costs` (same seed, call --by model --format csv, parse with DictReader, assert numeric cost == pytest.approx(0.006150, abs=1e-9), cross-format parity for the new model slice); `test_usage_by_model_table_renders` (same seed, --by model --format table, assert stdout contains "claude-sonnet-4-6" AND "0.006150"); `test_usage_by_model_byte_for_byte_parity_with_route` (THE KEY USAGE-03 TEST. Seed a multi-row DB 3 rows across 2 models. Call run_usage with --by model --format json --since 7d, capture stdout. ALSO TestClient(create_app(data_dir=tmp_path)).get("/api/observability/cost-by-model?since=7d"), capture response.json(). Compare: `cli_payload["rows"]` MUST equal `route_payload["models"]` element-by-element. The envelope keys differ ("rows" in CLI to keep envelope uniform across --by modes; "models" in route to match the noun convention) but the inner row data is byte-for-byte identical because both call the same `cost_by_model` function with the same window arg. This is the load-bearing USAGE-03 contract test).
    Re-run all prior tests (Tasks 1-3); they should still pass since dispatch behavior for agent/tool unchanged.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/observability/test_queries_cost_by_model.py tests/test_server_observability_routes.py tests/test_cli_usage.py -q && .venv/bin/python -m pytest tests/ -q && grep -c 'def cost_by_model' src/horus_os/observability/queries.py | grep -q '^1$' && grep -c 'cost_by_model' src/horus_os/observability/__init__.py | awk '$1 >= 2' | grep -q . && grep -q '"/api/observability/cost-by-model"' src/horus_os/server/api.py && grep -c 'NTILE' src/horus_os/observability/queries.py | awk '$1 >= 4' | grep -q . && grep -c 'error_message' src/horus_os/observability/queries.py | grep -q '^0$' && git diff --stat HEAD src/horus_os/observability/bus.py src/horus_os/observability/persist.py src/horus_os/observability/cost.py src/horus_os/observability/pricing.py src/horus_os/observability/pricing.json src/horus_os/agent.py src/horus_os/tools/loop.py src/horus_os/storage.py pyproject.toml | grep -E '^\s+[0-9]+ file' | grep -q '^ 0 file' || ! git diff --quiet HEAD -- src/horus_os/observability/bus.py src/horus_os/observability/persist.py src/horus_os/observability/cost.py src/horus_os/observability/pricing.py src/horus_os/observability/pricing.json src/horus_os/agent.py src/horus_os/tools/loop.py src/horus_os/storage.py pyproject.toml && echo "anti-scope OK" || echo "anti-scope VIOLATED" && .venv/bin/ruff check src/ tests/ && .venv/bin/ruff format --check src/ tests/ && .venv/bin/python scripts/lint_no_wallclock.py</automated>
  </verify>
  <done>
    `--by model` slice ships with a new additive cost_by_model query plus matching /api/observability/cost-by-model route plus CLI wiring plus 4 dedicated CLI tests including the load-bearing USAGE-03 byte-for-byte parity test. The new query mirrors cost_by_agent's structure precisely: same SQL CTE pattern, same Python rounding boundary, same Pitfall 11 honesty contract (uncosted_calls counter), same Pitfall 7 hygiene (no text-content error column reads, structural rather than enforced since this query does not touch tool_invocations). No existing query function modified. No bus/persist/cost/pricing/agent/tools/storage/pyproject touches. Existing 607 tests still green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write 37-01-SUMMARY.md</name>
  <files>
    .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/35-query-module-and-read-apis/35-01-SUMMARY.md (canonical SUMMARY shape: frontmatter, sections, commit table, ROADMAP SC checklist, Pitfalls table, anti-scope diff statement, threat register, Self-Check);
    .planning/phases/36-observability-dashboard-tab/36-01-SUMMARY.md (most recent SUMMARY, current naming conventions for sections and metrics);
    .planning/ROADMAP.md Phase 37 section (the 4 ROADMAP SCs to check off);
    .planning/REQUIREMENTS.md USAGE-01..04 (mark requirements-completed in frontmatter);
    `git log --oneline main..HEAD` for this branch, capture commit SHAs and titles for the commit table.
  </read_first>
  <action>
    Write .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md following the Phase 35/36 SUMMARY structure exactly:
    YAML frontmatter: phase, plan, subsystem (cli), tags ([cli, usage, observability, v0.4, usage-01, usage-02, usage-03, usage-04]), requires (Phase 35 plus Phase 36), provides (the artifacts shipped), requirements-completed (USAGE-01, USAGE-02, USAGE-03, USAGE-04), tech-stack.added=[], tech-stack.patterns (stdlib-only, route-byte-parity, float-precision-round, etc.), key-files.created (all new files), key-files.modified (__main__.py, cli/__init__.py, queries.py, observability/__init__.py, server/api.py), duration, completed (current date), total-tests, commits.
    Sections: "What shipped", commit table, "Requirements satisfied" with one bullet per USAGE-01..04 naming the owner test, "ROADMAP Success Criteria" 4-item checklist citing pin tests, "Pitfalls guarded" table (Pitfall float-precision UX trap = USAGE-04, owner Task 2/3, pinned by canary-substring tests), "Anti-scope held" with the verbatim git diff --stat zero-line claim for bus.py / persist.py / cost.py / pricing.py / pricing.json / agent.py / tools/loop.py / storage.py / pyproject.toml, "Threat register outcomes" (T-37-01 Tampering on --since CLI arg mitigated via parse_window ValueError to rc=1; T-37-02 Tampering on --data-dir mitigated via Config.load; T-37-03..04 Information Disclosure on rendered cost data and the new route, both accepted per localhost-bound posture; T-37-05 SQL injection mitigated via parameterized ?; T-37-06 byte-for-byte route parity guards against CLI/dashboard drift; T-37-07 DoS bounded by index plus model-count cap; T-37-08 float-precision noise mitigated via Python round before serialization), "Deviations from plan" (likely none if executed clean), "Authentication gates" (none), "Test counts" before/after with new-tests breakdown by file, "Out of scope (deliberate)" listing what was NOT touched (Phase 38 OTel, Phase 39 release gate, etc.), "Forward dependencies for Phase 38" noting the CLI is stable substrate that Phase 38 does not depend on, "Self-Check" 10+ post-write verification items (pytest count, ruff clean, anti-scope diff, grep counts on key markers like NTILE preservation, cost_by_model presence, /api/observability/cost-by-model route, run_usage exported, etc.).
  </action>
  <verify>
    <automated>test -f .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md && grep -cE '^---$' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | grep -q '^2$' && grep -cE '^## ' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | awk '$1 >= 10' | grep -q . && grep -cF 'USAGE-01' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | awk '$1 >= 2' | grep -q . && grep -cF 'USAGE-02' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | awk '$1 >= 2' | grep -q . && grep -cF 'USAGE-03' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | awk '$1 >= 2' | grep -q . && grep -cF 'USAGE-04' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | awk '$1 >= 2' | grep -q . && grep -F '0.006150' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | grep -q . && grep -F 'Self-Check' .planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md | grep -q .</automated>
  </verify>
  <done>
    Phase 37 SUMMARY follows the established Phase 35/36 structure exactly. All 4 USAGE requirements marked completed with named pin tests. ROADMAP SCs all checked. Pitfall float-precision UX trap addressed with the 0.006150 canonical evidence. Anti-scope hold documented. Threat register answered. Self-Check verifies the actual shipped artifacts. Ready for verifier in Phase 37's verification pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User shell to `horus-os usage` CLI | The `--since` arg is user-supplied and reaches `parse_window`; the `--data-dir` arg reaches the filesystem |
| CLI to SQLite | Both CLI and HTTP routes hit the same DB file; CLI inherits the parameterized-query contract from Phase 35's queries.py |
| HTTP client to new `/api/observability/cost-by-model` route | `?since=` reaches the same `parse_window` plus `cost_by_model` substrate; route is localhost-bound (no auth surface in v0.4) |
| `cost_by_model` to llm_calls table | Read-only SELECT on indexed columns; no user input round-trips back into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | `--since` CLI arg | mitigate | `parse_window(args.since)` raises `ValueError` on malformed input; usage_cmd.py converts to `stderr.write(f"{exc}\n")` plus `return 1` BEFORE the SQL query runs. Mirrors the route boundary's `HTTPException(400)` pattern (server/api.py:334-335). Same `parse_window` substrate already pinned by `tests/observability/test_queries_window.py` (8 tests, Phase 35). |
| T-37-02 | Tampering | `--data-dir` CLI arg to filesystem | mitigate | `Config.load(Path(args.data_dir).expanduser())` follows the established pattern from `traces_cmd.py:run_traces`. No new file writes (read-only subcommand); `db_path.exists()` guard returns rc=1 BEFORE any DB open if the path is bogus. |
| T-37-03 | Information Disclosure | CLI JSON / CSV / table output renders cost data | accept | Costs and token counts are user's own data; CLI runs in user's own shell; no PII in cost rollups (the queries module already strips the text-content error column from tool_invocations per Phase 35 T-35-02 mitigation, and cost_by_model never touches tool_invocations at all). |
| T-37-04 | Information Disclosure | New `/api/observability/cost-by-model` route | accept | Per the existing Phase 36 T-36-02 register entry: localhost-bound, non-PII metadata, no auth surface in v0.4. Same posture as the other 5 observability routes. |
| T-37-05 | Tampering / SQL injection | `?since=` route param plus `cost_by_model` SQL | mitigate | All filters use parameterized `?` placeholders; `since` is converted to an ISO threshold via `parse_window` before reaching the SQL layer; the SQL CTE never substitutes user input via Python string formatting. Mirrors Phase 35 T-35-01 contract verbatim. |
| T-37-06 | Repudiation | CLI/route disagreement on what a cost number means | mitigate | USAGE-03 byte-for-byte parity test (`test_usage_by_model_byte_for_byte_parity_with_route`) compares CLI `--format json --by model` output element-by-element against `/api/observability/cost-by-model?since=7d`. Both call the same `cost_by_model(db, window)` function so they cannot drift. Schema drift is caught by `tests/test_cli_usage.py::test_json_format_matches_pinned_fixture_schema` diffing against `tests/fixtures/usage_output_schema.json`. |
| T-37-07 | Denial of Service | `cost_by_model` unbounded scan | mitigate | `idx_llm_calls_created_at` index from Phase 32 makes the WHERE created_at >= ? scan O(log N). The `?since=` filter is required (default `7d` if omitted). No LIMIT needed because GROUP BY model naturally caps rows at the model-count (typically 5-10 models per user). |
| T-37-08 | Information Disclosure | Float-precision noise leaking through JSON serializer to `jq` consumers | mitigate | USAGE-04 contract: Python `round(value, 6)` BEFORE `json.dumps` (NOT after, NOT via format string, NOT via SQLite ROUND, SQLite's banker rounding is platform-variable). Canonical fixture pins `0.006150` exactly; anti-canary tests assert `0.006149` substring absent from JSON, CSV, AND table output (cross-format parity in `test_all_three_formats_emit_same_numeric_cost_for_canonical_row`). |
</threat_model>

<verification>
After all 5 tasks complete:
- `.venv/bin/python -m pytest tests/ -q` returns 0 with the full suite green (expected approximately 628+ passed: 607 from Phase 36 plus around 21 new in test_cli_usage.py plus 7 in test_queries_cost_by_model.py plus 4 in test_server_observability_routes.py)
- `.venv/bin/ruff check src/ tests/` exits 0
- `.venv/bin/ruff format --check src/ tests/` exits 0
- `.venv/bin/python scripts/lint_no_wallclock.py` exits 0
- Anti-scope hold: `git diff --stat $(git merge-base main HEAD)..HEAD -- src/horus_os/observability/bus.py src/horus_os/observability/persist.py src/horus_os/observability/cost.py src/horus_os/observability/pricing.py src/horus_os/observability/pricing.json src/horus_os/agent.py src/horus_os/tools/loop.py src/horus_os/storage.py pyproject.toml` returns 0 lines changed in any of these files
- USAGE-01: `.venv/bin/python -m horus_os usage --since 24h --by agent --format table` exits 0 against a real DB; `--since garbage` exits 1 with stderr containing "invalid window:"
- USAGE-02: `tests/fixtures/usage_output_schema.json` exists and is referenced by `test_json_format_matches_pinned_fixture_schema`; `docs/CLI.md` contains "## horus-os usage", "### JSON output schema", "### Precision contract"
- USAGE-03: `test_usage_by_model_byte_for_byte_parity_with_route` passes, CLI `--by model` output and `/api/observability/cost-by-model` route body element-by-element identical
- USAGE-04: `test_json_format_canonical_cost_renders_exactly_six_decimals` passes, `0.006150` present, `0.006149` substring ABSENT across JSON, CSV, AND table modes (cross-format parity per `test_all_three_formats_emit_same_numeric_cost_for_canonical_row`)
- `.planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md` exists with all 10+ sections per Phase 35/36 conventions
</verification>

<success_criteria>
- All 4 ROADMAP Phase 37 SCs satisfied with pin tests named in the SUMMARY's "ROADMAP Success Criteria" checklist
- All 4 USAGE requirements (USAGE-01..04) marked completed in SUMMARY frontmatter `requirements-completed`
- Pitfall float-precision UX trap (PITFALLS.md line 403) closed with the `0.006150` canonical fixture plus cross-format parity test
- Zero new runtime dependencies (`git diff pyproject.toml` empty)
- Zero modifications to existing queries.py functions; cost_by_model is purely additive (line-range diff shows insert, not modify)
- Phase 37 atomic commits follow conventional commit style: `feat(37):`, `test(37):`, `docs(37):` prefixes
- `gsd-sdk query frontmatter.validate .planning/phases/37-horus-os-usage-cli-subcommand/37-01-PLAN.md --schema plan` returns valid=true
</success_criteria>

<output>
After completion, create `.planning/phases/37-horus-os-usage-cli-subcommand/37-01-SUMMARY.md` (Task 5).
</output>
