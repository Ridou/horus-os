---
phase: 46-test-surface-three-tier-fixtures-pitfall-suite
plan: 01
type: execute
wave: 1
depends_on: [45]
files_modified:
  - tests/plugins/conftest.py
  - tests/conftest.py
  - tests/test_plugin_pitfalls/__init__.py
  - tests/test_plugin_pitfalls/conftest.py
  - tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py
  - tests/test_plugin_pitfalls/test_pitfall_02_manifest_drift.py
  - tests/test_plugin_pitfalls/test_pitfall_03_entry_point_drift.py
  - tests/test_plugin_pitfalls/test_pitfall_04_pip_corruption.py
  - tests/test_plugin_pitfalls/test_pitfall_05_capability_expansion.py
  - tests/test_plugin_pitfalls/test_pitfall_06_lifecycle_hang.py
  - tests/test_plugin_pitfalls/test_pitfall_07_observability_attribution.py
  - tests/test_plugin_pitfalls/test_pitfall_08_public_api_leak.py
  - tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py
  - tests/test_plugin_pitfalls/test_pitfall_10_permission_ui.py
  - tests/test_plugin_pitfalls/test_pitfall_11_test_isolation.py
  - tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py
  - pyproject.toml
autonomous: true
requirements:
  - TEST-16
  - TEST-17

must_haves:
  truths:
    - "Tier 1 (in-process unit) is the default tier; >80% of plugin tests operate directly on `PluginSpec`, `PermissionGate`, `CapabilityGuard` objects with no discovery, no install, no entry-point monkeypatch. A helper `make_synthetic_plugin(name, capabilities, raise_in=None)` in `tests/plugins/conftest.py` returns a `(PluginSpec, module_obj)` pair the pitfall suite consumes for tier-1 tests."
    - "Tier 2 (`fake_plugin_entry_points` monkeypatch fixture) — existing from Phase 42 — gains a `make_fake_entry_point(spec, module_obj)` helper exported alongside `FakeEntryPointsBundle.inject` so tests can wire a synthetic plugin into the discovery walk with one line; the existing `inject(entries=[(name, value, manifest_bytes)])` signature stays byte-identical to preserve Phase 42 callers."
    - "Tier 3 (`clean_venv` fixture) gated by `@pytest.mark.installer_e2e`: creates a fresh venv via `venv.create(<tmp_path>/venv, with_pip=True)`, runs `[venv_python, '-m', 'pip', 'install', '-e', <repo_root>]` once per session (cached in `tmp_path_factory.getbasetemp()`), yields the venv's python executable path; tear-down deletes the venv directory. Cold-start cost ~30s amortized across all `installer_e2e`-marked tests."
    - "`tests/conftest.py` exposes a `pytest_addoption` hook adding `--run-installer-e2e` CLI flag; default off. `pytest_collection_modifyitems` skips every `@pytest.mark.installer_e2e`-marked test with reason `'requires --run-installer-e2e flag (slow tier-3 venv tests)'` unless the flag is passed. The default `pytest` invocation NEVER spawns a venv, NEVER calls pip; verified by an in-test assertion that asserts `clean_venv` fixture is uninstantiated when the flag is absent."
    - "`pyproject.toml` `[tool.pytest.ini_options]` declares `markers = ['installer_e2e: requires --run-installer-e2e CLI flag (tier-3 clean-venv tests; ~30s session startup; ~5s per test)']`; the existing `--strict-markers` invocation flag (line 125) means a typo on the marker name raises an error, not a silent skip."
    - "`tests/test_plugin_pitfalls/` directory contains exactly 12 test files matching the glob `test_pitfall_<NN>_<slug>.py` where `<NN>` is the zero-padded 2-digit pitfall number from `.planning/research/PITFALLS.md` and `<slug>` is a short kebab-cased identifier; each file's module docstring opens with the literal string `'Pitfall N: <title>'` matching PITFALLS.md verbatim so a grep `'^Pitfall <N>:'` over both files returns the same set."
    - "`test_pitfall_01_default_allow.py`: asserts `from horus_os.plugins.permissions import DEFAULT_GRANT_POLICY; DEFAULT_GRANT_POLICY == 'deny'`. Uses tier-1 `make_synthetic_plugin('p1', capabilities=['filesystem.read'])` to construct a spec; `PermissionGate(db).partition(spec)` returns `(granted=set(), pending={'filesystem.read'})` on a fresh DB (no grant row exists, default policy is deny). Calling a `CapabilityGuard`-wrapped helper raises `PermissionDenied`."
    - "`test_pitfall_02_manifest_drift.py`: loads `tests/fixtures/manifests/manifest_v1_minimum.toml` through `load_manifest()` and asserts the result; constructs a v1 manifest dict in memory with an UNKNOWN top-level key `signing_key_fingerprint='abc'` and asserts the loader emits a `WARN: unknown manifest field` log record (via `caplog`) AND still produces a valid `PluginSpec` (unknown fields are warned not refused); constructs a manifest with `manifest_version=2` and asserts `ManifestError` is raised cleanly with `'manifest_version=2 not supported by horus-os'` in the message; the v0.5 ships-now `manifest_v1_minimum.toml` round-trips through the loader byte-stable (its parsed spec equals the spec re-loaded after re-serialization)."
    - "`test_pitfall_03_entry_point_drift.py`: greps the source tree for the canonical call shape — `re.search(r'entry_points\\(group=', src)` MUST hit `src/horus_os/plugins/discovery.py`; the deprecated `entry_points()[group]` dict-access shape MUST NOT hit anywhere in `src/` (the lint regex `re.search(r'entry_points\\(\\)\\[', src)` returns no matches). Parametrized on `sys.version_info[:2] in {(3, 11), (3, 12)}` — both supported Python versions structurally exercise the discovery path via the tier-2 fixture. Bundled cold-start benchmark assertion: `discover_plugins()` on an empty entry-point registry completes in <100ms (matches TEST-18's existing budget; this is a regression guard, not a new benchmark)."
    - "`test_pitfall_04_pip_corruption.py` is a META-test: asserts all five Phase 44 installer guards exist as collectable pytest items by spawning `pytest --collect-only -q tests/plugins/test_installer_venv_refusal.py tests/plugins/test_installer_sdist_refusal.py tests/plugins/test_installer_pth_refusal.py tests/plugins/test_installer_downgrade_refusal.py tests/plugins/test_installer_rollback.py` in a subprocess and parsing the output for `<no errors>` plus at least one test id per file. The five guards (venv check, sdist refusal, .pth refusal, downgrade refusal, rollback) are the Phase 44 substrate of Pitfall 4; this meta-test catches accidental deletion."
    - "`test_pitfall_05_capability_expansion.py`: tier-1 — constructs a `PluginSpec('foo', version='1.0', capabilities=['filesystem.read'])`, registers via `PluginRegistry.register(spec)`, runs `PermissionService(db).grant(plugin_name='foo', plugin_version='1.0', capability='filesystem.read', manifest_hash=<v1.0_hash>, actor='test')`. Then constructs a v1.1 spec with `capabilities=['filesystem.read', 'net.outbound']` (different `manifest_hash`), simulates upgrade via `PermissionGate.partition(v11_spec)`. Asserts the v1.0 grant row is treated as `pending` (manifest_hash differs); the v1.1 partition returns `(granted=set(), pending={'filesystem.read', 'net.outbound'})` — no silent inheritance."
    - "`test_pitfall_06_lifecycle_hang.py` is a META-test: asserts `tests/plugins/test_bounded_lifecycle.py` exists, contains a `_SlowStartAdapter` class with `await asyncio.sleep(5.0)` in `start`, and collects without error via `pytest --collect-only -q tests/plugins/test_bounded_lifecycle.py`. The bounded-lifecycle substrate is Phase 43's ISOLATE-02 deliverable; this meta-test catches accidental deletion. ALSO runs a tier-1 inline regression: a synthetic `_SlowStart` with `sleep(3.0)` wrapped in `asyncio.wait_for(adapter.start(ctx), timeout=2.0)` must raise `asyncio.TimeoutError` within 2.5s wall clock (`time.monotonic()` before/after assertion)."
    - "`test_pitfall_07_observability_attribution.py`: tier-1 with `clean_plugin_registry` fixture — seeds a `tool_invocations` row with `plugin_name='foo-plugin'` AND another with `plugin_name=NULL` (the 'core' bucket); asserts `SELECT plugin_name FROM tool_invocations WHERE tool_name='read_file' AND status='error'` returns `'foo-plugin'` for the first and NULL for the second; asserts the v5→v6 migration created the `plugin_name` column (introspection via `PRAGMA table_info('tool_invocations')`); asserts the same column exists on `llm_calls`."
    - "`test_pitfall_08_public_api_leak.py`: introspects `horus_os.plugins.api.__all__` and asserts it contains the canonical set `{'PluginContext', 'Capability', 'PluginSpec', 'Adapter', 'AdapterContext', 'LifecycleAdapter', 'CapabilityGuard', 'PermissionDenied', 'require_capability', 'Tool'}` (exact set — additions to `__all__` MUST update this test as a forced docs/code review trip wire). Asserts `from horus_os.plugins.api import *` exposes ONLY names in that set (no internal helpers leak). The Phase 48 ruff rule banning `from horus_os` imports outside `horus_os.plugins.api` in the reference plugin is documented in the docstring but NOT enforced here (that's Phase 48's TEST-21 scope)."
    - "`test_pitfall_09_schema_migration_regression.py`: loads `tests/fixtures/v0_4_database.sqlite3` (committed in Phase 40 as BASELINE-02 substrate), runs `Database.init()` which fires v5→v6 migration, asserts: (a) `SELECT version FROM schema_version` returns 6, (b) every pre-existing row in `traces` reads back with identical `(trace_id, agent_name, status)` values, (c) `tool_invocations.plugin_name` exists on all rows with value NULL (additive column, no backfill), (d) the new tables `plugins`, `plugin_capabilities`, `plugin_status` exist and are empty, (e) calling `Database.init()` a second time on the now-v6 database is a no-op (no rows changed, no exceptions raised). Reverse migration v6→v5 is explicitly NOT supported and the test docstring says so."
    - "`test_pitfall_10_permission_ui.py`: asserts that `Capability` enum members each expose a `description` attribute (plain-English string >= 20 chars); asserts the `format_grant_prompt(spec)` formatter (Phase 44 installer surface) renders the description, not the raw capability dotted-key string. Tier-1 — invokes the formatter directly with a synthetic spec and grep-asserts the output contains `'Read any file'` (the description for `filesystem.read`) and NEVER contains the substring `'filesystem.read'` outside a parenthetical reference."
    - "`test_pitfall_11_test_isolation.py`: structural — invokes the `fake_plugin_entry_points` fixture twice in sequence and asserts the second call sees no entries from the first (monkeypatch is per-test). Asserts `from importlib.metadata import entry_points; entry_points(group='horus_os.plugins')` AFTER the tier-2 fixture exits returns the same set it returned BEFORE (the host venv is unpolluted). Tier-3 portion (marked `installer_e2e`): asserts `clean_venv` fixture creates a venv at a unique `tmp_path` for each invocation (two calls in two tests get different paths); never reuses the runner venv (`venv_python != sys.executable`)."
    - "`test_pitfall_12_docs_drift.py`: asserts `MANIFEST_V1_SCHEMA.model_json_schema()` is callable on the runtime constant and returns a dict with stable top-level keys `{'type': 'object', 'properties': {...}, 'required': [...]}`. Asserts the schema dict serializes via `json.dumps(schema, indent=2, sort_keys=True)` to a byte-stable string (same input -> same output across two calls). The actual `docs/manifest-v1.schema.json` file does NOT exist yet (Phase 47 ships it); the test contains a `pytest.skip('docs/manifest-v1.schema.json shipped in Phase 47')` for the diff portion AND a TODO comment pointing to Phase 47 — the docs-drift gate becomes active when Phase 47 lands."
    - "Default pytest invocation (`pytest tests/` with no flags) collects and runs every test in `tests/test_plugin_pitfalls/` (12 files); installer_e2e-marked tests are skipped silently with the `'requires --run-installer-e2e flag'` reason. Total wall-clock budget on Ubuntu CI: <90s for the full plugin test suite including the 12 new pitfall files."
    - "Ruff clean across `tests/test_plugin_pitfalls/` and the conftest extensions. `--strict-markers` passes (no typos on `installer_e2e`)."

  artifacts:
    - path: tests/plugins/conftest.py
      provides: "EXTENDED — existing Phase 42 fixtures preserved byte-identically (`fake_plugin_entry_points`, `tmp_plugin_dir`, `install_broken_fixture`, `clean_plugin_registry`, `installer_fixture_wheels`, `installed_db`). NEW: `make_synthetic_plugin(name, capabilities, raise_in=None)` helper returning `(PluginSpec, ModuleObj)` tuple for tier-1 tests; NEW: `make_fake_entry_point(spec, module_obj)` helper that wraps `FakeEntryPointsBundle.inject` with a friendlier signature. Existing imports and class definitions untouched."
      contains: "def make_synthetic_plugin"
    - path: tests/conftest.py
      provides: "NEW top-level conftest (does not yet exist; the test suite has only per-subdir conftests). Adds `pytest_addoption(--run-installer-e2e)` and `pytest_collection_modifyitems` that adds skip markers to `installer_e2e`-marked tests when the flag is absent. ALSO defines the `clean_venv` fixture (tier-3) — placed at the top level because `installer_e2e` tests may live anywhere under `tests/`. Yields a `CleanVenv` dataclass with `.python` (path to the venv's python executable), `.site_packages` (path to site-packages), `.tmp_dir`."
      contains: "def pytest_addoption"
    - path: tests/test_plugin_pitfalls/__init__.py
      provides: "Empty marker file (pytest discovers the directory; the marker keeps ruff and mypy happy)."
      min_lines: 1
    - path: tests/test_plugin_pitfalls/conftest.py
      provides: "Shared fixture `pitfall_db` returning an in-memory `Database` instance with v6 schema applied (no disk I/O). Used by pitfall tests that need persistence without touching tmp_path. Mirrors `tests/plugins/conftest.py::installed_db` but for in-memory SQLite (`sqlite3.connect(':memory:')`)."
      contains: "def pitfall_db"
    - path: tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py
      provides: "Pitfall 1 regression. Module docstring opens `Pitfall 1: Default-allow capability grants normalize compromise`. Asserts `DEFAULT_GRANT_POLICY == 'deny'`, a fresh `PermissionGate.partition(spec)` returns empty granted set, `CapabilityGuard.check(cap)` raises `PermissionDenied` without a grant row."
      contains: "DEFAULT_GRANT_POLICY"
    - path: tests/test_plugin_pitfalls/test_pitfall_02_manifest_drift.py
      provides: "Pitfall 2 regression. Loads `tests/fixtures/manifests/manifest_v1_minimum.toml` cleanly; unknown v1 fields produce WARN log + valid spec; `manifest_version=2` raises `ManifestError` with clear message; round-trip stability assertion."
      contains: "manifest_v1_minimum"
    - path: tests/test_plugin_pitfalls/test_pitfall_03_entry_point_drift.py
      provides: "Pitfall 3 regression. Source-tree grep for `entry_points(group=` (positive) and `entry_points()[` (negative); parametrized on `sys.version_info[:2]`; cold-start <100ms assertion on `discover_plugins()` over the empty entry-point registry."
      contains: "entry_points(group="
    - path: tests/test_plugin_pitfalls/test_pitfall_04_pip_corruption.py
      provides: "Pitfall 4 META-test. Subprocesses `pytest --collect-only -q` over five existing installer guard files and asserts each yields at least one test id without collection errors. Documents the Phase 44 -> Phase 46 substrate relationship."
      contains: "test_installer_venv_refusal"
    - path: tests/test_plugin_pitfalls/test_pitfall_05_capability_expansion.py
      provides: "Pitfall 5 regression. Constructs v1.0 spec, grants, upgrades to v1.1 with expanded capabilities, asserts the v1.0 grant becomes `pending` and no silent inheritance occurs."
      contains: "manifest_hash"
    - path: tests/test_plugin_pitfalls/test_pitfall_06_lifecycle_hang.py
      provides: "Pitfall 6 META-test + inline regression. Asserts `tests/plugins/test_bounded_lifecycle.py` collectable; runs an inline `asyncio.wait_for(slow_start, timeout=2.0)` assertion proving the 2s budget is enforced structurally."
      contains: "asyncio.wait_for"
    - path: tests/test_plugin_pitfalls/test_pitfall_07_observability_attribution.py
      provides: "Pitfall 7 regression. Seeds rows with `plugin_name='foo-plugin'` and `plugin_name=NULL`; asserts column exists on both `llm_calls` and `tool_invocations`; asserts attribution semantics."
      contains: "plugin_name"
    - path: tests/test_plugin_pitfalls/test_pitfall_08_public_api_leak.py
      provides: "Pitfall 8 regression. Introspects `horus_os.plugins.api.__all__` against the canonical set; asserts `import *` exposes only those names. Documents but does not yet enforce the Phase 48 reference-plugin ruff rule."
      contains: "__all__"
    - path: tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py
      provides: "Pitfall 9 regression. Loads `v0_4_database.sqlite3` fixture, runs `Database.init()`, asserts v6 migration is additive, idempotent, NULL on new columns. Reverse migration explicitly unsupported."
      contains: "v0_4_database.sqlite3"
    - path: tests/test_plugin_pitfalls/test_pitfall_10_permission_ui.py
      provides: "Pitfall 10 regression. Asserts `Capability` enum members carry plain-English descriptions; asserts the grant-prompt formatter surfaces descriptions, not dotted-keys."
      contains: "description"
    - path: tests/test_plugin_pitfalls/test_pitfall_11_test_isolation.py
      provides: "Pitfall 11 regression. Asserts tier-2 fixture is per-test (monkeypatch); asserts tier-3 `clean_venv` creates unique venvs per call; asserts the runner venv is never polluted. Tier-3 portion `@pytest.mark.installer_e2e`."
      contains: "fake_plugin_entry_points"
    - path: tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py
      provides: "Pitfall 12 regression. Asserts `MANIFEST_V1_SCHEMA.model_json_schema()` is callable and byte-stable. The docs-drift diff against `docs/manifest-v1.schema.json` is `pytest.skip`'d until Phase 47 ships the docs file (TODO comment + skip reason inline)."
      contains: "model_json_schema"
    - path: pyproject.toml
      provides: "EXTENDED `[tool.pytest.ini_options]` block — adds `markers = ['installer_e2e: ...']` list. Existing lines 122-126 (`minversion`, `testpaths`, `addopts`, `asyncio_mode`) preserved byte-identically; the `markers` key is appended as a new line."
      contains: "installer_e2e"

  key_links:
    - from: "tests/conftest.py:pytest_collection_modifyitems"
      to: "tests/test_plugin_pitfalls/test_pitfall_11_test_isolation.py"
      via: "the `installer_e2e` marker — tier-3 portion of pitfall 11 is skipped unless `--run-installer-e2e` is passed"
      pattern: "pytest.mark.installer_e2e"
    - from: "tests/plugins/conftest.py:make_synthetic_plugin"
      to: "tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py + test_pitfall_05 + test_pitfall_10"
      via: "tier-1 spec construction helper consumed by multiple pitfall tests; returns `(PluginSpec, ModuleObj)`"
      pattern: "make_synthetic_plugin"
    - from: "tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py"
      to: "tests/fixtures/v0_4_database.sqlite3"
      via: "loads the Phase 40 BASELINE-02 fixture; asserts byte-stable migration"
      pattern: "v0_4_database.sqlite3"
    - from: "tests/test_plugin_pitfalls/test_pitfall_04_pip_corruption.py"
      to: "tests/plugins/test_installer_{venv,sdist,pth,downgrade,rollback}_refusal.py"
      via: "subprocess `pytest --collect-only -q`; meta-asserts the Phase 44 substrate exists"
      pattern: "test_installer_"
    - from: "tests/test_plugin_pitfalls/test_pitfall_06_lifecycle_hang.py"
      to: "tests/plugins/test_bounded_lifecycle.py"
      via: "subprocess `pytest --collect-only -q` + inline `asyncio.wait_for(timeout=2.0)` regression"
      pattern: "test_bounded_lifecycle"
    - from: "tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py"
      to: "src/horus_os/plugins/manifest.py:MANIFEST_V1_SCHEMA"
      via: "introspection of `model_json_schema()` for byte-stable serialization"
      pattern: "MANIFEST_V1_SCHEMA"
---

<objective>
Land the v0.5 plugin-system test surface: the three-tier fixture strategy (tier 1 in-process unit, tier 2 monkeypatched entry-points, tier 3 opt-in clean-venv) and the 12 pitfall regression tests in `tests/test_plugin_pitfalls/` that turn the documented PITFALLS.md prevention patterns into automatic CI tripwires.

Purpose: PITFALLS.md is research; without a per-pitfall regression test, every documented trap is one careless PR away from re-landing. Phase 46 converts the document into 12 executable assertions plus the fixture machinery that lets future plugin-system work move fast without polluting the test-runner venv. After this phase, a future contributor who reintroduces (e.g.) default-allow grants in `permissions.py` sees `test_pitfall_01_default_allow.py` turn red before they can merge; a future contributor who tries to silently expand capabilities on plugin upgrade sees `test_pitfall_05`. The tier-3 `clean_venv` fixture is the only path that touches `pip install`; the default `pytest` run never spawns a venv, so v0.5 CI stays under the 90s/Ubuntu budget Phase 49 will gate against.

Scope is strictly test infrastructure + regression assertions; no production code in `src/horus_os/` is modified. The one exception is `pyproject.toml` for the `installer_e2e` marker registration, which is a CI configuration change, not a runtime change.

Output: extended `tests/plugins/conftest.py`, new `tests/conftest.py` (does not exist today), new `tests/test_plugin_pitfalls/` directory with `__init__.py` + `conftest.py` + 12 pitfall test files, `pyproject.toml` marker registration. No source modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/research/PITFALLS.md
@.planning/REQUIREMENTS.md

@tests/plugins/conftest.py
@tests/plugins/test_loader_isolation.py
@tests/plugins/test_bounded_lifecycle.py

<interfaces>
<!-- Key types and exports the pitfall tests consume. Extracted from src/. -->
<!-- Executor uses these directly — no codebase exploration needed beyond the cited files. -->

From src/horus_os/plugins/permissions.py:
```python
DEFAULT_GRANT_POLICY = "deny"  # module-level constant; line 69

class PermissionGate:
    def partition(self, spec: PluginSpec) -> tuple[set[str], set[str]]:
        """Returns (granted, pending). manifest_hash mismatch flips granted->pending."""

class PermissionService:
    def grant(self, plugin_name, plugin_version, capability, manifest_hash, actor): ...
    def revoke(self, plugin_name, plugin_version, capability, actor): ...

class CapabilityGuard:
    def check(self, capability: str) -> None:  # raises PermissionDenied

class PermissionDenied(Exception): pass
```

From src/horus_os/plugins/manifest.py:
```python
class MANIFEST_V1_SCHEMA(pydantic.BaseModel):
    manifest_version: int
    name: str
    version: str
    description: str
    # ... (see file for full fields)

def load_manifest(path: Path) -> PluginManifest: ...
class ManifestError(Exception): pass
```

From src/horus_os/plugins/spec.py:
```python
@dataclass
class PluginSpec:
    name: str
    version: str
    capabilities: tuple[str, ...]
    manifest_hash: str
    # ... (see file)
```

From src/horus_os/plugins/capability_catalog.py:
```python
class Capability(StrEnum):
    FILESYSTEM_READ = "filesystem.read"  # description: "Read any file in your home directory..."
    FILESYSTEM_WRITE = "filesystem.write"
    NET_OUTBOUND = "net.outbound"
    SECRETS_READ = "secrets.read"
    # each has a .description attribute
```

From src/horus_os/plugins/api.py (line 227):
```python
__all__ = (
    "PluginContext", "Capability", "PluginSpec",
    "Adapter", "AdapterContext", "LifecycleAdapter",
    "CapabilityGuard", "PermissionDenied",
    "require_capability", "Tool",
)
```

From src/horus_os/storage.py:
```python
SCHEMA_VERSION = 6  # line 28

class Database:
    def __init__(self, path: Path): ...
    def init(self) -> None: ...  # idempotent; runs v5->v6 migration
```

From tests/plugins/conftest.py (Phase 42, existing):
```python
class FakeEntryPointsBundle:
    def inject(self, entries: Iterable[tuple[str, str, bytes] | tuple[str, str, bytes, Any]]) -> None: ...

@pytest.fixture
def fake_plugin_entry_points(monkeypatch) -> FakeEntryPointsBundle: ...

@pytest.fixture
def tmp_plugin_dir(tmp_path, monkeypatch) -> Path: ...

@pytest.fixture
def clean_plugin_registry(tmp_path) -> tuple[Database, PluginRegistry]: ...
```

From pyproject.toml (current, lines 122-126):
```toml
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers"
asyncio_mode = "auto"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/plugins/conftest.py with tier-1 helper + add tier-3 clean_venv fixture in tests/conftest.py + register installer_e2e marker</name>
  <files>tests/plugins/conftest.py, tests/conftest.py, pyproject.toml</files>
  <behavior>
    - `make_synthetic_plugin(name='foo', capabilities=['filesystem.read'], raise_in=None)` returns `(PluginSpec, types.SimpleNamespace)` tuple — spec has the right name/version/capabilities/manifest_hash, the namespace acts as a stand-in for the loaded module; `raise_in='start'|'stop'|'load'` injects an attribute on the namespace that the loader can raise from. No regression to existing fixtures (`fake_plugin_entry_points`, `tmp_plugin_dir`, `install_broken_fixture`, `clean_plugin_registry`, `installer_fixture_wheels`, `installed_db`) — all six remain byte-identical.
    - `make_fake_entry_point(spec, module_obj)` is a top-level helper (not a method) exported from `tests/plugins/conftest.py` that returns the `(name, value, manifest_bytes)` tuple shape `FakeEntryPointsBundle.inject` expects, so callers can write `bundle.inject([make_fake_entry_point(spec, mod)])`.
    - `tests/conftest.py` (NEW file at the top-level tests/ directory) defines `pytest_addoption(parser)` adding `--run-installer-e2e` boolean flag; defines `pytest_collection_modifyitems(config, items)` that scans each item's markers for `installer_e2e` and adds `pytest.mark.skip(reason='requires --run-installer-e2e flag (slow tier-3 venv tests)')` when the flag is absent. Defines the `clean_venv` fixture (scope='session') that lazily creates a venv via `venv.create(<tmp_path_factory.getbasetemp() / "clean_venv">, with_pip=True)`, runs `[venv_python, '-m', 'pip', 'install', '-e', <repo_root>]` once, yields a `CleanVenv` dataclass with `.python`, `.site_packages`, `.tmp_dir` attributes. The fixture is GUARDED: if `not config.getoption('--run-installer-e2e')`, the fixture raises `pytest.skip('clean_venv requires --run-installer-e2e flag')` on first access — defense-in-depth alongside the collection-time skip.
    - `pyproject.toml` `[tool.pytest.ini_options]` block (lines 122-126) gets ONE new key appended: `markers = ["installer_e2e: requires --run-installer-e2e CLI flag (tier-3 clean-venv tests; ~30s session startup; ~5s per test)"]`. Existing keys (`minversion`, `testpaths`, `addopts`, `asyncio_mode`) preserved byte-identically with no whitespace churn. `--strict-markers` in `addopts` means a typo on the marker name raises a clean error at collection time.
  </behavior>
  <action>
    Extend `tests/plugins/conftest.py` by APPENDING (not rewriting) the `make_synthetic_plugin` and `make_fake_entry_point` helpers below the existing `installed_db` fixture. Use `types.SimpleNamespace` for the synthetic module object so tests can monkey-set attributes inline. Compute `manifest_hash` via `hashlib.sha256(json.dumps(sorted(capabilities)).encode()).hexdigest()` so two specs with the same capability set produce the same hash (matches Phase 41 manifest_hash semantics).

    Create NEW `tests/conftest.py` at the top-level `tests/` directory. Define `pytest_addoption`, `pytest_collection_modifyitems`, and the `clean_venv` fixture. The fixture uses `venv.create()` from the stdlib (Python 3.11+), NOT `subprocess.run(['python', '-m', 'venv', ...])` — direct stdlib call is faster and avoids the `sys.executable` resolution dance. The `pip install -e <repo_root>` step uses `pathlib.Path(__file__).resolve().parent.parent` for `<repo_root>` discovery. Cache the install via a session-scoped fixture so the 30s cost is paid once per `pytest --run-installer-e2e` invocation.

    Extend `pyproject.toml` by adding ONE line to `[tool.pytest.ini_options]`: `markers = ["installer_e2e: requires --run-installer-e2e CLI flag (tier-3 clean-venv tests; ~30s session startup; ~5s per test)"]`. Position the new key after `asyncio_mode = "auto"` (line 126). Do NOT touch lines 122-126.

    Verify the marker is registered: `pytest --markers | grep installer_e2e` returns the marker description.
  </action>
  <verify>
    <automated>
      cd /Users/santino/Projects/horus-os && pytest -q tests/plugins/conftest.py --collect-only 2>&1 | grep -v "no tests collected" && pytest --markers 2>&1 | grep installer_e2e && ruff check tests/plugins/conftest.py tests/conftest.py
    </automated>
  </verify>
  <done>
    `tests/plugins/conftest.py` has new `make_synthetic_plugin` + `make_fake_entry_point` helpers, all six existing fixtures byte-identical. `tests/conftest.py` exists with `pytest_addoption(--run-installer-e2e)` + `pytest_collection_modifyitems` + `clean_venv` fixture. `pyproject.toml` registers the `installer_e2e` marker. `pytest --markers | grep installer_e2e` returns the registered marker. Ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_plugin_pitfalls/ directory with __init__.py + conftest.py + 12 pitfall test files (pitfalls 01-06)</name>
  <files>tests/test_plugin_pitfalls/__init__.py, tests/test_plugin_pitfalls/conftest.py, tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py, tests/test_plugin_pitfalls/test_pitfall_02_manifest_drift.py, tests/test_plugin_pitfalls/test_pitfall_03_entry_point_drift.py, tests/test_plugin_pitfalls/test_pitfall_04_pip_corruption.py, tests/test_plugin_pitfalls/test_pitfall_05_capability_expansion.py, tests/test_plugin_pitfalls/test_pitfall_06_lifecycle_hang.py</files>
  <behavior>
    - Each test file's module docstring opens with the literal string `Pitfall N: <title>` where the title matches PITFALLS.md verbatim. Grep `^Pitfall \d+:` over `tests/test_plugin_pitfalls/` and `.planning/research/PITFALLS.md` returns the same 12 numeric prefixes (no missing, no extras).
    - `test_pitfall_01`: imports `DEFAULT_GRANT_POLICY` from `horus_os.plugins.permissions`, asserts `== "deny"`. Constructs synthetic spec via `make_synthetic_plugin("p1", capabilities=["filesystem.read"])`. Uses `pitfall_db` fixture. `PermissionGate(db).partition(spec)` returns `(set(), {"filesystem.read"})`. Calling `CapabilityGuard.check("filesystem.read")` with no grant row raises `PermissionDenied`.
    - `test_pitfall_02`: loads `tests/fixtures/manifests/manifest_v1_minimum.toml` via `load_manifest(Path(...))`; asserts no exception. Injects an unknown field `signing_key_fingerprint='abc123'` into a v1 manifest dict; asserts `load_manifest` (or the dict equivalent) emits a `WARN` log via `caplog.records` with `'unknown manifest field'` in the message AND returns a valid spec. Constructs a manifest dict with `manifest_version=2`; asserts `ManifestError` raised with `'manifest_version=2 not supported'` in `str(exc)`. Re-loads `manifest_v1_minimum.toml` twice and asserts both calls produce equal `PluginSpec` instances (round-trip stability).
    - `test_pitfall_03`: opens `Path('src/horus_os/plugins/discovery.py').read_text()`, asserts `re.search(r'entry_points\(group=', src)` matches at least once; asserts `re.search(r'entry_points\(\)\[', src)` does NOT match (deprecated dict-access shape banned). Parametrized on `sys.version_info[:2] in {(3, 11), (3, 12)}` (uses `pytest.mark.parametrize` with the version tuple as the param to make both shapes structurally exercised). Times `discover_plugins()` over the empty entry-point registry (via tier-2 `fake_plugin_entry_points` with no entries) and asserts wall-clock <100ms via `time.monotonic()` before/after.
    - `test_pitfall_04`: subprocesses `[sys.executable, '-m', 'pytest', '--collect-only', '-q', 'tests/plugins/test_installer_venv_refusal.py', 'tests/plugins/test_installer_sdist_refusal.py', 'tests/plugins/test_installer_pth_refusal.py', 'tests/plugins/test_installer_downgrade_refusal.py', 'tests/plugins/test_installer_rollback.py']` from the repo root; asserts return code 0 AND output contains at least one `::test_` per file (5 distinct file-relative test ids minimum). Docstring documents that this is a META-test for Pitfall 4 — the actual installer guards live in Phase 44's five installer tests.
    - `test_pitfall_05`: constructs v1.0 spec via `make_synthetic_plugin("foo", capabilities=["filesystem.read"])` and grants via `PermissionService(db).grant(plugin_name="foo", plugin_version="1.0", capability="filesystem.read", manifest_hash=<v1.0 hash>, actor="test")`. Constructs v1.1 spec via `make_synthetic_plugin("foo", capabilities=["filesystem.read", "net.outbound"])` — different `manifest_hash`. Asserts `PermissionGate(db).partition(v11_spec)` returns `(granted=set(), pending={"filesystem.read", "net.outbound"})` — the v1.0 grant does NOT inherit because `manifest_hash` differs; pitfall-5 prevention is structurally enforced.
    - `test_pitfall_06`: subprocesses `[sys.executable, '-m', 'pytest', '--collect-only', '-q', 'tests/plugins/test_bounded_lifecycle.py']` — META-assertion that the Phase 43 substrate exists. Inline regression: defines a synthetic `_SlowStart` class with `async def start(self, ctx): await asyncio.sleep(3.0)`; wraps in `asyncio.wait_for(adapter.start(None), timeout=2.0)`; `start_t = time.monotonic()`; expects `asyncio.TimeoutError` raised; `assert time.monotonic() - start_t < 2.5` (the wait_for budget bound).
  </behavior>
  <action>
    Create `tests/test_plugin_pitfalls/__init__.py` (empty file, single comment line is fine). Create `tests/test_plugin_pitfalls/conftest.py` with `pitfall_db` fixture returning a `Database(':memory:')` instance with `db.init()` already called. Mirror the `installed_db` fixture shape from `tests/plugins/conftest.py:215-220` but use `:memory:` instead of `tmp_path / 'horus.sqlite3'`.

    Create the six pitfall test files listed above. Each file:
    - Opens with `"""Pitfall N: <title from PITFALLS.md>` as the module docstring.
    - Imports from `horus_os.plugins.api` where possible (per pitfall 8); imports from non-`api` submodules ONLY when introspecting the source as part of the regression (pitfall 8 itself is the structural assertion).
    - Uses pytest fixtures from `tests/plugins/conftest.py` (via the auto-discovery walk — pytest finds parent conftests) plus the new `pitfall_db` from the sibling conftest.
    - For pitfall 4 + 6 (meta-tests), the subprocess call uses `subprocess.run([sys.executable, '-m', 'pytest', '--collect-only', '-q', ...], capture_output=True, text=True, cwd=<repo_root>)`. Asserts `result.returncode == 0` and parses `result.stdout` for `::test_` markers.
    - For pitfall 3, the source-grep test reads the file via `pathlib.Path('src/horus_os/plugins/discovery.py')` resolved from the repo root (use `Path(__file__).resolve().parents[2]` to find the repo root).
    - For pitfall 5, `manifest_hash` is computed inline via `hashlib.sha256(json.dumps(sorted(capabilities)).encode()).hexdigest()` — matches the `make_synthetic_plugin` helper from Task 1.

    Run `pytest tests/test_plugin_pitfalls/test_pitfall_0{1,2,3,4,5,6}_*.py -v` and verify all six pass. Verify with `--collect-only` that 6 files yield 6+ test items (some files have parametrized sub-tests).
  </action>
  <verify>
    <automated>
      cd /Users/santino/Projects/horus-os && pytest -q tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py tests/test_plugin_pitfalls/test_pitfall_02_manifest_drift.py tests/test_plugin_pitfalls/test_pitfall_03_entry_point_drift.py tests/test_plugin_pitfalls/test_pitfall_04_pip_corruption.py tests/test_plugin_pitfalls/test_pitfall_05_capability_expansion.py tests/test_plugin_pitfalls/test_pitfall_06_lifecycle_hang.py && ruff check tests/test_plugin_pitfalls/
    </automated>
  </verify>
  <done>
    All six files exist with the right module docstrings (`Pitfall N: <title>`); pitfall_db fixture exists in `tests/test_plugin_pitfalls/conftest.py`; all six tests pass on Ubuntu and macOS; ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the remaining six pitfall test files (07-12) and verify the full pitfall suite + the default pytest run stays under 90s</name>
  <files>tests/test_plugin_pitfalls/test_pitfall_07_observability_attribution.py, tests/test_plugin_pitfalls/test_pitfall_08_public_api_leak.py, tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py, tests/test_plugin_pitfalls/test_pitfall_10_permission_ui.py, tests/test_plugin_pitfalls/test_pitfall_11_test_isolation.py, tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py</files>
  <behavior>
    - `test_pitfall_07`: uses `pitfall_db` (in-memory). Asserts `PRAGMA table_info('tool_invocations')` includes a row with `name='plugin_name'`. Asserts `PRAGMA table_info('llm_calls')` includes the same. Inserts a `tool_invocations` row with `plugin_name='foo-plugin'` and another with `plugin_name=NULL`; reads back via `SELECT plugin_name FROM tool_invocations WHERE ...` and asserts the values match (string vs `None`). Test docstring documents that v0.4 rows roll up under `plugin_name IS NULL` → `'horus-os core'` bucket per OBSERVE-01.
    - `test_pitfall_08`: imports `horus_os.plugins.api`, introspects `api.__all__`. Asserts the set equals `{'PluginContext', 'Capability', 'PluginSpec', 'Adapter', 'AdapterContext', 'LifecycleAdapter', 'CapabilityGuard', 'PermissionDenied', 'require_capability', 'Tool'}` exactly. Performs a controlled `from horus_os.plugins.api import *` via `exec` into a fresh namespace dict; asserts the set of public names (no leading underscore) equals `set(api.__all__)`. Test docstring notes the Phase 48 ruff rule banning `from horus_os` imports outside `horus_os.plugins.api` in reference-plugin code is out of scope here (TEST-21 ships it).
    - `test_pitfall_09`: copies `tests/fixtures/v0_4_database.sqlite3` to `tmp_path/horus.sqlite3` (DO NOT mutate the checked-in fixture); opens it via `Database(tmp_path/'horus.sqlite3')`; reads `SELECT version FROM schema_version` BEFORE `db.init()` and asserts `5`; calls `db.init()`; reads version AFTER and asserts `6`; asserts `PRAGMA table_info('tool_invocations')` has `plugin_name`; reads existing `traces` rows and asserts row count + a sample row's `(trace_id, agent_name, status)` tuple matches the pre-migration sample (read via raw `sqlite3.connect` on the pre-init fixture path); asserts new tables `plugins`, `plugin_capabilities`, `plugin_status` exist and are empty (`SELECT COUNT(*) FROM <table>` returns 0); calls `db.init()` a second time and asserts no exceptions raised, schema version still 6, traces row count unchanged.
    - `test_pitfall_10`: imports `Capability` from `horus_os.plugins.capability_catalog`; iterates `Capability` enum members and asserts each has a `.description` attribute of length >= 20 chars. Imports the grant-prompt formatter (Phase 44 — likely `horus_os.plugins.installer.format_grant_prompt` or similar; grep the codebase if exact location uncertain). Constructs a synthetic spec via `make_synthetic_plugin("foo", capabilities=["filesystem.read"])`; calls the formatter; asserts the rendered output contains the `filesystem.read` description string ('Read any file in your home directory' or whatever the catalog defines) AND does NOT contain the substring `'filesystem.read'` as a standalone token outside a parenthetical/code-block context.
    - `test_pitfall_11`: tier-2 isolation test — uses the `fake_plugin_entry_points` fixture twice in two separate test functions. First test injects entries, asserts they appear via `discover_plugins()`. Second test fixture is fresh (different fixture instance per test); injects nothing; asserts `discover_plugins()` returns whatever the real registry has (i.e., NO leak from test 1). Also asserts `from importlib.metadata import entry_points; entry_points(group='horus_os.plugins')` returns the runner-venv state (unchanged by the monkeypatch — verified by capturing the result before and after the tier-2 fixture goes out of scope). Tier-3 portion of pitfall 11 is in a SEPARATE test function decorated with `@pytest.mark.installer_e2e`: uses the `clean_venv` fixture, asserts the venv's python executable path is NOT `sys.executable` (different interpreter), asserts the venv's site-packages directory is under `tmp_path` (not the runner venv's site-packages).
    - `test_pitfall_12`: imports `MANIFEST_V1_SCHEMA` from `horus_os.plugins.manifest`. Calls `MANIFEST_V1_SCHEMA.model_json_schema()` and asserts the return is a dict with top-level keys `{'type', 'properties', 'required'}` (Pydantic v2 JSON Schema shape). Calls it twice and `json.dumps(schema_a, sort_keys=True) == json.dumps(schema_b, sort_keys=True)` — byte-stable serialization. The docs-drift diff portion (against `docs/manifest-v1.schema.json`) is `pytest.skip('docs/manifest-v1.schema.json shipped in Phase 47 — docs-drift gate activates then')` and a TODO comment in the docstring points to Phase 47.
    - Final assertion at the end of Task 3: run `pytest tests/test_plugin_pitfalls/ tests/plugins/ -q --no-header --tb=no` and measure wall-clock; assert <90s on the developer's machine. The Ubuntu CI runner is faster than typical dev hardware, so a 90s local budget gives headroom.
  </behavior>
  <action>
    Create the six remaining pitfall test files. Each follows the structural rules from Task 2 (module docstring `Pitfall N: <title>`, imports from `horus_os.plugins.api` where possible, uses `pitfall_db` from sibling conftest + `make_synthetic_plugin` from parent conftest).

    For `test_pitfall_09`, use `shutil.copy()` to copy `tests/fixtures/v0_4_database.sqlite3` into `tmp_path` BEFORE opening it via `Database()` — never mutate the checked-in fixture file. Verify the pre-migration version with a raw `sqlite3.connect()` read (do NOT call `Database.init()` on the checked-in fixture path).

    For `test_pitfall_10`, locate the grant-prompt formatter in the codebase: `grep -rn "def format_grant_prompt\|def format_capability_prompt\|capability description" src/horus_os/plugins/` — use whichever function is exported. If multiple candidates exist, pick the one consumed by `horus_os.plugins.installer` (Phase 44 ships the prompt UI).

    For `test_pitfall_11`'s tier-3 portion, decorate the function with `@pytest.mark.installer_e2e` and depend on the `clean_venv` fixture from `tests/conftest.py`. The tier-2 portions of pitfall 11 do NOT need the marker — they run in the default pytest invocation.

    For `test_pitfall_12`, the docs-drift gate is intentionally a skip until Phase 47 ships `docs/manifest-v1.schema.json`. Use `pytest.skip(reason='...')` (skip at runtime; the test still collects). The byte-stable serialization assertion runs unconditionally — that part doesn't depend on the docs file.

    After all 12 files exist: run `pytest tests/test_plugin_pitfalls/ -v` and verify all 12 pass (the tier-3 portion of pitfall 11 SKIPS without `--run-installer-e2e`). Run `pytest tests/test_plugin_pitfalls/ tests/plugins/ -q` and assert total wall-clock <90s. Run `ruff check tests/test_plugin_pitfalls/` and assert clean. Run `pytest --strict-markers tests/test_plugin_pitfalls/ -q` and assert no marker errors (catches typos on `installer_e2e`).

    Run the FULL test suite once: `pytest tests/ -q --no-header --tb=no` to confirm no regression to the existing 520+ tests.
  </action>
  <verify>
    <automated>
      cd /Users/santino/Projects/horus-os && pytest -q tests/test_plugin_pitfalls/ && pytest -q --strict-markers tests/test_plugin_pitfalls/ && ruff check tests/test_plugin_pitfalls/ && python -c "import time; import subprocess; t=time.monotonic(); subprocess.run(['pytest', '-q', '--no-header', '--tb=no', 'tests/test_plugin_pitfalls/', 'tests/plugins/'], check=True); print(f'wall_clock={time.monotonic()-t:.1f}s'); assert time.monotonic()-t < 90, 'budget exceeded'"
    </automated>
  </verify>
  <done>
    All 12 pitfall test files exist with correct module docstrings (`Pitfall N: <title>` matches PITFALLS.md verbatim, 1:1 by number); all 12 collect and pass on the default pytest invocation (tier-3 portion of pitfall 11 skips with the documented reason); `pytest tests/test_plugin_pitfalls/ tests/plugins/` runs under 90s; ruff clean; `--strict-markers` clean; the full `pytest tests/` suite still passes (no regression to existing 520+ tests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner venv ↔ tier-3 clean_venv | Tier-3 fixture creates a fresh venv; pip-installs the repo via `[sys.executable, '-m', 'pip', 'install', '-e', <repo_root>]`. The clean venv may be polluted by future tests, but the runner venv must remain pristine. |
| CI host filesystem ↔ tier-3 venv tmp_path | The clean_venv is created under `tmp_path_factory.getbasetemp()` — pytest cleans this between sessions on its own schedule. We do NOT manually delete; pytest's tmp-path machinery owns the lifecycle. |
| pytest collection ↔ installer_e2e marker | Without `--run-installer-e2e`, no installer_e2e-marked test ever runs; defense-in-depth: the `clean_venv` fixture also raises `pytest.skip` on first access if the flag is absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | T (Tampering) | tests/fixtures/v0_4_database.sqlite3 | mitigate | `test_pitfall_09` copies the fixture to `tmp_path` via `shutil.copy()` BEFORE opening it; never mutates the checked-in file. Verified by hashing the fixture before and after the test run (out-of-band check; not in the test itself but documented in `done`). |
| T-46-02 | T (Tampering) | tests/conftest.py:clean_venv | mitigate | The fixture writes only under `tmp_path_factory.getbasetemp()`; never touches `~/.horus-os/`, `sys.executable`'s venv, or any project-relative path. Tear-down is owned by pytest's tmp-path machinery (no manual `shutil.rmtree`). |
| T-46-03 | T (Tampering) | tier-2 monkeypatched entry_points | mitigate | The `fake_plugin_entry_points` fixture is per-test scope (`@pytest.fixture` default); pytest's monkeypatch fixture auto-undoes at test teardown. `test_pitfall_11` structurally asserts no leak between two consecutive test invocations. |
| T-46-04 | D (Denial of Service) | tier-3 clean_venv 30s startup cost | accept | The `installer_e2e` marker is off by default; cost only materializes when `--run-installer-e2e` is explicitly passed (CI nightly job, never on the developer-default `pytest` run). |
| T-46-05 | I (Information Disclosure) | subprocess pytest --collect-only output | accept | Pitfall 4 + 6 meta-tests parse pytest stdout; output is structured pytest collection format; no secrets, no env vars, no user data. The subprocess inherits the parent env via default `subprocess.run` semantics — acceptable for a CI test that needs to find pytest the same way the user runs it. |
| T-46-06 | E (Elevation of Privilege) | pip install -e <repo_root> inside clean_venv | mitigate | The clean_venv is isolated from the runner venv (different `sys.prefix`). Installing the repo into the clean venv cannot affect the runner; the clean venv is torn down by pytest's tmp-path lifecycle. The fixture refuses to install anything beyond `-e <repo_root>` — no arbitrary packages from PyPI, no plugin-installer exercise (that's Phase 49's TEST-20 scope). |
| T-46-07 | R (Repudiation) | test_pitfall_12 skip on docs-drift | accept | The skip is intentional: docs/manifest-v1.schema.json ships in Phase 47. The skip reason is explicit and surfaces in pytest output; future Phase 47 work activates the gate by deleting the skip and adding the diff. No risk of silent regression because the byte-stable serialization assertion still runs. |

Mitigation references — T-46-01 / T-46-02 / T-46-03 / T-46-06 are encoded in task action text and verified by `done` criteria. T-46-04 / T-46-05 / T-46-07 are accepted with documented rationale.
</threat_model>

<verification>
After all three tasks:

1. `pytest tests/test_plugin_pitfalls/ -v` — all 12 pass; pitfall_11's tier-3 portion skips with `requires --run-installer-e2e flag` reason.
2. `pytest tests/test_plugin_pitfalls/ tests/plugins/ -q --no-header --tb=no` — under 90s wall clock.
3. `pytest tests/ -q --no-header --tb=no` — no regression to the existing 520+ tests.
4. `pytest --markers | grep installer_e2e` — marker registered with the documented description.
5. `pytest --strict-markers tests/test_plugin_pitfalls/ -q` — no marker typos.
6. `ruff check tests/plugins/conftest.py tests/conftest.py tests/test_plugin_pitfalls/` — clean.
7. `grep '^Pitfall \d\+:' .planning/research/PITFALLS.md | wc -l` returns 12; `grep -rh '^Pitfall \d\+:' tests/test_plugin_pitfalls/*.py | sort -u | wc -l` returns 12; the two sets match by pitfall number.
8. `pytest tests/test_plugin_pitfalls/ --run-installer-e2e -q` — pitfall_11's tier-3 portion runs and passes (manually verified once locally before pushing; CI runs this on a separate nightly job per ROADMAP Phase 49 design).
</verification>

<success_criteria>
- Three-tier fixture strategy live in `tests/plugins/conftest.py` (tier-1 helper + tier-2 monkeypatch from Phase 42) and `tests/conftest.py` (tier-3 clean_venv).
- 12 pitfall test files in `tests/test_plugin_pitfalls/` matching `.planning/research/PITFALLS.md` 1:1 by number; each file's module docstring opens with `Pitfall N: <title>` verbatim.
- `installer_e2e` marker registered in `pyproject.toml`; default pytest run never spawns a venv; `--run-installer-e2e` flag opts into tier-3.
- Default `pytest tests/test_plugin_pitfalls/ tests/plugins/` runs <90s.
- Full suite (`pytest tests/`) passes with no regression to existing 520+ tests.
- Ruff clean across new files; `--strict-markers` clean.
- TEST-16 and TEST-17 satisfied per ROADMAP Phase 46 success criteria 1-5.
</success_criteria>

<output>
After completion, create `.planning/phases/46-test-surface-three-tier-fixtures-pitfall-suite/46-01-SUMMARY.md` per the gsd-execute-plan summary template.
</output>
