---
phase: 05
plan: "00"
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/horus_os/memory/__init__.py
  - src/horus_os/memory/notes.py
  - src/horus_os/memory/tools.py
  - src/horus_os/types.py
  - src/horus_os/__init__.py
  - tests/test_notes_store.py
  - tests/test_memory_tools.py
autonomous: true
requirements:
  - MEM-01
tags:
  - memory
  - notes-folder
  - search

must_haves:
  truths:
    - "`NotesStore(notes_dir)` accepts a directory path and stores it. Construction does NOT touch the filesystem."
    - "`NotesStore.list_notes() -> list[NoteRef]` returns all `.md` files under notes_dir, recursive, sorted by relative path"
    - "`NotesStore.read_note(rel_path) -> str` returns the text content. Raises PermissionError if rel_path escapes notes_dir."
    - "`NotesStore.search_notes(query, *, limit=20) -> list[NoteRef]` does case-insensitive substring match against filename and file body. Returns the top `limit` matches sorted by score (count of substring hits), ties broken by relative path."
    - "Empty query string returns an empty list, not all notes"
    - "`NoteRef(path, title, size_bytes, modified_at, preview)` holds metadata. `title` is the first markdown heading or filename. `preview` is the first 240 characters of the body."
    - "Tool factories `search_notes_tool(store)`, `read_note_tool(store)`, `list_notes_tool(store)` return Tools that bind to a NotesStore instance"
    - "Tools return JSON-serializable Python data structures (list of dicts for refs, string for read), so they can be sent directly back to the model as tool results"
    - "Public API includes `NotesStore`, `NoteRef`, `search_notes_tool`, `read_note_tool`, `list_notes_tool`"
  artifacts:
    - path: "src/horus_os/memory/notes.py"
      provides: "NotesStore class"
      contains: "class NotesStore"
      min_lines: 50
    - path: "src/horus_os/memory/tools.py"
      provides: "Three Tool factories bound to a NotesStore"
      contains: "def search_notes_tool"
      min_lines: 40

tasks:
  - id: "1"
    title: "NoteRef dataclass in types.py"
    files:
      - src/horus_os/types.py

  - id: "2"
    title: "NotesStore class in memory/notes.py"
    files:
      - src/horus_os/memory/__init__.py
      - src/horus_os/memory/notes.py

  - id: "3"
    title: "Memory tool factories in memory/tools.py"
    files:
      - src/horus_os/memory/tools.py

  - id: "4"
    title: "Re-export from horus_os.__init__"
    files:
      - src/horus_os/__init__.py

  - id: "5"
    title: "Tests"
    files:
      - tests/test_notes_store.py
      - tests/test_memory_tools.py

  - id: "6"
    title: "Verify lint + format + tests + CLI smoke"
    files: []

  - id: "7"
    title: "Commit + SUMMARY + ROADMAP/STATE advance + push"
    files:
      - .planning/phases/05-memory-layer-read-path/05-00-SUMMARY.md
      - .planning/ROADMAP.md
      - .planning/STATE.md
      - ROADMAP.md

success_criteria:
  - "ruff check exits 0"
  - "ruff format --check exits 0"
  - "pytest -v exits 0 with at least 12 new tests"
  - "`from horus_os import NotesStore, NoteRef, search_notes_tool, read_note_tool, list_notes_tool` succeeds"
  - "`horus-os --version` still prints `horus-os 0.0.1`"
  - "05-00-SUMMARY.md committed and ROADMAP/STATE advanced"
  - "Branch pushed to origin/main"
