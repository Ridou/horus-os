---
phase: 09
plan: "00"
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - src/horus_os/cli/wizard.py
  - src/horus_os/cli/init_cmd.py
  - src/horus_os/__main__.py
  - src/horus_os/__init__.py
  - tests/test_wizard.py
autonomous: true
requirements:
  - WIZARD-01
  - WIZARD-02
  - WIZARD-03
  - WIZARD-04
tags:
  - wizard
  - onboarding
  - api-keys
  - validate

must_haves:
  truths:
    - "`horus-os init --interactive` walks the user through configuration after creating files"
    - "Wizard prints direct links to https://console.anthropic.com/settings/keys and https://aistudio.google.com/apikey"
    - "Wizard prompts for each provider key with the link inline. Pasting a key triggers a live-ping validation that does a 1-token request. Leaving the prompt blank skips that provider."
    - "Validated keys are written to `<data_dir>/.env` in `KEY=value` lines, atomically (tmp file + rename), with mode 0600 on POSIX"
    - "Wizard state lives at `<data_dir>/.horus-init-state.json`. Resuming an interrupted wizard skips completed steps."
    - "Wizard is idempotent: rerunning with the same answers writes the same files. No duplicate .env entries."
    - "Final step asks for the default provider when both keys validated; if only one validated it auto-picks that one; if neither, prints a warning and saves default=anthropic."
    - "All prompts and validation calls take injectable input/output streams + a validator function so tests can drive the flow deterministically."
    - "Run-wizard helper exits cleanly on EOF (e.g., Ctrl-D) and preserves any progress in the state file."
  artifacts:
    - path: "src/horus_os/cli/wizard.py"
      provides: "Wizard implementation: prompts, env file write, state file, key validators"
      contains: "def run_wizard"
      min_lines: 100

tasks:
  - id: "1"
    title: "Implement wizard module"
    files:
      - src/horus_os/cli/wizard.py
    notes: "Functions: `run_wizard(config, *, stdin, stdout, validators)`, `_load_state`, `_save_state`, `_write_env`, `_validate_anthropic_key(key)`, `_validate_gemini_key(key)`. Validators are factored out so tests can stub them without monkeypatching the SDKs."

  - id: "2"
    title: "Wire --interactive into init_cmd"
    files:
      - src/horus_os/cli/init_cmd.py
      - src/horus_os/__main__.py
    notes: "After the existing init logic creates files and saves the config, call run_wizard when args.interactive is True. The wizard receives stdin=sys.stdin, stdout=stdout, validators=DEFAULT_VALIDATORS."

  - id: "3"
    title: "Tests"
    files:
      - tests/test_wizard.py
    notes: "Drive run_wizard with StringIO buffers for input/output and a dict of fake validators. Cover: both keys pasted + validated, one key validated other skipped, both skipped, validation failure, resume from existing state, .env file content, .env file mode (POSIX only)."

  - id: "4"
    title: "Verify + commit + summary + push"

success_criteria:
  - "ruff + format clean"
  - "pytest passes with at least 8 new tests"
  - "horus-os init --interactive prompts and writes .env when keys provided"
  - "Phase 09 marked complete in ROADMAP and STATE"
  - "Branch pushed"
