---
phase: 51-ci-hardening-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_contribution_gate_pitfalls/__init__.py
  - tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py
  - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py
  - tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py
autonomous: true
requirements:
  - TEST-23
  - CIHARD-01
  - CIHARD-03
  - CIHARD-04
user_setup: []

must_haves:
  truths:
    - "Directory tests/test_contribution_gate_pitfalls/ exists and is pytest-collectible"
    - "test_pitfall_01_pull_request_target.py asserts no workflow declares pull_request_target (CIHARD-01)"
    - "test_pitfall_02_action_sha_pinning.py asserts every third-party uses: is @<40-hex> or ./local (CIHARD-04)"
    - "test_ci_hardening_workflow_structure.py asserts top-level permissions, persist-credentials false on checkout, no github.event.*/github.head_ref/github.base_ref interpolation in run shells (CIHARD-02 + CIHARD-03; head_ref/base_ref aliases included per OQ-3 RESOLVED in RESEARCH.md)"
    - "Every TEST-23 scanner has at least one non-vacuity check (synthetic-fixture test proves the regex fires on a known violation)"
    - "ruff check and ruff format --check are clean on all four new files"
    - "TEST-23 production assertions are RED against the pre-Phase-51 workflow tree (proves they will detect regressions later)"
    - "Implements D-04 (TEST-23 three-file split under tests/test_contribution_gate_pitfalls/) and D-05 (pull_request_target PR-time enforcement via test_pitfall_01; release-time half deferred to Phase 57) per 51-CONTEXT.md"
  artifacts:
    - path: "tests/test_contribution_gate_pitfalls/__init__.py"
      provides: "Package marker for the contribution-gate pitfall regression suite"
      contains: "Phase 51"
    - path: "tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py"
      provides: "CIHARD-01 regression: no pull_request_target trigger anywhere"
      contains: "def test_no_workflow_uses_pull_request_target"
    - path: "tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py"
      provides: "CIHARD-04 regression: every uses: line is SHA-pinned or local"
      contains: "def test_every_uses_line_is_sha_pinned"
    - path: "tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py"
      provides: "CIHARD-02 + CIHARD-03 regression: structural workflow assertions"
      contains: "def test_permissions_read_all_on_every_workflow"
  key_links:
    - from: "tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py"
      to: ".github/workflows/*.yml"
      via: "REPO_ROOT = Path(__file__).resolve().parents[2]; WORKFLOWS_DIR = REPO_ROOT / .github / workflows"
      pattern: "parents\\[2\\].*\\.github.*workflows"
    - from: "tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py"
      to: ".github/workflows/*.yml"
      via: "sorted(WORKFLOWS_DIR.glob('*.yml')) iteration"
      pattern: "glob.*\\*\\.yml"
    - from: "tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py"
      to: ".github/workflows/*.yml"
      via: "sorted(WORKFLOWS_DIR.glob('*.yml')) iteration"
      pattern: "glob.*\\*\\.yml"
---

<objective>
Land the four-file Wave 0 test scaffolding under `tests/test_contribution_gate_pitfalls/` that enforces CIHARD-01, CIHARD-03 (partial), and CIHARD-04 at PR-time. Tests scan `.github/workflows/*.yml` with stdlib `re` parsing (no PyYAML) and use the v0.5 `tests/test_plugin_pitfalls/` directory shape verbatim per D-04.

Purpose: Establish the regression-test surface BEFORE the workflow rewrites land, so Plan 02's hardening edits are guarded by a failing-then-green Nyquist signal. Each scanner ships a non-vacuity synthetic-fixture test that proves the regex fires on a known violation (mirrors v0.5 TEST-21 layer-2 idiom).

Output: One package marker `__init__.py`, three regression test modules. Production assertions are expected RED in this plan (pre-edit workflows fail), GREEN after Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-ci-hardening-substrate/51-CONTEXT.md
@.planning/phases/51-ci-hardening-substrate/51-RESEARCH.md
@.planning/phases/51-ci-hardening-substrate/51-PATTERNS.md
@.planning/phases/51-ci-hardening-substrate/51-VALIDATION.md
@CLAUDE.md
@tests/test_plugin_pitfalls/__init__.py
@tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py
@tests/test_plugin_pitfalls/test_pitfall_08_public_api_leak.py
@tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py
@tests/plugins/test_reference_plugin_public_api_only.py
@scripts/lint_no_wallclock.py
@.github/workflows/ci.yml
@.github/workflows/issue-claim-watcher.yml

<interfaces>
<!-- The three regression tests resolve REPO_ROOT identically (parents[2]) -->
<!-- because tests/test_contribution_gate_pitfalls/<file>.py is the same nesting -->
<!-- depth as tests/plugins/<file>.py (the v0.5 analog). -->

Canonical idiom (all three test files):
- REPO_ROOT = Path(__file__).resolve().parents[2]
- WORKFLOWS_DIR = REPO_ROOT / ".github" / "workflows"
- Iterate: for workflow in sorted(WORKFLOWS_DIR.glob("*.yml")):
- Comment skip in line-by-line scans: if line.strip().startswith("#"): continue
- Failure message convention: "<CIHARD-NN>: <claim>. Offenders:\n  <path:line:text>"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create package marker + test_pitfall_01_pull_request_target.py + test_pitfall_02_action_sha_pinning.py</name>
  <files>
    tests/test_contribution_gate_pitfalls/__init__.py,
    tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py,
    tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py
  </files>
  <read_first>
    - tests/test_plugin_pitfalls/__init__.py (analog for the two-line comment marker)
    - tests/test_plugin_pitfalls/test_pitfall_01_default_allow.py (file-shape analog: docstring, __future__, single-claim-per-def)
    - tests/plugins/test_reference_plugin_public_api_only.py (regex-over-source-tree mechanism + sorted glob + non-vacuity test pattern)
    - .planning/phases/51-ci-hardening-substrate/51-RESEARCH.md sections "TEST-23 file 1" (lines 521-571) and "TEST-23 file 2" (lines 573-627) for the verified regex shapes
    - .github/workflows/ci.yml (the file being scanned; verifies the regex matches actual content)
    - .github/workflows/issue-claim-watcher.yml (second file in scope)
  </read_first>
  <behavior>
    - test_no_workflow_uses_pull_request_target (CIHARD-01): scans every .github/workflows/*.yml; offenders list collects (relative_path, line_number, line_text); comment-only lines (stripped startswith("#")) skipped; matcher is _TRIGGER_PATTERN = re.compile(r"\bpull_request_target\b"); failure message names the requirement ("CIHARD-01") and lists offenders one per line; passes vacuously against current tree (no pull_request_target present)
    - test_every_uses_line_is_sha_pinned (CIHARD-04): scans every .github/workflows/*.yml; iterates line-by-line; skips comment-only lines via stripped.startswith("#") continue (symmetry with the test_pitfall_01 pull_request_target scanner; ensures a future YAML-comment-as-doc line like `# uses: bad/action@v1` is NOT flagged); uses _USES_PATTERN = re.compile(r"^\s*-?\s*uses:\s*([^@\s#]+)@(\S+)") matched per non-comment line to capture (path, ref) pairs; allowlist: path startswith "./" or "." backslash (local action) OR ref matches _ALLOWED_REF = re.compile(r"^[0-9a-f]{40}$"); offenders list collects (relative_path, path, ref); failure message names "CIHARD-04 requires 40-char commit SHA"; EXPECTED RED in this plan because ci.yml has 5 @v4 + 5 @v5 + 0 @sha and issue-claim-watcher.yml has 2 @v7; will go GREEN after Plan 02 ships the SHA rewrite
    - test_scanner_catches_synthetic_sha_violation (non-vacuity, CIHARD-04 defense): writes a tmp_path synthetic .yml with the line "  - uses: actions/checkout@v4" into tmp_path/.github/workflows/fake.yml; reuses the scanner helper against tmp_path; asserts exactly one offender containing "actions/checkout" and "@v4"; proves the scanner is non-vacuous; required per Pattern Map "Non-vacuity test pattern"
    - test_scanner_accepts_synthetic_local_action_ref (non-vacuity, Pitfall 51-C defense): writes a tmp_path synthetic .yml with "  - uses: ./.github/actions/local-foo"; asserts zero offenders; proves the ./ branch of the allowlist works; required per Pitfall 51-C in RESEARCH.md
    - Determinism: sorted() wrapper on every glob; required for cross-OS test ordering per CLAUDE.md
  </behavior>
  <action>
    Create tests/test_contribution_gate_pitfalls/__init__.py exactly mirroring tests/test_plugin_pitfalls/__init__.py shape: a two-line "#" comment block. Line 1: "# Marker file for the Phase 51 contribution-gate pitfall regression suite." Line 2: "# See .planning/research/PITFALLS.md for the v0.6 contribution-gate pitfalls (CIHARD-01..05)."

    Create tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py using the verified shape from 51-RESEARCH.md lines 521-571. Top of file: triple-quoted docstring matching that section verbatim (it names PITFALL 1, CIHARD-01, references the escape-hatch grammar deferral). Then `from __future__ import annotations`, `import re`, `from pathlib import Path`. Module constants: REPO_ROOT = Path(__file__).resolve().parents[2]; WORKFLOWS_DIR = REPO_ROOT / ".github" / "workflows"; _TRIGGER_PATTERN = re.compile(r"\bpull_request_target\b"). Single test function test_no_workflow_uses_pull_request_target() -> None with the comment-skip discipline (strip().startswith("#") continue) per Pattern Map note. Failure message uses the convention "pull_request_target trigger found in:\n  <path>:<line>: <text>\nv0.6 ships ZERO pull_request_target triggers (CIHARD-01)." No em-dashes anywhere (CLAUDE.md HR3); use commas/periods only. Iterate sorted(WORKFLOWS_DIR.glob("*.yml")) for cross-OS determinism per CLAUDE.md.

    Create tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py using the verified shape from 51-RESEARCH.md lines 573-627. Same opening matter (docstring naming PITFALL 2 / CIHARD-04 with allowed/rejected forms table from research, __future__ import, re, pathlib imports, REPO_ROOT/WORKFLOWS_DIR constants). Module constants: _USES_PATTERN per behavior block above; _ALLOWED_REF per behavior block above. Helper function _is_local_action(path: str) -> bool returning path.startswith("./") or path.startswith(".\\"). Helper function _scan_workflow_dir(workflows_dir: Path) -> list[tuple[str, str, str]] that iterates sorted(workflows_dir.glob("*.yml")), walks the file line-by-line, skips comment-only lines (stripped.startswith("#") continue) so doc-comments quoting bad pin examples are NOT flagged, applies _USES_PATTERN.match to each non-comment line, skips local actions, returns (relative_path_str, path, ref) tuples for offenders. Symmetry note: this comment-skip mirrors the test_pitfall_01 pull_request_target scanner so the two TEST-23 file shapes are uniform; without it, a future PITFALLS.md-quoting comment in a workflow YAML would create a false positive. Test functions: (a) test_every_uses_line_is_sha_pinned that calls _scan_workflow_dir(WORKFLOWS_DIR) and asserts empty; (b) test_scanner_catches_synthetic_sha_violation(tmp_path: Path) that creates tmp_path/".github"/"workflows" mkdir parents=True, writes a fake.yml with one bad uses: line, calls _scan_workflow_dir(tmp_path/".github"/"workflows"), asserts len == 1 and "actions/checkout" appears in the path field and "v4" appears in the ref; (c) test_scanner_accepts_synthetic_local_action_ref(tmp_path: Path) that writes a fake.yml with "  - uses: ./.github/actions/local-foo" and asserts the scanner returns []. Failure message convention for the production assertion: "Non-SHA action pins found (CIHARD-04 requires 40-char commit SHA):\n  <rel>: <path>@<ref>". No em-dashes. Use sorted() on every glob.

    Wave-0 expectation: the production assertion test_every_uses_line_is_sha_pinned will FAIL when run against the current tree (ci.yml has 10 non-SHA pins, issue-claim-watcher.yml has 2). This is the expected RED state. Plan 02 turns it GREEN. Do NOT mark the test xfail or skip; the red state IS the signal that wave 0 is correctly installed.
  </action>
  <verify>
    <automated>pytest tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py -v &amp;&amp; pytest tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py::test_scanner_catches_synthetic_sha_violation tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py::test_scanner_accepts_synthetic_local_action_ref -v &amp;&amp; ruff check tests/test_contribution_gate_pitfalls/ &amp;&amp; ruff format --check tests/test_contribution_gate_pitfalls/ &amp;&amp; test -f tests/test_contribution_gate_pitfalls/__init__.py &amp;&amp; grep -c "Phase 51" tests/test_contribution_gate_pitfalls/__init__.py</automated>
  </verify>
  <done>
    - tests/test_contribution_gate_pitfalls/__init__.py exists with a two-line comment containing "Phase 51" and "PITFALLS.md"
    - tests/test_contribution_gate_pitfalls/test_pitfall_01_pull_request_target.py exists; pytest collects and runs test_no_workflow_uses_pull_request_target; PASSES (no pull_request_target in current tree)
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py exists; pytest collects three test functions; the two non-vacuity tests (test_scanner_catches_synthetic_sha_violation, test_scanner_accepts_synthetic_local_action_ref) PASS; the production test_every_uses_line_is_sha_pinned FAILS in this plan (expected RED; Plan 02 makes it green)
    - ruff check tests/test_contribution_gate_pitfalls/ exits 0
    - ruff format --check tests/test_contribution_gate_pitfalls/ exits 0
    - No em-dashes anywhere in the three new files (HR3): grep -F "—" tests/test_contribution_gate_pitfalls/ -r returns no matches
    - REPO_ROOT resolution uses Path(__file__).resolve().parents[2] (verified: grep -F "parents[2]" returns at least 2 hits, one per scanner file)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_ci_hardening_workflow_structure.py (CIHARD-02 + CIHARD-03 structural assertions)</name>
  <files>
    tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py
  </files>
  <read_first>
    - tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py (multi-def structural test file shape)
    - tests/test_plugin_pitfalls/test_pitfall_08_public_api_leak.py (long-form failure-message convention, lines 59-66)
    - scripts/lint_no_wallclock.py (line-by-line state machine analog with comment skip)
    - tests/plugins/test_reference_plugin_public_api_only.py (sorted glob + non-vacuity discipline)
    - .planning/phases/51-ci-hardening-substrate/51-RESEARCH.md section "TEST-23 file 3" (lines 629-752) for verified regex shapes
    - .planning/phases/51-ci-hardening-substrate/51-PATTERNS.md "Line-by-line state machine with substring needle pattern" (lines 314-341) for the run-block state machine shape
    - .github/workflows/ci.yml (current state, will not have permissions: read-all or persist-credentials: false yet)
    - .github/workflows/issue-claim-watcher.yml (current state, has top-level permissions: issues: write, different shape that will be refactored in Plan 02)
  </read_first>
  <behavior>
    - test_permissions_read_all_on_every_workflow (CIHARD-02): scans every .github/workflows/*.yml; offenders = workflows missing a column-0 "permissions:" line; matcher _TOP_LEVEL_PERMISSIONS = re.compile(r"^permissions:\s*", re.MULTILINE); WILL PASS on issue-claim-watcher.yml today (has top-level permissions: issues: write); WILL FAIL on ci.yml today (no top-level permissions block); EXPECTED RED in this plan; goes GREEN after Plan 02
    - test_persist_credentials_false_on_every_checkout (CIHARD-03 first clause): scans every workflow; uses _CHECKOUT_USES = re.compile(r"^(\s*)-?\s*uses:\s*actions/checkout@\S+", re.MULTILINE); for each match, slices the next ~15 lines and checks for the literal "persist-credentials: false" substring; offenders = (path, line_number) of checkout uses without persist-credentials in lookahead; EXPECTED RED in this plan (zero checkouts currently have persist-credentials: false); goes GREEN after Plan 02
    - test_no_event_interpolation_in_shells (CIHARD-03 second clause): line-by-line walk per the state machine in PATTERNS.md; tracks in_run_block state via "^\s*-?\s*run:\s*[|>]" detection and indent comparison; comment-only lines skipped; matcher _EVENT_INTERPOLATION = re.compile(r"\$\{\{\s*[^}]*\b(?:github\.event\.[a-zA-Z0-9_.\[\]'\"]+|github\.head_ref|github\.base_ref)\b") per RESEARCH.md Open Question 3 RESOLVED (extends the surface to cover Pitfall 1's documented head_ref/base_ref aliases; the Ultralytics CVE used github.event.pull_request.head.ref but github.head_ref is the shorter and more commonly used alias and a strict github.event.*-only regex would miss it); PASSES on current tree (issue-claim-watcher.yml uses env: BODY: ${{ github.event.comment.body }} pattern which keeps the interpolation OUT of the shell line, mapped to $BODY which the shell reads safely, and the workflow does NOT use github.head_ref or github.base_ref anywhere)
    - test_scanner_catches_synthetic_missing_permissions (non-vacuity for CIHARD-02): writes tmp_path workflow with no top-level permissions and asserts scanner flags it
    - test_scanner_catches_synthetic_checkout_without_persist_credentials (non-vacuity for CIHARD-03 first clause): writes tmp_path workflow with `uses: actions/checkout@abc...` followed by an empty `with:` and asserts scanner flags it
    - test_scanner_catches_synthetic_event_interpolation (non-vacuity for CIHARD-03 second clause, github.event.* branch): writes tmp_path workflow with `run: echo ${{ github.event.pull_request.title }}` and asserts scanner flags it
    - test_scanner_catches_synthetic_head_ref_interpolation (non-vacuity for CIHARD-03 second clause, head_ref alias branch per OQ-3 RESOLVED): writes tmp_path workflow with `run: echo ${{ github.head_ref }}` inside a `run: |` block and asserts scanner flags it; this is the proof-of-coverage for the extended branch of _EVENT_INTERPOLATION (without this test, the head_ref/base_ref alternative would be silently untested even though the regex contains it)
    - Determinism: sorted(workflows_dir.glob("*.yml")) on every iteration
  </behavior>
  <action>
    Create tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py using the verified shape from 51-RESEARCH.md lines 629-752. Module docstring per that section: opens "CIHARD-02 + CIHARD-03: structural workflow hardening assertions." then enumerates the three structural checks one per paragraph (top-level permissions, persist-credentials on every checkout, no github.event.* in run shells), then explains the complementarity with actionlint (actionlint validates contents of an existing permissions block but does not flag absence; actionlint has no persist-credentials rule; actionlint expression rule catches github.event interpolation but the regression is defense-in-depth against the rule being disabled by a # actionlint: ignore comment AND extends the surface to the github.head_ref/github.base_ref aliases that actionlint does NOT flag by default, per RESEARCH.md OQ-3 RESOLVED + PITFALLS.md Pitfall 1 documented threat model). The docstring for test_no_event_interpolation_in_shells MUST explicitly state that the rule covers github.event.*, github.head_ref, and github.base_ref (the Pitfall 1 head_ref/base_ref aliases are in scope). No em-dashes.

    Imports: `from __future__ import annotations`, `import re`, `from pathlib import Path`. Module constants: REPO_ROOT = Path(__file__).resolve().parents[2]; WORKFLOWS_DIR = REPO_ROOT / ".github" / "workflows"; _TOP_LEVEL_PERMISSIONS, _CHECKOUT_USES, _EVENT_INTERPOLATION regexes exactly per the behavior block. The _EVENT_INTERPOLATION regex is the EXTENDED form per OQ-3 RESOLVED, covering github.event.*, github.head_ref, and github.base_ref in a single non-capturing alternation, anchored with \b on both ends to avoid substring false positives. Helper function _workflow_yaml_files() -> list[Path] returning sorted(WORKFLOWS_DIR.glob("*.yml")).

    Refactor the three scanner bodies into helper functions parametrized on the workflows directory so the non-vacuity tests can reuse them against tmp_path:
    - _find_workflows_missing_top_level_permissions(workflows_dir: Path) -> list[Path]
    - _find_checkouts_without_persist_credentials(workflows_dir: Path) -> list[tuple[Path, int]]
    - _find_event_interpolation_in_run_shells(workflows_dir: Path) -> list[tuple[Path, int, str]]

    Production tests call helpers with WORKFLOWS_DIR; non-vacuity tests build a fake .github/workflows/ tree under tmp_path and call helpers with that tmp directory.

    Failure messages follow the v0.5 long-form convention from test_pitfall_08_public_api_leak.py: name the requirement ("CIHARD-02"), list offenders with file:line:text, and tell the maintainer what to do (e.g., "Add `permissions: read-all` to the top of every workflow above `jobs:`." for CIHARD-02; "Add `with:\n  persist-credentials: false` to every actions/checkout step." for CIHARD-03 first clause; "Pass the value via env: instead; see PITFALLS.md Pitfall 1." for the interpolation clause). No em-dashes anywhere.

    For the run-block state machine (third production test) follow the PATTERNS.md analog from scripts/lint_no_wallclock.py: enumerate(text.splitlines(), start=1); detect run: |, run: >, and single-line run: forms; track current run block indent; exit the block when indent drops back; skip comment-only lines (strip().startswith("#") continue) per lint_no_wallclock.py line 90.

    Wave-0 expectation: test_permissions_read_all_on_every_workflow and test_persist_credentials_false_on_every_checkout will FAIL against the current tree. test_no_event_interpolation_in_shells will PASS (issue-claim-watcher.yml carefully passes the comment body via env: BODY rather than ${{ github.event... }} in the shell directly). The three non-vacuity tests all PASS. This RED-on-production, GREEN-on-fixtures is the correct Wave 0 signal; Plan 02 closes the gap.
  </action>
  <verify>
    <automated>pytest tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_no_event_interpolation_in_shells tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_scanner_catches_synthetic_missing_permissions tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_scanner_catches_synthetic_checkout_without_persist_credentials tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_scanner_catches_synthetic_event_interpolation tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_scanner_catches_synthetic_head_ref_interpolation -v &amp;&amp; ruff check tests/test_contribution_gate_pitfalls/ &amp;&amp; ruff format --check tests/test_contribution_gate_pitfalls/ &amp;&amp; pytest tests/test_contribution_gate_pitfalls/ --collect-only -q | grep -v '^#' | grep -c "::" </automated>
  </verify>
  <done>
    - tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py exists
    - pytest collects at least 7 test functions in this file (3 production + 4 non-vacuity: missing-permissions, checkout-without-persist-credentials, event-interpolation, head_ref-interpolation)
    - test_no_event_interpolation_in_shells PASSES (current tree clean)
    - The four non-vacuity tests (test_scanner_catches_synthetic_missing_permissions, test_scanner_catches_synthetic_checkout_without_persist_credentials, test_scanner_catches_synthetic_event_interpolation, test_scanner_catches_synthetic_head_ref_interpolation) all PASS
    - test_permissions_read_all_on_every_workflow FAILS (expected RED; current ci.yml has no top-level permissions)
    - test_persist_credentials_false_on_every_checkout FAILS (expected RED; current checkouts have no persist-credentials)
    - ruff check + ruff format --check both clean on the entire tests/test_contribution_gate_pitfalls/ directory
    - No em-dashes (HR3): grep -F "—" on the new file returns nothing
    - pytest collect across the whole tests/test_contribution_gate_pitfalls/ directory finds at least 10 test items total (test_pitfall_01: 1; test_pitfall_02: 3; this file: 7+), verified by the final pipeline in &lt;automated&gt;
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `pytest tests/test_contribution_gate_pitfalls/ --collect-only -q` lists at minimum 10 test items across 3 files (test_pitfall_01: 1; test_pitfall_02: 3; test_ci_hardening_workflow_structure: 7+ including the head_ref non-vacuity test per OQ-3 RESOLVED)
- All non-vacuity / synthetic-fixture tests PASS
- Production assertions tied to Plan 02 outputs (CIHARD-02 presence, CIHARD-03 persist-credentials, CIHARD-04 SHA-pin) are FAILING; this is the expected Wave 0 RED state that Plan 02 turns GREEN
- `ruff check . && ruff format --check .` clean (project-wide; HR4)
- No em-dashes anywhere new: `grep -RF "—" tests/test_contribution_gate_pitfalls/` returns no matches
- `test -f tests/test_contribution_gate_pitfalls/__init__.py` exits 0
</verification>

<success_criteria>
- Four new files exist under `tests/test_contribution_gate_pitfalls/` matching the D-04 contract exactly: `__init__.py`, `test_pitfall_01_pull_request_target.py`, `test_pitfall_02_action_sha_pinning.py`, `test_ci_hardening_workflow_structure.py`
- Every regex / scanner has a corresponding synthetic-fixture non-vacuity test that PASSES on `tmp_path` fixtures
- Production assertions FAIL against the current `.github/workflows/` tree exactly where Plan 02 will fix them (CIHARD-02 presence, CIHARD-03 persist-credentials, CIHARD-04 SHA-pin), and PASS where the current tree is already compliant (CIHARD-01 no pull_request_target, CIHARD-03 no shell interpolation)
- All new files pass `ruff check` and `ruff format --check`
- Zero em-dashes in committed prose (HR3)
- `Path(__file__).resolve().parents[2] / ".github" / "workflows"` is the resolution idiom in every test file (no string concat, per CLAUDE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/51-ci-hardening-substrate/51-01-SUMMARY.md` per template, calling out which production assertions are RED-by-design (waiting for Plan 02) versus PASSING (already-compliant invariants).
</output>
