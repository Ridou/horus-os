---
phase: 51-ci-hardening-substrate
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/issue-claim-watcher.yml
autonomous: true
requirements:
  - CIHARD-01
  - CIHARD-02
  - CIHARD-03
  - CIHARD-04
  - CIHARD-05
  - TEST-23
user_setup: []

must_haves:
  truths:
    - "ci.yml job NAMES are byte-identical pre/post: lint-and-test, install-smoke, install-smoke-no-otel, install-smoke-with-otel, install-smoke-plugin (release_gate.py grep targets)"
    - "ci.yml has a top-level permissions: read-all block between the on: stanza and jobs: (CIHARD-02)"
    - "Every actions/checkout in ci.yml is pinned to actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2 with persist-credentials: false (CIHARD-03 + CIHARD-04)"
    - "Every actions/setup-python in ci.yml is pinned to actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0 (CIHARD-04)"
    - "ci.yml lint-and-test job has a new step named exactly 'Run actionlint (CIHARD-05)' using raven-actions/actionlint@205b530c5d9fa8f44ae9ed59f341a0db994aa6f8 # v2.1.2 with version: v1.7.12 (CIHARD-05); step is positioned after 'Run ruff format check' and before 'time.time() lint gate (Pitfall 3)'"
    - "issue-claim-watcher.yml has top-level permissions: read-all (CIHARD-02) and per-job permissions: issues: write on detect-claim (preserves canned-reply behavior per Pitfall 51-E)"
    - "issue-claim-watcher.yml has both actions/github-script@v7 sites pinned to actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7.1.0 (CIHARD-04)"
    - "No workflow declares pull_request_target (CIHARD-01; invariant preserved from pre-edit state)"
    - "No run: shell line contains ${{ github.event.* }} interpolation (CIHARD-03 invariant preserved)"
    - "scripts/release_gate.py is UNTOUCHED (Phase 57 extends it from 8 to 13 checks; not in Phase 51 scope)"
    - "pyproject.toml is UNTOUCHED (pip-audit lands Phase 53; no PyYAML; no dep changes)"
    - "All four Plan 01 production tests are GREEN after this plan's edits land"
    - "All pre-Phase-51 pytest tests remain green (zero regressions on the existing suite; the verify pipeline runs the full pytest -v so the actual baseline count is observed at execution time, not hard-coded)"
    - "Implements D-01 (actionlint as a step in lint-and-test named 'Run actionlint (CIHARD-05)'), D-02 (Phase 51 ships PR-time SHA-pin enforcement only; release-time release_gate.py check deferred to Phase 57), D-03 (pinact NOT added to CI; maintainer-driven per Phase 56 RUNBOOK), and D-06 (issue-claim-watcher.yml SHA-pinned + read-all + per-job permissions: issues: write here; file deletion deferred to Phase 59 atomic flip) per 51-CONTEXT.md"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Post-hardening CI workflow: SHA-pinned actions, top-level read-all, persist-credentials: false on every checkout, actionlint step"
      contains: "permissions: read-all"
      min_lines: 220
    - path: ".github/workflows/issue-claim-watcher.yml"
      provides: "Post-hardening issue-claim watcher: SHA-pinned actions, top-level read-all + per-job issues: write"
      contains: "permissions: read-all"
      min_lines: 85
  key_links:
    - from: ".github/workflows/ci.yml lint-and-test job"
      to: "raven-actions/actionlint@205b530c5d9fa8f44ae9ed59f341a0db994aa6f8"
      via: "Run actionlint (CIHARD-05) step"
      pattern: "Run actionlint \\(CIHARD-05\\)"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py"
      via: "every uses: line matches @<40-hex>"
      pattern: "uses: [^@]+@[0-9a-f]{40}"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_permissions_read_all_on_every_workflow"
      via: "^permissions: read-all$ at column 0"
      pattern: "^permissions: read-all$"
    - from: ".github/workflows/issue-claim-watcher.yml"
      to: "tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py"
      via: "top-level permissions: read-all + per-job issues: write"
      pattern: "^permissions: read-all$"

  threat_model:
    boundaries:
      - boundary: "fork PR → CI runner"
        description: "Untrusted contributor pushes a branch; CI checks it out and runs. Pre-Phase-51 risk: persist-credentials: true writes GITHUB_TOKEN into .git/config, exfiltrable by any later step in the same job."
      - boundary: "third-party action @ tag → fork's runner"
        description: "actions/checkout@v4 resolves to whatever SHA the tag points at TODAY; tj-actions/changed-files CVE-2025-30066 retargeted ~23k repos this way."
      - boundary: "workflow `run:` shell ← github.event payload"
        description: "Attacker-controlled PR titles, comment bodies, branch names can land verbatim in shell context via ${{ github.event.* }} interpolation."
    threats:
      - id: T-51-01
        category: I  # Information disclosure
        component: "actions/checkout step in any ci.yml job (5 sites) + future fork-PR triggers"
        disposition: mitigate
        plan: "Set persist-credentials: false on every actions/checkout (CIHARD-03); no in-tree workflow pushes back to repo so credential persistence is unnecessary. Verified by test_persist_credentials_false_on_every_checkout."
      - id: T-51-02
        category: T  # Tampering (supply chain)
        component: "Every third-party `uses:` reference in ci.yml + issue-claim-watcher.yml (12 sites total)"
        disposition: mitigate
        plan: "Pin every `uses:` to a 40-char commit SHA (CIHARD-04). actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2, actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0, actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7.1.0, raven-actions/actionlint@205b530c5d9fa8f44ae9ed59f341a0db994aa6f8 # v2.1.2. Verified by test_every_uses_line_is_sha_pinned."
      - id: T-51-03
        category: E  # Elevation of privilege
        component: "GITHUB_TOKEN default scope on ci.yml + issue-claim-watcher.yml"
        disposition: mitigate
        plan: "Set top-level permissions: read-all on both workflows (CIHARD-02). On issue-claim-watcher.yml, downgrade from top-level issues: write to top-level read-all + per-job issues: write on detect-claim only (per Pitfall 51-E mitigation; preserves canned-reply behavior). Verified by test_permissions_read_all_on_every_workflow."
      - id: T-51-04
        category: T  # Tampering (expression injection)
        component: "Any run: shell line that interpolates github.event.* directly (none today; invariant preserved)"
        disposition: mitigate
        plan: "Hard ban on ${{ github.event.* }} inside run: shells; current pattern (issue-claim-watcher.yml passes BODY via env: + shell reads $BODY) preserved. Verified by test_no_event_interpolation_in_shells and actionlint's expression rule."
      - id: T-51-05
        category: T  # Tampering (fork-PR + secret leak)
        component: "Any potential pull_request_target trigger across all workflows (none today)"
        disposition: mitigate
        plan: "Hard ban on pull_request_target across v0.6 (CIHARD-01). v0.5 tests use recorded provider responses so no live-secret fork-CI path is needed. Verified by test_no_workflow_uses_pull_request_target."
      - id: T-51-06
        category: D  # Denial of service / disruption (process)
        component: "actions/cache reads inside actions/setup-python under read-all permissions"
        disposition: accept
        plan: "Per Assumption A2 and Pitfall 51-D in RESEARCH.md, actions/cache reads work under workflow-level read-all without per-job actions: write. If CI fails on the first run with a permissions error on cache POST, add per-job actions: write to the affected job. LOW risk; caught immediately on first CI run."
---

<objective>
Apply the five CIHARD hardening edits to `.github/workflows/ci.yml` and `.github/workflows/issue-claim-watcher.yml` so every Plan 01 production assertion turns GREEN. Edits are surgical and preserve every byte-identity contract: job names, trigger discipline (no `pull_request_target`), step ordering for grep-anchored references (lint_no_wallclock, capture-overhead benchmark), and the `(CIHARD-05)` tag-suffix grep convention.

Purpose: Land the supply-chain substrate (SHA pins) and least-privilege baseline (`read-all` + `persist-credentials: false`) that every downstream v0.6 phase depends on. SHA-pinning is two-layer per D-02; this plan ships layer 1 (PR-time enforcement via actionlint + Plan 01 regression tests). Phase 57 ships layer 2 (release_gate.py).

Output: Two modified workflow YAMLs. Plan 01 tests go from RED to GREEN. All pre-Phase-51 pytest tests remain byte-identically green (the verify pipeline runs full `pytest -v` so the actual baseline count is observed at execution time, not hard-coded). `scripts/release_gate.py` and `pyproject.toml` are NOT touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-ci-hardening-substrate/51-CONTEXT.md
@.planning/phases/51-ci-hardening-substrate/51-RESEARCH.md
@.planning/phases/51-ci-hardening-substrate/51-PATTERNS.md
@.planning/phases/51-ci-hardening-substrate/51-VALIDATION.md
@.planning/phases/51-ci-hardening-substrate/51-01-SUMMARY.md
@CLAUDE.md
@.github/workflows/ci.yml
@.github/workflows/issue-claim-watcher.yml

<interfaces>
<!-- Resolved 40-char commit SHAs (from RESEARCH.md §"Resolved Action SHAs", -->
<!-- VERIFIED via gh api repos/{owner}/{repo}/git/matching-refs/tags/... on 2026-05-29). -->
<!-- Use these values verbatim; do NOT substitute a different patch tag. -->

actions/checkout       → 11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
actions/setup-python   → a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
actions/github-script  → f28e40c7f34bde8b3046d885e986cb6290c5673b  # v7.1.0
raven-actions/actionlint → 205b530c5d9fa8f44ae9ed59f341a0db994aa6f8  # v2.1.2
actionlint binary version (pinned via raven-actions/actionlint `with: version:`) → v1.7.12

<!-- Byte-identity job NAMES in ci.yml (grep'd by scripts/release_gate.py per ARCHITECTURE.md). -->
<!-- These five top-level keys MUST be unchanged: -->

lint-and-test:
install-smoke:
install-smoke-no-otel:
install-smoke-with-otel:
install-smoke-plugin:

<!-- Byte-identity step name in ci.yml that is grep'd later: -->
"time.time() lint gate (Pitfall 3)"        # MUST be preserved
"capture-overhead benchmark (METRIC-05 / TEST-12)"  # MUST be preserved
"install-smoke-plugin"                     # job key grep'd by Phase 49 release-gate check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden .github/workflows/ci.yml (SHA pins + top-level permissions + persist-credentials + actionlint step)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the full current file; 224 lines; 5 jobs, 5 actions/checkout sites, 5 actions/setup-python sites)
    - .planning/phases/51-ci-hardening-substrate/51-RESEARCH.md sections "Resolved Action SHAs" (lines 115-126), "actionlint integration shape" (lines 209-238), "Permissions semantics (CIHARD-02)" (lines 240-281), "persist-credentials: false shape (CIHARD-03)" (lines 283-294), "Code Examples" lines 433-466 for the exact YAML diff shapes
    - .planning/phases/51-ci-hardening-substrate/51-PATTERNS.md "Step-name-with-tag-suffix grep convention" (around line 403) for the (CIHARD-05) substring convention
    - .planning/phases/51-ci-hardening-substrate/51-CONTEXT.md §"Specific Ideas" for the (CIHARD-05) literal-substring requirement
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py (the regex shape these edits must satisfy)
    - tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py (the structural shape these edits must satisfy)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml` in place. Apply four classes of edit; the file otherwise stays byte-identical (every job name, every step name except the new addition, every existing key/value preserved verbatim).

    EDIT CLASS 1, Top-level permissions (CIHARD-02). Insert a new line between the current line 7 (`    branches: [main]` closing the pull_request stanza) and the blank-line-then-`jobs:` block. Exact insertion: blank line, then `permissions: read-all`, then blank line, then `jobs:`. The final top-of-file shape is:
        name: CI
        (blank)
        on:
          push:
            branches: [main]
          pull_request:
            branches: [main]
        (blank)
        permissions: read-all
        (blank)
        jobs:
    The `permissions:` key is at column 0 (workflow-level). Per Assumption A2 + Pitfall 51-D: no per-job permissions block is added to any of the five jobs (read-all is sufficient; actions/cache reads under setup-python work without write opt-in).

    EDIT CLASS 2, SHA-pin every uses: line (CIHARD-04). Five actions/checkout sites and five actions/setup-python sites get rewritten. For every `uses: actions/checkout@v4` line, replace with `uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`. For every `uses: actions/setup-python@v5` line, replace with `uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0`. Preserve the existing two-space indentation pattern (`      - name: ...` then `        uses: ...`). The existing `with:` blocks on setup-python stay byte-identical. Trailing `# vN.M.P` comment is REQUIRED (pinact v4.0.0 enforces this; tag-comment convention from RESEARCH.md §"Tag-comment convention").

    EDIT CLASS 3, persist-credentials: false on every actions/checkout (CIHARD-03). For each of the five actions/checkout sites, add a `with:` block immediately after the rewritten `uses:` line. Indentation: the `with:` key is indented to align with the existing `        uses: ...` (8 spaces); the `persist-credentials: false` line is indented two more (10 spaces). Exact shape per `actions/checkout` site:
        - name: Check out repository
          uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
          with:
            persist-credentials: false
    Where the existing job already has a `with:` block after another step (e.g., setup-python), do NOT touch it; CIHARD-03 only requires the persist-credentials key on checkout sites. The install-smoke-plugin job's existing `shell: bash` and `shell: pwsh` markers are on `run:` steps, not on `uses:` checkout steps; leave them untouched.

    EDIT CLASS 4, Insert actionlint step (CIHARD-05) into lint-and-test job. Insert a new step between the existing `Run ruff format check` step (currently lines 38-39) and the existing `time.time() lint gate (Pitfall 3)` step (currently lines 41-42). The new step's `- name:` key MUST contain the literal substring `(CIHARD-05)` per CONTEXT.md §"Specific Ideas". Exact shape (8-space step indentation matching the surrounding steps):
        - name: Run actionlint (CIHARD-05)
          uses: raven-actions/actionlint@205b530c5d9fa8f44ae9ed59f341a0db994aa6f8 # v2.1.2
          with:
            version: v1.7.12
    The `version: v1.7.12` pins the actionlint binary itself per RESEARCH.md §"actionlint integration shape". This step inherits the workflow-level read-all permissions; no per-step permissions block needed.

    INVARIANTS to preserve byte-identically (verified by grep after editing):
    - The five top-level job keys: `  lint-and-test:`, `  install-smoke:`, `  install-smoke-no-otel:`, `  install-smoke-with-otel:`, `  install-smoke-plugin:` (release_gate.py grep targets per ARCHITECTURE.md backward-compat invariants 1-3)
    - The literal step name `time.time() lint gate (Pitfall 3)` (line ordering anchor)
    - The literal step name `capture-overhead benchmark (METRIC-05 / TEST-12)` (METRIC-05 grep target)
    - All `run:` shell content unchanged (no edits to the existing pytest, ruff, install_smoke, install_smoke_plugin invocations)
    - No `pull_request_target` introduced (CIHARD-01 invariant)
    - No `${{ github.event.* }}` introduced in any run: shell (CIHARD-03 invariant)

    HARD RULES:
    - No em-dashes anywhere in the edited file (HR3). Use commas, periods, or hyphens.
    - No new env: blocks unless required (none are required for this edit).
    - Do NOT touch scripts/release_gate.py (Phase 57 owns the release-gate extension).
    - Do NOT touch pyproject.toml (PyYAML rejected per RESEARCH.md §"PyYAML decision"; pip-audit lands Phase 53).
    - Do NOT add a `# actionlint:` disable comment anywhere; the regression test in Plan 01 is defense-in-depth specifically against this.
  </action>
  <verify>
    <automated>grep -c "^  lint-and-test:" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "^  install-smoke:" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "^  install-smoke-no-otel:" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "^  install-smoke-with-otel:" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "^  install-smoke-plugin:" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "^permissions: read-all" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "Run actionlint (CIHARD-05)" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "raven-actions/actionlint@205b530c5d9fa8f44ae9ed59f341a0db994aa6f8" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683" .github/workflows/ci.yml | grep -q "^5$" &amp;&amp; grep -c "actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065" .github/workflows/ci.yml | grep -q "^5$" &amp;&amp; grep -c "persist-credentials: false" .github/workflows/ci.yml | grep -q "^5$" &amp;&amp; grep -c "time.time() lint gate (Pitfall 3)" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; grep -c "capture-overhead benchmark (METRIC-05 / TEST-12)" .github/workflows/ci.yml | grep -q "^1$" &amp;&amp; ! grep -F "pull_request_target" .github/workflows/ci.yml &amp;&amp; ! grep -F "—" .github/workflows/ci.yml &amp;&amp; pytest tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py::test_every_uses_line_is_sha_pinned tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py::test_persist_credentials_false_on_every_checkout -v</automated>
  </verify>
  <done>
    - ci.yml has exactly one `permissions: read-all` line at column 0, positioned between `on:` block and `jobs:`
    - All five top-level job keys preserved byte-identically (grep -c returns 1 for each)
    - All 5 actions/checkout uses are at SHA 11bd71901bbe5b1630ceea73d27597364c9af683 with trailing `# v4.2.2`
    - All 5 actions/setup-python uses are at SHA a26af69be951a213d495a4c3e4e4022e16d87065 with trailing `# v5.6.0`
    - Exactly 5 lines contain `persist-credentials: false` (one per actions/checkout)
    - The new step `Run actionlint (CIHARD-05)` exists exactly once and uses raven-actions/actionlint@205b530c... with `version: v1.7.12`
    - Anchor step names `time.time() lint gate (Pitfall 3)` and `capture-overhead benchmark (METRIC-05 / TEST-12)` preserved
    - No `pull_request_target` anywhere
    - No em-dashes
    - Plan 01 production tests test_every_uses_line_is_sha_pinned and test_persist_credentials_false_on_every_checkout now PASS (verified inline in the &lt;automated&gt; pipeline)
    - No other workflow tests regress (all of Plan 01's non-vacuity tests still pass)
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden .github/workflows/issue-claim-watcher.yml (SHA pins + permissions refactor) + final whole-phase verification</name>
  <files>.github/workflows/issue-claim-watcher.yml</files>
  <read_first>
    - .github/workflows/issue-claim-watcher.yml (the full current file; 85 lines; 2 actions/github-script@v7 sites, current top-level permissions: issues: write)
    - .planning/phases/51-ci-hardening-substrate/51-RESEARCH.md section "CIHARD-02 + CIHARD-04, issue-claim-watcher.yml after Phase 51" (lines 485-519) for the verified post-edit shape
    - .planning/phases/51-ci-hardening-substrate/51-RESEARCH.md "Pitfall 51-E" (lines 392-398) for the per-job permissions placement (issues: write must be on detect-claim, not a wrapper; preserves canned-reply behavior)
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py
    - tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py
  </read_first>
  <action>
    Edit `.github/workflows/issue-claim-watcher.yml` in place. Two edits; rest of file byte-identical (comment block, claim-language regex, dedupe script body, canned-reply script body all preserved).

    EDIT 1, Permissions refactor (CIHARD-02). The current top-level block on lines 17-18 is:
        permissions:
          issues: write
    Replace those two lines with a single line: `permissions: read-all`. Then add a per-job `permissions:` block inside the `detect-claim:` job, indented to align under the job key (4 spaces for `    permissions:`, 6 spaces for `      issues: write`). Per RESEARCH.md verified shape (lines 485-519) and Pitfall 51-E: the per-job permissions block lands as the FIRST key under `detect-claim:`, BEFORE the existing `if:` and `runs-on:` keys. The exact post-edit shape for the detect-claim job header is:
        jobs:
          detect-claim:
            permissions:
              issues: write
            if: >-
              github.event.issue.state == 'open'
              && github.event.comment.user.type != 'Bot'
              && github.event.comment.user.login != github.event.repository.owner.login
            runs-on: ubuntu-latest
            steps:
    The `if:` condition and its three predicate lines stay byte-identical (including the existing `${{ github.event.* }}` interpolation, that interpolation is inside an `if:` expression, NOT inside a `run:` shell, so CIHARD-03 does NOT prohibit it; the regression test in Plan 01 only flags interpolation inside `run:` blocks per the state machine).

    EDIT 2, SHA-pin both actions/github-script@v7 sites (CIHARD-04). Two sites:
    - Step `Skip if already replied on this thread` (currently line 44): rewrite `        uses: actions/github-script@v7` to `        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7.1.0`
    - Step `Post canned reply` (currently line 64): same rewrite, identical SHA and trailing comment.
    Both `with: script: |` blocks underneath are preserved byte-identically (the canned-reply script body, the dedupe script body, the marker constant, the body array literal, every line stays the same).

    NO actions/checkout in this file (issue_comment-triggered workflows don't check out a repo), so persist-credentials does NOT apply here.

    INVARIANTS to preserve:
    - File name: issue-claim-watcher.yml (D-06 has this file deleted in Phase 59; we are NOT renaming or deleting it now)
    - Trigger: `issue_comment: types: [created]` byte-identical (CIHARD-01 invariant: no drift to pull_request_target)
    - Canned-reply marker constant `<!-- horus-os-claim-watcher -->` byte-identical
    - The claim-language detection regex on the `run:` step byte-identical
    - The github-script invocation surface (`github.rest.issues.createComment`, `github.paginate`) byte-identical, verifies that per-job permissions: issues: write is sufficient
    - The `env: BODY: ${{ github.event.comment.body }}` pattern preserved; the run: shell reads `$BODY` (not `${{ github.event... }}`). This is the canonical interpolation-via-env defense.

    HARD RULES:
    - No em-dashes (HR3)
    - Do NOT modify the comment block at the top of the file
    - Do NOT modify the claim-language detection regex
    - Do NOT modify scripts/release_gate.py or pyproject.toml

    FINAL PHASE VERIFICATION (after both edits land): run the complete TEST-23 suite plus the full pytest suite to confirm Phase 51's substrate is correct end-to-end. Expectations:
    - All Plan 01 production tests now GREEN (test_no_workflow_uses_pull_request_target, test_every_uses_line_is_sha_pinned, test_permissions_read_all_on_every_workflow, test_persist_credentials_false_on_every_checkout, test_no_event_interpolation_in_shells)
    - All Plan 01 non-vacuity tests still PASS
    - The whole pytest suite (all pre-Phase-51 tests + the new TEST-23 tests) PASSES with zero regressions; baseline count observed at execution time via `pytest -v`, not hard-coded in this plan. Executor: do NOT reintroduce a literal test count in the SUMMARY; report the observed pre-Phase-51 baseline (captured via `git stash && pytest --collect-only -q | tail -1 && git stash pop` before the YAML edits land) as a single line in the SUMMARY ("pre-Phase-51 baseline: N tests; post-Phase-51: N + 10 tests") so the count is a recorded fact, not a planning literal that rots
    - `ruff check .` and `ruff format --check .` clean on the entire repo
    - `scripts/release_gate.py` byte-identical to pre-Phase-51 state (sha256 unchanged)
    - `pyproject.toml` byte-identical to pre-Phase-51 state (sha256 unchanged)
  </action>
  <verify>
    <automated>grep -c "^permissions: read-all$" .github/workflows/issue-claim-watcher.yml | grep -q "^1$" &amp;&amp; grep -c "^    permissions:$" .github/workflows/issue-claim-watcher.yml | grep -q "^1$" &amp;&amp; grep -c "^      issues: write$" .github/workflows/issue-claim-watcher.yml | grep -q "^1$" &amp;&amp; grep -c "actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b" .github/workflows/issue-claim-watcher.yml | grep -q "^2$" &amp;&amp; ! grep -E "^permissions:$" .github/workflows/issue-claim-watcher.yml &amp;&amp; grep -F "issue_comment:" .github/workflows/issue-claim-watcher.yml &amp;&amp; ! grep -F "pull_request_target" .github/workflows/issue-claim-watcher.yml &amp;&amp; ! grep -F "—" .github/workflows/issue-claim-watcher.yml &amp;&amp; pytest tests/test_contribution_gate_pitfalls/ -v &amp;&amp; pytest -v &amp;&amp; ruff check . &amp;&amp; ruff format --check . &amp;&amp; git diff --quiet scripts/release_gate.py pyproject.toml</automated>
  </verify>
  <done>
    - issue-claim-watcher.yml has exactly one `permissions: read-all` line at column 0
    - issue-claim-watcher.yml has exactly one per-job `    permissions:` block (under detect-claim) with `      issues: write` immediately below
    - The old `permissions:\n  issues: write` two-line block at the top is gone
    - Both actions/github-script@v7 sites are now actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b with trailing `# v7.1.0`
    - Trigger byte-identical: `issue_comment: types: [created]` still present
    - No `pull_request_target` anywhere
    - No em-dashes anywhere
    - All 9+ tests in tests/test_contribution_gate_pitfalls/ PASS (CIHARD-01..05 + TEST-23 substrate complete)
    - Full pytest suite runs green (no regression in any pre-Phase-51 test; baseline observed at execution time)
    - ruff check + ruff format --check clean on the whole repo
    - `git diff --quiet scripts/release_gate.py pyproject.toml` exits 0 (both files byte-identical to pre-Phase-51 state)
    - All six Phase 51 requirements (CIHARD-01..05, TEST-23) are now demonstrably enforced
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `pytest tests/test_contribution_gate_pitfalls/ -v` → all tests PASS (production + non-vacuity)
- `pytest -v` → full suite PASS, zero regressions in any pre-Phase-51 test (baseline count is whatever `pytest -v` reports against the pre-Phase-51 HEAD; we enforce delta-zero against that observed baseline, not against a hard-coded literal)
- `ruff check . && ruff format --check .` → both exit 0
- `grep -c "^  lint-and-test:" .github/workflows/ci.yml` → 1 (byte-identity preserved)
- `grep -c "^  install-smoke-plugin:" .github/workflows/ci.yml` → 1 (release_gate.py grep target preserved)
- `grep -c "Run actionlint (CIHARD-05)" .github/workflows/ci.yml` → 1 (TEST-23 CIHARD-05 grep target)
- `grep -c "actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683" .github/workflows/ci.yml` → 5
- `grep -c "actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065" .github/workflows/ci.yml` → 5
- `grep -c "persist-credentials: false" .github/workflows/ci.yml` → 5
- `grep -c "actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b" .github/workflows/issue-claim-watcher.yml` → 2
- `! grep -RF "pull_request_target" .github/workflows/` → no matches
- `! grep -RF "—" .github/workflows/` → no em-dashes
- `git diff --quiet scripts/release_gate.py pyproject.toml` → exits 0 (both untouched)

MANUAL verification (deferred per VALIDATION.md §"Manual-Only Verifications"):
- On first CI run of this branch, the new `Run actionlint (CIHARD-05)` step in `lint-and-test` actually executes and exits 0 (the step EXISTS in YAML is verified above; that it RUNS in CI requires a PR and the GitHub Actions runner)
- Optional local maintainer check: `pinact run --update` produces zero diff (proves the pins are current at lock time)
</verification>

<success_criteria>
- ci.yml job NAMES byte-identical pre/post Phase 51 (5 jobs; release_gate.py grep targets preserved)
- ci.yml has top-level `permissions: read-all` (CIHARD-02)
- 5 actions/checkout sites SHA-pinned to 11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2 with persist-credentials: false below each (CIHARD-03 + CIHARD-04)
- 5 actions/setup-python sites SHA-pinned to a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0 (CIHARD-04)
- 1 raven-actions/actionlint@205b530c5d9fa8f44ae9ed59f341a0db994aa6f8 # v2.1.2 step named exactly `Run actionlint (CIHARD-05)`, positioned after ruff format check and before time.time() lint gate, with `version: v1.7.12` (CIHARD-05 + CIHARD-04)
- issue-claim-watcher.yml has top-level `permissions: read-all` + per-job `permissions: issues: write` on detect-claim (CIHARD-02 + Pitfall 51-E mitigation)
- 2 actions/github-script sites in issue-claim-watcher.yml SHA-pinned to f28e40c7f34bde8b3046d885e986cb6290c5673b # v7.1.0 (CIHARD-04)
- No workflow declares pull_request_target (CIHARD-01)
- No run: shell contains ${{ github.event.* }} interpolation (CIHARD-03 second clause; current pattern of env:-then-$VAR preserved on issue-claim-watcher.yml)
- All Plan 01 tests (production + non-vacuity) PASS (CIHARD-01..05 + TEST-23 substrate complete)
- Full pytest suite PASS (zero regressions)
- ruff check + ruff format --check clean (HR4)
- scripts/release_gate.py + pyproject.toml UNTOUCHED (Phase 57 + Phase 53 own those)
- No em-dashes in committed prose (HR3)
</success_criteria>

<output>
After completion, create `.planning/phases/51-ci-hardening-substrate/51-02-SUMMARY.md` per template. Include:
- Final TEST-23 status (all production + non-vacuity tests GREEN)
- Byte-identity verification (job name grep counts, release_gate.py + pyproject.toml sha256 unchanged)
- Backward-compat invariants 1-5 (from `<planning_context>`) all preserved with evidence
- Pinned SHAs used (table mirroring RESEARCH.md §"Resolved Action SHAs")
- Plan 01 RED-to-GREEN transition (which assertions flipped)
- Hand-off note for Phase 57 (release_gate.py extension picks up the `actions-pinned-by-sha` layer-2 enforcement next)
- Hand-off note for Phase 56 (RUNBOOK documents `pinact run --update` quarterly cadence)
</output>
