---
phase: 04
plan: "00"
type: execute
wave: 1
depends_on: ["02-00", "03-00"]
files_modified:
  - src/horus_os/tools/__init__.py
  - src/horus_os/tools/registry.py
  - src/horus_os/tools/builtin.py
  - src/horus_os/tools/loop.py
  - src/horus_os/types.py
  - src/horus_os/__init__.py
  - tests/test_tool_registry.py
  - tests/test_tool_builtin.py
  - tests/test_tool_loop.py
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
tags:
  - tool-registry
  - read-file
  - execute-loop

must_haves:
  truths:
    - "`ToolRegistry` accepts `Tool` objects, stores them keyed by name, rejects duplicates by default"
    - "`ToolRegistry.register(tool)` adds the tool. `replace=True` allows overwrite."
    - "`ToolRegistry.get(name)` returns the Tool or None. `ToolRegistry.list()` returns all registered tools."
    - "`ToolRegistry.invoke(name, input)` calls the tool's handler with the input dict and returns the handler's return value. Raises KeyError if the tool is not registered. Raises RuntimeError if the tool has no handler."
    - "`execute_tool_uses(registry, result, *, on_log=None) -> list[ToolResult]` walks `result.tool_uses`, invokes each handler, and returns a `ToolResult` for each. Captures handler exceptions into the ToolResult so the caller never has to wrap each invocation."
    - "`ToolResult(tool_use_id, name, output, error, latency_ms)` mirrors the model's tool_use plus the actual handler outcome"
    - "`read_file_tool(base_dir=None)` factory returns a `Tool` named `read_file` with description and JSON schema. Handler reads the file at the given path. When `base_dir` is set, paths resolve relative to base_dir AND any path that escapes base_dir raises PermissionError."
    - "Public API includes `ToolRegistry`, `ToolResult`, `execute_tool_uses`, `read_file_tool` re-exported from `horus_os`"
    - "Tests cover registry CRUD, duplicate rejection, missing-tool error, handler-less Tool error, execute loop happy path, execute loop with mixed success and exception, read_file happy path, read_file path traversal blocked, read_file relative to base_dir"
  artifacts:
    - path: "src/horus_os/tools/registry.py"
      provides: "`ToolRegistry` class"
      contains: "class ToolRegistry"
      min_lines: 40
    - path: "src/horus_os/tools/builtin.py"
      provides: "`read_file_tool(base_dir=None)` factory"
      contains: "def read_file_tool"
      min_lines: 30
    - path: "src/horus_os/tools/loop.py"
      provides: "`execute_tool_uses` helper"
      contains: "def execute_tool_uses"
      min_lines: 20

tasks:
  - id: "1"
    title: "Add `ToolResult` dataclass to `types.py`"
    files:
      - src/horus_os/types.py

  - id: "2"
    title: "ToolRegistry class in `src/horus_os/tools/registry.py`"
    files:
      - src/horus_os/tools/__init__.py
      - src/horus_os/tools/registry.py

  - id: "3"
    title: "Built-in `read_file` tool in `src/horus_os/tools/builtin.py`"
    files:
      - src/horus_os/tools/builtin.py
    notes: "Factory takes optional `base_dir` for sandboxing. When set, all paths must resolve to a child of base_dir. The handler returns the file's text content."

  - id: "4"
    title: "Execute-loop helper `execute_tool_uses` in `src/horus_os/tools/loop.py`"
    files:
      - src/horus_os/tools/loop.py
    notes: "Walks AgentResult.tool_uses, invokes each via the registry, captures handler exceptions into ToolResult.error. Optional `on_log` callback receives each ToolResult so callers can persist them. Does NOT do the multi-turn model loop; that lands when CLI and dashboard wire the full conversation flow."

  - id: "5"
    title: "Re-export new public API"
    files:
      - src/horus_os/__init__.py

  - id: "6"
    title: "Tests"
    files:
      - tests/test_tool_registry.py
      - tests/test_tool_builtin.py
      - tests/test_tool_loop.py

  - id: "7"
    title: "Verify lint, format, tests, CLI smoke"
    files: []

  - id: "8"
    title: "Commit + SUMMARY + ROADMAP/STATE advance + push"
    files:
      - .planning/phases/04-tool-registry/04-00-SUMMARY.md
      - .planning/ROADMAP.md
      - .planning/STATE.md
      - ROADMAP.md

threat_model:
  - threat: "read_file used to exfiltrate sensitive files (e.g. /etc/passwd or ~/.ssh/id_rsa) when an agent runs untrusted prompts"
    mitigation: "`base_dir` parameter sandboxes paths. CLI and dashboard wiring (Phase 07, 08) will default to a user-chosen sandbox directory. The unsandboxed mode is opt-in for callers who explicitly construct `read_file_tool()` with no base_dir."
  - threat: "Handler exception crashes the caller mid-loop"
    mitigation: "`execute_tool_uses` catches BaseException, wraps it in `ToolResult.error`, continues to the next tool_use. Caller sees a structured outcome for every tool the model requested."

success_criteria:
  - "ruff check exits 0"
  - "ruff format --check exits 0"
  - "pytest -v exits 0 with at least 12 new tests"
  - "`from horus_os import ToolRegistry, ToolResult, execute_tool_uses, read_file_tool` succeeds"
  - "`horus-os --version` still prints `horus-os 0.0.1`"
  - "04-00-SUMMARY.md exists and is committed"
  - "ROADMAP and STATE mark Phase 04 complete"
  - "Branch pushed to origin/main cleanly"
