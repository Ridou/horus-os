---
phase: 48-reference-plugin-horus-os-example-plugin
plan: 01
type: execute
wave: 1
depends_on: [47]
files_modified:
  - examples/horus-os-example-plugin/pyproject.toml
  - examples/horus-os-example-plugin/horus-plugin.toml
  - examples/horus-os-example-plugin/README.md
  - examples/horus-os-example-plugin/src/horus_os_example_plugin/__init__.py
  - examples/horus-os-example-plugin/src/horus_os_example_plugin/tools.py
  - examples/horus-os-example-plugin/src/horus_os_example_plugin/adapter.py
  - examples/horus-os-example-plugin/tests/__init__.py
  - examples/horus-os-example-plugin/tests/test_example_plugin.py
  - pyproject.toml
  - tests/plugins/test_reference_plugin_public_api_only.py
  - tests/plugins/test_reference_plugin_install_local.py
  - CHANGELOG.md
autonomous: true
requirements:
  - REFERENCE-01
  - TEST-21

must_haves:
  truths:
    - "`examples/horus-os-example-plugin/` exists as a self-contained installable Python package: it has its own `pyproject.toml` (setuptools build backend), its own `horus-plugin.toml` (v1 manifest), its own `README.md` (one-page authoring-starter), and a populated `src/horus_os_example_plugin/` package layout. `pip install -e ./examples/horus-os-example-plugin` from the host repo root succeeds in a clean venv that already has `horus-os` installed in editable mode. (REFERENCE-01)"
    - "The reference plugin demonstrates all four canonical scenarios in ONE package: (a) `tools.echo_text_tool(path: str) -> str` decorated with `@require_capability(Capability.FILESYSTEM_READ)` reads file content via `ctx.filesystem.read(path)`; (b) `tools.lookup_secret_tool(key: str) -> str | None` decorated with `@require_capability(Capability.SECRETS_READ)` reads via `ctx.secrets.read(key)` and returns None on missing key (NOT an exception, matching `DESCRIPTIONS[SECRETS_READ]` semantics); (c) `adapter.ExampleAdapter` implements both `start(ctx: AdapterContext) -> Awaitable[None]` and `stop() -> Awaitable[None]`; `start` is a non-blocking no-op that schedules with `asyncio.create_task` and yields control within < 1 second so the Phase 43 `asyncio.wait_for(start, timeout=2.0)` bound always passes; `stop` cancels the task and awaits its completion (also < 1 second); (d) the SINGLE `horus-plugin.toml` registers BOTH `[[contributions.tools]]` entries (two tools) AND a `[[contributions.adapters]]` entry (the lifecycle adapter), proving one package can contribute multiple surfaces. (REFERENCE-01)"
    - "`examples/horus-os-example-plugin/horus-plugin.toml` validates against the runtime `MANIFEST_V1_SCHEMA` (pydantic v2, from `src/horus_os/plugins/manifest.py`) with zero errors. Concretely the file carries: `manifest_version = 1`, `name = \"horus-os-example-plugin\"`, `version = \"0.1.0\"`, `description = \"Reference plugin demonstrating the v0.5 contract.\"`, `author = \"horus-os contributors\"`, `license = \"Apache-2.0\"`, `homepage = \"https://github.com/Ridou/horus-os/tree/main/examples/horus-os-example-plugin\"`, `issue_tracker = \"https://github.com/Ridou/horus-os/issues\"`, `horus_os_compat = \">=0.5,<0.6\"`, `capabilities = [\"filesystem.read\", \"secrets.read\"]`, plus `[[contributions.tools]]` arrays referencing `horus_os_example_plugin.tools:echo_text_tool` and `horus_os_example_plugin.tools:lookup_secret_tool`, and a `[[contributions.adapters]]` array referencing `horus_os_example_plugin.adapter:ExampleAdapter`. The two declared capabilities cover scenarios (a) and (b); the adapter `ExampleAdapter` does NOT request any capability (lifecycle hooks do not need a grant). (REFERENCE-01, MANIFEST-04)"
    - "`examples/horus-os-example-plugin/pyproject.toml` is a self-contained PEP 621 metadata file using the `setuptools.build_meta` backend (mirrors host `pyproject.toml:1-3`). `dependencies = [\"horus-os>=0.5,<0.6\"]`. `[project.entry-points.\"horus_os.plugins\"]` declares the plugin: `horus-os-example-plugin = \"horus_os_example_plugin\"` (entry-point name MATCHES the manifest's `name` field — Phase 42's discovery joins on this key). `[tool.setuptools.packages.find]` points at `src/`. The package is installable via `pip install -e ./examples/horus-os-example-plugin` from the repo root and via `pip install ./examples/horus-os-example-plugin` (non-editable). After install, `importlib.metadata.entry_points(group=\"horus_os.plugins\")` returns at least one EntryPoint whose `.name == \"horus-os-example-plugin\"`."
    - "The reference plugin's source tree contains EXACTLY ONE `from horus_os` import path: `from horus_os.plugins.api import ...`. Every other name the plugin needs (`Adapter`, `AdapterContext`, `Capability`, `LifecycleAdapter`, `PluginContext`, `PluginSpec`, `Tool`, `require_capability`) is re-exported from `horus_os.plugins.api` (verified by `src/horus_os/plugins/api.py:__all__`). Concretely: `tools.py` does `from horus_os.plugins.api import Capability, require_capability, PluginContext` (or a subset); `adapter.py` does `from horus_os.plugins.api import Adapter, AdapterContext, LifecycleAdapter` (or a subset). NO `from horus_os.plugins.<other_module>` imports, NO `from horus_os.adapters ...`, NO `from horus_os._<private> ...`, NO `import horus_os` bare imports. (TEST-21, Pitfall 8)"
    - "**TEST-21 enforcement layer 1 (ruff banned-api):** The host repo's `pyproject.toml` extends `[tool.ruff.lint.flake8-tidy-imports.banned-api]` with at least one entry that bans every non-public `horus_os.*` submodule import on the reference plugin's path, scoped via `[tool.ruff.lint.per-file-ignores]`-equivalent targeting (concretely: a new `[tool.ruff.lint.per-file-ignores]` entry for `examples/horus-os-example-plugin/src/**/*.py` does NOT silence TID rules, AND the banned-api entries explicitly list `horus_os.adapters`, `horus_os.types`, `horus_os.plugins.spec`, `horus_os.plugins.manifest`, `horus_os.plugins.permissions`, `horus_os.plugins.capability_catalog`, `horus_os.plugins.discovery`, `horus_os.plugins.loader`, `horus_os.plugins.registry`, `horus_os.plugins.installer`, `horus_os.plugins` (bare), and any leading-underscore `horus_os._*` module by glob equivalent). The banned entry message names Pitfall 8 and points at `docs/PLUGINS.md` 'Public API surface' for plugin authors. Running `ruff check examples/horus-os-example-plugin/` against the SHIPPED reference plugin source produces zero violations (only `from horus_os.plugins.api import ...` is used). (TEST-21)"
    - "**TEST-21 enforcement layer 2 (source-tree backstop):** `tests/plugins/test_reference_plugin_public_api_only.py` exists, runs in the default `pytest` invocation (no markers), walks every `*.py` file under `examples/horus-os-example-plugin/src/`, and for each file scans every line for the regex `^\\s*(from horus_os(?:\\.[a-zA-Z_][a-zA-Z0-9_]*)*\\s+import|import horus_os(?:\\.[a-zA-Z_][a-zA-Z0-9_]*)*)`. Any matched line that is NOT `from horus_os.plugins.api import <names>` fails the test with a precise file:line:offender message. This is the byte-level backstop in case `ruff` config drift silences the layer-1 rule. Same two-layer guard pattern as `tests/plugins/test_pkg_resources_banned.py` (Phase 42 banned-api). The test also synthesizes a bad-import fixture path (in-test `tmp_path` mirror of the reference plugin with an illegal `from horus_os.adapters import Adapter` line) and asserts the same function returns a failure verdict for it — proving the rule actually fires. (TEST-21)"
    - "`tests/plugins/test_reference_plugin_install_local.py` exists, is marked `@pytest.mark.installer_e2e` (the Phase 46 marker, gated by `--run-installer-e2e`), and consumes the session-scoped `clean_venv` fixture from `tests/conftest.py:104-159`. The test (a) runs `pip install -e ./examples/horus-os-example-plugin` against the clean venv's Python, (b) boots horus-os via the FastAPI `TestClient` shape (mirrors `tests/plugins/test_lifespan_integration.py`'s pattern), (c) calls `GET /api/plugins` and asserts the response contains `{ \"name\": \"horus-os-example-plugin\", \"status\": \"pending\", \"version\": \"0.1.0\" }` (status pending because no grant yet), (d) calls `PermissionService.grant(...)` directly for both `Capability.FILESYSTEM_READ` and `Capability.SECRETS_READ` against the plugin's manifest hash, (e) restarts the registry (or reloads), and asserts `GET /api/plugins` now shows `status=\"loaded\"` with both tools (`echo_text_tool`, `lookup_secret_tool`) and the one adapter (`ExampleAdapter`) registered. The full E2E install-smoke gate across the 3-OS matrix lands in Phase 49 (TEST-20); this Phase 48 test is the single-host smoke that proves the package shape is correct before the matrix runs. (REFERENCE-01)"
    - "`examples/horus-os-example-plugin/tests/test_example_plugin.py` exists and uses the in-process tier-1 fixture pattern (`make_synthetic_plugin` from `tests/plugins/conftest.py`, available because the reference plugin's test runs under the host repo's pytest invocation, NOT as an isolated package test suite — the file is collected because the host `pyproject.toml`'s `[tool.pytest.ini_options].testpaths` is `[\"tests\"]` AND the host adds `examples/horus-os-example-plugin/tests` via a glob update). The plugin's own tests cover: (a) `echo_text_tool` raises `PermissionDenied` when invoked through a `PluginContext` whose `CapabilityGuard` lacks `FILESYSTEM_READ`; (b) `echo_text_tool` returns the file's content string when the guard grants `FILESYSTEM_READ`; (c) `lookup_secret_tool` returns `None` on missing env var and the env var's value when set; (d) `ExampleAdapter.start(ctx)` completes within 1 second wall-clock and `ExampleAdapter.stop()` completes within 1 second wall-clock; (e) the manifest validates against `validate_manifest()` from `src/horus_os/plugins/manifest.py` with zero errors and yields a `PluginSpec` whose `name == \"horus-os-example-plugin\"` and whose contributions list both tools and one adapter."
    - "`examples/horus-os-example-plugin/README.md` is a one-page authoring starter (≤200 lines) covering, in order: 'What this plugin demonstrates' (the four scenarios), 'How to install' (`pip install -e ./examples/horus-os-example-plugin` + `horus-os plugins grant horus-os-example-plugin --all`), 'Anatomy' (one paragraph per source file: `tools.py`, `adapter.py`, `horus-plugin.toml`), 'Using this as a starting point' (rename instructions: package name, manifest name, entry point, capability set), and 'See also' (links to `docs/PLUGINS.md`, `docs/PLUGIN-SECURITY.md`). The README is INTENDED to be copied by third-party authors as a starting template; it explicitly says 'You may copy this directory wholesale and rename three identifiers (`horus-os-example-plugin`, `horus_os_example_plugin`, `ExampleAdapter`) to start a new plugin.'"
    - "`CHANGELOG.md` `[0.5.0]` section gains a new **Added** bullet: 'Reference plugin (`examples/horus-os-example-plugin/`) demonstrating the v0.5 plugin contract across four scenarios (simple capability tool, secret-reading tool, lifecycle adapter, combined tool + adapter package); this directory is the canonical starting template for third-party plugin authors.' The bullet appears in the **Added** subsection ahead of the existing reference-plugin-forward-reference bullet from Phase 47 (which is now replaced by this concrete bullet). No other CHANGELOG sections (Changed, Migration) are modified."
    - "All Phase 47 forward references in `docs/PLUGINS.md` (lines 109-118: 'Phase 48 will ship', 'Phase 48 lands the reference plugin under', 'The Phase 48 ruff custom rule will also pin') remain accurate after Phase 48: the file path `examples/horus-os-example-plugin/` exists, the four scenarios are implemented as described, and the ruff banned-api wiring is in place. NO edit to `docs/PLUGINS.md` is required from Phase 48; the Phase 47 docs were authored as forward-reference contract and Phase 48 fulfills it. (Phase 47 docs-drift test `tests/docs/test_plugins_md_anatomy.py` continues to pass byte-identical.)"

  artifacts:
    - path: examples/horus-os-example-plugin/pyproject.toml
      provides: "PEP 621 + setuptools metadata for the reference plugin. Declares `[project.entry-points.\"horus_os.plugins\"]` mapping `horus-os-example-plugin` to the `horus_os_example_plugin` package, depends on `horus-os>=0.5,<0.6`, points setuptools at `src/`."
      min_lines: 25
      contains: "[project.entry-points.\"horus_os.plugins\"]"
    - path: examples/horus-os-example-plugin/horus-plugin.toml
      provides: "v1 manifest declaring two tools + one adapter + two capabilities. Byte-validates against runtime MANIFEST_V1_SCHEMA."
      min_lines: 25
      contains: "manifest_version = 1"
    - path: examples/horus-os-example-plugin/README.md
      provides: "One-page authoring starter: install, anatomy, rename-to-start, see-also links."
      min_lines: 60
    - path: examples/horus-os-example-plugin/src/horus_os_example_plugin/__init__.py
      provides: "Package marker. Tiny module docstring naming the four scenarios. Empty `__all__` (public surface flows through entry points, not bare imports)."
      min_lines: 5
    - path: examples/horus-os-example-plugin/src/horus_os_example_plugin/tools.py
      provides: "Scenarios (a) + (b): `echo_text_tool` (requires `filesystem.read`, reads via `ctx.filesystem.read`) and `lookup_secret_tool` (requires `secrets.read`, reads via `ctx.secrets.read`)."
      min_lines: 35
      contains: "from horus_os.plugins.api import"
    - path: examples/horus-os-example-plugin/src/horus_os_example_plugin/adapter.py
      provides: "Scenario (c): `ExampleAdapter` implementing `start(ctx)` + `stop()` with non-blocking < 1s semantics respecting Phase 43's `asyncio.wait_for(timeout=2.0)` bound."
      min_lines: 40
      contains: "from horus_os.plugins.api import"
    - path: examples/horus-os-example-plugin/tests/test_example_plugin.py
      provides: "In-process tests for the four scenarios using tier-1 helpers from `tests/plugins/conftest.py`."
      min_lines: 60
    - path: pyproject.toml
      provides: "Host repo ruff banned-api extension scoped to the reference plugin path (TEST-21 layer 1). Pre-existing pkg_resources ban (line 116) preserved byte-identical."
      contains: "horus_os.adapters"
    - path: tests/plugins/test_reference_plugin_public_api_only.py
      provides: "TEST-21 layer-2 source-tree backstop: walks reference plugin source, asserts only `from horus_os.plugins.api` imports exist, plus a synthetic bad-import fixture proving the rule fires."
      min_lines: 55
    - path: tests/plugins/test_reference_plugin_install_local.py
      provides: "Installer-e2e smoke (gated by `--run-installer-e2e`): pip-installs the reference plugin into the `clean_venv` fixture, boots horus-os, asserts pending → loaded transition via permission grant."
      min_lines: 50
      contains: "@pytest.mark.installer_e2e"
    - path: CHANGELOG.md
      provides: "`[0.5.0]` **Added** bullet for the reference plugin replaces the Phase 47 forward-reference bullet."
      contains: "horus-os-example-plugin"

  key_links:
    - from: examples/horus-os-example-plugin/horus-plugin.toml
      to: examples/horus-os-example-plugin/src/horus_os_example_plugin/tools.py
      via: "[[contributions.tools]] entry_point dotted paths"
      pattern: "horus_os_example_plugin\\.tools:(echo_text_tool|lookup_secret_tool)"
    - from: examples/horus-os-example-plugin/horus-plugin.toml
      to: examples/horus-os-example-plugin/src/horus_os_example_plugin/adapter.py
      via: "[[contributions.adapters]] entry_point dotted path"
      pattern: "horus_os_example_plugin\\.adapter:ExampleAdapter"
    - from: examples/horus-os-example-plugin/pyproject.toml
      to: examples/horus-os-example-plugin/src/horus_os_example_plugin/
      via: "[project.entry-points.\"horus_os.plugins\"] mapping name -> package"
      pattern: "horus-os-example-plugin\\s*=\\s*\"horus_os_example_plugin\""
    - from: examples/horus-os-example-plugin/src/horus_os_example_plugin/tools.py
      to: src/horus_os/plugins/api.py
      via: "single public import surface: `from horus_os.plugins.api import ...`"
      pattern: "from horus_os\\.plugins\\.api import"
    - from: examples/horus-os-example-plugin/src/horus_os_example_plugin/adapter.py
      to: src/horus_os/plugins/api.py
      via: "single public import surface"
      pattern: "from horus_os\\.plugins\\.api import"
    - from: tests/plugins/test_reference_plugin_public_api_only.py
      to: examples/horus-os-example-plugin/src/
      via: "source-tree scan: regex over every *.py for non-public horus_os imports"
      pattern: "from horus_os(?!\\.plugins\\.api)"
    - from: pyproject.toml
      to: examples/horus-os-example-plugin/src/
      via: "ruff banned-api entries scoped to reference plugin path"
      pattern: "horus_os\\.(adapters|types|plugins\\.[a-z_]+)"
    - from: tests/plugins/test_reference_plugin_install_local.py
      to: tests/conftest.py
      via: "consumes session-scoped clean_venv fixture"
      pattern: "clean_venv"
---

<objective>
Ship `examples/horus-os-example-plugin/` — a self-contained installable Python
package that is the canonical reference for third-party plugin authors and the
contract Phase 49's 3-OS install-smoke gate runs against. Demonstrates four
scenarios (capability-gated filesystem tool, capability-gated secret tool,
bounded-lifecycle adapter, combined tool+adapter package) entirely against the
single public API surface `horus_os.plugins.api`. Locks the public-surface
contract via a two-layer guard: ruff `flake8-tidy-imports.banned-api` (layer 1)
and a source-tree regex backstop in pytest (layer 2). Closes REFERENCE-01 and
TEST-21.

Purpose: v0.5 is the moment we draw the public-API line for plugin authors.
Pitfall 8 (`.planning/research/PITFALLS.md:225-255`) names two failure shapes —
"too simple to teach" and "too coupled to internals" — and Phase 41 already
defined `horus_os.plugins.api` as the SINGLE public surface (`api.py:227-236`
`__all__` tuple). Phase 47 wrote the docs as a forward-reference contract
(`docs/PLUGINS.md:109-122`). Phase 48 turns that contract into shipped code
plus the byte-level enforcement that keeps the contract from drifting once
third-party plugins start importing.

Output: One installable example package + two enforcement tests (one always-on
backstop + one gated install-smoke) + ruff config extension + CHANGELOG entry.
The example is the contract that Phase 49's 3-OS gate (TEST-20) and the Phase
49 release-gate manifest-validates check both consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md

# Phase 47 docs already describe the four scenarios + the ruff rule as a
# forward-reference contract. Phase 48 fulfills that contract.
@.planning/phases/47-documentation-refresh-docs-trio/47-01-SUMMARY.md
@docs/PLUGINS.md

# The single public API surface. Reference plugin imports MUST come from here.
@src/horus_os/plugins/api.py

# Closed capability enum. Manifest's [capabilities] array values must be members.
@src/horus_os/plugins/capability_catalog.py

# Manifest reader + pydantic v2 schema. Reference plugin's horus-plugin.toml
# validates against MANIFEST_V1_SCHEMA at install time (Phase 41).
@src/horus_os/plugins/manifest.py

# Full manifest fixture — shape to mirror in the reference plugin's horus-plugin.toml.
@tests/fixtures/manifests/manifest_v1_full.toml

# Adapter Protocol + LifecycleAdapter Protocol (re-exported through plugins.api).
@src/horus_os/adapters/base.py

# Host pyproject.toml: [project.entry-points."horus_os.adapters"] is the
# precedent shape for the reference plugin's [project.entry-points."horus_os.plugins"].
# The pre-existing [tool.ruff.lint.flake8-tidy-imports.banned-api] section
# (line 115) extends in Phase 48 with reference-plugin-scoped bans.
@pyproject.toml

# Phase 46 tier-3 clean_venv fixture — consumed by the installer-e2e smoke test.
@tests/conftest.py

# Phase 42 two-layer banned pattern (ruff banned-api + source-tree grep) is
# the template for TEST-21's two-layer guard.
@tests/plugins/test_pkg_resources_banned.py

# Phase 43 lifespan-integration test pattern. The installer-e2e smoke test
# uses the same TestClient + lifespan-boot shape.
@tests/plugins/test_lifespan_integration.py

# CHANGELOG.md: Phase 47 added a forward-reference bullet to [0.5.0] Added;
# Phase 48 replaces it with a concrete reference-plugin bullet.
@CHANGELOG.md

<interfaces>
<!-- Names the reference plugin imports from horus_os.plugins.api (single
     public surface). All listed in src/horus_os/plugins/api.py:__all__. -->

From src/horus_os/plugins/api.py:
```
__all__ = (
    "Adapter",            # Protocol — minimum adapter contract; has `name: str`, `bind(app, context)`
    "AdapterContext",     # Frozen dataclass — passed to adapter.start(ctx)
    "Capability",         # Closed StrEnum — FILESYSTEM_READ, FILESYSTEM_WRITE, NET_OUTBOUND, SECRETS_READ
    "LifecycleAdapter",   # Protocol — adapter with async start(context) and stop()
    "PluginContext",      # Class with filesystem/secrets/net shims + name/version/data_dir/guard
    "PluginSpec",         # Frozen dataclass — discovered plugin's parsed shape
    "Tool",               # Tool-registration shape (mirrors built-in tool registry)
    "require_capability", # Decorator: @require_capability(Capability.FILESYSTEM_READ)
)
```

From src/horus_os/plugins/api.py (PluginContext shape):
```
class PluginContext:
    plugin_name: str
    plugin_version: str
    data_dir: Path
    guard: CapabilityGuard
    filesystem: _FilesystemShim   # .read(path) -> str, .write(path, content) -> None
    secrets: _SecretsShim         # .read(key) -> str | None  (None on missing env var)
    net: _NetShim                 # .outbound(url, method=...) -> httpx.Response
```

From src/horus_os/adapters/base.py (LifecycleAdapter Protocol shape, line 173-200):
```
class LifecycleAdapter(Protocol):
    name: str
    def start(self, context: AdapterContext) -> Awaitable[None]: ...
    def stop(self) -> Awaitable[None]: ...
```

From src/horus_os/plugins/capability_catalog.py:
```
class Capability(StrEnum):
    FILESYSTEM_READ = "filesystem.read"
    FILESYSTEM_WRITE = "filesystem.write"
    NET_OUTBOUND = "net.outbound"
    SECRETS_READ = "secrets.read"
```

From src/horus_os/plugins/manifest.py:
```
def validate_manifest(toml_bytes: bytes) -> PluginSpec
def format_validation_error(exc: ValidationError) -> str
MANIFEST_V1_SCHEMA: type[BaseModel]   # pydantic v2 model; top-level fields:
    # manifest_version, name, version, description, author, license,
    # homepage, issue_tracker, horus_os_compat, capabilities,
    # contributions.tools (list of {name, entry_point}),
    # contributions.adapters (list of {name, entry_point})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reference plugin package — manifest + pyproject + source + plugin-local tests</name>
  <files>
    examples/horus-os-example-plugin/pyproject.toml,
    examples/horus-os-example-plugin/horus-plugin.toml,
    examples/horus-os-example-plugin/README.md,
    examples/horus-os-example-plugin/src/horus_os_example_plugin/__init__.py,
    examples/horus-os-example-plugin/src/horus_os_example_plugin/tools.py,
    examples/horus-os-example-plugin/src/horus_os_example_plugin/adapter.py,
    examples/horus-os-example-plugin/tests/__init__.py,
    examples/horus-os-example-plugin/tests/test_example_plugin.py
  </files>
  <behavior>
    - `validate_manifest(Path('examples/horus-os-example-plugin/horus-plugin.toml').read_bytes())` returns a `PluginSpec` with zero errors; `spec.name == "horus-os-example-plugin"`, `spec.version == "0.1.0"`, capabilities set is `{Capability.FILESYSTEM_READ, Capability.SECRETS_READ}`, contributions list two tools (`echo_text_tool`, `lookup_secret_tool`) and one adapter (`ExampleAdapter`).
    - `echo_text_tool(path)` invoked via a `PluginContext` whose guard lacks `FILESYSTEM_READ` raises `PermissionDenied`; with the cap granted, returns the file's text content.
    - `lookup_secret_tool(key)` invoked via a `PluginContext` whose guard lacks `SECRETS_READ` raises `PermissionDenied`; with the cap granted, returns `os.environ.get(key)` (None when unset, string when set; never raises on missing key).
    - `ExampleAdapter().start(ctx)` completes in < 1 second wall clock and `ExampleAdapter().stop()` completes in < 1 second wall clock; both are coroutine-returning callables compatible with `asyncio.wait_for(timeout=2.0)`. `start` schedules a no-op background task with `asyncio.create_task` and returns; `stop` cancels that task and awaits its completion.
    - The package's `__init__.py` is collection-only (module docstring naming the four scenarios; no top-level side effects, no decorator registration — the discovery seam is the entry-point group, not import-time execution).
  </behavior>
  <action>
    Create the `examples/horus-os-example-plugin/` directory tree per `files`.

    `pyproject.toml`: PEP 621 metadata using `setuptools.build_meta` build backend (mirrors host `pyproject.toml:1-3`). Set `name = "horus-os-example-plugin"`, `version = "0.1.0"`, `requires-python = ">=3.11"`, `license = { text = "Apache-2.0" }`, `description = "Reference plugin demonstrating the v0.5 contract."`, `authors = [{ name = "horus-os contributors" }]`. `dependencies = ["horus-os>=0.5,<0.6"]`. Declare the discovery hook: `[project.entry-points."horus_os.plugins"]` with one line: `horus-os-example-plugin = "horus_os_example_plugin"`. Add `[tool.setuptools.packages.find] where = ["src"]`. No optional extras. No `[project.scripts]`.

    `horus-plugin.toml`: v1 manifest mirroring `tests/fixtures/manifests/manifest_v1_full.toml` SHAPE but with reference-plugin values. Set the eleven top-level fields: `manifest_version = 1`, `name = "horus-os-example-plugin"`, `version = "0.1.0"`, `description = "Reference plugin demonstrating the v0.5 contract."`, `author = "horus-os contributors"`, `license = "Apache-2.0"`, `horus_os_compat = ">=0.5,<0.6"`, `homepage = "https://github.com/Ridou/horus-os/tree/main/examples/horus-os-example-plugin"`, `issue_tracker = "https://github.com/Ridou/horus-os/issues"`. Declare `capabilities = ["filesystem.read", "secrets.read"]` (the only two needed; the adapter has no capability requirements). Add `[[contributions.tools]]` arrays for `echo_text_tool` (entry_point `horus_os_example_plugin.tools:echo_text_tool`) and `lookup_secret_tool` (entry_point `horus_os_example_plugin.tools:lookup_secret_tool`). Add `[[contributions.adapters]]` array for `example_adapter` (entry_point `horus_os_example_plugin.adapter:ExampleAdapter`). Validate against `validate_manifest()` as part of the verify step.

    `README.md`: one-page authoring starter. Sections in order: H1 title, 'What this plugin demonstrates' (numbered list of the four scenarios with one sentence each), 'How to install' (the two commands: `pip install -e ./examples/horus-os-example-plugin` and `horus-os plugins grant horus-os-example-plugin --all`), 'Anatomy' (one paragraph per source file naming what it does), 'Using this as a starting point' (explicit rename list: package name `horus_os_example_plugin`, manifest `name` field, dist name `horus-os-example-plugin`, adapter class `ExampleAdapter`, entry-point key), 'See also' (relative links to `../../docs/PLUGINS.md` and `../../docs/PLUGIN-SECURITY.md`). Plain prose, no emojis, no horizontal rules, no em dashes.

    `src/horus_os_example_plugin/__init__.py`: module docstring naming the four scenarios; declare `__all__ = ()` (the public surface is the entry-point group, not bare attribute imports); no other code. Keep under 15 lines.

    `src/horus_os_example_plugin/tools.py`: single import line `from horus_os.plugins.api import Capability, PluginContext, require_capability`. Define `echo_text_tool` (signature `def echo_text_tool(ctx: PluginContext, path: str) -> str:`) decorated with `@require_capability(Capability.FILESYSTEM_READ)` whose body calls `ctx.filesystem.read(path)` and returns the result. Define `lookup_secret_tool` (signature `def lookup_secret_tool(ctx: PluginContext, key: str) -> str | None:`) decorated with `@require_capability(Capability.SECRETS_READ)` whose body calls `ctx.secrets.read(key)` and returns the result. Add module docstring naming scenarios (a) and (b) and pointing readers at the manifest as the discovery seam.

    `src/horus_os_example_plugin/adapter.py`: single import line `from horus_os.plugins.api import Adapter, AdapterContext, LifecycleAdapter`. Define `class ExampleAdapter:` with class attribute `name = "example_adapter"`, instance attribute `_task: asyncio.Task | None = None` (initialized to None in `__init__`). Implement `def bind(self, app, context: AdapterContext) -> None` as a no-op pass (the lifecycle is in start/stop). Implement `async def start(self, context: AdapterContext) -> None` that schedules `asyncio.create_task(asyncio.sleep(0))` (assigned to `self._task`) and returns immediately — completes in microseconds, well under the 1-second budget. Implement `async def stop(self) -> None` that cancels `self._task` if present, then `await`s it in a `try / except asyncio.CancelledError: pass` block — also returns promptly. Add module docstring naming scenario (c) and the Phase 43 `asyncio.wait_for(timeout=2.0)` contract the start/stop pair must respect.

    `tests/__init__.py`: empty (marker file only).

    `tests/test_example_plugin.py`: pytest module with five test functions covering the BEHAVIOR block above. Use `from horus_os.plugins.api import Capability, PluginContext` plus internal `from horus_os.plugins.permissions import CapabilityGuard` (test code is exempt from TEST-21 — only the `src/` tree is scanned). For tests (a)/(b): construct a `CapabilityGuard` with empty granted set, build a `PluginContext`, assert `pytest.raises(PermissionDenied)`; then construct with the relevant cap granted, assert successful read. For (c): use `asyncio.wait_for(adapter.start(ctx), timeout=2.0)` and assert it returns within 1.5 seconds; same for `stop()`. For (d): read manifest bytes, call `validate_manifest()`, assert resulting `PluginSpec` has both tools and the one adapter declared.

    Self-check: the only `from horus_os` imports in `src/` are `from horus_os.plugins.api import ...`. Anything else fails TEST-21 in Task 2.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && python -c "from horus_os.plugins.manifest import validate_manifest; import pathlib; spec = validate_manifest(pathlib.Path('examples/horus-os-example-plugin/horus-plugin.toml').read_bytes()); assert spec.name == 'horus-os-example-plugin' and spec.version == '0.1.0', spec; print('manifest OK:', spec.name, spec.version)" && python -c "import ast, pathlib; [print(f) for f in pathlib.Path('examples/horus-os-example-plugin/src').rglob('*.py') if (lambda src: any(isinstance(n, ast.ImportFrom) and n.module and n.module.startswith('horus_os') and n.module != 'horus_os.plugins.api' for n in ast.walk(ast.parse(src))))(f.read_text())]; print('public-API-only: pass')"</automated>
  </verify>
  <done>
    `examples/horus-os-example-plugin/` exists with the 8 files listed in `files`. Manifest validates against `MANIFEST_V1_SCHEMA` with zero errors. All `from horus_os` lines in `src/` resolve to `from horus_os.plugins.api import ...`. The four scenarios (a/b/c/d) are implemented as described.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST-21 two-layer guard — ruff banned-api wiring + source-tree backstop test</name>
  <files>
    pyproject.toml,
    tests/plugins/test_reference_plugin_public_api_only.py
  </files>
  <behavior>
    - `ruff check examples/horus-os-example-plugin/` against the SHIPPED reference plugin source (only public-API imports) returns 0 violations.
    - The same `ruff check` against a SYNTHETIC reference plugin source line `from horus_os.adapters import Adapter` (added via in-test `tmp_path` mirror) returns at least 1 violation citing the banned-api entry.
    - `pytest tests/plugins/test_reference_plugin_public_api_only.py -v` passes against the shipped reference plugin (only public-API imports).
    - `pytest tests/plugins/test_reference_plugin_public_api_only.py -v` FAILS against an in-memory synthetic source string containing `from horus_os.adapters import Adapter`, with an assertion message naming the file and the offending import.
  </behavior>
  <action>
    Edit `pyproject.toml` (host repo). Extend the existing `[tool.ruff.lint.flake8-tidy-imports.banned-api]` table (currently line 115-116 contains only `pkg_resources`) with new entries that ban every non-public `horus_os.*` import path. Concretely add entries (using TOML inline-table syntax matching the pkg_resources precedent at line 116):

    `"horus_os.adapters".msg = "Reference plugin must import only from horus_os.plugins.api (Pitfall 8 — single public API surface). See docs/PLUGINS.md 'Public API surface' section."`

    Same `.msg` template for: `horus_os.types`, `horus_os.plugins.spec`, `horus_os.plugins.manifest`, `horus_os.plugins.permissions`, `horus_os.plugins.capability_catalog`, `horus_os.plugins.discovery`, `horus_os.plugins.loader`, `horus_os.plugins.registry`, `horus_os.plugins.installer`, `horus_os.plugins` (bare-package import).

    Scope these bans to the reference plugin path ONLY (the rest of the host repo, including `src/horus_os/*` itself and `tests/`, must continue to use any horus_os submodule freely). Use `[tool.ruff.lint.per-file-ignores]` with the inverse: ignore TID251 for ALL files EXCEPT `examples/horus-os-example-plugin/src/**/*.py`. Concretely add a new `[tool.ruff.lint.per-file-ignores]` table with entries:

    `"src/**/*.py" = ["TID251"]`
    `"tests/**/*.py" = ["TID251"]`
    `"scripts/**/*.py" = ["TID251"]`
    `"docs/**/*.py" = ["TID251"]`
    `"examples/horus-os-example-plugin/tests/**/*.py" = ["TID251"]`

    The reference plugin's `examples/horus-os-example-plugin/src/**/*.py` files are NOT listed → they DO get hit by the ban → only `from horus_os.plugins.api import ...` survives. The pre-existing `pkg_resources` ban (line 116) continues to apply globally because `pkg_resources` is NOT a `horus_os.*` submodule and the per-file-ignores only silence TID251 for specific paths but pkg_resources is a separate banned-api entry that fires across all paths.

    IMPORTANT: verify the per-file-ignores syntax does NOT silence the pkg_resources ban globally (it would not; per-file-ignores targets rule codes per glob, not banned-api entries individually). If ruff's resolution does in fact silence ALL TID251 entries together (rule-code granularity), fall back to the inverse approach: drop per-file-ignores and instead use a custom selector — e.g. `[tool.ruff.lint.extend-per-file-ignores]` syntax or a separate `[tool.ruff]` block scoped via `extend-include`. Read `ruff --version` docs (via Context7 MCP for `astral-sh/ruff`) if uncertain about per-file-ignores semantics for banned-api in the installed ruff version.

    If the ruff banned-api rule cannot be reliably scoped to a single subpath without affecting `pkg_resources` enforcement elsewhere, layer 1 is a soft guard and layer 2 (the pytest source-tree backstop below) is the hard guard. Document the chosen approach in a TOML comment above the new banned-api entries.

    Create `tests/plugins/test_reference_plugin_public_api_only.py`. Module docstring naming TEST-21 and Pitfall 8. Define `REF_PLUGIN_SRC = Path(__file__).parents[2] / "examples" / "horus-os-example-plugin" / "src"`. Compile a regex `BAD_IMPORT = re.compile(r"^\s*(from horus_os(?:\.[a-zA-Z_][a-zA-Z0-9_]*)*\s+import|import horus_os(?:\.[a-zA-Z_][a-zA-Z0-9_]*)*)")` and a partner regex `GOOD_IMPORT = re.compile(r"^\s*from horus_os\.plugins\.api\s+import\s+")`. Define a pure helper `def _scan_for_bad_imports(root: Path) -> list[str]` that walks `root.rglob("*.py")`, reads each file, splits into lines, and returns a list of `f"{file}:{lineno}:{line.rstrip()}"` strings for every line that matches `BAD_IMPORT` but does NOT match `GOOD_IMPORT`. Define three tests:

    1. `test_reference_plugin_uses_only_public_api()`: call `_scan_for_bad_imports(REF_PLUGIN_SRC)`, assert the returned list is empty; if not, fail with the full list formatted as a multi-line message.
    2. `test_reference_plugin_source_dir_exists()`: assert `REF_PLUGIN_SRC.is_dir()` (defensive: a refactor that moves the example must update this test, not silently pass on an empty directory). Also assert `(REF_PLUGIN_SRC / "horus_os_example_plugin" / "tools.py").is_file()` and `(REF_PLUGIN_SRC / "horus_os_example_plugin" / "adapter.py").is_file()`.
    3. `test_scanner_catches_synthetic_bad_import(tmp_path)`: write a fake plugin source tree under `tmp_path` containing one file with body `"from horus_os.adapters import Adapter\n"` plus another file with `"from horus_os.plugins.api import Capability\n"`; call `_scan_for_bad_imports(tmp_path)`; assert the returned list has length 1 and the only offender string contains `horus_os.adapters`. Proves the scanner ACTUALLY fires on bad imports rather than vacuously passing.

    Two-layer guard rationale (also in the test module docstring): layer 1 is `ruff` config; layer 2 is this pytest scan. If ruff is misconfigured or skipped in a future CI refactor, the pytest test still fails the build. Same defense-in-depth pattern as `tests/plugins/test_pkg_resources_banned.py` (Phase 42).
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && ruff check examples/horus-os-example-plugin/ 2>&1 | (grep -E "(TID251|error|All checks passed)" || echo "ruff: no output") && pytest tests/plugins/test_reference_plugin_public_api_only.py -v --tb=short</automated>
  </verify>
  <done>
    `ruff check examples/horus-os-example-plugin/` exits 0 (zero violations) against the shipped reference plugin source. `pytest tests/plugins/test_reference_plugin_public_api_only.py` passes all three tests, including the synthetic bad-import assertion that proves the scanner is not vacuous. The pre-existing `pkg_resources` ban (Phase 42) continues to fire on the host source tree — `grep -r "pkg_resources" src/horus_os/` returns no results (or the existing test_pkg_resources_banned.py still passes).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Installer-e2e smoke + CHANGELOG entry</name>
  <files>
    tests/plugins/test_reference_plugin_install_local.py,
    CHANGELOG.md
  </files>
  <behavior>
    - `pytest tests/plugins/test_reference_plugin_install_local.py` is SKIPPED in the default invocation (no `--run-installer-e2e` flag); collection logs show the skip with reason `clean_venv requires --run-installer-e2e flag`.
    - `pytest --run-installer-e2e tests/plugins/test_reference_plugin_install_local.py` (on the host dev machine where the `clean_venv` session fixture can build) RUNS the test, completes in under 90 seconds total wall clock (the venv build is ~30s amortized across the session; this single test adds ~5s), and PASSES.
    - The passing test asserts (a) the reference plugin discovers as `status="pending"` immediately after install (no grants), (b) after granting both capabilities through `PermissionService`, `status="loaded"` with two tools and one adapter registered.
    - `CHANGELOG.md` has a new bullet under `[0.5.0]` -> `### Added` naming the reference plugin and the four scenarios; the Phase 47 forward-reference bullet is removed (or replaced by this one — same Added subsection).
  </behavior>
  <action>
    Create `tests/plugins/test_reference_plugin_install_local.py`. Module docstring naming REFERENCE-01 and the Phase 49 forward dependency (Phase 48 ships the single-host smoke; Phase 49 generalizes to a 3-OS matrix via TEST-20).

    Mark the module with `pytestmark = pytest.mark.installer_e2e` (module-level marker so every test inside the file gets the gate; the Phase 46 `pytest_collection_modifyitems` hook in `tests/conftest.py:62-69` then skips them all unless `--run-installer-e2e` is passed).

    Imports: `subprocess`, `pytest`, `pathlib.Path`, `fastapi.testclient.TestClient`, `from horus_os.plugins.permissions import PermissionService` (test-internal — exempt from TEST-21), `from horus_os.plugins.capability_catalog import Capability`, `from horus_os.server.api import create_app` (or whatever the canonical app-factory import is — confirm via `grep -n "def create_app" src/horus_os/server/*.py`).

    Define `REPO_ROOT = Path(__file__).parents[2]` and `REF_PLUGIN_PATH = REPO_ROOT / "examples" / "horus-os-example-plugin"`.

    Single test function `test_reference_plugin_installs_and_loads_after_grant(clean_venv: CleanVenv, tmp_path: Path)`:

    1. `pip install -e $REF_PLUGIN_PATH` against `clean_venv.python` via `subprocess.run([str(clean_venv.python), "-m", "pip", "install", "-e", str(REF_PLUGIN_PATH)], check=True, cwd=str(REPO_ROOT))`.
    2. Configure a test-scoped `data_dir` under `tmp_path` and a config that points at a fresh SQLite database; call `create_app(config)` to boot horus-os in-process. Use the lifespan-integration pattern from `tests/plugins/test_lifespan_integration.py` — open `with TestClient(app) as client:` so the lifespan startup fires, then `response = client.get("/api/plugins")`. (NOTE: importing `horus_os` from the clean_venv's Python is not actually needed for the in-process test — the host `horus_os` package is already on the test runner's sys.path; the `pip install -e` step installs the REFERENCE PLUGIN's entry point into the venv's `site-packages`, but the test runs against the host's interpreter. To make discovery see the just-installed entry point, add the venv's `site-packages` to `sys.path` for the duration of the test via a context manager, OR set `HORUS_OS_PLUGIN_DIRS` env var to point at `REF_PLUGIN_PATH` so the filesystem-walk discovery path picks it up. The filesystem-walk approach is cleaner — no sys.path mutation needed. Confirm `HORUS_OS_PLUGIN_DIRS` is the actual env var by `grep -rn "HORUS_OS_PLUGIN" src/horus_os/plugins/discovery.py` and adjust accordingly.)
    3. Assert response status 200; parse JSON; assert any entry has `{"name": "horus-os-example-plugin", "status": "pending"}` (no grants yet).
    4. Open a `PermissionService` against the same database, call `service.grant(plugin_name="horus-os-example-plugin", plugin_version="0.1.0", manifest_hash=<hash>, capability=Capability.FILESYSTEM_READ, actor="test")` and the same for `SECRETS_READ`. The `manifest_hash` value comes from `validate_manifest(REF_PLUGIN_PATH / "horus-plugin.toml").manifest_hash`. Refer to Phase 43's `tests/plugins/test_permission_service.py` for the canonical call shape.
    5. Trigger a registry reload (the canonical path is via `app.state.plugin_registry.reload()` or by closing and re-opening the TestClient — pick whichever Phase 43's lifespan-integration test uses).
    6. Re-call `GET /api/plugins`; assert the example plugin now has `status="loaded"`; assert the response's `tools` field for that plugin contains `echo_text_tool` and `lookup_secret_tool`; assert the `adapters` field contains `example_adapter`.

    If any of the API shapes above turn out to differ from Phase 45's actual `/api/plugins` response schema, defer to the Phase 45 routes module (`grep -rn "api/plugins" src/horus_os/server/`) for the canonical field names. Do NOT invent field names.

    Edit `CHANGELOG.md`: in the `[0.5.0]` -> `### Added` subsection (the Phase 47 SUMMARY says this section already lists "reference plugin forward-reference for Phase 48"), REPLACE that forward-reference bullet with a concrete one: `- Reference plugin (`examples/horus-os-example-plugin/`) demonstrating the v0.5 plugin contract across four scenarios — capability-gated filesystem tool, capability-gated secret tool, bounded-lifecycle adapter, and a combined tool + adapter package — and shipped as the canonical starting template for third-party plugin authors. The TEST-21 ruff banned-api rule + pytest source-tree backstop pin the public API surface to `horus_os.plugins.api`.` Preserve every other Added/Changed/Migration bullet byte-identical.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pytest tests/plugins/test_reference_plugin_install_local.py -v --collect-only 2>&1 | grep -E "(skipped|collected|installer_e2e)" && grep -c "horus-os-example-plugin" CHANGELOG.md</automated>
  </verify>
  <done>
    `tests/plugins/test_reference_plugin_install_local.py` exists, is module-marked `pytest.mark.installer_e2e`, gets collected but skipped under default `pytest` invocation (matches Phase 46 marker contract). Running it locally with `--run-installer-e2e` completes successfully (the user can validate this on the dev machine; CI 3-OS coverage is Phase 49). CHANGELOG `[0.5.0]` -> `Added` contains the new reference-plugin bullet replacing the Phase 47 forward-reference bullet; `grep -c "horus-os-example-plugin" CHANGELOG.md` returns ≥1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference-plugin source -> host horus_os | A plugin author copies this directory wholesale; bad patterns here become bad patterns in every downstream plugin. The only sanctioned import surface is `horus_os.plugins.api`. |
| ruff config -> CI gate | A reviewer editing `pyproject.toml`'s `[tool.ruff.lint.per-file-ignores]` could silently disable TEST-21 layer 1. Layer 2 (pytest source-tree scan) is the backstop. |
| reference-plugin tests -> host fixtures | The plugin-local test module imports `horus_os.plugins.permissions` (a non-public surface) — this is test-only code that lives under `examples/horus-os-example-plugin/tests/`, NOT under `src/`. TEST-21 scans `src/` only; test code is exempt by design. |
| installer-e2e smoke -> clean_venv | The `clean_venv` fixture rebuilds the venv once per pytest session. If the fixture's pip-install step pollutes the venv with non-horus_os packages, downstream tests in the same session might see contaminated state. Phase 46 already shipped this fixture with per-session caching; we are a consumer, not a re-implementor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | reference plugin source | mitigate | Two-layer guard: ruff `flake8-tidy-imports.banned-api` (layer 1) + pytest source-tree scan in `tests/plugins/test_reference_plugin_public_api_only.py` (layer 2). Bad-import fixture in layer 2 proves the scanner fires; defense-in-depth against config drift. |
| T-48-02 | Tampering | host pyproject.toml ruff config | mitigate | Layer-2 pytest backstop runs in default invocation regardless of ruff config state. A reviewer cannot silence TEST-21 by editing only `pyproject.toml`. |
| T-48-03 | Spoofing | manifest fields | accept | The reference plugin's `homepage` and `issue_tracker` point at the canonical GitHub URLs for `Ridou/horus-os`; if those URLs are taken over (e.g. account compromise), every plugin in the ecosystem has the same problem. Out of scope per PROJECT.md (PyPI supply-chain attacks are explicitly not defended against). |
| T-48-04 | Repudiation | grant/revoke during e2e smoke | mitigate | The installer-e2e test calls `PermissionService.grant(..., actor="test")`; the audit-log table `plugin_capability_grants_log` (Phase 43) records every grant with an actor field, making test-side mutations distinguishable from human-side mutations. |
| T-48-05 | Information disclosure | `lookup_secret_tool` returning env vars | accept | The tool is the demonstration of `secrets.read` and intentionally surfaces env-var values to the caller. The grant-prompt flow (Phase 44) shows the user the plain-English `DESCRIPTIONS[SECRETS_READ]` BEFORE granting; informed consent is the boundary. |
| T-48-06 | Denial of service | `ExampleAdapter.start` hanging | mitigate | `start` schedules `asyncio.create_task(asyncio.sleep(0))` and returns synchronously after the `await` — completes in microseconds. Phase 43's `asyncio.wait_for(start, timeout=2.0)` bound (loader.py:69) is the hard ceiling; the reference plugin sits well inside it. Plugin authors copying this pattern get bounded-by-default behavior. |
| T-48-07 | Elevation of privilege | reference plugin requesting more caps than declared | mitigate | The plugin requests exactly `filesystem.read` + `secrets.read` in its manifest. The Phase 43 `CapabilityGuard` raises `PermissionDenied` if either tool is invoked without the matching grant; the `require_capability` decorator (api.py:190) records the required cap set, the loader's wrap site enforces. End-to-end smoke test verifies the pending→loaded transition only happens after explicit grant. |
| T-48-08 | Tampering | `from horus_os._private` imports sneaking in | mitigate | Both layers of TEST-21 reject any `from horus_os` line that doesn't start with `from horus_os.plugins.api`. Leading-underscore submodules (`horus_os._observability`, etc.) are rejected by the same regex — no special-casing needed. |
</threat_model>

<verification>
**Layer-1 ruff lint (TEST-21 layer 1):**
```bash
ruff check examples/horus-os-example-plugin/
# exit code 0; reports "All checks passed"
```

**Layer-2 source-tree backstop (TEST-21 layer 2):**
```bash
pytest tests/plugins/test_reference_plugin_public_api_only.py -v
# 3 tests passing: only-public-API, source-dir-exists, synthetic-bad-catches
```

**Manifest validates against runtime schema:**
```bash
python -c "from horus_os.plugins.manifest import validate_manifest; import pathlib; spec = validate_manifest(pathlib.Path('examples/horus-os-example-plugin/horus-plugin.toml').read_bytes()); print(spec.name, spec.version, [c.name for c in spec.capabilities], len(spec.contributions.tools), len(spec.contributions.adapters))"
# horus-os-example-plugin 0.1.0 ['FILESYSTEM_READ', 'SECRETS_READ'] 2 1
```

**Installer-e2e smoke (gated, run locally):**
```bash
pytest --run-installer-e2e tests/plugins/test_reference_plugin_install_local.py -v
# 1 test passing; ~5s after the ~30s clean_venv session warm-up
```

**Default invocation still skips the gated test:**
```bash
pytest tests/plugins/test_reference_plugin_install_local.py -v
# 1 skipped (installer_e2e marker, missing --run-installer-e2e)
```

**CHANGELOG mentions the reference plugin:**
```bash
grep -v '^#' CHANGELOG.md | grep -c "horus-os-example-plugin"
# ≥1
```

**Phase 47 docs forward references remain accurate:**
```bash
pytest tests/docs/test_plugins_md_anatomy.py -v
# Still green — Phase 48 fulfills the contract, no docs edit needed
```

**Pre-existing pkg_resources ban (Phase 42) still fires:**
```bash
pytest tests/plugins/test_pkg_resources_banned.py -v
# Still green — per-file-ignores does not silence the global pkg_resources ban
```

**No regression in plugin test suite:**
```bash
pytest tests/plugins/ -v --ignore=tests/plugins/test_reference_plugin_install_local.py
# All Phase 41-47 tests green
```
</verification>

<success_criteria>
1. `examples/horus-os-example-plugin/` ships as a self-contained installable package: `pyproject.toml` declares `[project.entry-points."horus_os.plugins"]`, `horus-plugin.toml` validates against `MANIFEST_V1_SCHEMA` with zero errors, `src/horus_os_example_plugin/{__init__,tools,adapter}.py` plus `tests/test_example_plugin.py` exist; `pip install -e ./examples/horus-os-example-plugin` from a clean venv with horus-os pre-installed succeeds. (REFERENCE-01)
2. Four scenarios demonstrated: `tools.echo_text_tool` (filesystem.read), `tools.lookup_secret_tool` (secrets.read), `adapter.ExampleAdapter` (bounded start/stop), one manifest registering both tools + adapter. (REFERENCE-01)
3. TEST-21 layer 1: ruff banned-api wiring in `pyproject.toml` rejects any non-public `horus_os.*` import inside `examples/horus-os-example-plugin/src/`; `ruff check examples/horus-os-example-plugin/` is green against the shipped source. (TEST-21)
4. TEST-21 layer 2: `tests/plugins/test_reference_plugin_public_api_only.py` walks the reference plugin source, fails on any non-public `horus_os` import, and is verified non-vacuous by a synthetic bad-import fixture. (TEST-21)
5. `tests/plugins/test_reference_plugin_install_local.py` marked `@pytest.mark.installer_e2e`, consumes the `clean_venv` fixture, asserts the pending→loaded transition after `PermissionService.grant`. Default `pytest` invocation skips it cleanly; `pytest --run-installer-e2e` runs it. (REFERENCE-01)
6. CHANGELOG `[0.5.0]` -> `Added` contains a bullet naming the reference plugin and the four scenarios; the Phase 47 forward-reference bullet is replaced.
7. Phase 47 forward references in `docs/PLUGINS.md` (lines 109-122) are now accurate WITHOUT any edit to that file; the Phase 47 docs-anatomy test continues to pass.
8. No regression: full `pytest tests/plugins/ tests/docs/` suite stays green; pkg_resources ban (Phase 42) continues to fire.
</success_criteria>

<output>
After completion, create `.planning/phases/48-reference-plugin-horus-os-example-plugin/48-01-SUMMARY.md` documenting:
- Files created (the 8 reference plugin files + 2 test files + CHANGELOG bullet + pyproject.toml diff).
- Validation: ruff banned-api green on shipped source, layer-2 pytest scan green (3 tests), manifest validates, installer-e2e smoke gated-skip in default invocation.
- The two-layer guard rationale: layer 1 catches at lint time during development, layer 2 catches in CI even if ruff config drifts.
- Forward dependency cleared: Phase 49 can now consume `examples/horus-os-example-plugin/` for the TEST-20 3-OS install-smoke matrix and the REL-11 release-gate manifest-validation check.
- Decisions made under Claude's Discretion (e.g. exact ruff per-file-ignores syntax if banned-api scoping differs from expectation; HORUS_OS_PLUGIN_DIRS env var vs sys.path mutation in the installer-e2e smoke test).
</output>