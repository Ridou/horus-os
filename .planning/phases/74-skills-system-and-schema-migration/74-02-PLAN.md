---
phase: 74-skills-system-and-schema-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/horus_os/storage.py
  - scripts/install_smoke.py
  - tests/test_v11_to_v12_migration.py
  - tests/test_storage.py
  - tests/test_install_smoke.py
  - tests/test_e2e_dashboard_composition.py
  - tests/test_v8_to_v9_migration.py
  - tests/test_v9_to_v10_migration.py
  - tests/test_v10_to_v11_migration.py
  - tests/plugins/test_v5_to_v6_migration.py
  - tests/plugins/test_v6_to_v7_migration.py
  - tests/docs/test_migration_v04_v05_schema_commands.py
  - tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py
autonomous: true
requirements:
  - SKILL-04
  - MIG-07
must_haves:
  truths:
    - "SCHEMA_VERSION is 12 and Database.init() on a fresh DB creates a skills table additively"
    - "A database stamped schema_version=11 upgrades to 12 on init(); pre-existing trace rows survive"
    - "Running init() twice on a v12 database is a no-op (idempotent); no ALTER TABLE re-runs"
    - "Every hardcoded version-expectation file in the tripwire set asserts 12, not 11, and the full suite is green"
    - "The vector index is unaffected: no vectors.sqlite schema change rides this bump (MIG-07)"
  artifacts:
    - path: "src/horus_os/storage.py"
      provides: "SCHEMA_VERSION=12, skills table in SCHEMA_SQL, v11->v12 migration block in init()"
      contains: "CREATE TABLE IF NOT EXISTS skills"
    - path: "tests/test_v11_to_v12_migration.py"
      provides: "additive + idempotent v11->v12 regression test"
      contains: "test_v11_upgrades_to_v12"
  key_links:
    - from: "src/horus_os/storage.py"
      to: "schema_version table"
      via: "init() bumps stored version 11 -> 12 and creates skills table"
      pattern: "SCHEMA_VERSION = 12"
    - from: "scripts/install_smoke.py"
      to: "on-disk schema version"
      via: "SCHEMA_VERSION_EXPECTED constant asserts 12"
      pattern: "SCHEMA_VERSION_EXPECTED = 12"
---

<objective>
Land the single unavoidable v0.8 schema bump: add a `skills` SQLite table, bump `SCHEMA_VERSION` from 11 to 12 with an additive + idempotent migration block, and sweep EVERY hardcoded version-expectation file in the tripwire set from 11 to 12 in this one plan so the suite never lands in a half-bumped state. This plan is deliberately isolated (ROADMAP execution note: skills get their own phase because of this tripwire) and touches NO skills-package source, so it runs parallel to plan 74-01.

Purpose: SKILL-04 and MIG-07 require the v11->v12 migration to be additive, idempotent, and to update every SCHEMA_VERSION tripwire file atomically. The `skills` table gives the dashboard and CLI a queryable index of skills (the filesystem in 74-01 is the source of truth; this table is the registry surface plan 74-03 reads). MEMORY.md records the tripwire breaks ~15 hardcoded files; this plan is the focused sweep.

Output: storage.py at v12 with the skills table, a new v11->v12 migration regression test, and the full tripwire file set re-pinned to 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The v10->v11 migration is the exact template for the v11->v12 block and its test. -->
<!-- The bump must follow the established additive-only, CREATE TABLE IF NOT EXISTS pattern. -->

From src/horus_os/storage.py (current state, v11):
- SCHEMA_VERSION = 11 (line 28). SCHEMA_SQL ends with the v11 sync_cursors block.
- Database.init(): executescript(SCHEMA_SQL); reads stored_version; INSERTs SCHEMA_VERSION on fresh
  or UPDATEs it on upgrade; then a ladder of `if stored_version is not None and stored_version < K:`
  guards. The v8/v9/v10/v11 guards are no-op `pass` blocks because each only added a new table
  (CREATE TABLE IF NOT EXISTS in SCHEMA_SQL handles fresh + upgrade; ALTER TABLE only needed for
  column adds). The skills table is a NEW TABLE, so the v11 -> v12 guard is also a `pass` block.
- The ARCHITECTURE.md skills table sketch: id INTEGER PK AUTOINCREMENT, skill_id TEXT UNIQUE,
  name TEXT NOT NULL, description TEXT NOT NULL DEFAULT '', plus created_at / updated_at; the
  filesystem is the source of truth, so this table is a thin registry. Keep columns minimal and
  additive. Add CREATE INDEX IF NOT EXISTS idx_skills_name ON skills(name).

From tests/test_v10_to_v11_migration.py (copy its shape for the new v11->v12 test):
- _get_version(path), _table_exists(path, name), _row_count(path, name) helpers.
- test_v10_upgrades_to_v11 bootstraps a DB, forces version back to 10, DROPs the new table,
  inserts a trace row, re-inits, asserts version==11 + table exists + trace survived.
- test_v11_init_is_idempotent and test_fresh_init_creates_<table>_table.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add skills table, bump SCHEMA_VERSION 11 -> 12, write the v11->v12 migration test</name>
  <read_first>
    - src/horus_os/storage.py (SCHEMA_VERSION, SCHEMA_SQL tail, init() migration ladder, _now_iso)
    - tests/test_v10_to_v11_migration.py (the exact template for the new test)
    - .planning/research/ARCHITECTURE.md (Feature 8 skills table sketch; SCHEMA_VERSION tripwire section)
  </read_first>
  <behavior>
    - Test (test_v11_to_v12_migration.py): a DB stamped schema_version=11 with the skills table dropped upgrades to 12 on init(); the skills table exists and is empty afterward.
    - Test: a pre-existing trace row inserted at v11 survives the v11->v12 upgrade (additive, not destructive).
    - Test: calling init() twice on a v12 database keeps version at 12 and raises nothing (idempotent).
    - Test: a fresh Database.init() produces schema_version=12 with a skills table.
    - Test: the v11->v12 upgrade does NOT touch or require any vectors.sqlite file (MIG-07: vector index lives in a separate store, no schema bump there).
  </behavior>
  <action>
    In `src/horus_os/storage.py`: append a `CREATE TABLE IF NOT EXISTS skills (...)` block to `SCHEMA_SQL` (after the v11 sync_cursors block) with the minimal registry columns from the ARCHITECTURE sketch: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `skill_id TEXT NOT NULL UNIQUE`, `name TEXT NOT NULL`, `description TEXT NOT NULL DEFAULT ''`, `kind TEXT NOT NULL DEFAULT 'prompt-template'`, `rel_path TEXT NOT NULL`, `tags TEXT NOT NULL DEFAULT '[]'`, `created_at TEXT NOT NULL`, `updated_at TEXT NOT NULL`. Add `CREATE INDEX IF NOT EXISTS idx_skills_name ON skills(name)`. Add a leading SQL comment that this is the v12 additive table and the filesystem under skills_dir is the source of truth (registry only). Bump `SCHEMA_VERSION = 12`. Add the `if stored_version is not None and stored_version < 12:` guard in `init()` as a `pass` block with a comment matching the v8/v9/v10/v11 precedent (skills is a NEW TABLE; CREATE TABLE IF NOT EXISTS in SCHEMA_SQL handles both fresh and upgraded DBs; no ALTER TABLE needed). Do NOT add any SkillStore CRUD method here; this plan only lands the schema. Keep the comment prose hyphenated, no em-dashes.
    Create `tests/test_v11_to_v12_migration.py` by copying the structure of `tests/test_v10_to_v11_migration.py`: reuse `_get_version`, `_table_exists`, `_row_count` helpers; `test_v11_upgrades_to_v12` (bootstrap, force version back to 11, DROP TABLE IF EXISTS skills, insert a trace row, re-init, assert version==12 + skills exists + empty + trace survived), `test_v12_init_is_idempotent`, `test_fresh_init_creates_skills_table`. Add one assertion that no `vectors.sqlite` file is created by init() (MIG-07 isolation).
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -m pytest tests/test_v11_to_v12_migration.py -x -q</automated>
  </verify>
  <done>
    SCHEMA_VERSION == 12; fresh init creates an empty skills table; a v11 DB upgrades idempotently to v12 with trace rows preserved; the new migration test passes; no vectors.sqlite is created by the migration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep every hardcoded SCHEMA_VERSION expectation from 11 to 12</name>
  <read_first>
    - scripts/install_smoke.py (SCHEMA_VERSION_EXPECTED = 11 at line ~39; print line "schema_version==")
    - tests/test_storage.py (8 occurrences of `version == 11`)
    - tests/test_install_smoke.py (asserts substring "schema_version==11")
    - tests/test_e2e_dashboard_composition.py (asserts version == 11 at line ~158)
    - tests/test_v8_to_v9_migration.py, tests/test_v9_to_v10_migration.py, tests/test_v10_to_v11_migration.py
    - tests/plugins/test_v5_to_v6_migration.py, tests/plugins/test_v6_to_v7_migration.py
    - tests/docs/test_migration_v04_v05_schema_commands.py (assert SCHEMA_VERSION == 11)
    - tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py
  </read_first>
  <action>
    Update every hardcoded SCHEMA_VERSION-value expectation in the tripwire set from 11 to 12. This is the MIG-07 / SKILL-04 sweep; do it in ONE plan so the suite is never half-bumped.
    1. `scripts/install_smoke.py`: change `SCHEMA_VERSION_EXPECTED = 11` to `12` (the print string derives from the constant, so the output becomes `schema_version==12` automatically).
    2. `tests/test_install_smoke.py`: change the asserted substring `"schema_version==11"` to `"schema_version==12"`.
    3. `tests/test_storage.py`: change all 8 `version == 11` (and any `expected schema_version=11` message text) to 12.
    4. `tests/test_e2e_dashboard_composition.py`: change `assert version == 11` to 12. (The seeded fixture there starts at version 1 and is migrated up by init(); only the final-version assertion changes.)
    5. `tests/test_v8_to_v9_migration.py`, `tests/test_v9_to_v10_migration.py`, `tests/test_v10_to_v11_migration.py`: each asserts the FINAL on-disk version equals the current SCHEMA_VERSION after init(). Update the trailing/final `== 11` assertions to `== 12` (these tests bootstrap a DB and force the version DOWN to test an OLD migration step, but the post-init final version is always the current SCHEMA_VERSION). Read each test carefully: the forced-down version (e.g. `UPDATE schema_version SET version = 8`) stays as-is; only the post-`init()` assertion that the DB reached the current version changes to 12. Preserve any message text consistency.
    6. `tests/plugins/test_v5_to_v6_migration.py` and `tests/plugins/test_v6_to_v7_migration.py`: these include `assert SCHEMA_VERSION == 11` (importing the live constant) plus post-init `== 11` assertions. Update both the imported-constant assertion and the post-init final-version assertions to 12.
    7. `tests/docs/test_migration_v04_v05_schema_commands.py`: update `assert SCHEMA_VERSION == 11` and the docstring/message referencing "== 11" to 12.
    8. `tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py`: update the 3 post-migration `== 11` version assertions to 12.
    Do NOT change the historical forced-down values inside any older migration test (those simulate an OLD database and must stay at their original version). ONLY the assertions about the CURRENT post-init schema version move to 12. Do NOT add em-dashes anywhere.
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -m pytest tests/test_storage.py tests/test_v8_to_v9_migration.py tests/test_v9_to_v10_migration.py tests/test_v10_to_v11_migration.py tests/test_v11_to_v12_migration.py tests/plugins/test_v5_to_v6_migration.py tests/plugins/test_v6_to_v7_migration.py tests/docs/test_migration_v04_v05_schema_commands.py tests/test_plugin_pitfalls/test_pitfall_09_schema_migration_regression.py tests/test_e2e_dashboard_composition.py tests/test_install_smoke.py -q</automated>
  </verify>
  <done>
    No tripwire file still asserts the current schema version is 11; the listed test files are green; `grep -rn "version == 11\|SCHEMA_VERSION == 11\|schema_version==11" tests/ scripts/` returns only intentional forced-down historical values inside old-version migration setup (never a post-init current-version assertion).
  </done>
</task>

<task type="auto">
  <name>Task 3: Tripwire-completeness guard and full-suite confirmation</name>
  <read_first>
    - src/horus_os/storage.py (SCHEMA_VERSION constant, now 12)
    - tests/test_v11_to_v12_migration.py (created in Task 1)
    - .planning/research/SUMMARY.md (note: ~15 files must update when SCHEMA_VERSION bumps)
  </read_first>
  <action>
    Add a single guard test to `tests/test_v11_to_v12_migration.py` (or a small `test_schema_version_tripwire.py` if cleaner) that asserts `horus_os.storage.SCHEMA_VERSION == 12` and that a freshly-initialized database reports on-disk version 12. This is the canonical anchor so a future bump immediately fails this one assertion and points the next contributor at the sweep. Add a short module docstring listing the tripwire file set swept in this phase (the same set in this plan's `read_first`) so the next bump has the inventory documented in-tree. Then run the FULL suite, filtering out the known pre-existing OTEL failures per MEMORY.md (do not block on `test_adapters_otel*`).
    Do NOT introduce any new dependency. Do NOT touch the skills package source (that is 74-01 / 74-03).
  </action>
  <verify>
    <automated>cd /Users/santino/gsd-workspaces/horus-os-v08 && python -m pytest tests/ -q --deselect tests/observability 2>/dev/null -k "not otel" | tail -20</automated>
  </verify>
  <done>
    A guard assertion pins SCHEMA_VERSION == 12 and on-disk fresh-init version 12; the tripwire inventory is documented in the test module docstring; the full suite (excluding the documented pre-existing OTEL failures) is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing user DB (v11) -> init() | A real user's populated SQLite at v11 crosses into the v12 migration; data loss here is the highest-severity failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05 | Tampering | v11->v12 migration on a populated DB | mitigate | Additive only: CREATE TABLE IF NOT EXISTS + no ALTER on existing tables; a regression test asserts a pre-existing trace row survives the upgrade. Migration is one-way and idempotent (matches v8/v9/v10/v11 precedent). |
| T-74-06 | Denial of service | half-bumped tripwire state | mitigate | The sweep updates every hardcoded expectation in one plan; the Task 3 guard pins SCHEMA_VERSION==12 so a partial bump fails loudly rather than silently passing some tests. |
| T-74-07 | Repudiation | vector index drift riding the bump | accept | MIG-07 keeps the vector index in a separate vectors.sqlite with its own versioning; the migration test asserts init() creates no vectors.sqlite, so the bump cannot silently alter vector state. |
</threat_model>

<verification>
- `python -c "from horus_os.storage import SCHEMA_VERSION; assert SCHEMA_VERSION == 12"` exits 0.
- `python -m pytest tests/test_v11_to_v12_migration.py -q` is green.
- The full suite (minus documented pre-existing `test_adapters_otel*` failures per MEMORY.md) is green.
- `grep -rn "== 11" tests/ scripts/` shows no post-init current-version assertion left at 11 (only intentional forced-down OLD-version setup lines remain).
- No em-dash (U+2014) in any changed file.
</verification>

<success_criteria>
- SCHEMA_VERSION is 12; the skills table is created additively; a v11 DB upgrades idempotently (SKILL-04, MIG-07).
- Every hardcoded SCHEMA_VERSION expectation in the tripwire set is at 12 and the suite is green.
- The vector index store is untouched by this bump (MIG-07).
- A guard test pins the version so the next bump fails fast with the tripwire inventory documented in-tree.
</success_criteria>

<output>
Create `.planning/phases/74-skills-system-and-schema-migration/74-02-SUMMARY.md` when done.
</output>
