---
phase: 03
plan: "00"
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - src/horus_os/storage.py
  - src/horus_os/__init__.py
  - tests/test_storage.py
autonomous: true
requirements:
  - AGENT-02
tags:
  - persistence
  - sqlite
  - traces
  - wal

must_haves:
  truths:
    - "`Database(path)` accepts a path string or Path object and lazily creates the parent directory"
    - "`Database.init()` is idempotent. Calling it on a fresh DB creates the schema. Calling it on an initialized DB is a no-op."
    - "WAL journal mode is enabled, synchronous=NORMAL, foreign_keys=ON, busy_timeout=5000ms"
    - "`Database.record_trace(prompt, result, *, latency_ms=None, status='success', error_message=None) -> str` writes one row and returns the trace_id (UUID4 string)"
    - "`Database.list_traces(*, limit=50, offset=0) -> list[TraceRecord]` returns most-recent-first ordering by created_at"
    - "`Database.get_trace(trace_id) -> TraceRecord | None` returns the record or None"
    - "Tool uses and usage are stored as JSON strings (no JSON1 extension required, decoded in Python on read)"
    - "Connections open per-operation via `sqlite3.connect`. No long-lived connection state, no thread-safety concerns."
    - "Public API includes `Database` and `TraceRecord` re-exported from `horus_os`"
    - "All tests use `tmp_path` fixtures. No tests touch a shared filesystem location."
  artifacts:
    - path: "src/horus_os/storage.py"
      provides: "`Database` class, `TraceRecord` dataclass, schema constants"
      contains: "class Database"
      min_lines: 80
    - path: "tests/test_storage.py"
      provides: "Schema init idempotence, record + roundtrip, list ordering, list pagination, get hit/miss, JSON serialization of tool_uses and usage, error-path recording, WAL pragma verification"
      contains: "def test_"
      min_lines: 80

tasks:
  - id: "1"
    title: "Schema and Database class in `src/horus_os/storage.py`"
    files:
      - src/horus_os/storage.py
    notes: "Module-level `SCHEMA_VERSION = 1` and `SCHEMA_SQL` constant. `TraceRecord` dataclass mirrors the table columns plus decoded tool_uses and usage. `Database` class wraps a path; methods `init`, `record_trace`, `list_traces`, `get_trace`. Use `sqlite3.connect(path, isolation_level=None)` with `PRAGMA journal_mode=WAL`, `PRAGMA synchronous=NORMAL`, `PRAGMA foreign_keys=ON`, `PRAGMA busy_timeout=5000`. Parent directory is created on init if missing."

  - id: "2"
    title: "Re-export Database and TraceRecord from `horus_os.__init__`"
    files:
      - src/horus_os/__init__.py
    notes: "Add to `__all__` and the import block. Public API is now: run_agent, run_agent_async, Tool, ToolUse, AgentResult, Database, TraceRecord, __version__."

  - id: "3"
    title: "Tests for storage layer"
    files:
      - tests/test_storage.py
    notes: "tmp_path-based tests covering: init creates file + tables, init is idempotent (run twice), WAL pragma is set, record_trace returns a UUID4 string, get_trace round-trips an AgentResult faithfully (tool_uses and usage decoded), list_traces returns most-recent-first, list_traces pagination via limit/offset, get_trace returns None for unknown id, error-path record stores status='error' + error_message, parent directory auto-created."

  - id: "4"
    title: "Local verification"
    files: []
    notes: "Run ruff check, ruff format --check, pytest -v. Public-API smoke import. All must pass."

  - id: "5"
    title: "Commit + SUMMARY.md + ROADMAP/STATE advance + push"
    files:
      - .planning/phases/03-persistence-layer/03-00-SUMMARY.md
      - .planning/ROADMAP.md
      - .planning/STATE.md
      - ROADMAP.md

threat_model:
  - threat: "SQL injection through prompt or response content"
    mitigation: "All writes use parameterized queries via `?` placeholders. The schema never interpolates user-supplied strings into SQL."
  - threat: "JSON deserialization of malformed tool_uses or usage rows"
    mitigation: "Reads use `json.loads` inside a try/except, defaulting to empty list/dict on parse failure. Manifests a structured TraceRecord regardless of input quality."
  - threat: "Concurrent writes causing locking errors"
    mitigation: "WAL mode allows concurrent reads with one writer. `busy_timeout=5000` gives writers a 5s window before raising. Phase 03 does not yet need finer concurrency primitives."

success_criteria:
  - "ruff check exits 0"
  - "ruff format --check exits 0"
  - "pytest -v exits 0 with at least 10 new tests"
  - "`from horus_os import Database, TraceRecord` succeeds"
  - "`horus-os --version` still prints `horus-os 0.0.1`"
  - "03-00-SUMMARY.md exists and is committed"
  - "ROADMAP and STATE marked Phase 03 complete, Phase 04 ready to plan"
  - "Branch pushed to origin/main cleanly"
