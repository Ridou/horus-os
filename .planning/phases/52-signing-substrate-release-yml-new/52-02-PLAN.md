---
phase: 52
plan: "02"
subsystem: signing-substrate
type: execute
wave: 1
depends_on: ["52-01"]
files_modified:
  - .github/workflows/release.yml
  - scripts/verify_release.py
  - .planning/decisions/no-pypi-in-v0.6.md
  - docs/RELEASE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [SIGN-01, SIGN-02, SIGN-03, SIGN-04, SIGN-05]
tags: [ci, security, signing, sigstore, release-workflow, verify-script, decision-file, wave-1, green-flip]
tech_stack:
  added:
    - "sigstore/gh-action-sigstore-python@04cffa1d795717b140764e8b640de88853c92acc # v3.3.0 (CI-only; never enters pyproject.toml)"
    - "actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0 (CI-only)"
  patterns:
    - "Top-level permissions: read-all + per-job opt-in (matches Phase 51 ci.yml convention)"
    - "Every uses: line SHA-pinned to 40-char hex with trailing # vN.M.P comment (Phase 51 pinact convention)"
    - "Stdlib-only Python script with subprocess shell-out (mirrors scripts/release_gate.py CheckResult shape)"
    - "Hardcoded module-level EXPECTED_IDENTITY_TEMPLATE constant + module-import-time assert (PITFALL 3 / Pitfall 1 mitigation)"
    - "@dataclass(frozen=True) CheckResult with ok: bool | None (None = SKIPPED)"
    - "Per-artifact attest-build-provenance invocations (one wheel, one sdist)"
    - "Insertions allowed, mutations not (Phase 51 D-06 STOP-BEFORE-TAG byte-identity invariant)"
key_files:
  created:
    - .github/workflows/release.yml
    - scripts/verify_release.py
    - .planning/decisions/no-pypi-in-v0.6.md
  modified:
    - docs/RELEASE.md
    - .planning/PROJECT.md
decisions:
  - "release.yml is ONE job (sign-and-attest) on ubuntu-latest per D-01; sequential steps build -> sign -> attest; no inter-job artifact upload"
  - "Top-level permissions: read-all + per-job {id-token, contents, attestations: write} on sign-and-attest only per D-02"
  - "verify_release.py is stdlib-only and shells out via subprocess to python -m sigstore + git + gh per D-03; sigstore NEVER added to pyproject.toml"
  - "EXPECTED_IDENTITY_TEMPLATE is a hardcoded module constant with {version} as the only substitution per D-04; module-import-time assert defends the contract; --cert-oidc-issuer is required and must equal EXPECTED_ISSUER exactly"
  - "docs/RELEASE.md edits are TWO insertions: NEW step 6.5 between current steps 6 and 7 + literal swap of `git tag -a` to `git tag -s` in step 7 per D-05; steps 1-6 and 8-9 prose remain byte-identical"
  - "Per-artifact attest-build-provenance: one invocation for dist/*.whl, one for dist/*.tar.gz per D-06; per-artifact granularity for 1:1 CI-step-to-verifiable-artifact mapping"
  - "verify_release.py ships full 5-check skeleton with check #4 (sbom-signature) stubbed CheckResult(ok=None, diagnostic='SKIPPED - Phase 53 lands SBOM generation + signing') per D-08; Phase 53 flips one stub instead of re-architecting"
  - "Decision file + .planning/PROJECT.md key-decisions table row land in the SAME commit per D-09 (SIGN-05 atomicity)"
  - "Sigstore inputs in Phase 52 are exactly `./dist/*.whl ./dist/*.tar.gz` per D-10; Phase 53 will APPEND ` ./dist/*.sbom.json` (the smallest possible Phase 53 diff)"
metrics:
  estimated_context: "~45%"
  task_count: 3
must_haves:
  truths:
    - "D-01: `.github/workflows/release.yml` exists with ONE job `sign-and-attest` on ubuntu-latest; sequential steps checkout -> setup-python -> install build -> python -m build -> sigstore sign -> attest wheel -> attest sdist; triggered by `on: release: types: [published]` ONLY (no push:tags, no workflow_dispatch)"
    - "D-02: release.yml declares top-level `permissions: read-all` (column 0) AND per-job `permissions: { id-token: write, contents: write, attestations: write }` on sign-and-attest ONLY"
    - "D-03: scripts/verify_release.py is stdlib-only Python; shells out to `python -m sigstore verify identity`, `git verify-tag`, `gh release view`; prints install hint and exits non-zero with clear diagnostic if `python -m sigstore --version` is not available; sigstore is NOT added to pyproject.toml [project.dependencies] or [dev] extras"
    - "D-04: scripts/verify_release.py defines EXPECTED_IDENTITY_TEMPLATE = 'https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/{version}' and EXPECTED_ISSUER = 'https://token.actions.githubusercontent.com' as module-level constants; module-import-time assert `'refs/tags/{version}' in EXPECTED_IDENTITY_TEMPLATE`; CLI rejects calls without --cert-oidc-issuer (argparse required=True) and rejects calls where --cert-oidc-issuer != EXPECTED_ISSUER"
    - "D-05: docs/RELEASE.md inserts NEW step 6.5 (`Confirm gitsign is configured. Run git config --get gitsign.connectorID...`) BETWEEN current step 6 and current step 7 + literal swap `git tag -a vN.M.P` -> `git tag -s vN.M.P` in step 7; steps 1-6 and 8-9 prose remain byte-identical to pre-edit baseline (insertions allowed, mutations not)"
    - "D-06: release.yml has TWO per-artifact actions/attest-build-provenance invocations: subject-path: 'dist/*.whl' AND subject-path: 'dist/*.tar.gz'; NOT a single dist/* glob"
    - "D-07: tests/fixtures/sigstore/canonical/README.md (already shipped in Plan 01) documents the rehearsal recording procedure; binary fixture (.whl + .sigstore[.json]) lands at v0.6.0-rc1 rehearsal time as Manual-Only Verification (out of scope for this plan)"
    - "D-08: scripts/verify_release.py ships full 5-check skeleton; check #4 sbom-signature returns CheckResult(name='sbom-signature', ok=None, diagnostic='SKIPPED - Phase 53 lands SBOM generation + signing'); skipped checks do NOT count as failures (ok is None is not False); --check {wheel|sdist|tag|sbom|changelog} runs one check"
    - "D-09: .planning/decisions/no-pypi-in-v0.6.md exists with the literal terminator `**Decision (final, until revisited):**` + .planning/PROJECT.md key-decisions table (.planning/PROJECT.md:74-81) APPENDS one row referencing `.planning/decisions/no-pypi-in-v0.6.md`; both changes land in the SAME commit"
    - "D-10: release.yml's sigstore-python `inputs:` value is exactly `./dist/*.whl ./dist/*.tar.gz` (whitespace-separated two-glob; Phase 53 appends one more glob)"
    - "Every `uses:` line in release.yml is SHA-pinned to 40-char hex with a trailing `# vN.M.P` comment per CIHARD-04 (Phase 51 convention); persist-credentials: false on actions/checkout per CIHARD-03"
    - "release.yml step containing the sigstore-python action has `timeout-minutes: 5` to bound the OIDC budget per PITFALL 2 / SIGN-01"
    - "All Plan 01 RED-by-design production assertions flip GREEN after this plan's edits (the 9 SIGN-01+02 release.yml structure tests, 5 SIGN-04 verify_release.py tests excluding the canonical-fixture tests that remain SKIPPED until rehearsal, 3 SIGN-03 docs/RELEASE.md tests, 3 SIGN-05 decision-file tests); Plan 01's non-vacuity tests remain GREEN"
    - "CLAUDE.md hard rules honored: ruff clean (`ruff check . && ruff format --check .`); no em-dashes in any committed prose (release.yml comments, verify_release.py docstrings + diagnostics, no-pypi-in-v0.6.md, .planning/PROJECT.md row, docs/RELEASE.md step 6.5); no PII other than `Ridou/horus-os`; conventional-commit prefixes referencing phase 52"
    - "Existing test suite delta: no NEW failures outside Phase 52 scope; the existing 1011-test suite continues to pass on Ubuntu/macOS/Windows x Python 3.11/3.12"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Single sign-and-attest job; SIGN-01 + SIGN-02 substrate"
      contains: "on: release: types: [published], permissions: read-all, sign-and-attest, id-token: write, contents: write, attestations: write, ubuntu-latest, actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683, actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065, python -m build, sigstore/gh-action-sigstore-python@04cffa1d795717b140764e8b640de88853c92acc, timeout-minutes: 5, inputs: ./dist/*.whl ./dist/*.tar.gz, release-signing-artifacts: true, actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32, subject-path: 'dist/*.whl', subject-path: 'dist/*.tar.gz', persist-credentials: false"
      min_lines: 40
    - path: "scripts/verify_release.py"
      provides: "Stdlib-only 5-check trust-chain verifier; SIGN-04 substrate; D-03 + D-04 + D-08 shape"
      contains: "EXPECTED_IDENTITY_TEMPLATE, https://github.com/Ridou/horus-os/.github/workflows/release.yml, refs/tags/{version}, EXPECTED_ISSUER, https://token.actions.githubusercontent.com, CheckResult, ok: bool | None, _print_result, check_wheel_signature, check_sdist_signature, check_tag_signature, check_sbom_signature, check_changelog_cross_ref, SKIPPED - Phase 53, argparse, --version, --cert-oidc-issuer, --check, --bundle, --artifact, sys.executable, python -m sigstore verify identity, git verify-tag, gh release view"
      min_lines: 180
      exports: ["CheckResult", "main", "check_wheel_signature", "check_sdist_signature", "check_tag_signature", "check_sbom_signature", "check_changelog_cross_ref"]
    - path: ".planning/decisions/no-pypi-in-v0.6.md"
      provides: "Documented rationale for PyPI Trusted Publishing PEP 807 deferral; SIGN-05 substrate"
      contains: "PyPI, Trusted Publishing, PEP 807, **Decision (final, until revisited):**, v0.7"
      min_lines: 30
    - path: "docs/RELEASE.md"
      provides: "STOP-BEFORE-TAG block with gitsign pre-flight (step 6.5) + signed-tag command (step 7); SIGN-03 substrate"
      contains: "6.5. Confirm `gitsign` is configured, git config --get gitsign.connectorID, docs/MAINTAINER-RUNBOOK.md, git tag -s vN.M.P"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table appends no-pypi-in-v0.6 row; SIGN-05 substrate (atomic with decision file per D-09)"
      contains: "no-pypi-in-v0.6.md, accepted (v0.6)"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "GitHub Release page (published)"
      via: "sigstore/gh-action-sigstore-python with release-signing-artifacts: true uploads .sigstore bundles + actions/attest-build-provenance writes to GH attestation API"
      pattern: "release-signing-artifacts: true, attestations: write"
    - from: "scripts/verify_release.py"
      to: ".github/workflows/release.yml @refs/tags/{version}"
      via: "EXPECTED_IDENTITY_TEMPLATE.format(version=args.version) passed to `python -m sigstore verify identity --cert-identity ...`"
      pattern: "https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/"
    - from: "docs/RELEASE.md step 6.5"
      to: "docs/MAINTAINER-RUNBOOK.md (Phase 56 forward-reference; acceptable per CONTEXT.md <specifics>)"
      via: "prose link"
      pattern: "docs/MAINTAINER-RUNBOOK.md"
    - from: ".planning/PROJECT.md key-decisions table"
      to: ".planning/decisions/no-pypi-in-v0.6.md"
      via: "Reference column with relative path link"
      pattern: ".planning/decisions/no-pypi-in-v0.6.md"
    - from: ".planning/decisions/no-pypi-in-v0.6.md (terminator block)"
      to: "tests/test_decision_no_pypi.py::test_decision_file_has_terminator"
      via: "literal substring `**Decision (final, until revisited):**`"
      pattern: "\\*\\*Decision \\(final, until revisited\\):\\*\\*"
---

<objective>
Wave 1 GREEN-flip for Phase 52: create the three NEW production files
(`.github/workflows/release.yml`, `scripts/verify_release.py`,
`.planning/decisions/no-pypi-in-v0.6.md`) and edit the two MODIFIED files
(`docs/RELEASE.md` step 6.5 insertion + step 7 command swap, `.planning/PROJECT.md`
key-decisions table append). All 10 locked decisions D-01..D-10 in CONTEXT.md
are implemented as task acceptance criteria.

Purpose: Land the v0.6 signing substrate end-to-end. After this plan, the Plan 01
RED-by-design production assertions flip GREEN (release.yml structure tests, verify_release.py
module-import + argparse tests, docs/RELEASE.md step 6.5 + tag -s tests, decision-file existence
+ terminator + cross-ref tests). Phase 53 inherits a release.yml ready to accept SBOM widening
with one whitespace-separated glob append (D-10) and a verify_release.py ready to flip its SBOM
stub from SKIPPED to active (D-08). Phase 57's release-gate extension inherits a release.yml
containing the literal `sigstore/gh-action-sigstore-python` that the `release-workflow-signing-present`
check greps for.

Output: 3 new files committed + 2 files edited, all in a coordinated set; Plan 01's
Wave-0 test suite reports the expected RED-to-GREEN transition; full pytest suite
continues green; ruff clean; em-dash-free; conventional-commit messages referencing phase 52.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md
@.planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md
@.planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md
@.planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md
@.planning/phases/52-signing-substrate-release-yml-new/52-01-PLAN.md
@.planning/phases/51-ci-hardening-substrate/51-02-SUMMARY.md
@.github/workflows/ci.yml
@docs/RELEASE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/release.yml (single sign-and-attest job; flips SIGN-01 + SIGN-02 Plan-01 RED assertions GREEN)</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/ci.yml — primary analog (post-Phase-51). Read in full. Note line 9 (top-level `permissions: read-all` at column 0; PATTERNS.md §1 Pattern A), lines 22-25 + 27-32 (SHA-pin shape with trailing `# vN.M.P` comment; PATTERNS.md §1 Pattern C), lines 34-37 (`python -m pip install --upgrade pip` pattern; PATTERNS.md §1 Pattern D), lines 45-48 (actionlint step naming convention)
    - .github/workflows/issue-claim-watcher.yml — secondary analog for per-job permissions opt-in block (PATTERNS.md §1 Pattern B); read lines 17-22 for placement of per-job `permissions:` block under the job name before `runs-on:` / `if:`
    - .planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md (Example 1 lines 452-510 — the literal release.yml file content; Standard Stack lines 89-94 — the 4 SHA-pinned actions with verified SHAs; Pitfalls 1 + 2 lines 375-403 — the mitigations bound by this file)
    - .planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md (§1 lines 31-134 — every release.yml pattern A-F; §S-3 lines 865-874 — SHA-pin convention; §S-4 lines 876-881 — em-dash prohibition; §S-5 lines 883-895 — comment-skip discipline)
    - .planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md (D-01, D-02, D-06, D-10; <specifics> on release.published trigger + step ordering + .sigstore bundle requirement)
    - .planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md (rows 1 + 2 of Per-Task Verification Map; Wave 0 Requirements bullet 1)
    - tests/test_release_yml_structure.py (just created by Plan 01) — the 9 production assertions this file must satisfy; the literals tested are the load-bearing identifiers
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py + test_ci_hardening_workflow_structure.py — the Phase 51 PR-time gate that runs against every workflow including this new one (no opt-out; release.yml MUST satisfy CIHARD-02 + CIHARD-03 + CIHARD-04 automatically)
  </read_first>
  <action>
    Create `.github/workflows/release.yml` byte-equivalent to RESEARCH.md Example 1 (lines 452-510). Authoritative target values:

    Trigger block: `on:\n  release:\n    types: [published]` (ONLY this trigger; no push:tags, no workflow_dispatch).

    Top-level permissions: `permissions: read-all` at column 0, placed above `jobs:` (matches ci.yml line 9 placement).

    Job structure: single job named `sign-and-attest`, `name: sign-and-attest`, `runs-on: ubuntu-latest`. Per-job permissions block under the job key (above runs-on, mirroring issue-claim-watcher.yml:17-22 placement):
      ```
      permissions:
        id-token: write
        contents: write
        attestations: write
      ```

    Steps in this exact order (load-bearing per CONTEXT.md <specifics> "Step ordering inside sign-and-attest is load-bearing"):
      1. `name: Check out repository` -> `uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2` -> `with: persist-credentials: false`
      2. `name: Set up Python 3.12` -> `uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0` -> `with: python-version: "3.12"`
      3. `name: Install build` -> `run: |\n  python -m pip install --upgrade pip\n  python -m pip install build` (NO `[dev]` extras; NO sigstore in pyproject.toml per D-03)
      4. `name: Build wheel and sdist` -> `run: python -m build`
      5. `name: Sign artifacts (sigstore keyless OIDC)` -> `uses: sigstore/gh-action-sigstore-python@04cffa1d795717b140764e8b640de88853c92acc # v3.3.0` -> `timeout-minutes: 5` (PITFALL 2 / SIGN-01 OIDC budget) -> `with: inputs: ./dist/*.whl ./dist/*.tar.gz` AND `release-signing-artifacts: true` (D-10 exact two-glob whitespace form; Phase 53 will append ` ./dist/*.sbom.json`)
      6. `name: Attest wheel (SLSA Build L2 provenance)` -> `uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0` -> `with: subject-path: 'dist/*.whl'`
      7. `name: Attest sdist (SLSA Build L2 provenance)` -> `uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0` -> `with: subject-path: 'dist/*.tar.gz'`

    File-level requirements:
    - Top-level `name: release` (matches RESEARCH.md Example 1 line 459)
    - Every `uses:` line is SHA-pinned to 40-char hex with a trailing `# vN.M.P` comment (per PATTERNS.md §S-3 + Phase 51 pinact convention)
    - NO `${{ github.event.* }}`, `${{ github.head_ref }}`, or `${{ github.base_ref }}` in any `run:` shell (PATTERNS.md §1 Pattern E; CIHARD-03 second clause enforced by Phase 51 actionlint + Phase 51 test_ci_hardening_workflow_structure.py); the release-published trigger exposes `github.event.release.tag_name` but it MUST NOT enter a shell — sigstore reads OIDC internally and attest-build-provenance reads subject-path from `with:` block
    - NO em-dashes in any comment (CLAUDE.md HR3; S-4)
    - NO secrets reference (`secrets.*`) — GITHUB_TOKEN is auto-provisioned by GH Actions per CONTEXT.md Runtime State Inventory; no project-managed secret name
    - The 4 verified SHAs from RESEARCH.md Standard Stack (lines 89-94) are the authoritative reference values; do NOT resolve fresh SHAs unless `gh api repos/{owner}/{repo}/git/refs/tags/{tag} --jq '.object.sha'` for any of the 4 actions returns a DIFFERENT value (in which case re-verify per the Verified-SHA procedure in RESEARCH.md lines 142-152 and update the plan SUMMARY with the corrected SHA)

    Behavioral mitigations encoded:
    - PITFALL 1 (wildcard identity): release.yml does NOT carry identity matching; that lives in verify_release.py. Nothing to enforce here.
    - PITFALL 2 (OIDC TTL): `timeout-minutes: 5` on the sigstore step bounds the failure mode; build (step 4) precedes sign (step 5) with no tests/network between them.
    - PITFALL 3 (.sigstore bundle): `release-signing-artifacts: true` emits `.sigstore` bundles (NOT detached `.sig` + `.crt`); this is the action default but setting explicitly is self-documenting.
    - PITFALL 11 (release rehearsal): release.yml will be first exercised on v0.6.0-rc1 on a fork (D-07 + CONTEXT.md <specifics>).
  </action>
  <verify>
    <automated>pytest tests/test_release_yml_structure.py tests/test_contribution_gate_pitfalls/ -v 2>&amp;1 | grep -E "passed|failed"</automated>
    Run additionally: `actionlint .github/workflows/release.yml` (must exit 0; same actionlint version Phase 51 pinned at raven-actions/actionlint@v2.1.2 / actionlint v1.7.12) and re-verify the SHAs:
    ```
    gh api repos/sigstore/gh-action-sigstore-python/git/refs/tags/v3.3.0 --jq '.object.sha'
    gh api repos/actions/attest-build-provenance/git/refs/tags/v4.1.0 --jq '.object.sha'
    gh api repos/actions/checkout/git/refs/tags/v4.2.2 --jq '.object.sha'
    gh api repos/actions/setup-python/git/refs/tags/v5.6.0 --jq '.object.sha'
    ```
    Each must match the SHAs hardcoded in release.yml.
  </verify>
  <acceptance_criteria>
    - File .github/workflows/release.yml exists
    - `grep -c 'release:' .github/workflows/release.yml` >= 1 AND `grep -c 'types: \[published\]' .github/workflows/release.yml` == 1
    - `grep -c '^permissions: read-all$' .github/workflows/release.yml` == 1 (column-0 top-level placement)
    - `grep -c 'id-token: write' .github/workflows/release.yml` == 1 AND `grep -c 'contents: write' .github/workflows/release.yml` == 1 AND `grep -c 'attestations: write' .github/workflows/release.yml` == 1
    - `grep -c 'sign-and-attest' .github/workflows/release.yml` >= 1 AND `grep -c 'runs-on: ubuntu-latest' .github/workflows/release.yml` == 1
    - `grep -c 'sigstore/gh-action-sigstore-python@04cffa1d795717b140764e8b640de88853c92acc' .github/workflows/release.yml` == 1
    - `grep -c 'actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32' .github/workflows/release.yml` == 2 (per-artifact: one wheel, one sdist; D-06)
    - `grep -c 'actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683' .github/workflows/release.yml` == 1
    - `grep -c 'actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065' .github/workflows/release.yml` == 1
    - `grep -c 'persist-credentials: false' .github/workflows/release.yml` == 1 (CIHARD-03)
    - `grep -c 'timeout-minutes: 5' .github/workflows/release.yml` == 1 (PITFALL 2 / SIGN-01 5-minute OIDC budget)
    - `grep -c "inputs: \./dist/\*.whl \./dist/\*.tar.gz" .github/workflows/release.yml` == 1 (D-10 exact two-glob form)
    - `grep -c 'release-signing-artifacts: true' .github/workflows/release.yml` == 1
    - `grep -cE "subject-path: 'dist/\*.whl'" .github/workflows/release.yml` == 1 AND `grep -cE "subject-path: 'dist/\*.tar.gz'" .github/workflows/release.yml` == 1
    - `grep -v '^[[:space:]]*#' .github/workflows/release.yml | grep -c '\${{ github\.event\.'` == 0 (no event interpolation outside comments; CIHARD-03 second clause)
    - `grep -Pl '[\x{2013}\x{2014}]' .github/workflows/release.yml` returns no matches (no em-dashes / en-dashes; CLAUDE.md HR3)
    - All 9 production tests in `pytest tests/test_release_yml_structure.py -v` PASS (flips Plan 01 RED assertions GREEN for SIGN-01 + SIGN-02)
    - The Phase 51 PR-time gate `pytest tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py -v` continues to PASS (release.yml passes the existing CIHARD-02 + CIHARD-03 + CIHARD-04 invariants automatically because it satisfies them all by construction)
    - `actionlint .github/workflows/release.yml` exits 0
  </acceptance_criteria>
  <done>release.yml committed (`feat(52): add release.yml signing substrate (SIGN-01 + SIGN-02)`); the 9 SIGN-01 + SIGN-02 production assertions in tests/test_release_yml_structure.py flip GREEN; Phase 51 PR-time gate continues GREEN; actionlint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scripts/verify_release.py (stdlib-only 5-check skeleton with SBOM stub; flips SIGN-04 Plan-01 RED assertions GREEN)</name>
  <files>scripts/verify_release.py</files>
  <read_first>
    - scripts/release_gate.py — primary analog. Read lines 1-93 (module docstring with check enumeration + stdlib-only declaration; PATTERNS.md §2 Pattern A), lines 95-135 (imports + REPO_ROOT + module-level constants; PATTERNS.md §2 Pattern B), lines 137-148 (CheckResult dataclass; PATTERNS.md §2 Pattern C), lines 307-330 (subprocess-with-capture pattern; PATTERNS.md §2 Pattern D), lines 634-640 (_print_result formatter; PATTERNS.md §2 Pattern E), lines 643-675 (HORUS_OS_*_PATH_OVERRIDE env-var pattern; PATTERNS.md §2 Pattern G), lines 678-766 (argparse + dispatch + main; PATTERNS.md §2 Pattern F)
    - scripts/lint_no_wallclock.py lines 1-46 — fallback minimal stdlib-only shape (PATTERNS.md §2 Pattern H) for the `_assert_sigstore_cli_available()` install-hint branch
    - .planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md (Example 2 lines 520-764 — the full skeleton this file matches; Pitfall 1 lines 375-389 — the wildcard-identity bug class the module-import assert defends against)
    - .planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md (§2 lines 138-340; §S-1 cross-OS subprocess; §S-2 pathlib-only)
    - .planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md (D-03, D-04, D-08; <specifics> on EXPECTED_IDENTITY shape rules and 0.6.0-rc1 canonical fixture)
    - .planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md (row 4; Wave 0 Requirements bullet 1)
    - tests/test_release_verification.py (just created by Plan 01) — the production tests this file must satisfy; the literals tested are the load-bearing exports + constants
  </read_first>
  <action>
    Create `scripts/verify_release.py` modeled on RESEARCH.md Example 2 (lines 520-764) and PATTERNS.md §2 Patterns A-G. Stdlib-only (no third-party imports). Target ~180-250 lines.

    Module docstring (PATTERNS.md §2 Pattern A): open with the 5-check enumeration (wheel-signature / sdist-signature / tag-signature / sbom-signature [SKIPPED until Phase 53] / changelog-cross-ref); close with the stdlib-only declaration verbatim in shape — list every stdlib module used (argparse, dataclasses, os, pathlib, re, subprocess, sys); state that sigstore, git, gh are external CLIs invoked via subprocess. Reference CONTEXT.md D-03 + D-04 + D-08 by name and SIGN-04 by requirement ID. No em-dashes (CLAUDE.md HR3).

    Imports + module-level constants (PATTERNS.md §2 Pattern B):
      ```
      from __future__ import annotations
      import argparse, os, re, subprocess, sys
      from dataclasses import dataclass
      from pathlib import Path
      ```
      `REPO_ROOT = Path(__file__).resolve().parent.parent`
      `EXPECTED_IDENTITY_TEMPLATE = "https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/{version}"`
      `EXPECTED_ISSUER = "https://token.actions.githubusercontent.com"`
      Module-import-time assert: `assert "refs/tags/{version}" in EXPECTED_IDENTITY_TEMPLATE, "EXPECTED_IDENTITY_TEMPLATE corrupted: must contain 'refs/tags/{version}'"` (PITFALL 1 / D-04 defense against accidental edits)

    `@dataclass(frozen=True) class CheckResult` copied verbatim from release_gate.py:137-148 (`name: str`, `ok: bool | None`, `diagnostic: str`).

    `def _print_result(result: CheckResult) -> None` copied verbatim from release_gate.py:634-640 (OK/FAIL/SKIP prefix).

    `def _assert_sigstore_cli_available() -> CheckResult | None` per RESEARCH.md Example 2 lines 594-622. Returns None when `[sys.executable, "-m", "sigstore", "--version"]` exits 0; returns failing CheckResult otherwise with diagnostic ending `"Install hint: pip install sigstore"`. Use `capture_output=True, text=True, check=False, timeout=10`. Catch FileNotFoundError and OSError (S-1 cross-OS).

    Five check functions (PATTERNS.md §2 Pattern D + RESEARCH.md Example 2 lines 625-713):
      - `def check_wheel_signature(version, cert_oidc_issuer, bundle_path, artifact_path) -> CheckResult`: shells out to `[sys.executable, "-m", "sigstore", "verify", "identity", "--cert-identity", EXPECTED_IDENTITY_TEMPLATE.format(version=version), "--cert-oidc-issuer", cert_oidc_issuer, "--bundle", str(bundle_path), str(artifact_path)]` with `cwd=str(REPO_ROOT), capture_output=True, text=True, check=False, timeout=60`. On returncode == 0 returns CheckResult(name="wheel-signature", ok=True, diagnostic=f"verified against {expected_identity}"). On failure tails last 3 stderr lines into diagnostic.
      - `def check_sdist_signature(version, cert_oidc_issuer, bundle_path, artifact_path) -> CheckResult`: same shape as wheel; name="sdist-signature".
      - `def check_tag_signature(version) -> CheckResult`: shells out to `["git", "verify-tag", f"v{version}"]` per RESEARCH.md Example 2 lines 672-697; name="tag-signature". timeout=30.
      - `def check_sbom_signature(version) -> CheckResult`: STUB returning `CheckResult(name="sbom-signature", ok=None, diagnostic="SKIPPED - Phase 53 lands SBOM generation + signing")` per D-08. (Use ASCII hyphen-space-hyphen, NOT em-dash; CLAUDE.md HR3.)
      - `def check_changelog_cross_ref(version) -> CheckResult`: shells out to `["gh", "release", "view", f"v{version}", "--json", "body", "--jq", ".body"]`; if returncode != 0, diagnostic must hint `"Install hint: https://cli.github.com/"`. Then read REPO_ROOT/"CHANGELOG.md" with encoding="utf-8" and use regex `re.compile(rf"^## \[{re.escape(version)}\]", re.MULTILINE)` to extract the section; whitespace-normalize both texts (`"\n".join(line.strip() for line in text.splitlines() if line.strip())`) and assert the gh body is contained in the CHANGELOG section (or vice versa; planner picks the direction that matches RESEARCH.md Example 2 line 711 — "asserts the body matches the [N.M.P] - YYYY-MM-DD section"). Return CheckResult(name="changelog-cross-ref", ok=True/False, diagnostic=...).

    Optional env-var overrides per PATTERNS.md §2 Pattern G (used by test_release_verification.py via monkeypatch.setenv):
      - `def _resolved_bundle_path(cli_arg) -> Path | None`: check HORUS_OS_VERIFY_RELEASE_BUNDLE_OVERRIDE first, then cli_arg, then None.
      - `def _resolved_artifact_path(cli_arg) -> Path | None`: same for HORUS_OS_VERIFY_RELEASE_ARTIFACT_OVERRIDE.

    `def main(argv: list[str] | None = None) -> int` (PATTERNS.md §2 Pattern F + RESEARCH.md Example 2 lines 716-759):
      - argparse: `--version` required=True (validate against `re.compile(r"^\d+\.\d+\.\d+(-rc\d+)?$")` and parser.error if not match); `--cert-oidc-issuer` required=True; `--check choices=("wheel", "sdist", "tag", "sbom", "changelog") default=None`; `--bundle type=Path default=None`; `--artifact type=Path default=None`.
      - After args parsed: if `args.cert_oidc_issuer != EXPECTED_ISSUER`: print `f"FAIL  verify_release: --cert-oidc-issuer must equal {EXPECTED_ISSUER!r}, got {args.cert_oidc_issuer!r}"` to stderr and return 1 (D-04 hard rule).
      - Resolve bundle + artifact via the env-var helpers.
      - Dispatch list mirroring release_gate.py:716-759: `if args.check in (None, "wheel"): results.append(check_wheel_signature(args.version, args.cert_oidc_issuer, resolved_bundle, resolved_artifact))`; same for sdist, tag, sbom, changelog.
      - For wheel + sdist + changelog checks that need bundle/artifact/network, if `resolved_bundle is None` for wheel/sdist, skip with `CheckResult(name="<name>", ok=None, diagnostic="SKIPPED - no --bundle / no HORUS_OS_VERIFY_RELEASE_BUNDLE_OVERRIDE; pass --bundle PATH for fixture-mode verification")` so the test_full_run_all_checks test from Plan 01 passes when fixtures are missing (CheckResult with ok=None does NOT count as failure per release_gate.py precedent and per `any(r.ok is False for r in results)` semantics).
      - For loop: `for result in results: _print_result(result)`.
      - Return `1 if any(r.ok is False for r in results) else 0`.

    Footer: `if __name__ == "__main__": raise SystemExit(main())` (mirrors release_gate.py:765 + RESEARCH.md Example 2 lines 762-763).

    Cross-OS discipline (S-1, S-2 from PATTERNS.md): `sys.executable` not bare `"python"`; `str(path)` everywhere subprocess args expect strings; `capture_output=True, text=True, check=False` everywhere; `encoding="utf-8"` on every read_text. NO em-dashes (S-4 + CLAUDE.md HR3). NO third-party imports.
  </action>
  <verify>
    <automated>pytest tests/test_release_verification.py -v 2>&amp;1 | grep -E "passed|failed|skipped"</automated>
    Also run: `python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://token.actions.githubusercontent.com --check sbom` (must exit 0 with `SKIP  sbom-signature: SKIPPED - Phase 53 lands SBOM generation + signing` on stdout). Run `python scripts/verify_release.py --version 0.6.0-rc1` (must exit 2 with argparse error about --cert-oidc-issuer required). Run `python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://example.com/oauth --check sbom` (must exit 1 with FAIL diagnostic about EXPECTED_ISSUER mismatch). Run `ruff check scripts/verify_release.py && ruff format --check scripts/verify_release.py` (both exit 0).
  </verify>
  <acceptance_criteria>
    - File scripts/verify_release.py exists
    - `python -c "import importlib.util; s=importlib.util.spec_from_file_location('m', 'scripts/verify_release.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print('OK')"` prints OK (module-import-time assert does not fire on the byte-correct EXPECTED_IDENTITY_TEMPLATE)
    - `grep -c 'EXPECTED_IDENTITY_TEMPLATE = (' scripts/verify_release.py` >= 1 OR `grep -c 'EXPECTED_IDENTITY_TEMPLATE =' scripts/verify_release.py` >= 1
    - `grep -c 'https://github.com/Ridou/horus-os/.github/workflows/release.yml' scripts/verify_release.py` >= 1
    - `grep -c 'refs/tags/{version}' scripts/verify_release.py` >= 2 (once in the template constant, once in the import-time assert)
    - `grep -c 'EXPECTED_ISSUER = "https://token.actions.githubusercontent.com"' scripts/verify_release.py` == 1
    - `grep -c '@dataclass(frozen=True)' scripts/verify_release.py` >= 1
    - `grep -c 'def check_wheel_signature' scripts/verify_release.py` == 1
    - `grep -c 'def check_sdist_signature' scripts/verify_release.py` == 1
    - `grep -c 'def check_tag_signature' scripts/verify_release.py` == 1
    - `grep -c 'def check_sbom_signature' scripts/verify_release.py` == 1
    - `grep -c 'def check_changelog_cross_ref' scripts/verify_release.py` == 1
    - `grep -c 'SKIPPED' scripts/verify_release.py` >= 1 AND `grep -c 'Phase 53' scripts/verify_release.py` >= 1
    - `grep -c 'sys.executable' scripts/verify_release.py` >= 2 (subprocess discipline; S-1)
    - `grep -c 'python -m' scripts/verify_release.py` >= 1 AND `grep -c 'sigstore' scripts/verify_release.py` >= 2 (the subprocess invocation + install hint)
    - `grep -c 'git verify-tag' scripts/verify_release.py` == 1
    - `grep -c 'gh release view' scripts/verify_release.py` == 1
    - `grep -c "choices=(\"wheel\", \"sdist\", \"tag\", \"sbom\", \"changelog\")" scripts/verify_release.py` == 1 OR an equivalent argparse choices tuple containing exactly those 5 names
    - `grep -c 'required=True' scripts/verify_release.py` >= 2 (--version AND --cert-oidc-issuer both required)
    - `grep -v '^[[:space:]]*#\|^[[:space:]]*"""\|^[[:space:]]*"' scripts/verify_release.py | grep -cE 'from sigstore|import sigstore' ` == 0 (sigstore is NEVER imported; only shelled out to per D-03)
    - `python -c "import sys; sys.path.insert(0, 'scripts'); import importlib; m = __import__('importlib').import_module('verify_release'); assert m.EXPECTED_IDENTITY_TEMPLATE == 'https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/{version}'; assert m.EXPECTED_ISSUER == 'https://token.actions.githubusercontent.com'; print('OK')"` prints OK
    - `python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://token.actions.githubusercontent.com --check sbom` exits 0 with stdout containing `SKIP` AND `sbom-signature` AND `Phase 53`
    - `python scripts/verify_release.py --version 0.6.0-rc1` exits 2 (argparse error: --cert-oidc-issuer required)
    - `python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://example.com/oauth` exits 1 with stderr containing `https://token.actions.githubusercontent.com`
    - `grep -Pl '[\x{2013}\x{2014}]' scripts/verify_release.py` returns no matches (no em-dashes / en-dashes)
    - `ruff check scripts/verify_release.py` exits 0
    - `ruff format --check scripts/verify_release.py` exits 0
    - Plan 01's SIGN-04 production tests in tests/test_release_verification.py flip GREEN: test_missing_issuer_refused PASSES; test_wrong_issuer_refused PASSES; test_sbom_stub_returns_skipped PASSES; test_module_imports_cleanly PASSES; test_expected_identity_template_invariant_is_documented PASSES; test_expected_issuer_constant_documented PASSES. test_canonical_fixture_passes_wheel_check + test_full_run_all_checks_with_canonical_fixture remain SKIPPED until v0.6.0-rc1 rehearsal binaries land (acceptable; documented in Plan 01 acceptance criteria).
    - `pyproject.toml` is UNCHANGED (verify with `git status pyproject.toml` returning clean); sigstore is NOT added to [project.dependencies] OR [dev] extras (D-03 hard rule)
  </acceptance_criteria>
  <done>scripts/verify_release.py committed (`feat(52): add verify_release.py 5-check trust-chain verifier (SIGN-04)`); Plan 01's SIGN-04 production tests (excluding canonical-fixture tests pending rehearsal binaries) flip GREEN; pyproject.toml unchanged; ruff clean; CLI rejects missing/wrong issuer; SBOM check returns SKIPPED diagnostic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create .planning/decisions/no-pypi-in-v0.6.md + edit docs/RELEASE.md (step 6.5 + tag -s) + edit .planning/PROJECT.md (key-decisions append) in ONE atomic commit</name>
  <files>.planning/decisions/no-pypi-in-v0.6.md, docs/RELEASE.md, .planning/PROJECT.md</files>
  <read_first>
    - docs/RELEASE.md (read in full) — pre-edit state to be preserved byte-identical for steps 1-6 (lines 122-141) and steps 8-9 (lines 145-157); step 6.5 inserts between lines 141 and 142; step 7 (lines 142-144) gets the literal `tag -a` -> `tag -s` substitution
    - .planning/PROJECT.md lines 74-81 — existing Key Decisions table with 3-column shape `| Decision | Rationale | Status |`; this is the table that gets the appended row (NOT a new table; existing rows MUST NOT be reordered or modified)
    - .planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md (Code Examples §5 lines 935-942 — full no-pypi-in-v0.6.md template prose; §"PROJECT.md key-decisions table exact format" lines 313-332 — the exact row shape for `.planning/PROJECT.md` table; Pattern 2 lines 266-286 — exact RELEASE.md step 6.5 prose)
    - .planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md (§3 lines 343-411 — no-pypi-in-v0.6.md prose shape + discipline checklist; §9 lines 750-782 — RELEASE.md post-edit shape with byte-identity invariant; §10 lines 786-818 — PROJECT.md key-decisions append shape)
    - .planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md (D-05, D-09; <code_context> on RELEASE.md step 6 -> step 6.5 -> step 7 boundary; <code_context> on PROJECT.md key-decisions table single-row append)
    - .planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md (rows 3 + 5)
    - tests/test_release_md_stop_before_tag.py (just created by Plan 01) — the 3 production tests this docs edit must satisfy (PRE_EDIT_STEPS_1_THROUGH_6, PRE_EDIT_STEPS_8_THROUGH_9, gitsign connectorID literal, git tag -s literal, MAINTAINER-RUNBOOK reference)
    - tests/test_decision_no_pypi.py (just created by Plan 01) — the 3 production tests this decision file + PROJECT.md edit must satisfy (file existence, terminator literal, cross-ref to `.planning/PROJECT.md`)
  </read_first>
  <action>
    Land THREE coordinated changes in ONE atomic commit per D-09 (`feat(52): land no-pypi-in-v0.6 decision file + docs/RELEASE.md gitsign + PROJECT.md key-decisions (SIGN-03 + SIGN-05)`).

    (A) Create `.planning/decisions/no-pypi-in-v0.6.md` using the RESEARCH.md Code Examples §5 template (lines 935-942) + PATTERNS.md §3 prose discipline (lines 343-411). Required structure:
     - `# Decision: no PyPI publishing in v0.6` H1 heading
     - `**Status:** OUT for v0.6 (revisited no earlier than v0.7).` line (do NOT include `**Date:**` or `**Owner:**` lines if they would carry PII; date can be the calendar date the commit lands; owner can be omitted entirely per CLAUDE.md HR1 "no personal information about any contributor")
     - `## Context` section (3-5 sentences) describing the v0.6 contribution gate substrate and the PEP 807 question
     - `## Decision criteria` section (4 numbered points: horus-os does not currently publish to PyPI; no PYPI_API_TOKEN secret exists; v0.6 milestone goal is contribution gate not distribution channel; PEP 807 wiring would introduce release-time failure modes unrelated to the contribution gate)
     - `## Decision (final, until revisited)` H2 heading immediately followed by a `**Decision (final, until revisited):**` line containing the LOAD-BEARING terminator literal (test_decision_no_pypi.py greps for exactly this string)
     - Then 1-2 paragraphs stating the decision: "PyPI Trusted Publishing (PEP 807) is OUT OF SCOPE for v0.6. v0.7 (or a later milestone) may revisit when..."
     - `## References` section listing PEP 807 URL, pypa/gh-action-pypi-publish URL, Phase 52 CONTEXT.md D-09 reference, Phase 52 RESEARCH.md Standard Stack reference
     - File length 30-200 lines; no em-dashes (CLAUDE.md HR3 + S-4); no PII other than `Ridou/horus-os`; placeholder values like `your-project-name` where the PyPI name would appear; no trailing whitespace.

    (B) Edit `docs/RELEASE.md` per D-05 + PATTERNS.md §9 lines 750-782. Make TWO surgical edits:
     - INSERT step 6.5 between current line 141 (end of step 6: `   matrix (\`gh run list --branch main --limit 1\`).`) and current line 142 (start of step 7: `7. Create the annotated tag:`). New step 6.5 text per RESEARCH.md Pattern 2 lines 268-278 / PATTERNS.md §9 lines 770-776:
       ```
       6.5. Confirm `gitsign` is configured. Run
            `git config --get gitsign.connectorID` and confirm the
            output is non-empty (typically
            `https://github.com/login/oauth`). If empty, follow the
            one-time gitsign setup in `docs/MAINTAINER-RUNBOOK.md`.
            Do not proceed to step 7 until configured.
       ```
       (Continuation lines indented with 5 spaces matching the `6.5. ` prefix width; this is the Markdown convention for the existing numbered-list continuations in the file.)
     - SWAP step 7 command: change `git tag -a vN.M.P -m "vN.M.P - <milestone-name>"` to `git tag -s vN.M.P -m "vN.M.P - <milestone-name>"`. ONLY the `-a` -> `-s` substitution; surrounding prose (`Create the annotated tag:`, `Push: git push origin vN.M.P.`) remains BYTE-IDENTICAL.
     - DO NOT touch steps 1-6 (lines 122-141), steps 8-9 (lines 145-157), or any other content in the file. The byte-identity invariant is enforced by tests/test_release_md_stop_before_tag.py::test_steps_1_through_6_and_8_through_9_byte_identical. Use diff-style precision; do not "improve" surrounding wording.
     - NOTE on the step 7 prose: "Create the annotated tag" remains as the introductory phrase even though `-s` produces a SIGNED tag (which is also annotated, since git's `-s` implies `-a`). Per CONTEXT.md D-05 "swap the literal command from ... to ..." the swap is ONLY the command literal; the prose label "annotated" remains unchanged to preserve byte-identity of the surrounding text. Phase 56 may later refine this prose if the maintainer chooses; Phase 52 keeps the minimal-diff discipline.

    (C) Edit `.planning/PROJECT.md` per D-09 + PATTERNS.md §10 lines 786-818 + RESEARCH.md §"PROJECT.md key-decisions table exact format" lines 313-332. Append ONE row to the existing Key Decisions table at lines 74-81 (`| Decision | Rationale | Status |` shape). New row exactly:
       ```
       | No PyPI publishing in v0.6 (Trusted Publishing PEP 807 deferred) | horus-os does not currently publish to PyPI. Standing up Trusted Publishing requires a PyPI project + maintainer setup outside the v0.6 contribution-gate scope. Decision documented in `.planning/decisions/no-pypi-in-v0.6.md`. | accepted (v0.6) |
       ```
     - The row appends to the end of the existing table (currently 4 data rows; this becomes the 5th). Existing rows are NOT reordered or modified (CONTEXT.md D-09 + PATTERNS.md §10 byte-identity invariant).
     - The Reference column MUST contain the literal `.planning/decisions/no-pypi-in-v0.6.md` string (this is what test_decision_no_pypi.py::test_project_md_references_decision_file greps for).
     - Status column uses `accepted (v0.6)` matching the existing rows' `accepted (vX.Y)` convention.
     - NO em-dashes anywhere in the new row (`deferred` not `—`; CLAUDE.md HR3 + S-4).
     - NO new "Status" column value of "deferred"; the existing column shape is `| Decision | Rationale | Status |` and status values are `accepted (vX.Y)`. The "deferred" notion is captured in the Decision column text "PEP 807 deferred", NOT in the Status column.

    All three edits MUST land in ONE COMMIT per D-09 (SIGN-05 atomicity). Pre-commit verification: `git diff --stat` should report exactly 3 files changed: `.planning/decisions/no-pypi-in-v0.6.md` (NEW), `docs/RELEASE.md` (small insert + 1-char swap), `.planning/PROJECT.md` (1-row table append).
  </action>
  <verify>
    <automated>pytest tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py -v 2>&amp;1 | grep -E "passed|failed"</automated>
    Also verify: `git log -1 --name-only` shows all 3 files in the SAME commit; `grep -c "git tag -a" docs/RELEASE.md` == 0 (the swap is complete); `ruff check . && ruff format --check .` exits 0 (decision file is Markdown, RELEASE.md and PROJECT.md are Markdown, none are Python — ruff only checks Python; this is a sanity check that no incidental Python edit slipped in).
  </verify>
  <acceptance_criteria>
    - File .planning/decisions/no-pypi-in-v0.6.md exists
    - .planning/decisions/no-pypi-in-v0.6.md contains the H1 heading `# Decision: no PyPI publishing in v0.6`
    - .planning/decisions/no-pypi-in-v0.6.md contains the literal terminator `**Decision (final, until revisited):**` (load-bearing for test_decision_no_pypi.py::test_decision_file_has_terminator)
    - .planning/decisions/no-pypi-in-v0.6.md contains the literals `PEP 807`, `Trusted Publishing`, `v0.7`
    - `wc -l .planning/decisions/no-pypi-in-v0.6.md` reports between 30 and 200 lines (PATTERNS.md §3 length discipline)
    - `grep -Pl '[\x{2013}\x{2014}]' .planning/decisions/no-pypi-in-v0.6.md` returns no matches (no em-dashes / en-dashes; CLAUDE.md HR3)
    - `grep -E 'santino62|@gmail|santino62@gmail' .planning/decisions/no-pypi-in-v0.6.md` returns no matches (CLAUDE.md HR1 no PII)
    - docs/RELEASE.md contains the literal `6.5. Confirm \`gitsign\` is configured.`
    - docs/RELEASE.md contains the literal `git config --get gitsign.connectorID`
    - docs/RELEASE.md contains the literal `docs/MAINTAINER-RUNBOOK.md` (forward-reference per CONTEXT.md <specifics>; Phase 56 lands this file)
    - docs/RELEASE.md contains the literal `git tag -s vN.M.P -m "vN.M.P - <milestone-name>"`
    - `grep -c 'git tag -a vN.M.P' docs/RELEASE.md` == 0 (the swap is complete; no leftover `-a` form)
    - docs/RELEASE.md byte-identity invariant: the text `6. Push to \`main\`. Wait for CI green on the full 3-OS x 2-Python\n   matrix (\`gh run list --branch main --limit 1\`).` is present unchanged; the text `8. Publish the GitHub Release. Extract the new CHANGELOG section` through line 157 is present unchanged
    - .planning/PROJECT.md contains the literal `.planning/decisions/no-pypi-in-v0.6.md` (load-bearing for test_decision_no_pypi.py::test_project_md_references_decision_file)
    - .planning/PROJECT.md contains the literal `No PyPI publishing in v0.6` AND `accepted (v0.6)`
    - .planning/PROJECT.md existing Key Decisions table rows (lines 76-81 pre-edit) are present byte-identical post-edit (only ONE new row appended after the Apache 2.0 license row)
    - `grep -Pl '[\x{2013}\x{2014}]' .planning/PROJECT.md docs/RELEASE.md` returns no matches (no em-dashes introduced by the edits; CLAUDE.md HR3)
    - All 3 production tests in `pytest tests/test_release_md_stop_before_tag.py -v` PASS (flips Plan 01 RED assertions GREEN for SIGN-03)
    - All 3 production tests in `pytest tests/test_decision_no_pypi.py -v` PASS (flips Plan 01 RED assertions GREEN for SIGN-05)
    - `git log -1 --name-only HEAD` shows exactly 3 files: `.planning/decisions/no-pypi-in-v0.6.md`, `docs/RELEASE.md`, `.planning/PROJECT.md` (D-09 single-commit atomicity)
    - The commit message follows the conventional-commit + present-tense + phase-prefix shape per CLAUDE.md workflow hard rule 6 (e.g., `feat(52): land no-pypi-in-v0.6 decision file + docs/RELEASE.md gitsign + PROJECT.md key-decisions (SIGN-03 + SIGN-05)`); no em-dashes in the commit message body
  </acceptance_criteria>
  <done>The three coordinated changes committed atomically; Plan 01's SIGN-03 + SIGN-05 production tests flip GREEN; docs/RELEASE.md preserves the byte-identity invariant for steps 1-6 and 8-9; .planning/PROJECT.md existing key-decisions table rows unchanged; decision file ends with the load-bearing terminator literal.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full Wave 1 GREEN-flip verification:

```bash
# All four Plan 01 test files now flip from RED to GREEN
pytest tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py -v

# Phase 51 PR-time gate continues green (release.yml passes by construction)
pytest tests/test_contribution_gate_pitfalls/ -v

# Verify ruff and actionlint
ruff check . && ruff format --check .
actionlint .github/workflows/release.yml

# Verify_release.py CLI smoke checks
python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://token.actions.githubusercontent.com --check sbom
python scripts/verify_release.py --version 0.6.0-rc1
python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://example.com/oauth
echo "exit codes captured: SBOM check exits 0; missing-issuer exits 2; wrong-issuer exits 1"

# Verify pyproject.toml unchanged (sigstore NOT added)
git diff pyproject.toml  # must be empty

# Full suite no-regression
pytest -v 2>&1 | tail -20

# D-09 atomicity check (Task 3 commit only)
git log -1 --name-only $(git log --oneline | grep -F "SIGN-03 + SIGN-05" | head -1 | awk '{print $1}')
```

Expected end state:
- ALL Plan-01 production assertions flip GREEN (release.yml structure, verify_release.py module/argparse/SBOM-stub, docs/RELEASE.md step 6.5 + tag -s, decision-file + cross-ref). Canonical-fixture tests remain SKIPPED until v0.6.0-rc1 rehearsal recording (Manual-Only per VALIDATION.md row 1).
- Plan-01 non-vacuity scanner tests continue to PASS.
- Phase 51 PR-time gate continues to PASS (release.yml satisfies CIHARD-02 + CIHARD-03 + CIHARD-04 by construction).
- Existing 1011-test suite continues to PASS on Ubuntu/macOS/Windows x Python 3.11/3.12.
- pyproject.toml is byte-identical to pre-Phase-52 (sigstore NOT added; D-03 zero-base-dep hard rule).
- ruff + actionlint clean.
- No em-dashes in any new or modified file.
- D-09 atomicity verified (one commit for decision file + RELEASE.md edits + PROJECT.md append).
</verification>

<success_criteria>
- 3 new files created (.github/workflows/release.yml, scripts/verify_release.py, .planning/decisions/no-pypi-in-v0.6.md) + 2 files modified (docs/RELEASE.md, .planning/PROJECT.md)
- All 10 locked decisions D-01..D-10 implemented as task acceptance criteria
- All 5 SIGN-NN requirements covered (SIGN-01 + SIGN-02 by Task 1; SIGN-04 by Task 2; SIGN-03 + SIGN-05 by Task 3)
- Plan 01's RED-by-design production assertions flip GREEN (verified by re-running Plan-01's test files)
- pyproject.toml byte-identical to pre-phase (D-03 zero-base-dep hard rule)
- D-09 atomicity preserved (decision file + PROJECT.md append + docs/RELEASE.md edits all in ONE commit per Task 3)
- Phase 51 PR-time gate continues GREEN (release.yml satisfies CIHARD-02 + CIHARD-03 + CIHARD-04 by construction; verified via tests/test_contribution_gate_pitfalls/)
- ruff clean, actionlint clean, no em-dashes, no PII, conventional-commit messages referencing phase 52
- Existing 1011-test suite continues GREEN on Ubuntu/macOS/Windows x Python 3.11/3.12
- Phase 53 hand-off: release.yml's sigstore `inputs:` is the exact two-glob form D-10 specifies (Phase 53 appends one more glob); verify_release.py's SBOM stub is wired (Phase 53 flips ok=None to active subprocess call); .planning/decisions/ directory exists (Phase 56 lands sibling files); .planning/PROJECT.md key-decisions table has 5 rows (Phase 55/56 append 4 more)
</success_criteria>

<output>
After completion, create `.planning/phases/52-signing-substrate-release-yml-new/52-02-SUMMARY.md`
using the standard SUMMARY template. Required sections:
- Tasks Completed table
- Plan 01 RED-to-GREEN Transition table (mirrors Phase 51 Plan 02 SUMMARY §"Plan 01 RED-to-GREEN Transition"; one row per SIGN-NN requirement showing the previously-RED test name flipping GREEN, with the file edit that flipped it)
- Pinned SHAs Applied table (4 rows: actions/checkout, actions/setup-python, sigstore/gh-action-sigstore-python, actions/attest-build-provenance; each row shows owner/repo, version tag, 40-char SHA, verification command output)
- Byte-Identity Verification table covering docs/RELEASE.md steps 1-6 + 8-9 + .planning/PROJECT.md existing 4 rows + pyproject.toml (all must be byte-identical to pre-phase)
- D-09 Atomicity Verification (single git log entry showing the 3-file commit)
- Full Suite Results (test count delta vs pre-phase; expected: 0 new failures)
- Phase 52 Requirements Completion table mapping SIGN-01..05 to the flipped-GREEN tests
- Hand-off Notes:
  - Phase 53: release.yml sigstore `inputs:` ready for one-glob append; verify_release.py SBOM stub ready to be flipped to active
  - Phase 56: docs/MAINTAINER-RUNBOOK.md forward-reference in docs/RELEASE.md step 6.5 needs the gitsign one-time config content; .planning/decisions/ directory exists and Phase 56 adds sibling files
  - Phase 57: release.yml contains the literal `sigstore/gh-action-sigstore-python` that `release-workflow-signing-present` check greps for; release.yml every `uses:` is SHA-pinned for `actions-pinned-by-sha` check
  - Phase 58: tests/fixtures/sigstore/canonical/ has README.md (Plan 01 ship) + needs the binary `.whl` + `.sigstore[.json]` files recorded at v0.6.0-rc1 rehearsal per VALIDATION.md Manual-Only Verifications row 1; sibling tests/fixtures/sigstore/wrong_identity/ directory is Phase 58's TEST-24 deliverable
</output>
