---
phase: 52-signing-substrate-release-yml-new
verified: 2026-05-29T00:00:00Z
status: human_needed
score: 14/14 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Canonical fixture recording (v0.6.0-rc1 rehearsal)"
    expected: "tests/fixtures/sigstore/canonical/<wheel>.whl + <wheel>.whl.sigstore[.json] committed; the 2 currently-SKIPPED tests (test_canonical_fixture_passes_wheel_check, test_wheel_check_with_canonical_fixture) flip GREEN; README.md `Observed bundle filename suffix` section filled in with the actual recorded suffix (.sigstore vs .sigstore.json)"
    why_human: "Requires a live `gh release create` on a fork with OIDC; cannot run in CI. Recording happens ONCE per major release substrate change. See tests/fixtures/sigstore/canonical/README.md + VALIDATION.md Manual-Only Verifications row 1."
  - test: "End-to-end gitsign tag signing on maintainer workstation"
    expected: "git config --get gitsign.connectorID returns non-empty; `git tag -s vX.Y.Z-test -m \"test\"` opens browser for OAuth and creates a signed tag; `git verify-tag vX.Y.Z-test` exits 0"
    why_human: "Requires interactive OAuth flow in browser; cannot be unit-tested. See VALIDATION.md Manual-Only Verifications row 2."
  - test: "`gh attestation verify` against a published release"
    expected: "After v0.6.0-rc1 rehearsal: `gh attestation verify dist/horus_os-0.6.0rc1-py3-none-any.whl --repo Ridou/horus-os` exits 0 + identity line matches EXPECTED_IDENTITY (`https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/v0.6.0-rc1`)"
    why_human: "Requires a published GitHub Release with attached attestations; cannot run pre-release. See VALIDATION.md Manual-Only Verifications row 3."
---

# Phase 52: Signing Substrate (`release.yml` NEW) Verification Report

**Phase Goal:** Wire keyless artifact and tag signing on a NEW `.github/workflows/release.yml` triggered by `on: release: types: [published]`. Sign wheel + sdist + SBOM JSON via sigstore-python keyless OIDC; emit SLSA Build L2 provenance via `actions/attest-build-provenance`; sign git tags via `gitsign` (no long-lived GPG keypair). Ship `scripts/verify_release.py` as the user-facing 5-check trust-chain verifier with workflow-scoped EXACT-match identity. PyPI Trusted Publishing is OUT OF SCOPE for v0.6; deferral rationale documented.

**Verified:** 2026-05-29
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (PLAN 52-02 frontmatter `must_haves.truths`)

| #   | Truth (D-NN locked decision)                                                                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | D-01: `release.yml` exists with ONE `sign-and-attest` job on ubuntu-latest; sequential checkout → setup-python → install build → `python -m build` → sigstore sign → attest wheel → attest sdist; triggered by `on: release: types: [published]` ONLY                  | ✓ VERIFIED | `.github/workflows/release.yml` lines 14-16 declare `on: release: types: [published]` ONLY (no `push:` no `workflow_dispatch:`); lines 20-27 declare single job `sign-and-attest` `runs-on: ubuntu-latest`; steps 28-62 in exact order checkout → setup-python → install → build → sign → attest-wheel → attest-sdist                                            |
| 2   | D-02: top-level `permissions: read-all` AND per-job `id-token: write` + `contents: write` + `attestations: write` on sign-and-attest ONLY                                                                                                                              | ✓ VERIFIED | Line 18 `permissions: read-all` at column 0; lines 24-27 per-job permissions block with all 3 elevated scopes; only job in file = only job carrying elevated perms                                                                                                                                                                                                |
| 3   | D-03: verify_release.py is stdlib-only Python; shells out via subprocess; sigstore NOT added to pyproject.toml                                                                                                                                                          | ✓ VERIFIED | `grep -E 'from sigstore\|^import sigstore' scripts/verify_release.py` returns 0 matches; imports at lines 39-47 are stdlib only (argparse, os, re, subprocess, sys, dataclasses, pathlib); `git diff` against last pyproject.toml change (ee22d98 = v0.5.0 release) returns empty for pyproject.toml                                                                |
| 4   | D-04: hardcoded EXPECTED_IDENTITY_TEMPLATE + EXPECTED_ISSUER module constants; module-import-time assert; `--cert-oidc-issuer` required; CLI rejects mismatched issuer; **POST-BL-01 FIX: substitution uses `f"v{version}"` at line 178 to match git's tag-prefix**  | ✓ VERIFIED | Lines 54-57 constants byte-exact; line 61-63 import-time assert `"refs/tags/{version}" in EXPECTED_IDENTITY_TEMPLATE`; line 390 `required=True` on `--cert-oidc-issuer`; line 424-430 issuer-mismatch path returns 1 with stderr; **line 178: `expected_identity = EXPECTED_IDENTITY_TEMPLATE.format(version=f"v{version}")` — BL-01 fix confirmed at exact line** |
| 5   | D-05: docs/RELEASE.md step 6.5 inserted between steps 6 and 7; step 7 uses `git tag -s`; steps 1-6 and 8-9 prose byte-identical                                                                                                                                       | ✓ VERIFIED | docs/RELEASE.md lines 142-147 contain new step 6.5 (`Confirm gitsign is configured. Run git config --get gitsign.connectorID…`); line 149 contains `git tag -s vN.M.P -m "vN.M.P - <milestone-name>"`; `grep -c 'git tag -a' docs/RELEASE.md` returns 0; `tests/test_release_md_stop_before_tag.py::test_steps_1_through_6_and_8_through_9_byte_identical` PASSES |
| 6   | D-06: TWO per-artifact `actions/attest-build-provenance` invocations (one for `dist/*.whl`, one for `dist/*.tar.gz`)                                                                                                                                                   | ✓ VERIFIED | `grep -c 'actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32'` returns 2; release.yml lines 54-62 contain TWO step blocks: `subject-path: 'dist/*.whl'` (line 57) and `subject-path: 'dist/*.tar.gz'` (line 62)                                                                                                                              |
| 7   | D-07: tests/fixtures/sigstore/canonical/README.md exists with rehearsal recording procedure; binary fixtures correctly deferred (Manual-Only)                                                                                                                          | ✓ VERIFIED | `tests/fixtures/sigstore/canonical/README.md` exists (109 lines); documents recording procedure verbatim from VALIDATION.md row 1; only README is present in the directory (no binary .whl/.sigstore) — correctly Manual-Only per spec                                                                                                                            |
| 8   | D-08: verify_release.py `check_sbom_signature` returns `CheckResult(name='sbom-signature', ok=None, diagnostic='SKIPPED - Phase 53 lands SBOM generation + signing')`                                                                                                  | ✓ VERIFIED | scripts/verify_release.py lines 283-295 define `check_sbom_signature` returning exact ok=None + diagnostic; behavioral spot-check: `python scripts/verify_release.py --check sbom` exits 0 with `SKIP  sbom-signature: SKIPPED - Phase 53 lands SBOM generation + signing` on stdout                                                                                |
| 9   | D-09: `.planning/decisions/no-pypi-in-v0.6.md` exists with literal terminator; .planning/PROJECT.md key-decisions table contains a row referencing the decision file path; both land in SAME commit                                                                    | ✓ VERIFIED | `.planning/decisions/no-pypi-in-v0.6.md` exists (65 lines) with literal `**Decision (final, until revisited):**` at line 37; `.planning/PROJECT.md` line 82 contains row with literal `.planning/decisions/no-pypi-in-v0.6.md` + `accepted (v0.6)`; commit `ebf2a47` includes all 3 files in same commit (verified via `git log -1 --name-only ebf2a47`)         |
| 10  | D-10: release.yml sigstore-python `inputs:` value is exactly `./dist/*.whl ./dist/*.tar.gz` (whitespace-separated two-glob; Phase 53 will append SBOM glob)                                                                                                            | ✓ VERIFIED | release.yml line 51: `inputs: ./dist/*.whl ./dist/*.tar.gz` — exact byte-identical match; grep returns 1                                                                                                                                                                                                                                                            |
| 11  | Every `uses:` in release.yml is SHA-pinned to 40-char hex with `# vN.M.P` comment per CIHARD-04; persist-credentials: false on actions/checkout per CIHARD-03                                                                                                          | ✓ VERIFIED | Lines 30, 35, 48, 55, 60 all carry `@<40-hex># vN.M.P` trailing-comment shape; line 32 `persist-credentials: false`; Phase 51 PR-time gate `tests/test_contribution_gate_pitfalls/` (42 tests) PASSES with release.yml in tree                                                                                                                                       |
| 12  | release.yml sigstore-python step has `timeout-minutes: 5` (OIDC budget per PITFALL 2 / SIGN-01)                                                                                                                                                                        | ✓ VERIFIED | release.yml line 49 `timeout-minutes: 5` on sigstore step block (between `uses:` and `with:`)                                                                                                                                                                                                                                                                       |
| 13  | All Plan 01 RED-by-design production assertions flip GREEN after Plan 02; canonical-fixture tests remain SKIPPED until v0.6.0-rc1 rehearsal recording                                                                                                                  | ✓ VERIFIED | `pytest tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py -v` reports `25 passed, 2 skipped`; 2 skips are the canonical-fixture tests (lines 146 + 223 of test_release_verification.py with matching skip reasons)                                                       |
| 14  | CLAUDE.md hard rules honored: ruff clean; no em-dashes in any committed Phase 52 file; no PII; conventional-commit prefixes; existing 1011-test suite continues GREEN (delta: 0 new failures outside Phase 52 scope)                                                   | ✓ VERIFIED | `ruff check` + `ruff format --check` on all 5 Phase 52 files: clean ("5 files already formatted"); em-dash scan: all 4 NEW/MODIFIED Phase-52-introduced files clean (PROJECT.md pre-existing em-dashes lines 32-109 predate Phase 52 per deviation note); commits cb8a504/615b754/ebf2a47/ad68f04 carry conventional `feat(52-NN):` prefix                       |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact                                       | Expected                                                                                                                              | Status     | Details                                                                                                                                                                                                                                                                       |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/release.yml`                | Single sign-and-attest job; D-01 + D-02 + D-06 + D-10 + 4 SHA-pinned actions; SIGN-01 + SIGN-02 substrate                              | ✓ VERIFIED | 63 lines (>=40 required); all literal contents pinned in PLAN frontmatter `contains:` are present; exists, substantive, wired (downstream Phase 57 release-gate greps the literal `sigstore/gh-action-sigstore-python`); data-flows N/A (workflow file)                          |
| `scripts/verify_release.py`                    | Stdlib-only 5-check trust-chain verifier with SBOM stub; SIGN-04 substrate; D-03 + D-04 + D-08 shape                                  | ✓ VERIFIED | 501 lines (>=180 required); exports `CheckResult`, `main`, all 5 `check_*` functions; behavioral spot-checks all PASS; module-import-time assert active; `_VERSION_PATTERN` widened post-review (accepts X.Y.Z + X.Y.Z-rcN + PEP 440 cadences); BL-01 fix at line 178 confirmed |
| `.planning/decisions/no-pypi-in-v0.6.md`       | Documented rationale for PEP 807 deferral; SIGN-05 substrate; literal terminator `**Decision (final, until revisited):**`             | ✓ VERIFIED | 65 lines (>=30 required); H1 heading present; literal terminator at line 37; references PEP 807 + Trusted Publishing + v0.7; em-dash free; no PII (only Ridou/horus-os identifier)                                                                                              |
| `docs/RELEASE.md`                              | STOP-BEFORE-TAG block with gitsign pre-flight (step 6.5) + signed-tag command (step 7); SIGN-03 substrate                              | ✓ VERIFIED | step 6.5 (lines 142-147) inserted between step 6 (ending line 141) and step 7 (starting line 148); step 7 uses `git tag -s`; `grep -c 'git tag -a' docs/RELEASE.md` returns 0; byte-identity invariant test PASSES                                                            |
| `.planning/PROJECT.md`                         | Key Decisions table appends `no-pypi-in-v0.6` row; SIGN-05 substrate (atomic with decision file per D-09)                              | ✓ VERIFIED | Line 82 contains new row with literal `.planning/decisions/no-pypi-in-v0.6.md` reference and `accepted (v0.6)` status; existing 4 rows (lines 78-81) preserved byte-identical; row is em-dash-free (pre-existing em-dashes in lines 32-109 predate Phase 52)                  |

### Key Link Verification

| From                                        | To                                                  | Via                                                                                                          | Status   | Details                                                                                                                                                              |
| ------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/release.yml`             | GitHub Release page (published)                     | `release-signing-artifacts: true` + `attestations: write`                                                    | ✓ WIRED  | release.yml line 52 carries `release-signing-artifacts: true`; line 27 carries `attestations: write` per-job permission; manual verify at v0.6.0-rc1 rehearsal       |
| `scripts/verify_release.py`                 | `.github/workflows/release.yml @refs/tags/{version}` | `EXPECTED_IDENTITY_TEMPLATE.format(version=f"v{version}")` passed to `python -m sigstore verify identity`   | ✓ WIRED  | scripts/verify_release.py line 178 substitution; line 187 passes to `--cert-identity`; **BL-01 fix verified**: f-string prefix `v` matches git tag convention (`v0.6.0`) |
| `docs/RELEASE.md` step 6.5                  | `docs/MAINTAINER-RUNBOOK.md` (Phase 56 forward)     | prose link                                                                                                   | ✓ WIRED  | Line 146 `docs/MAINTAINER-RUNBOOK.md` literal present; forward-reference acceptable per CONTEXT.md `<specifics>` (Phase 56 lands the runbook before v0.6.0 ships)    |
| `.planning/PROJECT.md` key-decisions table  | `.planning/decisions/no-pypi-in-v0.6.md`            | Reference column with relative path link                                                                     | ✓ WIRED  | Line 82 row contains literal `.planning/decisions/no-pypi-in-v0.6.md`; `tests/test_decision_no_pypi.py::test_project_md_references_decision_file` PASSES            |
| `.planning/decisions/no-pypi-in-v0.6.md`    | `tests/test_decision_no_pypi.py`                    | literal substring `**Decision (final, until revisited):**`                                                   | ✓ WIRED  | Line 37 carries the literal; `tests/test_decision_no_pypi.py::test_decision_file_has_terminator` PASSES                                                              |

### Behavioral Spot-Checks

| Behavior                                                        | Command                                                                                                                                | Result                                                                                                                                  | Status   |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| SBOM stub returns SKIPPED + Phase 53 diagnostic; exits 0         | `python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://token.actions.githubusercontent.com --check sbom`     | stdout: `SKIP  sbom-signature: SKIPPED - Phase 53 lands SBOM generation + signing`; exit=0                                              | ✓ PASS   |
| Missing `--cert-oidc-issuer` rejected by argparse with exit 2    | `python scripts/verify_release.py --version 0.6.0-rc1`                                                                                 | stderr: `error: the following arguments are required: --cert-oidc-issuer`; exit=2                                                       | ✓ PASS   |
| Wrong issuer rejected with exit 1 + EXPECTED_ISSUER in stderr    | `python scripts/verify_release.py --version 0.6.0-rc1 --cert-oidc-issuer https://example.com/oauth`                                    | stderr: `FAIL  verify_release: --cert-oidc-issuer must equal 'https://token.actions.githubusercontent.com', got 'https://example.com/oauth'`; exit=1 | ✓ PASS   |
| Plan 02 full test surface: 25 passed + 2 skipped (canonical pending) | `pytest tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py -v` | `25 passed, 2 skipped in 0.07s`                                                                                                          | ✓ PASS   |
| Phase 51 PR-time gate regression: 42 passed                      | `pytest tests/test_contribution_gate_pitfalls/ tests/test_release_gate.py tests/test_release_gate_v0_5_checks.py tests/test_lint_no_wallclock.py --tb=no -q` | `42 passed in 0.48s`                                                                                                                     | ✓ PASS   |
| pyproject.toml byte-identical to pre-Phase-52 (D-03 zero-base-dep) | `git log --all --oneline -- pyproject.toml` and inspect last touch                                                                     | Last touch commit `ee22d98 chore(release): bump version to 0.5.0` (predates Phase 52 by milestone); Phase 52 commits don't appear        | ✓ PASS   |
| Ruff clean on all Phase 52 production + test files               | `ruff check ...` + `ruff format --check ...` on the 5 phase files                                                                       | `All checks passed!` + `5 files already formatted`                                                                                       | ✓ PASS   |

### Probe Execution

No probes declared for Phase 52 (signing substrate is workflow YAML + Python verifier + docs + decision-file; verified via pytest + ruff + behavioral spot-checks above). N/A.

### Requirements Coverage

| Requirement | Source Plan       | Description                                                                                                                              | Status      | Evidence                                                                                                                                                                                                                |
| ----------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SIGN-01     | 52-01, 52-02      | NEW `.github/workflows/release.yml` triggered on `release: types: [published]`; signs wheel + sdist via sigstore-python keyless OIDC; ≤5 min OIDC mint-to-sign budget; `.sigstore` bundle format | ✓ SATISFIED | release.yml exists with all required structure; 9 SIGN-01+02 production tests in tests/test_release_yml_structure.py PASS; sigstore step has `timeout-minutes: 5`; `release-signing-artifacts: true` ensures bundle format |
| SIGN-02     | 52-01, 52-02      | `actions/attest-build-provenance@<sha>` generates SLSA Build L2 provenance; runs for every signed artifact                                | ✓ SATISFIED | TWO per-artifact attest-build-provenance invocations (lines 54-62 of release.yml); per-job `attestations: write` permission; full verification (`gh attestation verify`) routed to human verification (Manual-Only row 3)   |
| SIGN-03     | 52-01, 52-02      | Tag signing via `gitsign`; docs/RELEASE.md STOP-BEFORE-TAG documents gitsign-configured `git tag` invocation                              | ✓ SATISFIED | docs/RELEASE.md step 6.5 added with `git config --get gitsign.connectorID` pre-flight; step 7 uses `git tag -s`; 3 production tests in tests/test_release_md_stop_before_tag.py PASS; workstation gitsign flow routed to human (Manual-Only row 2) |
| SIGN-04     | 52-01, 52-02      | `scripts/verify_release.py` 5-check user-facing trust-chain verifier with workflow-scoped EXACT-match `EXPECTED_IDENTITY`; mandatory `--cert-oidc-issuer`; negative test rejects wrong-identity (deferred to Phase 58 / TEST-24) | ✓ SATISFIED | scripts/verify_release.py exists (501 lines); 7 SIGN-04 production tests PASS; BL-01 fix at line 178 (`f"v{version}"` substitution) verified; 2 canonical-fixture tests SKIP pending v0.6.0-rc1 rehearsal recording (Manual-Only row 1) |
| SIGN-05     | 52-01, 52-02      | PyPI Trusted Publishing (PEP 807) OUT OF SCOPE for v0.6; deferral documented in `.planning/decisions/no-pypi-in-v0.6.md`; v0.7+ may revisit | ✓ SATISFIED | `.planning/decisions/no-pypi-in-v0.6.md` (65 lines, terminator literal at line 37); `.planning/PROJECT.md` line 82 row references the decision file; 3 production tests in tests/test_decision_no_pypi.py PASS; D-09 atomicity confirmed in commit ebf2a47 (3 files) |

No orphaned requirements: all 5 SIGN-NN ids declared in PLAN frontmatter are present in REQUIREMENTS.md mapped to Phase 52; no extra SIGN ids in REQUIREMENTS.md left unclaimed.

### Anti-Patterns Found

| File                                          | Line          | Pattern                              | Severity  | Impact                                                                                                                                                                                                                                                                                  |
| --------------------------------------------- | ------------- | ------------------------------------ | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.planning/PROJECT.md`                        | 32, 33, 34, … | em-dashes (`—`)                       | ℹ️ Info    | Pre-existing (lines 32-109; predate Phase 52); explicitly excluded from Phase 52 deviation scope per SUMMARY.md (Deviations §3). The newly-appended row at line 82 is em-dash-free. NOT a Phase 52 regression.                                                                          |
| `tests/test_adapters_otel_pii_redaction.py`   | various       | 30 pre-existing test failures        | ℹ️ Info    | Pre-existing on main HEAD pre-Phase-52 (verified per deferred-items.md). Belongs to Phase 38 OpenTelemetry adapter; Phase 52 changes do not touch `src/horus_os/adapters/otel_adapter.py` or `src/horus_os/observability/*`. Logged to `.planning/phases/52-.../deferred-items.md`. NOT a Phase 52 regression. |

No `TODO`, `FIXME`, `XXX`, `HACK`, `PLACEHOLDER`, hardcoded empty data, stub patterns, or `console.log`-only implementations in any Phase-52-introduced file.

### Human Verification Required

3 items routed to `human_verification` per VALIDATION.md `Manual-Only Verifications`:

#### 1. Canonical fixture recording (v0.6.0-rc1 rehearsal)

**Test:** On a fork: (1) configure `gitsign`; (2) push `v0.6.0-rc1` tag; (3) `gh release create v0.6.0-rc1 --prerelease`; (4) wait for `release.yml` to complete and verify GitHub Release page shows wheel + `.sigstore` bundle; (5) `gh release download v0.6.0-rc1 -D tests/fixtures/sigstore/canonical/`; (6) update README.md with recording date + observed bundle suffix; (7) commit.

**Expected:** Binary `.whl` + `.sigstore[.json]` files land at `tests/fixtures/sigstore/canonical/`; README.md `Observed bundle filename suffix` section is filled in; the 2 currently-SKIPPED tests (`test_canonical_fixture_passes_wheel_check`, `test_wheel_check_with_canonical_fixture`) flip GREEN.

**Why human:** Requires a live `gh release create` on a fork with OIDC; cannot run in CI. Recording happens ONCE per major release substrate change. Reference: tests/fixtures/sigstore/canonical/README.md + VALIDATION.md Manual-Only row 1.

#### 2. End-to-end gitsign tag signing on maintainer workstation

**Test:** (1) Run the four `git config` commands from gitsign install docs (Phase 56 lands `docs/MAINTAINER-RUNBOOK.md` content); (2) `git config --get gitsign.connectorID` returns non-empty; (3) `git tag -s vX.Y.Z-test -m "test"` opens browser for OAuth and creates tag; (4) `git verify-tag vX.Y.Z-test` exits 0; (5) delete test tag.

**Expected:** SIGN-03 end-to-end flow works on a real maintainer workstation with browser OAuth.

**Why human:** Requires interactive OAuth flow in browser; cannot be unit-tested. Reference: VALIDATION.md Manual-Only row 2.

#### 3. `gh attestation verify` against a published release

**Test:** After v0.6.0-rc1 rehearsal: `gh attestation verify dist/horus_os-0.6.0rc1-py3-none-any.whl --repo Ridou/horus-os`.

**Expected:** Exit 0 + identity line matches `EXPECTED_IDENTITY` (`https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/v0.6.0-rc1`).

**Why human:** Requires a published GitHub Release with attached attestations; cannot run pre-release. Reference: VALIDATION.md Manual-Only row 3.

### Gaps Summary

No automated gaps. All 14 must-haves VERIFIED. All 5 SIGN-NN requirements SATISFIED in code. All 25 phase tests pass; the 2 SKIPs are the canonical-fixture binary tests intentionally deferred per VALIDATION.md row 1 (covered by `human_verification` item #1).

The phase ships the v0.6 signing substrate end-to-end (workflow + verifier + decision file + docs + key-decisions row). Three remaining items require human verification because they need real OIDC, live OAuth, or a published GitHub Release that cannot be exercised in CI. Once a maintainer runs the v0.6.0-rc1 rehearsal on a fork (item #1), the gitsign workstation test (item #2), and the post-release `gh attestation verify` (item #3), all 5 SIGN-NN requirements will be observable end-to-end.

### Deferred (NOT actionable for Phase 52)

The following items appear in CONTEXT.md `<deferred>` and are explicitly owned by later phases:

| Item                                                             | Addressed In | Evidence (later phase goal/SC)                                                                                                                |
| ---------------------------------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| SBOM generation + signing                                        | Phase 53     | ROADMAP Phase 53: "SBOM generation in release.yml runs after Phase 52 lands the sign step"                                                     |
| Flip `check_sbom_signature` from SKIPPED to active               | Phase 53     | verify_release.py line 14: "Phase 53 flips this single stub to active" + D-08                                                                  |
| Wrong-identity negative test fixture + assertion                 | Phase 58     | CONTEXT.md `<deferred>`: "Wrong-identity negative test fixture + assertion (TEST-24)"                                                          |
| `release-workflow-signing-present` release-gate check            | Phase 57     | CONTEXT.md `<deferred>`: "Phase 57 (REL-14)"                                                                                                   |
| `docs/MAINTAINER-RUNBOOK.md` gitsign one-time-setup content      | Phase 56     | CONTEXT.md `<deferred>`: "docs/MAINTAINER-RUNBOOK.md content (gitsign one-time setup) → Phase 56 (RUNBOOK-01)"                                |
| PyPI Trusted Publishing wiring                                   | v0.7+        | D-09 + `.planning/decisions/no-pypi-in-v0.6.md` line 41: "v0.7 (or a later milestone) may revisit"                                            |

These are NOT Phase 52 gaps; they appear in the deferred plan and are correctly outside the Phase 52 boundary.

---

_Verified: 2026-05-29_
_Verifier: Claude (gsd-verifier)_
