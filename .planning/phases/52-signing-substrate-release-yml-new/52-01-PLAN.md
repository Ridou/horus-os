---
phase: 52
plan: "01"
subsystem: signing-substrate
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_release_verification.py
  - tests/test_release_yml_structure.py
  - tests/test_release_md_stop_before_tag.py
  - tests/test_decision_no_pypi.py
  - tests/fixtures/sigstore/canonical/README.md
autonomous: true
requirements: [SIGN-01, SIGN-02, SIGN-03, SIGN-04, SIGN-05]
tags: [ci, security, signing, sigstore, regression-tests, wave-0, red-by-design]
tech_stack:
  added: []
  patterns:
    - "stdlib re regex scanning over .github/workflows/release.yml (no PyYAML)"
    - "Path(__file__).resolve().parents[1] for top-level tests (one fewer than nested)"
    - "@pytest.fixture(scope=module) loading docs/RELEASE.md and the decision file text"
    - "importlib.util.spec_from_file_location to load scripts/verify_release.py in-process (mirrors tests/test_release_gate.py:31-46)"
    - "RED-by-design production assertions (Wave 0 signal; Plan 02 turns GREEN)"
    - "Non-vacuity synthetic fixtures (tmp_path); scanners proven to fire BEFORE Wave 1 production code lands"
key_files:
  created:
    - tests/test_release_verification.py
    - tests/test_release_yml_structure.py
    - tests/test_release_md_stop_before_tag.py
    - tests/test_decision_no_pypi.py
    - tests/fixtures/sigstore/canonical/README.md
  modified: []
decisions:
  - "Tests live at tests/ top-level (not nested) per CONTEXT.md <canonical_refs> line 168; therefore REPO_ROOT = Path(__file__).resolve().parents[1] (mirrors tests/test_release_gate.py)"
  - "Pre-edit step-1..6 and step-8..9 prose from docs/RELEASE.md is pinned as Python string constants inside test_release_md_stop_before_tag.py to enforce the insertions-allowed-mutations-not byte-identity invariant (D-05, Phase 51 D-06 precedent)"
  - "Binary fixture files (.whl + .sigstore[.json]) are out of scope for Plan 01; this plan ships ONLY the canonical/ directory + README.md placeholder. The binary fixture is a Manual-Only Verification per VALIDATION.md row 1 (recorded at v0.6.0-rc1 rehearsal time, before Phase 58 needs the wrong-identity sibling for TEST-24). Tests that exercise the canonical bundle skip with pytest.skip(...) until the binary lands; tests that exercise file-existence + argparse + import-time assertions are RED-by-design now."
  - "Use importlib.util in-process load (mirrors tests/test_release_gate.py:31-46) so tests can call mod.check_sbom_signature() directly without subprocess overhead; subprocess shell-out integration test is also included as an OPTIONAL Pattern C (mirrors tests/test_lint_no_wallclock.py:20-32)"
metrics:
  estimated_context: "~35%"
  task_count: 2
must_haves:
  truths:
    - "tests/test_release_verification.py exists and fails RED on SIGN-04 production assertions until Wave 1 lands scripts/verify_release.py with the 5-check skeleton, hardcoded EXPECTED_IDENTITY_TEMPLATE + EXPECTED_ISSUER, mandatory --version + --cert-oidc-issuer argparse flags, and SBOM check returning ok=None (D-03, D-04, D-08)"
    - "tests/test_release_yml_structure.py exists and fails RED on SIGN-01 + SIGN-02 production assertions until Wave 1 lands .github/workflows/release.yml with on: release: types: [published], top-level permissions: read-all, per-job id-token: write + contents: write + attestations: write on sign-and-attest, sigstore/gh-action-sigstore-python literal + SHA-pin + timeout-minutes: 5, and TWO per-artifact actions/attest-build-provenance invocations (D-01, D-02, D-06)"
    - "tests/test_release_md_stop_before_tag.py exists and fails RED on SIGN-03 production assertions until Wave 1 edits docs/RELEASE.md to insert step 6.5 (gitsign connector-id pre-flight referencing docs/MAINTAINER-RUNBOOK.md) BETWEEN current steps 6 and 7, and swaps the literal `git tag -a vN.M.P` to `git tag -s vN.M.P` in step 7 (D-05)"
    - "tests/test_decision_no_pypi.py exists and fails RED on SIGN-05 production assertions until Wave 1 lands .planning/decisions/no-pypi-in-v0.6.md (terminated by the literal `**Decision (final, until revisited):**` block) and appends a PROJECT.md key-decisions table row referencing the path `.planning/decisions/no-pypi-in-v0.6.md` (D-09)"
    - "tests/fixtures/sigstore/canonical/README.md exists and documents the rehearsal recording procedure (D-07; verbatim from VALIDATION.md Manual-Only Verifications row 1)"
    - "All non-vacuity / synthetic-fixture tests pass NOW (Wave 0): each scanner is proven to fire on a known violation written to tmp_path, mirroring the Phase 51 Plan 01 pattern (51-01-SUMMARY.md §Non-Vacuity Coverage)"
    - "PITFALL 3 (wildcard identity) regression coverage: tests/test_release_verification.py asserts the script REFUSES a wildcard `*` in EXPECTED_IDENTITY_TEMPLATE at module-import time via the `assert 'refs/tags/{version}' in EXPECTED_IDENTITY_TEMPLATE` invariant"
    - "Every test file uses encoding='utf-8' on read_text(), str(path) on all subprocess args, and sys.executable not bare python (S-1 + S-2 from PATTERNS.md; cross-OS-green discipline)"
    - "No em-dashes in any docstring, diagnostic message, or fixture README prose (CLAUDE.md HR3)"
  artifacts:
    - path: "tests/test_release_verification.py"
      provides: "5 production tests + non-vacuity synthetic-fixture tests for SIGN-04 (D-03, D-04, D-08)"
      contains: "EXPECTED_IDENTITY_TEMPLATE, EXPECTED_ISSUER, _load_verify_release_module, check_sbom_signature, --cert-oidc-issuer, --version"
    - path: "tests/test_release_yml_structure.py"
      provides: "9 structural workflow lint assertions for SIGN-01 + SIGN-02 (D-01, D-02, D-06)"
      contains: "release.yml, on: release:, types: [published], permissions: read-all, id-token: write, attest-build-provenance, sigstore/gh-action-sigstore-python, timeout-minutes: 5"
    - path: "tests/test_release_md_stop_before_tag.py"
      provides: "3 prose-lint tests for SIGN-03 + byte-identity invariant enforcement for steps 1-6 + 8-9 (D-05)"
      contains: "git config --get gitsign.connectorID, git tag -s, git tag -a, MAINTAINER-RUNBOOK"
    - path: "tests/test_decision_no_pypi.py"
      provides: "3 cross-reference tests for SIGN-05 (D-09)"
      contains: ".planning/decisions/no-pypi-in-v0.6.md, **Decision (final, until revisited):**, PROJECT.md"
    - path: "tests/fixtures/sigstore/canonical/README.md"
      provides: "Documentation of the v0.6.0-rc1 rehearsal recording procedure (D-07)"
      contains: "Recording procedure, Why committed instead of generated, Observed bundle filename suffix"
  key_links:
    - from: "tests/test_release_yml_structure.py"
      to: ".github/workflows/release.yml (will be created by Plan 02)"
      via: "file scan from REPO_ROOT/.github/workflows/release.yml"
      pattern: "permissions: read-all, sigstore/gh-action-sigstore-python, attest-build-provenance"
    - from: "tests/test_release_verification.py"
      to: "scripts/verify_release.py (will be created by Plan 02)"
      via: "importlib.util.spec_from_file_location on REPO_ROOT/scripts/verify_release.py"
      pattern: "EXPECTED_IDENTITY_TEMPLATE, EXPECTED_ISSUER, check_wheel_signature, check_sbom_signature, main"
    - from: "tests/test_release_md_stop_before_tag.py"
      to: "docs/RELEASE.md (will be edited by Plan 02)"
      via: "module-scoped fixture read_text(encoding='utf-8') on REPO_ROOT/docs/RELEASE.md"
      pattern: "git config --get gitsign.connectorID, git tag -s vN.M.P"
    - from: "tests/test_decision_no_pypi.py"
      to: ".planning/decisions/no-pypi-in-v0.6.md (will be created by Plan 02) + PROJECT.md (will be edited by Plan 02)"
      via: "module-scoped fixture read_text(encoding='utf-8') + cross-ref substring scan"
      pattern: "**Decision (final, until revisited):**, .planning/decisions/no-pypi-in-v0.6.md"
---

<objective>
Wave 0 RED-by-design test scaffolding for Phase 52: ship the four NEW test files
(`tests/test_release_verification.py`, `tests/test_release_yml_structure.py`,
`tests/test_release_md_stop_before_tag.py`, `tests/test_decision_no_pypi.py`)
plus the canonical fixture directory (`tests/fixtures/sigstore/canonical/README.md`).

Purpose: Mirror Phase 51 Plan 01's Wave 0 pattern. Tests use non-vacuity synthetic
fixtures so scanners are proven to fire BEFORE Wave 1 production code lands. Production
assertions are RED-by-design (the files they scan do not exist yet); Plan 02 flips them
GREEN by creating `.github/workflows/release.yml`, `scripts/verify_release.py`,
`.planning/decisions/no-pypi-in-v0.6.md`, editing `docs/RELEASE.md`, and appending to
`PROJECT.md`. The binary `.whl` + `.sigstore` fixture is intentionally a
Manual-Only Verification (recorded at v0.6.0-rc1 rehearsal time per VALIDATION.md
Manual-Only Verifications row 1); this plan ships ONLY the directory + README placeholder.

Output: 5 new test/fixture files committed; running `pytest tests/test_release_verification.py
tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py
tests/test_decision_no_pypi.py -v` exits non-zero with predictable RED assertions on the
production tests and GREEN assertions on every non-vacuity synthetic-fixture test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md
@.planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md
@.planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md
@.planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md
@.planning/phases/51-ci-hardening-substrate/51-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_release_yml_structure.py + tests/test_release_md_stop_before_tag.py + tests/test_decision_no_pypi.py (workflow + docs + decision-file RED scaffolds)</name>
  <files>tests/test_release_yml_structure.py, tests/test_release_md_stop_before_tag.py, tests/test_decision_no_pypi.py</files>
  <read_first>
    - tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py — primary analog for test_release_yml_structure.py (regex scanners over .github/workflows/*.yml; assert-on-violation-list-with-diagnostic shape; non-vacuity helper functions parametrized on workflows_dir per Phase 51 Plan 01 decision)
    - tests/test_contribution_gate_pitfalls/test_pitfall_02_action_sha_pinning.py — secondary analog; provides _USES_PATTERN = re.compile(r"^\s*-?\s*uses:\s*([^@\s#]+)@(\S+)") and _ALLOWED_REF = re.compile(r"^[0-9a-f]{40}$") (PATTERNS.md §6 Pattern B); also S-5 comment-skip discipline lines 49-52
    - tests/docs/test_migration_v04_v05_schema_commands.py — primary analog for test_release_md_stop_before_tag.py (module-scoped @pytest.fixture(scope="module") + .read_text(encoding="utf-8") + substring-pin assertions with helpful diagnostics; uses parents[2] because nested under tests/docs/; this plan's top-level test uses parents[1])
    - tests/docs/test_plugin_security_threat_sentence.py — primary analog for test_decision_no_pypi.py (cross-ref pattern: doc-file existence + literal-substring + referencing-source-file existence + substring check; same shape as test_installer_references_security_doc at lines 71-84 of the analog)
    - .planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md (§6 "tests/test_release_yml_structure.py" lines 567-625, §7 "tests/test_release_md_stop_before_tag.py" lines 628-687, §8 "tests/test_decision_no_pypi.py" lines 690-747, §S-1..S-5 "Shared Patterns" lines 822-895)
    - .planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md (rows 1, 2, 3, 5 of the Per-Task Verification Map; Wave 0 Requirements bullets 1, 2, 3)
    - .planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md (D-01, D-02, D-05, D-06, D-09; <specifics> on release.published trigger and EXPECTED_IDENTITY shape; <canonical_refs> on Phase 56 forward reference being acceptable)
    - .planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md (Pattern 2 lines 266-286 for the exact RELEASE.md step 6.5 prose to assert against; Example 1 lines 452-510 for the literal strings test_release_yml_structure.py greps for; Code Examples §5 prose convention test_decision_no_pypi.py greps for)
    - .planning/phases/51-ci-hardening-substrate/51-01-SUMMARY.md (Wave 0 RED-by-design pattern: non-vacuity synthetic-fixture tests pass NOW while production assertions are RED until Wave 1 lands)
    - docs/RELEASE.md lines 140-144 — the verbatim current step 6 + current step 7 prose that test_release_md_stop_before_tag.py pins as byte-identity invariant string constants
    - .planning/PROJECT.md lines 74-81 — the existing key-decisions table with `| Decision | Rationale | Status |` 3-column shape that PROJECT.md cross-ref test scans
  </read_first>
  <action>
    Create three sibling test files at tests/ top-level. Each file MUST set `REPO_ROOT = Path(__file__).resolve().parents[1]` (one fewer parent than the nested tests/test_contribution_gate_pitfalls/ analog uses; PATTERNS.md §6 Pattern A "Note" line 591-594). Each file MUST follow Shared Pattern S-1 (sys.executable / str(path) / capture_output=True, text=True, check=False) for any subprocess and S-2 (encoding="utf-8" on every read_text). NO em-dashes in any docstring or diagnostic (S-4 + CLAUDE.md HR3). Skip pure-comment lines in regex scanners (S-5).

    (A) tests/test_release_yml_structure.py — 9 production assertions covering SIGN-01 + SIGN-02 per VALIDATION.md row 1 + PATTERNS.md §6 enumerated test list:
      1. test_release_yml_exists: `RELEASE_YML_PATH = REPO_ROOT / ".github" / "workflows" / "release.yml"` must be a file; failure diagnostic: "Phase 52 Plan 02 (Wave 1) must create .github/workflows/release.yml per D-01."
      2. test_on_release_published_trigger: regex-or-substring assert text contains the canonical `on:\n  release:\n    types: [published]` block (allow whitespace flexibility but the literal `release:` + `types: [published]` substrings must both be present; use re.search with `re.compile(r"on:\s*\n\s*release:\s*\n\s*types:\s*\[\s*published\s*\]")`).
      3. test_top_level_permissions_read_all: `re.compile(r"^permissions:\s*read-all\s*$", re.MULTILINE)` must match (column-0 placement per PATTERNS.md §1 Pattern A note line 57). Mirror tests/test_contribution_gate_pitfalls/test_ci_hardening_workflow_structure.py:141-149 assert-on-violation shape.
      4. test_per_job_id_token_write: file contains substring `id-token: write` AND substring `contents: write` AND substring `attestations: write`. Diagnostic must cite D-02 explicitly.
      5. test_per_artifact_attest: assert text contains EXACTLY TWO occurrences of `uses: actions/attest-build-provenance@` (one wheel, one sdist; D-06 per-artifact granularity, NOT a single dist/* glob).
      6. test_sigstore_action_literal_present: assert substring `sigstore/gh-action-sigstore-python` (Phase 57's release-gate `release-workflow-signing-present` check greps for this exact literal; diagnostic must say so).
      7. test_sigstore_action_sha_pinned: extract the sigstore-action `uses:` line via _USES_PATTERN; assert the ref half matches _ALLOWED_REF (40-char hex).
      8. test_sigstore_step_timeout_minutes_5: the block of YAML containing the sigstore-action `uses:` must also contain `timeout-minutes: 5` within the same step (parse by walking lines until next `- name:` boundary; SIGN-01 OIDC 5-minute budget per PITFALL 2 in RESEARCH.md).
      9. test_every_uses_in_release_yml_sha_pinned: scoped reuse of the _USES_PATTERN + _ALLOWED_REF helpers; iterate every `uses:` line in release.yml ONLY (not all workflows); each ref MUST be 40-char hex. Skip comment-only lines (S-5).

      Add non-vacuity helpers `_scan_release_yml(workflows_dir: Path)` parametrized on `workflows_dir` (mirrors Phase 51 Plan 01 decision; SUMMARY.md §Non-Vacuity Coverage). Add at minimum THREE non-vacuity synthetic-fixture tests using tmp_path:
       - test_scanner_catches_synthetic_missing_permissions: write a minimal valid YAML without `permissions: read-all` to tmp_path; assert the scanner flags it
       - test_scanner_catches_synthetic_sha_violation: write a minimal valid YAML with `uses: actions/checkout@v4` (mutable tag); assert the SHA-pin scanner flags it
       - test_scanner_catches_synthetic_missing_attest: write a minimal valid YAML with only ONE `attest-build-provenance` invocation; assert the per-artifact scanner flags it
      Each non-vacuity test passes NOW (Wave 0); production tests 1-9 fail RED until Plan 02.

    (B) tests/test_release_md_stop_before_tag.py — 3 production assertions covering SIGN-03 per VALIDATION.md row 3 + PATTERNS.md §7 enumerated test list:
      Module fixture: `RELEASE_MD_PATH = REPO_ROOT / "docs" / "RELEASE.md"`; @pytest.fixture(scope="module") `release_text() -> str` returning .read_text(encoding="utf-8") (mirrors test_migration_v04_v05_schema_commands.py:30-37 shape). pytest.fail() with the SIGN-03 phase hint if file missing.

      Pin the pre-edit text of steps 1-6 (verbatim from docs/RELEASE.md:122-141, ending with the `gh run list --branch main --limit 1).` line) and steps 8-9 (verbatim from docs/RELEASE.md:145-157, starting with `8. Publish the GitHub Release.` and ending with `https://github.com/Ridou/horus-os/releases/tag/vN.M.P`.) as Python multi-line string constants at module top (PRE_EDIT_STEPS_1_THROUGH_6, PRE_EDIT_STEPS_8_THROUGH_9). These are the byte-identity baselines per D-05 + Phase 51 D-06 + ARCHITECTURE.md line 102.

      1. test_step_6_5_gitsign_inserted: assert `"git config --get gitsign.connectorID"` literal is in release_text; assert it appears AFTER PRE_EDIT_STEPS_1_THROUGH_6 (via .find() index comparison) and BEFORE the `git tag -s vN.M.P` literal; assert step 6.5 references `docs/MAINTAINER-RUNBOOK.md` (PATTERNS.md §7 Pattern C optional 4th assert; CONTEXT.md <specifics> "Phase 56 forward reference is acceptable").
      2. test_step_7_uses_tag_dash_s: assert `'git tag -s vN.M.P -m "vN.M.P - <milestone-name>"'` is present AND `'git tag -a vN.M.P -m "vN.M.P - <milestone-name>"'` is absent (the literal swap; D-05).
      3. test_steps_1_through_6_and_8_through_9_byte_identical: assert PRE_EDIT_STEPS_1_THROUGH_6 is in release_text AND PRE_EDIT_STEPS_8_THROUGH_9 is in release_text. Diagnostic must say "insertions allowed, mutations not (Phase 51 D-06 + Phase 52 D-05)".

    (C) tests/test_decision_no_pypi.py — 3 production assertions covering SIGN-05 per VALIDATION.md row 5 + PATTERNS.md §8 enumerated test list:
      Module-level: `DECISION_PATH = REPO_ROOT / ".planning" / "decisions" / "no-pypi-in-v0.6.md"`, `PROJECT_MD_PATH = REPO_ROOT / "PROJECT.md"`. `@pytest.fixture(scope="module") decision_text() -> str` mirrors PATTERNS.md §8 Pattern A. pytest.fail() with the SIGN-05 + D-09 phase hint if DECISION_PATH absent.

      1. test_decision_file_exists: assert decision_text.strip() is non-empty.
      2. test_decision_file_has_terminator: assert literal `"**Decision (final, until revisited):**"` is in decision_text (load-bearing per CONTEXT.md <code_context> decision-file convention).
      3. test_project_md_references_decision_file: read PROJECT.md (top-level repo PROJECT.md, NOT .planning/PROJECT.md — the analog test_installer_references_security_doc reads from the source-of-truth file; for this cross-ref use the top-level PROJECT.md per CLAUDE.md "Read PROJECT.md" hierarchy). Pytest.fail if PROJECT.md absent. Assert the literal substring `".planning/decisions/no-pypi-in-v0.6.md"` is present.

      NOTE: The repo has BOTH `PROJECT.md` (top-level) and `.planning/PROJECT.md` (planning-side). CONTEXT.md D-09 + RESEARCH.md Pattern 4 + §"PROJECT.md key-decisions table exact format" both refer to `.planning/PROJECT.md` (since the existing 3-column Key Decisions table at .planning/PROJECT.md:74-81 is what gets the appended row). Use `REPO_ROOT / ".planning" / "PROJECT.md"` as PROJECT_MD_PATH for this test. Diagnostic must cite D-09 and the existing table location.
  </action>
  <verify>
    <automated>pytest tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py -v 2>&amp;1 | grep -E "passed|failed"</automated>
    Expected Wave-0 state: 9 RED + 3 non-vacuity GREEN (test_release_yml_structure.py); 3 RED (test_release_md_stop_before_tag.py); 3 RED (test_decision_no_pypi.py). Then run `ruff check tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py` and `ruff format --check tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py` (both must exit 0).
  </verify>
  <acceptance_criteria>
    - File tests/test_release_yml_structure.py exists at top-level tests/
    - File tests/test_release_md_stop_before_tag.py exists at top-level tests/
    - File tests/test_decision_no_pypi.py exists at top-level tests/
    - `grep -c 'REPO_ROOT = Path(__file__).resolve().parents\[1\]' tests/test_release_yml_structure.py` == 1
    - `grep -c 'REPO_ROOT = Path(__file__).resolve().parents\[1\]' tests/test_release_md_stop_before_tag.py` == 1
    - `grep -c 'REPO_ROOT = Path(__file__).resolve().parents\[1\]' tests/test_decision_no_pypi.py` == 1
    - test_release_yml_structure.py contains every literal: `sigstore/gh-action-sigstore-python`, `actions/attest-build-provenance`, `permissions: read-all`, `id-token: write`, `contents: write`, `attestations: write`, `timeout-minutes: 5`, `types: [published]`
    - test_release_yml_structure.py defines AT MINIMUM 9 production tests + 3 non-vacuity synthetic-fixture tests parametrized on workflows_dir (pytest --collect-only -q reports >= 12 tests)
    - test_release_md_stop_before_tag.py contains the literal Python constant strings PRE_EDIT_STEPS_1_THROUGH_6 and PRE_EDIT_STEPS_8_THROUGH_9 with values byte-identical to docs/RELEASE.md lines 122-141 and 145-157 respectively
    - test_release_md_stop_before_tag.py contains the literals `git config --get gitsign.connectorID`, `git tag -s vN.M.P`, `git tag -a vN.M.P`, `docs/MAINTAINER-RUNBOOK.md`
    - test_decision_no_pypi.py contains the literal `**Decision (final, until revisited):**` AND the literal `.planning/decisions/no-pypi-in-v0.6.md`
    - test_decision_no_pypi.py uses `REPO_ROOT / ".planning" / "PROJECT.md"` as PROJECT_MD_PATH (NOT top-level PROJECT.md) per D-09 + RESEARCH.md Pattern 4
    - `pytest tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py --collect-only -q` reports >= 15 tests total (12 + 3 + 3 at minimum)
    - All non-vacuity synthetic-fixture tests in test_release_yml_structure.py PASS NOW (run them isolated: `pytest tests/test_release_yml_structure.py -v -k 'scanner'`; expect all selected tests pass)
    - Production tests are RED (the .yml + docs edits + decision file do not exist yet); each failure diagnostic names the missing file + cites the SIGN-NN requirement and D-NN decision
    - `ruff check tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py` exits 0
    - `ruff format --check tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py` exits 0
    - `grep -Pl '[\x{2013}\x{2014}]' tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py` returns no matches (no em-dashes / en-dashes; CLAUDE.md HR3)
  </acceptance_criteria>
  <done>Three test files committed at tests/ top-level with the assertions above; the production tests are RED-by-design (releasing the GREEN state is Plan 02's job); non-vacuity scanner tests are GREEN; ruff is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/test_release_verification.py + tests/fixtures/sigstore/canonical/README.md (verify_release.py RED scaffold + fixture-dir placeholder)</name>
  <files>tests/test_release_verification.py, tests/fixtures/sigstore/canonical/README.md</files>
  <read_first>
    - tests/test_release_gate.py — primary analog (importlib.util.spec_from_file_location pattern at lines 31-46; per-check unit test shape at lines 84-99; monkeypatch.setenv pattern at lines 146-159). PATTERNS.md §5 lines 467-563 enumerates the exact tests to mirror.
    - tests/test_lint_no_wallclock.py lines 20-32 — secondary analog for the optional subprocess shell-out end-to-end smoke test (PATTERNS.md §5 Pattern C; cross-OS subprocess shape S-1).
    - scripts/release_gate.py — the script-under-test analog whose CheckResult dataclass (lines 137-148), _print_result formatter (lines 634-640), check_pytest_pass subprocess shape (lines 307-330), HORUS_OS_*_PATH_OVERRIDE env-var pattern (lines 643-675), and main() argparse dispatch (lines 678-766) PATTERNS.md §2 maps verify_release.py onto.
    - tests/fixtures/installer/__init__.py lines 1-16 — primary analog for tests/fixtures/sigstore/canonical/README.md content shape (what / how-built / why-committed informational categories); PATTERNS.md §4a documents the deviation to Markdown form per CONTEXT.md D-07.
    - .planning/phases/52-signing-substrate-release-yml-new/52-PATTERNS.md (§2 lines 138-340 verify_release.py shape; §4a lines 414-448 fixture README shape; §5 lines 467-563 test file shape; §S-1, §S-2 cross-OS discipline)
    - .planning/phases/52-signing-substrate-release-yml-new/52-VALIDATION.md (row 4 of Per-Task Verification Map; Wave 0 Requirements bullet 1 enumerating the 5 SIGN-04 tests; Manual-Only Verifications row 1 for the recording procedure prose verbatim)
    - .planning/phases/52-signing-substrate-release-yml-new/52-CONTEXT.md (D-03 stdlib-only contract; D-04 hardcoded EXPECTED_IDENTITY_TEMPLATE + mandatory --cert-oidc-issuer; D-07 fixture provenance; D-08 5-check skeleton with SBOM stub returning ok=None; <specifics> on EXPECTED_IDENTITY mutation rules and canonical fixture --version 0.6.0-rc1)
    - .planning/phases/52-signing-substrate-release-yml-new/52-RESEARCH.md (Example 2 lines 520-764 — the verify_release.py skeleton this test file imports + exercises; Pitfall 1 lines 375-389 — wildcard-identity bug class the import-time assertion defends against)
    - .planning/phases/51-ci-hardening-substrate/51-01-SUMMARY.md (Wave 0 RED-by-design + non-vacuity coverage discipline)
  </read_first>
  <action>
    (A) tests/test_release_verification.py — 5 production tests + supporting non-vacuity + optional subprocess smoke per VALIDATION.md row 4 + Wave 0 Requirements bullet 1 + PATTERNS.md §5 Patterns A-D.

    File header: `from __future__ import annotations`; imports: `importlib.util`, `subprocess`, `sys`, `pathlib.Path`, `pytest`. Set `REPO_ROOT = Path(__file__).resolve().parents[1]`. Define `EXPECTED_ISSUER = "https://token.actions.githubusercontent.com"` (must match the constant Plan 02 lands in scripts/verify_release.py per D-04). Define `CANONICAL_FIXTURE_DIR = REPO_ROOT / "tests" / "fixtures" / "sigstore" / "canonical"` (PATTERNS.md §4b Pattern B path shape).

    Define `_load_verify_release_module()` mirroring PATTERNS.md §5 Pattern A verbatim (lines 472-499): module name `_verify_release_under_test`; script path `REPO_ROOT / "scripts" / "verify_release.py"`; importlib.util.spec_from_file_location + module_from_spec + spec.loader.exec_module; register in sys.modules. If the script file does not exist, the module loader will raise; wrap the loader call in a helper that re-raises with a helpful diagnostic citing SIGN-04 + Plan 02 + D-03.

    Five production tests (mirror PATTERNS.md §5 Pattern B numbered list lines 522-527):
     1. test_canonical_fixture_passes_wheel_check — calls `mod.check_wheel_signature(version="0.6.0-rc1", cert_oidc_issuer=EXPECTED_ISSUER, bundle_path=<canonical .sigstore in CANONICAL_FIXTURE_DIR>, artifact_path=<canonical .whl in CANONICAL_FIXTURE_DIR>)`; asserts `result.ok is True`. Skip with `pytest.skip("Canonical fixture binaries land at v0.6.0-rc1 rehearsal recording time per VALIDATION.md Manual-Only Verifications row 1")` if either fixture file does not yet exist (binaries are out of scope for this plan per Plan 01 decision).
     2. test_missing_issuer_refused — calls `mod.main(["--version", "0.6.0-rc1"])` (no --cert-oidc-issuer); asserts `SystemExit` with `exit_code == 2` (argparse `required=True` exits 2 per release_gate.py precedent).
     3. test_wrong_issuer_refused — calls `mod.main(["--version", "0.6.0-rc1", "--cert-oidc-issuer", "https://example.com/oauth"])`; asserts non-zero exit AND captured stderr contains EXPECTED_ISSUER literal.
     4. test_sbom_stub_returns_skipped — calls `mod.check_sbom_signature(version="0.6.0-rc1")` directly; asserts `result.ok is None` AND `"SKIPPED"` in result.diagnostic AND `"Phase 53"` in result.diagnostic (D-08 SBOM stub return shape).
     5. test_full_run_all_checks_with_canonical_fixture — calls `mod.main(["--version", "0.6.0-rc1", "--cert-oidc-issuer", EXPECTED_ISSUER, "--bundle", str(<canonical>), "--artifact", str(<canonical>)])`; asserts exit 0 (skipped checks per release_gate precedent do NOT count as failure). Skip with the same canonical-binary skip reason as test 1 if fixtures missing.

    Add at minimum TWO non-vacuity tests that PASS NOW even before scripts/verify_release.py exists (these prove the test-file infrastructure works):
     - test_expected_identity_template_invariant_is_documented: open scripts/verify_release.py via .read_text(encoding="utf-8") if file exists else pytest.skip; assert the literal `'refs/tags/{version}'` substring is present in the file source (defends against the wildcard-identity bug class Pitfall 1).
     - test_expected_issuer_constant_documented: same skip-if-missing; assert the literal `'https://token.actions.githubusercontent.com'` substring is present in the file source.

    Add an additional import-time assertion test (RED until Plan 02):
     - test_module_imports_cleanly: call `_load_verify_release_module()`; assert it returns a module object; assert `getattr(mod, "EXPECTED_IDENTITY_TEMPLATE", None)` equals the byte-exact string `"https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/{version}"` (D-04 hardcoded constant); assert `getattr(mod, "EXPECTED_ISSUER", None) == EXPECTED_ISSUER`.

    Optional Pattern C end-to-end subprocess smoke (mirror tests/test_lint_no_wallclock.py:20-32):
     - test_script_runs_via_subprocess: shell out to `[sys.executable, str(REPO_ROOT / "scripts" / "verify_release.py"), "--version", "0.6.0-rc1", "--cert-oidc-issuer", EXPECTED_ISSUER, "--check", "sbom"]` with capture_output=True, text=True, check=False; assert returncode == 0 AND `"SKIP"` in stdout AND `"sbom-signature"` in stdout. Skip if the script file does not exist.

    Note: the binary canonical fixture files (.whl + .sigstore[.json]) are NOT created by this plan (Manual-Only Verification per VALIDATION.md row 1; recorded at v0.6.0-rc1 rehearsal time on a fork before Phase 58 needs the wrong-identity sibling for TEST-24). Tests that need them skip cleanly via pytest.skip with a citation of the recording procedure in CANONICAL_FIXTURE_DIR / "README.md".

    (B) tests/fixtures/sigstore/canonical/README.md — Markdown documentation of the rehearsal recording procedure per CONTEXT.md D-07 + PATTERNS.md §4a + VALIDATION.md Manual-Only Verifications row 1.

    Sections (planner finalizes prose; concrete heading list per PATTERNS.md §4a):
     - `# Canonical sigstore fixtures for verify_release.py`
     - `## Files in this directory` — enumerate the expected filenames (`horus_os-0.6.0rc1-py3-none-any.whl`, `horus_os-0.6.0rc1-py3-none-any.whl.sigstore`; note that the bundle suffix MAY be `.sigstore.json` depending on sigstore-python version, and this README pins the observed suffix at recording time per RESEARCH.md confidence note flagging this as MEDIUM)
     - `## Recording procedure (v0.6.0-rc1 rehearsal)` — verbatim transcribe the 7-step procedure from VALIDATION.md "Manual-Only Verifications" row 1 column "Test Instructions" (gitsign-configure → push v0.6.0-rc1 tag → gh release create --prerelease → wait for release.yml → gh release download to this directory → update this README with the observed suffix → commit)
     - `## Why committed instead of generated` — mirror the rationale of tests/fixtures/installer/__init__.py docstring lines 1-16 + cite that live-signing in CI test runner is forbidden by CIHARD-02 per-job permission discipline (no `id-token: write` outside release.yml's sign-and-attest job)
     - `## Observed bundle filename suffix` — placeholder text saying "PENDING: filled in at v0.6.0-rc1 rehearsal recording time" with a sub-bullet citing the two possible values (`.sigstore` or `.sigstore.json`)
     - `## Phase 58 sibling: tests/fixtures/sigstore/wrong_identity/` — one-paragraph cross-reference noting the wrong-identity fixture is owned by Phase 58 / TEST-24 and follows the same filename convention

    NO em-dashes anywhere (CLAUDE.md HR3; PATTERNS.md S-4). No PII / contributor handles other than `Ridou/horus-os` (CLAUDE.md HR1). Use placeholder values like `your-fork-name` where the maintainer's fork name would appear.
  </action>
  <verify>
    <automated>pytest tests/test_release_verification.py -v 2>&amp;1 | grep -E "passed|failed|skipped"</automated>
    Expected Wave-0 state: production tests 1-5 either RED (cannot import scripts/verify_release.py) or SKIPPED (canonical binaries pending); non-vacuity tests test_expected_identity_template_invariant_is_documented + test_expected_issuer_constant_documented skip cleanly because scripts/verify_release.py does not yet exist. Run `ruff check tests/test_release_verification.py` and `ruff format --check tests/test_release_verification.py` (both must exit 0). Run `ls tests/fixtures/sigstore/canonical/README.md` (must exist).
  </verify>
  <acceptance_criteria>
    - File tests/test_release_verification.py exists at top-level tests/
    - File tests/fixtures/sigstore/canonical/README.md exists
    - `grep -c 'REPO_ROOT = Path(__file__).resolve().parents\[1\]' tests/test_release_verification.py` == 1
    - tests/test_release_verification.py contains the literals: `EXPECTED_IDENTITY_TEMPLATE`, `EXPECTED_ISSUER`, `https://token.actions.githubusercontent.com`, `https://github.com/Ridou/horus-os/.github/workflows/release.yml@refs/tags/{version}`, `_load_verify_release_module`, `_verify_release_under_test`, `check_wheel_signature`, `check_sbom_signature`, `--cert-oidc-issuer`, `--version`, `0.6.0-rc1`
    - tests/test_release_verification.py uses `importlib.util.spec_from_file_location` (mirrors PATTERNS.md §5 Pattern A)
    - tests/test_release_verification.py defines AT MINIMUM 5 production tests + 2 non-vacuity tests + 1 module-import test (pytest --collect-only -q reports >= 8 tests in this file)
    - `pytest tests/test_release_verification.py -v 2>&1 | grep -c "SKIP\|FAIL\|skipped\|failed"` >= 5 (any combination of skips/fails proves the file collects without import error and the production tests are not silently passing while the production code is absent)
    - tests/fixtures/sigstore/canonical/README.md is non-empty and contains the headings `# Canonical sigstore fixtures for verify_release.py`, `## Files in this directory`, `## Recording procedure (v0.6.0-rc1 rehearsal)`, `## Why committed instead of generated`, `## Observed bundle filename suffix`, `## Phase 58 sibling: tests/fixtures/sigstore/wrong_identity/`
    - tests/fixtures/sigstore/canonical/README.md contains the literal `v0.6.0-rc1` AND the literal `gh release create` AND the literal `CIHARD-02`
    - `ruff check tests/test_release_verification.py` exits 0
    - `ruff format --check tests/test_release_verification.py` exits 0
    - `grep -Pl '[\x{2013}\x{2014}]' tests/test_release_verification.py tests/fixtures/sigstore/canonical/README.md` returns no matches (no em-dashes / en-dashes; CLAUDE.md HR3)
    - The test file uses `sys.executable` (not bare `"python"`) and `str(path)` on every subprocess arg position (S-1 cross-OS discipline)
  </acceptance_criteria>
  <done>tests/test_release_verification.py committed at tests/ top-level with the assertions above; tests/fixtures/sigstore/canonical/README.md committed; production tests are RED-or-SKIPPED-by-design; ruff is clean; non-vacuity tests are infrastructure-correct (cleanly skip in the absence of scripts/verify_release.py); the test file is ready for Plan 02 to flip GREEN by creating the script.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full Wave 0 verification sequence:

```bash
pytest tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py -v
ruff check tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py
ruff format --check tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py
pytest --collect-only -q tests/test_release_verification.py tests/test_release_yml_structure.py tests/test_release_md_stop_before_tag.py tests/test_decision_no_pypi.py
```

Expected end state:
- Total tests collected across the four files: >= 23 (>=8 + >=12 + >=3 + >=3)
- All non-vacuity synthetic-fixture tests PASS (the scanners are proven non-vacuously to fire)
- All production tests FAIL or SKIP (release.yml, scripts/verify_release.py, .planning/decisions/no-pypi-in-v0.6.md, docs/RELEASE.md edits, and PROJECT.md key-decisions append do not yet exist)
- Ruff clean on all four files
- No em-dashes in any new file
- Full suite delta: no NEW failures outside these four files (existing 1011-test suite continues to pass)
</verification>

<success_criteria>
- Five Wave 0 files exist and are committed: 4 test files + 1 fixture README
- Production assertions in each test file are RED-by-design and cite the SIGN-NN requirement + D-NN decision in their failure diagnostics
- Non-vacuity / synthetic-fixture tests pass NOW, proving each scanner fires on a known violation written to tmp_path
- Tests use `Path(__file__).resolve().parents[1]` (top-level tests/ shape) not `parents[2]` (nested shape)
- Every test follows cross-OS subprocess discipline (`sys.executable`, `str(path)`, `capture_output=True, text=True, check=False`, `encoding="utf-8"`)
- No em-dashes anywhere in committed prose (CLAUDE.md HR3)
- Ruff lint + format pass on every new file
- The Wave-0 test infrastructure is provably ready for Plan 02 to flip GREEN by creating .github/workflows/release.yml + scripts/verify_release.py + .planning/decisions/no-pypi-in-v0.6.md and editing docs/RELEASE.md + .planning/PROJECT.md
</success_criteria>

<output>
After completion, create `.planning/phases/52-signing-substrate-release-yml-new/52-01-SUMMARY.md`
using the standard SUMMARY template. Required sections:
- Tasks Completed table
- Production Assertions: RED vs GREEN State table (mirrors Phase 51 Plan 01 SUMMARY §"Production Assertions: RED vs GREEN State")
- Non-Vacuity Coverage table (which scanners are proven to fire on synthetic violations)
- Test Collection Counts (pytest --collect-only -q output for each new file)
- Self-Check section confirming all file existence + ruff clean + em-dash absence
- Requirements Coverage table mapping SIGN-01..05 to test files
- Hand-off Notes section listing what Plan 02 must create to flip each RED assertion GREEN
</output>
