---
phase: 49-three-os-install-gate-release-gate-extension
plan: 01
type: execute
wave: 1
depends_on: [48]
files_modified:
  - .github/workflows/ci.yml
  - src/horus_os/__main__.py
  - src/horus_os/cli/plugins_cmd.py
  - src/horus_os/plugins/installer.py
  - scripts/release_gate.py
  - tests/test_release_gate_v0_5_checks.py
  - tests/plugins/test_plugins_grant_all.py
  - docs/RELEASE.md
autonomous: true
requirements:
  - TEST-20
  - REL-11

must_haves:
  truths:
    - "`.github/workflows/ci.yml` carries a NEW top-level job named `install-smoke-plugin` (verbatim string literal — the release-gate grep check in this plan asserts this exact name) with a 3-OS × 2-Python matrix (`os: [ubuntu-latest, macos-latest, windows-latest]` × `python-version: ['3.11', '3.12']`, six jobs total, `fail-fast: false`) that `needs: lint-and-test` and runs ADDITIVELY alongside the existing v0.4 `install-smoke`, `install-smoke-no-otel`, and `install-smoke-with-otel` jobs. None of the v0.4 jobs are touched — the diff against `main` shows zero hunks in the existing job blocks (lines 1-159 byte-identical pre/post). (TEST-20)"
    - "The `install-smoke-plugin` job's steps execute in order: (a) `actions/checkout@v4`, (b) `actions/setup-python@v5` with `cache: pip` and `cache-dependency-path: pyproject.toml` (mirror of every other job in the file), (c) `python -m pip install --upgrade pip && python -m pip install -e .[dev]` to install horus-os in editable mode + dev tools (`pytest`, `jq` via the OS path), (d) `python -m pip install -e ./examples/horus-os-example-plugin --no-deps` to install the Phase 48 reference plugin, (e) `python -m horus_os init --non-interactive --provider anthropic --api-key dummy --data-dir $HORUS_DATA` to bootstrap a clean data dir (the wizard's idempotent path; `dummy` key is acceptable because the smoke does not run an agent), (f) launch `python -m horus_os serve --port 18000 --data-dir $HORUS_DATA` IN THE BACKGROUND with OS-specific syntax: bash `python -m horus_os serve ... &` on ubuntu+macos, PowerShell `Start-Process -NoNewWindow python -ArgumentList '-m','horus_os','serve','--port','18000','--data-dir',$env:HORUS_DATA` on windows, (g) poll `curl -fsS http://127.0.0.1:18000/api/system/ping` with up to 30 attempts at 1s intervals (a `bash`/`pwsh` `for`-loop, NOT `sleep 30` — Pitfall 3 time.time() lint gate is repo-wide and a flat sleep would also be fragile), (h) `curl -fsS http://127.0.0.1:18000/api/plugins` piped through `python -c \"import json,sys; b=json.load(sys.stdin); rows=[p for p in b['plugins'] if p['name']=='horus-os-example-plugin']; assert len(rows)==1, rows; r=rows[0]; assert r['status'] in ('pending','error'), r; assert set(r['pending_capabilities'])=={'filesystem.read','secrets.read'}, r\"` (no `jq` dependency; the assertion is hermetic Python that does NOT require horus-os to be importable from the curl-running shell). (TEST-20)"
    - "The `install-smoke-plugin` job, AFTER asserting `status='pending'`, runs `python -m horus_os plugins grant horus-os-example-plugin --all --data-dir $HORUS_DATA` (the NEW `--all` flag this plan ships in scope item 7). The command MUST exit 0 and stdout MUST contain the literal substring `Granted 2 capabilities` (the new grant handler in `plugins_cmd.py` prints this verbatim count message). The job then kills the background `horus-os serve` process (bash `kill $SERVE_PID`; PowerShell `Stop-Process -Id $servePid -Force`), re-launches it with the same args, polls `/api/system/ping` until ready, and re-curls `/api/plugins` with the same Python assertion BUT now asserting `r['status']=='loaded'`, `set(r['granted_capabilities'])=={'filesystem.read','secrets.read'}`, and `r['pending_capabilities']==[]`. All six matrix combinations (3-OS × 2-Python) MUST go green; matrix entries `fail-fast: false` so one OS failure does not mask others. (TEST-20)"
    - "`src/horus_os/__main__.py:383-396` (the `pi_grant` argparse block) gains a NEW mutually-exclusive group: EITHER positional `capability` (the v0.4 shape, preserved byte-identically for backwards compat) OR `--all` (the new flag, NO positional capability). Implementation: replace the bare `pi_grant.add_argument('capability', ...)` with `grant_target = pi_grant.add_mutually_exclusive_group(required=True); grant_target.add_argument('capability', nargs='?', default=None, help='Capability string (e.g. \\'filesystem.read\\')'); grant_target.add_argument('--all', action='store_true', dest='grant_all', help='Grant every capability declared in the plugin manifest')`. Tests assert: `python -m horus_os plugins grant FOO` (no capability and no `--all`) exits 2 with argparse error; `python -m horus_os plugins grant FOO filesystem.read` matches v0.4 behavior byte-identically; `python -m horus_os plugins grant FOO --all filesystem.read` exits 2 (mutually exclusive)."
    - "`src/horus_os/cli/plugins_cmd.py::_cmd_grant` (lines 259-268) is extended: when `args.grant_all is True`, the handler reads the plugin's manifest hash + capabilities from the `plugins` table (already loaded via `_load_existing_plugin` in `installer.py`), iterates the capability list (which IS the manifest's declared `capabilities` field, persisted at install time), calls `installer.grant_capability(args.name, cap, db=db)` once per capability, accumulates the count, and prints `f'Granted {n} capabilities to {args.name}: {\", \".join(caps)}.'`. When `args.grant_all is False` (the v0.4 path), the handler invokes the existing `installer.grant_capability(args.name, args.capability, db=db)` + prints the existing message — byte-identical to v0.4. A new `installer.grant_all_capabilities(name, *, db) -> list[str]` helper centralizes the read-manifest-then-iterate logic so the same code path is reachable from a future REST endpoint without re-implementing it in `_cmd_grant`."
    - "`scripts/release_gate.py` gains FOUR new check functions, each shaped EXACTLY like the existing `check_pricing_freshness`/`check_ci_two_variant_smoke_present` pattern (one positional `Path` arg, returns `CheckResult` dataclass, has a module-level path-override constant resolved via env-var helper `_resolved_*_path()`). The four new checks: (a) `check_docs_manifest_schema_drift(schema_path: Path, *, runtime_schema_factory=None) -> CheckResult` — call MANIFEST_V1_SCHEMA.model_json_schema() (via the imported `horus_os.plugins.manifest` module loaded with the same lazy-sys.path-insert trick `scripts/build_manifest_schema.py` uses at lines 31-37), serialize with `json.dumps(schema, indent=2, sort_keys=True) + '\\n'`, compare byte-for-byte against `schema_path.read_bytes()`; on mismatch return `ok=False` with a diff snippet (first 5 lines of `difflib.unified_diff` output) named `docs-drift`. (b) `check_plugin_install_smoke_ci_present(ci_yml_path: Path) -> CheckResult` — read `ci_yml_path.read_text(encoding='utf-8')`, assert the literal substring `install-smoke-plugin` appears at least once; on miss return `ok=False, diagnostic=\"install-smoke-plugin job missing from ci.yml; TEST-20 contract violated\"`, named `plugin-install-smoke-ci`. (c) `check_reference_plugin_manifest_valid(manifest_path: Path) -> CheckResult` — read the file as bytes, call `validate_manifest(toml_bytes)` from `horus_os.plugins.manifest` (lazy-import via the same sys.path trick), catch `ManifestValidationError` (existing exception from manifest.py) and return `ok=False, diagnostic=f'reference plugin manifest invalid: {exc}'`; on success return `ok=True, diagnostic=f'manifest validates as plugin={spec.name} v{spec.version}'`, named `reference-plugin-manifest-valid`. (d) `check_v0_4_fixture_roundtrip(fixture_path: Path) -> CheckResult` — copy the fixture to a tempfile (NEVER mutate the committed fixture — Pitfall 11 isolation), open via `Database(tmp_path)`, call `db.init()` (triggers v5→v6 ADD COLUMNs), then introspect via `PRAGMA table_info(llm_calls)` and `PRAGMA table_info(tool_invocations)` to assert `plugin_name` column exists in both, plus `SELECT name FROM sqlite_master WHERE type='table'` returns `plugins`, `plugin_capabilities`, `plugin_status`, plus `SELECT name FROM sqlite_master WHERE type='index'` contains `idx_tool_invocations_plugin`, plus assert the existing pre-migration row counts in `llm_calls`/`tool_invocations` are preserved post-init; run `db.init()` a SECOND time and assert no exception (idempotency); named `v0-4-fixture-roundtrip`. (REL-11)"
    - "`scripts/release_gate.py::main()` is extended: the `argparse` `--check` choices tuple gains four new members `('docs-drift', 'plugin-install', 'reference-manifest', 'fixture-roundtrip')` so the existing six-line dispatch block grows by four parallel `if selected in (None, '<name>'):` blocks. Module-level constants added: `DEFAULT_DOCS_SCHEMA_PATH = REPO_ROOT / 'docs' / 'manifest-v1.schema.json'`, `DEFAULT_REFERENCE_PLUGIN_MANIFEST_PATH = REPO_ROOT / 'examples' / 'horus-os-example-plugin' / 'horus-plugin.toml'`, `DEFAULT_V0_4_FIXTURE_PATH = REPO_ROOT / 'tests' / 'fixtures' / 'v0_4_database.sqlite3'`. Three new env-var override hooks named `HORUS_OS_DOCS_SCHEMA_PATH_OVERRIDE`, `HORUS_OS_REFERENCE_PLUGIN_MANIFEST_PATH_OVERRIDE`, `HORUS_OS_V0_4_FIXTURE_PATH_OVERRIDE` — same pattern as the existing `HORUS_OS_PRICING_PATH_OVERRIDE` / `HORUS_OS_CI_YML_PATH_OVERRIDE`. The four existing v0.4 checks remain BYTE-IDENTICAL (no edits to `check_pricing_freshness`, `check_ci_two_variant_smoke_present`, `check_wheel_pricing_bundle`, `check_pytest_pass` — `git diff scripts/release_gate.py | grep -A1 '^-' | grep -v '^@@' | wc -l` returns zero deletions in lines 110-262). Default invocation (`python scripts/release_gate.py` with no args) runs ALL EIGHT checks (4 old + 4 new); selective invocation (`python scripts/release_gate.py --check docs-drift`) runs only the named one. Exit semantics preserved: 0 on full pass, 1 on any fail, ONE diagnostic per failing check (no short-circuit). (REL-11)"
    - "`scripts/release_gate.py`'s `docs-drift` check imports `MANIFEST_V1_SCHEMA` via the SAME lazy-sys.path-insert idiom used by `scripts/build_manifest_schema.py:31-37` (insert `REPO_ROOT / 'src'` into `sys.path[0]` if not already present, then `from horus_os.plugins.manifest import MANIFEST_V1_SCHEMA`). The serializer is the canonical pair `json.dumps(schema, indent=2, sort_keys=True) + '\\n'` (matching `build_manifest_schema.py:44`) so the comparison is byte-identical when the docs file was generated by the build script — no risk of the release gate and the build script disagreeing on serialization. A regression check in the new test file asserts the serializer output of BOTH scripts is byte-identical on the same in-memory schema dict."
    - "`tests/test_release_gate_v0_5_checks.py` exists, runs in the default `pytest` invocation (no markers), and contains AT LEAST seven test functions — one positive-path test per new check plus the mutation-driven negative-path tests called out in the scope: `test_docs_drift_passes_when_in_sync` (regenerate the docs file via `build_manifest_schema.main()` into a tmp path, then point the check at it via `HORUS_OS_DOCS_SCHEMA_PATH_OVERRIDE`, assert `ok=True`), `test_docs_drift_fails_on_mutation` (copy the docs file to tmp, mutate one byte — e.g. add `\"x\":\"x\",` after the opening brace — assert `ok=False` and the diagnostic contains `difflib` markers `---`/`+++`), `test_plugin_install_smoke_ci_passes_when_job_present` (point at the live `ci.yml` after this phase ships, assert `ok=True`), `test_plugin_install_smoke_ci_fails_when_job_removed` (copy `ci.yml` to tmp, `sed`-equivalent strip the `install-smoke-plugin` job lines, point the check at the tmp file, assert `ok=False` and the diagnostic mentions `TEST-20`), `test_reference_plugin_manifest_valid_passes` (point at `examples/horus-os-example-plugin/horus-plugin.toml`, assert `ok=True` and the diagnostic names the plugin), `test_v0_4_fixture_roundtrip_passes` (point at the live fixture, assert `ok=True` and the diagnostic mentions the v5→v6 table count delta), `test_v0_4_fixture_roundtrip_fails_on_data_loss` (build a synthetic SQLite file with `schema_version=5` BUT WITHOUT `plugin_name` columns on `llm_calls`/`tool_invocations` AND with the pre-migration row in `tool_invocations` deleted between `init()` calls — assert the check returns `ok=False`; OR alternatively assert that pointing at a non-SQLite file like `pyproject.toml` returns `ok=False, diagnostic` containing `not a database`). All seven tests use the in-process `importlib.util.spec_from_file_location` loader pattern from `tests/test_release_gate.py:_load_release_gate_module` — no subprocess shells out. (REL-11)"
    - "`tests/plugins/test_plugins_grant_all.py` exists and contains tests for the new `--all` flag: `test_grant_all_grants_every_capability` (set up a plugins-table row with `capabilities=['filesystem.read','secrets.read']`, invoke `_cmd_grant` with a Namespace where `grant_all=True`, assert two rows land in `plugin_capability_grants` with state `granted` and the stdout contains the literal `'Granted 2 capabilities'`), `test_grant_all_idempotent_on_already_granted` (run twice, assert second invocation does not raise and final stdout still says `'Granted 2 capabilities'`), `test_grant_all_mutually_exclusive_with_positional` (argparse-level: `python -m horus_os plugins grant FOO --all filesystem.read` returns exit code 2 and stderr contains `not allowed with argument`), `test_grant_positional_byte_identical_to_v0_4` (run the v0.4 shape `grant FOO filesystem.read` and assert ONE grant row, stdout is the v0.4 message `'Granted filesystem.read to FOO.'` UNCHANGED). Tests use the same `tmp_path / 'horus.sqlite'` + `Database.init()` + in-process namespace pattern as `tests/test_cli_*` files; no subprocess."
    - "`docs/RELEASE.md` is updated additively (no edit to lines 1-9 — the doc preamble — and no edit to the 'Why the release gate exists' section at lines 137-162) with three concrete changes: (a) the 'Release gate' section gains a new sentence after the existing four-bullet list of checks: 'v0.5 extends the gate with four additional checks: docs-drift between `MANIFEST_V1_SCHEMA` and `docs/manifest-v1.schema.json`, plugin-install-smoke-ci presence in `ci.yml`, reference-plugin manifest validation, and v0.4 fixture v5→v6 round-trip — the gate runs all EIGHT checks by default at v0.5.' (b) the 'Refreshing pricing.json' section gains a sibling sub-section 'Refreshing docs/manifest-v1.schema.json' with the one-step recipe `python scripts/build_manifest_schema.py` and the explanation that the script is idempotent + the release gate's `docs-drift` check refuses to tag when the file is out of sync. (c) the 'Release procedure' section step 1 ('Refresh `pricing.json` if its `updated_at` is older than 14 days') gains a sibling step 1b: 'Regenerate `docs/manifest-v1.schema.json` via `python scripts/build_manifest_schema.py` and commit the result if there is a diff (the docs-drift gate refuses to tag otherwise).' Step 2's reference to 'all four checks pass' updates to 'all eight checks pass'. The pre-release-checklist line about `install-smoke-no-otel` and `install-smoke-with-otel` jobs gains a third entry: `install-smoke-plugin`. No other prose in the doc moves. (REL-11)"
    - "After this phase ships, running `python scripts/release_gate.py --skip-build` (skips only the wheel build, runs the other seven checks) from a clean repo with the Phase 48 reference plugin present and the Phase 47 schema doc committed exits 0; running it with `HORUS_OS_DOCS_SCHEMA_PATH_OVERRIDE` pointed at a deliberately-mutated copy exits 1 with a single FAIL line naming `docs-drift`. The exit-code contract is asserted by `tests/test_release_gate_v0_5_checks.py::test_main_exit_codes_with_each_new_check_failing` which uses `subprocess.run([sys.executable, 'scripts/release_gate.py', '--check', 'docs-drift'], env=ENV_WITH_OVERRIDE)` — the ONE subprocess shell-out in the test file, mirroring `tests/test_release_gate.py::test_main_returns_one_on_pricing_fail` shape."
    - "After this phase ships, pushing the branch to GitHub triggers six new `install-smoke-plugin` matrix jobs (3 OS × 2 Python) alongside the existing six `install-smoke-no-otel` jobs and six `install-smoke-with-otel` jobs and three `install-smoke` jobs and six `lint-and-test` jobs. The total CI job count goes from 21 to 27 (no v0.4 job removed). The reference plugin (`examples/horus-os-example-plugin/`, Phase 48) is BYTE-IDENTICAL to its Phase 48 shape — no edits to the plugin source tree are required from Phase 49; Phase 49 only TESTS what Phase 48 shipped. (TEST-20)"

  artifacts:
    - path: .github/workflows/ci.yml
      provides: "v0.4 byte-identical lines 1-159 PLUS a new install-smoke-plugin job block (~45-60 lines) appended after install-smoke-with-otel. Matrix: 3-OS × 2-Python = 6 jobs. Steps run pip install, init data dir, launch serve in background, poll ping, curl /api/plugins twice (before + after grant --all)."
      min_lines: 200
      contains: "install-smoke-plugin"
    - path: src/horus_os/__main__.py
      provides: "Argparse update: the pi_grant parser gains a mutually-exclusive group between positional 'capability' and '--all' flag. Byte-identical elsewhere."
      contains: "add_mutually_exclusive_group"
    - path: src/horus_os/cli/plugins_cmd.py
      provides: "_cmd_grant gains an args.grant_all branch that calls installer.grant_all_capabilities; falls through to the v0.4 single-capability path otherwise."
      contains: "grant_all"
    - path: src/horus_os/plugins/installer.py
      provides: "New grant_all_capabilities(name, *, db) -> list[str] helper centralizing the read-manifest-then-iterate logic. grant_capability (v0.4 shape) untouched."
      contains: "def grant_all_capabilities"
    - path: scripts/release_gate.py
      provides: "Four NEW check functions (check_docs_manifest_schema_drift, check_plugin_install_smoke_ci_present, check_reference_plugin_manifest_valid, check_v0_4_fixture_roundtrip) PLUS three NEW default-path constants PLUS three NEW env override hooks PLUS argparse choices extended PLUS main() dispatch extended. Four EXISTING checks byte-identical."
      min_lines: 500
      contains: "check_docs_manifest_schema_drift"
    - path: tests/test_release_gate_v0_5_checks.py
      provides: "Seven+ test functions covering positive + mutation-driven negative paths for each of the four new checks, plus one subprocess shell-out test for end-to-end exit-code contract."
      min_lines: 250
      contains: "test_docs_drift_passes_when_in_sync"
    - path: tests/plugins/test_plugins_grant_all.py
      provides: "Four tests covering --all flag: grants every capability, idempotent on re-run, mutually-exclusive with positional, v0.4 positional path byte-identical."
      min_lines: 100
      contains: "test_grant_all_grants_every_capability"
    - path: docs/RELEASE.md
      provides: "Three additive edits: release-gate section gains the four-new-checks paragraph; refreshing-pricing section gains a sibling refreshing-schema sub-section; release-procedure step 1 gains a sibling step 1b for schema regeneration; step 2 text updates from 'four' to 'eight' checks."
      contains: "manifest-v1.schema.json"

  key_links:
    - from: ".github/workflows/ci.yml"
      to: "examples/horus-os-example-plugin"
      via: "pip install -e ./examples/horus-os-example-plugin"
      pattern: "pip install -e \\./examples/horus-os-example-plugin"
    - from: "scripts/release_gate.py"
      to: "src/horus_os/plugins/manifest.py::MANIFEST_V1_SCHEMA"
      via: "lazy sys.path insert + from horus_os.plugins.manifest import MANIFEST_V1_SCHEMA (mirrors build_manifest_schema.py)"
      pattern: "from horus_os.plugins.manifest import"
    - from: "scripts/release_gate.py::check_docs_manifest_schema_drift"
      to: "docs/manifest-v1.schema.json"
      via: "byte-for-byte comparison after json.dumps(indent=2, sort_keys=True) + trailing newline"
      pattern: "json\\.dumps\\(.*indent=2.*sort_keys=True"
    - from: "scripts/release_gate.py::check_reference_plugin_manifest_valid"
      to: "src/horus_os/plugins/manifest.py::validate_manifest"
      via: "import + call with toml_bytes from examples/horus-os-example-plugin/horus-plugin.toml"
      pattern: "validate_manifest"
    - from: "scripts/release_gate.py::check_v0_4_fixture_roundtrip"
      to: "src/horus_os/storage.py::Database.init (v5→v6 migration)"
      via: "tempfile copy of tests/fixtures/v0_4_database.sqlite3 → Database(tmp).init() → PRAGMA table_info introspection"
      pattern: "Database\\(.*tmp"
    - from: "src/horus_os/cli/plugins_cmd.py::_cmd_grant (--all branch)"
      to: "src/horus_os/plugins/installer.py::grant_all_capabilities"
      via: "args.grant_all is True → installer.grant_all_capabilities(args.name, db=db) → loops via PermissionService.grant"
      pattern: "grant_all_capabilities"
    - from: ".github/workflows/ci.yml::install-smoke-plugin (windows steps)"
      to: "horus-os serve background process"
      via: "Start-Process -NoNewWindow python -ArgumentList '-m','horus_os','serve',...  (NOT '&' which is bash-only)"
      pattern: "Start-Process"
    - from: "docs/RELEASE.md (release-gate section)"
      to: "scripts/release_gate.py (four new checks)"
      via: "additive paragraph naming docs-drift, plugin-install-smoke-ci, reference-plugin-manifest-valid, v0-4-fixture-roundtrip"
      pattern: "docs-drift"
---

<objective>
Ship Phase 49: the v0.5 release-quality gate. Two parallel deliverables:

1. **TEST-20 — 3-OS × 2-Python install-smoke matrix for the reference plugin.** Six new CI jobs (`install-smoke-plugin` on macos-latest + ubuntu-latest + windows-latest × Python 3.11 + 3.12) that `pip install -e ./examples/horus-os-example-plugin`, boot horus-os, assert the plugin appears in `/api/plugins` with `status='pending'`, run `horus-os plugins grant horus-os-example-plugin --all` (the new `--all` flag), restart, and assert `status='loaded'`. Lives alongside (not replacing) the existing v0.4 `install-smoke-no-otel` + `install-smoke-with-otel` matrices.

2. **REL-11 — `scripts/release_gate.py` extended with four new v0.5 checks** alongside the four existing v0.4 checks (eight checks total at v0.5): docs-drift (`MANIFEST_V1_SCHEMA` ↔ `docs/manifest-v1.schema.json`), plugin-install-smoke-ci (grep `install-smoke-plugin` literal in `ci.yml`), reference-plugin-manifest-valid (runtime `validate_manifest()` accepts the reference plugin's TOML), and v0-4-fixture-roundtrip (`tests/fixtures/v0_4_database.sqlite3` survives the v5→v6 migration with all expected tables/columns/indexes).

Purpose: gates the v0.5.0 tag in Phase 50. v0.4 byte-identity contract preserved everywhere — no existing test, no existing CI job, no existing release-gate check changes shape. This phase is purely ADDITIVE plus the small `--all` flag extension on the grant CLI.

Output: 8 files modified/created; CI grows from 21 to 27 jobs; release gate grows from 4 to 8 checks; `horus-os plugins grant --all` shipped as a v0.5 ergonomics improvement (the CI step needs it; the dashboard's grant-all button will also call it eventually).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<!-- v0.4 release gate substrate — the script we are extending -->
@scripts/release_gate.py
@scripts/build_manifest_schema.py
@docs/RELEASE.md

<!-- v0.5 manifest schema substrate (Phase 41 + Phase 47) -->
@src/horus_os/plugins/manifest.py
@src/horus_os/plugins/spec.py
@docs/manifest-v1.schema.json

<!-- v0.5 storage substrate (Phase 41) — the v5→v6 migration we re-assert -->
@src/horus_os/storage.py

<!-- Reference plugin (Phase 48) — installed by the 3-OS smoke -->
@examples/horus-os-example-plugin/horus-plugin.toml
@examples/horus-os-example-plugin/pyproject.toml
@tests/plugins/test_reference_plugin_install_local.py

<!-- Existing CI matrix — the file we extend additively -->
@.github/workflows/ci.yml

<!-- Existing grant CLI shape — the byte-identity baseline for --all extension -->
@src/horus_os/__main__.py
@src/horus_os/cli/plugins_cmd.py
@src/horus_os/plugins/installer.py

<!-- Phase 12 docs-drift test — already asserts the in-test schema diff; release-gate's check is the same idea from the script -->
@tests/test_plugin_pitfalls/test_pitfall_12_docs_drift.py

<!-- Existing release-gate tests — pattern to mirror for the new tests -->
@tests/test_release_gate.py

<interfaces>
<!-- The contract surface this plan consumes. Executor uses these directly. -->

From src/horus_os/plugins/manifest.py:
```python
class MANIFEST_V1_SCHEMA(BaseModel):
    # Pydantic v2 model. Use .model_json_schema() to produce canonical JSON Schema dict.

def validate_manifest(toml_bytes: bytes) -> PluginSpec:
    # Parses + validates a horus-plugin.toml. Raises ManifestValidationError on failure.
```

From src/horus_os/plugins/spec.py:
```python
@dataclass(frozen=True)
class PluginSpec:
    name: str
    version: str
    description: str
    author: str
    license: str
    homepage: str | None
    issue_tracker: str | None
    horus_os_compat: str
    capabilities: tuple[CapabilityRequest, ...]
    contributions: ManifestContributions
    manifest_hash: str  # sha256(capabilities_set) — used by grant flow
    source: str  # "filesystem" or "entry_point"
```

From src/horus_os/storage.py (the v5→v6 migration target):
```python
SCHEMA_VERSION = 6

class Database:
    def __init__(self, path: Path) -> None: ...
    def init(self) -> None:
        # Idempotent. On a v5 fixture, runs the ALTER TABLE blocks
        # at lines 264-281 (adds llm_calls.plugin_name + tool_invocations.plugin_name)
        # AND creates idx_tool_invocations_plugin (lines 294-297).
        # The plugins / plugin_capabilities / plugin_status tables come from
        # SCHEMA_SQL's CREATE TABLE IF NOT EXISTS blocks (lines 136-184).
```

From scripts/release_gate.py (the script we are extending):
```python
@dataclass(frozen=True)
class CheckResult:
    name: str
    ok: bool | None  # True=pass, False=fail, None=skip
    diagnostic: str

REPO_ROOT = Path(__file__).resolve().parent.parent

# Existing checks (BYTE-IDENTITY PROTECTED — do not modify):
def check_pricing_freshness(pricing_path, max_age_days=14) -> CheckResult: ...
def check_ci_two_variant_smoke_present(ci_yml_path: Path) -> CheckResult: ...
def check_wheel_pricing_bundle(repo_root: Path) -> CheckResult: ...
def check_pytest_pass(repo_root: Path) -> CheckResult: ...

# Existing --check choices (extend, do not replace):
parser.add_argument("--check", choices=("pricing", "wheel", "ci", "tests"), ...)
```

From scripts/build_manifest_schema.py (the canonical serializer — REUSE, do not reimplement):
```python
# Line 31-37: lazy sys.path insert pattern
REPO_ROOT = pathlib.Path(__file__).resolve().parents[1]
SRC = REPO_ROOT / "src"
if str(SRC) not in sys.path:
    sys.path.insert(0, str(SRC))

# Line 42-47: the canonical serialization the release gate must use byte-identically
def main() -> int:
    schema = MANIFEST_V1_SCHEMA.model_json_schema()
    payload = json.dumps(schema, indent=2, sort_keys=True) + "\n"
    OUTPUT_PATH.write_text(payload, encoding="utf-8")
```

From src/horus_os/cli/plugins_cmd.py (the grant handler we extend):
```python
def _cmd_grant(db: Database, args: argparse.Namespace, stdout, stderr) -> int:
    # v0.4 shape (lines 259-268). args has .name + .capability.
    # After this phase: args may have .grant_all (bool); when True, ignore .capability,
    # iterate the plugin's manifest capabilities, call installer.grant_all_capabilities.
```

From src/horus_os/plugins/installer.py (the grant helper we extend):
```python
def grant_capability(name: str, capability: str, *, db: Database) -> None:
    # v0.4 shape (line 1002). Single capability. UNCHANGED.

# NEW in this phase:
def grant_all_capabilities(name: str, *, db: Database) -> list[str]:
    # Reads plugin's capabilities from the plugins table, iterates, calls grant_capability
    # per capability, returns the list of granted capability strings.
```

From the existing CI matrix shape (replicate exactly):
```yaml
# .github/workflows/ci.yml — the matrix block to mirror for install-smoke-plugin
strategy:
  fail-fast: false
  matrix:
    os: [ubuntu-latest, macos-latest, windows-latest]
    python-version: ["3.11", "3.12"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the --all flag on horus-os plugins grant (CLI + installer + tests)</name>
  <files>src/horus_os/__main__.py, src/horus_os/cli/plugins_cmd.py, src/horus_os/plugins/installer.py, tests/plugins/test_plugins_grant_all.py</files>
  <behavior>
    - Test 1: `python -m horus_os plugins grant FOO --all` (with FOO registered with capabilities ['filesystem.read','secrets.read']) inserts two granted rows in plugin_capabilities and stdout contains literal "Granted 2 capabilities".
    - Test 2: `python -m horus_os plugins grant FOO --all` run twice in succession — second run does not raise, final grant_capabilities row count is still 2 (idempotent), stdout still contains "Granted 2 capabilities".
    - Test 3: `python -m horus_os plugins grant FOO --all filesystem.read` (both positional AND --all) exits 2 with argparse mutex error; stderr contains "not allowed with argument".
    - Test 4: `python -m horus_os plugins grant FOO filesystem.read` (v0.4 positional shape, no --all) BYTE-IDENTICAL to v0.4: one grant row, stdout contains "Granted filesystem.read to FOO." UNCHANGED.
    - Test 5: `python -m horus_os plugins grant FOO` (neither positional nor --all) exits 2 with argparse required-one-of error.
  </behavior>
  <action>
Extend the `pi_grant` argparse block at `src/horus_os/__main__.py:383-396` to use `add_mutually_exclusive_group(required=True)` between the positional `capability` (preserved verbatim, nargs='?', default=None) and a new `--all` flag (action='store_true', dest='grant_all', help='Grant every capability declared in the plugin manifest'). All other lines in the file BYTE-IDENTICAL.

Extend `_cmd_grant` in `src/horus_os/cli/plugins_cmd.py:259-268`: when `getattr(args, 'grant_all', False)` is True, call `installer.grant_all_capabilities(args.name, db=db)` (the new helper), receive the list of granted capability strings, print `f'Granted {len(granted)} capabilities to {args.name}: {", ".join(granted)}.\n'` to stdout. When grant_all is False, fall through to the EXISTING v0.4 path — `installer.grant_capability(args.name, args.capability, db=db)` + the existing `f'Granted {args.capability} to {args.name}.\n'` message. The v0.4 branch is byte-identical to today.

Add `grant_all_capabilities(name: str, *, db: Database) -> list[str]` to `src/horus_os/plugins/installer.py` (right after `grant_capability` at line ~1019): reuse `_load_existing_plugin` to fetch the (version, manifest_hash) tuple, then SELECT the requested capability names for this plugin from the plugins/plugin_capabilities tables (use the `capabilities` field stored on the plugins row via Phase 44's install path — concretely, parse `manifest_hash` back to the capability set via PluginRegistry.get(name).capabilities OR re-validate the on-disk manifest at install-record time; pick the path that does not require touching the on-disk manifest because the plugin may be uninstalled mid-flight). Iterate the capability list, call `PermissionService(db).grant(name, version, cap, actor='cli', manifest_hash=manifest_hash)` once per capability, accumulate the list, return it. The function is silent on already-granted state — PermissionService.grant is idempotent per the Phase 43 contract.

Wire tests in NEW file `tests/plugins/test_plugins_grant_all.py` mirroring the in-process Namespace + StringIO pattern from `tests/test_cli_*` (do NOT spawn subprocesses for tests 1, 2, 4 — only Test 3 + Test 5 use subprocess for the argparse mutex check, via `subprocess.run([sys.executable, '-m', 'horus_os', 'plugins', 'grant', 'FOO', '--all', 'filesystem.read'])`).

Behavior must be implemented FIRST as a failing test, then the code; the `tdd="true"` flag on this task means RED → GREEN → REFACTOR cycle.

NO edits to any other CLI subcommand. NO edits to `installer.grant_capability` (v0.4 shape preserved). NO edits to PermissionService.
  </action>
  <verify>
    <automated>python -m pytest tests/plugins/test_plugins_grant_all.py -v</automated>
  </verify>
  <done>All 5 tests in `tests/plugins/test_plugins_grant_all.py` pass; `python -m horus_os plugins grant --help` shows the new `--all` flag; v0.4 grant-positional regression suite (existing `tests/plugins/test_installer_capability_prompt.py` + any grant-touching test) continues to pass byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend scripts/release_gate.py with four new v0.5 checks + tests</name>
  <files>scripts/release_gate.py, tests/test_release_gate_v0_5_checks.py</files>
  <behavior>
    - Test 1: `check_docs_manifest_schema_drift(DOCS_SCHEMA_PATH)` returns CheckResult(ok=True) on the live repo state (Phase 47's docs file is in sync with MANIFEST_V1_SCHEMA).
    - Test 2: Same check pointed at a mutated tmp copy of the docs file returns ok=False; diagnostic contains difflib markers '---' and '+++'.
    - Test 3: `check_plugin_install_smoke_ci_present(CI_YML_PATH)` returns ok=True after Task 3 ships the install-smoke-plugin job to ci.yml (test runs AFTER Task 3 — see wave dependency note).
    - Test 4: Same check pointed at a tmp copy of ci.yml with the install-smoke-plugin block stripped returns ok=False; diagnostic mentions 'TEST-20'.
    - Test 5: `check_reference_plugin_manifest_valid(REFERENCE_PLUGIN_MANIFEST_PATH)` returns ok=True; diagnostic names 'horus-os-example-plugin'.
    - Test 6: `check_v0_4_fixture_roundtrip(V0_4_FIXTURE_PATH)` returns ok=True; the fixture is COPIED to a tmpfile (committed fixture NEVER mutated); post-init() the tmpfile has plugin_name columns in llm_calls and tool_invocations, the three plugin tables, and the idx_tool_invocations_plugin index; second db.init() call does not raise.
    - Test 7: Synthetic SQLite without expected v6 state → ok=False; diagnostic names the missing element.
    - Test 8 (subprocess): `python scripts/release_gate.py --check docs-drift` with HORUS_OS_DOCS_SCHEMA_PATH_OVERRIDE pointed at a mutated tmp file exits 1 and stdout contains a single 'FAIL  docs-drift:' line.
  </behavior>
  <action>
In `scripts/release_gate.py`, ADD four new check functions IMMEDIATELY AFTER `check_pytest_pass` (preserve the existing four functions BYTE-IDENTICAL — verify with `git diff --stat scripts/release_gate.py` showing only additions, zero deletions, in the existing check function bodies):

1. `check_docs_manifest_schema_drift(schema_path: Path, *, runtime_schema_factory=None) -> CheckResult` named `docs-drift`. If schema_path missing → ok=False, diagnostic 'docs/manifest-v1.schema.json not found at {schema_path}'. Else: use the lazy-sys.path-insert idiom (mirror `build_manifest_schema.py:31-37`), `from horus_os.plugins.manifest import MANIFEST_V1_SCHEMA`, call `.model_json_schema()`, serialize with `json.dumps(schema, indent=2, sort_keys=True) + '\n'`, compare to `schema_path.read_text(encoding='utf-8')`. On mismatch: `import difflib; diff_lines = list(difflib.unified_diff(committed_text.splitlines(keepends=False), runtime_text.splitlines(keepends=False), fromfile='committed', tofile='runtime', lineterm=''))[:10]`; diagnostic = `'docs/manifest-v1.schema.json drifted from MANIFEST_V1_SCHEMA; regen via scripts/build_manifest_schema.py. Diff: ' + ' | '.join(diff_lines)`. Allow `runtime_schema_factory` kw for tests to inject a mock.

2. `check_plugin_install_smoke_ci_present(ci_yml_path: Path) -> CheckResult` named `plugin-install-smoke-ci`. Reads ci_yml_path as text; asserts the literal substring `install-smoke-plugin` is present (one occurrence is sufficient — single grep, same shape as `check_ci_two_variant_smoke_present`). On miss: diagnostic = `f'install-smoke-plugin job missing from {ci_yml_path}; TEST-20 contract violated'`. On hit: diagnostic = `'install-smoke-plugin job present'`.

3. `check_reference_plugin_manifest_valid(manifest_path: Path) -> CheckResult` named `reference-plugin-manifest-valid`. If missing → ok=False, diagnostic `f'reference plugin manifest not found at {manifest_path}'`. Else: read `toml_bytes = manifest_path.read_bytes()`; lazy-import `from horus_os.plugins.manifest import validate_manifest`. Catch ANY exception (rather than just `ManifestValidationError` — the script must be robust to schema-class changes); on success build diagnostic = `f'manifest validates as plugin={spec.name} v{spec.version}'`; on failure diagnostic = `f'reference plugin manifest invalid: {type(exc).__name__}: {exc}'`.

4. `check_v0_4_fixture_roundtrip(fixture_path: Path) -> CheckResult` named `v0-4-fixture-roundtrip`. If missing → ok=False, diagnostic `f'v0.4 fixture not found at {fixture_path}'`. Else: `import shutil, tempfile; with tempfile.NamedTemporaryFile(suffix='.sqlite3', delete=False) as tmp: shutil.copy(fixture_path, tmp.name)`. Lazy-import `from horus_os.storage import Database`. Construct `Database(Path(tmp.name))`, call `.init()`. Use `sqlite3.connect(tmp.name)` to introspect: `PRAGMA table_info(llm_calls)` must include a row with name='plugin_name'; same for `tool_invocations`; `SELECT name FROM sqlite_master WHERE type='table' AND name IN ('plugins','plugin_capabilities','plugin_status')` must return 3 rows; `SELECT name FROM sqlite_master WHERE type='index' AND name='idx_tool_invocations_plugin'` must return 1 row. Call `Database(Path(tmp.name)).init()` a second time — must not raise (idempotent). Build diagnostic = `f'v5->v6 migration green: 2 plugin_name cols + 3 plugin tables + 1 plugin index present; second init idempotent'`. ALWAYS unlink the tmp file in a finally block; NEVER touch the source fixture.

Module-level additions BEFORE `check_pricing_freshness`:
```
DEFAULT_DOCS_SCHEMA_PATH = REPO_ROOT / "docs" / "manifest-v1.schema.json"
DEFAULT_REFERENCE_PLUGIN_MANIFEST_PATH = (
    REPO_ROOT / "examples" / "horus-os-example-plugin" / "horus-plugin.toml"
)
DEFAULT_V0_4_FIXTURE_PATH = REPO_ROOT / "tests" / "fixtures" / "v0_4_database.sqlite3"
```

Three new env-override helpers paralleling `_resolved_pricing_path` / `_resolved_ci_yml_path`:
`_resolved_docs_schema_path()`, `_resolved_reference_plugin_manifest_path()`, `_resolved_v0_4_fixture_path()`. Each consults `os.environ.get('HORUS_OS_*_PATH_OVERRIDE')` first.

In `main()`: extend the `--check` choices tuple from `("pricing", "wheel", "ci", "tests")` to `("pricing", "wheel", "ci", "tests", "docs-drift", "plugin-install", "reference-manifest", "fixture-roundtrip")`. Add four parallel dispatch blocks (mirror the existing `if selected in (None, "ci"): results.append(check_ci_two_variant_smoke_present(ci_yml_path))` shape — four new such blocks).

Update the module docstring at lines 1-54: bump the "Runs FOUR checks" to "Runs EIGHT checks (4 v0.4 + 4 v0.5)". Bump the docstring's numbered list from 4 entries to 8.

Write tests in NEW file `tests/test_release_gate_v0_5_checks.py` covering the 8 behavior tests above. Mirror `tests/test_release_gate.py::_load_release_gate_module` exactly (import the script as a module via `importlib.util.spec_from_file_location` so the dataclass introspection works). All tests except Test 8 are in-process; Test 8 uses `subprocess.run`.

NO edits to `check_pricing_freshness`, `check_ci_two_variant_smoke_present`, `check_wheel_pricing_bundle`, `check_pytest_pass`. NO edits to `CheckResult`, `_truthy_env`, `_read_max_age_days`, `_print_result`. The four v0.4 checks remain BYTE-IDENTICAL; `git blame scripts/release_gate.py` on those lines is unchanged.

Implementation order (TDD): write the test file first with all 8 tests failing; then add each check function one at a time, watching tests flip green; then refactor any duplicated boilerplate (e.g. the path-override pattern can be a single helper at the top).
  </action>
  <verify>
    <automated>python -m pytest tests/test_release_gate_v0_5_checks.py tests/test_release_gate.py -v && python scripts/release_gate.py --check docs-drift --skip-build && python scripts/release_gate.py --check fixture-roundtrip --skip-build && python scripts/release_gate.py --check reference-manifest --skip-build</automated>
  </verify>
  <done>All 8 new tests pass; all existing `tests/test_release_gate.py` tests continue to pass byte-identically; running `python scripts/release_gate.py --skip-build --skip-tests` (use `HORUS_OS_RELEASE_GATE_SKIP_TESTS=1`) exits 0 against the live repo state after Task 3 ships the install-smoke-plugin job; running with `HORUS_OS_DOCS_SCHEMA_PATH_OVERRIDE` pointed at a mutated docs file exits 1 with exactly one FAIL line.</done>
</task>

<task type="auto">
  <name>Task 3: Ship install-smoke-plugin CI job + docs/RELEASE.md updates</name>
  <files>.github/workflows/ci.yml, docs/RELEASE.md</files>
  <action>
Append a new top-level job `install-smoke-plugin` to `.github/workflows/ci.yml` AFTER the `install-smoke-with-otel` block (preserve lines 1-159 BYTE-IDENTICAL). Skeleton (concrete values inline; do NOT generalize away the literal job name `install-smoke-plugin` — the release-gate's `check_plugin_install_smoke_ci_present` greps for that exact string):

Top-level fields:
- `install-smoke-plugin:` name `install-smoke-plugin / ${{ matrix.os }} / Python ${{ matrix.python-version }}`
- `needs: lint-and-test` (mirror v0.4 install-smoke jobs)
- `runs-on: ${{ matrix.os }}`
- `strategy: { fail-fast: false, matrix: { os: [ubuntu-latest, macos-latest, windows-latest], python-version: ["3.11", "3.12"] } }`

Steps (in order):
1. `actions/checkout@v4`.
2. `actions/setup-python@v5` with `python-version: ${{ matrix.python-version }}`, `cache: pip`, `cache-dependency-path: pyproject.toml`.
3. Install horus-os in editable mode: `python -m pip install --upgrade pip && python -m pip install -e .[dev]`. Use `run: |` block (multi-line bash on linux/macos; PowerShell on windows automatically picks the right shell because GitHub Actions defaults `shell: bash` works on windows for these commands — verify with the existing `install-smoke-no-otel` job which uses `python -m pip install -e .[dev]` cross-OS).
4. Install reference plugin: `python -m pip install -e ./examples/horus-os-example-plugin --no-deps`. The `--no-deps` is important — horus-os is already editable in this venv; reinstalling it would defeat the purpose.
5. Init data dir (cross-OS via Python):
   ```
   shell: bash
   run: |
     mkdir -p $HOME/horus-smoke-data
     python -m horus_os init --non-interactive --provider anthropic --api-key dummy --data-dir $HOME/horus-smoke-data
   ```
   For windows use a parallel step with `shell: pwsh` and `$env:USERPROFILE\\horus-smoke-data`. (If `horus-os init` does not yet have `--non-interactive`, fall back to the existing data-dir pattern from `tests/plugins/test_reference_plugin_install_local.py` lines 105-107 — `Database(data_dir / 'horus.sqlite').init()` via inline python `-c`.)
6. Launch horus-os in the background. Two parallel steps gated on `runs-on`:
   - linux/macos (`if: runner.os != 'Windows'`): `nohup python -m horus_os serve --port 18000 --data-dir $HOME/horus-smoke-data > /tmp/horus-serve.log 2>&1 & echo $! > /tmp/horus-serve.pid`
   - windows (`if: runner.os == 'Windows'`, `shell: pwsh`): `Start-Process -NoNewWindow -RedirectStandardOutput "$env:TEMP\\horus-serve.log" -RedirectStandardError "$env:TEMP\\horus-serve.err" python -ArgumentList '-m','horus_os','serve','--port','18000','--data-dir',"$env:USERPROFILE\\horus-smoke-data" -PassThru | Select-Object -ExpandProperty Id | Out-File "$env:TEMP\\horus-serve.pid"`
7. Wait for `/api/system/ping` to return 200 (bash polling loop with a counter, NOT a flat `sleep N` — Pitfall 3 lint gate is repo-wide). Linux/macos:
   ```
   for i in $(seq 1 30); do
     if curl -fsS http://127.0.0.1:18000/api/system/ping; then echo OK; break; fi
     sleep 1
   done
   curl -fsS http://127.0.0.1:18000/api/system/ping  # final assertion exits non-zero if still down
   ```
   Windows pwsh equivalent with `Invoke-WebRequest` in a `for ($i=0; $i -lt 30; $i++)` loop. Both fail the step (exit non-zero) if ping never goes green within 30 seconds.
8. Pre-grant assertion (cross-OS via Python — no jq dependency):
   ```
   curl -fsS http://127.0.0.1:18000/api/plugins | python -c "import json,sys; b=json.load(sys.stdin); rows=[p for p in b['plugins'] if p['name']=='horus-os-example-plugin']; assert len(rows)==1, rows; r=rows[0]; assert r['status'] in ('pending','error'), r; assert set(r['pending_capabilities'])=={'filesystem.read','secrets.read'}, r; print('OK pre-grant', r)"
   ```
9. Grant all (the NEW Task 1 flag):
   ```
   python -m horus_os plugins grant horus-os-example-plugin --all --data-dir $HOME/horus-smoke-data
   ```
   Assert stdout includes the literal `Granted 2 capabilities` via grep — `python -m horus_os plugins grant horus-os-example-plugin --all --data-dir $HOME/horus-smoke-data | tee /tmp/grant.out && grep -q "Granted 2 capabilities" /tmp/grant.out`.
10. Restart horus-os: kill the background process via the captured PID, re-launch with the same args, re-poll ping.
11. Post-grant assertion: same Python one-liner but with `assert r['status']=='loaded'`, `assert set(r['granted_capabilities'])=={'filesystem.read','secrets.read'}`, `assert r['pending_capabilities']==[]`.
12. Tear-down step (`if: always()`): kill the background process if still alive; cat the server log on failure for CI debug.

The job MUST exit with success on all six matrix combinations. `fail-fast: false` so one OS failure does not mask others.

Now update `docs/RELEASE.md`:

(a) `## Release gate` section — after the existing four bullets (lines 30-41), insert ONE paragraph:
> v0.5 extends the gate with four additional checks: `docs-drift` (between `MANIFEST_V1_SCHEMA` and `docs/manifest-v1.schema.json`), `plugin-install-smoke-ci` (asserts the `install-smoke-plugin` job is present in `ci.yml`), `reference-plugin-manifest-valid` (runtime `validate_manifest()` accepts `examples/horus-os-example-plugin/horus-plugin.toml` with zero errors), and `v0-4-fixture-roundtrip` (`tests/fixtures/v0_4_database.sqlite3` survives the v5→v6 migration with all three new tables, two new columns, and one new index present). Default invocation runs all EIGHT checks (4 v0.4 + 4 v0.5).

Also update line 28 from "four checks" to "eight checks" and the CLI flags block (lines 56-59) to extend `--check` enumeration: `--check pricing|wheel|ci|tests|docs-drift|plugin-install|reference-manifest|fixture-roundtrip`.

(b) `## Refreshing pricing.json` section (lines 64-88): insert a sibling sub-section AFTER (a fresh `### Refreshing docs/manifest-v1.schema.json`):
> The runtime pydantic schema (`MANIFEST_V1_SCHEMA` in `src/horus_os/plugins/manifest.py`) and the bundled JSON Schema (`docs/manifest-v1.schema.json`) must stay byte-identical. The release gate's `docs-drift` check refuses to tag when they diverge.
>
> Procedure:
> 1. Run `python scripts/build_manifest_schema.py`. The script is idempotent; running it twice in a row produces no diff.
> 2. If `git diff docs/manifest-v1.schema.json` shows changes, commit them with `chore(release): regenerate docs/manifest-v1.schema.json`.

(c) `## Release procedure` section (lines 90-125): insert step `1b` between step 1 (pricing) and step 2 (release gate):
> 1b. Regenerate `docs/manifest-v1.schema.json` via `python scripts/build_manifest_schema.py` and commit the result if there is a diff. The docs-drift gate refuses to tag otherwise.

Update step 2's "Confirm exit 0 and all four checks pass" to "Confirm exit 0 and all EIGHT checks pass."

(d) `## Pre-release checklist` section step 1 (line 12-15): extend the "Expect SUCCESS for the full 3-OS x 2-Python matrix" to also name `install-smoke-plugin` alongside the existing two job names.

No edits anywhere else in `docs/RELEASE.md` — preserve `## Post-release` and `## Why the release gate exists` byte-identically. The doc grows by ~25-35 lines net.

VERIFY: After this task, running Task 2's release-gate `check_plugin_install_smoke_ci_present` against the live `ci.yml` returns ok=True (it grepped for the literal `install-smoke-plugin` which now exists). Tests 3 in Task 2's test file are now green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_release_gate_v0_5_checks.py::test_plugin_install_smoke_ci_passes_when_job_present -v && python -c "import yaml; ci=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'install-smoke-plugin' in ci['jobs'], list(ci['jobs'].keys())" && grep -c 'install-smoke-plugin' .github/workflows/ci.yml | python -c "import sys; n=int(sys.stdin.read()); assert n >= 2, f'expected install-smoke-plugin at least twice (job name + matrix display name), got {n}'" && grep -q 'docs-drift' docs/RELEASE.md && grep -q 'manifest-v1.schema.json' docs/RELEASE.md && grep -q 'eight checks' docs/RELEASE.md</automated>
  </verify>
  <done>`.github/workflows/ci.yml` parses as valid YAML; the `install-smoke-plugin` job is declared with the 3-OS × 2-Python matrix and 12+ steps in the order specified; `docs/RELEASE.md` mentions `docs-drift`, `manifest-v1.schema.json`, and updates the four-checks count to eight; running `python scripts/release_gate.py --check plugin-install` exits 0 (the live ci.yml now contains the literal job name); pushing this branch to GitHub triggers 6 new install-smoke-plugin matrix jobs (verification of CI green is the responsibility of the post-merge phase-checker — this task's `done` is the local-side state).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ↔ horus-os serve | The install-smoke-plugin job boots horus-os listening on 127.0.0.1:18000 inside the runner; the curl client and the server share the same trust domain (the runner). No untrusted input crosses the boundary. |
| release_gate.py ↔ docs/manifest-v1.schema.json | The script reads a docs file the maintainer committed. The file is in-repo, not user-supplied. |
| release_gate.py ↔ tests/fixtures/v0_4_database.sqlite3 | The script reads a fixture and copies it to a tempfile. The tempfile is mutated; the source fixture is read-only. |
| `horus-os plugins grant --all` ↔ PermissionService | The CLI handler reads the plugins table (already-validated capabilities from install time) and calls PermissionService.grant per capability. No new external input enters the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | `tests/fixtures/v0_4_database.sqlite3` mutation by `check_v0_4_fixture_roundtrip` | mitigate | Always copy fixture to tempfile before mutation; never touch the source file. Wrap in `try/finally: tmp.unlink()`. Assert (in tests) that the file's mtime + size are unchanged pre/post check. |
| T-49-02 | Elevation of Privilege | `horus-os plugins grant --all` over-grants capabilities the user did not consent to | mitigate | The capabilities granted are EXACTLY the set the user already accepted at install time (Phase 44's grant-prompt — INSTALL-05 refuses install on partial refusal). Re-granting an already-accepted set is not a privilege expansion. The `--all` flag does NOT consult the live on-disk manifest (which the user could not have approved); it consults the `plugins` table row whose `manifest_hash` matches the install-time approval. |
| T-49-03 | Denial of Service | `install-smoke-plugin` CI job hangs forever waiting for `/api/system/ping` | mitigate | Ping poll bounded to 30 attempts at 1s intervals (30s max wall clock). On timeout the curl assertion at step 7 exits non-zero and the job fails. The teardown step (`if: always()`) kills the background process. |
| T-49-04 | Information Disclosure | `install-smoke-plugin` CI job logs leak secrets | accept | The job uses `--api-key dummy` (a literal `dummy` string, not a real Anthropic key); no real secrets are loaded. The CI runner's `GITHUB_TOKEN` is not exposed to the serve process. |
| T-49-05 | Spoofing | Mutated `docs/manifest-v1.schema.json` ships in a release because the gate was bypassed | mitigate | The gate exits 1 on drift; `docs/RELEASE.md` Release Procedure step 2 requires "Confirm exit 0 and all eight checks pass" before tagging. The gate is local (per the existing "Why the release gate exists" doc); the maintainer is the single point of trust. |
| T-49-06 | Repudiation | A maintainer disables the gate via env var (`HORUS_OS_RELEASE_GATE_SKIP_TESTS=1`) without leaving a trail | accept | Same posture as v0.4. The skip env vars are documented as debug aids only; commit messages + git history are the audit trail. Codifying this further (e.g. printing a warning when skip is set) is a v0.6+ change. |
</threat_model>

<verification>
- All three tasks pass their own `<verify>` automation.
- Full repo pytest suite is green: `python -m pytest -v` exits 0.
- `python scripts/release_gate.py --skip-build` (skips only the wheel build, runs the other 7 checks) exits 0 against the live repo state.
- `python -m horus_os plugins grant --help` shows the new `--all` flag and the mutually-exclusive group with positional `capability`.
- Pushed branch triggers 27 CI jobs (was 21 before this phase); all 6 `install-smoke-plugin` matrix entries land green within 5 minutes of push (cap reflects the existing install-smoke variant cadence).
- `git diff scripts/release_gate.py` shows zero deletions in the existing four-check function bodies; only additions.
- `git diff src/horus_os/plugins/installer.py` shows the v0.4 `grant_capability` function unchanged; only a new `grant_all_capabilities` function added.
</verification>

<success_criteria>
- TEST-20: 6 CI matrix entries (3-OS × 2-Py) under `install-smoke-plugin` go green on push.
- REL-11 (a): `scripts/release_gate.py --check docs-drift` exits 0 against live repo, exits 1 against mutated docs.
- REL-11 (b): `scripts/release_gate.py --check plugin-install` exits 0 against the live ci.yml (post-Task-3).
- REL-11 (c): `scripts/release_gate.py --check reference-manifest` exits 0 against `examples/horus-os-example-plugin/horus-plugin.toml`.
- REL-11 (d): `scripts/release_gate.py --check fixture-roundtrip` exits 0 against `tests/fixtures/v0_4_database.sqlite3`.
- `python scripts/release_gate.py --skip-build` (the default invocation minus the slow wheel build) runs all 8 checks and exits 0.
- `python -m horus_os plugins grant FOO --all` (Task 1) grants every capability the plugin declared at install time; positional `grant FOO filesystem.read` shape preserved byte-identically.
- `docs/RELEASE.md` describes all 8 checks, the eighth-check-doc-regen step, and lists `install-smoke-plugin` in the pre-release checklist.
- Phase 50 (the v0.5.0 release) can run `python scripts/release_gate.py` and rely on its exit code to gate `git tag`.
</success_criteria>

<output>
After completion, create `.planning/phases/49-three-os-install-gate-release-gate-extension/49-01-SUMMARY.md` per the standard template. Include:
- The 8 release-gate checks (4 v0.4 + 4 v0.5) and how each was wired in.
- The `install-smoke-plugin` job's step list and the `--all` flag's mutex shape.
- The git diff statistics (zero deletions in v0.4 release-gate check function bodies; zero edits to existing CI jobs).
- The post-push CI job count (21 → 27).
- Any pitfalls encountered (TOML field shape, OS-specific shell syntax, fixture isolation, pydantic schema serializer drift).
</output>
