---
phase: 17-adapter-plugin-interface
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/horus_os/adapters/__init__.py
  - src/horus_os/adapters/webhook.py
  - pyproject.toml
  - tests/test_adapters_webhook.py
autonomous: true
requirements:
  - ADAPT-02
tags:
  - adapter
  - webhook
  - hmac
  - reference
must_haves:
  truths:
    - "`WebhookAdapter` satisfies the `Adapter` Protocol from Plan 01"
    - "`bind(app, context)` mounts `POST /api/adapters/webhook` on the FastAPI app"
    - "The handler reads `HORUS_OS_WEBHOOK_SECRET` from the environment and returns 503 when unset"
    - "A request with no `X-Horus-Signature` header returns 401"
    - "A request whose signature does not match HMAC-SHA256(secret, body) returns 401"
    - "A request whose signature matches but whose body lacks `prompt` returns 400"
    - "A request whose `agent` field names an unknown profile returns 404"
    - "A valid request runs `run_agent` with the profile's system prompt and default model and returns the trace id, the final text, and the latency in ms"
    - "The recorded trace row carries the `agent_profile_name` from the request"
    - "The reference adapter is declared in pyproject under `[project.entry-points.\"horus_os.adapters\"]` as `webhook = \"horus_os.adapters.webhook:WebhookAdapter\"`"
  artifacts:
    - path: src/horus_os/adapters/webhook.py
      provides: "WebhookAdapter class with HMAC-SHA256 validation, profile lookup, run_agent call, and trace recording"
    - path: src/horus_os/adapters/__init__.py
      provides: "Re-exports WebhookAdapter alongside the contract symbols"
    - path: pyproject.toml
      provides: "Entry-point declaration registering WebhookAdapter under horus_os.adapters"
    - path: tests/test_adapters_webhook.py
      provides: "End-to-end TestClient tests for every documented status code path"
  key_links:
    - from: "adapters/webhook.py:WebhookAdapter.bind"
      to: "agent.py:run_agent"
      via: "run_agent(prompt, provider=..., model=..., system_prompt=...)"
      pattern: "run_agent"
    - from: "adapters/webhook.py:WebhookAdapter._verify_signature"
      to: "hmac.compare_digest"
      via: "hmac.compare_digest(expected_hex, received_hex)"
      pattern: "compare_digest"
    - from: "pyproject.toml"
      to: "adapters/webhook.py:WebhookAdapter"
      via: "[project.entry-points.\"horus_os.adapters\"]"
      pattern: "horus_os.adapters"
---

<objective>
Ship the reference HTTP webhook adapter that demonstrates the Phase 17
adapter contract and satisfies ADAPT-02 ("One reference adapter ships:
HTTP webhook receiver").

Purpose: proves the contract from Plan 01 is usable, gives third-party
authors a working example, and provides horus-os with a built-in
inbound HTTP channel that does not require a long-lived dashboard
session. External services (GitHub, Stripe, a cron job) can POST a
signed payload and trigger an agent run.

Output: a `WebhookAdapter` class with HMAC-SHA256 signature validation,
agent profile routing, trace recording, and a single FastAPI route
at `/api/adapters/webhook`. Comprehensive tests cover every error
path. The pyproject entry-point declaration registers the adapter so
discovery finds it after `pip install -e .`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# horus-os Phase 17 Plan 02

## Webhook handler shape

```
POST /api/adapters/webhook
Headers:
  X-Horus-Signature: sha256=<hex>
  Content-Type: application/json
Body:
  {
    "prompt": "...",        // required
    "agent":  "...",        // optional, names an AgentProfile
    "provider": "anthropic" // optional, defaults to config
  }
```

Response 200:
```
{ "trace_id": "...", "text": "...", "latency_ms": 123 }
```

Errors: 400 (bad body), 401 (bad signature), 404 (unknown agent),
500 (run_agent crashed), 503 (no secret configured).

## HMAC algorithm

```
expected_hex = hmac.new(
    secret_bytes,
    raw_body_bytes,
    hashlib.sha256,
).hexdigest()
```

Header parsing: split on `=`, expect prefix `sha256`. Compare with
`hmac.compare_digest(expected_hex, received_hex)`. Reject any
non-`sha256=` prefix; we ship one algorithm.

## Secret source

`HORUS_OS_WEBHOOK_SECRET` env var, read on every request rather than
cached at bind time. Reading per-request keeps tests deterministic
(monkeypatching `setenv` works without re-creating the app) and lets
operators rotate the secret without restarting the server.

When the env var is unset (or empty), the handler returns
503 with `{"detail": "webhook adapter is not configured; set HORUS_OS_WEBHOOK_SECRET"}`.
This is the "no open webhook" guarantee.

## Adapter class shape

```
class WebhookAdapter:
    name = "webhook"

    def bind(self, app, context: AdapterContext) -> None:
        ...

    def describe(self) -> dict[str, Any]:
        return {
            "name": "webhook",
            "version": 1,
            "endpoint": "/api/adapters/webhook",
            "auth": "hmac-sha256",
            "env": "HORUS_OS_WEBHOOK_SECRET",
        }
```

The route closure captures `context` for `config` and `data_dir`
access. The handler creates a fresh `Database(config.db_path)` per
call, matching the request-scoped pattern used elsewhere.

## run_agent call

The webhook drives `run_agent` (single-turn) rather than
`run_agent_loop` (multi-turn with tools). The reference adapter is
intentionally narrow: a stateless HTTP endpoint that returns one
response. Tool dispatch and multi-turn iteration are deferred to a
future "advanced webhook" or the dashboard.

```
result = run_agent(
    prompt,
    provider=provider,
    model=model,
    tools=None,
    system_prompt=profile.system_prompt if profile else None,
)
```

`run_agent` is sync; the FastAPI handler is `async def` but calls
`run_agent` directly because the handler is brief and offloading to
a thread is unnecessary for the reference adapter. The existing
`/api/chat` route follows the same pattern.

## Trace recording

```
trace_id = db.record_trace(
    prompt,
    result,
    latency_ms=latency_ms,
    agent_profile_name=agent_name,  # None when no agent specified
)
```

The trace row carries the agent_profile_name when the request asked
for one, so subsequent dashboard queries (Phase 16's
`/api/agents/{name}` last_activity_at) reflect webhook traffic.

## Test stubbing of run_agent

Tests import `horus_os.adapters.webhook as webhook_mod` and
`monkeypatch.setattr(webhook_mod, "run_agent", fake_run_agent)`.
The fake captures kwargs and returns a canned `AgentResult`. The
WebhookAdapter handler closure resolves `run_agent` at call time
(via the module-level binding) so the monkeypatch takes effect.

## Entry-point declaration

```
[project.entry-points."horus_os.adapters"]
webhook = "horus_os.adapters.webhook:WebhookAdapter"
```

After `pip install -e .` (which the dev environment already runs),
`importlib.metadata.entry_points(group="horus_os.adapters")` returns
the webhook entry. Tests that exercise the real discovery path are
intentionally skipped in favor of the stubbed integration test in
Plan 01; checking the pyproject text directly is enough for a
contract-level assertion.

## Determinism check

One test asserts the pyproject entry-point declaration exists and
points at `WebhookAdapter`. This is a string-level contract test
that catches accidental removal during a future pyproject refactor.

## Required imports

```
import hashlib
import hmac
import os
import time
from typing import Any

from horus_os.adapters.base import Adapter, AdapterContext
from horus_os.agent import run_agent
from horus_os.config import Config
from horus_os.storage import Database
```

The `Adapter` import is purely for documentation; we do not inherit
(Protocols don't require inheritance) but mention it in the
docstring so static analyzers can verify shape.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WebhookAdapter with HMAC validation and run_agent dispatch</name>
  <files>src/horus_os/adapters/webhook.py, src/horus_os/adapters/__init__.py, tests/test_adapters_webhook.py</files>
  <behavior>
    - WebhookAdapter.name == "webhook"
    - bind(app, context) mounts POST /api/adapters/webhook on the app
    - Handler returns 503 when HORUS_OS_WEBHOOK_SECRET is unset or empty
    - Handler returns 401 when X-Horus-Signature header is missing
    - Handler returns 401 when the signature scheme is not "sha256=..."
    - Handler returns 401 when the HMAC does not match
    - Handler returns 400 when the body cannot be parsed as JSON
    - Handler returns 400 when the body has no "prompt" field
    - Handler returns 404 when "agent" names an unknown profile
    - Handler returns 200 with trace_id, text, latency_ms on success
    - The recorded trace row carries the agent_profile_name supplied in the body
    - run_agent receives the profile's system_prompt and default_model when an agent is named
    - Handler uses hmac.compare_digest for the signature check
  </behavior>
  <action>
1. Create `src/horus_os/adapters/webhook.py`:

```
"""Reference HTTP webhook adapter for horus-os.

Mounts `POST /api/adapters/webhook`. Validates an HMAC-SHA256
signature on every request, parses a JSON body, looks up an
optional agent profile, runs one `run_agent` turn, records a
trace row, and returns the trace id with the final text.

Security: the adapter refuses to run when `HORUS_OS_WEBHOOK_SECRET`
is unset. Requests must include `X-Horus-Signature: sha256=<hex>`
where `<hex>` is the HMAC-SHA256 of the raw request body keyed
by the configured secret. A constant-time comparison via
`hmac.compare_digest` is used to avoid timing oracles.

This adapter satisfies the `Adapter` Protocol from
`horus_os.adapters.base`. It is declared as the package's reference
adapter in pyproject under `[project.entry-points."horus_os.adapters"]`.
"""

from __future__ import annotations

import hashlib
import hmac
import os
import time
from typing import Any

from horus_os.adapters.base import AdapterContext
from horus_os.agent import SUPPORTED_PROVIDERS, run_agent
from horus_os.config import Config
from horus_os.storage import Database

WEBHOOK_SECRET_ENV = "HORUS_OS_WEBHOOK_SECRET"
SIGNATURE_HEADER = "X-Horus-Signature"
SIGNATURE_PREFIX = "sha256="


class WebhookAdapter:
    """An inbound HTTP webhook that routes signed payloads to `run_agent`."""

    name = "webhook"

    def describe(self) -> dict[str, Any]:
        return {
            "name": self.name,
            "version": 1,
            "endpoint": "/api/adapters/webhook",
            "auth": "hmac-sha256",
            "env": WEBHOOK_SECRET_ENV,
        }

    def bind(self, app: Any, context: AdapterContext) -> None:
        from fastapi import HTTPException, Request

        @app.post("/api/adapters/webhook")
        async def _handle_webhook(request: Request) -> dict[str, Any]:
            secret = os.environ.get(WEBHOOK_SECRET_ENV) or ""
            if not secret:
                raise HTTPException(
                    503,
                    detail=(
                        f"webhook adapter is not configured; set {WEBHOOK_SECRET_ENV}"
                    ),
                )

            raw_body = await request.body()
            signature_header = request.headers.get(SIGNATURE_HEADER, "")
            if not signature_header.startswith(SIGNATURE_PREFIX):
                raise HTTPException(401, detail="missing or malformed signature")
            received_hex = signature_header[len(SIGNATURE_PREFIX) :]
            expected_hex = hmac.new(
                secret.encode("utf-8"), raw_body, hashlib.sha256
            ).hexdigest()
            if not hmac.compare_digest(expected_hex, received_hex):
                raise HTTPException(401, detail="invalid signature")

            try:
                import json

                payload = json.loads(raw_body.decode("utf-8")) if raw_body else {}
            except (UnicodeDecodeError, ValueError) as exc:
                raise HTTPException(400, detail=f"invalid JSON body: {exc}") from exc
            if not isinstance(payload, dict):
                raise HTTPException(400, detail="payload must be a JSON object")
            prompt = payload.get("prompt")
            if not isinstance(prompt, str) or not prompt.strip():
                raise HTTPException(400, detail="prompt is required")

            cfg = context.config
            provider = payload.get("provider") or cfg.default_provider
            if provider not in SUPPORTED_PROVIDERS:
                raise HTTPException(400, detail=f"unknown provider {provider!r}")

            db = Database(cfg.db_path)
            agent_name = payload.get("agent")
            profile = None
            if isinstance(agent_name, str) and agent_name:
                profile = db.load_profile(agent_name)
                if profile is None:
                    raise HTTPException(
                        404, detail=f"agent profile {agent_name!r} not found"
                    )
            else:
                agent_name = None

            model = (
                payload.get("model")
                or (profile.default_model if profile else None)
                or _default_model(cfg, provider)
            )
            system_prompt = profile.system_prompt if profile else None

            start = time.perf_counter()
            try:
                result = run_agent(
                    prompt,
                    provider=provider,
                    tools=None,
                    model=model,
                    system_prompt=system_prompt,
                )
            except Exception as exc:
                latency_ms = int((time.perf_counter() - start) * 1000)
                from horus_os.types import AgentResult

                trace_id = db.record_trace(
                    prompt,
                    AgentResult(text="", provider=provider, model=model),
                    latency_ms=latency_ms,
                    status="error",
                    error_message=f"{type(exc).__name__}: {exc}",
                    agent_profile_name=agent_name,
                )
                raise HTTPException(
                    500,
                    detail={
                        "error": f"{type(exc).__name__}: {exc}",
                        "trace_id": trace_id,
                    },
                ) from exc

            latency_ms = int((time.perf_counter() - start) * 1000)
            trace_id = db.record_trace(
                prompt,
                result,
                latency_ms=latency_ms,
                agent_profile_name=agent_name,
            )
            return {
                "trace_id": trace_id,
                "text": result.text,
                "latency_ms": latency_ms,
            }


def _default_model(cfg: Config, provider: str) -> str:
    if provider == "anthropic":
        return cfg.anthropic_model
    return cfg.gemini_model
```

2. Extend `src/horus_os/adapters/__init__.py` to re-export
   `WebhookAdapter`:

```
from horus_os.adapters.base import (
    ADAPTER_ENTRY_POINT_GROUP,
    Adapter,
    AdapterContext,
    discover_adapters,
)
from horus_os.adapters.webhook import WebhookAdapter

__all__ = [
    "ADAPTER_ENTRY_POINT_GROUP",
    "Adapter",
    "AdapterContext",
    "WebhookAdapter",
    "discover_adapters",
]
```

3. Create `tests/test_adapters_webhook.py`:

```
"""End-to-end tests for the reference HTTP webhook adapter."""

from __future__ import annotations

import hashlib
import hmac
import json
from pathlib import Path
from typing import Any

import pytest
from fastapi import FastAPI
from fastapi.testclient import TestClient

from horus_os import Config, Database
from horus_os.adapters import AdapterContext, WebhookAdapter
from horus_os.adapters import webhook as webhook_mod
from horus_os.types import AgentProfile, AgentResult


def _init_db(tmp_path: Path) -> Database:
    cfg = Config.with_defaults(tmp_path)
    cfg.save()
    db = Database(cfg.db_path)
    db.init()
    return db


def _client(tmp_path: Path) -> TestClient:
    """Build a FastAPI app with only the webhook adapter mounted."""
    cfg = Config.with_defaults(tmp_path)
    cfg.save()
    db = Database(cfg.db_path)
    db.init()

    app = FastAPI()
    adapter = WebhookAdapter()
    adapter.bind(app, AdapterContext(config=cfg, data_dir=tmp_path))
    return TestClient(app)


def _sign(body: bytes, secret: str) -> str:
    digest = hmac.new(secret.encode("utf-8"), body, hashlib.sha256).hexdigest()
    return f"sha256={digest}"


def _fake_run_agent(captured: dict[str, Any], *, text: str = "ok") -> Any:
    def _impl(prompt: str, **kwargs: Any) -> AgentResult:
        captured["prompt"] = prompt
        captured["kwargs"] = kwargs
        return AgentResult(text=text, provider="anthropic", model="m")

    return _impl


def test_webhook_returns_503_when_secret_unset(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.delenv("HORUS_OS_WEBHOOK_SECRET", raising=False)
    response = _client(tmp_path).post(
        "/api/adapters/webhook", content=b"{}", headers={"X-Horus-Signature": "x"}
    )
    assert response.status_code == 503
    assert "HORUS_OS_WEBHOOK_SECRET" in response.json()["detail"]


def test_webhook_returns_401_when_signature_missing(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    response = _client(tmp_path).post("/api/adapters/webhook", content=b"{}")
    assert response.status_code == 401


def test_webhook_returns_401_when_scheme_wrong(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=b"{}",
        headers={"X-Horus-Signature": "md5=abcd"},
    )
    assert response.status_code == 401


def test_webhook_returns_401_when_signature_invalid(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=b'{"prompt":"hi"}',
        headers={"X-Horus-Signature": "sha256=deadbeef"},
    )
    assert response.status_code == 401


def test_webhook_returns_400_on_invalid_json(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    body = b"not json"
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 400


def test_webhook_returns_400_when_prompt_missing(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    body = b"{}"
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 400


def test_webhook_returns_404_on_unknown_agent(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    captured: dict[str, Any] = {}
    monkeypatch.setattr(webhook_mod, "run_agent", _fake_run_agent(captured))
    body = json.dumps({"prompt": "hi", "agent": "ghost"}).encode()
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 404


def test_webhook_returns_400_on_unknown_provider(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    body = json.dumps({"prompt": "hi", "provider": "openai"}).encode()
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 400


def test_webhook_happy_path_runs_agent_and_records_trace(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    captured: dict[str, Any] = {}
    monkeypatch.setattr(
        webhook_mod, "run_agent", _fake_run_agent(captured, text="hi back")
    )
    body = json.dumps({"prompt": "hello world"}).encode()
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 200
    payload = response.json()
    assert payload["text"] == "hi back"
    assert isinstance(payload["trace_id"], str) and len(payload["trace_id"]) == 32
    assert payload["latency_ms"] >= 0
    assert captured["prompt"] == "hello world"
    # the trace row was persisted
    cfg = Config.with_defaults(tmp_path)
    db = Database(cfg.db_path)
    record = db.get_trace(payload["trace_id"])
    assert record is not None
    assert record.prompt == "hello world"


def test_webhook_forwards_agent_profile(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    cfg = Config.with_defaults(tmp_path)
    cfg.save()
    db = Database(cfg.db_path)
    db.init()
    db.save_profile(
        AgentProfile(
            name="terse",
            system_prompt="be terse",
            default_model="claude-sonnet-4-6",
        )
    )

    captured: dict[str, Any] = {}
    monkeypatch.setattr(webhook_mod, "run_agent", _fake_run_agent(captured))

    body = json.dumps({"prompt": "hi", "agent": "terse"}).encode()
    # build a client against the already-initialized tmp_path
    from fastapi import FastAPI

    app = FastAPI()
    adapter = WebhookAdapter()
    adapter.bind(app, AdapterContext(config=cfg, data_dir=tmp_path))
    client = TestClient(app)

    response = client.post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 200
    assert captured["kwargs"].get("system_prompt") == "be terse"
    assert captured["kwargs"].get("model") == "claude-sonnet-4-6"

    record = db.get_trace(response.json()["trace_id"])
    assert record is not None
    assert record.agent_profile_name == "terse"


def test_webhook_user_model_wins_over_profile(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")
    cfg = Config.with_defaults(tmp_path)
    cfg.save()
    db = Database(cfg.db_path)
    db.init()
    db.save_profile(
        AgentProfile(
            name="terse",
            system_prompt="be terse",
            default_model="profile-model",
        )
    )
    captured: dict[str, Any] = {}
    monkeypatch.setattr(webhook_mod, "run_agent", _fake_run_agent(captured))

    body = json.dumps(
        {"prompt": "hi", "agent": "terse", "model": "override"}
    ).encode()
    from fastapi import FastAPI

    app = FastAPI()
    WebhookAdapter().bind(app, AdapterContext(config=cfg, data_dir=tmp_path))
    response = TestClient(app).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 200
    assert captured["kwargs"].get("model") == "override"


def test_webhook_adapter_describe_metadata(tmp_path: Path) -> None:
    adapter = WebhookAdapter()
    meta = adapter.describe()
    assert meta["name"] == "webhook"
    assert meta["endpoint"] == "/api/adapters/webhook"
    assert meta["auth"] == "hmac-sha256"
    assert meta["env"] == "HORUS_OS_WEBHOOK_SECRET"


def test_webhook_run_agent_exception_recorded_as_error_trace(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("HORUS_OS_WEBHOOK_SECRET", "topsecret")

    def _boom(prompt: str, **kwargs: Any) -> AgentResult:
        raise RuntimeError("provider exploded")

    monkeypatch.setattr(webhook_mod, "run_agent", _boom)
    body = json.dumps({"prompt": "hi"}).encode()
    response = _client(tmp_path).post(
        "/api/adapters/webhook",
        content=body,
        headers={"X-Horus-Signature": _sign(body, "topsecret")},
    )
    assert response.status_code == 500
    detail = response.json()["detail"]
    assert "RuntimeError" in detail["error"]
    cfg = Config.with_defaults(tmp_path)
    db = Database(cfg.db_path)
    record = db.get_trace(detail["trace_id"])
    assert record is not None
    assert record.status == "error"
```
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && python -m pytest tests/test_adapters_webhook.py -x -q --tb=short</automated>
  </verify>
  <done>tests/test_adapters_webhook.py passes (every status code path covered). ruff check . and ruff format --check . clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare the reference adapter in pyproject and verify suite green</name>
  <files>pyproject.toml</files>
  <behavior>
    - pyproject.toml registers WebhookAdapter under [project.entry-points."horus_os.adapters"]
    - importlib.metadata.entry_points(group="horus_os.adapters") returns the webhook entry after a fresh editable install
    - All previous tests stay green
  </behavior>
  <action>
1. Open `pyproject.toml`. After the existing `[project.scripts]` block,
   add:

```
[project.entry-points."horus_os.adapters"]
webhook = "horus_os.adapters.webhook:WebhookAdapter"
```

The block goes near the other `[project.*]` sections, before
`[project.urls]`, to keep the project metadata grouped.

2. Reinstall the package in editable mode so the new entry point is
   registered with importlib.metadata:

```
cd /Users/santino/Projects/horus-os && pip install -e . --quiet
```

3. Add an assertion test at the bottom of
   `tests/test_adapters_webhook.py` that the entry point resolves:

```
def test_webhook_entry_point_registered() -> None:
    """Sanity check: the reference adapter is declared in pyproject."""
    from importlib.metadata import entry_points

    eps = list(entry_points(group="horus_os.adapters"))
    names = {ep.name for ep in eps}
    assert "webhook" in names
    # And the target resolves to WebhookAdapter
    webhook_ep = next(ep for ep in eps if ep.name == "webhook")
    assert webhook_ep.load() is WebhookAdapter
```

This test depends on the editable install having been run; the
CI pipeline runs `pip install -e .[dev]` so it picks up the new
entry point automatically.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && pip install -e . --quiet && python -m pytest tests/test_adapters_webhook.py -x -q --tb=short && python -m pytest -q --tb=no</automated>
  </verify>
  <done>The entry-point test passes. Full suite green. ruff check . and ruff format --check . clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inbound HTTP request -> webhook handler | Public internet (if exposed) can send arbitrary bytes and headers |
| Webhook handler -> run_agent | Validated payload crosses into the provider call |
| Env var -> handler | HORUS_OS_WEBHOOK_SECRET is the only auth credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Spoofing | Forged webhook request | mitigate | HMAC-SHA256 with constant-time compare; missing or malformed signature returns 401 |
| T-17-07 | Tampering | Modified body in transit | mitigate | HMAC covers the raw bytes; any modification flips the digest |
| T-17-08 | Denial | Large body forces hashing on every request | accept | FastAPI imposes default body-size limits; an attacker who can bypass that can also DoS the network layer |
| T-17-09 | Information Disclosure | Open webhook leaks model output | mitigate | Refusing to run without HORUS_OS_WEBHOOK_SECRET makes the open-by-default failure mode impossible |
| T-17-10 | Elevation of Privilege | Webhook triggers an arbitrary agent profile | mitigate | The trust comes from the HMAC; an attacker without the secret cannot route to any profile |
| T-17-11 | Timing oracle | Comparing signatures with `==` | mitigate | `hmac.compare_digest` is used everywhere |
| T-17-12 | Replay | Captured signed request replayed | accept | Documented limitation; replay protection (timestamp + nonce) is deferred to a future revision |
</threat_model>

<verification>
- `python -m pytest tests/test_adapters_webhook.py -q` passes (12 test cases, every status code path)
- `python -m pytest -q` full suite passes (275 + Plan 01 tests + Plan 02 tests)
- `ruff check .` clean
- `ruff format --check .` clean
- After `pip install -e .`, `python -c "from importlib.metadata import entry_points; print([ep.name for ep in entry_points(group='horus_os.adapters')])"` shows `['webhook']`
- `python -c "from horus_os.adapters import WebhookAdapter; w = WebhookAdapter(); print(w.name, w.describe())"` runs without error
</verification>

<success_criteria>
- WebhookAdapter is importable from horus_os.adapters and satisfies the Adapter Protocol
- A valid HMAC-signed POST runs run_agent and returns trace_id, text, latency_ms
- Every documented error path returns the documented status code
- pyproject declares the entry-point; entry_points discovery finds the adapter
- All Phase 12-16 tests still pass byte-for-byte
- The webhook refuses to run without HORUS_OS_WEBHOOK_SECRET set
</success_criteria>

<output>
After completion, create `.planning/phases/17-adapter-plugin-interface/17-02-SUMMARY.md`.
</output>
