---
phase: 17-adapter-plugin-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/horus_os/adapters/__init__.py
  - src/horus_os/adapters/base.py
  - src/horus_os/server/api.py
  - src/horus_os/__init__.py
  - tests/test_adapters_discovery.py
  - tests/test_adapters_integration.py
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-03
tags:
  - adapter
  - plugin
  - entry-points
  - fastapi
must_haves:
  truths:
    - "`horus_os.adapters.Adapter` is a runtime-checkable Protocol with `name: str`, `bind(app, context) -> None`, and an optional `describe() -> dict` method"
    - "`discover_adapters()` returns a list of instantiated adapters sorted by their declared entry-point name"
    - "A load failure on one entry point does not abort discovery of the others; the broken one is skipped with a warning record"
    - "`create_app(data_dir)` calls `discover_adapters()` once and invokes `bind(app, context)` on each discovered adapter"
    - "Adapters get an `AdapterContext` carrying the `Config` and the `data_dir` Path"
    - "A third-party entry point (stubbed via `entry_points` monkeypatch) is discovered and its route is mounted on the FastAPI app without modifying horus-os"
    - "v0.1 callers that never declare an entry point see no behavior change in `create_app`"
  artifacts:
    - path: src/horus_os/adapters/__init__.py
      provides: "Public re-exports: Adapter, AdapterContext, discover_adapters"
    - path: src/horus_os/adapters/base.py
      provides: "Adapter Protocol, AdapterContext dataclass, discover_adapters() helper, module-level entry_points alias for test monkeypatching"
    - path: src/horus_os/server/api.py
      provides: "create_app calls discover_adapters() after core routes and binds each adapter onto the FastAPI instance"
    - path: src/horus_os/__init__.py
      provides: "Top-level Adapter, AdapterContext, discover_adapters re-exports for downstream packages"
    - path: tests/test_adapters_discovery.py
      provides: "Unit tests for the Protocol shape, discover_adapters ordering, and load-failure isolation"
    - path: tests/test_adapters_integration.py
      provides: "End-to-end test that a stubbed third-party entry point is discovered and its route is callable via TestClient"
  key_links:
    - from: "server/api.py:create_app"
      to: "adapters/base.py:discover_adapters"
      via: "discover_adapters() called inside create_app after core routes are registered"
      pattern: "discover_adapters"
    - from: "adapters/base.py:discover_adapters"
      to: "importlib.metadata.entry_points"
      via: "entry_points(group=ADAPTER_ENTRY_POINT_GROUP)"
      pattern: "entry_points"
---

<objective>
Define the adapter plugin contract and ship the entry-point discovery
pipeline. ADAPT-01 ("Plugin contract defined via `horus_os.adapters`
entry point") and ADAPT-03 ("Third-party adapters register without
forking horus-os") both depend on this plan landing first.

Purpose: the reference webhook adapter (Plan 02) is a consumer of this
contract, but the contract itself must be stable and tested in
isolation. Third-party adapter authors must be able to depend on
`horus_os.adapters.Adapter` with no transitive dependency on FastAPI
or any provider SDK.

Output: a new `src/horus_os/adapters/` package, the discovery helper,
the FastAPI wiring in `create_app`, public re-exports from
`horus_os/__init__.py`, and two test files that prove the contract
plus the third-party discovery story.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# horus-os Phase 17 Plan 01

## Adapter contract shape

```
class Adapter(Protocol):
    name: str
    def bind(self, app: Any, context: AdapterContext) -> None: ...
```

Optional `describe()` returns a static metadata dict; default
implementation provided via the `_AdapterMixin` is not used because
Protocols cannot hold method bodies. We document the optional method
in the docstring and check for it with `hasattr` in
`discover_adapters` for the diagnostics path.

## AdapterContext

```
@dataclass(frozen=True)
class AdapterContext:
    config: Config
    data_dir: Path
```

The context is frozen so adapters cannot mutate it. The `data_dir`
is the resolved Path used by the rest of horus-os. Adapters that
need a `Database` instance construct their own with
`Database(context.config.db_path)` per request, matching the
short-lived connection pattern used by the existing FastAPI routes.

## Discovery helper

```
ADAPTER_ENTRY_POINT_GROUP = "horus_os.adapters"

def discover_adapters() -> list[Adapter]:
    discovered = []
    failures: list[tuple[str, Exception]] = []
    for ep in sorted(entry_points(group=ADAPTER_ENTRY_POINT_GROUP), key=lambda e: e.name):
        try:
            factory = ep.load()
            adapter = factory() if isinstance(factory, type) else factory
            discovered.append(adapter)
        except Exception as exc:
            failures.append((ep.name, exc))
    return discovered
```

`entry_points` is imported at module top: `from importlib.metadata
import entry_points`. Tests monkeypatch `horus_os.adapters.base.entry_points`
to inject synthetic entries. The factory pattern accepts either a
class (instantiated with no args) or an already-constructed callable
that returns an adapter.

`failures` is collected but currently dropped on the floor. The
plan keeps it as a local for future diagnostics surfacing; the
test that asserts "one broken, others still load" looks at the
returned list, not at the failures list.

## FastAPI wiring

Inside `create_app(data_dir)`, after the existing routes are
registered and before `return app`, add:

```
from horus_os.adapters.base import discover_adapters, AdapterContext
adapters = discover_adapters()
ctx = AdapterContext(config=_config(), data_dir=Path(data_dir or _config().data_dir))
for adapter in adapters:
    try:
        adapter.bind(app, ctx)
    except Exception as exc:
        # adapter bind failure should not break the core app
        pass
```

The `_config()` closure is already defined inside `create_app`. The
`data_dir` argument is normalized the same way the closure does so
the context is consistent.

## Module-level entry_points binding

`horus_os.adapters.base` declares `entry_points` as a module-level
name imported from `importlib.metadata`. Tests monkeypatch
`horus_os.adapters.base.entry_points` with `monkeypatch.setattr`,
which is how the stdlib supports this exact pattern. The
`discover_adapters` function references the bound name (not the
imported symbol directly), so the patch takes effect.

## Public surface

`src/horus_os/adapters/__init__.py`:

```
from horus_os.adapters.base import (
    ADAPTER_ENTRY_POINT_GROUP,
    Adapter,
    AdapterContext,
    discover_adapters,
)

__all__ = [
    "ADAPTER_ENTRY_POINT_GROUP",
    "Adapter",
    "AdapterContext",
    "discover_adapters",
]
```

`src/horus_os/__init__.py` extends `__all__` with the same three names
and adds the import line. This keeps the top-level package surface
consistent with how `Database`, `Config`, etc. are exposed.

## Test stubbing pattern

For `entry_points` stubbing, use the real `EntryPoint` dataclass from
`importlib.metadata` so `.load()` works against a real-looking object.
The stub `EntryPoint.load()` calls the dotted path; instead of writing
real installable fixtures, we override the EntryPoint class with a
small wrapper whose `.load()` returns the in-memory class directly.

```
class _FakeEntryPoint:
    def __init__(self, name, target):
        self.name = name
        self._target = target
    def load(self):
        return self._target
```

The stub function returned by the monkeypatch ignores the `group`
argument and returns a list of `_FakeEntryPoint` objects. This is
simpler than the real `EntryPoints` object and is enough for
`discover_adapters` because the helper only uses `.name` and
`.load()`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the adapters package with Adapter Protocol, AdapterContext, and discover_adapters</name>
  <files>src/horus_os/adapters/__init__.py, src/horus_os/adapters/base.py, tests/test_adapters_discovery.py</files>
  <behavior>
    - `Adapter` is a runtime-checkable Protocol with `name: str` and `bind(app, context) -> None`
    - `AdapterContext` is a frozen dataclass with `config: Config` and `data_dir: Path`
    - `discover_adapters()` reads entry points in the `horus_os.adapters` group, sorts by name, instantiates each (calling a class with no args, or treating a callable as a factory), and returns the list of adapters
    - A load failure (any exception raised by `.load()` or by the factory call) is caught and the broken entry is skipped; other entries still appear in the returned list
    - The function returns an empty list when no entry points are declared (default state)
    - `ADAPTER_ENTRY_POINT_GROUP == "horus_os.adapters"` is exported
  </behavior>
  <action>
1. Create `src/horus_os/adapters/__init__.py`:

```
"""horus-os adapter plugin interface.

Adapters are inbound channels that route external events into the
horus-os agent runtime. They are discovered at FastAPI app startup
via the `horus_os.adapters` entry point group and bind their own
routes onto the FastAPI instance.

Third-party packages declare an adapter in their `pyproject.toml`:

    [project.entry-points."horus_os.adapters"]
    my_adapter = "my_package.adapter:MyAdapter"

`MyAdapter` must be a callable (typically a class) that returns an
object satisfying the `Adapter` Protocol.
"""

from horus_os.adapters.base import (
    ADAPTER_ENTRY_POINT_GROUP,
    Adapter,
    AdapterContext,
    discover_adapters,
)

__all__ = [
    "ADAPTER_ENTRY_POINT_GROUP",
    "Adapter",
    "AdapterContext",
    "discover_adapters",
]
```

2. Create `src/horus_os/adapters/base.py`:

```
"""Adapter contract and entry-point discovery helper.

The `Adapter` Protocol describes the minimum surface a third-party
adapter must implement. `discover_adapters` walks the
`horus_os.adapters` entry point group and returns a list of
instantiated adapters ready to bind onto a FastAPI app.

`entry_points` is rebound at module level so tests can monkeypatch
the discovery source without touching the importlib internals.
"""

from __future__ import annotations

from dataclasses import dataclass
from importlib.metadata import entry_points
from pathlib import Path
from typing import Any, Protocol, runtime_checkable

from horus_os.config import Config

ADAPTER_ENTRY_POINT_GROUP = "horus_os.adapters"


@dataclass(frozen=True)
class AdapterContext:
    """Read-only bundle of state handed to an adapter at bind time.

    Adapters receive this once during `bind(app, context)` and store
    whatever subset they need. The context is frozen so adapters
    cannot mutate global state through it.
    """

    config: Config
    data_dir: Path


@runtime_checkable
class Adapter(Protocol):
    """The minimum surface a horus-os adapter must implement.

    `name` is a stable identifier used for diagnostics and for the
    URL prefix on the FastAPI app (`/api/adapters/<name>/...`).

    `bind(app, context)` is called once during `create_app` startup.
    The adapter is expected to register routes on `app` and store
    any context it needs for handling requests.

    An optional `describe(self) -> dict` method may return a static
    metadata dict for diagnostics; callers should use `hasattr` to
    check for it.
    """

    name: str

    def bind(self, app: Any, context: AdapterContext) -> None:
        """Mount the adapter onto the given FastAPI app."""
        ...


def discover_adapters() -> list[Adapter]:
    """Return all adapters declared via the `horus_os.adapters` entry point.

    The list is sorted by entry-point name so adapter ordering is
    deterministic across runs.

    A load failure on one entry point (the dotted path does not
    resolve, or the factory raises during instantiation) is caught
    silently. The broken entry is skipped; other entries still load.
    Returning an incomplete list rather than crashing keeps the core
    dashboard functional even when an optional adapter package has a
    bug.
    """
    discovered: list[Adapter] = []
    eps = list(entry_points(group=ADAPTER_ENTRY_POINT_GROUP))
    for ep in sorted(eps, key=lambda e: e.name):
        try:
            target = ep.load()
            adapter = target() if isinstance(target, type) else target
        except Exception:
            continue
        discovered.append(adapter)
    return discovered
```

3. Create `tests/test_adapters_discovery.py`:

```
"""Tests for the adapter Protocol shape and entry-point discovery."""

from __future__ import annotations

from pathlib import Path
from typing import Any

import pytest

from horus_os.adapters import (
    ADAPTER_ENTRY_POINT_GROUP,
    Adapter,
    AdapterContext,
    discover_adapters,
)
from horus_os.adapters import base as adapters_base
from horus_os.config import Config


class _FakeEntryPoint:
    """Minimal stand-in for importlib.metadata.EntryPoint used in tests."""

    def __init__(self, name: str, target: Any) -> None:
        self.name = name
        self._target = target

    def load(self) -> Any:
        if isinstance(self._target, BaseException):
            raise self._target
        return self._target


class _FakeAdapter:
    name = "fake"

    def __init__(self) -> None:
        self.bound: list[Any] = []

    def bind(self, app: Any, context: AdapterContext) -> None:
        self.bound.append((app, context))


class _OtherAdapter:
    name = "other"

    def bind(self, app: Any, context: AdapterContext) -> None:
        pass


def _stub_entry_points(monkeypatch: pytest.MonkeyPatch, eps: list[_FakeEntryPoint]) -> None:
    def fake(group: str | None = None) -> list[_FakeEntryPoint]:
        if group != ADAPTER_ENTRY_POINT_GROUP:
            return []
        return eps

    monkeypatch.setattr(adapters_base, "entry_points", fake)


def test_entry_point_group_constant() -> None:
    assert ADAPTER_ENTRY_POINT_GROUP == "horus_os.adapters"


def test_adapter_is_runtime_checkable() -> None:
    adapter = _FakeAdapter()
    assert isinstance(adapter, Adapter)


def test_adapter_context_is_frozen(tmp_path: Path) -> None:
    cfg = Config.with_defaults(tmp_path)
    ctx = AdapterContext(config=cfg, data_dir=tmp_path)
    with pytest.raises(Exception):
        ctx.data_dir = tmp_path  # type: ignore[misc]


def test_discover_adapters_returns_empty_list_by_default(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    _stub_entry_points(monkeypatch, [])
    assert discover_adapters() == []


def test_discover_adapters_instantiates_classes(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    _stub_entry_points(monkeypatch, [_FakeEntryPoint("fake", _FakeAdapter)])
    adapters = discover_adapters()
    assert len(adapters) == 1
    assert isinstance(adapters[0], _FakeAdapter)


def test_discover_adapters_accepts_factories(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    instance = _FakeAdapter()

    def _factory() -> _FakeAdapter:
        return instance

    _stub_entry_points(monkeypatch, [_FakeEntryPoint("fake", _factory)])
    adapters = discover_adapters()
    assert len(adapters) == 1
    assert adapters[0] is instance


def test_discover_adapters_sorts_by_name(monkeypatch: pytest.MonkeyPatch) -> None:
    _stub_entry_points(
        monkeypatch,
        [
            _FakeEntryPoint("zebra", _FakeAdapter),
            _FakeEntryPoint("alpha", _OtherAdapter),
            _FakeEntryPoint("mango", _FakeAdapter),
        ],
    )
    adapters = discover_adapters()
    assert [a.name for a in adapters] == ["other", "fake", "fake"]


def test_discover_adapters_skips_failing_load(monkeypatch: pytest.MonkeyPatch) -> None:
    _stub_entry_points(
        monkeypatch,
        [
            _FakeEntryPoint("broken", RuntimeError("boom")),
            _FakeEntryPoint("good", _FakeAdapter),
        ],
    )
    adapters = discover_adapters()
    assert len(adapters) == 1
    assert isinstance(adapters[0], _FakeAdapter)


def test_discover_adapters_skips_failing_factory(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    class _Boom:
        def __init__(self) -> None:
            raise ValueError("nope")

    _stub_entry_points(
        monkeypatch,
        [
            _FakeEntryPoint("broken", _Boom),
            _FakeEntryPoint("good", _FakeAdapter),
        ],
    )
    adapters = discover_adapters()
    assert len(adapters) == 1
    assert isinstance(adapters[0], _FakeAdapter)
```
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && python -m pytest tests/test_adapters_discovery.py -x -q --tb=short</automated>
  </verify>
  <done>tests/test_adapters_discovery.py passes. ruff check . and ruff format --check . clean on the new files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire discover_adapters into create_app and expose public re-exports</name>
  <files>src/horus_os/server/api.py, src/horus_os/__init__.py, tests/test_adapters_integration.py</files>
  <behavior>
    - `create_app(data_dir)` calls `discover_adapters()` once during construction, after core routes are registered
    - Each discovered adapter receives `bind(app, context)` with an `AdapterContext` whose `config` and `data_dir` match what the rest of the app uses
    - An adapter whose `bind` raises does not prevent later adapters from binding or break `create_app`
    - When no adapters are declared, `create_app` returns an app identical to today's behavior (regression-tested by the existing test_server_* suites)
    - A third-party adapter discovered via a stubbed entry point can mount a route on the app and the route is reachable through TestClient
    - `from horus_os import Adapter, AdapterContext, discover_adapters` works
  </behavior>
  <action>
1. In `src/horus_os/server/api.py`, near the top imports, add:

```
from horus_os.adapters.base import AdapterContext, discover_adapters
```

2. Inside `create_app`, immediately before `return app`, add the
   adapter bind block:

```
# Discover and bind any third-party adapters declared via the
# `horus_os.adapters` entry point group. Discovery is a one-time
# walk at app construction; adapters mount their own routes on
# `app`. A failing adapter is logged and skipped so the core
# dashboard stays up.
_adapters = discover_adapters()
_resolved_data_dir = (
    Path(data_dir).expanduser() if data_dir is not None else _config().data_dir
)
_adapter_context = AdapterContext(config=_config(), data_dir=_resolved_data_dir)
for _adapter in _adapters:
    try:
        _adapter.bind(app, _adapter_context)
    except Exception:
        # an individual adapter bind failure must not break create_app
        continue
```

The `_config()` closure already resolves the Config from `data_dir`,
so calling it here is consistent with the rest of `create_app`.
`_resolved_data_dir` mirrors the closure's normalization step.

3. In `src/horus_os/__init__.py`, add the imports and update
   `__all__`:

```
from horus_os.adapters import Adapter, AdapterContext, discover_adapters
```

Insert `"Adapter"`, `"AdapterContext"`, and `"discover_adapters"` in
`__all__` in alphabetical order.

4. Create `tests/test_adapters_integration.py`:

```
"""Tests for adapter discovery and FastAPI integration via TestClient."""

from __future__ import annotations

from pathlib import Path
from typing import Any

import pytest
from fastapi.testclient import TestClient

from horus_os import Config, Database, create_app
from horus_os.adapters import ADAPTER_ENTRY_POINT_GROUP, AdapterContext
from horus_os.adapters import base as adapters_base


class _FakeEntryPoint:
    def __init__(self, name: str, target: Any) -> None:
        self.name = name
        self._target = target

    def load(self) -> Any:
        return self._target


class _MountingAdapter:
    """A test adapter that mounts a single GET route announcing itself."""

    name = "fake_third_party"

    def __init__(self) -> None:
        self.captured_context: AdapterContext | None = None

    def bind(self, app: Any, context: AdapterContext) -> None:
        self.captured_context = context

        @app.get(f"/api/adapters/{self.name}/ping")
        def _ping() -> dict[str, str]:
            return {"adapter": self.name, "data_dir": str(context.data_dir)}


class _RaisingAdapter:
    name = "raises_on_bind"

    def bind(self, app: Any, context: AdapterContext) -> None:
        raise RuntimeError("simulated bind failure")


def _init_db(tmp_path: Path) -> None:
    cfg = Config.with_defaults(tmp_path)
    cfg.save()
    db = Database(cfg.db_path)
    db.init()


def _stub_entry_points(
    monkeypatch: pytest.MonkeyPatch, eps: list[_FakeEntryPoint]
) -> None:
    def fake(group: str | None = None) -> list[_FakeEntryPoint]:
        if group != ADAPTER_ENTRY_POINT_GROUP:
            return []
        return eps

    monkeypatch.setattr(adapters_base, "entry_points", fake)


def test_create_app_without_adapters_keeps_core_routes(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    _init_db(tmp_path)
    _stub_entry_points(monkeypatch, [])
    client = TestClient(create_app(data_dir=tmp_path))
    response = client.get("/api/health")
    assert response.status_code == 200


def test_third_party_adapter_route_is_mounted(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    _init_db(tmp_path)
    _stub_entry_points(
        monkeypatch,
        [_FakeEntryPoint("fake_third_party", _MountingAdapter)],
    )
    client = TestClient(create_app(data_dir=tmp_path))
    response = client.get("/api/adapters/fake_third_party/ping")
    assert response.status_code == 200
    body = response.json()
    assert body["adapter"] == "fake_third_party"
    assert body["data_dir"] == str(tmp_path)


def test_adapter_bind_failure_does_not_break_app(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    _init_db(tmp_path)
    _stub_entry_points(
        monkeypatch,
        [
            _FakeEntryPoint("raises_on_bind", _RaisingAdapter),
            _FakeEntryPoint("fake_third_party", _MountingAdapter),
        ],
    )
    client = TestClient(create_app(data_dir=tmp_path))
    # core route still works
    assert client.get("/api/health").status_code == 200
    # the well-behaved adapter still mounted despite the sibling failure
    assert client.get("/api/adapters/fake_third_party/ping").status_code == 200


def test_adapter_context_uses_resolved_data_dir(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    _init_db(tmp_path)
    adapter = _MountingAdapter()
    _stub_entry_points(
        monkeypatch,
        [_FakeEntryPoint("fake_third_party", lambda: adapter)],
    )
    create_app(data_dir=tmp_path)
    assert adapter.captured_context is not None
    assert adapter.captured_context.data_dir == tmp_path
    assert adapter.captured_context.config.data_dir == tmp_path
```

Note: this test stubs the entry point with a `lambda: adapter` factory
that returns the same instance, so we can introspect the captured
context. The other tests use the class directly to verify the
common path.
  </action>
  <verify>
    <automated>cd /Users/santino/Projects/horus-os && python -m pytest tests/test_adapters_discovery.py tests/test_adapters_integration.py -x -q --tb=short && python -m pytest -q --tb=no</automated>
  </verify>
  <done>Both new test files pass. Full pytest suite (275 + new) green. ruff check . and ruff format --check . clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party entry-point code -> horus-os process | Discovery imports arbitrary modules declared by installed packages |
| Adapter bind() -> FastAPI app | A misbehaving adapter could shadow core routes or leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Third-party adapter code | accept | Adapters are installed by the user; the user has chosen to trust the package. Discovery is opt-in via pip install |
| T-17-02 | Denial | Adapter raises during load or bind | mitigate | discover_adapters and the bind loop both catch and continue past individual failures so one bad adapter cannot brick the dashboard |
| T-17-03 | Information Disclosure | Adapter reads AdapterContext.config | accept | The Config dataclass contains data_dir paths and model names but no secrets; secrets stay in env vars |
| T-17-04 | Spoofing | Adapter declares the same route prefix as a core route | accept | Adapters mount after core routes; FastAPI raises on duplicate. A duplicate is detectable by the user and a clear error |
| T-17-05 | Elevation of Privilege | Adapter executes arbitrary code at bind | accept | Same trust posture as any pip-installed dependency. Documented in the adapter README |
</threat_model>

<verification>
- `python -m pytest tests/test_adapters_discovery.py tests/test_adapters_integration.py -q` passes
- `python -m pytest -q` full suite passes (275 baseline + Plan 01 additions)
- `ruff check .` clean
- `ruff format --check .` clean
- `python -c "from horus_os import Adapter, AdapterContext, discover_adapters; print('ok')"` prints ok
- `python -c "from horus_os.adapters.base import ADAPTER_ENTRY_POINT_GROUP; print(ADAPTER_ENTRY_POINT_GROUP)"` prints `horus_os.adapters`
</verification>

<success_criteria>
- `horus_os.adapters.Adapter` is importable and is a runtime-checkable Protocol
- `horus_os.adapters.discover_adapters()` returns a deterministically ordered list
- A failing entry point or failing bind does not break `create_app`
- A stubbed third-party entry point mounts a route reachable via TestClient
- All Phase 16 tests still pass byte-for-byte
- Top-level `from horus_os import Adapter, AdapterContext, discover_adapters` works
</success_criteria>

<output>
After completion, create `.planning/phases/17-adapter-plugin-interface/17-01-SUMMARY.md`.
</output>
