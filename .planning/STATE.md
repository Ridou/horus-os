---
gsd_state_version: 1.0
milestone: v0.8
milestone_name: Local-first & Autonomous Research
status: complete
last_updated: "2026-06-03T00:00:00.000Z"
last_activity: 2026-06-03
progress:
  total_phases: 8
  completed_phases: 8
  total_plans: 22
  completed_plans: 22
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md and .planning/README.md.

**Core value:** Run a personal team of AI agents on your laptop, with full transparency over every action.
**Current focus:** v0.8 shipped. Planning v0.9 (Autonomy and Control). See .planning/PROGRAM-v0.9-v0.14.md and .planning/research/v0.9-gap-analysis.md. Three post-v0.8 product surfaces (streaming dashboard chat, agent store, opt-in Twilio voice adapter) are on main, unreleased, in CHANGELOG [Unreleased].

## Current Position

Phase: 76 (three-os-gate-and-v0-8-0-release) - COMPLETE (shipped)
Plan: all plans complete
Status: v0.8 SHIPPED 2026-06-03 as v0.8.0 (PR #15 squash-merged to main 8576b02, tag pushed, Release published, schema v13). v0.8 planning artifacts (ROADMAP/REQUIREMENTS + phases 69-76) synced from the v0.8 worktree to main on 2026-06-03 so the planning history is complete (the squash merge had dropped them). Open follow-ups: enable repo Dependency-graph (dependency-review CI fails without it), delete superseded v0.8 branches (v0.8-backup-pre-rebase, v0.8-pre-rebase tag), remove the v0.8 worktree once confirmed synced.
Last activity: 2026-06-03

## Prior Milestones

- **v0.1 Foundation** (Phases 01-11): SHIPPED 2026-05-23 as v0.1.0. 175 tests, 3-OS install-smoke green.
- **v0.2 Multi-Agent + Streaming** (Phases 12-21): SHIPPED 2026-05-23 as v0.2.0. 319 tests, 3-OS install-smoke green. Multi-agent runtime, streaming, adapter contract, HMAC webhook reference adapter, dashboard SSE + agents view.
- **v0.3 Adapter Ecosystem** (Phases 22-31): SHIPPED 2026-05-24 as v0.3.0. 447 tests, 3-OS install-smoke green. Adapter lifecycle hooks, Discord + Slack + Email + Calendar adapters, AdapterRegistry, Dashboard Adapters tab, four per-adapter setup guides, four runnable examples, v0.2-to-v0.3 migration guide.
- **v0.4 Observability** (Phases 32-39): SHIPPED 2026-05-26 as v0.4.0. ObservationBus + SQLitePersister, llm_calls + tool_invocations child tables, bundled pricing.json with cache-aware cost annotation, /observability dashboard tab + horus-os usage CLI, opt-in OtelAdapter behind [otel] extra with default-deny content capture + bounded shutdown, scripts/release_gate.py with pricing freshness + two-variant install-smoke matrix.
- **v0.5 Plugin System** (Phases 40-50): SHIPPED 2026-05-27 as v0.5.0. 1011 tests green, 3-OS install-smoke green including the new install-smoke-plugin matrix. TOML manifest contract (pydantic v2), entry-point + filesystem discovery, default-deny capability grants pinned to manifest-hash with re-prompt on upgrade, two-phase `pip install` flow with sdist + `.pth` + runtime-dep-downgrade refusals, bounded `asyncio.wait_for(start, timeout=2.0)` failure isolation, `/plugins` dashboard tab + per-plugin observability attribution, reference plugin (`examples/horus-os-example-plugin/`) demonstrating four scenarios with the two-layer TID251 + source-tree-grep API surface lock, docs trio (`docs/PLUGINS.md` + `docs/PLUGIN-SECURITY.md` + `docs/MIGRATION-v0.4-to-v0.5.md`), `scripts/release_gate.py` extended from 4 to 8 checks. Two new base direct deps: `pydantic>=2.7,<3` + `packaging>=24.0`. v5->v6 schema migration additive only.
- **v0.6 Contribution Gate** (Phases 51-59): Substantially complete and rehearsal-ready. The single-atomic-commit gate flip (Phase 59) is drafted but not yet landed (carried as a pending todo). No v0.6.0 tag yet (v0.7.0 follows v0.5.0 directly in tag history).
- **v0.7 Command Center** (Phases 60-68): SHIPPED 2026-06-03 as v0.7.0. Next.js command center with Tailwind v4 + Radix design system, Setup-and-Verify integrations surface, Discord control bot, Supabase sync loop, cron scheduler + always-on service, Vercel deploy path, GitHub tool. Schema v12. Four new opt-in extras ([discord], [supabase], [vercel], [github]), all excluded from [all].
- **v0.8 Local-first & Autonomous Research** (Phases 69-76): SHIPPED 2026-06-03 as v0.8.0. Local LLM provider, on-device ONNX vector memory, MCP client, web access, vision/PDF, Deep Research flagship, skills system, gated shell execution. Schema v13. Per-capability opt-in extras plus a [research] meta-extra; a bare install activates none of it.

## Decisions

- 60-02: Modal wraps Radix Dialog.Root/Portal/Overlay/Content with "use client"; no hand-rolled focus management (D-01, D-03)
- 60-02: StatusDot pulse uses separate outer animate-pulse ring and inner status-pulse dot (avoids animation collision per Pitfall 5)
- 60-02: StatusDot is pure presentational (no "use client" required); StatusDotState type re-exported from barrel for Phase 61/62 consumers
- 60-03: CI guards scoped to git-diff changed files (not repo-wide) because legacy committed files contain em-dashes predating CLAUDE.md rule 3; enforces the rule going forward on all new and modified files
- 60-03: Private-name guard uses RESEARCH.md Pattern 4 reserved tokens; comments reference only "reserved private-pipeline names" without naming the pipeline
- 60-04: Stepper root uses role=group + aria-label "Step N of M" and aria-live=polite on progress row; primary CTA sets both disabled and aria-disabled for maximum AT compatibility
- 60-04: CopyButton and CommandBlock extracted verbatim from get-started/page.tsx into standalone barrel-exported components; get-started no longer defines them inline
- 61-02: vi.resetModules + vi.doMock in beforeEach used for per-test isDemoMode isolation in vitest (vi.mock hoists prevent per-test scoping)
- 61-02: demo mode renders read-only step body without Stepper (no CTA suppress prop on Stepper) plus DemoModeNotice; satisfies SETUP-06 and no-Continue test requirement
- 62-02: loopback guard uses request.client.host (TCP socket peer) with frozenset fast-path and ipaddress fallback; no header is read (REMOTE-01, T-62-04)
- 62-02: probe error messages return only type(exc).__name__ to prevent key material from appearing in VerifyResponse.error (T-62-06)
- 62-02: test isolation - tests writing keys use monkeypatch.setenv to register original values so route handler os.environ mutations are cleaned up at teardown
- 62-03: CredentialRow uses local localStatus state initialized from integration.status for immediate UI updates post-save/verify without waiting for a full query refetch
- 62-03: onRestartRequired callback lifted to settings page so RestartBanner is managed at page level (single banner for all rows)
- 63-01: tasks table uses INSERT OR IGNORE on task_id unique constraint for safe demo re-seeding
- 63-01: v8->v9 migration is a no-op block (new table only, SCHEMA_SQL handles idempotency)
- 63-01: load_profile_icase uses LOWER(name) = LOWER(?) to avoid collation config dependency
- 63-02: DELETE routes use response_class=Response + return Response(status_code=204) to satisfy FastAPI 0.115 assertion that 204 must not have a response body
- 63-02: is_example detection uses module-level _SEED_VAULT_FILENAMES frozenset plus agents/*/SOUL.md path-pattern match
- 63-02: TEAM-03 test is parametrized over four specialist names for clean per-specialist failure messages
- 63-04: TourOverlay uses createPortal + fixed divs (no Radix Dialog) for non-blocking spotlight without focus trap
- 63-04: spotlight default position is off-screen (-9999px) when data-tour-step target absent - graceful degradation before Plan 05 targets exist
- 63-04: Done button calls skipTour() - same localStorage set and deactivation as Skip
- 63-05: AgentDetailView extracted to separate client component (AgentDetailView.tsx); page.tsx is server component with dynamic=force-static + revalidate=0 to bypass Next.js 16 output:export prerenderedRoutes check for empty generateStaticParams
- 63-05: Tests import AgentDetailView directly (not page default export) to avoid React.use() Suspense suspension in jsdom
- [Phase ?]: 64-02: commands.Bot dual-mode start; GUILD_ID required for v0.7 path; admin gate deny-by-default; _bootstrap is create-only with zero deletes (TEST-28)
- 64-03: discord required_vars expanded to three vars (TOKEN + GUILD_ID + ADMIN_ROLE_ID); in-dashboard walkthrough auto-renders all three via existing IntegrationWalkthrough (no new React code)
- 64-03: permission integer 309237738560 hard-coded in DISCORD.md with explicit not-Administrator warning (T-64-08 mitigated)
- 65-01: sync_cursors cursors stored locally in SQLite (not Supabase) -- local-first survives Supabase downtime (SUPA-01)
- 65-01: list_rows_after uses _SYNC_ALLOWLIST to prevent SQL injection via identifier interpolation (T-65-01)
- 65-01: [supabase] extra = httpx>=0.27 only; excluded from [all] per SUPA-05 and REL-17 (T-65-03)
- 65-02: bind() returns None -- service key never mounted on a browser-accessible route (SUPA-02, T-65-04)
- 65-02: _sync_headers() factored as module-level function so tests assert header shape without running the infinite loop
- 65-02: _sync_table and _push_sync_health are module-level async functions (not instance methods) for direct test-driving
- 65-03: TEST-29 greps for process.env.SUPABASE_SERVICE_KEY pattern (not bare string) -- bare string appears legitimately in required_vars metadata; process.env form is the actual browser-exposure threat (SUPA-02)
- 65-03: doctor_cmd lazy import httpx requires sys.modules injection in tests (patch cannot find module-level attribute for function-local imports)
- 65-03: doctor returns 0 only when all tables report rls_enabled=true; returns 2 if any table is off; service key value never printed (T-65-09)
- 65-04: dynamic import uses @vite-ignore on variable specifier so Vite does not attempt static resolution of @supabase/supabase-js (not in package.json); try/catch handles missing-package case at runtime
- 65-04: vi.doMock + vi.resetModules in beforeEach for per-test supabase/api mock isolation (vi.mock hoisting prevents per-test scoping - same pattern as 61-02)
- 65-04: source-text assertions check process.env access patterns, not bare string mentions - doc comments may reference forbidden key names; only env reads are the actual security concern
- [Phase ?]: 66-01: schedules table stores cron as canonical form (D-01) with per-schedule catch_up_policy defaulting to coalesce (D-04)
- [Phase ?]: 66-01: croniter is a BASE dependency (scheduler core-on-by-default per D-13), not an optional extra
- [Phase ?]: 66-01: update_schedule uses a static _SCHEDULE_UPDATABLE allowlist so dynamic SET assignments never interpolate caller-supplied column names (T-66-01)
- [Phase ?]: 66-01: Wave 0 stubs use pytest.mark.skip (not xfail) so the suite stays green while exact downstream test names exist as targets
- [Phase ?]: 66-02: SchedulerAdapter is opt-out via HORUS_OS_DISABLE_SCHEDULER (D-13); coalesce max(croniter_next, now) persisted before dispatch with _running_ids overlap guard (D-03, T-66-03); cron-only via to_cron desugar+validate (D-01); dispatch tasks tracked to avoid asyncio GC (RUF006 fix)
- [Phase ?]: 66-03: service definitions are pure str generators (no subprocess) so --print and TEST-30 share one OS-free path; all OS mutation behind manager.py sys.platform dispatch; shutil.which nssm guard guides not crashes (D-12); doctor --service maps manager.status() to an exit code
- 66-04: docs/REMOTE.md ships serve --host 0.0.0.0 no-auth caveat + Tailscale serve happy path (tailnet=auth boundary, D-10) + funnel hard-do-not (D-11) + Windows OpenSSH note; activated docs grep test (5 assertions incl em-dash guard) pins the content (REMOTE-02, REMOTE-03)
- [Phase ?]: 67-01: API_BASE derives from NEXT_PUBLIC_API_BASE via an SSR-safe typeof-process read with a single-trailing-slash strip; unset = same-origin /api, set = remote-origin /api (VERCEL-01, D-01)
- 67-03: github_read tool uses stdlib urllib lazy-imported inside the handler (no PyGithub, three-OS gate safe, D-08); returns {error: type(exc).__name__} so GITHUB_TOKEN is never echoed (T-67-07); registered identically in both _build_default_registry functions (run_cmd.py + server/api.py), pre-existing config/cfg naming left unchanged (D-07)
- 67-03: [vercel] and [github] extras pin httpx>=0.27 mirroring [supabase] and are both excluded from [all]; a tomllib install-smoke test asserts the exclusion invariant (TEST-31, D-09, also Phase 68 criterion 2)
- 68-02: CHANGELOG [0.7.0] + docs/MIGRATION-v0.5-to-v0.7.md authored (v0.5 -> v0.7, v0.6 never tagged); additive v10->v12 schema; docs-grep test pins sections + em-dash + private-name + SCHEMA_VERSION==12 (D-04, D-05, D-08, REL-17); release actions deferred to owner (D-07)
- 68-03: tests/test_ci_matrix_three_os.py pins the 3-OS x 2-Python matrix and the four install-smoke job names; three-OS green framed as a post-push CI / human-verified item, the test only asserts the matrix is WIRED (D-06, REL-16 criterion 1)
- 68-03: v0.7.0 staged locally only; the local release-readiness proxy (release_gate --skip-build --allow-offline + full pytest + install-smoke) was run and recorded; push, tag, tag push, gh release create, and merge to main await explicit owner confirmation and were performed by no task (D-07, HARD)

## Last Activity

2026-06-02, Phase 68 VERIFIED and v0.7 milestone closed against autonomous scope. Cleared the three CI private-name guard trips before the release PR (ci.yml self-match, DISCORD.md example agent profile, the migration test's literal regex), then ran the gsd-code-reviewer (standard depth) over the 12 changed phase-68 files. The review surfaced two Critical doc-correctness defects that were verified against source and fixed: CR-01, the migration verification command used PRAGMA user_version (always 0) and the path ~/.horus-os/data.db (exists on no platform), corrected to the runnable per-OS `SELECT version FROM schema_version` query against horus.sqlite and re-proven on a freshly initialized DB (returns 12); CR-02, v0.5.0 ships SCHEMA_VERSION 6 so the milestone range is v6 to v12 (not v10 to v12), adds five tables (discord_feedback was omitted) plus three nullable agent_profiles columns, corrected identically across MIGRATION, RELEASE-NOTES, and CHANGELOG. Two warnings also fixed: WR-01, the Discord invite integer 309237738560 set bit 35 (private threads) while the bot's message.create_thread() needs bit 34 (public threads), replaced with 292057869376 matching the eight documented permissions; WR-02, the reserved-name CI guard no longer blanket-excludes .github/workflows/, its pattern now carries one single-char class per word (r[a]lph, ...) so it matches the real names without self-matching, keeping workflow files under coverage. IN-02 de-staled the DISCORD.md v0.3 limitations list. WR-03 (push-path HEAD~1 scan) and WR-04 (the [all] assertions run in the dev job) verified non-blocking and deferred. Full suite after fixes: 1454 passed, 31 failed (the documented OTEL + release_gate baseline, zero new regressions), 9 skipped; release_gate.py --skip-build --allow-offline = 10 OK / 3 expected SKIP / 1 pytest FAIL (same baseline). gsd-verifier verdict: REL-16 wiring ACHIEVED + green-run human_needed, REL-17 ACHIEVED, no genuine blockers. 68-REVIEW.md (status resolved) and 68-VERIFICATION.md persisted; 68-HUMAN-UAT.md carries the three owner/post-push items (cross-OS CI green, owner-confirmed release publication, full-strength release_gate). version 0.7.0, SCHEMA_VERSION 12. Release-action boundary (D-07, HARD) honored: no push, no tag, no gh release, no merge. All 10 v0.7 phases (60-68) complete.

Prior: 2026-06-02, 68-03 complete. Three-OS gate proxy plus v0.7.0 staged release (REL-16, REL-17). tests/test_ci_matrix_three_os.py added (5 pathlib tests reading .github/workflows/ci.yml): asserts the os matrix lists ubuntu-latest + macos-latest + windows-latest, the python matrix lists 3.11 and 3.12, the install-smoke job installs '.[all]', and the four install-smoke job names (install-smoke, install-smoke-no-otel, install-smoke-with-otel, install-smoke-plugin) are present; the docstring frames the cross-OS GREEN as a post-push CI item the test cannot produce (D-06, REL-16 criterion 1 regression guard). The local release-readiness proxy was run on this OS and recorded in the SUMMARY: scripts/release_gate.py --skip-build --allow-offline exited 0 (11 OK, 3 expected SKIP for wheel-build / network pip-audit / sbom-vs-wheel, 1 pytest FAIL that is the transitive consequence of the pre-existing OTEL failures); python -m pytest -q reported 31 failed / 1454 passed / 9 skipped where all 31 failures are the pre-existing test_adapters_otel* set (29) plus the two test_release_gate.py main-exit-zero assertions (2), none introduced by this plan; install-smoke is green (schema_version==12, All install-smoke checks passed, tests/test_install_smoke.py 4 passed). docs/RELEASE-NOTES-v0.7.0.md created as the drafted GitHub Release body (title "v0.7.0 - Command Center", per-phase 60-67 summary, additive v10-to-v12 schema note, the ten-extra optional surface with the [all] include/exclude split naming [discord]/[supabase]/[vercel]/[github], the new env vars, the no-auth/open-CORS security note, a link to docs/MIGRATION-v0.5-to-v0.7.md, and an explicit "Release actions pending owner confirmation" footer). docs/RELEASE.md gained a "v0.7.0 cut (staged, awaiting owner go-ahead)" subsection pointing at the drafted body and a STOP-BEFORE-TAG owner-only boundary. Verification: tests/test_ci_matrix_three_os.py 5 passed, tests/docs + tests/test_install_smoke.py 43 passed, ruff check + format clean on the new test, all three changed files em-dash clean (grep -lP empty) and reserved-private-name clean. No deviations. Release-action boundary (D-07, HARD) honored: no git push, no git tag, no tag push, no gh release create, no merge to main, no local tag created either. Phase 68 EXECUTED, all 3 plans complete, ready for verification.

Prior: 2026-06-02, 68-02 complete. v0.7.0 release-notes content authored (REL-17). CHANGELOG.md gained a dated `## [0.7.0] - 2026-06-02` entry in Keep a Changelog format: a prose paragraph naming the Command Center milestone and that v0.6 was never tagged so 0.7.0 follows 0.5.0, an `### Added` block summarizing all eight v0.7 phases (60 design system + layout shell, 61 integrations surface + read-only API, 62 key-write endpoint + verification engine, 63 tier-1 pages + starter team + seed content, 64 Discord control bot, 65 Supabase sync loop + schema migrations, 66 cron scheduler + always-on service, 67 Vercel deploy path + GitHub tool + NEXT_PUBLIC_API_BASE) plus the four new opt-in extras ([discord], [supabase], [vercel], [github]) excluded from [all], and a `### Changed` block stating the additive/idempotent schema bump v10 -> v12 and the twelve new optional env vars. A reference line points at docs/MIGRATION-v0.5-to-v0.7.md; a fresh empty [Unreleased] stub sits above the entry (the prior stub was already empty). docs/MIGRATION-v0.5-to-v0.7.md created mirroring the docs/MIGRATION-v0.4-to-v0.5.md structure: TL;DR (v0.5 -> v0.7, additive, no manual steps), per-phase what-is-new, "Schema migration v10 to v12" naming the four additive tables (integration_verification_state, tasks, sync_cursors, schedules), "New optional extras" with per-extra install commands and the [all]-exclusion note, "New environment variables" (all twelve), "New CLI surfaces" (horus-os schedule, horus-os service, doctor --service, doctor --supabase), a prominent blockquote warning that the local /api has NO auth layer and CORS is open so an auth layer is a prerequisite before any non-localhost exposure, "Breaking change scan" (none), and "Verification" with the `sqlite3 ~/.horus-os/data.db "PRAGMA user_version"` command and expected output 12. tests/docs/test_migration_v05_v07.py added (modeled on test_migration_v04_v05_schema_commands.py + test_remote_md.py): module-scoped pathlib fixture plus 10 discrete tests (title, PRAGMA command, four opt-in extras, no-auth/CORS caveat, twelve env vars, four CLI surfaces, EM_DASH=chr(8212) absent, reserved-private-name regex absent, and `from horus_os.storage import SCHEMA_VERSION; assert SCHEMA_VERSION == 12` tripwire). Verification: 16 passed (new + sibling migration test), broader tests/docs/ 39 passed, ruff check + format clean on the new test, both new files fully em-dash clean (grep -lP empty), CHANGELOG [0.7.0] segment em-dash clean and references the migration note, no reserved-private-name in either new artifact. No deviations. Release-action boundary (D-07) honored: no push, no tag push, no gh release, no merge; Plan 03 assembles the drafted release body from these artifacts. REL-17 content satisfied; Phase 68 has one plan remaining.

Prior: 2026-06-02, 67-03 complete. Optional read-only github_read agent tool (GH-02) plus the [vercel]/[github] extras (TEST-31). New src/horus_os/tools/github_tool.py: make_github_read_tool() factory returns a Tool whose handler reads repo metadata (GET /repos/{owner}/{repo}) or file/dir contents (/contents/{path} when path is non-empty), lazy `import urllib.request` INSIDE the handler so the module imports with no extra and pulls no compiled dep (no PyGithub; the three-OS gate stays safe, D-08). Reads server-side GITHUB_TOKEN (the committed registry name) and adds Authorization Bearer only when set; unauthenticated public reads work when absent (about 60 req/hr). On any exception returns {"error": type(exc).__name__} only, never str(exc) and never the token (T-67-07). Exported from tools/__init__.py (__all__ + import) and registered via the identical line registry.register(make_github_read_tool()) in BOTH _build_default_registry functions (cli/run_cmd.py uses `config`, server/api.py uses `cfg`; the pre-existing parameter-name difference is unchanged, D-07). tests/test_github_tool.py: 7 tests built TDD RED->GREEN (repo-metadata read, contents-path read, no-token public fallback with no Authorization header, token-set Bearer header, token-never-echoed-on-error, factory name + lazy-import, source-grep banning any PyGithub reference). pyproject.toml: vercel = ["httpx>=0.27"] and github = ["httpx>=0.27"] added after [supabase]; both deliberately excluded from [all] (discord/supabase precedent, D-09). scripts/install_smoke.py: GITHUB_TOOL_IMPORT_SNIPPET proves github_read imports and builds with the [github] extra absent, GITHUB_TOOL_IMPORT_OK echoed to the driver stdout; SCHEMA_VERSION_EXPECTED unchanged at 12. tests/test_install_smoke.py: asserts the marker plus a new tomllib test (otel precedent) that vercel+github exist and that neither they nor any dep unique to them leak into [all] (Phase 68 criterion 2). Plan verification: 9 tests green (tests/test_github_tool.py + tests/test_install_smoke.py), 159 broader tool/registry/loop/integrations tests green, ruff clean on all touched files, no PyGithub reference. Two pre-existing em-dash files (api.py, pyproject.toml) flagged by the local guard but none on lines added this plan; out of scope (git-diff-scoped guard, Phase 60-03), logged to deferred-items.md. No deviations. GH-02 and TEST-31 satisfied; Phase 67 has one plan remaining.

Prior: 2026-06-02, 66-04 complete. Phase 66 EXECUTED (all 4 plans). docs/REMOTE.md authored (REMOTE-02, REMOTE-03): five sections matching the docs/SUPABASE.md voice. (1) serve --host: the 127.0.0.1:8765 loopback default is local-only; --host 0.0.0.0 exposes the dashboard on every interface with NO app-level auth; cross-links the Phase 62 key-write loopback guard while noting it is not a dashboard-wide auth layer. (2) Tailscale serve happy path: tailscale up -> horus-os serve (loopback) -> tailscale serve 8765 -> open https://your-machine.your-tailnet.ts.net from another tailnet device; the tailnet (WireGuard + ACLs) IS the auth boundary and serve injects identity headers (D-10). (3) A blockquote hard-do-not warning that tailscale funnel is the public internet with no authentication and no identity headers, banned until app auth ships (D-11). (4) Windows host management uses the built-in OpenSSH server, not Tailscale SSH (roadmap criterion 5), with the Add-WindowsCapability snippet; Linux/macOS point at the systemd/launchd service path. (5) Service install guide: horus-os service install + horus-os doctor --service, full lifecycle, the systemd loginctl enable-linger headless note (Pitfall 9), the launchd GUI-login caveat (Pitfall 8), the NSSM admin caveat + Task Scheduler no-admin fallback, and how to forward provider keys / HORUS_OS_DATA_DIR into the service env (Pitfall 10). Placeholder hostnames only (your-machine, your-tailnet, your-user, your-api-key); zero personal data; zero em-dashes (grep -nP verified empty). Activated tests/docs/test_remote_md.py: removed all four Wave 0 skip markers, implemented the four named docs grep tests plus a fifth no-em-dash guard (chr(8212) absent) enforcing CLAUDE.md rule 3 at the unit level, mirroring tests/docs/test_plugins_md_anatomy.py (module-scoped pathlib fixture). 5 passed zero skips; broader docs suite 23 passed; ruff clean on the touched test file. No deviations. REMOTE-02 and REMOTE-03 satisfied; Phase 66 ready for verification.

Prior: 2026-06-02, 66-03 complete. Cross-platform always-on service (REMOTE-04, TEST-30). New src/horus_os/service/ package: definitions.py holds three PURE string generators (generate_systemd_unit with Restart=on-failure + RestartSec=5 + Environment=HORUS_OS_DATA_DIR + WantedBy=default.target; generate_launchd_plist with sh.horus-os Label + RunAtLoad + KeepAlive; generate_nssm_commands with AppExit Default Restart + AppThrottle + AppRestartDelay) that import no subprocess and embed no personal paths (systemd %h specifier), so service install --print and TEST-30 share one OS-free code path (a test greps the module source to enforce purity). manager.py holds ALL OS mutation behind sys.platform dispatch (linux systemd --user / darwin launchctl / win32 nssm), writes definitions via pathlib to per-OS user paths, and a shutil.which('nssm') guard returns install guidance with a non-zero code instead of crashing (D-12, T-66-07); status() returns (running, report) and reports the systemd linger note on Linux (Pitfall 9). horus-os service {install,uninstall,start,stop,status} CLI (run_service) follows the agents_cmd dispatch contract; install honors --print by calling only the pure generator for the current platform (no subprocess); other ops delegate to the manager and surface guidance on failure. doctor --service queries manager.status(), prints the report, returns 0 only when registered+running, never tracebacks on a missing supervisor binary. Registered the service nested subparser (5 leaves) via _build_service_parser and added the doctor --service flag, mirroring the Wave 2 schedule registration. 6 plan tests pass (4 definition + 2 CLI, zero skips); broader suite 1368 passed excluding the pre-existing otel/release_gate failures. ruff clean on all touched files; no em-dash introduced. One auto-fixed deviation (Rule 1): reshaped the doctor usage text to preserve the existing 'horus-os doctor --supabase' substring contract while documenting --service. REMOTE-04 and TEST-30 satisfied.

Prior: 2026-06-02, 66-02 complete. SchedulerAdapter built on the supabase_adapter lifecycle with the disable gate inverted to opt-out: HORUS_OS_DISABLE_SCHEDULER=true makes start() a silent no-op so the local runtime starts cleanly with a passing health check (D-13). resolve_tz (HORUS_TZ override else OS-local stdlib zoneinfo, no network, D-05/D-06) and next_fire (max(croniter_next, base) coalesce, D-03) as pure helpers. _tick persists last_run_at/next_run_at BEFORE dispatch (double-fire guard), skips ids in _running_ids (overlap guard, T-66-03), and fires the profile + prompt via asyncio.to_thread(run_agent_loop) recording a normal trace with last_trace_id linkage (D-02). horus-os schedule {create,list,edit,delete,enable,disable} subcommand with to_cron desugar (small explicit _SUGAR table) + croniter.is_valid validation, cron the only stored form (D-01, T-66-02). scheduler entry-point registered under horus_os.adapters so discover_adapters auto-starts it on the FastAPI lifespan with no api.py edit. 7 plan tests pass (5 scheduler + 2 CLI, zero skips); full-suite failures confined to the pre-existing otel/release_gate set. ruff clean on all touched files. REMOTE-05 satisfied.

Prior: 2026-06-02, 66-01 complete. SCHEMA_VERSION bumped 11 to 12 with an additive, idempotent schedules table (cron_expression, agent_profile_name, prompt, enabled, catch_up_policy default 'coalesce', last_run_at/next_run_at/last_trace_id run-state). ScheduleRecord dataclass + _row_to_schedule helper + eight accessors (create/list/list_enabled/get/update/delete/set_enabled/update_run) with a static _SCHEDULE_UPDATABLE column allowlist (T-66-01). croniter>=6.1,<7 added as a base dependency. v11->v12 migration regression test (6 tests) proves additive + idempotent upgrade with row preservation. All 11 schema-version tripwire files bumped to 12; genuinely-intermediate force-backs (== 8/9/10) and the v5 fixture pin (== 5) left unchanged. 5 Wave 0 stub files created (test_scheduler, test_service_definitions, test_schedule_cli, test_service_cli, docs/test_remote_md) exposing the exact downstream target names. 82 passed / 17 skipped in the plan verification set, ruff clean on all touched files. REMOTE-05 and TEST-30 (foundation) satisfied.

Prior: 2026-06-01, 65-04 complete. SUPA-04 anon read path: frontend/lib/supabase.ts (isSupabaseConfigured + getSupabaseClient, @vite-ignore dynamic import, anon key only) + frontend/lib/supabase-reads.ts (tracesFromSupabaseOrLocal, Supabase anon PostgREST select when configured, api.traces fallback otherwise). 9 vitest tests (5 client + 4 reads) prove configured/unconfigured/error-fallback branches and no-service-key guarantee. npm run build clean. SUPA-04 satisfied. Phase 65 complete (SUPA-01 through SUPA-05, TEST-29 all satisfied).

Prior: 2026-06-01, 65-03 complete. Supabase Postgres migration (traces/agent_profiles/tasks/sync_health, ENABLE ROW LEVEL SECURITY + service_role_only deny-all + anon_select on every table, check_rls_status() RPC). TEST-29 secret-safety grep (process.env.SUPABASE_SERVICE_KEY pattern, not bare string) + SUPA-03 RLS-presence tests (7 tests). horus-os doctor --supabase CLI (lazy httpx, PostgREST RPC, per-table RLS=on/OFF report, no key echo, 10 mocked-httpx tests). docs/SUPABASE.md two-key model guide. 1350 tests pass, ruff clean. SUPA-02, SUPA-03, TEST-29 satisfied.

Prior: 2026-06-01, 65-02 complete. SupabaseAdapter (env-guarded, lazy httpx import, asyncio background sync loop, traces/agent_profiles/tasks only, _sync_headers factored as module-level helper). 12 mocked-httpx tests prove SUPA-05 silent no-op, SUPA-01 upsert headers (Authorization Bearer + apikey + Prefer: resolution=merge-duplicates), cursor advance, no-rows-no-post. 1351 tests pass, ruff clean. SUPA-01 and SUPA-05 runtime adapter requirements satisfied.

Prior: 2026-06-01, 65-01 complete. SCHEMA_VERSION 10->11, sync_cursors table + cursor accessor methods (get/upsert/list_rows_after with _SYNC_ALLOWLIST allowlist guard, T-65-01), v10->v11 migration regression test, all tripwire files updated atomically, [supabase] extra (httpx>=0.27, excluded from [all]) + entry point declared. 1339 tests pass, ruff clean. SUPA-01 (foundation) and SUPA-05 (packaging) requirements satisfied.

Prior: 2026-06-01, 64-03 complete. Discord INTEGRATION_REGISTRY entry expanded with HORUS_OS_DISCORD_GUILD_ID and HORUS_OS_DISCORD_ADMIN_ROLE_ID in required_vars; description updated to describe control-bot capability. Regression test added asserting all three required vars (count stays 10). DISCORD.md extended with v0.7 section: Message Content intent portal toggle, minimal-permission invite (309237738560, not Administrator), #horus thread-dispatch flow, slash commands, admin-role setup. 5 integrations tests + 18 docs tests pass, ruff clean. DISC-09 and DISC-10 satisfied. Phase 64 complete.

Prior: 2026-05-31, 63-05 complete. /tasks page (status filter tabs, inline cancel-confirm, retry/cancel actions, loading/error/empty states), /team/[slug] full-page agent detail route (AgentDetailView + server page wrapper), font-semibold -> font-bold on AgentCard name and AgentDetailPanel h2 (two-weight lock), data-tour-step="2" on team card grid. 17 vitest tests (10 tasks-page + 7 slug-page), npm run build clean. PAGES-04 frontend, PAGES-02, PAGES-01 polish satisfied.

Prior: 2026-05-31, 63-04 complete. ExampleDataBanner (accent-cyan left-border, inline clear/confirm, dismiss X), TourProvider (createContext + 5 TOUR_STEPS, SSR-guarded localStorage, auto-start, startTour/skipTour/nextStep/prevStep, useTour hook), TourOverlay (createPortal spotlight, getBoundingClientRect + 8px pad, 200ms transition, graceful no-target degradation, step card with role=dialog/aria-live/Escape handler). Barrel extended with 3 exports. layout.tsx wired with TourProvider + TourOverlay inside Providers. 13 vitest tests green, 49 total tests pass, npm run build clean. SEED-01, SEED-02, SEED-03 component half satisfied.

Prior: 2026-05-31, 63-02 complete. Three new FastAPI routes (GET /api/tasks, DELETE /api/tasks/{id}, DELETE /api/traces/{id}), case-insensitive /api/team/{slug} via load_profile_icase fallback, is_example flag on MemoryNoteDetail, five generic demo tasks seeded idempotently on init. TEAM-03 confirmed: parametrized test proves Coordinator delegates to Engineer, Researcher, Writer, and Operator with trace linkage. 56 tests pass, ruff clean. PAGES-04, SEED-01, SEED-02, PAGES-02, TEAM-03 satisfied.

Prior: 2026-05-31, 63-01 complete. Additive v8->v9 SQLite migration: tasks table with five-value status CHECK, TaskRecord dataclass, save_task/list_tasks/delete_task/delete_trace/load_profile_icase methods. SCHEMA_VERSION=9. Every schema-version tripwire across 9 test/doc files updated from 8 to 9. 289 tests pass, ruff clean. PAGES-04 (tasks data source), SEED-02 (delete_trace), PAGES-02 (load_profile_icase) persistence layer satisfied.

Prior: 2026-05-31, 62-03 complete. Credential management UI on /settings: masked 8-bullet display, password-input replace form (CredentialReplaceForm), CredentialRow with save/verify flows, RestartBanner (role=status), demo-mode absence enforced. api.saveCredential and api.verifyIntegration POST methods added. 34/34 vitest tests pass, npm run build clean. KEYS-01, KEYS-04, VERIFY-01 frontend surface satisfied. Phase 62 fully complete.

Prior: 2026-05-31, 62-02 complete. POST /api/integrations/{name}/keys and POST /verify endpoints with loopback guard, demo-mode refusal, chmod 600, no-echo, key-hash invalidation. compute_status extended to return "verified" when SQLite row hash matches current env value. starlette>=0.46.0 pinned as direct dependency (REMOTE-01). Six-test TEST-27 regression suite green; Phase 61 tests unbroken. KEYS-02, KEYS-03, VERIFY-01, VERIFY-03, REMOTE-01, TEST-27 satisfied.

Prior: 2026-05-31, 61-03 complete. SETUP-05 fulfilled: get-started page extended with Step 4 "Connect your integrations" linking to /integrations/ (one click to Integrations surface). PLACEHOLDER comment removed from Step 2. font-medium in ExternalLink and font-semibold in closing CTA normalized to font-bold (two-weight lock complete). npm run build clean. Phase 61 fully complete (SETUP-01..06, VERIFY-02, VERIFY-04, GH-01 all satisfied).

Prior: 2026-05-31, 61-02 complete. 10-card Integrations page with IntegrationCard (StatusDot, keyboard activation), IntegrationWalkthrough (Modal+Stepper, demo mode read-only with DemoModeNotice), ReadinessSummary (N of 10 ready), data layer (types, query-keys, api.integrations(), useIntegrations hook, fixture). EmptyState font-medium normalized to font-bold. 24/24 vitest tests pass, npm run build clean. SETUP-01, SETUP-03, SETUP-04, SETUP-06, VERIFY-02, VERIFY-04, GH-01 (surface) satisfied.

Prior: 2026-05-31, 61-01 complete. INTEGRATION_REGISTRY (10 entries: anthropic, gemini, discord, slack, email, calendar, github, supabase, vercel, tailscale) + compute_status helper + GET /api/integrations route + IntegrationStatus/IntegrationsResponse Pydantic models + 3 regression tests (no-secret-echo, count, Phase 61 status constraint). SETUP-02 and GH-01 (surface half) satisfied.

Prior: 2026-05-31, 60-04 complete. Stepper controlled wizard primitive built via TDD (5 jsdom tests). CopyButton and CommandBlock extracted from get-started/page.tsx into standalone components. All three exported from components/index.ts. get-started/page.tsx rewired to import from @/components. 17 total vitest tests pass; npm run build clean. DESIGN-03 and DESIGN-05 (complete) satisfied. Phase 60 fully complete.

Prior: 2026-05-31, 60-03 complete. Sidebar extended to locked ten-item nav (Home, Team, Memory, Tasks, Activity, Traces, Costs, Integrations, Settings, About) with usePathname active-route highlight, font-bold labels, text-xs footer. AppShell DemoBanner link changed to font-bold. /tasks and /integrations stub pages created (PageHeader + EmptyState with locked copy, build to out/tasks/ and out/integrations/). CI em-dash guard and reserved-name guard added to ci.yml (ubuntu/3.11 only, git-diff scoped). Self-test fixtures in tests/ci_guards/. Sidebar.test.tsx proves 10-item nav and active-route logic (3/3 pass). DESIGN-04, DESIGN-01 (shell), DESIGN-05 (partial), D-08 guardrails satisfied.

Prior: 2026-05-31, 60-02 complete. Modal (Radix Dialog wrapper with portal, focus trap, Escape-to-close) and StatusDot (four-state pulsing indicator with two-layer pulse animation) built via TDD. 9 jsdom tests pass, npm run build clean. DESIGN-02 and DESIGN-05 (partial) satisfied.

Prior: 2026-05-31, 60-01 complete. radix-ui@1.4.3 installed, vitest/jsdom stack installed, globals.css declared single canonical token source, hardcoded hex colors migrated to CSS variables in costs/page.tsx, font-weight two-weight lock enforced in costs/page.tsx and DashboardHome.tsx. DESIGN-01 substrate landed.

Prior: 2026-05-31, v0.7 roadmap created. 9 phases (60-68), 63 v0.7 requirements mapped 1:1 with zero orphans. Execution order 60 -> (61 || 63) -> 62 -> (64 || 65 || 66) -> 67 -> 68. Phase 60 lands first as a universal dependency (Tailwind v4 tokens, Radix Modal/Stepper, AppShell sidebar - all required by every page and the Setup-and-Verify UX). Phase 61 and Phase 63 run concurrently after Phase 60. Phase 62 (key-write + security substrate) is a security-focused gate isolating the highest-risk backend code (starlette CVE pin, loopback guard, chmod 600, no-echo response, demo-mode 403, key-hash change detection, MIG-06 schema migration). Phases 64 (Discord), 65 (Supabase), 66 (cron/service) run in parallel after Phase 62. Phase 67 (Vercel + GitHub tool + NEXT_PUBLIC_API_BASE) depends on Phase 65 as data source and completes the static-export/remote-deploy abstraction. Phase 68 is the three-OS hard gate and v0.7.0 release behind REL-16 and REL-17.

Prior: 2026-05-29, v0.6 roadmap landed. 9 phases (51-59), 46 v0.6 requirements mapped 1:1 with zero orphans.
